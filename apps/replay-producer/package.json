{
  "name": "@legendary-arena/replay-producer",
  "version": "0.1.0",
  "private": true,
  "description": "CLI Producer App (D-6301) that wraps buildSnapshotSequence with file I/O to emit deterministic ReplaySnapshotSequence JSON",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "node --import tsx --test src/**/*.test.ts",
    "produce-replay": "node --import tsx src/cli.ts"
  },
  "dependencies": {
    "@legendary-arena/game-engine": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.19.17",
    "tsx": "^4.15.7",
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=22"
  }
}
