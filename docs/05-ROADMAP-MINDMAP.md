# Legendary Arena -- Development Roadmap (Mindmap)

> **Checklist rule (hard):** one line per item; status-first; no subordinate clauses; no file lists / commit hashes / decisions / dependency prose. If the line forces the reader to *read* before answering "done / drafted / blocked", it's still wrong.
>
> **Status vocabulary (closed set):**
> `✅ Done` · `🚧 In Progress` · `📝 Drafted` (WP file authored; awaiting execution) · `📦 Queued` (deps met; WP file not yet authored) · `⏸ Blocked` (dep unmet) · `📝 Placeholder` (forward-looking only).
>
> All audit detail (per-WP file lists, commit hashes, decision IDs, baselines, deltas, post-mortems) lives in `docs/ai/work-packets/WORK_INDEX.md`, the per-WP files under `docs/ai/work-packets/`, and `docs/ai/STATUS.md`. This file is navigation — not a record.

```mermaid
mindmap
  root((Legendary Arena))
    ["Multiplayer Deck-Builder\nboardgame.io + TypeScript + R2"]

      Foundation
        ["FP-00.4 ✅ Environment check"]
        ["FP-00.5 ✅ R2 validation"]
        ["FP-01 ✅ Backend hosting"]
        ["FP-02 ✅ Database migrations"]

      Phase 0 — Coordination
        ["WP-001 ✅ Coordination system"]
        ["WP-002 ✅ Game skeleton"]
        ["WP-003 ✅ Card registry"]
        ["WP-004 ✅ Server bootstrap"]
        ["WP-043..047 ✅ Governance alignment"]

      Phase 1 — Game Setup
        ["WP-005A/B ✅ Deterministic match setup"]
        ["WP-006A/B ✅ Player zones and piles"]

      Phase 2 — Core Turn Engine
        ["WP-007A/B ✅ Turn structure and loop"]
        ["WP-008A ✅ Core move contracts"]
        ["WP-008B ✅ Core move implementation"]
        ["WP-236 ✅ Engine-authoritative start-of-turn draw (auto-draw + once-per-turn guard)"]

      Phase 3 — MVP Multiplayer
        ["WP-009A/B ✅ Rule hooks and execution"]
        ["WP-010 ✅ Victory and loss"]
        ["WP-011 ✅ Lobby flow"]
        ["WP-012 ✅ Match list and join"]
        ["WP-013 ✅ Persistence boundaries"]

      Phase 4 — Core Gameplay Loop
        ["WP-014A/B ✅ Villain deck and reveal"]
        ["WP-015 ✅ City and HQ zones"]
        ["WP-015A ✅ Reveal safety fixes (stage gate + no-card-drop)"]
        ["WP-016 ✅ Fight and recruit"]
        ["WP-017 ✅ KO, wounds, bystanders"]
        ["WP-018 ✅ Attack / recruit economy"]
        ["WP-019 ✅ Mastermind and tactics"]
        ["WP-020 ✅ VP scoring and summary"]

      Phase 5 — Card Mechanics
        ["WP-021 ✅ Hero hooks"]
        ["WP-022 ✅ Hero keywords"]
        ["WP-023 ✅ Conditional effects"]
        ["WP-024 ✅ Scheme / mastermind execution"]
        ["WP-025 ✅ Board keywords"]
        ["WP-026 ✅ Scheme setup"]
        ["WP-182 ✅ Scheme twist resolver framework (engine)"]

      Phase 6 — Verification & Production
        ["✅ Phase-6 closed (tag phase-6-complete)"]
        ["WP-027 ✅ Replay harness"]
        ["WP-028 ✅ UIState contract"]
        ["WP-029 ✅ Spectator permissions"]
        ["WP-030 ✅ Campaign framework"]
        ["WP-031 ✅ Production hardening"]
        ["WP-032 ✅ Network sync"]
        ["WP-033 ✅ Content authoring toolkit"]
        ["WP-034 ✅ Versioning and save migration"]
        ["WP-035 ✅ Release and ops playbook"]
        ["WP-042 ✅ Deployment checklists"]
        ["WP-066 ✅ Registry image/data toggle"]
        ["WP-067 ✅ UIState PAR projection"]
        ["WP-079 ✅ Determinism-only replay labeling"]
        ["WP-080 ✅ Step-level replay API"]
        ["WP-048..051 ✅ PAR pipeline (see Scoring & PAR)"]

      UI Implementation Chain
        ["WP-065 ✅ Vue SFC test transform"]
        ["WP-061 ✅ Gameplay client bootstrap"]
        ["WP-062 ✅ Arena HUD and scoreboard"]
        ["WP-063 ✅ Replay snapshot producer"]
        ["WP-064 ✅ Game log and replay inspector"]

      Content Layer
        ["WP-055 ✅ Theme data model"]
        ["WP-060 ✅ Glossary R2 migration"]
        ["WP-221 ✅ Theme supplemental setup fields + tips display"]

      Pre-Planning System
        ["WP-056 ✅ State model and lifecycle"]
        ["WP-057 ✅ Sandbox execution"]
        ["WP-058 ✅ Disruption pipeline"]
        ["WP-059 ✅ UI integration"]
        ["WP-070 ✅ Done — live mutation middleware"]

      Post-Phase-6 Hygiene
        ["WP-081 ✅ Registry build pipeline cleanup"]
        ["WP-082 ✅ Glossary schema and labels"]
        ["WP-083 ✅ Fetch-time schema validation"]
        ["WP-084 ✅ Auxiliary metadata deletion"]
        ["WP-085 ✅ Vision alignment audit"]

      Phase 7 — Beta, Launch & PAR
        ["WP-036 ✅ AI playtesting and balance simulation"]
        ["WP-037 ✅ Public beta strategy"]
        ["WP-038 ✅ Launch readiness checklist"]
        ["WP-039 ✅ Live ops framework"]
        ["WP-040 ✅ Change governance and budget"]
        ["WP-041 ✅ System architecture definition"]

      Scoring & PAR Pipeline
        ["WP-048 ✅ PAR scenario scoring and leaderboards"]
        ["WP-049 ✅ PAR simulation engine"]
        ["WP-050 ✅ PAR artifact storage and indexing"]
        ["WP-051 ✅ PAR publication and server gate"]
        ["WP-365 ✅ final-score VP by printed card VP; flat table demoted to fallback (live capture matchId sGTM7LWSIHy 2026-07-12 — victory pile Super-Skrull vp2 + Skrull-Shapeshifters vp2 + Juggernaut vp4 = printed 8 reported as villainVP 3, total 45 vs correct 50; root cause: computeFinalScores WP-020 uses flat VP_VILLAIN=VP_HENCHMAN=1/VP_TACTIC=5 and never reads printed vp — G.cardStats carries attack/recruit/cost/fightCost but NOT vp, registry VillainCardSchema.vp/MastermindSchema.vp never plumbed into G; also feeds parScoring.logic → can crown the wrong multiplayer winner; adds immutable setup snapshot G.cardVictoryPoints sibling to cardStats built in buildInitialGameState via total/defensive normalizePrintedVictoryPoints nullable string|number null/NaN/non-int→omit, computeFinalScores reads G.cardVictoryPoints[cardId] ?? VP_<category> per villain/henchman + tacticVP = tacticsDefeated × (mastermind vp ?? VP_TACTIC), flat constants DEMOTED to fallbacks values unchanged so null-vp cards score sanely; bystander stays 1 wound stays −1, PlayerScoreBreakdown shape/field-names unchanged value-only no consumer break; determinism conditional-spread/omit-when-empty per WP-290 → EMPTY_REGISTRY fixtures byte-identical sentinel re-pin execution-measured expected none; out of scope: no retro-rescore of historical DB rows, no PAR-weight change, no registry-schema change; standard two-session lane §6 scoring/competitive exclusion; executed 2026-07-12 via a dedicated setup/buildCardVictoryPoints.ts (economy/mastermind/CardStatEntry byte-unchanged — concurrency-safe amendment of the inline draft); engine 1893/441/0, sim:coverage sentinel unchanged; EC-392; D-24157 Active)"]
        ["WP-367 ✅ Deck-exhaustion final turn → tie (engine; implements the rulebook end condition the engine previously ignored — the moment the Hero Deck or Villain Deck runs out, a final turn is declared and the game ends in a TIE rather than continuing. Adds finalTurn.logic + an endgame condition + a UIState projection so the client can warn; villainDeck.reveal reworked. Reserves D-24159/60/61; EC-397)"]
        ["WP-368 📝 Final-turn warning banner (arena client; the client follow-on to WP-367 — surfaces the declared final turn in the match UI so a player is told the shared deck ran out and this is the last turn, rather than discovering it at the tie. Reads the UIState projection WP-367 added; no engine change. Drafted alongside WP-367, not yet executed)"]

      Beta-Launch Pillar
        ["WP-052 ✅ Player identity and replay ownership"]
        ["WP-053a ✅ PAR artifact carries scoring config"]
        ["WP-053 ✅ Competitive score submission"]
        ["WP-054 ✅ Public leaderboards library"]
        ["WP-103 ✅ Replay storage and loader"]

      Engine Hardening
        ["WP-087 ✅ Engine type hardening"]
        ["WP-088 ✅ Setup module hardening"]

      Client Integration Cluster
        ["WP-089 ✅ Engine PlayerView wiring"]
        ["WP-090 ✅ Live match client wiring"]
        ["WP-091 ✅ Loadout builder"]
        ["WP-092 ✅ Lobby loadout intake"]
        ["WP-093 ✅ Match-setup rule-mode envelope"]
        ["WP-254 ✅ Lobby qualified-form ext_id guard (parseLoadoutJson rejects bare-slug/flat-key ids in the lobby instead of a Game.setup() 500; tenth code unqualified_ext_id; grammar-only mirror of parseQualifiedId, re-derived/layer-boundary-safe; D-24025)"]
        ["WP-094 ✅ Viewer hero key uniqueness"]
        ["WP-100 ✅ Interactive gameplay surface"]
        ["WP-163 ✅ Autoplay playback controls (server: pause/step/rewind endpoints)"]
        ["WP-165 ✅ Autoplay status endpoint (server: GET .../status read-only probe)"]
        ["WP-177 ✅ Autoplay rewind requester audience (server: D-17701 scopes D-16303)"]
        ["WP-164 ✅ Autoplay playback controls (client: media-player bar + status probe gating)"]
        ["WP-261 ✅ Autoplay bot-loop crash surfacing + defensive stage progress (server; EC-292): markAborted(reason) + abort-on-abnormal-exit keeps the controller registered for the 5-min review window + surfaces aborted/abortReason on the playback envelope; per-stage _stateID progress assertion (a stalled stage aborts instead of spinning to maxTurns); routes ALL stages through getLegalMoves so a parked KO-hero resolve fires anywhere; pure botLoopProgress.mjs helper; engine untouched; D-24037/D-24038 Active; WP-262 client banner fast-follow)"]
        ["WP-262 ✅ Autoplay 'Bot match stopped' banner + stall-detection poll (client; EC-296): consumes WP-261's aborted/abortReason envelope — mirrors both onto the client AutoplayControlResponse (no server import) + a bounded abort-state-only stall poll in AutoplayControls.vue (STALL_POLL_INTERVAL_MS=3000, single in-flight, stops on abort/stopped/unmount, never moves cursor/mode/history/paused) + a pure interpretStallProbe helper + a data-testid=autoplay-aborted banner extending the expired span + live-control disable on abort (rewind kept for the review window) + initial-aborted seed with no poll start; no server/engine/api-endpoints diff; D-24042 Active; D-24026 live-verify pending post-deploy)"]
        ["WP-166 ✅ arena-client vue-tsc green + CI typecheck gate (engine barrel publishes the 6 WP-128 UIState sub-types; D-16502)"]
        ["WP-227 ✅ arena-client vue-tsc green (WP-214/222 UIState/UICityCard fixture + prop backfill; 3rd recurrence of engine-field-add → client-typecheck drift after WP-166/207)"]
        ["WP-171 ✅ Pile browse modal (click-to-view card piles)"]
        ["WP-178 ✅ Card image rendering on play surface (CardTile component)"]
        ["WP-179 ✅ Card traits + superpower condition evaluation"]
        ["WP-228 ✅ Arena-client diagnostic capture + export (shareable freeze log)"]
        ["WP-246 ✅ Arena-client diagnostic UIState snapshot (richer freeze report)"]

      Auth Stack & Profile Surface
        ["WP-099 ✅ Auth provider selection (Hanko)"]
        ["WP-101 ✅ Handle claim flow"]
        ["WP-102 ✅ Public player profile page"]
        ["WP-104 ✅ Owner profile and /me edit"]
        ["WP-109 ✅ Team affiliation"]
        ["WP-111 ✅ UIState card display projection"]
        ["WP-112 ✅ Session token validation middleware"]
        ["WP-126 ✅ Hanko session verifier"]
        ["WP-131 ✅ Authenticated route production wiring"]
        ["WP-160 ✅ Hanko client UI (production sign-in surface)"]
        ["WP-161 ✅ Arena client API base URL surfacing (VITE_API_BASE_URL)"]
        ["WP-174 ✅ First-sign-in auto-provisioning (read-or-create account resolver)"]
        ["WP-175 ✅ Auth-aware navigation surface"]
        ["WP-192 ✅ Hanko JWKS refresh-interval parse guard"]
        ["WP-293 ✅ Game-signup → Brevo marketing list (server fire-and-forget, fail-open after WP-174 provisioning; D-24077..D-24080)"]

      Engine + Server Wiring & Leaderboard HTTP
        ["WP-113 ✅ Engine-server registry wiring"]
        ["WP-114 ✅ Viewer URL-parameterized setup preview"]
        ["WP-115 ✅ Public leaderboard HTTP and pg.Pool bootstrap"]

      Registry Viewer Enhancements
        ["WP-121 ✅ Card zoom slider"]
        ["WP-122 ✅ Henchman emission fix"]
        ["WP-123 ✅ cardType widening and other dispatch"]
        ["WP-124 ✅ Theme zoom slider"]
        ["WP-125 ✅ Card abilities effect-tag filter"]
        ["WP-086 ✅ Card-types upgrade"]
        ["WP-096 ✅ Grid data view mode"]
        ["WP-127 ✅ Grid tile team and ability text"]
        ["WP-170 ✅ Card count display"]
        ["WP-183 ✅ Scheme twist pattern taxonomy"]
        ["WP-184 ✅ Card mechanical pattern taxonomies"]
        ["WP-208 ✅ devLog category union extension (cardPatterns + schemeTwist)"]
        ["WP-213 ✅ devLog category single-source LOG_CATEGORIES array"]
        ["WP-245 ✅ LAGN export in registry viewer loadout tab"]
        ["WP-269 ✅ Hero mechanic metadata feed (producer half of the mechanic-query slice; deterministic transform turns the committed hero ledger into a normalized viewer-safe data/metadata/card-mechanics.json + data-only CardMechanicsIndexSchema + CI freshness gate; hidden fail-closed; D-24046)"]
        ["WP-270 ✅ Registry-viewer hero mechanic filter surface (consumer half; cardMechanicsClient + MechanicFilter ribbon over WP-269's feed, hidden!==true, OR-within / AND-across the text query; consumes D-24046)"]
        ["WP-271 ✅ Villain & henchman mechanic ledger (data-production half of beyond-heroes; new scripts/villain-mechanic-ledger.mjs mirrors the hero ledger, classifies [effect:X] by-hook via the dist's buildVillainAbilityHooks — resolved→executable / unresolvedMarkers→unsupported / no-marker→unmarked; ledger:villains:check CI gate; mastermind/scheme deferred — no ability-hook parser exists, named follow-ups; data-production only, feed-widening + dashboard view are consumption follow-ups per the D-24046 split; Shared Tooling, no engine/registry/app/data change, hero instruments byte-unchanged; User-Visible Surface none-infrastructure; D-24048)"]
        ["WP-484 ✅ Effect Implementation Index (first slice of the ewiki debug-effects.md direction; deterministic transform JOINS the committed hero + villain mechanic ledgers into one dual-scope schema-validated data/metadata/effect-implementation-index.json carrying per card×mechanic scope/status/handler/wp/decision + cards join + summary; handler/wp/decision verbatim from BOTH ledgers [populated for resolved rows both scopes, empty on unsupported/unmarked, never fabricated]; new data-only EffectImplementationIndexSchema on the /schema subpath + effect-index:check CI freshness gate, mirroring WP-269; NO engine touch / NO /debug/effects viewer / NO runtime tracing — future WPs; drafted 2026-08-01; EC-519; User-Visible Surface none-infrastructure; D-24289)"]
        ["WP-276 ✅ Registry-viewer mechanic filter searchable dropdown (reworks WP-270's chip ribbon into a searchable multi-select toggle + position:fixed popover listing all 134 mechanics, escaping the drawer's overflow:hidden clip; supersedes WP-270 AC-7 curated-visible ribbon as a consumer presentation choice — feed + v-model contract unchanged, filter stays OR-within / AND-across; D-24052)"]
        ["WP-277 ✅ Mechanic dropdown scroll fix (regression in WP-276 — the capture-phase window scroll listener that closes the position:fixed popover also caught the popover list's own scroll → instantly closed the dropdown, making the list look unscrollable; onViewportChange now ignores scrolls whose target is inside the popover root, still closes on outside page/drawer scroll + resize; bugfix, no DECISIONS change)"]
        ["WP-278 ✅ Registry-viewer search header redesign — unified FilterDropdown (new shared FilterDropdown.vue standard control rebuilds Set·Class·Type·Mechanics·Effects on one row + a contextual Patterns dropdown, collapsing the scheme-twist + mechanical-pattern ribbons and deleting AbilityEffectFilter/SchemeTwistFilter/PatternFilter/MechanicFilter; filtering logic unchanged — OR-within / AND-across, D-24046 + D-24052 preserved; D-24053)"]
        ["WP-279 ✅ Cards tab add-to-loadout (lifts useLoadoutDraft from LoadoutBuilder.vue to App.vue as ONE shared instance — lifted not singletonized, instantiated post-registry-load mirroring useSetupFromUrl; CardDetail.vue gains a contextual add/remove button for the 5 composition types only, hero→addHeroGroup(card.extId)/scheme/mastermind set-clear/villain/henchman add-remove, Always-Leads groups not removable; new floating LoadoutTray.vue pill; pure loadoutCardActions.ts helper carries the tested cardType→slot invariant; no engine/contract/MatchSetupConfig change, consumes existing UseLoadoutDraftApi + FlatCard.extId D-24018; D-24054)"]
        ["WP-288 ✅ Cards tab 'View loadout as cards' gallery (reverse of WP-279 — render the loaded loadout/LAGN as a gallery via a filter MODE over the Cards tab, not a dropdown/new tab/second grid; loadoutGalleryActive state + a final inert-when-off narrowing stage in applyFilters mirroring the WP-270 mechanic stage, navigateToLoadoutGallery mirroring navigateToCard, dismissible inline banner; '🖼 View as cards' on LoadoutBuilder (disabled when empty) + a secondary tray action, both emitting view-as-cards; pure loadoutGalleryCards.ts helper — compositionExtIdSet skips empty single slots + for...of, isCardInLoadoutComposition by card.extId D-24018 — carries the composition→member-card expansion as the unit-tested invariant; no engine/registry/server/contract/loader/card-data change, CardGrid untouched; D-24072)"]
        ["WP-291 ✅ Loadout tab 'Load LAGN' import (closes the LAGN export/import round-trip surfaced after WP-288 — the tab could Download LAGN but the only importer 'Load JSON' validates the MATCH-SETUP schema so a LAGN file lagn_version/setup was rejected; adds a SEPARATE 'Load LAGN (paste or file)' control beside Load JSON, operator chose two explicit controls over auto-detect; pure loadoutLagnImport.ts parseLagnLoadout → the published @legendary-arena/lagn validate → reverse WP-245 compositionToLagnSetup into the 5 composition ext_id fields + 4 counts + playerCount, shield_officers_count→officersCount the only rename, ids already set-qualified D-24018 no registry lookup; applyLagnImport REPLACES the draft via the existing setters resetDraft→set*/add*/setCount/setPlayerCount, a non-LAGN file shows validator errors and preserves the draft; no composable/contract/loadFromJson/App.vue/gallery/CardGrid change; Lightweight Lane D-24028; D-24075)"]
        ["WP-361 📝 Current-match loadout as LAGN — GET /api/match/:matchId/lagn (server; read-only Tier-1 LAGN endpoint + blob-read carve-out extension so a live game's loadout is fetchable as a LAGN document; drafted 2026-07-11 via PR #695; EC-391 at execution-prep; D-24153)"]
        ["WP-362 📝 Loadout tab: open a LAGN from the URL ?lagn= (registry viewer; deep-link ingest into the Loadout tab so a shared ?lagn= URL renders that loadout; drafted 2026-07-11 via PR #695; EC-392 at execution-prep; D-24154)"]
        ["WP-363 📝 In-match 'View loadout in Registry Viewer' link (arena client; a link from a live match to the WP-362 ?lagn= deep-link; ⛔ BLOCKED on WP-361 + WP-362; drafted 2026-07-11 via PR #695; EC-393 at execution-prep; D-24155)"]

      Phase 8 — Interactive Board Layout
        ["WP-128 ✅ UIState board projections"]
        ["WP-129 ✅ Board layout (desktop/mobile)"]
        ["WP-130 ✅ Playmat / reskin selector"]

      G-State Extensions
        ["WP-153 ✅ Destination piles (strike, twist, escaped)"]
        ["WP-154 ✅ Mastermind attached bystanders"]
        ["WP-155 ✅ Turn economy extensions (piercing, wounds drawn)"]
        ["WP-156 ✅ Horrors pile"]

      Monetization Stack
        ["WP-132 ✅ Entitlements data model and read endpoint"]
        ["WP-133 ✅ Stripe checkout and webhook ingestion"]
        ["WP-134 ✅ Webhook to entitlement fulfillment (closed-loop LIVE for cosmetic SKUs)"]

      Engine & Test-Harness Cleanup
        ["WP-135 ✅ HQ population and hero deck reservoir"]
        ["WP-136 ✅ JSDOM opaque-origin storage fix"]
        ["WP-137 ✅ Hero card-instance distinctness + data-driven cardCounts"]
        ["WP-191 ✅ Card ext_id grammar reconciliation (zone instance IDs)"]
        ["WP-294 ✅ Separate the message log from the finalStateHash oracle (hashGameState excludes G.messages; notableEvents stays hashed; D-24081)"]

      Physical Card Pipeline
        ["WP-138 ✅ Physical card abstraction layer"]
        ["WP-140 ✅ Physical card phase 1b"]
        ["WP-141 ✅ Physical card phase 2"]
        ["WP-147 ✅ PhysicalCard companionSlug + physical-side order"]
        ["WP-151 ✅ Physical card phase 3 (imageUrl removal)"]

      Domain Cutover & Infrastructure
        ["WP-139 ✅ Engineering wiki viewer"]
        ["WP-144 ✅ Arena-client production bundle isolation"]
        ["WP-145 ✅ Architecture inventory ↔ wiki integration"]
        ["WP-146 ✅ cards.legendary-arena.com cutover prep"]
        ["WP-148 ✅ legendary-arena.com + www cutover prep"]
        ["WP-240 ✅ Roadmap count-table generator (WORK_INDEX × mindmap; cron auto-PR)"]
        ["WP-244 ✅ LAGN spec publication (npm package + GitHub repo + schema hosting)"]
        ["WP-392 ✅ Derive the published LAGN JSON Schema from the zod schema (contract; src/validator.ts maintained the format TWICE — lagnSchema (zod, labelled Single Source of Truth) and a hand-written generateSchema() literal that was not derived from it. Closes the hazard D-24193 recorded as known-and-unfixed; lagn-spec suite 34 → 44/0; generate:schema then git diff --exit-code on schemas/ is clean. D-24196 Active; done 2026-07-18)"]
        ["WP-393 ✅ Registry version + per-set content hash surface (registry; CardRegistry cannot say which snapshot of the card data it loaded — RegistryInfo carries no version, SetIndexEntry no hash, and a grep for registryVersion/content_hash/sha256 over packages/registry/src returns nothing. That makes the load-bearing half of WP-394 provenance unanswerable. Reserves D-24197; EC-423; drafted 2026-07-18)"]
        ["WP-394 ✅ LAGN 1.2.0 — card metadata provenance (contract; BLOCKED on WP-393. Lets a LAGN answer which card effect a replay references and whether it verifies without the registry, via optional hash-anchored provenance: catalog_ref pins the producer load scope, registry_ref uses stable ext_id + face_id rather than JSON pointers (which break on array reorder), effect_snapshot is frozen evidence and explicitly not authoritative. Registry stays authoritative. Reserves D-24198; EC-424; drafted 2026-07-18)"]

      Public Leaderboard (Marketing)
        ["WP-149 ✅ Public leaderboard Hugo page"]
        ["WP-150 ✅ Leaderboard theme + global aggregation endpoints"]

      Legends Public Scoreboard
        ["WP-142 ✅ Legends snapshot publisher"]
        ["WP-143 ✅ Legends attract board (public scoreboard SPA)"]

      Villain Deck Pipeline
        ["WP-167 ✅ Villain deck composition data (registry)"]
        ["WP-168 ✅ Villain deck composition logic (engine)"]
        ["WP-169 ✅ Scheme villain-deck count curation"]
        ["WP-172 ✅ Villain-deck display data coverage"]
        ["WP-173 ✅ Well-known ext_id display data"]

      Villain & Henchman Effects
        ["WP-185 ✅ Fight + ambush effects (engine)"]
        ["WP-186 ✅ Escape + overrun effects (engine)"]
        ["WP-187 ✅ Effect-marker enrichment (card data)"]
        ["WP-188 ✅ Escape/overrun effect-marker enrichment (card data)"]
        ["WP-189 ✅ koHeroEachPlayer vocabulary expansion (engine)"]
        ["WP-190 ✅ Each-player-KO effect-marker curation (card data)"]
        ["WP-202 ✅ Magnitude-N each-player-KO (engine + data)"]
        ["WP-212 ✅ Once-per-turn villain reveal guard (engine)"]
        ["WP-214 ✅ Villain hero capture + dynamic attack resolution (engine + data)"]
        ["WP-242 ✅ Villain Fight KO-Hero player choice (engine: park → resolve, bot auto-resolve)"]
        ["WP-386 ✅ Red Skull Master Strike — 'Each player KOs a Hero from their hand' (engine; the WP-024 per-mastermind strike dispatcher implemented only Magneto, so Red Skull's printed strike silently no-op'd — surfaced by the 2026-07-16 Red Skull live-game review, 3 strikes / 6 skipped KOs; adds a Red Skull id set core/red-skull + co2e/red-skull (identical base-face text; epic face NOT matched) + resolveRedSkullStrike on the Magneto pattern — sorted players, KO the lowest-cost Hero (cardStats cost ?? 0, tie → lowest hand index) from hand to G.ko, Wounds excluded, empty/all-Wound hand → logged no-op, one pushLog line per player; D-24188 auto-KO ≈ player-optimal pick, avoids a blocking multi-player pending-choice; generic strike behavior D-15401 capture + counter + WP-200 emission byte-unchanged; NO sentinel re-pin — recorded fixture + runtime-observed matrix are core/dr-doom; engine 1981→1991/0; EC-415)"]
        ["WP-389 ✅ Mastermind base-face selection — stop silently selecting Epic faces (engine; the setup card loop assigned baseCard on every non-tactic face with no early exit, so the LAST won — 65 masterminds across 24 sets played their Epic variant unchosen, e.g. co2e Doom at attack 12+ instead of 10+. core was unaffected (single non-tactic face), which is why no oracle caught it. Fix is one assignment guard so the FIRST non-tactic face wins, keeping tactic as the D-1413 discriminator; Epic becomes unreachable until an explicit opt-in exists. Unblocks WP-388. Reserves D-24193; executed 2026-07-18)"]
        ["WP-388 ✅ co2e mastermind strike texts — Doom / Loki / Magneto / Doctor Octopus (engine; four of five co2e base faces still no-op their printed Master Strike beyond the generic bookkeeping. Adds a per-mastermind resolver for each on the WP-386 pattern, resolving every printed or/may clause by deterministic auto-pick — no new G field, no RNG, no pending-choice. Doom derives its Omen count from masterStrikeCount+1 rather than storing a stack. Loki's Hypno-Thrall and Doc Ock's reveal-8 branches are deliberately deferred as a recorded fidelity gap. Reserves D-24192; executed 2026-07-18)"]
        ["WP-397 ✅ Doctor Octopus reveal-eight branch (engine; WP-388 implemented only the first branch of the printed strike, so a player holding no Spider-Friends Hero escaped it entirely. Reveals the top 8, discards every non-grey Hero revealed, returns the rest shuffled. D-24192 deferred this claiming it needed ctx.random threading — WRONG: the reveal pipeline already passes the full RevealContext with random.Shuffle, the handler just ignored _ctx. Non-grey is rulebook-defined = no Hero Class, so Wounds are grey and go back. Reserves D-24200; executed 2026-07-19)"]
        ["WP-398 📝 Loki Hypno-Thrall zone (engine; adds MastermindState.hypnoThralls, append-only, mirroring the D-15401 attachedBystanders precedent. A new G field is unavoidable here where Doom Omens needed none: Omens are counted, Thralls are specific cards that must be identified durably. A probe measured the cost — one new MastermindState field = 3 engine-suite failures incl PRE_WP080_HASH — so both hash oracles are re-pinned in the impl commit, the sentinel regenerated by script and never hand-edited. Ships infrastructure-only; not visible until WP-399. Reserves D-24201; drafted 2026-07-19)"]
        ["WP-399 📝 Hypno-Thralls UIState projection + client display (engine projection + arena-client; BLOCKED on WP-398. Projects the zone through the SAME display-entry resolution as attachedBystanders so an unresolvable id degrades identically instead of rendering a placeholder, and renders the group on the mastermind tile. Required rather than polish — without it a Loki strike shrinks the hand while the card vanishes from view. arena-client vue-tsc is the load-bearing gate for UIState required-field adds; fixtures backfilled in the same change. Completes the D-24192 gap. Reserves D-24202; drafted 2026-07-19)"]
        ["WP-390 ✅ Council masterminds fail setup loudly instead of resolving to an inert shell (engine; four masterminds ship ZERO non-tactic faces — both S.H.I.E.L.D. ones plus 2099/sinister-six-2099 and 2099/alchemax-executives — so findMastermindCards returned null and buildMastermindState fell through to a degenerate empty state: no Master Strike, no tactics, no game text. Every shld gauntlet was played against an inert obstacle and nothing failed. Game.setup() now THROWS for the found-but-zero-base-face case only; not-found / unparseable-id / narrow-test-mock keep the fallback, pinned by a test. Councils are four members each with their own Master Strike + [rule:Adapt], a shape the one-base-face model cannot express — modelling that is still deferred; this only makes the gap unshippable. Supersedes WP-389 AC-4. Reserves D-24206; executed 2026-07-19)"]
        ["WP-391 📝 Support card pools — name the cards behind the four supply piles (registry + LAGN + viewer; MatchSetupConfig carries bystanders/wounds/officers/sidekicks as bare counts, so nothing records WHICH cards fill a pile. Blocks a frozen Support Preset that would make hero selection the only variable in a legends comparison. Pools ride the MATCH-SETUP envelope, never the composition — D-1244 stands unamended, following the heroSelectionMode precedent. Three ECs: EC-420 picker set filter, EC-421 envelope pools with a cross-block sum-equals-count validator, EC-422 LAGN 1.1.0 version seam plus version-gated pools. Design source is the marketing-site repo, where it is numbered WP-036. Reserves D-24194 and D-24195; drafted 2026-07-18)"]
        ["WP-243 ✅ Villain Fight KO-Hero player choice (UX: engine projection + client prompt + discard visibility)"]

      Hero Ability Coverage & Markup Pipeline
        ["WP-215 ✅ Hero rescue + reveal-draw effects (engine + data)"]
        ["WP-216 ✅ Markup corpus sweep: rescue + reveal-draw"]
        ["WP-217 ✅ Reveal-KO-if-zero + reveal-draw-at-least executors (engine + data)"]
        ["WP-218 ✅ Reveal compound executor + VP-cost corpus"]
        ["WP-219 ✅ Reveal cost-attack + odd-draw executors (engine + data)"]
        ["WP-220 ✅ Reveal attack-choose executor (player-choice infrastructure)"]
        ["WP-222 ✅ Pending hero choice UX (engine projection + client prompt)"]
        ["WP-223 ✅ Reveal KO-attack compound executor (engine + data)"]
        ["WP-224 ✅ Hero ability markup corpus sweep (all 40 sets)"]
        ["WP-225 ✅ Hero draw markup corpus sweep"]
        ["WP-247 ✅ Count-scaled hero attack framework (attack-per-count keyword + HeroCountSource resolver)"]
        ["WP-248 ✅ Optional-KO-then-Reward hero effect framework (optional-ko-reward keyword + resolveOptionalKoReward move + reward dispatch)"]
        ["WP-249 ✅ Optional-KO-then-Reward UX (chooser-only UIState projection + non-dismissible OptionalKoRewardPrompt + turn-end gating)"]
        ["WP-250 ✅ Hero-effect coverage gate (pnpm sim:coverage + CI non-regression; hybrid posture)"]
        ["WP-251 ✅ Hero effect ImplementationMap (executeSingleEffect switch → HERO_EFFECT_HANDLERS registry; behavior-preserving Lever-2 foundation)"]
        ["WP-252 ✅ Parameterized villain effect primitives (10 keywords → 5 VillainEffectPrimitive + VillainEffectDescriptor via VILLAIN_EFFECT_HANDLERS; dual legacy/parameterized parser; Mag3 data-only; reverse-map keeps narrative byte-identical; Lever 1; retires D-20201/D-18901)"]
        ["WP-253 ✅ Hero reveal-* collapse (8 reveal keywords → 1 parameterized reveal + RevealRule branch-list via revealRulesForLegacyKeyword; dual legacy/parameterized parser; no reverse-map needed; Lever 1 for heroes; D-24024)"]
        ["WP-255 ✅ The Amazing Spider-Man reveal-top-N (deck[peekOffset] dual-bound peek-advance multi-peek + reveal-count marker; first visible-win card under D-24026; D-24027)"]
        ["WP-256 ✅ Berserk via composable effect primitives (D-24029 first proof case; bootstraps the homogeneous effect-descriptor AST + interpreter with transient bind/ref context never in G + open HERO_COMPOSITION_MARKERS seam; Berserk + Recruit cousin are data; D-24030 + D-24031)"]
        ["WP-257 ✅ Hollow Effect Detector (engine runtime invariant; handler-reachability NOT state-diff; EFFECT_EXECUTION_REASONS + HollowEffectRecord + capped runtime-only G.diagnostics channel + parser unresolvedMarkers; DEFERRED_BY_DESIGN_MECHANICS allowlist; write-directly, no caller change; foundation for WP-258/259/260; D-24033 + D-24034)"]
        ["WP-258 ✅ Hollow effects on the arena-client diagnostics surface (reporting-loop consumer 1 of 3; optional UIState.hollowEffects projection read-only + public pass-through D-12803 + HollowEffectRecord/EffectExecutionReason barrel re-export; HollowEffectsPanel.vue mounted once in shared PlayViewport; rides the Download-diagnostics export free; no new DECISIONS)"]
        ["WP-263 ✅ Surface sim hollow-effect diagnostics on the capture/sweep projection (WP-259 predecessor; captureGameDiagnostics pure helper + additive sibling hollowEffects/hollowEffectsDropped on CapturedGameResult + SweepCellResult; runtime-only derived read, never persisted/gameplay-input, not nested into CapturedOutcomeSummary; both field-set drift guards updated; sim byte-identical, finalStateHash unchanged; unblocks WP-259; D-24039)"]
        ["WP-259 ✅ Runtime-observed hollow-effect /coverage overlay (reporting-loop surface 3 of 3; runtime-observed-hollows.mjs drives sweepSetupMatrix + reads cell.hollowEffects off the WP-263 sibling fields → committed canonical artifact + per-PR sim:runtime-observed:check in the hero-effect-coverage job; dashboard /coverage purple 'Observed in play' overlay + 'not observed in play' empty state via build-time-copy; committed artifact = fast random-policy RECORDED ZERO-STATE, heavier competent-play sweep deferred to cron per the CI-affordability fallback; D-24035)"]
        ["WP-260 ✅ Architect-lane gap intake (reporting-loop consumer 3 of 3's architect sibling; useArchitectGapIntake projects useCoverageLedger().runtimeObservedByMechanic → ArchitectGapCandidates folded into the Pipeline Architect lane via an optional 4th useAgentPipeline arg unshifted into architectBacklog only; consumer-owned ArchitectGapProjection D-23901 + single-lane D-23902 + WP-239 triageData backward-compat; fields copy the overlay entry, proposedTargetLayer from a fixed cardType map, reason opaque pass-through D-20703, invents no facts; live overlay zero-state ⇒ empty path; D-24036)"]
        ["WP-264 ✅ Parameterized simulation turn cap (maxTurns option; WP-265 enabler; optional trailing maxTurns default MAX_TURNS_PER_GAME on the six sim entry points runPerTurnLoop/buildGameOutcome/simulateOneGame/simulateOneGameAndCaptureMoves/runSimulation/sweepSetupMatrix → a downstream sweep runs short terminating games instead of grinding to the 200-turn safety cap; PARAM not a result field so field-set drift guards untouched; warm-up shares the same cap for PRNG parity; validity caller-owned no throw/clamp; finalStateHash unchanged replay-guarded; index.ts byte-unchanged; D-24040)"]
        ["WP-265 ✅ Real-signal runtime-observed hollows via a competent hero-diverse per-PR sweep (flips WP-259's /coverage overlay from zero-state to real signal via a competent-heuristic maxTurns-bounded WP-264 hero-diverse sweep — 39 hero-deck sets over the sentinel core × 8 seeds/board, the measured signal lever; enabled by WP-266/D-24043 onBegin parity; RE-SCOPE 2026-06-19 DROPPED the weekly cron — competent ~2.7ms/game so the per-PR sim:runtime-observed:check is kept, no runtime-observed-refresh.yml, no ci.yml change; matrix is a hardcoded locked value not a ledger read; artifact = 16 mechanics/176 obs/dropped 0/312 games, byte-identical; 2 files; no engine edit, dashboard untouched; D-24041 Active, D-24026 ✅ live-verified 2026-06-19 — /coverage Observed-in-play column populated, completes the hollow reporting loop end-to-end)"]
        ["WP-266 ✅ Simulation onBegin parity (WP-265 unblocker; the three observation-only per-turn loops runPerTurnLoop/par.aggregator simulateOneGame/runFixture rotateToNextTurn mirror the play-phase onBegin via ONE shared pure helper applyOnBeginParity = reset villainRevealedThisTurn+hasDrawnThisTurn + auto-draw to HAND_SIZE, rule hooks deferred D-0205, extracted at the third use since runFixture already had it inline WP-212+WP-236 but runner+aggregator did not → empty hand forever, playCard never legal; plus a one-shot reveal gate in getLegalMoves stage==='start' && !villainRevealedThisTurn ending the competent policy's turn-1 infinite re-reveal; game-determinism preserved, replay byte-behavior-identical, finalStateHash unchanged; scaffold-confirmed 1454/1454 + competent sweep surfaces ≥1 hero hollow in ~17ms; regenerates WP-259's runtime-observed-hollows.json off the zero-state; D-24043)"]
        ["WP-268 ✅ By-hook composition ledger (parser resolvedMarkers — the positive counterpart of WP-257's unresolvedMarkers — so the mechanic ledger marks a parameterized composition marker executable only when that card's hook resolved it, resolving the WP-267 by-name over-claim on /coverage By-card; parse-time provenance only, finalStateHash unchanged; D-24045)"]
        ["WP-267 ✅ Empowered via a class-count value primitive (first effect-authoring grind mechanic off /coverage; first PARAMETERIZED composition over the WP-256 substrate — new count-cards-by-class-in-zone value expr + its own shared-zone EffectCountZoneKind=['hq'] separate from per-player EFFECT_ZONE_KINDS, reads G.cardTraits[id].heroClass over G.hq no self-exclusion; buildEmpoweredComposition(color) + PARAMETERIZED_COMPOSITION_MARKER_NAMES deduped into HERO_COMPOSITION_MARKER_NAMES; parser parameterized-marker branch resolves the core ONLY on an anchored by-[hc:COLOR] tail whose color is the line's sole condition + suppresses it from heroClassConditions; Honest-Partial — deferred variants color-of-choice/Double-Triple/conditional-prefix/multi-class stay parse-unrecognized runtime hollows; no executor edit, no coverage-script edit, no HeroKeyword/node-type/EFFECT_ZONE_KINDS change, data/cards byte-unchanged; engine test 1462→1473/0, runtime-observed empowered cleared 16→15/176→163 dropped 0 byte-stable, coverage +5 core-form hooks, ledger 119→126 by-name over-claims ~4 deferred cards follow-up; finalStateHash unchanged EMPTY_REGISTRY; D-24044 Active, D-24026 ✅ live-verified 2026-06-20)"]
        ["WP-272 ✅ Empowered conditional-prefix class-gated core form (second Empowered form; parser-only — lifts WP-267/D-24044's conditional-prefix deferral for the class-gated case so [hc:X]: You get [keyword:Empowered] by [hc:Y] resolves to buildEmpoweredComposition(Y) RETAINED behind the [hc:X]: heroClassMatch gate, the WP-256 conditions-gate executor firing it only when the gate passes — no executor/interpreter/builder/contract edit; new anchored EMPOWERED_PREFIX_GATE_PATTERN + detection-only tryResolveEmpoweredConditionalPrefix structural gate (single [keyword:Empowered] marker + leading [hc:X]: + anchored fixed-color tail + no and-[hc:Z] + no [team:…]) — condition-counting forbidden, it mis-resolves fight-or-flight's choose-one; suppress-one-retain-gate removes exactly one heroClassMatch(Y) and keeps heroClassMatch(X); Honest-Partial — color-of-choice/multi-class/choose-one/team-gated/Double-Triple stay parse-unrecognized hollows, one-hit-wonder still resolves via core + fight-or-flight still unresolved; engine test 1477→1488/0, coverage executable 2614→2639 +25 hooks antm+10/bkpt+15, ledger 120→123 exactly 3 hero rows unsupported→executable by-hook antm/jocasta+bkpt/princess-shuri+bkpt/queen-storm-of-wakanda, wonder-man already executable via one-hit-wonder, torrential-downpour line-1 ambush-prefix stays hollow; runtime-observed-hollows.json byte-identical 15/163/dropped 0 empowered obs-delta 0 — competent sweep doesn't sample the 5 plays; data/cards byte-unchanged, finalStateHash unchanged EMPTY_REGISTRY; D-24047 Active, D-24026 ⏳ pending post-deploy /coverage)"]
        ["WP-280 ✅ Spectrum ≥3-hero-class conditional keyword + simple-effect markup (effect-authoring grind off /coverage — Spectrum hollows on Silk/ssw2; models the printed Spectrum gate as a new self-INCLUSIVE distinctHeroClassesAtLeast hero condition SPECTRUM_CLASS_THRESHOLD=3 attached by a parser branch recognizing [keyword:Spectrum] — NOT a HeroKeyword; marks up 3 simple gated effects quiver-of-thunderbolts/cascading-maneuver draw + long-range-spider-sense [keyword:reveal:2] cost-lte; the 6 ungated icon Spectrum lines now honor the gate + the 4 plain-English lines fire; honest-partial — borrowed-cloaking-device multi-card sum-cost reveal stays a reported hollow follow-up; ssw2-only; sentinel finalStateHash re-pins EXPECTED — bot plays Spectrum cards, gating changes effects; no new HeroKeyword, heroKeywords/apps/registry/server/ai.legalMoves byte-unchanged; D-24055 + D-24056)"]
        ["WP-281 ✅ Mechanic coverage dashboard condition-gate status display (dashboard-only UI for the new 'condition' status WP-280's ledger schema introduced; Spectrum's 5 ssw2 cards now render as 'Condition' on /coverage instead of false 'Unsupported'; AC-1..AC-6 verified, dashboard test 376→392/0; D-24057 + D-24058)"]
        ["WP-282 ✅ Undercover face-down zone architecture + send/play moves (foundational hidden-identity mechanic — Undercover is the #2 in-play hollow, 20 obs across 2099/bkwd/shld; Session 1 EC-313 adds faceDownCards: FaceDownCard[] zone on G.playerZones + sendUndercover(cardId, sourceZone)/playFromUndercover(cardId) moves under the Move Validation Contract + a determinism rule identity-stored/display-randomized-per-render; Session 2 EC-314 adds the undercover keyword to HERO_KEYWORDS + case-insensitive [keyword:Undercover] parser recognition wired into the conditional-hook system + full 2099 hero integration; move-executed MVP membership; sentinel re-pinned Option B with zero behavior change; unblocks future face-down mechanics cloaking/pending-choice; D-24059 + D-24060 + D-24061 + D-24062)"]
        ["WP-283 ✅ Empowered oracle: free-choice + binary-choose-one forms (third + fourth Empowered forms; oracle-max approximation D-24063 — scan HQ classes, grant +Attack = max count; adds tryResolveEmpoweredFreeChoice for amulet-of-avalon 'by the color of your choice' + tryResolveEmpoweredChooseOneLine pre-pass for fight-or-flight 'Choose one: by [hc:X] or by [hc:Y]'; new max-class-count-in-zone ValueExpression D-24064 with classes:'all'|string[]; processedAsChooseOne flag suppresses per-token loop for the choose-one line; no executor/interpreter/contract edit; data/cards byte-unchanged; engine test 1572/0 +17; D-24063 + D-24064 Active)"]
        ["WP-284 ✅ Empowered dynamic deck-peek (fifth Empowered form; D-24065 — final fallback tryResolveEmpoweredDynamic recognizes 'by the Hero Classes of the card you revealed this way' / cross-the-multiverse / wtif/star-lord-tchalla; new top-deck-card-class-count-in-zone ValueExpression D-24066: peek deck[0] class → count HQ matches → +Attack; playerID threaded through ValueExpressionEvaluator; guard in tryResolveEmpoweredFreeChoice prevents premature capture; peek-only no zone move; wtif.json byte-unchanged; engine test 1572→1586/0 +14; D-24065 + D-24066 Active)"]
        ["WP-285 ✅ Ebony Blade victory-pile villain-pick infrastructure (PendingVictoryPileCardPick FIFO queue mirrors WP-248 optional-ko-reward topology; new 'victory-villain-attack' keyword = 21st HERO_KEYWORDS entry D-24068; new resolveVictoryPileCardPick move reads G.cardStats[id].fightCost — villain printed attack stored as fightCost not attack; block-all guards at 8 standard sites; getEligibleVictoryVillains filters G.villainDeckCardTypes === 'villain'; bot default = highest-fightCost, ties by lowest victory-pile index; antm/the-ebony-blade prefixed [keyword:victory-villain-attack]; game.ts 14→15 moves; engine test 1586→1626/0 +40 (all 16 ACs directly asserted); D-24067 + D-24068 Active; 2026-06-24)"]
        ["WP-286 ✅ One-Hit Wonder draw-or-empowered interactive choose-one"]
        ["WP-287 ✅ Draw-or-empowered choose-one UX (projection + client prompt)"]
        ["WP-289 ✅ Sim move-dispatch completeness for interactive resolve moves (WP-286 follow-up)"]
        ["WP-295 ✅ Hero play + condition-skip observability logging (G.messages → UIState.log; condition-fail mutates only the log; D-24082)"]
        ["WP-290 ✅ Size-Changing hero class-grant on play (third re-draft target off the runtime-observed ranking — clears the size-changing/parse-unrecognized hollow on antm/jocasta/holographic-image-inducer gitSha 988ad2e; implements the printed 'when you play this card, it has the [Class] class' as a class-grant realized at class-read time, mirroring the WP-273 wall-crawl recognized-keyword-no-onPlay-handler pattern; new 'size-changing' keyword 22→23 + HeroAbilityHook.sizeChangingClasses + immutable G.cardSizeChangingClasses parsed at setup where the same-line [hc:...] is the GRANTED class NOT a heroClassMatch condition; new pure hero/sizeChanging.logic.ts cardHasClassWhenPlayed/getGrantedClasses effective class = printed ∪ granted with cardTraits never mutated, consulted by both inPlay class reads heroClassMatch + distinctHeroClassesAtLeast; 'size-changing' joins a new CLASS_GRANT_KEYWORDS set → MVP_KEYWORDS with no HERO_EFFECT_HANDLERS entry handler count unchanged at 10; grant proven load-bearing — null-printed-class yellowjacket/goliath + printed≠granted giant-ego/swarm-tactics; conditional-spread omit-when-empty G field keeps no-Size-Changing games byte-identical so no sentinel re-pin; Attack-as-VP no hero-deck VP scoring + Microscopic + villain/divided-card + UIState class display deferred follow-ups; engine test 1666→1687/0, pnpm -r build 0, ledger size-changing→executable 16 rows, runtime-observed hollow removed 11→10 mechanics, sim:coverage no regression; User-Visible Surface play.legendary-arena.com D-24026 post-deploy; 2026-06-28 commit 396526f3; D-24074)"]
        ["WP-292 ✅ Villain defeat-requirement gate — 'You can't defeat X unless you have a [class/team] Hero' (operator field report: Blob defeated with an all-Avengers/Guardians board + no X-Men Hero, gitSha b108dc4 match FC6toc2rQQG; ledger confirmed Blob=(unmarked) — the printed restriction was cosmetic card text, never enforced; a fight PRECONDITION distinct from the onFight/onAmbush/onEscape consequence hooks, the first fight-precondition primitive in the engine; new VillainDefeatRequirement {kind 'team'|'hero-class', value} + VILLAIN_DEFEAT_REQUIREMENT_KINDS drift array; new marker [require-to-defeat:<kind>:<value>] team→'team' hc→'hero-class' parsed by setup/villainDefeatRequirement.setup.ts into per-instance entries via villainCardInstanceExtIds, unknown-kind/empty-value/no-marker→no entry no throw; immutable G.villainDefeatRequirements built via conditional-spread omit-when-empty so matches without a marked villain stay byte-identical; pure moves/villainDefeatRequirement.logic.ts getDefeatRequirement + playerMeetsDefeatRequirement scanning hand∪inPlay ONLY — discard/deck excluded operator decision — the single gate authority; fightVillain silent-return precondition gate after the attack-cost check mirroring the Guard-block posture no mutation/message/event/throw on block; data overlay apply-defeat-requirement-markers.mjs + villain-defeat-requirements.json marks Blob core/brotherhood team:x-men + Venom core/spider-foes hc:covert + Zombie Venom ssw1/deadlands-the hc:covert, cvwr Size-Changing Venom left unmarked, idempotent surgical append re-run=zero-line diff; mastermind/henchman/multi-requirement/ledger-recognition/UX-hint deferred follow-ups; engine test 1687→1710/0 +23, pnpm -r build 0, sim:runtime-observed:check byte-current no re-pin; User-Visible Surface play.legendary-arena.com D-24026 post-deploy; 2026-06-29 commit 3e732c7f; D-24076)"]
        ["WP-356 ✅ shuffle-discard-empty-reward hero keyword: Jocasta Reprocess + Electromagnetic Eyebeams (clears the two-branch empty-discard-reward-or-shuffle hollow diagnosed from a live log 2026-07-11; one mandatory immediate keyword HERO_KEYWORDS 29→30 + handler registry 16→17 on the D-24029 substrate; D-24019-style 3-segment token, seeded rewards exactly recruit/attack, heroAbility.types.ts untouched; executor = empty discard → addResources grant, else combined deterministic discard→deck shuffle via ShuffleProvider + G.messages both branches; execution amendments: icon-suppression — the icon step had granted a flat unconditional +2 on every play, D-24016 precedent now subsumes it — + executor magnitude>=1 floor; 2 marker rows + antm.json + ledger + card-mechanics regen; engine 1903/444→1914/447/0 exactly the locked delta; optional-shuffle dead/ssw2 + Flying Steed deferred; Done 2026-07-12; EC-386; D-24148)"]
        ["WP-364 ✅ gain-wound-self / gain-wound-each hero keywords: the plain 'gain a Wound' family (live-game diagnosis 2026-07-11 matchId sGTM7LWSIHy — Hulk Crazed Rampage 'Each player gains a Wound' played twice with no effect: Wound supply stayed 30, player gained 0; two stacked gaps — bare-prose no marker AND generic hero 'wound' keyword is DEFERRED_BY_DESIGN_MECHANICS; two NARROW keywords reuse WP-017 gainWound(woundsPile,discard) via the WP-316 villain per-target loop, self=active player / each=sorted Object.keys(G.playerZones) + woundsDrawn bump, wound→discard so NO targeting UI needed; single-segment tokens [keyword:gain-wound-{self,each}], one shared executor under both keys + NO_MAGNITUDE/HANDLED/MVP sets, G.messages both branches; Honest-Partial — generic 'wound' + all 40 targeting/conditional hero wound forms stay deferred, heroAbility.types.ts untouched since each keyword encodes its target; 7 marker rows across 6 sets 3× crazed-rampage each + 4× self colossus/human-torch/luke-cage/hulkling + VALID_TOKEN_PATTERN + regenerated 3dtc/core/msp1/cvwr/dkcy/ff04 + hero ledger; no RNG top-of-pile draw, no VP-scoring change; executed 2026-07-12; parser UNCHANGED (generic plain-keyword builder emits {type}), only HERO_KEYWORDS + NO_MAGNITUDE_KEYWORDS registration; engine 1903/444/0, ledger all 7 executable by-hook, sentinel unchanged; EC-395 (renumbered from EC-391 — WP-361 landed EC-391 first); D-24156 Active)"]
        ["WP-382 ✅ ko-wound-reward hero keyword — auto-resolve 'you may KO a Wound → reward' (implements the hollow Healing Factor family surfaced in the Red Skull live game, played ~10× no-op; a Wound-restricted auto-resolving variant of optional-ko-reward D-24019 — executor immediately KOs one WOUND_EXT_ID hand-first else discard to G.ko + grants the reward by reusing executeSingleEffect {type:rewardType,magnitude} rewardType ∈ draw/attack/recruit, no Wound → G.messages no-op D-24017; AUTO-RESOLVE not a pending choice because a Wound is a fungible dead card + KO-plus-reward is strictly beneficial so the 'you may' decline + hand/discard choice are strategically inert unlike optional-ko-reward which KOs any card; KO filtered to WOUND_EXT_ID only so a Hero in hand is never KO'd; keyword carries reward magnitude so NOT in NO_MAGNITUDE_KEYWORDS; marker token [keyword:ko-wound-reward:rewardType:n]; marks 8 core-vocab cards draw×2 incl Healing Factor + attack×4 + recruit×2 across core/dstr/cvwr/3dtc/msp1/ff04/msis — refined from optimistic 9 after corpus inspection, 4 candidates deferred to the marker map _deferred: mdns/morbius [keyword:Moonlight]: state gate + wpnx/weapon-x [hc:instinct]: gate & Berserk reward outside seeded vocab + xmen/x-23 & cvwr/peter-parker rewardless; SIM-OUTCOME CASCADE regen mechanics:metadata + ledger:heroes + runtime-observed, NO sentinel re-pin — no recorded fixture plays a marked card so finalStateHash + PRE_WP080_HASH byte-identical; engine+data NO client; out of scope pending-choice UX + the 4 deferred family members Honest-Partial; User-Visible Surface play.legendary-arena.com D-24026 live-verify Healing Factor KOs a Wound + draws; Done 2026-07-15 exec off origin/main @ 4ed649ff, -r build 0 + engine 1957→1965/0 +8 7 handler + 1 registration; EC-411; D-24183 Active; D-24026 live-verify operator-pending)"]
        ["WP-383 ✅ discard-to-play hero card cost — mandatory 'discard a card to play this card' (Cyclops Determination/Optic Blast + 3 siblings print 'To play this card, you must discard a card from your hand' but the cost is silently skipped so the cards are played ~free, strictly stronger than printed; surfaced in the Red Skull live game; faithful fix Jeff-locked = mandatory + client prompt; new discard-to-play keyword; the engine's FIRST card-specific pre-commit precondition in playCard blocks an unpayable play before commit else the base recruit/attack leaks — every existing pending choice fires AFTER commit so the onPlay hook can't veto; a payable play commits then parks a mandatory PendingDiscardToPlay resolved by a new resolveDiscardToPlay move + DiscardToPlayPrompt.vue mirroring return-zero-cost-discard D-24139 end-to-end; block-all guard added to every action move + ai.legalMoves forced-resolve; marks 7 single-discard cards core/cyclops determination+optic-blast + ssw2/ruby-summers heir-to-legends + vill/juggernaut runaway-train + xmen/havok unleash-havok + co2e/cyclops determination+optic-blast 2nd-edition reprints (co2e landed on main mid-execution via #766), defers the one n=3 card ssw2/ruby-summers extinction-blast to _deferred; SIM-OUTCOME CASCADE regen mechanics:metadata + ledger:heroes + runtime-observed, sentinel re-pin only if a recorded game plays a marked card; engine+client+data engine-first commit; out of scope reveal-N reorder gap Amazing Spider-Man family + unpayable-play visual feedback + n>1 multi-discard; User-Visible Surface play.legendary-arena.com D-24026 live-verify Optic Blast with a spare card prompts discard, as last card cannot play no free attack; Done 2026-07-15 exec off origin/main @ 688c180e, -r build 0 + engine 1965→1981/0 +16 (incl 15-test resolveDiscardToPlay + precondition suite) + arena-client typecheck 0 + test 963→974 +11 (prompt + gate); NO sentinel re-pin — pendingDiscardToPlay optional G field undefined-unless-triggered + no recorded fixture plays a marked card so finalStateHash + PRE_WP080_HASH byte-identical, runtime-observed byte-identical too; ARCHITECTURE.md Move Validation Contract clause added + rules mirror; EC-412; D-24184 + D-24185 Active; D-24026 live-verify operator-pending)"]
        ["WP-379 ✅ Wound 'Healing' ability — KO all Wounds from hand (engine; implements the printed universal rule rules v23 §Healing Wounds — 'If you don't recruit or fight anything on your turn, you may KO all the Wounds from your hand'; new healWounds move KOs every WOUND_EXT_ID from the current player's hand into G.ko permanently, non-core contract main-gate + block-all hasPending* cluster + hasActedThisTurn precondition then mutate, no pending-choice state, one pushLog line no notableEvent; two optional LegendaryGameState flags carry the mutual exclusion — hasActedThisTurn set by fightVillain/recruitHero/fightMastermind on successful commit + gates Healing, hasHealedThisTurn set by healWounds + reverse-locks those three — both reset in play turn.onBegin, structural NOT economy-derived so a 0-cost fight/recruit still counts D-24180; move registered client:false D-10008, game.test.ts move-set drift bumped; determinism — the new G flags may re-pin a sentinel/golden hash via the canonical record tool never hand-edit, else unchanged; out of scope the client Heal-Wounds affordance + UIState projection deferred to a follow-up client WP, AI/sim integration ai.legalMoves untouched so PAR/sweep baselines unchanged, notableEvent, the playCard wound-block, Enraging-Wound variants; User-Visible Surface none — infrastructure; Done 2026-07-14 exec off origin/main @ 161cd432, engine 1927→1943/0 +16, all greps pass; determinism re-pin behaviour-neutral — sentinel finalStateHash 47afd86a→3da2c374 via record-game-fixture.mjs + PRE_WP080_HASH be266d02→ec64506a, same class as WP-236/WP-282; EC-408; D-24179+D-24180 Active — reserved D-24176/D-24177 renumbered on collision, D-24176 taken by the concurrent scoring fix + D-24178 by open PR #751)"]
        ["WP-380 ✅ Wound 'Healing' client affordance — Heal Wounds button + UIState projection (WP-379 follow-up; surfaces the engine healWounds move to players; single cross-layer WP, engine-first commit; ENGINE projects the two WP-379 per-turn flags hasActedThisTurn+hasHealedThisTurn onto UIState.game as PUBLIC read-only booleans siblings of currentStage — NOT per-player-redacted since acted/healed is observable not secret — + drift pin, UIState is not in computeStateHash so NO sentinel re-pin; CLIENT adds 'healWounds' to UiMoveName + a useTurnActions().canHealWounds() GatingResult predicate turn→main→no-pending→wound-in-hand→not-acted→not-healed with the locked disabled-tooltip precedence + a Heal Wounds button in TurnActionBar Step 2 dispatching submitMove('healWounds',{}); PlayDesktop/PlayMobile derive hasWoundInHand by scanning viewer.handCards for a client-local 'pile-wound' constant drift-tested vs engine WOUND_EXT_ID since components may not import engine runtime code, + drill the two projected flags; out of scope AI/sim integration + a heal notableEvent/overlay cosmetic follow-up + playCard wound-block + Enraging Wounds; User-Visible Surface play.legendary-arena.com D-24026 live-verify; Done 2026-07-15 exec off origin/main @ 08193a6b, -r build 0 + arena-client typecheck 0 + test 923→939 (+16) + engine 1953/0 sentinel+PRE_WP080_HASH byte-identical NO re-pin (UIState not in computeStateHash); inline amendment +12 fixture backfills for the required UIState.game field add (documented recurrence, mechanical); EC-409; D-24181 Active; D-24026 live-verify operator-pending)"]
        ["WP-381 ✅ Wound 'Healing' notableEvent overlay — healResolved center-screen announcement (WP-379/380 cosmetic follow-up; WP-380's Heal Wounds button live-verified — Red Skull game 2026-07-15 gitSha 80ba584 healed turns 16/24/32; adds a 6th notableEvent variant healResolved mirroring mastermindDefeated D-20008; healWounds emits it LAST — minimal-payload type+playerId+woundsHealed+engine-composed narrative no eventId/seq/timestamp D-20001, unconditional G.notableEvents.push like fightVillain; PUBLIC not audience-redacted, rides the existing UIState.notableEvents spread with NO UIState projection change; arena-client NotableEventOverlay renders a 'Healed' chip + verbatim narrative D-20002 via one CHIP_LABELS entry + optional CSS; composeHealNarrative pure golden-tested + NOTABLE_EVENT_TYPES drift 5→6; NO competitive-hash re-pin — no recorded sentinel/golden fixture heals since healWounds is not in ai.legalMoves so finalStateHash + PRE_WP080_HASH stay byte-identical; ~8 files 6 engine + 2 client; out of scope engine gameplay + UIState projection + AI/sim + Healing Factor hero-card wound-KO the deferred generic wound keyword family WP-364 + Enraging Wounds; User-Visible Surface play.legendary-arena.com D-24026 live-verify; Done 2026-07-15 exec off 73d015e5, -r build 0 + arena-client typecheck 0 + test 961→963 + engine 1953→1957/0 sentinel+PRE_WP080_HASH byte-identical NO re-pin; inline amendment +2 eventCardId exhaustiveness in useNotableEventStream; EC-410; D-24182 Active; D-24026 live-verify operator-pending; note WP-380 D-24026 now live-verified via the same Red Skull game gitSha 80ba584 heals turns 16/24/32)"]
        ["WP-273 ✅ Wall-Crawl onRecruit keyword + optional recruit-to-deck placement (first effect-authoring grind target off /coverage's runtime-observed ranking — wall-crawl is the 2nd-highest in-play hollow, 23 obs, 14 heroes/29 lines, Spider sets; makes the printed when-you-recruit-this-Hero-you-may-put-it-on-top-of-your-deck keyword execute: recognizes the existing [keyword:Wall-Crawl] marker + gives it an onRecruit default timing via a new KEYWORD_TIMING_DEFAULTS map so the recognized marker leaves the onPlay path empty and stops firing parse-unrecognized, + adds an additive optional toTopOfDeck arg to recruitHero placing a recruited wall-crawl hero on the top of the player's own deck instead of discard; flips wall-crawl unsupported→executable 29 lines + drops the 23 onPlay hollows; clean self-contained — no new zone model, no pending-choice/board-freeze, no new move so game.test.ts move-count unchanged; builds the first reusable onRecruit execution path; marker already in card data so no data/cards change; bot defaults toTopOfDeck false so the deterministic sweep zone state is unchanged, sentinel re-pinned only on divergence, all coverage artifacts regenerated; dodge/undercover/unleash ecosystem + the arena-client put-on-top toggle deferred; User-Visible Surface dashboard/coverage, D-24026 post-deploy; D-24049)"]
        ["WP-274 ✅ In-play coverage metric — % of in-play hollow observations resolved (dashboard /coverage gets a 2nd headline beside %-executable, OBS-WEIGHTED — each unsupported mechanic contributes its runtime-observed hitCount, resolved when it's executable in the hero ledger — so fixing a high-frequency hollow dodge-37/undercover-20/moonlight-18 moves the needle proportionally, unlike the existing mechanic-counted worst-case rollup that hides partial progress; percentResolved = Σ peakObs[executable] / Σ peakObs[all], peakObs = max(committed-baseline, live), ledger-gated NOT live-obs-gated since the sweep is a sample; reads 0% today 0/140, 26.4% once an OBSERVED mechanic like dodge-37 lands (wall-crawl is NOT in the sweep so it leaves the needle flat); self-contained dashboard layer only — no @legendary-arena import, additive existing headline+table untouched; new useInPlayCoverage composable + a committed obs-baseline seed in-play-hollow-baseline.json that preserves fixed mechanics' obs after they vanish from the live artifact + a deliberate monotonic maintenance script; the dashboard CI gate set lint/typecheck/test:coverage/format:check/build stayed green; stacks on WP-273 for governance only; User-Visible Surface dashboard/coverage D-24026 post-deploy; Done 2026-06-21 commit 8704c782; D-24050)"]
        ["WP-275 ✅ Dodge hand-discard-to-draw move + recognized keyword (second effect-authoring grind target off /coverage's runtime-observed ranking — dodge is the #1 in-play hollow, 37 obs, the single biggest player-facing gap; 25 [keyword:Dodge] lines bkwd-10+vill-15; makes the printed during-your-turn-discard-this-from-hand-to-draw-another keyword execute: recognizes the existing [keyword:Dodge] marker so the recognized keyword leaves the onPlay path empty and stops firing parse-unrecognized, + adds a new non-core dodgeCard({cardId}) move on the recruitHero internal-gating precedent that discards a Dodge card from hand to discard + draws one replacement; ignore-all-other-text is automatic since the card is discarded never played; dodge ∈ MVP_KEYWORDS via a new HAND_ACTION_EXECUTED_KEYWORDS set sibling to WP-273's RECRUIT_TIME_EXECUTED_KEYWORDS, no play-time handler so the play-time visit is a not-hollow no-op; flips dodge unsupported→executable + drops the 37 onPlay hollows; clean self-contained — no new zone model, no pending-choice/board-freeze; builds the first hand-resident optional move; marker already in card data so no data/cards change; the new move bumps game.test.ts move-count 11→12 but is NOT added to the sim's getLegalMoves so the deterministic sweep never dodges and the sentinel finalStateHash is unchanged — the determinism lever, mirrors WP-273's bot decline; honest-partial — the dodge-entangled rider lines Twilight-Ops flip not-hollow per the mixed-hook rule but undercover/unleash stay reported on their standalone lines; teaching the bot to dodge + the arena-client discard-to-draw affordance + undercover/unleash+zone-model are deferred follow-ups; extends WP-273's MVP move-executed category + ledger handler-module mapping; User-Visible Surface dashboard/coverage, D-24026 post-deploy; D-24051)"]

      Notable Events & Overlays
        ["WP-200 ✅ Notable game event log (engine)"]
        ["WP-201 ✅ Notable event overlays (arena client)"]
        ["WP-207a ✅ notableEvents fixture backfill (client)"]
        ["WP-207b ✅ notableEvents test backfill (client)"]

      Simulation Sweep & Analytics Pipeline
        ["WP-181 ✅ Bot decision logging"]
        ["WP-193 ✅ Policy-mode fixture recording (engine + scripts)"]
        ["WP-194 ✅ Setup-matrix sweep runner (scheme × mastermind)"]
        ["WP-195 ✅ Sweep manifest anomaly oracle (engine + scripts)"]
        ["WP-205 ✅ analytics_events server (capture + query endpoints)"]
        ["WP-209 ✅ sweep_runs server (storage + submission + query + nightly)"]
        ["WP-211 ✅ Cross-app sweep type drift test (dashboard ↔ server)"]
        ["WP-304 ✅ Done — Engine-runner host + CLI (Windows Engine Exe Target A / Phase-1 A1): new apps/engine-runner headless CLI that loads the local registry + drives the engine's already-public bot-vs-bot harness (runSimulation + createCompetentHeuristicPolicy) for a scenario+seed; run mode emits SimulationResult JSON, verify mode is a byte-identical determinism self-check; no packaging + no engine/registry source change (the simulation surface is already re-exported); packaging (esbuild + pkg/SEA/bun → .exe), fixture-replay + exe-vs-node finalStateHash parity are explicitly follow-on WPs; EC-334; D-24088)"]

      Dashboard & Operator Analytics
        ["WP-157 ✅ Dashboard scaffold (PrimeVue + Pinia + ECharts)"]
        ["WP-162 ✅ Dashboard daily execution panel + UI polish"]
        ["WP-196 ✅ Net revenue + paid-action errors widgets"]
        ["WP-197 ✅ Live deploy (CF Pages + Access gate)"]
        ["WP-198 ✅ Ops-machine patterns (cadence horizons + status chip + vision card)"]
        ["WP-199 ✅ Daily-driver: STATUS feed + governance KPIs"]
        ["WP-203 ✅ Acquisition + activation + retention surfaces"]
        ["WP-204 ✅ Public-surface health + error monitor + cost watchdog"]
        ["WP-206 ✅ Analytics MOCK→LIVE flip"]
        ["WP-378 ✅ Analytics client emitter — feed the acquisition/activation/retention funnel (arena client; closes the WP-205 producer gap — the guest POST /api/analytics/events + analytics_events table + dashboard reads all exist but nothing writes events, so Traffic Sources/Activation Funnel/Retention Cohorts read 'No data captured'; adds a fire-and-forget analyticsEmitter [opaque sessionStorage session id, RAW user_id server-hashed per D-20502, silent-failure, keepalive; 3-arg explicit-userId signature ratified in D-24173 for layer isolation], a pure channelClassifier [referrer/UTM → direct/search/referral/paid, paid-first precedence D-24175], and one useAnalyticsCapture reactive hub [App.vue, one mount] emitting channel + retention on load + watching auth/match stores for signup/first-match — client-local localStorage detection non-authoritative v1 D-24174, 1-day retention threshold; nine frozen event types; no server/engine/registry/preplan/framework import grep-gated; no server/schema change; EC-407; D-24173..D-24175 Active; Done 2026-07-15 — arena-client typecheck 0 / suite green / all §After greps pass; D-24026 live-verify operator-pending on deploy [dashboard.legendary-arena.com/players])"]
        ["WP-210 ✅ SweepHealthWidget dashboard surface"]
        ["WP-226 ✅ Global mock-mode banner"]
        ["WP-229 ✅ Agent Pipeline page (Architect/Builder/Inspector/Evaluator lanes)"]
        ["WP-238 ✅ Done — Sweep MOCK→LIVE flip (dashboard sweep panels render real GET /api/sweep/latest)"]
        ["WP-241 ✅ Done — Operator auth + Bearer cutover (real Hanko login → Authorization: Bearer on the LIVE fetchers; supersedes the cookie posture, complies with the bearer-only server)"]
        ["WP-373 ✅ Dashboard billing + revenue endpoints (server) — DONE 2026-07-13 (EC-402): first slice of wiring the dashboard's live /api/dash/* family to real data (operator chose billing+revenue first; the endpoints.ts family was mock-only — no server route served it). New apps/server/src/dashboard/ module + /api/dash/* sub-surface, 4 read-only admin-session-required routes (requireAdminSession; no finance role → admin per D-19603): /api/dash/metrics/billing/health (+/sparklines) fulfilling the D-19603 forward contract (BillingHealth-byte-compatible + rate invariants) from stripe_events.process_error + stripe_checkout_sessions.intent_status; /api/dash/revenue + /metrics/revenue deriving the amount from stripe_events.payload->data->object->amount_total (cents) — the price allowlist carries no amount, skip-on-missing never fabricated. Bare {data:T} (D-20503); no migration/write/dashboard-app change (live flip = deploy env). /kpis//players//matches/DAU later; /system/nodes+/alerts blocked on absent infra; executed 2026-07-13 via new apps/server/src/dashboard/dashboardBilling.{types,logic,routes}.ts + server.mjs wiring (4 admin-session-required routes); full server suite fail 0 (154 DB-skip) + DB integration 3/3 on local Postgres; 4 Wired api-endpoints.md rows (D-11804); EC-402; D-24168 Active"]
        ["WP-374 ✅ Dashboard matches + players + KPIs endpoints (server) — second /api/dash/* slice after WP-373 billing+revenue; 3 read-only admin-session-required routes on the WP-373 dashboard module: /api/dash/matches projects the bgio.matches blob (initial_state.G.matchConfiguration scheme/mastermind → registry names, ctx.numPlayers, metadata createdAt/updatedAt/gameover) as a READ-ONLY match-summary projection that EXTENDS the D-24095/24119/24153 bgio-blob-read carve-out (new D-24169 + ARCHITECTURE.md §Persistence Boundary + rules-mirror edit) — projection-only, never state/log/write, gameover absent→in_progress present→hero/villain win; /api/dash/players = players LEFT JOIN aggregated competitive_scores (matchesPlayed/winRate from outcome), status from is_suspended, approximate lastActive; /api/dash/kpis = derivable subset (players/matches/revenue-reusing-WP-373/hero-win-rate) + prior-window trends, DAU OMITTED not fabricated (honest-partial). Bare {data:T}; no migration/write/dashboard-app change. 3 Wired rows (D-11804); /system-nodes+/alerts still blocked on infra; large lane; D-24169"]
        ["WP-439 ✅ Server runtime-health signal on the operator dashboard (server + dashboard; answers do-we-need-to-cluster with data. Came out of the 2026-07-27 clustering discussion — a deploy log showed Render setting WEB_CONCURRENCY=2, which is INERT today because the server runs a single node process, no cluster/throng wrapper. Whether to cluster to use the second core should be decided by EVIDENCE of single-process CPU saturation, not a hunch, and Node's standard saturation signal is event-loop delay: a rising p99 while one core maxes and the others idle is the clustering-would-help tell. New admin-gated GET /api/dash/system/runtime returns a live RuntimeHealthSnapshot — capturedAt, uptimeSeconds, cpuCount, cpuPercent, eventLoopDelayMs{mean,p50,p99,max}, memoryRssMb, webConcurrency — sampled ON REQUEST from process/perf_hooks/os: a module-load monitorEventLoopDelay histogram read+reset per request so each reading is the delay distribution over the window since the last poll, and a process.cpuUsage() delta over an hrtime span held in module state, seeded at load so the first request has a baseline, normalised ÷cpuCount to a machine-capacity percent so a pure-JS process caps near 100/cpuCount, one core, and approaching that ceiling while other cores idle is the tell. Reads NO database, NO registry, NO engine — the shared pool is threaded to the route ONLY for the requireAdminSession gate; server layer clock/CPU reads are allowed, the no-clock rule is an engine rule. Metric-math is PURE, buildRuntimeHealthSnapshot / computeCpuPercent / parseWebConcurrency, unit-tested with fixed inputs; only the gatherer getRuntimeHealthSnapshot is impure. Route follows the WP-373/374 /api/dash idiom exactly: no-store first, admin gate, bare {data:T} envelope per D-20503. A Server Runtime Health TILE leads the dashboard System Health page, mirroring ServerStatusWidget with 4-arm loading/error/empty/data state, a p99 headline + status chip, a metric grid, and a plain-English clustering hint from a pure utils/runtimeHealth.ts; status thresholds event-loop p99 at-or-above 50ms watch, 200ms saturated, RUNTIME_HEALTH_STATUSES canonical array. Dashboard follows Pattern A, endpoints.ts isMockMode branch → useFetch composable → widget, plus the wrapMock mock idiom and a cross-app runtimeHealth.drift.test.ts field-set guard since the dashboard cannot import apps/server. Section 21 / D-11804 APPLIES — api-endpoints.md gains the /api/dash/system/runtime row, Wired, admin-session-required. OUT of scope: actually clustering the server, which needs a shared Socket.IO adapter plus singleton leader-election for the reaper/harvester/publisher — a separate deliberate call this tile exists to inform; the Overview strip; time-series retention. Section 17 Vision, section 14 observability, no conflict. Reserves D-24258; EC-474; standard cross-app lane drafted+executed 2026-07-27. Server runtime tests 9/0 plus server suite green; dashboard 418/0 + typecheck 0 + coverage at-or-above 90/80/88 + lint + format + build all green; pnpm -r build 0; repo-wide green. User-visible surface = operator dashboard, D-24026 live-verify operator-pending because the tile sits behind the dashboard admin auth gate and is only meaningful against the deployed server, the norm for every prior dashboard-widget WP. Hard-deps WP-373, WP-374)"]

      Agent Triage Pipeline
        ["WP-230 ✅ Done — Pipeline page sweep integration (agent lanes consume nightly sweep findings)"]
        ["WP-231 ✅ Done — Scheduled agent triage sessions (Inspector reads sweep → files findings)"]
        ["WP-232 ✅ Done — Agent handoff chain (Inspector → Builder → Architect)"]
        ["WP-233 ✅ Done — Closed-loop sweep verification (Builder fix → re-sweep → Inspector verify)"]
        ["WP-234 ✅ Done — Full-corpus sweep expansion (weekly rotating window beyond 2×2 smoke)"]
        ["WP-235 ✅ Done — Pipeline page sweep health trend view (cadence-aware health-rate trends + healthy-class constant)"]
        ["WP-349 📝 Draft — Sweep health rate = anomaly-free rate (fix the structural 0% from the D-23503 endgame-reached healthy-class; D-24141)"]
        ["WP-239 ✅ Done — Triage dashboard surfaces (inspection findings + handoff lifecycle on the Pipeline Inspector lane, read-only)"]

      Admin & Route Wiring
        ["WP-110 ✅ Admin billing visibility"]
        ["WP-176 ✅ Admin billing auth cutover (shared-secret → session)"]
        ["WP-152 ✅ Wire public profile route in server.mjs"]
        ["WP-159 ✅ Admin session gate (session-based admin auth)"]

      Phase 9 — Profile Surface Follow-ups
        ["WP-105 ✅ Player badges"]
        ["WP-106 ✅ Done — avatar upload pipeline"]
        ["WP-107 ✅ Profile integrity / anti-cheat surface"]
        ["WP-108 ✅ Profile billing & funding history UI"]
        ["WP-296 ✅ Avatar CDN host unification (images.barefootbetters.com → images.legendary-arena.com — the card-image host/bucket; AVATAR_CDN_BASE + closed-origin validateAvatarUrl allowlist both retargeted + migration 021_rewrite_avatar_url_host.sql; api-endpoints catalog + wiki reconciled; D-24083 supersedes the host string in D-10601/D-10602; EC-328)"]
        ["WP-298 ✅ Owner profile avatar upload UI (wires the already-shipped WP-106 POST /api/me/avatar pipeline into MyProfilePage.vue ?route=me — a file-input + 'Upload avatar' control replacing the unusable free-text-only avatar-URL field; new uploadOwnerAvatar(authToken,file) wrapper multipart 'avatar' field no Content-Type, failure code read from body.code not the sibling body.error, + client-local drift-guarded AVATAR_UPLOAD_ERROR_CODES mirror; additive client-only, no server/contract/catalog change; consumes D-10601/D-10602/D-24083 no new D-entry; Lightweight Lane D-24028; EC-329; typecheck 0/arena-client test 618→624/build 0; User-Visible Surface play.legendary-arena.com D-24026 post-deploy)"]
        ["WP-299 ✅ Owner profile edit-page UX polish (avatar preview thumbnail that hides on broken URL, accurate PNG/JPEG/WebP up-to-5MB upload hint sourced from server ALLOWED_MIME_TYPES + MAX_FILE_SIZE_BYTES, live About-me char counter, scoped card layout + one-column link row; presentation-only, no API/contract/store change, no new D-entry; Lightweight Lane D-24028; EC-330; arena-client test 624 unchanged/build 0)"]
        ["WP-300 ✅ Public profile link-preview meta (Open Graph / Twitter Card via the repo's first Cloudflare Pages Function — functions/_middleware.ts uses HTMLRewriter to inject per-player preview meta tags into the SPA shell for ?profile=handle, fail-soft on any API failure/bad handle; buildProfileMeta.ts unit-tested attribute-escaping + §23 guard; extends client-app category to functions/; D-24085 edge-subsurface classification; EC-331; test 624→634/typecheck 0/build 0)"]
        ["WP-301 ✅ Profile loadout library — data model + endpoints (server) (migration 022_create_player_loadouts.sql — player_id FK CASCADE, lagn_json jsonb, visibility CHECK, partial-unique share_slug; loadoutLibrary.{types,logic,routes}.ts + tests; 5 endpoints POST/GET /api/me/loadouts + PATCH/DELETE /api/me/loadouts/:id + guest GET /api/loadouts/:shareSlug public-only; server-side LAGN validate on every write, 50-cap, decorative-not-merit §19b; D-24086; EC-332; server suite 716→746/build 0)"]
        ["WP-302 ✅ Profile loadout library — owner UI + public share view (client) (consumes the WP-301 endpoints, no new server surface; MyProfilePage.vue Saved Loadouts section — paste-LAGN create / list / rename / public-private toggle / delete / copy-share-link; net-new unguarded ?loadout=<shareSlug> SharedLoadoutPage.vue — name + displayHandle + composition summary, 404 on private/missing, never an account id; loadoutLibraryApi.ts Bearer client + guest read + loadoutSummary.ts defensive helper, lagn opaque unknown no @legendary-arena/lagn import, no new npm dep; decorative-not-merit §19b/§19a; D-24087; EC-333; arena-client test 634→650/typecheck 0/build 0; lobby integration deferred WP-303)"]
        ["WP-305 ✅ Owner-page identity fields — accountId / displayName / handleCanonical on OwnerProfileView + an editable display name; EC-335"]

      Architecture & API Governance
        ["WP-116 ✅ Disconnect & reconnect semantics"]
        ["WP-117 ✅ Client routing strategy"]
        ["WP-118 ✅ HTTP API surface catalog"]
        ["WP-119 ✅ Architecture doc hygiene"]

      Complete-Game Testing
        ["WP-158 ✅ Complete-game regression tests (seed-faithful fixture harness)"]

      Cross-App Infrastructure
        ["WP-180 ✅ Build-time version stamping"]
        ["WP-416 ✅ Provider-independent PostgreSQL backup (pg_dump → R2 daily, CI-driven; D-24236)"]

      Multiplayer Play & Match Durability (2026-07)
        ["WP-306 ✅ Setup-contract per-field ext_id validation (henchman id-space fix) — per-field FlatCard.extId checks in parseLoadoutJson/setupContract; henchmen aren't flat cards; EC-336; D-24091"]
        ["WP-307 ✅ Multiplayer-play authentication gate (soft) — server + app gate the native games create/join behind auth; EC-337; D-24092/24093"]
        ["WP-308 ✅ Multiplayer-play hard gate (close the native-lobby bypass) — server.app.middleware.unshift precedes the bgio lobby router so a raw native create returns 401; EC-338; D-24094"]
        ["WP-309 ✅ Durable boardgame.io match storage — custom StorageAPI.Async over the WP-115 pg.Pool, blob→jsonb in a dedicated bgio schema; survives deploy/restart (root-cause fix for the mid-match freeze); EC-339; D-24095"]
        ["WP-311 ✅ Client reconnect & desync auto-resync — connection store + non-blocking banner + resync() (stop/start re-anchors _stateID); EC-340; D-24096"]
        ["WP-312 ✅ Client move-ack watchdog — arm on submitMove, resync when _stateID doesn't advance within the timeout (storm-guarded); closes the connected-desync freeze; EC-341; D-24097"]
        ["WP-326 ✅ Lobby join list shows only joinable matches — filter stale/finished bgio matches out of the join list (client half of stale-match hygiene); EC-356; D-24112"]
        ["WP-327 ✅ Server-side reaper for stale bgio matches — in-process reaper DELETEs finished (1 h grace) / non-gameover (24 h) rows from the bgio schema; 15 min setInterval, SIGTERM stop; EC-357; D-24113"]

      Hero/Villain Effects & Diagnostics (2026-07)
        ["WP-310 ✅ Empowered multi-class form (by [hc:X] and [hc:Y]) — per-class composition sum on the WP-256 substrate; clears the 8th-wonder empowered hollow; EC-342; D-24098"]
        ["WP-314 ✅ Diagnostic export: card-effect provenance — awaitingPlayerInput + recentlyPlayedCards with an outcome, so a 'froze after card X' report names its own cause; EC-344; D-24100"]
        ["WP-315 ✅ Card ability text in UICardDisplay + diagnostic — optional abilityText populated from the registry at setup; rides every display projection; EC-345; D-24101"]
        ["WP-316 ✅ Villain-deck effect narration (Fight/Ambush/Escape, per-target) — executor returns VillainEffectResult[]; the log names the specific hero; byte-identity held (no new notableEvent); EC-346; D-24102"]
        ["WP-317 ✅ Composable gain-resource grant observability logging — interpretGainResourceNode logs each Empowered/Berserk grant (incl +0); the per-effect amount logging WP-295 deferred; EC-347; D-24103"]

      Live-Play HUD & Pending-Choice UX (2026-07)
        ["WP-313 ✅ Victory-pile villain-pick UX — projects pendingVictoryPileCardPick + a client prompt + End-Turn gate; closes The Ebony Blade hard-freeze; EC-343; D-24099"]
        ["WP-318 ✅ Game log panel in the live play HUD — mounts GameLogPanel in PlayDesktop/PlayMobile fed snapshot.log (was replay-inspector-only); surfaces the WP-316/317 narration during play; EC-348; D-24104"]
        ["WP-319 ✅ Per-target hero naming in the fight/ambush center-screen overlay — enriches the fightResolved/ambushResolved narrative (overlay renders it verbatim, no client change); finalStateHash byte-unchanged; EC-349; D-24105"]
        ["WP-321 ✅ Compact, auto-scrolling chronological game log in the live HUD — ~5-6 line window + polite auto-scroll-to-bottom; replaces the abandoned WP-320 newest-first (D-24106 void); EC-351; D-24107"]
        ["WP-322 ✅ Copy, Save, and full-screen Expand for the live HUD game log — Copy→clipboard / Save→game-log.txt / Expand→Teleport overlay; mirrors PileBrowseModal + DiagnosticExportButton; pure client render; EC-352; D-24108"]
        ["WP-323 ✅ Game log name enrichment: card plays + mastermind tactics — played {Name} ({ext-id}) — {printed effect}; fought {Mastermind} and defeated the tactic {Tactic}; new pure logDisplay.ts; EC-353; D-24109"]
        ["WP-324 ✅ Game log name enrichment: remaining log sites — fights/recruits/dodges/escapes/captures/claims/grants → {Name} ({ext-id}) via formatCardRef; EC-354; D-24110"]
        ["WP-325 ✅ Reveal / What If…? test-result logging — revealed {card} (cost N) — {predicate} matched: {action}; the last silent effect path; new pure hero/revealLog.ts; EC-355; D-24111"]
        ["WP-328 ✅ Turn.step.action log numbering (+ effectProvenance parse fix) — {turn}.{step}.{action} prefix (step = start/main/cleanup) via hash-excluded G.logMeta + central pushLog over 17 player-facing push files; live-only (real onBegin); folds the WP-323/324 effectProvenance ext-id regression; EC-358; D-24114"]
        ["WP-329 ✅ Remove the redundant <ol> ordinal from the HUD game log — GameLogPanel .entries list-style:none so the browser ordinal (167.) stops double-numbering the in-text {turn}.{step}.{action}; pure client CSS; EC-359; D-24115"]
        ["WP-330 ✅ Header username label (play) — useAuthNav fetches the owner profile once on sign-in via existing fetchOwnerProfile and resolves displayLabel: displayName → @handleCanonical → My account (non-blocking, silent-fallback, fetch-once); completes WP-175 Amendment 1 now that WP-305/D-24089 ship the fields; client-only; EC-360; D-24116"]
        ["WP-331 ✅ HUD turn header reads the same turn the log numbers by — uiState.build game.turn = G.logMeta.turn ?? ctx.turn so the header stops showing Turn 20 while the log ends 19.2.13 (play→end phase change bumps ctx.turn; end phase has no onBegin); read-only projection; EC-361; D-24117"]
        ["WP-346 ✅ Header username is the profile link — Header.vue merges the auth-nav-display name + the separate 'My profile' link into one <a href=?route=me> so the header reads Home · Cards · name · Sign out (name is the link); useAuthNav/displayLabel untouched; EC-375; D-24136"]
        ["WP-347 ✅ Cross-subdomain Hanko session cookie — both hankoClient.ts wrappers pass cookieDomain=.legendary-arena.com to register() on production hosts (resolveSessionCookieDomain: localhost/*.pages.dev → undefined) so play/dashboard/www share one sign-in (SSO); enabler for marketing WP-033; EC-377; D-24137 (amends D-16002)"]
        ["WP-348 ✅ Sign-out clears the Domain-scoped session cookie — fixes a WP-347 regression: hanko-frontend-sdk 2.6.0 removeAuthCookie() deletes by name with no Domain, so logout() couldn't remove the .legendary-arena.com cookie and Sign out did nothing; signOutCurrentSession now clearHankoSessionCookie() in a finally (both apps); EC-378; D-24140"]
        ["WP-417 ✅ Every played card prints its effect + the action taken — strips the pipeline's apply-effect-markers from player prose by a corpus-verified shape test (keeps printed keywords), folds a starter's base +recruit/+attack onto the play line (formatBaseEconomyClause), logs the 4 silent onPlay handlers (draw w/ realized count + shortfall, attack, recruit, self-KO), and names a matched-but-guard-blocked reveal action (the WP-B.2 reveal realized-results slice); message-text only, finalStateHash unchanged; EC-452; D-24237"]
        ["📦 Effect-outcome fill-in (WP-B.2) — reveal-action realized results LANDED (WP-417); remaining move-card/sequence empty-source no-ops still deferred per D-24111"]
        ["📝 Structured log-outcome contract + colour-coding (WP-B.3) — DESIGN LANDED 2026-07-25 (DESIGN-LOG-OUTCOME-CONTRACT.md + D-24253): G.messages string array → LogEntry[] records carrying a coarse LogOutcome (neutral/applied/partial/blocked → green/red/yellow); decomposed into B.3a engine-contract (two-session) + B.3b client-colour + B.3c effectProvenance-retirement; retires the D-24100 heuristic"]
        ["WP-434 ✅ Structured Log-Outcome Engine Contract (WP-B.3a) — G.messages/UIState.log string[]→LogEntry[] + drift-detected LOG_OUTCOMES enum + pushLog(…, outcome='neutral'); bounded outcome-bearing emissions opt in, 'played X' stays neutral; single fixture regenerated, finalStateHash unchanged (D-24081); arena-client renders entry.text visually-unchanged (colour=B.3b); implements D-24253; EC-469"]
        ["WP-435 ✅ Colour-Code the Game Log by Outcome (WP-B.3b) — GameLogPanel colours each line by LogEntry.outcome (applied→green/partial→amber/blocked→red/neutral unstyled) via theme-aware --color-par-* tokens; colour is never the only signal (aria-hidden glyph + sr-only word, design §Fork E); static/reduced-motion; new pure logOutcomeDisplay helper; buildGameLogText [outcome] tag; arena-client only, D-24026 REQUIRED; implements D-24253; EC-470"]
        ["WP-436 ✅ Retire the effectProvenance Outcome Heuristic (WP-B.3c) — the freeze-diagnostic's recentlyPlayedCards[].outcome reads the authoritative LogEntry.outcome (blocked→conditionNotMet, applied/partial→resolved) instead of string-matching 'did not activate'; deletes DID_NOT_ACTIVATE_LINE; keeps awaitingPlayerInput + structured hollowEffects read + played-card identification parse; 4-value output contract preserved; arena-client only, D-24026 N/A; implements D-24253 §Fork F; EC-471; FINAL log-outcome slice"]
        ["WP-438 ✅ Structured LogEntry.card — resolves the B.3c §14 residual: adds optional card?:CardExtId to LogEntry (engine populates on played/hero-effect/hollow lines; reveal-outcome carries the revealed card), so effectProvenance reads entry.card for the ext-id + condition-fail association and RETIRES the fragile PLAYED_LABEL_EXTID extractor (broke WP-328/417) + the B.3c (ext-id) substring; PLAYED_LINE line-kind kept (LogEntry.kind = next increment); additive/optional, log renders identically, finalStateHash unchanged; cross-layer atomic; reserves D-24257; EC-473"]

      Competitive Score Submission & Verification (2026-07)
        ["WP-332 ✅ Competitive score submission HTTP endpoint — the score-submission route lands in apps/server (12 route tests + wrapper delegation; competition.logic.ts additive-only); opens the play-to-leaderboard loop; EC-362; D-24118"]
        ["WP-333 ✅ Seat → account identity persistence at match join — migration 024 stamps which account held which seat (UPSERT re-stamp idempotent, FK-guarded); identity substrate for replay ownership; EC-363; D-24120"]
        ["WP-334 ✅ Server-layer faithful reducer-replay — reads a completed match's bgio initialState + log and re-executes through boardgame.io's own reducer (D-24119 carve-out); golden test proves reduced final G equals live final G; EC-364; D-24121"]
        ["WP-335 ✅ Live-match capture harvester — migration 025; captures finished matches' replay artifacts + replayHash→matchId mapping + scenarioKey + owners; EC-365; D-24122"]
        ["WP-336 ✅ Competitive verifier repointed onto the faithful reducer path — reduceReplayByHash / readReplayArtifactByHash; competition DB suite proves the full pipeline; EC-366; D-24123"]
        ["WP-337 ✅ Turns-native competitive scoring — retires the moveCount-as-rounds proxy; turnCount flows engine→server and the rawScore recompute proves it end-to-end; EC-367; D-24125"]
        ["WP-338 ✅ Submit-by-matchId submission + on-demand capture + GET /api/me/scores — a finished match submits by matchId (capture-on-demand + auto-publish + idempotent re-submit); EC-368; D-24126"]
        ["WP-339 ✅ Arena-client submit-after-match + 'My Scores' profile view — competitionApi + useCompetitiveSubmitOnGameover; client-only; EC-369; D-24127"]
        ["WP-340 ✅ Competitive verifier co-owner hardening — by-account ownership lookup so a non-first co-owner's submit verifies; EC-370; D-24128"]
        ["WP-341 ✅ Play-route session hydration (on-gameover submit fix) + restored My-Scores view — client-only; cleared a pre-existing vue-tsc red on main; EC-371; D-24129"]

      Gauntlet Leaderboards (Legends) (2026-07)
        ["WP-342 ✅ Mastermind set-gauntlet boards (server) — outcome persistence + gauntlet read-layer + legends publisher emitting per-gauntlet board snapshots; EC-372; D-24131"]
        ["WP-343 ✅ Legends-board gauntlet index + board panel (client) — SPA renders the gauntlet index + per-board standings; hash-route grammar locked; cleared the pre-existing legends-board vue-tsc red; EC-373; D-24131/D-24135"]
        ["WP-344 ✅ Player-count gauntlet boards (server) — migration 027 player_count column; roster-keyed standings per count from one query; publisher adds lazy per-count boards + index entryCounts/legs; EC-376; D-24134 server half"]
        ["WP-345 ✅ Player-count gauntlet boards + challenge links (legends-board client) — player-count selector, full-roster display, 'Challenge this leg' links into the WP-114 registry-viewer preview; EC-379; D-24134 client half"]
        ["WP-384 ✅ Fixed-hero-pool gauntlet division (server) — migration 034 team_key column + SQL-jsonb artifact backfill (D-24187 carve-out) + one-query {open, fixed} standings (heroCount + 2 budgets riding the catalog; exact-optimum subset search, cap 12 logged) + lazy -fixed board emission + heroPool/fixedEntryCounts; EC-413; D-24187 server half"]
        ["WP-385 ✅ Fixed-hero-pool gauntlet division (legends-board client) — Open | Fixed-Pool Championship division toggle (division = the route), Hero Pool column + championship subtitle on fixed boards, feeder line, claimed-only ★ index chips, unclaimed-guard extension for -fixed deep links; EC-414; D-24187 client half — completes the arc"]
        ["WP-387 ✅ Scenario preview deep-link carries player count (registry-viewer + legends-board) — Shape A of 'play this scenario from the leaderboard': the gauntlet challenge link carries the board's player count into the cards builder so the WP-372 required-count readout matches; new parsePlayerCountFromUrl (envelope, 1..5 else null) + App.vue seeds the editor draft setPlayerCount at mount + buildChallengeUrl optional playerCount; NOT a ?lagn= switch (a seed can't be a valid LAGN); no D-entry (the WP-114 future-extension hook); Shape B save-to-profile deferred; D-24026 dev-verified; EC-416"]
        ["WP-440 ✅ Gauntlet Pack contract (registry; first WP of the Mastermind Gauntlets download → import → build → track epic. Adds an identity-only Gauntlet Pack contract to packages/registry — a new src/gauntletPack.ts exporting GAUNTLET_PACK_VERSION 1, a strict GauntletPackSchema, a pure buildGauntletPack, and validateGauntletPack — plus the ./gauntletPack subpath export and index.ts re-export, mirroring gauntletLoadouts. The pack is the tiny download token naming WHICH gauntlet a player starts: pack_version plus gauntlet setAbbr, mastermindSlug, division fixed|open, playerCount 1..5, and nothing else — it carries NO legs, NO hero picks, NO approved compositions; the server re-resolves all of those from the live registry at import, WP-5, so no pack/approved-loadout drift is even possible. Forward-compat via a pack_version major-reject gate — an unknown major version and unknown fields are rejected with a clear full-sentence error, not lax-parsed — locked by D-24260. Reuses GauntletLoadoutMenu identity fields and SupportedPlayerCount. Strictly additive: a new contract file, no existing registry contract modified; validation is SHAPE-only, existence/leg/composition resolution is the server's job at import. Unit tests round-trip a core/magneto identity pack and assert identity-only, no legs/heroes/compositions, plus the version/field/count/division reject paths. Deliberately NOT a LAGN document, a multi-leg container breaks LAGN's one-match invariant. Registry layer only: zod plus Node built-ins, no engine/server/pg/apps import. Section 17 Vision, sections 20-26 leaderboard-adjacent, No conflict, the pack stores/derives no scoring and the server stays authoritative; section 20 N/A no funding surface; section 21 N/A registry only, no apps/server endpoint. User-visible surface none — infrastructure, D-24026 inverted, STATUS states No user-observable change. No hard-dep WP, epic #1. Reserves D-24260; EC-475; standard two-session lane, drafted 2026-07-27 off origin/main cc206e8c, not yet executed. The epic supersedes the D-24131 §8b Gauntlet-progress-on-profiles backlog line in scope, reference only, reconciled by a later epic WP)"]
        ["WP-441 ✅ Legends Gauntlet pin + download (apps/legends-board; second WP of the Mastermind Gauntlets download → import → build → track epic, the smallest shippable user-visible zero-API slice. Two additions to legends.legendary-arena.com: pin the Core Set / Magneto gauntlet to the top of the Mastermind Gauntlets index as the showcase example, a display-only reorder via a new pure pinShowcaseGauntlet helper applied AFTER groupGauntletsBySet whose order-preserving contract stays untouched, absent core/magneto returns an unchanged copy and never mutates the input; and a per-row Download Mastermind Gauntlet control with a player-count 1..5 plus division fixed|open selector defaulting to solo 1 plus fixed that builds the WP-440 identity pack client-side from the already-cached gauntlet-index.json entry setAbbr plus mastermindSlug and the selector, then Blob/anchor-downloads gauntlet-setAbbr-mastermindSlug-division-pN.gauntlet.json, mirroring matchResultDownload.ts minus the fetch. No server call, no endpoint, no snapshot or publisher change, no migration, no packages/registry edit. Zero-API and vue-sole-runtime invariant preserved, WP-343/345: the pack is built INLINE as a plain literal pack_version 1 plus gauntlet, satisfies GauntletPack, with the WP-440 contract imported type-only and @legendary-arena/registry added to devDependencies only, mirroring @legendary-arena/lagn, so nothing enters the runtime bundle, enforced by a grep of dist for legendary-arena/registry returning zero matches, the EC-164 precedent. New gauntletPackDownload.ts and .test.ts plus modified gauntletDisplay.ts and .test.ts, GauntletIndexPanel.vue, and package.json. Section 17 Vision sections 20-26 leaderboard-adjacent No conflict; section 20 N/A no funding surface; section 21 N/A App only no apps/server endpoint. User-visible surface legends.legendary-arena.com, D-24026 live-verify operator-pending: Magneto renders first, the download yields a valid .gauntlet.json parsing against the WP-440 GauntletPackSchema, zero API calls. Reserves D-24261; EC-476; standard two-session lane, drafted 2026-07-27 off origin/main 01ec0a27, not yet executed. Hard-dep WP-440 done)"]
        ["WP-442 ✅ Extract shared gauntlet-truth helper (apps/server; third WP of the Mastermind Gauntlets download → import → build → track epic. Behavior-preserving refactor: extract the clause a-g leg-clear qualification predicate plus findBestPoolAssignment, the fixed-division pool-assignment search, out of apps/server/src/legends/gauntlet.logic.ts into a new pure, data-injected, layer-locked gauntletTruth.logic.ts, and rewire getGauntletStandings to consume them with ZERO behavior change. The new module exports qualifiesAsLegClear taking a LegClearReplayFacts value object plus approvedLoadouts plus a resolved publishedScoringConfigVersion number-or-null, LegClearReplayFacts, matchesApprovedLoadout, findBestPoolAssignment with its four private helpers collectDistinctTeams selectBoundedTeams evaluatePoolUnion pickBestTeamForLeg plus FIXED_POOL_TEAM_CAP 12 moved verbatim, and the moved-and-exported RosterLegAccumulator interface; selectBoundedTeams and findBestPoolAssignment decouple from GauntletDefinition by taking a precomputed boardName string so the new module needs no board-name-builder edge, the cap-warning string stays byte-identical. The moved internals have no external consumers, only buildGauntletCatalog and the GauntletDefinition type cross the module boundary and neither moves; getGauntletStandings keeps its exact public signature. Primary correctness gate: the existing gauntlet.logic.test.ts passes UNCHANGED, the behavior-preservation proof, since it exercises only the public surface and imports none of the moved internals; a new gauntletTruth.logic.test.ts unit-tests the extracted predicate, matchesApprovedLoadout, and findBestPoolAssignment in isolation against seeded row fixtures. Lands before the run persistence and import API so the rule engine, not the endpoint, defines the shape WP-5's per-run derived read reuses, so tracker and leaderboard cannot drift on what clears a leg or crowns a champion. Layer lock: the new module imports nothing from registry, game-engine, preplan, pg, or boardgame.io, only Node built-ins and type-only sibling imports GauntletApprovedLoadouts. No reduce in the extracted or rewired logic, the owner-visibility list is built with an explicit for-of. Runtime edge is one-directional gauntlet.logic to gauntletTruth, the reverse is type-only so no import cycle. Section 17 Vision sections 20-26 plus 22, No conflict, determinism-preserving proven by the unchanged suite; section 20 N/A no funding surface; section 21 N/A no apps/server endpoint added or modified, getGauntletStandings is not a catalogued Library-only function. NO D-entry, this is a behavior-preserving refactor that inherits the existing layer lock from gauntlet.logic.ts and D-24187/D-24199 and locks no new decision. User-visible surface none — infrastructure, D-24026 inverted, STATUS states No user-observable change. EC-477; standard two-session lane, drafted 2026-07-27 off origin/main ff812ae1, not yet executed. Hard-dep none, WP-4 and WP-5 depend on this)"]
        ["WP-443 ✅ Gauntlet run persistence, player_gauntlet_runs (apps/server / migration; fourth WP of the Mastermind Gauntlets download → import → build → track epic, the account-local run-workspace storage. New migration 039_create_player_gauntlet_runs.sql, 039 because 038 is add_driver_owner_to_match_bot_ally, creating ONE table legendary.player_gauntlet_runs: id uuid PK gen_random_uuid, player_id bigint NOT NULL REFERENCES legendary.players player_id ON DELETE CASCADE, set_abbr text, mastermind_slug text, division text CHECK IN fixed|open, player_count smallint CHECK BETWEEN 1 AND 5, leg_picks jsonb NOT NULL DEFAULT the empty object mapping schemeSlug to heroDeckIds the single authoritative hero state, created_at, updated_at, and a write-once nullable first_completed_at. A partial-unique active-run index UNIQUE player_id set_abbr mastermind_slug division player_count WHERE first_completed_at IS NULL — a player may run multiple counts or divisions concurrently but not two active identical runs, and a completed run frees the slot — plus a player_id listing index. Maximally derived: NO status column, NO hero_pool column, NO child table, NO history or flag or pool-validity or champion table; status, pool, budget headroom, champion, and where-you-left-off are ALL derived at read time WP-5 from leg_picks plus competitive_scores, never stored, locked by D-24262 the load-bearing derived-progression lock of the whole epic. Run legs live in leg_picks NOT in player_loadouts so the 50-loadout cap D-24086 is untouched. first_completed_at is a write-once audit plus archive-boundary stamp set the first time a read derives champion, NEVER read as championship truth since every read re-derives champion from competitive_scores. Optional minimal apps/server/src/gauntlet/gauntletRun.types.ts a plain row-shape types file NOT a repository, plus a DB-gated gauntletRun.migration.test.ts that applies the migration and asserts the table plus columns plus both indexes exist, that a second active run of the same identity while first_completed_at IS NULL is blocked but a new active run succeeds once the first is completed, the division and player_count CHECKs reject bad input, and deleting the player cascades. Persistence boundary respected: G and ctx untouched, this is ordinary legendary.* domain storage the player_loadouts precedent, NOT a snapshot or save-game or bgio framework store, no carve-out added. Section 17 Vision section 19b account-local saved content plus 20-26 leaderboard-adjacent No conflict; section 20 N/A no funding surface; section 21 N/A no apps/server endpoint, the run API is WP-5. Reserves D-24262; EC-478; standard two-session lane, drafted 2026-07-27 off origin/main 61bef39a, not yet executed. Parallel-safe with WP-440 and WP-441; supersedes the D-24131 §8b Gauntlet-progress-on-profiles backlog line in scope; enables WP-5. Hard-dep none)"]
        ["WP-444 ✅ Registry-Viewer gauntlet-pack import (apps/registry-viewer; fifth WP of the Mastermind Gauntlets download → import → build → track epic, the cards-side consumer of the identity pack. Added after a live product test: Jeff downloaded the live legends pack pack_version 1 gauntlet core magneto fixed p1 and loaded it into the cards.legendary-arena.com Registry-Viewer builder, which errored with 5 schema issues because the builder's existing importers expect a full LAGN doc or a MATCH-SETUP composition and the identity-only pack, WP-440 / D-24260, carries neither, it had no consumer on that surface. This WP teaches the builder to ingest a WP-440 identity pack: a new dedicated Load Gauntlet Pack paste or file importer, a third one beside the existing Load JSON MATCH-SETUP and Load LAGN boxes, strictly validates a pack_version plus gauntlet object via the registry's validateGauntletPack, then presents a leg scheme picker listing the schemes of the pack mastermind's home set, the gauntlet's legs per WP-342 / D-24131 both-sides-same-set. Picking a leg resolves the approved villain/henchmen composition for setAbbr, mastermindSlug, playerCount from getGauntletLoadoutMenu / GAUNTLET_LOADOUT_MENUS, default variant 0 optionally selectable, and prefills the builder draft schemeId, mastermindId, villainGroupIds, henchmanGroupIds, playerCount, leaving heroDeckIds EMPTY for the user to fill, bring your own heroes. The four supply-pile counts bystanders/wounds/officers/sidekicks stay at the builder's createBlankDraft defaults 30/30/30/0 — PLAYER_COUNT_SETUP carries only heroCount, NOT the four supply counts, a factual correction to the plan framing; and heroCount, since heroes are empty, only informs the existing required-count readout. Villains/henchmen are the AUTHORITATIVE approved variant, they override setMastermind's auto-added Always-Leads set so the leg matches the approved menu, not the mastermind default. Ids are set-qualified setAbbr/slug ext_ids, D-10014, matching the loadout menu and composition id-space. Zero-API preserved: all resolution is client-side from the registry the viewer already bundles, registry is a runtime dependency here not type-only, value-imported via narrow subpaths gauntletPack, gauntletLoadouts, playerCountSetup, never the root barrel which pulls Node built-ins and breaks the browser build; no server call, no endpoint, no snapshot change. A pack naming an unknown gauntlet no loadout menu or an unoffered player count resolves to a clear friendly message via a discriminated ok false reason unknown-gauntlet / unoffered-count / unknown-variant result, NOT a crash and NOT the raw Zod schema-error wall. New pure data-injected loadoutGauntletPackImport.ts plus .test.ts, parseGauntletPack plus resolveGauntletLegLoadout plus listGauntletLegSchemeIds, never throw, plus modified LoadoutBuilder.vue. No server/endpoint/persistence/migration, no packages/registry edit, no change to WP-440's pack contract, no ?pack= URL deep link out of scope, no hero prefill, no match launch. Section 17 Vision section 10a Registry Viewer public surface plus 20-26 leaderboard-adjacent, No conflict; section 20 N/A no funding surface; section 21 N/A App only no apps/server endpoint, the play-side import is WP-5. User-visible surface cards.legendary-arena.com, D-24026 live-verify operator-pending: the leg picker renders, picking a leg prefills the draft with heroes empty, zero API calls, an unknown-gauntlet pack shows the friendly message. Reserves D-24263; EC-479; standard two-session lane, drafted 2026-07-28 off origin/main cef4f0a6, not yet executed. Hard-deps WP-440 done plus WP-395 done)"]
        ["WP-445 ✅ Gauntlet run import + run-CRUD API (apps/server; sixth WP of the Mastermind Gauntlets download → import → build → track epic, the play-side / play.legendary-arena.com consumer of the WP-440 identity pack. SPLIT off the plan's #5 import + run API: this WP is import + run CRUD only, the DERIVED-progression read is the queued follow-on WP-446. Four authenticated endpoints under /api/me/gauntlet-runs mirroring loadoutLibrary routes/logic/types for auth, player_id scoping, Cache-Control no-store, and typed error codes. POST: requireAuthenticatedSession then requireUnsuspendedAccount, validate a WP-440 identity pack via validateGauntletPack to a 400 invalid_pack, confirm the gauntlet plus approved loadout menu exists for setAbbr, mastermindSlug, division, playerCount via an INJECTED gauntlet-existence resolver built in server.mjs from the already-built gauntletCatalog to a 422 unknown_gauntlet, then INSERT an active run into legendary.player_gauntlet_runs with EMPTY leg_picks. IDEMPOTENT: catch the partial-unique active-run conflict player_id set_abbr mastermind_slug division player_count WHERE first_completed_at IS NULL and return the existing active run 200 instead of erroring on the unique violation per D-24264, a brand-new insert returns 201, and a fresh POST after first_completed_at is set opens a NEW active run. GET returns the caller's RAW stored runs, a GauntletRunView of identity plus legPicks plus timestamps plus firstCompletedAt, with NO derived projection, the 5-state status plus hero pool plus budget headroom plus per-leg cleared plus last-played leg via gauntletTruth.logic.ts is the WP-446 follow-on. PATCH /:id edits leg_picks, structural shape only, an object of schemeSlug to heroDeckIds array, NO gameplay validity check on hero ids, and advances updated_at. DELETE /:id removes. All player_id-scoped, a cross-account or malformed or missing :id returns 404 not_found with no existence leak, and the :id is UUID-guarded before any ::uuid cast so a malformed id never 500s. New apps/server/src/gauntlet/gauntletRun.routes.ts plus gauntletRun.logic.ts plus gauntletRun.logic.test.ts, additive API types in the existing WP-443 gauntletRun.types.ts, the GauntletRunErrorCode closed union unauthorized account_suspended invalid_pack unknown_gauntlet invalid_leg_picks not_found plus its canonical array, GauntletRunView, GauntletRunResult, ImportGauntletRunInput, UpdateGauntletRunPatch, GauntletRunRouteDependencies, and the injected GauntletExistenceResolver, with the row-shape types reused unchanged, plus server.mjs wiring under the 01.5 runtime-wiring allowance. Server may import legendary-arena/registry validateGauntletPack plus pg plus Node built-ins, and NOT game-engine, preplan, boardgame.io, or any apps/* package, gauntlet existence is INJECTED so the logic layer never builds the registry catalog and stays DB-test-friendly. Persistence: ordinary legendary.* domain storage the player_loadouts precedent, no G or ctx read or write, no bgio-blob read, no snapshot, no carve-out, and stores NOTHING derived per D-24262. No migration since 039 exists, and this WP never writes first_completed_at since every run it creates is active. DB-gated tests loud-skip without TEST_DATABASE_URL: import-creates-one with empty leg_picks, re-import-attaches at 200 with one row, invalid-pack, unknown-gauntlet, PATCH-updates and updated_at advances, DELETE-removes, cross-account 404, malformed-id 404, and the error-code drift assertion. Section 17 Vision section 3 identity plus section 11 ownership plus section 19b account-local saved content plus sections 20-26 leaderboard-adjacent, No conflict, no scoring or derived state is stored and the server stays authoritative. Section 20 N/A no funding surface. Section 21 / D-11804 APPLIES, api-endpoints.md gains four Wired authenticated-session-required rows with Authorizing WP WP-445. Reserves D-24264, the import-idempotency plus run-API-shape lock, complementing D-24262's no-stored-derived-state lock. User-visible surface none — infrastructure, D-24026 inverted, STATUS states No user-observable change, the payoff is WP-446's derived read plus the WP-7 profile tracker consuming these endpoints. EC-480, standard two-session lane, drafted 2026-07-28 off origin/main 9c788d98, not yet executed. Hard-deps WP-440 done plus WP-442 done plus WP-443 done)"]
        ["WP-446 ✅ Gauntlet run derived progression read (apps/server; seventh WP of the Mastermind Gauntlets download → import → build → track epic, the DERIVED read WP-445 deferred, on play.legendary-arena.com). Enhances GET /api/me/gauntlet-runs so each run carries its DERIVED progression computed at read time — endpoint path/method/auth/cache unchanged and POST/PATCH/DELETE untouched, only the GET Response schema grows — storing NOTHING per D-24262. A GauntletRunProgressView extends the raw WP-445 GauntletRunView with status, pool the sorted union of the run's leg_picks hero ids, budgetHeadroom the fixed budget heroCount plus 2 minus the pool size, heroCount, budget, isChampion, and per-leg legs each schemeId schemeName cleared hasFullPicks lastPlayedAt. Per-leg cleared reuses qualifiesAsLegClear and isChampion reuses findBestPoolAssignment from WP-442's gauntletTruth.logic.ts VERBATIM — the single source of truth so the tracker and the leaderboard cannot diverge on cleared-a-leg or champion, re-implementing either is a FAIL — with the SAME injected inputs the leaderboard's getGauntletStandings uses: approvedLoadouts plus the published scoring-config version resolved from leaderboardDependencies.checkParPublished plus the injected heroCount poolBudget and boardName, all built in server.mjs from the startup gauntletCatalog plus PLAYER_COUNT_SETUP so the logic layer never imports the registry. The read is caller-scoped: one caller-centric RosterLegAccumulator is built from the caller's own qualifying replays at the run's player_count keyed by team_key hero-identity, intentionally distinct from getGauntletStandings' per-roster grouping, and for a solo player_count 1 run the two coincide which the cross-check test asserts. The 5-state status needs-heroes no leg has heroCount picks, ready at least one leg fully picked and 0 cleared, playing at least one cleared but not all, all-legs-cleared every leg cleared but NO budget-valid pool, champion every leg cleared AND a budget-valid fixed pool — evaluated in that order, and all-legs-cleared and champion are DISTINCT states, the invisible-failure class the epic exists to make legible. A GauntletRunStatus closed union plus a drift-asserted GAUNTLET_RUN_STATUSES canonical array. New apps/server/src/gauntlet/gauntletRunProgress.logic.ts a pure deriveGauntletRunStatus plus a pure deriveGauntletRunProgress over injected inputs and fetched rows plus a DB entrypoint listGauntletRunProgress that reuses listGauntletRuns and runs one caller-scoped competitive_scores read per run, plus gauntletRunProgress.logic.test.ts. Additive GauntletRunProgressView types in the existing WP-445 gauntletRun.types.ts authorized by D-24265, mirroring the WP-445 to WP-443 extension under D-24264; the GET handler in gauntletRun.routes.ts delegates to the new logic while POST/PATCH/DELETE stay byte-unchanged; server.mjs wiring under the 01.5 runtime-wiring allowance; and the api-endpoints.md GET row is replaced whole per D-11804 section 21. Server may import the sibling server truth helper plus pg plus Node built-ins, and NOT game-engine, preplan, boardgame.io, the registry, or any apps/* package in the logic layer since inputs are injected. READ-ONLY: no migration, no column, no cache, no write path, it never writes first_completed_at, and the score fold uses for...of not reduce. DB-gated tests loud-skip without TEST_DATABASE_URL: all 5 status states, champion versus all-legs-cleared, per-leg cleared plus last-played, budget headroom, and the agrees-with-getGauntletStandings solo cross-check, plus a no-DB pure status and drift test. Section 17 Vision section 3 identity plus section 11 ownership plus sections 20-26 leaderboard-adjacent, No conflict, stores no scoring or derived state and reuses the authoritative leaderboard truth logic. Section 20 N/A no funding surface. Section 21 / D-11804 APPLIES, the GET row Response schema changes, replace-whole-row, Authorizing WP WP-446. Reserves D-24265 the single-run derived-progression-semantics lock, complementing D-24262 no-stored-derived-state and D-24264 import-and-API-shape. User-visible surface none — infrastructure, D-24026 inverted, STATUS states No user-observable change, the payoff is the WP-7 profile tracker consuming this derived read. EC-481, standard two-session lane, drafted 2026-07-28 off origin/main 85d096d9, not yet executed. Deferred design forks flagged for the operator: caller-centric versus per-roster aggregation for multiplayer runs, and the uniform fixed-pool treatment of open runs with the open-division badge deferred to a future WP-8. Hard-deps WP-442 done plus WP-443 done plus WP-445 done)"]
        ["WP-447 ✅ Villain and henchman scry-ko-own-deck Fight effect (packages/game-engine plus card-data generated artifact). Implements the first self-deck scry mechanic in the villain and henchman effect vocabulary, closing the dead Doombot Legion Fight Jeff reported from a live Magneto match, look at the top two cards of your deck, KO one of them and put the other back, for which D-24266 shipped only the unmarked-ability observability breadcrumb. Appends ONE no-param primitive scry-ko-own-deck to the closed VillainEffectPrimitive union plus the VILLAIN_EFFECT_PRIMITIVES canonical array at position 6, append-only, with the bidirectional drift test moved together, extends parseParameterizedEffect to accept it as a no-param token that rejects a trailing colon token, and adds a villainEffectScryKoOwnDeck handler registered in VILLAIN_EFFECT_HANDLERS plus a new deterministic selectScryKoTarget with worst-first priority, first a Wound, else a starting SHIELD card Trooper or Agent starter-first then lex-asc, else the lexically-lowest ext id. Wounds are KO-preferred here because this thins the deck, deliberately unlike selectKoHeroTarget which excludes Wounds in the KO-a-Hero pool. The handler looks at min two of the current defeating player deck, KOs exactly one, and leaves the other on top of the deck in original order, an empty deck is a reachable no-op that is never hollow. Marks Doombot Legion Fight line effect scry-ko-own-deck in the curated villain-effect-markers.json and adds the primitive to the apply-effect-markers.mjs hand-synced local primitives array since the script does not import the engine union, then regenerates the marker onto data cards core and co2e only via apply-effect-markers.mjs with no hand-edit. The msp1 look-2 KO-1 card is a differently-named henchman Hammer Drone Army with a bracket keyword Fight prefix that is not a recognized timing line, deferred to a follow-on not silently dropped. AUTO-RESOLVED, NO pending-choice, NO UIState projection, NO prompt, NO End-Turn gate, and NO bot getLegalMoves change, the interactive player-picks-which-to-KO upgrade is the queued follow-on, the exact koHeroCurrentPlayer WP-185 auto to WP-242 interactive path, because shipping a block-all pending state without its UX hard-freezes the client. Determinism-adjacent, no ctx.random, no I O, no new G field, deck and KO mutation via zoneOps helpers and no reduce, Verification pins replay-hash handling with regen-with-note only if a fixture game defeats a Doombot. Section 17 gameplay fidelity, No conflict. Section 20 N A no funding surface. Section 21 N A no apps server endpoint or catalogued library-only function. Reserves D-24267 the scry-ko-own-deck primitive plus auto-resolve decision. User-visible surface game log plus KO pile, D-24026 live-verify operator-pending, a driven match or fixture fighting a Doombot logs Fight effect naming the KO'd card. Done 2026-07-30, game-engine 2112 of 2112 pass and whole-repo build green, the two Doombot rows core and co2e flip in the villain mechanic ledger from unmarked to executable, NO fixture re-pin since no record-game or replay fixture defeats a Doombot, and setup villainAbility.setup.ts needed no code change because the generic no-param parse branch already accepts the new primitive with AC-2 locked by a setup test. EC-482, standard two-session lane, drafted 2026-07-28 off origin main ee5a7817. Hard-deps D-24266 done plus WP-252 done)"]
        ["WP-448 ✅ Extract the reusable composition to match launch primitive (apps/arena-client; eighth WP of the Mastermind Gauntlets download to import to build to track epic, the reusable launch primitive the WP-7 tracker will call). Behavior-preserving arena-client refactor: extract the create-and-join-from-a-composition chain, createMatch config playerCount authToken then persistMatchSetup matchID config then joinMatch matchID seat 0 playerName authToken then navigate match id player 0 credentials, out of apps/arena-client/src/lobby/LobbyView.vue submitFromJson the loadout-JSON path AND the byte-identical submitCreate the manual-form path into a NEW reusable module src/lobby/useCreateMatchFromComposition.ts exporting a never-throw async launchMatchFromComposition config playerCount playerName authToken that returns a typed ok true matchID or ok false message, failure message byte-identical to the current Failed to create and join the match, and rewire LobbyView to consume it as the SINGLE source with ZERO behavior change, the existing arena-client lobby suite passes UNCHANGED as the correctness gate, editing a lobby test to pass is a behavior change and a FAIL. submitCreate keeps buildConfig plus parsePositiveInteger inside its own try so the pre-launch throw to catch parity holds. Explicitly out, createWithBotAlly bot endpoint different message, joinExisting join-only, startAutoplay autoplay endpoint, and any loadout deep-link play. Layer-clean, the module imports only ./lobbyApi createMatch and joinMatch, ../diagnostics/matchSetupSession persistMatchSetup, and type MatchSetupConfig type-only, no new npm dep, no runtime registry or server or pg import, MatchSetupConfig passed through unrenamed. New useCreateMatchFromComposition.ts and .test.ts with stubbed fetch isolation tests, success create then join seat 0 then nav then ok true, and create-fails then ok false with the locked message and no join, plus modified LobbyView.vue. This is the primitive the future WP-7 gauntlet-leg launch will call, a leg approved composition plus picked heroes to launchMatchFromComposition, and it delivers the launch mechanism the long-deferred WP-303 Load into lobby play a saved loadout placeholder needs, a future WP-303 becomes a call site not a launch-chain build. Section 17 Vision section 3 identity plus section 19b account-local saved content plus 20-26 leaderboard-adjacent, No conflict, behavior-preserving, the account gate and create-join contract unchanged. Section 20 N A no funding surface. Section 21 N A no apps server endpoint or catalogued library-only function, reuses the existing createMatch and joinMatch client helpers. Reserves D-24268, the composition to match launch is a single reusable primitive, narrows the WP-303 placeholder launch mechanism, D-24087 Profile Loadout Library unaffected. User-visible surface none infrastructure, D-24026 inverted, STATUS states No user-observable change, the payoff is WP-7 plus a future WP-303 call site. EC-483, standard two-session lane, drafted 2026-07-28 off origin main 71a90213, not yet executed. Hard-dep none, parallel-safe with the epic, enables WP-7)"]
        ["WP-449 ✅ Profile gauntlet tracker UI plus Play-this-leg launch (apps/arena-client plus a minimal additive apps/server extension; final WP of the Mastermind Gauntlets download to import to build to track epic, the play-side route me tracker). Adds a Gauntlet Runs section to apps/arena-client/src/pages/MyProfilePage.vue plus a new never-throw client API module lib/api/gauntletRunApi.ts with typed Bearer wrappers for the four WP-445 api me gauntlet-runs calls, mirroring loadoutLibraryApi.ts. Delivers import via file picker plus drag-drop plus paste-JSON JSON.parse-guarded to POST with friendly invalid pack and unknown gauntlet copy, the active tracker from the WP-446 GauntletRunProgressView with the 5-state status rendered so all-legs-cleared and champion are VISIBLY DISTINCT, champion celebratory and all-legs-cleared cleared but the winning teams do not fit one budgeted pool with the budget and headroom shown, the emerging hero pool plus budget headroom, per-leg rows scheme name plus cleared chip plus hasFullPicks plus lastPlayedAt, and the derived where you left off leg highlighted, per-leg hero picks PATCH plus Play this leg assembling a MatchSetupConfig from the picked heroes plus a server-supplied launch block to the WP-448 launchMatchFromComposition primitive, on gameover the existing auto-submit records the score and the tracker updates on next load, plus completed history and delete reset. KEY DESIGN RESOLUTION operator-reviewed D-24269, arena-client CANNOT import the registry so villains and henchmen from approved variant 0 D-24199 plus the four supply counts which have no registry source come from a minimal additive server extension, GauntletRunProgressView gains a derived launch block that is a GauntletRunLaunch or null, no new endpoint no migration no contract-field rename, the client derives only schemeId set-qualified plus mastermindId plus heroDeckIds from legPicks. Supply counts do NOT gate leg-clear D-24187 so canonical fixed server-wiring constants suffice. Operator forks flagged, fold-vs-split of the server half, the supply-count source fixed constants vs a new registry table, and the deferred variant selector. Section 21 triggered, the GET api me gauntlet-runs response gains launch so the catalog row is replaced wholesale. Derived-display only D-24262, the client never recomputes status champion or pool. Section 17 Vision section 3 identity plus 11 plus 19b plus 20-26, No conflict, renders derived server truth the account gate unchanged. Section 20 N A no funding surface. User-visible surface play legendary-arena com route me, D-24026 live-verify operator-pending, import a Magneto pack then the tracker renders then pick heroes then Play this leg then the cleared chip updates and all-legs-cleared is distinct from champion. Reserves D-24269. EC-484, standard two-session lane cross-layer, drafted 2026-07-28 off origin main 7ebb8375, not yet executed. Hard-deps WP-440 plus WP-445 plus WP-446 plus WP-448)"]
        ["WP-450 ✅ Villain gain-attached-hero Fight effect, the D-24266 false-positive fix (packages/game-engine plus card-data generated artifact). Corrects a false positive in the D-24266 breadcrumb surfaced by a live core game: Fight Gain that Hero villain lines were flagged unmarked-ability even though the effect fires, because the captured-hero return is handled GENERICALLY at the fight site by awardAttachedHeroes from WP-431, not via a villain-effect hook, so the line is markerless and D-24266 mis-reports it as unhandled, and it fires on every capture-villain defeat across 3 villains. Appends ONE no-param primitive gain-attached-hero to the closed VillainEffectPrimitive union plus the VILLAIN_EFFECT_PRIMITIVES array, append-only, with the position resolved against main per the WP-447 ordering coordination since both WPs append one primitive, and the bidirectional drift test moved together. Extends parseParameterizedEffect to accept it as a no-param token, and adds a villainEffectGainAttachedHero handler registered in VILLAIN_EFFECT_HANDLERS that is a DELIBERATE no-op returning targets empty and mutating nothing, because the real award is the generic WP-431 awardAttachedHeroes at the fight site which runs before the executor, so the handler exists solely to classify the printed line reachable and applied so the breadcrumb stops crying wolf. Marks the three Fight Gain that Hero lines, core skrull-queen-veranke, core skrull-shapeshifters, and rvlt klaw, in the curated villain-effect-markers.json plus the apply-effect-markers.mjs hand-synced local primitives array, regenerated onto data cards core and rvlt via apply-effect-markers.mjs with no hand-edit. NO gameplay change and awardAttachedHeroes untouched, the handler adds no new G mutation, but note the determinism fact, marking suppresses a G.diagnostics hollow write which IS part of the hashed final state since computeStateHash serializes the whole G and the finalStateHash oracle excludes only messages and logMeta not diagnostics, so the change is hash-relevant wherever the three villains are fought and is re-pin-free only because no pinned or golden fixture defeats Skrull Queen Veranke or Skrull Shapeshifters or Klaw, a fixture-content fact the executor confirms by running the suite rather than assuming no-op means no re-pin. Klaw's own Ambush line, a class and cost filtered capture, stays unimplemented and keeps its onAmbush breadcrumb, out of scope. Three design options were weighed, marker plus no-op handler chosen over detector-suppression which lacks the fight-site signal and over moving the award into the handler which is a hash and ordering risk. Section 17 gameplay fidelity and observability, No conflict. Section 20 N A no funding surface. Section 21 N A no apps server endpoint. Reserves D-24270 the gain-attached-hero primitive plus no-op-handler decision. User-visible surface game log, the false breadcrumb stops, D-24026 live-verify operator-pending, defeating Veranke shows the WP-431 hero-return line and NO Unhandled effect observed. Done 2026-07-30, game-engine 2115 of 2115 pass and whole-repo build green, the three Gain-that-Hero villains core skrull-queen-veranke, core skrull-shapeshifters, and rvlt klaw now carry effect gain-attached-hero and the D-24266 breadcrumb stops firing on their defeat, no gameplay change with the WP-431 hero-return untouched, villain mechanic ledger regenerated with three rows flipping to gain-attached-hero executable, NO fixture re-pin since no pinned or golden or sentinel fixture defeats any of the three confirmed by the green suite, and the finalStateHash half of the determinism concern is moot post-WP-451 which excluded diagnostics from hashGameState, landed at position 7 after WP-447's position-6 scry-ko-own-deck, and setup villainAbility.setup.ts needed no code change with AC-2 locked by a setup test. EC-485, standard two-session lane, drafted 2026-07-28 off origin main 7ebb8375. Hard-deps D-24266 done plus WP-431 done plus WP-252 done, soft-coordinates with WP-447)"]
        ["WP-451 ✅ Exclude G.diagnostics from the state-hash surfaces (packages/game-engine, server inherits with no edit). DONE 2026-07-29 hashGameState-only per the operator-locked scope, game-engine 2087/0 and server 1008/0 with NO fixture re-pin since the sentinel and PRE_WP080_HASH are byte-identical, and the computeStateHash competitive-oracle exclusion is a deferred follow-on. Closes the observability-hash footgun surfaced while drafting WP-447 and WP-450: G.diagnostics, a runtime-only observation channel that WP-257 documents as never gameplay input, is currently serialized into BOTH state-hash surfaces, so an observability-only change like marking a villain or the D-24266 breadcrumb firing silently shifts the finalStateHash. Excludes diagnostics from surface one, hashGameState the test-fixture golden finalStateHash oracle which is the load-bearing fix that stops golden-fixture churn on every breadcrumb or marking WP by appending diagnostics to the existing messages logMeta lastPlayEffectsFired rest-destructure, and surface two, computeStateHash the production whole-G determinism and competitive oracle, which decouples the competitive replayHash and the step-9 anti-tamper compare from observability drift. Corrects the now-false hash comments, chiefly the real stale rationale in buildInitialGameState.ts around 566-568 which says hashGameState serializes the whole G, comment-only with the seed literal untouched, plus a game.ts onBegin full-G caveat and the new diagnostics why-comment in hashGameState.ts. Adds diagnostics-invariance tests on both oracles plus an exclusion-set pin. Expected NO pinned-hash re-pin because no current fixture materializes a hollow record since D-24266 shipped in 1065 with none, so diagnostics is absent in every fixture final G and stripping an absent field is a no-op, with a conditional 01.5 re-pin only if the suite shows a shift. Risk, competitive, operator sign-off at review: the computeStateHash half changes the algorithm behind the competitive replayHash, but accepted scores are immutable and never re-verified so they are immune, the score math never reads diagnostics, and the only exposure is a one-time transition window where a replay captured pre-deploy but submitted post-deploy hits a step-9 mismatch, which is rare, recoverable, and loses no data. SCOPE LOCKED by the operator on 2026-07-29 to hashGameState-only, the load-bearing zero-competitive-risk fix that stops the fixture churn; the computeStateHash competitive-oracle exclusion is deferred to its own future WP and a dedicated low-competitive-activity deploy, not executed here. No DB migration and no apps server code change since the server inherits the engine hash. Section 17 determinism and observability boundary, No conflict. Section 20 N A. Section 21 N A no endpoint or signature change. Reserves D-24271, the invariant that runtime-only observation channels are excluded from ALL state-hash surfaces plus the competitive-risk decision. User-visible surface none infrastructure, D-24026 inverted, the payoff is hash-neutral observability. EC-486, standard two-session lane, drafted 2026-07-29 off origin main f6d03070, not yet executed. Hard-deps WP-257 done plus D-24081 and D-24221 done)"]
        ["WP-452 ✅ Co-op win-rate and loss-cause harness (Game Engine simulation; first WP of the Bot Ally Strengthening epic, the operator-chosen direction on 2026-07-28 to build a dedicated T3 ally policy measured by win rate; renumbered from WP-444 after a landed collision with the gauntlet cards-consumer. Adds a deterministic co-op win-rate plus loss-cause harness on top of the shipped runSimulation and evaluateEndgame path, changing neither. classifyCoopOutcome in a new coopOutcome.ts maps each terminal game to one of exactly five categories win, loss-scheme-completed, loss-villains-escaped, loss-tie, inconclusive-turn-cap, reading ESCAPE_LIMIT and the EndgameOutcome union heroes-win scheme-wins tie with NO re-derived literals; evaluation order win then turn-cap when endgameReached is false then tie then for a scheme-wins terminal escaped-villains at or above ESCAPE_LIMIT is villains-escaped otherwise scheme-completed, the first-tripped-condition invariant documented. runCoopWinRate in a new coopWinRate.ts plays each seed to terminal via an ADDITIVE simulateOneCoopGame narrow projection exported from simulation.runner.ts leaving runSimulation aggregate return byte-unchanged, threading an explicit registry CardRegistryReader param the copilot BLOCK fix, a fresh policy instance per seed for position-independence, and an empty-seeds zeroed-report guard; aggregates games wins winRate and a byCategory count map with an explicit for-of no reduce. A scripts coop-winrate.mjs entrypoint loads the real registry via createRegistryFromLocalFiles and runs a fixed default two-player core Magneto board over a fixed 60-seed list and prints the report, sim:coop-winrate alias. Execution amendment 2026-07-29 the pinned scheme was swapped from midtown-bank-robbery to legacy-virus-the after the harness surfaced that midtown-bank-robbery and negative-zone-prison-breakout trip SCHEME_LOSS at turn 0 an auto-loss before any move flagged for a follow-up operator-approved; baseline recorded competent 0.0 percent co-op win rate over 60 seeds all loss-scheme-completed. Prints only, NO committed artifact and NO CI freshness gate locked by D-24272 deliberately avoiding the runtime-observed-hollows and dashboard coverage cascade class; each later epic WP records its own before and after number in its STATUS not a regression-pinned file. Engine imports only the CardRegistryReader type never the registry package, the mjs supplies the reader. Gates pre-flight READY, copilot BLOCK on the missing registry input then RESOLVED, lint PASS. Reserves D-24272 the co-op-strength metric decision; EC-487; standard two-session lane, drafted 2026-07-29 off origin main edf8f7e9, not yet executed. The dedicated T3 BotAlly policy split leaving the WP-049 CompetentHeuristic frozen as the balance baseline is a later WP with its own D-entry. User-visible surface none infrastructure, D-24026 inverted STATUS states No user-observable change. Hard-dep none)"]
        ["WP-453 📝 Simulation setup deck shuffle fidelity, seeded not reverse-mock (Game Engine simulation plus test fixtures; the WP-452 follow-up for the flagged turn-0 SCHEME_LOSS on midtown-bank-robbery and negative-zone-prison-breakout. Investigation 2026-07-29 found the root cause is NOT scheme setup, buildInitialGameState leaves G.counters empty and evaluateEndgame null at initial state for every scheme including the two failing ones, and it is NOT a zero or undefined loss threshold. It is a simulation reverse-shuffle artifact: buildVillainDeck lexically sorts the assembled deck before shuffling for deterministic pre-shuffle order, the virtual scheme-twist-slug-NN ids sort LAST since s beats m h c b, and the simulation and fixture setup path builds its SetupContext via makeMockCtx whose random Shuffle is deck to reversed-deck, a deterministic reverse the unit tests rely on to prove shuffle ran, so reversing a list whose twists are at the bottom stacks all 8 scheme-twists on TOP of the villain deck, verified top 8 are scheme-twist 07 down to 00. midtown-bank-robbery and negative-zone-prison-breakout are the only two core schemes whose twist resolver CHAINS extra villain reveals via performVillainReveal, so the turn-1 twist reveals the next twist which chains the next, cascading through all 8 clustered twists in one reveal, and schemeTwistHandler increments schemeTwistCount each time until it hits the lossThreshold 8 doom-clock proxy from D-24178 and evaluateEndgame returns scheme-wins at turn 0. Non-chaining schemes are front-loaded too, one top-of-deck twist per turn losing around turn 8, so the reverse mock systematically depresses ALL simulated win rates including the WP-452 0.0 percent baseline, which this WP does not predict the post-fix number for, the executor measures it. Production is unaffected since boardgame.io supplies the real seeded PRNG shuffle in the live engine. Fix is to point the 5 simulation and fixture setup sites, runner simulateOneGame and simulateOneCoopGame and simulateOneGameAndCaptureMoves, par.aggregator simulateOneGame, and test fixtures runFixture, at each file's EXISTING seeded shuffleWithPrng deck nextRandom, the same mulberry32 PRNG the per-turn reveal loop already uses, dropping the makeMockCtx setup call and its now-unused import. makeMockCtx itself is NOT touched since its reverse is a deliberate shuffle-ran proof relied on by about 190 importers and pinned by buildInitialGameState.determinism.test.ts and replay.execute.test.ts PRE_WP080_HASH. Recorder and replay move in lockstep, simulateOneGameAndCaptureMoves and runFixture change together or the capture-to-replay round-trip in simulation.captureMoves.test.ts breaks, and the sole committed replay fixture sentinel-core-doom-2p.replay.json is re-recorded with a new finalStateHash via scripts record-game-fixture.mjs while PRE_WP080_HASH stays byte-identical. No committed PAR artifact or CI gate exists so there is no PAR re-pin. Adds a registry-free regression test asserting the seeded setup shuffle is neither identity nor reverse and does not cluster scheme-twist ids contiguously at the top, and is deterministic per seed. Twist counting and lossThreshold and schemeTwistHandler and evaluateEndgame and the D-24178 doom-clock semantics are faithful and out of scope. Section 17 determinism boundary, No conflict, swaps one deterministic shuffle for another seeded one. Section 20 N A. Section 21 N A no endpoint. Reserves D-24273 the seeded simulation setup shuffle invariant. User-visible surface none infrastructure, D-24026 inverted, STATUS states No user-observable change, payoff is a trustworthy simulation PAR and co-op yardstick. EC-488, standard two-session lane, drafted 2026-07-29 off origin main 7417a0e8, not yet executed. Hard-deps WP-452 done plus WP-036 done plus WP-411 done)"]
        ["WP-454 ✅ Gauntlet loadout qualification guard (cards builder, apps/registry-viewer only; renumbered from WP-453 after PR 1083 landed a different WP-453 EC-488 D-24273 simulation-setup-shuffle-fidelity first. A build-time UX guard that closes the wrong-villains-henchmen footgun surfaced 2026-07-29. WP-444 prefills the approved adversaries but leaves them editable, and a hand-built loadout has no guard at all, so a visitor can play a non-qualifying match and only discover post-game it never counted toward a gauntlet because qualification is enforced only server-side at aggregation matchesApprovedLoadout WP-395 or rejected at submission par-not-published. Two additive client-side behaviors. One, a new pure sibling helper gauntletQualificationCheck.ts with checkGauntletQualification comparing the draft villainGroupIds plus henchmanGroupIds plus playerCount against the draft mastermind approved GAUNTLET_LOADOUT_MENUS by EXACT set equality on the full set-qualified ext_ids order-insensitive sorting a spread copy never the live draft array, stricter than the server bare-slug ScenarioKey villain-segment and correct because the draft and menu both hold full ext_ids and cannot false-negative per D-24131 same-set plus exact henchman_key, returning a discriminated status not-a-gauntlet or unoffered-count or qualifies plus variantIndex or not-qualifying plus approvedVariantCount, rendered as a pre-play qualification badge near the villain and henchmen fields that shows for hand-built drafts too. Two, a pack-sourced adversary lock, after onPickGauntletLeg the villain and henchmen chips render read-only no-remove and no-add with an explicit Unlock adversaries escape hatch cleared in the reset handler and unlock not inside resetDraft, so a deliberate non-gauntlet loadout stays possible turning a silent mistake into a deliberate choice, while a hand-built draft is never auto-locked. Advisory only, never blocks build export or launch, the server stays the sole adjudicator, the badge is a prediction not enforcement. Zero-API no new registry import reuses the already-imported getGauntletLoadoutMenu and GauntletLoadoutMenu. New data-injected gauntletQualificationCheck ts and test ts covering not-a-gauntlet, qualifies incl reordered ids, non-zero variant, not-qualifying, unoffered-count, and non-mutation, plus modified LoadoutBuilder.vue. Out of scope the D-24187 fixed-hero-pool constraint which is cross-leg and server-adjudicated, any server registry or persistence change, scheme-leg validity, and export or launch blocking. Section 17 Vision section 10a Registry Viewer plus 20-26 leaderboard-adjacent No conflict. Section 20 N A. Section 21 N A App only no apps server endpoint. Reserves D-24274. User-visible surface cards legendary-arena com, D-24026 live-verify operator-pending. EC-489, standard two-session lane, drafted 2026-07-29 off origin main 62938572, not yet executed. Hard-deps WP-444 done plus WP-395 done)"]
        ["WP-455 ✅ Local WORK_INDEX row-pattern check (infrastructure, root tooling only; a local authoring-time mirror of the CI-only WORK_INDEX row-format drift guard. Surfaced by the WP-453 and WP-454 arc where a non-canonical draft-row status the Drafted-date-not-yet-executed phrasing instead of the canonical Draft-date bold form broke the dashboard workIndexRowPattern.test.ts drift guard, failed the whole Dashboard Gates CI job, and was twice mis-diagnosed as a coverage failure before gh run view log-failed revealed the row regex as the real cause. Adds a root scripts check-workindex-rows.mjs exposing pure extractCanonicalPattern from the generator source plus findUnparsedWpRows content plus a guarded main, reusing the single-source WORK_INDEX_ROW_PATTERN extracted from apps dashboard scripts build-governance-snapshot.mjs exactly as the dashboard test does so there is no third regex copy, validating docs ai work-packets WORK_INDEX.md and exiting non-zero with offending rows listed. New scripts check-workindex-rows.test.ts node:test on the pure helpers including the exact WP-453 regression and throw-on-rename and prose-not-flagged, plus root package.json workindex:rows:check and workindex:rows:test scripts mirroring roadmap:counts:check and :test. Out of scope any git-hook framework since none exists and introducing one is disproportionate, any apps dashboard change with the generator and CI test byte-unchanged, any CI-workflow edit since CI enforcement already exists via Dashboard Gates and this is the local mirror, and moving or exporting the canonical pattern. Section 17 N A governance tooling no Vision or product surface. Section 20 N A. Section 21 N A no apps server endpoint. Reserves D-24275. User-visible surface none infrastructure, D-24026 inverted STATUS states No user-observable change. EC-490, standard two-session lane, drafted 2026-07-29 off origin main 6b4ae29c, not yet executed. Hard-deps none)"]
        ["WP-456 ✅ Legends board per-mastermind gauntlet details reveal (apps/legends-board only, client-only zero-API; each gauntlet in the legends.legendary-arena.com index gains a keyboard-accessible Show-details reveal, a native details element, showing the mastermind schemes or legs plus the approved villain and henchmen groups per player count 1 to 5. The data is ALREADY published in the gauntlet-index snapshot GauntletIndexEntry.legs plus approvedLoadouts WP-395 D-24199 and already parsed by the board where it is used today only to build challenge links, so the reveal renders what is already in hand and completes the D-24199 promise that the qualification requirement be visible to players, a rule the player cannot see reads as a broken feature D-24186 D-24190 per legends.types.ts. Surfaced when a 2-player core magneto challenge-link run had only Brotherhood auto-added and the WP-454 badge flagged it wont-count but the board offered no way to learn the approved 2p variant needs Brotherhood plus Enemies of Asgard. Adds a pure buildGauntletDetails entry playerCounts helper returning schemes plus loadoutsByCount, reusing formatApprovedLoadout and listApprovedLoadouts and formatGroupId with graceful handling of absent approvedLoadouts or legs, plus node:test cases schemes and per-count configs including the Magneto 2p Enemies-of-Asgard variant and undefined-loadouts no-throw and no-legs, plus the reveal in GauntletIndexPanel.vue. Zero-API preserved, vue-only runtime NO registry import type or value, NO publisher server or snapshot-contract change, additive-only standings panels kiosk and challenge links untouched. Out of scope the challenge-link URL-param prefill fix which is a separate apps registry-viewer follow-on. Section 17 Vision section 10 board plus 20-26, No conflict. Section 20 N A. Section 21 N A no apps server endpoint. Reserves D-24276. User-visible surface legends.legendary-arena.com, D-24026 live-verify operator-pending. EC-491, standard two-session lane, drafted 2026-07-29 off origin main 74164b2c, not yet executed. Hard-deps WP-395 done plus WP-343 and WP-345 done)"]
        ["WP-457 ✅ Challenge-link player-count consistency, legends index CTA (apps/legends-board only, client-only zero-API; a bug fix. The gauntlet index Challenge-arrow CTA firstLegChallengeUrl in GauntletIndexPanel.vue pins the SOLO approved loadout because selectApprovedLoadout gauntlet with no count returns the count-1 config Brotherhood plus Doombot Legion for Magneto, but it emits NO playerCount param, so the cards builder falls back to DEFAULT_PLAYER_COUNT 2 and evaluates a solo composition at 2 players, the needs-2-villain-groups-has-1 mismatch an operator hit. The fix threads the row selected count selectionFor board playerCount, the same source the row Download control uses, into BOTH selectApprovedLoadout and buildChallengeUrl so the pinned villains and henchmen and the URL count agree and the link opens a qualifying draft, matching GauntletBoardPanel per-leg links which already pass a consistent count. The registry-viewer consumer is ALREADY correct via D-24190 applyPreviewToDraft which applies the URL villainGroupIds and henchmanGroupIds onto the shared draft after setMastermind, so it is untouched. Adds a pure buildRowChallengeUrl entry playerCount helper wrapping the two existing primitives, returns null on no legs and degrades gracefully when a count has no approved loadout, plus node:test cases 2p villains-plus-count, 1p solo, no-legs null, unpinned-count graceful, plus the GauntletIndexPanel.vue wiring. Zero-API, vue-only runtime NO registry import, NO publisher server or snapshot change, no change to buildChallengeUrl or selectApprovedLoadout signatures, no board-panel change. Out of scope any apps registry-viewer change. Section 17 Vision section 10 plus 20-26, No conflict. Section 20 N A. Section 21 N A. Reserves D-24277. User-visible surface legends.legendary-arena.com, D-24026 live-verify operator-pending. EC-492, standard two-session lane, drafted 2026-07-30 off origin main 7e5f7b2c, not yet executed. Hard-deps WP-345 done plus WP-395 done plus WP-387 done)"]
        ["WP-458 ✅ Collapse approved gauntlet loadouts to one canonical configuration variant 0 (registry tooling; an operator decision 2026-07-30 that the approved villain and henchmen menu should be ONE configuration per mastermind per player count, variant 0 the canonical rotation the challenge links and pack import and WP-454 badge already default to, NOT the D-24199 menu of three, so the ONLY ranked-qualification variable is the heroes while casual play free selection stays untouched. Rationale, a ranked leaderboard is a fair fixed-course time-trial, three approved courses per leg fragment competition three ways since villains are part of ScenarioKey without adding strategic depth because an optimizer converges on whichever is easiest, one config concentrates scores for denser boards and faster PAR calibration about 2,118 scenarios instead of 6,354 and gives a legible same-fight-your-heroes format. Zero migration cost because competitive_scores is empty so dropping variants 1 and 2 re-keys and invalidates no scores, the cheapest this will ever be, the day the first ranked score lands this would start voiding runs on retired variants. Single lever change, scripts generate-gauntlet-loadouts.mjs VARIANTS_PER_MASTERMIND 3 to 1 plus regenerate gauntletLoadouts.generated.ts to 110 times 1 plus update the registry test to exactly-one and remove the now-meaningless distinctness test plus comment-only fixes in gauntletLoadouts.ts and apps server legends gauntlet.logic.ts. Qualification logic matchesApprovedLoadout untouched, it already compares against an approved config and there is now one. Variant 0 compositions are byte-identical, only variants 1 and 2 drop. Supersedes D-24199 menu-size only, the core rule that ranked legs require an approved loadout and casual stays free is intact. Out of scope casual play, qualification logic, consumer-surface code which iterates or defaults to variant 0, ScenarioKey and henchman_key shape, PAR calibration. Section 17 Vision section 10 plus 20-26, No conflict. Section 20 N A. Section 21 N A. Reserves D-24278. User-visible surface legends.legendary-arena.com, D-24026 live-verify operator-pending. EC-493, standard two-session lane, drafted 2026-07-30 off origin main 4e0f3261, not yet executed. Hard-deps WP-395 done)"]
        ["WP-459 ✅ Compact multi-column Show-details reveal layout, legends board (apps legends-board only, presentation-only zero-API; makes the WP-456 Show-details reveal compact. The expanded reveal currently stacks every player count in a single column with generous gaps, about 40 rows of mostly whitespace that forces scrolling. This lays the per-count approved-adversary blocks in a responsive multi-column grid using repeat auto-fit minmax so the five count blocks flow into two or three columns on a wide index row and one column on narrow viewports, keeps the Schemes line full-width above the count grid, and tightens the Villains and Henchmen spacing to tight adjacent lines. Presentation-only CSS plus minor supporting markup regrouping in GauntletIndexPanel.vue, the rendered text content is unchanged same schemes and Villains and Henchmen labels from WP-456, buildGauntletDetails and gauntletDisplay.ts and the data are untouched. vue-only runtime zero-API, responsive single-column with no horizontal overflow on mobile, native details kept keyboard-accessible, reuse the existing la-color tokens no theme change. Pairs with WP-458 which collapses each count to one config cutting the reveal height about three times but does NOT hard-depend on it since the grid lays out one or three configs. Out of scope any data or helper change, the challenge link download control chips or claimed-board links, any theming overhaul. No D-entry, UI layout polish. Section 17 section 10, No conflict. Section 20 N A. Section 21 N A. User-visible surface legends.legendary-arena.com, D-24026 live-verify operator-pending. EC-494, standard two-session lane, drafted 2026-07-30 off origin main 4e0f3261, not yet executed. Hard-deps WP-456 done)"]
        ["WP-460 ✅ Move the row divider below the Show-details reveal panel, legends board (apps legends-board only, scoped-CSS zero-API; a one-line refinement of WP-459 from an operator visual request 2026-07-30. Removes the .gauntlet-details border-top, the horizontal divider that sat ABOVE the Show-details panel separating it from the row controls; the .gauntlet-row border-bottom already sits BELOW the expanded reveal because WP-459 flex-wrap wrap puts the reveal on the row's own line, so it becomes the sole correctly-placed divider. Presentation-only GauntletIndexPanel.vue, rendered content unchanged, vue-only zero-API. Browser-verified live, gauntlet-details computed border-top-width 0px, row border-bottom below the expanded reveal body, no overflow, no console errors, legends-board 114/0 vue-tsc 0 build 0. No D-entry UI polish. Section 17 section 10 No conflict. Section 20 N A. Section 21 N A. User-visible surface legends.legendary-arena.com, D-24026 live-verify operator-pending. EC-495, lightweight draft-plus-exec one PR, 2026-07-30 off origin main 7757a8db. Hard-deps WP-459 done)"]
        ["WP-461 ✅ Publish per-set gauntlet roster and coverage to the legends index, server (apps server legends publisher, no registry or engine import; the data half of an operator-requested per-set Show set details reveal on legends.legendary-arena.com 2026-07-30. The legends snapshot publisher emits an additive optional sets field into gauntlet-index.json, one entry per set with at least one scheme carrying the full per-set roster of masterminds and schemes and villain groups and henchman groups each with the registry authoritative name, plus per villain and henchman group a usedByGauntlets boolean that is true when its set-qualified setAbbr slash slug id appears in at least one approved gauntlet config across the set masterminds at any player count, masterminds and schemes carry no flag being covered by construction. Computed once server-side in a pure buildSetDetailsCatalog from wiring-injected set rosters plus the approvedLoadoutsByGauntlet menu so the zero-API board WP-462 stays purely presentational and the snapshot is self-describing, the legends module keeps its no-registry-import lock and rosters ride the same server.mjs to index.mjs to legends.scheduler.ts to publishAllBoards injection path as gauntletCatalog. This REPORTS but does not close the coverage gap surfaced 2026-07-30, across a set masterminds the approved menu caps at about four villains and two henchmen alphabetically-first so Core covers four of seven villains and two of four henchmen, the operator chose ship-transparency-now leaving whether a set challenge should fight everything a separate decision. Modifies legends.types.ts and gauntlet.logic.ts and legends.publisher.ts plus tests plus wiring server.mjs index.mjs legends.scheduler.ts, additive and optional so a pre-WP-461 consumer ignores it. Out of scope changing which adversaries the gauntlets use, GauntletDefinition or standings or PAR or scoring, any board file but the index, the client WP-462. Section 17 Vision section 10 plus 22, No conflict. Section 20 N A. Section 21 N A publisher writes R2 no endpoint. Reserves D-24279. User-visible surface legends.legendary-arena.com, D-24026 live-verify operator-pending. EC-496, standard two-session lane, drafted 2026-07-30 off origin main 5b086f66, not yet executed. Hard-deps WP-395 done plus WP-458 done, paired producer with WP-462)"]
        ["WP-462 ✅ Legends board per-set Show set details reveal, client (apps legends-board only, client-only zero-API; the render half of the per-set Show set details reveal. Each set group on the legends gauntlet index gains a keyboard-accessible reveal listing the set full roster masterminds and schemes and villains and henchmen with each villain and henchman marked fought by the gauntlets or in the set but unused, text-labelled not colour-only. Renders WP-461 sets field with the registry authoritative names, no client-side recompute of coverage it renders the usedByGauntlets flag verbatim. Mirrors the additive snapshot types in snapshotClient.ts never importing the server module the zero-API layer boundary, vue-only runtime, degrades cleanly on a pre-WP-461 snapshot with no sets to no reveal and no throw. Adds a pure findSetDetails sets setAbbr helper plus tests in gauntletDisplay.ts and the reveal in GauntletIndexPanel.vue. Out of scope any server or publisher or snapshot or registry change, the WP-456 per-mastermind reveal, standings panels or challenge links or download control or kiosk. No D-entry client mirror of D-24279. Section 17 Vision section 10, No conflict. Section 20 N A. Section 21 N A. User-visible surface legends.legendary-arena.com, D-24026 live-verify operator-pending. EC-497, standard two-session lane, drafted 2026-07-30 off origin main 5b086f66, not yet executed. Hard-deps WP-461 paired producer)"]
        ["WP-463 ✅ Villain zone-restricted each-player Hero KO, Juggernaut Ambush plus Escape (packages game-engine plus card-data generated artifact). Closes the last onAmbush hollow observed in a live Magneto match 2026-07-30 gitSha b685eff after WP-447 Doombot scry-KO and WP-450 gain-attached-hero closed the onFight classes. Juggernaut Ambush Each player KOs two Heroes from their discard pile is a source-zone-restricted each-player KO the generic ko-hero each N never covered because its resolver falls through discard then hand then inPlay whereas Juggernaut is discard-only. Adds an optional zone field discard or hand to VillainEffectDescriptor plus the four-token grammar ko-hero colon each colon N colon zone in the parser and the apply-effect-markers.mjs local validator, NO new primitive so VILLAIN_EFFECT_PRIMITIVES and its drift test are untouched. A new koHeroesFromZoneForPlayer resolver KOs up to magnitude heroes from only the named zone reusing selectKoHeroTarget with Wounds excluded and starter-first then lex-asc, no cross-zone fallback, a short or empty zone is a reachable no-op, wired into the villainEffectKoHero each branch behind descriptor.zone. descriptorKey deliberately OMITS zone so the zone-bearing descriptor reverse-maps to the existing koHeroEachPlayerMag2 keyword and narrates per-target KO'd hero names for free per WP-316, the frozen ten-keyword surface D-24023 and the injective round-trip test untouched. Marks Juggernaut core brotherhood juggernaut ambush ko-hero each 2 discard plus escape ko-hero each 2 hand via villain-effect-markers.json regenerated onto data cards core only, a corpus scan of all twelve Each player KOs from their discard or hand lines shows every other carries a filter or choice clause and stays deferred, the Escape sibling is the identical mechanic closed in the same WP. Auto-resolved, the each-player KO stays auto per D-18902. Determinism-adjacent no ctx.random no I O no new G field KO via zoneOps, a real hashed KO so Verification pins replay-hash handling with regen-with-note only if a fixture reveals or escapes Juggernaut. Section 17 gameplay fidelity No conflict. Section 20 N A. Section 21 N A no apps server endpoint. Reserves D-24280 the zone descriptor param plus the descriptorKey-omits-zone narration-reuse decision. User-visible surface game log plus KO pile, D-24026 live-verify operator-pending. EC-498, standard two-session lane, drafted 2026-07-30 off origin main b8d479df, Done 2026-07-30, game-engine 2123 of 2123 pass and whole-repo build green, the last onAmbush hollow closed with core Juggernaut Ambush KOing two Heroes from each player discard pile and Escape from each hand both narrating per-target via the koHeroEachPlayerMag2 reverse-map, VILLAIN_EFFECT_PRIMITIVES unchanged at 7 with the primitive drift test untouched, villain mechanic ledger regenerated core Juggernaut flips unmarked to ko-hero executable and the co2e non-grey sibling stays unmarked as disclosed, NO fixture re-pin since no pinned or golden or sentinel fixture reveals or escapes Juggernaut confirmed by the green suite, the setup parser gained the real four-token ko-hero each N zone code and a reverse-map unit test guards the descriptorKey-omits-zone decision. Hard-deps D-24266 done plus WP-202 done plus WP-252 done)"]
        ["WP-464 ✅ Core Set gauntlet-coverage matrix, legends board (apps legends-board only, client-only zero-API; an operator-requested collapsible coverage matrix at the bottom of the Core Set group on legends.legendary-arena.com. Transposed layout, the operator's choice to avoid horizontal scroll, rows are the set masterminds times schemes Core four times eight equals 32 grouped by mastermind and columns are the seven villains plus four henchmen. A cell shows a checkmark hyperlink to that scheme-leg cards-builder challenge with the approved loadout pinned if and only if the column villain or henchman is in that mastermind approved config at the selected player count, otherwise the cell is empty. A player-count selector defaults to 2 players and is switchable 1 to 5, the checkmarks update live. Zero-API, it renders data ALREADY published in the gauntlet index, each gauntlet approvedLoadouts plus legs plus the WP-461 sets roster, via the existing buildChallengeUrl, no new fetch or snapshot or contract. Confirmed data reality the approved villains and henchmen are fixed per mastermind times player count NOT per scheme so the checkmark pattern repeats across a mastermind eight scheme-rows and only the per-leg link differs, the matrix is a per-leg launch grid. Adds a pure buildCoverageMatrix plus tests in gauntletDisplay.ts and the matrix in GauntletIndexPanel.vue. Core-only pilot with a render guard setAbbr equals core, the builder is generic so a later WP extends it. Out of scope other sets, any server or publisher or snapshot or registry change, the WP-456 and WP-462 reveals, the count chips or challenge CTAs or download control or kiosk. No D-entry presentation-only like WP-459. Section 17 section 10, No conflict. Section 20 N A. Section 21 N A. User-visible surface legends.legendary-arena.com, D-24026 live-verify operator-pending. EC-499, standard two-session lane, drafted 2026-07-30 off origin main fa3c0007, not yet executed. Hard-deps WP-461 done plus WP-462 done plus WP-395 done)"]
        ["WP-465 ✅ Honest per-reason copy for the score-submission banner, arena client (apps arena-client only, client-only zero-API; fixes a misleading post-match banner Jeff hit repeatedly on ad-hoc Magneto matches 2026-07-30. Today useCompetitiveSubmitOnGameover blanket-maps every non-200 from POST api competition scores to the failed status showing Couldn't submit your score It may still be counted shortly, which is false on two counts, the hasSubmitted guard means there is NO retry and the dominant real cause is par_not_published meaning a casual non-ranked-gauntlet match is permanently ineligible not a transient failure. The fix is client-only and zero-API because SubmitScoreResult.error ALREADY carries the server rejection reason via readErrorCode and the composable simply ignores it today. Adds a new SubmissionStatus value ineligible, maps result.error equals par_not_published to ineligible otherwise failed, adds the honest ineligible copy plus a neutral info CSS variant in PlayViewport.vue, rewords the failed copy to drop the false may still be counted shortly promise, and hardens SUBMISSION_MESSAGES from Record string string to Record Exclude SubmissionStatus idle string so a future status without copy fails vue-tsc. Touches useCompetitiveSubmitOnGameover.ts and its test plus PlayViewport.vue, NO server change since the reason is already on the wire and NO scoring or logic change. Out of scope finer per-reason copy for the rare permanent reasons not_owner and visibility_not_eligible and replay_not_found and match_not_finished which keep failed, the guest path, my-scores, any retry mechanism. Section 17 honest competitive feedback No conflict. Section 20 N A score-display honesty not monetization. Section 21 N A no apps server endpoint change the server surface is untouched. No D-entry presentation-only like WP-459 and WP-464. User-visible surface play.legendary-arena.com, D-24026 live-verify operator-pending. Done 2026-07-30, arena-client 1128 of 1128 pass, vue-tsc 0, build 0, pnpm -r build 0; client-only since SubmitScoreResult.error already carried the reason, par_not_published now maps to a new non-error ineligible status with honest copy and a neutral blue slate CSS variant, the failed copy reworded retry-neutral, and SUBMISSION_MESSAGES tightened to Record Exclude SubmissionStatus idle string which caught the message-lookup site now narrowing idle explicitly, no server change and the diff is exactly the three arena-client files, D-24026 live-verify operator-pending on the next Cloudflare Pages client deploy. EC-500, standard two-session lane, drafted 2026-07-30 off origin main a76892d2. Hard-deps WP-339 done)"]
        ["WP-466 ✅ Compact the Core coverage matrix, remove horizontal scroll, legends board (apps legends-board only, presentation-only zero-API; three operator-directed refinements 2026-07-30 to the WP-464 Core coverage matrix all to remove the horizontal scroll. One, each mastermind is named ONCE as a full-width group-header row above its eight scheme rows via the existing isMatrixMastermindStart, so the row-label column holds only the scheme name instead of repeating the mastermind on every row. Two, the eleven villain and henchman column headings are rotated 90 degrees via writing-mode vertical-rl so each column shrinks to about one line-height wide and the header row grows taller instead. Three, a long scheme name wraps to two lines via white-space normal plus max-width plus overflow-wrap instead of forcing a wide single line. Single-file GauntletIndexPanel.vue, scoped CSS plus a template regroup, buildCoverageMatrix and its tests are untouched, vue-only zero-API, the checkmark cells and challenge links and count selector and accessible names are unchanged. Live-data DOM smoke, four mastermind headers, headings computed writing-mode vertical-rl, thirty-two scheme rows, long scheme label two lines, table intrinsic width about 462px down from about 1300px. Out of scope other sets, any buildCoverageMatrix or data change, any server or snapshot or registry change. No D-entry presentation-only. Section 17 section 10, No conflict. Section 20 N A. Section 21 N A. User-visible surface legends.legendary-arena.com, D-24026 live-verify operator-pending. EC-501, lightweight draft-plus-exec one PR, 2026-07-30 off origin main c7ad0407. Hard-deps WP-464 done)"]
        ["WP-467 ✅ Fix the coverage-matrix scheme-name wrap, legends board (apps legends-board only, presentation-only zero-API bug fix; the WP-466 scheme-name wrap did not take effect on the deployed page, operator reported 2026-07-30 that a long scheme name stayed on one line and bled into the checkmark columns. Two compounding root causes, one the general coverage-matrix-table th white-space nowrap rule out-specifies the coverage-matrix-rowhead class on the same th so nowrap won, two max-width on a th is ignored under auto table-layout so the cell expanded to fit the name on one line. Fix, move both the wrap and the width bound onto an inner inline-block span coverage-matrix-scheme-text with display inline-block plus max-width 8.5rem plus white-space normal plus overflow-wrap, which a table cell sizes to and auto-layout cannot override, and trim the ineffective props off coverage-matrix-rowhead. Single-file GauntletIndexPanel.vue scoped CSS plus one span, buildCoverageMatrix and the mastermind grouping and rotated headings and checkmark cells and links and count selector are unchanged, vue-only zero-API. Own miss, the WP-466 scheme-wrap acceptance was falsely passed on a zero-viewport non-compositing dev-pane measurement, this WP verifies at real intrinsic geometry, long Replace Earths Leaders with Killbots span 28px two lines bounded 136px, short The Legacy Virus 14px one line, scheme cell right edge at the first checkmark cell left edge zero overlap. Out of scope table-layout fixed, other sets, any data or server change. No D-entry presentation-only bug fix. Section 17 section 10, No conflict. Section 20 N A. Section 21 N A. User-visible surface legends.legendary-arena.com, D-24026 live-verify operator-pending. EC-502, lightweight draft-plus-exec one PR, 2026-07-30 off origin main 4d528bcc. Hard-deps WP-466 done)"]
        ["WP-468 ✅ Coverage-matrix dropdown readability plus collapsible masterminds, legends board (apps legends-board only, presentation-only zero-API; two operator-directed changes 2026-07-30. One, the download-select option text was the dark-theme light colour so it was invisible on the native light dropdown popup until the blue hover highlight, give the options explicit dark on light 111827 on white so values read when not highlighted, covers the download count and division selects plus the coverage-matrix count select and the closed control keeps its light on dark toolbar styling. Two, collapsible masterminds, each mastermind eight scheme rows collapse under a clickable full-width toggle button in the group-header th with aria-expanded and a caret, the scheme tr is v-if on isMastermindExpanded and defaults collapsed so the matrix opens compact with four mastermind headers and the operator expands one at a time avoiding the 32-row vertical scroll and keeping the rotated column headings lined up with the visible data. Single-file GauntletIndexPanel.vue scoped CSS plus template plus a small reactive expandedMasterminds record, buildCoverageMatrix and the data and checkmark cells and links and count selector and rotated headings and scheme-wrap are unchanged, vue-only zero-API. Live browser verify with the port pinned to 5173 by pull 1112, option color rgb 17 24 39 on white and closed select light, four collapsed toggles by default then clicking Magneto reveals its eight scheme rows plus aria-expanded true plus caret down then clicking again collapses to zero. Out of scope the gauntlet-variety data change per-scheme approved adversaries which is a separate larger arc, any server or snapshot change. No D-entry presentation-only. Section 17 section 10 plus a11y, No conflict. Section 20 N A. Section 21 N A. User-visible surface legends.legendary-arena.com, D-24026 live-verify operator-pending. EC-503, lightweight draft-plus-exec one PR, 2026-07-30 off origin main e7bc018b. Hard-deps WP-464 done plus WP-466 done plus WP-467 done)"]
        ["WP-469 ✅ Villain reveal-or-wound conditional each-player effect, Sabretooth plus core siblings (packages game-engine plus card-data generated artifact; renumbered from WP-468 after a concurrent collision, number 1114 took WP-468 EC-503 for matrix-dropdown-collapse). Closes the last onFight hollow observed in a live Magneto match 2026-07-30, core brotherhood sabretooth Fight Each player reveals an team x-men Hero or gains a Wound plus Escape Same effect, a conditional each-player reveal-or-wound which is the single most common unimplemented villain mechanic with 41 corpus lines deferred as conditional for lack of a hero-trait predicate. Appends one primitive reveal-or-wound at position 8 append-only with the drift test moved to 8, parameterized by a predicate via the grammar reveal-or-wound colon team-or-hc colon value in the parser and the apply-effect-markers.mjs validator where hc parses to hero-class and requireValue is normalized to the cardTraits slug space, and adds requireKind plus requireValue descriptor fields. A villainEffectRevealOrWound handler scans each player HAND in sorted order for a hero whose G.cardTraits team or heroClass matches, reusing the D-24076 cardTraits lookup but HAND ONLY which is faithfully narrower than playerMeetsDefeatRequirement hand plus inPlay because the printed reveal is a hand action, and gains a Wound via gainWound only when NO match, mirroring gain-wound each woundsDrawn bump for the current player. Auto-resolved since revealing to avoid the Wound is optimal for a Wound-averse player so no player choice, self-narrates via pushLog naming which players were wounded, keyword-less descriptor so descriptorToLegacyKeyword returns undefined and descriptorKey is NOT extended keeping the frozen ten-keyword surface and injective round-trip test untouched. Marks CORE only 5 cards 8 markers, sabretooth fight plus escape team x-men, enemies-of-asgard frost-giant fight plus escape hc ranged, enemies-of-asgard ymir-frost-giant-king ambush hc ranged, masters-of-evil ultron escape hc tech, radiation zzzax fight plus escape hc strength, every core Same effect Escape marked, plus removes the stale _unassigned rows, the roughly 35 cross-set instances are a data-only follow-on and the conditional or compound variants stay deferred. Determinism-adjacent no ctx.random no I O no new G field Wound via gainWound and zoneOps, a real hashed Wound and since these are common Core adversaries a re-pin is LIKELY. Section 17 gameplay fidelity No conflict. Section 20 N A. Section 21 N A no apps server endpoint. Reserves D-24281 the reveal-or-wound primitive plus predicate plus hand-only plus auto-resolve decision. User-visible surface game log plus Wound piles, D-24026 live-verify operator-pending. EC-504, standard two-session lane, drafted 2026-07-30 off origin main 35bd1351, executed 2026-07-31. Hard-deps D-24266 done plus D-24076 done plus WP-252 done)"]
        ["WP-470 ✅ Interactive Doombot scry-KO choice, look at top 2 pick one to KO (packages game-engine plus apps arena-client, cross-layer engine-then-client). The deferred WP-447 phase-2 follow-on Jeff requested from a live Magneto match 2026-07-30, Doombot Legion Fight look at the top two cards of your deck KO one and put the other back currently auto-resolves via villainEffectScryKoOwnDeck KOing the deterministic worst by selectScryKoTarget giving no agency, it KO'd a real Dangerous Rescue at log line 172 when both top cards were heroes. Upgrades scry-ko to an interactive choice mirroring the WP-242 and WP-243 KO-a-Hero pending-choice machinery, the parker pushes a PendingScryKoChoice choiceType scry-ko playerID revealedCardIds top up to two onto a new runtime G.pendingScryKoChoices FIFO queue when deck is at least two and KOs nothing yet, a one-card deck still auto-KOs and zero is a no-op the WP-242 zero-one-two branch. A resolveScryKoChoice bgio move client false registered in game.ts validates the front playerID and choiceType and cardId in revealedCardIds then KOs the chosen card leaving the other on top and front-pops, all invalid states silent no-ops queue intact. A hasPendingScryKoChoice block-all guard is threaded into every action-move site plus the End-Turn and advanceStage gates so the choice cannot be skipped. UIState gains pendingScryKoChoice projecting the front revealed cards plus the WP-249 D-24020 private filtered variant visible only to the choosing player. The arena-client PendingScryKoChoicePrompt.vue mirrors PendingKoHeroChoicePrompt.vue plus useTurnActions and uiMoveName wiring and a PlayViewport mount. Determinism preserved, bots and sims via ai.legalMoves and simulation.runner and par.aggregator resolve through selectScryKoTarget the same worst-first card the auto-resolve KO'd so par and replay stay faithful and only live human play gets the prompt, but bot fixtures fighting a Doombot now park then resolve two moves versus one plus a new hashed G.pendingScryKoChoices field so a fixture re-pin is LIKELY and game.test.ts move-set plus one. The pending state its guard its projection and its client prompt ship together because a block-all pending state without its UX hard-freezes the client, the project_pending_choice_no_ux_freeze rule. Section 17 gameplay fidelity plus agency No conflict. Section 20 N A. Section 21 N A bgio move no apps server endpoint. Reserves D-24282. User-visible surface play.legendary-arena.com, D-24026 live-verify operator-pending. EC-505, standard two-session lane, drafted 2026-07-30 off origin main 371345d5, not yet executed. Hard-deps WP-447 done plus WP-242 and WP-243 done)"]
        ["WP-471 ✅ Per-scheme gauntlet registry loader, arc 1 of 5 (registry loader, infrastructure; the additive foundation of the operator-directed per-scheme gauntlet variety re-drafted 2026-07-31 and renumbered off pull 1118 which took WP-470 EC-505 D-24282 for the interactive Doombot scry-KO. Execution reconciliation operator-confirmed 2026-07-31: data gauntlet-configs.json already shipped via pull 1116 as a Core-only richly-annotated file with full set-qualified ext_ids and the matching Core swaps, so instead of the drafted new all-sets simple-shape file plus seeder this packet adds ONLY the registry loader over the existing 1116 file unchanged. getGauntletConfig set mastermind scheme count slices villainPool and henchmanPool by PLAYER_COUNT_SETUP and returns undefined for any absent leg so the consumer falls back to the per-mastermind GAUNTLET_LOADOUT_MENUS, the WP-472 absent-scheme to menu model. No seeder, data file not modified, AC1 narrowed. Adds a Zod validator plus getGauntletConfig plus getActiveYear plus Core-leg guards. Additive, nothing consumes it yet. Files packages registry gauntletConfigs ts and test plus index barrel, a subset of the allowlist. No D-entry, D-24283 lands at WP-472. Section 17 No conflict. Section 20 N A. Section 21 N A. User-visible surface none infrastructure, D-24026 inverted. Done 2026-07-31, registry 201 tests. EC-506, executed off origin main 2dabda97. Hard-deps WP-458 done, paired with WP-472 and WP-473 and WP-474)"]
        ["WP-472 ✅ Per-scheme gauntlet server truth plus leaderboard plus publisher, arc 2 of 5 (apps server registry-free; migrates the shared truth plus leaderboard plus publisher to per scheme times mastermind times count. matchesApprovedLoadout and qualifiesAsLegClear gain an ADDITIVE per-scheme lookup where absent scheme falls back to today per-mastermind so the run-tracker WP-473 and client WP-474 keep building until they migrate, getGauntletStandings passes the leg scheme, buildSetDetailsCatalog D-24279 coverage recomputes per scheme, and the publisher emits the per-leg loadout onto GauntletIndexLeg. Lands D-24283 which supersedes D-24278 per-mastermind axis, keeps one variant per leg and D-24199 core rule, PAR about 2118 unchanged, competitive_scores empty zero migration. Modifies server.mjs plus gauntlet.logic.ts plus gauntletTruth.logic.ts plus legends.publisher.ts plus tests plus legends.types.ts. Run-tracker and launch go to WP-473, client to WP-474. Section 17 No conflict D-24283. Section 20 N A. Section 21 N A. Lands D-24283 Active. User-visible surface legends.legendary-arena.com, D-24026 live-verify operator-pending. EC-507, Done 2026-08-01 off origin main 80193f05, server legends plus gauntlet 129 of 0 with 16 DB-gated skipped. Hard-deps WP-471 done)"]
        ["WP-473 ✅ Per-scheme gauntlet run-tracker leg-clear plus per-leg launch, arc 3 of 5 (apps server registry-free; migrated the run-tracker consumers of WP-472 additive truth. deriveGauntletRunProgress leg-clear builds a per-scheme map from inputs.legs (each leg effective loadout stamped by WP-472) and passes it into qualifiesAsLegClear so a leg clears only against its own config; a win with a different scheme villains of the same mastermind does not clear the requiring leg. deriveGauntletRunLaunch re-keys GauntletRunLaunch from one per-run block to a per-leg legLaunch map (gauntletRun.types adds GauntletRunLaunchComposition plus additive legLaunch, per-run block preserved RS-1) so Play this leg fields the leg adversaries not the mastermind default. Closes the functional break where a leg requiring Skrulls could never qualify while launching Brotherhood. Reconciliation: server.mjs NOT modified, WP-472 already stamps definition.legs and resolveGauntletRunProgressInputs already injects them, so no new injected field. Modifies gauntletRunProgress.logic ts and test plus gauntletRun.types.ts plus api-endpoints.md. No D-entry consumes D-24283. Section 17 No conflict. Section 20 N A. Section 21 whole-row update of GET api me gauntlet-runs, additive legLaunch overlay. User-visible surface play.legendary-arena.com, D-24026 live-verify operator-pending. EC-508, Done 2026-08-01 off origin main 09cbbc81, server gauntlet plus legends 133 of 0 with 16 DB-gated skipped. Hard-deps WP-472 done)"]
        ["WP-474 ✅ Per-scheme gauntlet client consumers, arc 4 of 5, SPLIT legends-board only (apps legends-board; the consumers read the per-scheme loadout WP-472 publishes on GauntletIndexLeg. Snapshot mirror gains GauntletIndexLeg.approvedLoadouts, buildCoverageMatrix selects per leg so each scheme-row checkmark pattern DIFFERS which is the point of the variety, reveal and challenge links per leg, buildGauntletDetails pairs each scheme with its own config GauntletDetailConfig gains schemeName, new selectLegApprovedLoadout reads leg-level with entry-level per-mastermind fallback for a pre-WP-472 snapshot. Modifies snapshotClient.ts plus gauntletDisplay ts and test plus GauntletIndexPanel.vue plus GauntletBoardPanel.vue, a subset of the drafted allowlist. Execution reconciliation operator-confirmed 2026-08-01 SPLIT: the registry-viewer cards half AC3 was infeasible as drafted, WP-471 getGauntletConfig is Node-only node fs at load root-barrel-only no browser-safe subpath and registry-viewer is a Vite browser app so there is no browser-safe per-scheme source in the cards builder, deferred to a follow-up WP next-free WP-483 EC-518 to be drafted which first adds a browser-safe per-scheme registry export. Degrades cleanly on an old snapshot. No D-entry consumes D-24283. Section 17 section 10 No conflict. Section 20 N A. Section 21 N A. User-visible surface legends.legendary-arena.com, D-24026 live-verify operator-pending. EC-509, Done 2026-08-01 off origin main a05f79f5, legends-board 129 of 0 typecheck clean. Hard-deps WP-471 done and WP-472 done)"]
        ["WP-475 ✅ Per-scheme gauntlet arena-client Play this leg launch consumer, arc 5 of 5, COMPLETES the per-scheme arc (apps arena-client client tier mirror-not-import; the arena-client Play this leg tracker consumes the per-leg launch map WP-473 added. Migrated gauntletRunApi.ts to mirror the additive GauntletRunLaunch.legLaunch map via GauntletRunLaunchComposition and MyProfilePage.vue playLeg to select run.launch.legLaunch of leg.schemeId the bare slug map key with the per-run block as the old-snapshot or absent-leg fallback, then assembles the MatchSetupConfig as before. Modifies gauntletRunApi ts and test plus MyProfilePage.vue. No D-entry consumes D-24283. Section 17 No conflict. Section 20 N A. Section 21 N A client tier reads only. User-visible surface play.legendary-arena.com, D-24026 live-verify operator-pending. EC-510, Done 2026-08-01 off origin main c7f42443, arena-client 1136 of 0 typecheck clean. Hard-deps WP-473 done)"],
        ["WP-483 ✅ Per-scheme gauntlet browser-safe registry export plus cards consumers, arc 4 of 5 cards half, COMPLETES the per-scheme arc (packages registry producer plus apps registry-viewer consumer; the cards half WP-474 split off. WP-474 shipped the legends-board half only because WP-471 getGauntletConfig is Node-only, it readFileSyncs data gauntlet-configs.json at module load and is reachable only via the registry root barrel which breaks the Vite browser build. This WP first adds a browser-safe per-scheme registry export mirroring the gauntletLoadouts.generated.ts precedent, a new scripts generate-gauntlet-configs.mjs plus gauntlet:configs and gauntlet:configs:check root scripts bakes the config to packages registry src gauntletConfigs.generated.ts, gauntletConfigs.ts imports the literal instead of node:fs with getGauntletConfig and getActiveYear and validateGauntletConfigs signatures plus the undefined to menu fallback preserved, and a new registry gauntletConfigs subpath export. Then it threads schemeId through the cards consumers, GauntletQualificationInput gains a required schemeId that gates the per-scheme branch plus an injected approvedComposition, resolveGauntletLegLoadout USES input.schemeId where today it echoes but selects scheme-blind, and LoadoutBuilder.vue resolves the leg config via the browser-safe getGauntletConfig with menu fallback and injects it into the badge and prefill call sites. Per-scheme qualifies reports variantIndex 0, one canonical composition per leg. Cross-layer Registry producer to App consumer downward allowed, single WP because the export has no consumer but this WP and the cards changes cannot resolve per scheme without it. No server change barrel import preserved, no legends-board change WP-474 owns. The generated literal preserves source key order and the enforcing drift gate is the in-test deep-equal assertion, gauntlet:configs:check is a convenience mirror not wired into ci.yml. Modifies generate-gauntlet-configs.mjs new plus gauntletConfigs.generated.ts new plus gauntletConfigs ts and test plus packages registry package.json plus root package.json plus gauntletQualificationCheck ts and test plus loadoutGauntletPackImport ts and test plus LoadoutBuilder.vue. No D-entry consumes D-24283. Section 17 section 10 section 20-26 No conflict. Section 20 N A. Section 21 N A. User-visible surface cards.legendary-arena.com, D-24026 live-verify operator-pending. EC-518, Done 2026-08-01 off origin main d184022f, registry 202 of 0 plus registry-viewer 203 of 0 typecheck clean, drift gate verified exit 1 on hand-edit. Hard-deps WP-471 done and WP-472 done and WP-474 done)"]
        ["WP-476 ✅ Magneto Master Strike reveal-an-X-Men-or-discard conditional plus interactive discard-to-4 (game-engine plus arena-client; fixes a live Magneto 1p bug where the Master Strike Each player reveals an team x-men Hero or discards down to four cards ALWAYS discarded and auto-picked, never checking the reveal alternative and giving no discard choice. resolveMagnetoStrike gains a reveal-check where a player with an X-Men Hero in HAND at start-of-turn discards nothing, hand-only because the strike fires at start stage with inPlay empty which is deliberately NOT the D-24281 hand plus inPlay scope, plus an interactive discard-to-4 for the current player via a new PendingDiscardChoice on G.pendingDiscardChoices resolved by resolveDiscardChoice with a hasPendingDiscardChoice block-all guard threaded through every action move and the start-to-main advance, a UIState projection with the D-24011 private filter, a deterministic bot and sim default registered in SIMULATION_MOVE_NAMES and both sim MOVE_MAPs and the drift test, and a PendingDiscardChoicePrompt.vue mirroring the WP-242 and WP-243 KO-a-Hero machinery. Non-current players auto-pick cheapest-first because the pending-choice model assumes a single current-player chooser, interactive non-current is a deferred follow-on, and for 1p the current player is the only player so this fully fixes it. Magneto only, other reveal-or-discard strikes are a code follow-on since strikes are hardcoded. Determinism-adjacent no ctx.random no I O but a new hashed pendingDiscardChoices field so a fixture re-pin is LIKELY. Section 17 gameplay fidelity No conflict. Section 20 N A. Section 21 N A. Reserves D-24284. User-visible surface play.legendary-arena.com, D-24026 live-verify operator-pending. EC-511, cross-layer engine-then-client, drafted 2026-07-31 off origin main post-1126 reserve, not yet executed. Hard-deps D-24281 done plus WP-242 and WP-243 done)"]
        ["WP-477 ✅ Wire the discard-choice gate into TurnActionBar End-Turn and Pass-Priority (arena-client; WP-476 follow-on, lightweight lane. WP-476 added a hasPendingDiscardChoice gate to useTurnActions canEndTurn and canPassPriority but TurnActionBar.vue was outside the WP-476 allowlist so the gate is inert today, the End-Turn and Pass-Priority buttons stay enabled while a Magneto discard choice is pending and clicking them no-ops server-side via the authoritative engine block-all guard. This WP adds a hasPendingDiscardChoice prop to TurnActionBar.vue threaded into the useTurnActions calls at the position-16 slot, plus a hasPendingDiscardChoice computed in PlayDesktop.vue and PlayMobile.vue derived from snapshot.pendingDiscardChoice not undefined and passed as the prop, so the buttons disable with the WP-476 tooltip like every other pending choice. No engine change, no contract, no determinism impact, useTurnActions signature untouched. Section 10 client interaction No conflict. Section 20 N A. Section 21 N A. No new D-entry. User-visible surface play.legendary-arena.com, D-24026 live-verify operator-pending. EC-512, single-app three-file additive, drafted 2026-07-31 off origin main 967cd323. Hard-dep WP-476 done)"]
        ["WP-478 ✅ Empty-deck reshuffle for reveal and scry effects (packages game-engine; closes two dead-effect bugs from a live Magneto 1p match 2026-07-31 where the diagnostic snapshot showed deckCount 0 and discardCount 8. The Amazing Spider-Man Reveal the top three cards at step 15.2.6 and Doombot Legion Look at the top two cards KO one at step 19.2.37 both silently no-op'd on an empty deck instead of reshuffling the discard. Applies the standard Legendary rule whenever you need to draw OR reveal cards and your deck is empty shuffle the discard to form a new deck mid-effect, which drawCardsIntoHand already honors for drawing but the reveal and scry handlers deliberately skipped, D-21502 for reveal and WP-447 Scope-Out for scry. Adds a pure reshuffleDiscardIntoDeck playerZones shuffleContext helper that appends the reshuffled discard after any cards already left on top and no-ops on an empty discard so it terminates, used by both heroEffectReveal covering the whole reveal family via the one peek loop and villainEffectScryKoOwnDeck topping up toward look-2 before the unchanged 0 1 or 2-plus branch. Threads the move random into executeVillainAbilities per the villainDeck.reveal.ts RevealContext precedent from all three callers fightVillain plus the two villainDeck.reveal.ts sites which is 01.5 runtime wiring. No new G field no new pending state no contract array or union change, non-empty-deck path byte-identical, determinism preserved via the injected ctx.random.Shuffle so regenerate-with-note any record-game or replay fixture that hits the path with no sentinel re-pin, and do NOT refactor drawCardsIntoHand. Bug 2 the Amazing Spider-Man put the rest back in any order face up interactive reorder is a SEPARATE follow-on WP and Scope Out here. Section 17 gameplay fidelity No conflict. Section 20 N A. Section 21 N A no apps server endpoint. Lands D-24285 and supersedes D-21502. User-visible surface game log plus hand plus KO pile, D-24026 live-verify operator-pending. EC-513, standard two-session lane, drafted 2026-07-31 off origin main bbdfdf4b, not yet executed. No hard-dep WP)"]
        ["WP-479 ✅ Reveal remainder interactive put the rest back in any order (game-engine plus arena-client; Bug 2 companion to WP-478 Bug 1 from the same live Magneto 1p report. The Amazing Spider-Man Reveal the top three cards Put any that cost 2 or less into your hand Put the rest back in any order draws the cheap cards but the put the rest back in any order clause is not offered, the non-drawn remainder is silently left on top in revealed order, log blocked left on top at 7.2.2 and 12.2.2 and 12.2.3. Adds an INTERACTIVE reorder, after the heroEffectReveal peek loop when a reveal-reorder marked reveal left two or more cards on top it parks a PendingReorderChoice choiceType reorder-deck-top playerID cardIds the remainder equals deck slice 0 peekOffset onto a new G.pendingReorderChoices FIFO, resolveReorderChoice G playerID orderedCardIds accepts only a permutation of the parked ids and rewrites the top N of the deck leaving cards below untouched front-pops, a remainder of 0 or 1 auto-skips. Mirrors the WP-470 scry-KO and WP-476 discard-to-N machinery, a hasPendingReorderChoice block-all guard into every action move plus advanceStage and endTurn, a UIState pendingReorderChoice projection with the D-24011 private filter, a deterministic IDENTITY-order bot and sim default registered in SIMULATION_MOVE_NAMES and both sim MOVE_MAPs and the drift test with game.test.ts move-set plus one, and a client PendingReorderChoicePrompt.vue. Opt-in via a new HeroEffectDescriptor reorderRemainder flag from a bare keyword reveal-reorder MODIFIER marker which is a RECOGNIZED_NON_KEYWORD_MARKERS entry hand-appended to core.json NOT a HeroKeyword following the reveal-count precedent so HERO_KEYWORDS is unchanged, on The Amazing Spider-Man only, other reveal-and-reorder cards are a data follow-on. The peek loop terminal exhaustion return becomes a break so the post-loop park is reachable. The reorder is a PURE permutation no ctx.random. Determinism-adjacent, a new hashed pendingReorderChoices G field so finalStateHash plus PRE_WP080 sentinel DUAL re-pin is LIKELY where a fixture hits it. Section 17 gameplay fidelity No conflict. Section 10 client interaction. Section 20 N A. Section 21 N A no apps server endpoint. Lands D-24286. User-visible surface play.legendary-arena.com, D-24026 live-verify operator-pending. EC-514, standard two-session cross-layer lane, drafted 2026-08-01 off origin main 9eafaaa6, not yet executed. Hard-deps WP-470 done plus WP-476 done plus WP-478 done)"]
        ["WP-480 ✅ Wire the reorder-choice gate into TurnActionBar End-Turn and Pass-Priority (arena-client; WP-479 follow-on, lightweight lane. WP-479 shipped the reveal-remainder reorder prompt plus the engine block-all but deferred the client button gate so End-Turn and Pass-Priority stayed enabled while a reorder is pending, clicking no-ops server-side via the authoritative engine block-all guard. This WP adds a hasPendingReorderChoice param at position 17 after hasPendingDiscardChoice plus a canEndTurn and canPassPriority gate to useTurnActions with the reason Choose the order to put the cards back on top before taking another action, threads it through TurnActionBar.vue as a prop into the three position-16-reaching useTurnActions calls, plus a hasPendingReorderChoice computed in PlayDesktop.vue and PlayMobile.vue derived from snapshot.pendingReorderChoice not undefined and passed as the prop, so the buttons disable with the tooltip like every other pending choice. Unlike WP-476 to WP-477 where the composable param pre-existed, WP-479 deferred the whole gate so the composable param plus gate plus wiring land together here. No engine change, no contract, no determinism impact, no new D-entry the mechanic and decision are D-24286. Section 10 client interaction No conflict. Section 20 N A. Section 21 N A. User-visible surface play.legendary-arena.com, D-24026 live-verify operator-pending. EC-515, lightweight single-app five-file, drafted and executed 2026-08-01 off origin main fc4c136c. Hard-dep WP-479 done)"]
        ["WP-481 ✅ Mystique Escape becomes a Scheme Twist (packages game-engine plus card-data; first villain effect to trigger the scheme-twist rule pipeline from the escape path. Closes Mystique unimplemented Escape Mystique becomes a Scheme Twist that takes effect immediately, surfaced twice as a D-24266 no-handler breadcrumb in a live Magneto match 2026-08-01 log lines 180 and 196, two free Scheme Twists the player never faced. Appends one primitive become-scheme-twist no-param to the closed VillainEffectPrimitive union plus array at position 9 append-only with the drift test moved to 9 plus the local array in apply-effect-markers.mjs, with a deliberate NO-OP executor handler villainEffectBecomeSchemeTwist returning targets empty which is breadcrumb-suppression only following the gain-attached-hero precedent because the executor cannot reach the rule pipeline. The mechanic is a FIRE-SITE BRIDGE in villainDeck.reveal.ts escape branch, villainCardEscapeTriggersSchemeTwist scans the escaped card onEscape hooks and when present runs executeRuleHooks onSchemeTwistRevealed cardId plus applyRuleEffects which is the same two-call pattern the reveal path uses for a revealed scheme-twist card, so the active scheme twist resolver runs and schemeTwistCount increments and the loss threshold is checked via buildGenericTwistEffects, placed after the escape own consequences takes effect immediately. Mystique still escapes normally, resolvers use twistCardId only to stamp a schemeTwistResolved notableEvent so she is not routed into a twist pile. Marks Mystique core brotherhood mystique escape only 2 copies, other becomes-a-twist villains and non-escape timings are follow-on. Determinism-adjacent no new G field no ctx.random of its own no I O, but a hashed schemeTwistCount increment plus a schemeTwistResolved notableEvent from a new trigger path so finalStateHash plus PRE_WP080 sentinel re-pin is LIKELY where a fixture escapes Mystique, none at execution confirmed by unchanged whole-suite hashes. Section 17 gameplay fidelity No conflict. Section 20 N A. Section 21 N A. Lands D-24287. User-visible surface game log plus scheme-twist counter, D-24026 live-verify operator-pending. EC-516, standard two-session lane operator pre-authorized draft plus execute, reserve-first applied for the concurrent WP-472 session, drafted and executed 2026-08-01 off origin main 80193f05. Hard-deps D-24266 done plus WP-252 done plus WP-200 done)"]
        ["WP-482 ✅ Doctor Octopus reveal-eight strike discard top-up (packages game-engine; the Master Strike reveal-eight branch for co2e doctor-octopus reveal the top 8 cards discard all non-grey Heroes put the rest back in random order reveals a SHORT deck as-is instead of reshuffling the discard to reveal a full 8, the standard Legendary reveal-reshuffle rule. WP-397 D-24200 deferred the top-up citing a reshuffle would need an interaction model this work does not introduce plus matching the D-21502 empty-deck no-op, and WP-478 later shipped the reshuffleDiscardIntoDeck helper so the blocker is moot, and the printed text is random order not any order so NO player reorder is needed and D-24200 shuffle-back stays. This WP adds the top-up to resolveDoctorOctopusReveal in mastermindHandlers.ts, when deck length is under 8 and discard is non-empty and shuffleFunction is present it reshuffles the discard into the deck reusing reshuffleDiscardIntoDeck wrapping shuffleFunction as a ShuffleProvider before computing revealCount min 8 deck length, then reveal discard non-grey Heroes heroClass not null and return the remainder in random order unchanged. Reverses D-24200 short deck revealed as-is clause AND the D-24285 reveal-eight carve-out, lands D-24288 rationale-only no new primitive or keyword. game-engine only mastermindHandlers.ts plus test. Determinism-adjacent the top-up consumes an extra ctx.random.Shuffle when the deck is short so finalStateHash re-pin only if a fixture hits a short-deck reveal-eight, D-24200 pins core dr-doom and the reveal-eight branch is not reached so none expected confirm empirically. Draft-only session operator asked to draft execution is a later session. Section 17 gameplay fidelity No conflict. Section 20 N A. Section 21 N A. User-visible surface game log, D-24026 live-verify operator-pending. EC-517, standard two-session lane, reserve-first applied for the concurrent WP-473 session, drafted 2026-08-01 off origin main 09cbbc81, not yet executed. Hard-deps WP-397 D-24200 done plus WP-478 D-24285 done)"]
        ["WP-485 ✅ Core villain-effect vocabulary tier A auto-resolve primitives (game-engine plus card-data markers; implements 3 currently-unmarked unmarked-ability no-handler D-24266 Core villain Fight abilities as 3 new AUTO-RESOLVE primitives: Enchantress draw-cards-current 3 current player draws three, Destroyer ko-heroes-current-by-trait team shield KO all current player shield heroes from hand plus in-play, Baron Zemo rescue-bystanders-current-by-trait-count team avengers rescue N equals avengers hero count bystanders. Adds primitives to the closed union plus array D-24034 plus parser grammar plus handlers plus a countPlayerHeroesMatchingTrait helper plus apply-effect-markers.mjs vocabulary sync plus card markers villain-effect-markers.json to core.json. No interactivity no city-space no new G field. Tier A of the Core villain-effect-vocabulary arc surfaced by a live Doc Ock hollow in a Magneto Spider-Foes gauntlet game; follow-on tiers B named-city-spaces C recursive villain-deck play D interactive plus Viper E Doc Ock cleanup-draw override mapped in WP context. Lands D-24290 three new closed-union primitives. Section 17 section 1-9 No conflict. Section 20 N A. Section 21 N A. User-visible surface play.legendary-arena.com D-24026 live-verify operator-pending. EC-520 standard two-session lane drafted 2026-08-01 off origin main e1ed1e09. Hard-deps D-24266 plus D-24034. Done 2026-08-01, game-engine 2216 of 2216 pass with 15 new tests and whole-repo no-bail suite green, the three core Fight abilities Enchantress Destroyer Baron Zemo flip from unmarked to executable in the regenerated villain mechanic ledger with WP-485 D-24290 provenance and correct handler anchors, only the three core.json Fight lines changed, a shared cardTraitMatches plus trait-predicate parser were extracted at the third copy, NO finalStateHash or PRE_WP080 re-pin since no committed fixture reaches these fights confirmed by the green suite, D-24290 flipped to Active, D-24026 live-verify operator-pending fight each in a live match)"]
        ["WP-486 ✅ Instant-Defeat-With-Bystander Silent Sniper hero keyword (game-engine plus registry card-data plus arena-client; implements Silent Sniper black-widow/silent-sniper reprinted core msp1 3dtc printed Defeat a Villain or Mastermind that has a Bystander which is unmarked and silently does nothing with no runtime breadcrumb unlike villains D-24266, surfaced by a live Magneto Thor 2p game 2026-08-01 and corroborated by a Loki Thor 2p game. Adds a new HERO keyword defeat-with-bystander bare no-magnitude to the HeroKeyword union plus HERO_KEYWORDS array plus MVP_KEYWORDS plus HERO_EFFECT_HANDLERS plus apply-hero-ability-markers.mjs VALID_TOKEN_PATTERN plus hero-ability-markers.json for the 3 reprints regenerated into data cards. onPlay handler defeats one eligible target city Villain with a Bystander OR the Mastermind current tactic when the Mastermind holds a Bystander with NO attack spend REUSING the existing defeat path no duplicated onFight or award. Mandatory-if-able 0 self-narrated no-op never hollow, exactly 1 auto-defeat, 2-or-more interactive PendingDefeatChoice new hashed FIFO G.pendingDefeatChoices plus resolveDefeatChoice move plus block-all guards at every sibling fire site plus uiState.build projection under the D-24011 private filter plus deterministic bot sim default in ai.legalMoves plus arena-client PendingDefeatChoicePrompt.vue, projection prompt gate ship together no-UX-freeze. New hashed G field dual re-pin finalStateHash PRE_WP080 only if a committed fixture plays it confirm empirically. Lands D-24291 keyword plus pending-choice contract. Section 17 section 1-9 No conflict. Section 20 N A. Section 21 N A a new bgio move is not an apps server endpoint. Full two-session lane cross-layer. Templates WP-479 D-24286 pending-choice vertical plus WP-485 D-24290 keyword-vocab. User-visible surface play.legendary-arena.com D-24026 live-verify operator-pending. EC-521 DONE 2026-08-03 off origin main 14986144, D-24291 Active, game-engine 2240 and arena-client 1156 pass whole-repo no-bail green, NO finalStateHash PRE_WP080 re-pin confirmed empirically the committed-fixture replay test passes, move count 24 to 25 resolveDefeatChoice not a CORE_MOVE_NAME, shared defeat-core a documented internal invocation defeatCityVillainCore defeatMastermindTacticCore excluding spendAttack and hasActedThisTurn with the fight-move tests byte-unchanged, block-all span mirrors the reorder grep-span including game.ts advanceStage. Hard-deps WP-479 D-24286 plus WP-485 D-24290 plus D-24011)"]
        ["WP-488 ✅ Runtime Effect Tracing (piece 2/3 of the ewiki debug-effects.md direction; piece 1 = WP-484 index, piece 3 = WP-487 viewer; a runtime-only structured EffectTrace emitted on every card-effect dispatch, appended to a new hash-excluded G.diagnostics.traces array [mirrors the WP-257 hollow-record discipline lazy-init plus cap plus never-throw plus never-gameplay-input], recording cardId scope timing effect handler-as-string-label status [fired no-op no-handler secondary-site] fireSite params turn; emits from the caller loops executeVillainAbilities [villain-executor, no-op by a fixed allowlist become-scheme-twist plus gain-attached-hero not targets.length] and executeHeroEffects [hero-executor plus hero-primitive sub-paths] and the villainDeck escape-to-scheme-twist SECONDARY site so the Mystique D-24287 no-op-executor-but-twist-fired-elsewhere case is captured; determinism-critical since traces materialize on every dispatch so the WP also completes the D-24271-deferred second oracle computeStateHash replay.hash.ts gains the diagnostics exclusion hashGameState already has keeping both oracles diagnostics-blind with no re-pin of PRE_WP080_HASH or finalStateHash; inert never read by any move rule endIf bot or scoring, JSON-safe, capped; deferred zone-helper-touch capture and any client viewer UIState surface engine data only like WP-484; drafted 2026-08-02; EC-523; game-engine; standard two-session lane; User-Visible Surface none-infrastructure; D-24294)"]
        ["WP-487 ✅ Debug Effects Viewer (piece 3/3 of the ewiki debug-effects.md direction; piece 1 = the WP-484 index, piece 2 = runtime tracing future; a READ-ONLY /debug/effects dashboard page rendering the committed dual-scope data/metadata/effect-implementation-index.json as a searchable per-card × mechanic table [extId/name/set/scope/mechanic/status/handler/wp/decision + summary + scope/status/has-handler filters] mirroring CoveragePage.vue; consumed via the /coverage build-time-bundle path [a new build-effect-index.mjs copies the root index into gitignored apps/dashboard/src/data, static-imported by a new useEffectIndex composable that EffectImplementationIndexSchema.safeParses it — NOT R2, NOT mock-mode] + a debug/effects route + an AppLayout nav link + a check-generated-data.mjs guard; renders VERBATIM [blank handler/wp/decision → dash]; no effect data authored, no second parser, no engine/registry/server touch; NO runtime tracing / NO descriptor-granularity / NO R2 fetch — future/out-of-scope; drafted 2026-08-01; EC-522; App apps/dashboard; standard two-session lane; User-Visible Surface dashboard /debug/effects; renumbered from WP-485; D-24292)"]
        ["WP-489 ✅ Core villain-effect vocabulary tier B named city spaces (game-engine plus card-data markers; the engine has no named city spaces today G.city is index 0-4. Adds CITY_SPACE_NAMES as a PURE ENGINE CONSTANT sewers bank rooftops streets bridge no new G field, threads the fought cityIndex from fightVillain through executeVillainAbilities to the handlers trailing-optional shuffleContext precedent undefined at reveal fire sites gate fails closed, and adds a universal requireCitySpaces descriptor gate at-space marker suffix. Implements 2 currently-unmarked D-24266 Core Fight abilities as AUTO-RESOLVE reusing existing primitives no new VillainEffectPrimitive: Abomination capture-bystander 3 at streets plus bridge rescue three adds a count param, and the Lizard gain-wound each-other at sewers adds an each-other target. Whirlwind DEFERRED to Tier D its KO two of your Heroes is interactive current-player choice will reuse this gate plus the WP-242 pendingKoHeroChoices pipeline. Determinism no new G field but markers land in the hashed villainAbilityHooks so hash re-pin only if a committed fixture deck includes these two cards none today. Index to name binding operator-confirmed against the authoritative rulebook keywords-full glossary corroborating only. Tier B of the Core villain-effect-vocabulary arc; follow-on tiers C recursive villain-deck play D interactive plus Whirlwind and Viper E Doc Ock cleanup-draw override. Lands D-24295 named city spaces plus location gate plus descriptor extensions no new primitive. Section 17 section 1-9 No conflict. Section 10a section 22 no new G field. Section 20 N A. Section 21 N A. User-visible surface play.legendary-arena.com D-24026 live-verify operator-pending. EC-524 standard two-session lane drafted 2026-08-02 renumbered from WP-486 to 488 to 489 (1160 instant-defeat took 486, 1169 effect-runtime-tracing took 488). Done 2026-08-03 game-engine 2281 pass whole-repo no-bail green D-24295 Active core.json diff the 2 Fight lines only Abomination and the Lizard flip unmarked to executable in the regenerated villain ledger NO finalStateHash PRE_WP080 re-pin confirmed empirically no committed fixture deck includes core radiation or core spider-foes provenance map left unchanged the reused primitives capture-bystander and gain-wound already carry WP-252 D-24023 which correctly populates the two new rows D-24026 live-verify operator-pending. Hard-deps WP-485 D-24290 plus WP-252 plus WP-202 plus WP-478 plus D-24266 plus D-24034)"]
        ["WP-490 ✅ Destroyer team shield KO matches the basic S.H.I.E.L.D. cards (game-engine; fixes the WP-485 D-24290 Destroyer surfaced hollow-in-practice by the D-24026 live-verify Loki Thor 2p 2026-08-03 where Fight effect KO'd 0 of your shield Heroes while the player held a S.H.I.E.L.D. Trooper plus Agent. Root cause the three basic S.H.I.E.L.D. cards starting-shield-agent starting-shield-trooper pile-shield-officer are synthetic game components with NO G.cardTraits entry cardTraits built only from registry hero entries so ko-heroes-current-by-trait team shield matched zero. Narrow fix operator ruling 2026-08-03 widen the KO handler ONLY via a local koHeroMatchesTraitOrBasicShield predicate so a team shield predicate also names those three ext_ids while the shared cardTraitMatches playerHasHeroMatchingTrait countPlayerHeroesMatchingTrait are LEFT UNCHANGED so the 65 corpus team shield synergy references plus Baron Zemo rescue-by-count are untouched root-cause trait-tagging rejected as an unvetted power shift. Relocates SHIELD_AGENT_EXT_ID and SHIELD_TROOPER_EXT_ID into pilesInit.ts beside SHIELD_OFFICER_EXT_ID a cycle-free neutral leaf and re-exports from buildInitialGameState for back-compat. No new G field no new primitive no card-data or marker or ledger change. Determinism no committed fixture reaches a Destroyer team shield fight verified via green replay and sentinel suites so NO finalStateHash or PRE_WP080 re-pin. Done 2026-08-03 game-engine 2217 to 2220 pass with 3 new cases whole-repo no-bail green D-24296 Active. Section 17 section 1-9 No conflict. Section 20 N A. Section 21 N A. Lightweight lane. User-visible surface play.legendary-arena.com D-24026 live-verify operator-pending. EC-525. Hard-deps WP-485 D-24290. Lands D-24296)"]
        ["WP-491 ✅ Hero ledger design attribution which of a hero's cards carries each effect (shared-tooling plus registry plus app; the hero mechanic ledger scripts/hero-mechanic-ledger.mjs buildLedger merges every hero card DESIGN under one hero extId so /debug/effects and /coverage cannot say which of a hero's ~4 designs carries each effect — Black Widow core shows attack-per-count defeat-with-bystander draw optional-ko-reward rescue all keyed to core/black-widow though the source data attributes them per design mission-accomplished draw plus rescue, dangerous-rescue optional-ko-reward, covert-operation attack-per-count, silent-sniper defeat-with-bystander. ADDITIVE ATTRIBUTION not a row re-partition the ledger keeps ONE row per extId-mechanic and gains a designs array slug-sorted slug name of the carrying designs empty for the hero-level unmarked row SCHEMA_VERSION 1 to 2 so the ledger summary the /coverage metrics and card-mechanics.json all stay byte-identical and no row key collides a per-design-row split would have made 246 duplicate extId-mechanic rows. Threads an additive optional designs slug-name array through EffectImplementationEntrySchema marked-hero-only villains and unmarked omit into a new Design column on BOTH the WP-487 /debug/effects and WP-259 /coverage viewers. Unmarked stays hero-level operator ruling avoids growing the registry-viewer glossary unmarked bucket 46 to 231. extId not re-keyed build-time artifacts only no engine G RNG replay or hash surface no re-pin. Section 17 section 10 section 22 No conflict. Section 20 N A. Section 21 N A. User-visible surface dashboard /debug/effects plus /coverage D-24026 live-verify operator-pending. EC-526 standard two-session lane drafted 2026-08-03 not yet executed. Lands D-24297. Hard-deps WP-484 D-24289 plus WP-487 D-24292 plus WP-259 D-24035 plus WP-271)"]
        ["WP-492 ✅ Core villain-effect vocabulary tier D Whirlwind interactive location-gated KO (game-engine plus card-data markers; the first INTERACTIVE Core villain effect, deferred from WP-489 Tier B. Implements Whirlwind Fight If you fight Whirlwind on the Rooftops or Bridge KO two of your Heroes currently unmarked D-24266. Sits at the intersection of two shipped mechanisms and REUSES both no reinvention. REUSES WP-489 universal requireCitySpaces location gate Rooftops index 2 Bridge index 4 no gate-engine change. REUSES the WP-242 interactive KO-a-Hero pipeline pendingKoHeroChoices plus resolveKoHeroChoice plus block-all guards plus the WP-243 UIState projection plus the arena-client PendingKoHeroChoicePrompt which ALREADY renders remaining greater than 1 so NO arena-client change. The ONE new capability generalize the ko-hero target current handler to a magnitude M auto-KO the forced heroes and park an interactive choice only where a genuine which-hero choice exists distinct options at least 2 else auto-KO the forced step so no no-decision freeze M equals 1 byte-identical to koHeroCurrentPlayer. Carried by an ADDITIVE optional PendingKoHeroChoice remaining field KOs owed absent equals 1 append-only D-24034 plus a ko-hero current N grammar N at least 2 magnitude 1 stays the bare ko-hero current plus the Whirlwind marker ko-hero current 2 at rooftops plus bridge. The resolve move decrements remaining and auto-resolves any now-forced remainder before front-pop. UIState remaining equals the sum over the queue of entry remaining default 1 total KOs owed keeps the WP-243 multi-entry projection. NO new VillainEffectPrimitive reuse ko-hero plus the existing magnitude field primitives-count stays 12 NO new G field NO new choice type. magnitude at least 2 is keyword-less no legacy reverse-map so it self-narrates M equals 1 keeps the keyword must not double-log. Single layer game-engine plus card-data markers the Tier A B shape. Follow-on the other Tier-D interactive cards Viper Melter Paibok HYDRA-Kidnappers Maestro Ymir-onFight reuse this magnitude-N KO plus the WP-489 gate C recursive villain-deck play E Doc-Ock cleanup-draw. Determinism no new G field but remaining decorates the hashed pendingKoHeroChoices and the markers land in the hashed villainAbilityHooks so re-pin only if a committed fixture deck includes core masters-of-evil none today verify at execution. Provenance map UNCHANGED ko-hero already WP-252 D-24023. Lands D-24298 magnitude-N interactive current-player KO plus remaining field plus ko-hero current N grammar no new primitive. Section 17 section 1-9 No conflict. Section 10a section 22 no new G field. Section 20 N A. Section 21 N A. User-visible surface play.legendary-arena.com D-24026 live-verify operator-pending. EC-527 standard two-session lane drafted 2026-08-03. Done 2026-08-03 game-engine 2281 to 2296 pass with 15 new cases whole-repo no-bail green arena-client 1156 unchanged confirming the no-arena-client-change claim D-24298 Active core.json diff the one Whirlwind Fight line only Whirlwind flips unmarked to executable ko-hero in the regenerated villain ledger WP-252 D-24023 provenance unchanged NO finalStateHash PRE_WP080 re-pin no committed fixture deck includes core masters-of-evil. Landed the auto-vs-park rule in the physical-count P-vs-owed form park only when P greater than owed AND at least 2 distinct options else auto-KO so exactly-2-distinct auto-KOs both with no prompt per AC-3 a fold-inline refinement from the drafted deduped-O rule identical for M equals 1. D-24026 live-verify operator-pending. Hard-deps WP-489 D-24295 plus WP-242 D-24007 plus WP-243 D-24010 plus WP-252 D-24023 plus D-24266 plus D-24034)"]
        ["WP-493 ✅ Mechanic provenance backfill fill the blank WP and Decision columns on /debug/effects and /coverage (shared-tooling; effects that are executable with a live handler render a BLANK WP and Decision on /debug/effects and /coverage surfaced by the operator 2026-08-03 via asrd/warriors-three-the draw. Root cause is a provenance gap not a code gap both ledger generators read scripts/coverage/mechanic-provenance.json VERBATIM for the wp and decision columns buildRow provenance mechanic and that map was only seeded from later mechanic-implementing WPs so the foundational MVP keywords plus 4 recent villain primitives were never added and the ledgers pass empty-string through and the effect index fed verbatim shows blank. Adds 17 traced keys 13 hero draw WP-022 D-2201, rescue WP-215 D-21501, undercover WP-282 D-24060, size-changing WP-290 D-24074, gain-wound-self and gain-wound-each WP-364 D-24156, ko-wound-reward WP-382 D-24183, discard-to-play WP-383 D-24184, victory-villain-attack WP-285 D-24099, plus 4 decision-only mechanics whose wp stays empty as INFRA or bug-fix commits with no formal WP put-any-number-bottom-hq D-24132 put-bottom-hq-icon-reward D-24133 optional-put-bottom-hq D-24130 return-zero-cost-discard D-24139, plus 4 villain scry-ko-own-deck WP-447 D-24267, reveal-or-wound WP-469 D-24281, become-scheme-twist WP-481 D-24287, gain-attached-hero WP-450 D-24270. DATA-ONLY 1 source edit the map plus regenerated hero and villain ledgers plus effect index ledger-heroes-check ledger-villains-check effect-index-check. Row identity untouched only the wp and decision cells of 173 executable rows change both summary blocks plus card-mechanics.json byte-identical. Verbatim pass-through means NO generator schema or dashboard change and every cited decision predates this WP reserves none. WP-491 the designs column on the SAME artifacts landed 1192 orthogonal fields regenerate on top. Section 17 section 10 section 22 No conflict. Section 20 N A. Section 21 N A. User-visible surface dashboard /debug/effects plus /coverage D-24026 live-verify operator-pending. EC-528 standard two-session lane drafted 2026-08-03 not yet executed. Lands NO new decision. Hard-deps WP-484 D-24289 plus WP-487 D-24292 plus WP-259 D-24035)"]
        ["WP-494 ✅ Core villain-effect vocabulary tier D Viper conditional victory-pile-gated each-player wound (game-engine plus card-data markers; implements Viper core/hydra/viper Fight Each player without another HYDRA Villain in their Victory Pile gains a Wound plus Escape Same effect currently unmarked D-24266. A CONDITIONAL each-player wound NOT interactive despite the WP-485 tier-map interactive label each player gains a Wound UNLESS their Victory Pile holds another HYDRA villain. Adds one new keyword-less VillainEffectPrimitive gain-wound-unless-victory-villain-group count 12 to 13 append-only union plus array plus drift D-24034 plus an additive descriptor field victoryVillainGroup plus the grammar gain-wound-unless-victory-villain-group groupSlug. The handler mirrors villainEffectRevealOrWound sorted per-player iteration supply-bound woundsDrawn current-only self-narrate. PATH B NO new G field NO broad re-pin the group ext_id prefix is DERIVED from the fought card own ext_id setAbbr before the unambiguous villain infix prefix setAbbr-villain-groupSlug Victory-Pile villains matched by startsWith excluding the fought cardId another. A new G.villainGroups setup map would be hashed with no exclusion bucket so it would force a BROAD finalStateHash PRE_WP080 re-pin of ALL committed fixtures Path B reads only already-hashed state. Marked on BOTH fight plus escape the onEscape fire site already runs the executor. New primitive so one new provenance entry WP-494 D-24299 unlike Whirlwind which reused ko-hero. Single layer game-engine plus card-data markers the Tier A B shape. Follow-on the sibling victory-pile-predicate cards Chitauri Leviathan no Bystanders in Victory Pile and Hela-2099 may reuse this primitive Endless Armies Tier C recursive and HYDRA Kidnappers optional not marked here. Lands D-24299 new primitive plus descriptor field plus Path-B group identification. Section 17 section 1-9 No conflict. Section 10a section 22 no new G field. Section 20 N A. Section 21 N A. User-visible surface play.legendary-arena.com D-24026 live-verify operator-pending. EC-529 standard two-session lane drafted 2026-08-03. Done 2026-08-03 game-engine 2296 to 2308 pass with 12 new cases whole-repo no-bail green arena-client 1156 unchanged D-24299 Active core.json diff Viper two Fight Escape lines only Viper flips unmarked to executable gain-wound-unless-victory-villain-group in the regenerated villain ledger and effect-implementation-index with new WP-494 D-24299 provenance co2e twin stays unmarked set-scoped NO finalStateHash PRE_WP080 re-pin no committed fixture deck includes core hydra. Path B held no new G field the group prefix derived from the fought ext_id anchored on the full setAbbr-villain-group prefix the bystander-villain-deck false-match guard is unit-pinned. D-24026 live-verify operator-pending. Hard-deps WP-469 D-24281 plus WP-202 D-20203 plus WP-252 D-24023 plus WP-489 D-24295 plus D-24266 plus D-24034)"]
        ["WP-495 ✅ Provenance decision backfill fill the remaining blank Decision cells on /debug/effects and /coverage (shared-tooling; WP-493 follow-on. After WP-493 filled the never-attributed mechanics the only executable rows still showing a blank Decision are the 6 provenance-map keys that carry a wp but decision empty-string. Fills 75 rows across 5 rendered mechanics koHeroCurrentPlayer WP-185 D-18503 its specific resolution decision mirroring captureBystander D-18506, gainWoundEachPlayer WP-185 D-18502 plus heroDeckTopToEscape WP-185 D-18502 plus gainWoundCurrentPlayer WP-185 D-18502 zero rows map-hygiene where D-18502 is the WP-185 five-keyword vocabulary-lock that DEFINES them no mechanic-specific D, attack-per-count WP-247 D-24016, optional-ko-reward WP-248 D-24019. DATA-ONLY sets 6 decision values each wp unchanged plus regenerated hero and villain ledgers plus effect index ledger-heroes-check ledger-villains-check effect-index-check. Row identity untouched only the decision cell of 75 executable rows changes both summary blocks plus card-mechanics.json byte-identical. Verbatim pass-through means NO generator schema or dashboard change and every cited decision predates this WP reserves none. Section 17 section 10 section 22 No conflict. Section 20 N A. Section 21 N A. User-visible surface dashboard /debug/effects plus /coverage D-24026 live-verify operator-pending. EC-530 standard two-session lane drafted 2026-08-03 not yet executed. Lands NO new decision. Hard-deps WP-493 EC-528 plus WP-484 D-24289)"]
        ["WP-496 ✅ Coverage decision column /coverage by-card table provenance parity (app apps/dashboard; the /coverage by-card table rendered Card Design Set Mechanic Status WP Handler but OMITTED the Decision column even though the same page by-mechanic table plus /debug/effects both show it and every by-card row already carries row.decision the LedgerRow type declares it and the build-time coverage bundle is a full byte-copy of the committed ledger. After WP-493 and WP-495 filled the decision values this was the last surface still hiding them. One additive Vue display column a th Decision header plus a td row.decision or dash cell inserted between WP and Handler mirroring the by-mechanic WP Decision Handler order and the /debug/effects WP plus Decision pairing. NO schema or type or bundle or generated-artifact or engine change reserves no decision. Verified locally on the dev server header Card Design Set Mechanic Status WP Decision Handler executable rows show filled decisions undercover WP-282 D-24060 defeat-with-bystander WP-486 D-24291 blanks show dash never fabricated. Dashboard test 435 of 435 plus build 0 pnpm -r build 0. Section 17 section 22 No conflict. Section 20 N A. Section 21 N A. User-visible surface dashboard /coverage D-24026 live-verify operator-pending verified locally deployed confirmation operator-gated. EC-531 lightweight lane 2026-08-04. Lands NO decision. Hard-deps WP-259 D-24035 plus WP-493 plus WP-495)"]
        ["WP-497 ✅ mastermind tactic onFight execution framework plus Doc Ock Octet of Valence Electrons (game-engine; first WP of the Mastermind-Tactic-Fight arc. Defeating a Mastermind tactic fires NONE of its printed Fight ability today defeatMastermindTacticCore moves the tactic to the victory pile rescues bystanders and on the final tactic sets the endgame counter but runs no tactic text the tactic text effects are WP-024 comment is stale WP-024 did strikes and schemes tactic Fight was scoped out of WP-316 WP-386 WP-388. Surfaced live 2026-08-04 co-op match KdHnMXaOPin all four Doc Ock tactics defeated zero effect. Adds a per-tactic onFight dispatcher keyed by the defeated tactic ext_id setAbbr-mastermind-slug-tacticSlug in a new rules tacticHandlers.ts mirroring the WP-386 WP-388 per-mastermind resolver pattern operator ruling 2026-08-04 not a data-driven marker vocabulary abstract later on the third copy unknown tactic id silent no-op every non-Octet tactic stays as inert as today strictly additive. Implements the first faithful resolver co2e Doctor Octopus Octet of Valence Electrons draw a new hand this turn draw 8 instead of 6 via a new optional lazily-created hashed field handSizeOverrides per-player next onBegin fill override NOT seeded in buildInitialGameState consumed and cleared at the play-phase onBegin fill in game.ts this engine has no end-of-turn cleanup draw so your new hand this turn equals the defeating player next onBegin fill. Determinism the field is gameplay-affecting hashed but lazy-create means no committed fixture empty-registry PRE_WP080 or core dr-doom sentinel ever creates the key so no re-pin expected verify both oracles drift is STOP never blind re-pin. No Magneto-cap interaction one mastermind per match no RNG Octet reveals nothing. Section 17 section 3 No conflict NG-1 to 7 not crossed. Section 20 N A. Section 21 N A. User-visible surface play.legendary-arena.com D-24026 defeat Octet next hand draws 8 plus Fight log line. Out of scope follow-on WPs the other three Doc Ock tactics Octal Octyls scry-1 High Octane Hero-Deck reveal-gain needs ctx.random Absolute Octarchy each-player reveal-or-wound plus all other masterminds tactics. Standard two-session lane new hashed G field plus determinism surface. Reserves D-24300. EC-532 draft 2026-08-04. Hard-deps WP-019 plus WP-024 plus WP-386 D-24188 plus WP-388 D-24192 plus WP-236)"]
        ["WP-506 ✅ core Magneto tactic Crushing Shockwave onFight resolver (game-engine; second resolver of the Mastermind-Tactic-Fight arc after WP-497 Doc Ock Octet. Defeating core Magneto Crushing Shockwave tactic fires none of its printed Fight ability today dispatchTacticOnFight ships only the Octet resolver so every core Magneto tactic ext_id falls through to the silent no-op surfaced live 2026-08-06 in a core Magneto co-op match log 17.2.10 Bitter Captor and 20.2.15 Xavier Nemesis tactic defeated zero tactic effect. Implements Fight each other player reveals an x-men Hero or gains two Wounds a new resolveCrushingShockwave in rules tacticHandlers.ts plus a dispatch branch keyed by core-mastermind-magneto-crushing-shockwave. No-choice deterministic per-player pass mirroring mastermindHandlers resolveCo2eMagnetoStrike reveal-or-penalty but three deltas REVEAL not discard an x-men holder loses nothing a log-only no-op TWO Wounds not one gainWound up to twice supply-bound and each OTHER player skip ctx.currentPlayer. Inlines the x-men-in-hand scan plus wound gain duplicate-first no mastermindHandlers.ts change no new G field. Determinism mutates G.piles.wounds plus player discard but only on a Magneto Crushing Shockwave defeat no committed fixture does so so finalStateHash plus PRE_WP080 byte-identical no re-pin expected drift is STOP. Scope tacticHandlers.ts plus its test only. Section 17 section 3 No conflict NG-1 to 7 not crossed determinism line. Section 20 N A. Section 21 N A. User-visible surface play.legendary-arena.com defeat Crushing Shockwave each other x-men-less player gains two Wounds plus Fight log lines D-24026 live-verify operator-pending. Standard two-session lane. EC-541 draft 2026-08-06. Reserves D-24312. Hard-deps WP-497 D-24300 plus WP-388 D-24192 plus WP-476 D-24284 plus WP-179 plus WP-016 D-15401)"]
        ["WP-507 ✅ mastermind-tactic coverage in the effect-implementation index debug effects (registry plus shared-tooling plus dashboard app; closes the observability gap that hid the WP-497 arc mastermind tactic cards appear nowhere in data metadata effect-implementation-index.json the debug effects surface because the index is a verbatim join of the hero plus villain mechanic ledgers only and tactics are in neither. Result debug effects shows just two Magneto rows hero dodge plus the mastermind-villain card unmarked and never the four tactic Fight abilities so an unimplemented tactic is invisible until played reported 2026-08-06. Adds a new closed-enum mastermind scope registry EFFECT_INDEX_ENTRY_SCOPES plus byScope fed by a generator tactic feed that enumerates tactic identity from card data masterminds cards tactic true ext_id setAbbr-mastermind-slug-tacticSlug mechanic equals tactic slug and overlays a small committed scripts coverage tactic-provenance.json for status handler wp decision verbatim honesty per WP-493 unmapped to unmarked plus blanks chosen over folding tactics under villain conflates or a parallel index dashboard would merge. Dashboard debug effects gains a Mastermind filter chip plus byScope count plus badge scopeLabel case plus EffectsPage.vue the bundled index JSON is gitignored regenerated by prebuild effect-index. CI-gated regen effect-index check. Generator stays engine-free card data plus committed JSON only. No engine G runtime determinism surface no card-data change. Section 17 section 22 product legibility No conflict determinism untouched NG-1 to 7 not crossed. Section 20 N A. Section 21 N A. User-visible surface dashboard.legendary-arena.com debug effects D-24026 live-verify operator-pending. Standard two-session lane cross-layer. EC-542 draft 2026-08-06. Reserves D-24313. Hard-deps WP-484 D-24289 plus WP-497 D-24300 plus WP-506 D-24312)"]
        ["WP-508 ✅ Escaped-pile bystander carry-away plus Midtown Bank Robbery resource loss (game-engine; first WP of the resource-loss-scheme-fidelity epic D-24178 records the six resource-loss schemes as unmodeled losing on a twist-count doom-clock proxy. Fixes Midtown Bank Robbery printed Evil Wins when 8 Bystanders are carried away by escaping Villains unmodeled today the escape path RETURNS captured bystanders to G.piles.bystanders resolveEscapedBystanders the opposite of carried away so the condition is untracked and Midtown loses on the twist proxy at twist 8. Surfaced live 2026-08-07 core Magneto Midtown co-op ended scheme-wins at twist 8 with an EMPTY escaped pile plus a dominant hero board. Scope one escaping villains carry captured bystanders into G.escapedPile not supply D-24314. Two new data-only SchemeTwistConfig.resourceLossCondition kind escaped-pile-count cardType threshold plus pure countEscapedPileByType and applyEscapedPileResourceLoss setting SCHEME_LOSS when escapedPile holds at least threshold entries of cardType classified via G.villainDeckCardTypes checked in the escape path evaluateEndgame stays counter-only D-24315. Three suppress the twist-count proxy for any scheme declaring a resourceLossCondition. Four wire Midtown bystander at least 8. Out of scope Negative Zone plus ESCAPE_LIMIT-per-scheme next WP stack-depletion schemes Legacy Virus Civil War conversion schemes Secret Invasion Killbots. No new G field. Determinism carry-away plus SCHEME_LOSS are hashed re-pins only a fixture that escapes a villain WITH captured bystanders none committed so finalStateHash plus PRE_WP080 byte-identical expected drift is STOP. Single-layer game-engine standard two-session lane. Section 17 section 3 faithful rules No conflict NG-1 to 7 not crossed determinism line. Section 20 N A. Section 21 N A. User-visible surface play.legendary-arena.com D-24026 live-verify operator-pending. EC-543 draft 2026-08-07. Reserves D-24314 plus D-24315. Hard-deps WP-200 D-24178 plus WP-153)"]
        ["WP-511 ✅ Legacy Virus wound-stack-depletion loss plus scheme-specific setup sizing (game-engine; fourth WP of the resource-loss-scheme-fidelity epic. Models Legacy Virus printed Evil-Wins if the Wound stack runs out by reusing WP-510 pile-depleted kind widened to pile wounds G.piles.wounds.length AND introduces the first scheme-specific setup sizing Legacy Virus wound stack built at 6 times numPlayers its printed 6 Wounds per player instead of the flat 30. Both ship together loss without sizing is unreachable sizing without loss stays on the proxy. Sizing is a post-validation engine override at the buildGlobalPiles call via a pure resolveEffectiveWoundsCount helper in buildInitialGameState the 30 woundsCount floor D-24032 is NOT changed it governs the requested config the scheme rule governs the built pile below floor by design. No tie interaction the final-turn latch ignores wounds. Deliberate hash re-pin of the sole Legacy Virus sentinel fixture sentinel-core-doom-2p finalStateHash 30 to 12 wounds regenerated via record-game-fixture. PRE_WP080_HASH is NOT Legacy Virus and MUST stay ec64506a. Nine-file game-engine allowlist incl new setup schemeSetupSizing and setup-layer buildInitialGameState. Section 17 section 3 deliberate-re-pin line. Section 20 N A. Section 21 N A. User-visible surface play.legendary-arena.com D-24026 operator-pending. EC-546 draft 2026-08-07. Reserves D-24320 plus D-24321. Hard-dep WP-510. Civil War 4-heroes-2p sizing to WP-512 conversion schemes later)"]
        ["WP-510 ✅ Super Hero Civil War hero-deck-depletion loss plus the pile-depleted resource-loss kind (game-engine; third WP of the resource-loss-scheme-fidelity epic. Models Super Hero Civil War printed Evil-Wins if the Hero Deck runs out. Introduces a NEW resourceLossCondition kind pile-depleted with pile heroDeck on the WP-508 framework widening SchemeResourceLossCondition to a discriminated union on kind with the escaped-pile-count member unchanged plus a new applyPileDepletionResourceLoss helper wired to the play-phase turn.onMove hook beside latchFinalTurnIfDeckExhausted a central per-move chokepoint that catches twist-driven hero-deck depletion not only recruitHero. Civil War own twist KO all HQ heroes drains the hero deck so the loss is reachable at default size. Tie-override is FREE D-24319 SCHEME_LOSS precedes FINAL_TURN_TIE in evaluateEndgame and the tie-resolution guard fires only when evaluateEndgame is null so hero-deck-empty pre-empts the deck-exhaustion tie with no finalTurn.logic change. Civil War ONLY Legacy Virus splits to WP-511 needs per-player wound-stack sizing 6 per player else unlosable when the proxy is suppressed conversion schemes shift to WP-512. No G shape change so finalStateHash plus PRE_WP080 byte-identical outcome-only shifts on the retired proxy. game.ts turn.onMove wiring per 01.5. Seven-file game-engine allowlist standard two-session lane. Section 17 section 3 determinism line. Section 20 N A. Section 21 N A. User-visible surface play.legendary-arena.com D-24026 operator-pending. EC-545 draft 2026-08-07. Reserves D-24318 plus D-24319. Hard-dep WP-508 WP-509)"]
        ["WP-509 ✅ Negative Zone Prison Breakout villain-escape loss plus retire the generic ESCAPE_LIMIT proxy (game-engine; second WP of the resource-loss-scheme-fidelity epic. Models Negative Zone printed Evil-Wins if 12 Villains escape AND retires the generic escapedVillains greater-or-equal ESCAPE_LIMIT 8 loss from evaluateEndgame. REUSES the WP-508 escaped-pile-count kind with cardType villain villains-only per rulebook Schemes that Count Escaped Villains henchmen excluded escaped villains are typed villain in villainDeckCardTypes so NO new kind and no schemeResourceLoss or types change. evaluateEndgame stays counter-only escape loss now flows through SCHEME_LOSS in the escape path. coopOutcome keeps its loss-villains-escaped category plus escapedVillains cap branch reworded to a loss-cause heuristic since Negative Zone losses carry escapedVillains at least 12 ESCAPED_VILLAINS counter plus ESCAPE_LIMIT constant retained no COOP_OUTCOME_CATEGORIES or CoopGameRecord churn. Determinism no G shape change evaluateEndgame is a pure counter read outside the hashed state so finalStateHash plus PRE_WP080 byte-identical outcome-only shifts on the retired proxy. Six-file game-engine allowlist standard two-session lane. Section 17 section 3 determinism line. Section 20 N A. Section 21 N A. User-visible surface play.legendary-arena.com D-24026 operator-pending. EC-544 draft 2026-08-07. Reserves D-24316 plus D-24317. Hard-dep WP-508 D-24314 D-24315)"]
        ["WP-512 📝 Recover runtime-observed coverage Portals backdrop plus deterministic-termination guard (shared tooling sim scripts plus coverage artifact plus dashboard test NO packages change; the D-24322 coverage-backdrop follow-up. WP-511 switched the runtime-observed hollows sweep runtime-observed-hollows.mjs plus the co-op yardstick coop-winrate.mjs off Legacy Virus deck-dependent loss stopped terminating the solo sweep onto Cosmic Cube costing coverage the committed artifact fell about 16 to 10 distinct mechanics dashboard totalObs 178 to 163. This WP switches both backdrops to Portals to the Dark Dimension core portals-to-the-dark-dimension a faithful true-twist-loss scheme printed Twist 7 Evil Wins that loses at twist 7 via the existing MVP_SCHEME_TWIST_THRESHOLD fallback NO engine change and whose Dark-Portal twists buff the board rather than adding Wounds to hero decks so the competent bot plays more abilities. Scaffolded at draft 10 to 12 distinct mechanics plus rooftops plus streets dashboard totalObs 163 to 188 above the pre-WP-511 178 resolvedObs 59 unchanged percentResolved 36.2 to 31.4 larger honest denominator 312 of 312 games terminate about 3 s. Rejects the explicit-lossThreshold engine option a SCHEME_TWIST_CONFIGS entry needs a required resolverId contract-touching for zero behavioral gain Portals already loses at 7 via the fallback. The 16 plateau is NOT recoverable Legacy-Virus-specific now deck-dependent Portals plateaus at 12 across 8 16 24 seeds so SEEDS_PER_BOARD stays 8. Adds a fast deterministic-termination guard to the coverage harness throw ProbeFailure if any swept game did not reach endgame so a future non-terminating backdrop fails loudly instead of timing out the sim runtime-observed check gate the WP-511 silent-timeout lesson. Coop yardstick switched in lockstep cosmetic 0 percent bot win-rate is backdrop-independent a Bot-Ally-epic concern. No finalStateHash PRE_WP080 interaction. Four-file allowlist standard two-session lane. Section 17 section 22 observability plus section 3 faithful determinism line. Section 20 N A. Section 21 N A. User-visible surface dashboard.legendary-arena.com coverage D-24026 live-verify operator-pending. EC-547 draft 2026-08-08. Reserves D-24323. Hard-dep WP-511 D-24322)"]
        ["WP-499 ✅ Join by match ID or link lobby manual-join affordance (app apps/arena-client; adds a Join by match ID or link input to the lobby paste a raw match ID or a full invite link route lobby match id the client extracts the id via matchReference parseMatchReference fetches that single match read-only via lobbyApi fetchMatch GET games legendary-arena id so unlisted matches the public list omits are reachable and joins the first open seat via the EXISTING joinExisting and joinMatch POST api match join no server change no new endpoint. Identity-agnostic mirroring the WP-369 copy-link that already works the reliable way to play with a brand-new friend while the never-wired handle-claim system claimHandle has zero non-test call sites surfaced 2026-08-04 blocks by-handle match invites the handle fix is a separate Track-2 follow-up WP. Empty or malformed input unknown match 404 to null no open seat or a finished match each show inline error copy and no join. Standard two-session lane not lightweight proper UI coverage touches LobbyView test file a sixth code test file and the surface is multiplayer-join-adjacent zero determinism persistence identity surface. Section 17 section 22 section 23b No conflict NG-1 to 8 clear. Section 20 N A. Section 21 N A reuses existing endpoint no apps server change. User-visible surface arena-client lobby D-24026 live-verify operator-pending. EC-534 standard two-session lane drafted 2026-08-04 not yet executed. Reserves D-24302. Hard-deps WP-307 D-24092 plus WP-369)"]
        ["WP-500 ✅ Auto-assign changeable handles at sign-in plus backfill (server apps/server; Track 2 core of the friend-invite fix. The handle-claim feature was never wired claimHandle has zero non-test callers so handle_canonical is NULL for every account and every at-handle affordance friend-request WP-351 match-invite WP-358 WP-366 returns handle_not_found for every target diagnosed under WP-499. Makes every account reachable by at-handle auto-assign a CHANGEABLE handle derived from display_name at first-sign-in provisioning new pure deriveHandle slug helper to HANDLE_REGEX with player fallback plus idempotent collision-safe assignAutoHandle which sets handle_canonical and display_handle and leaves handle_locked_at NULL equals unlocked changeable wired fail-open into attemptProvisioning idempotent WHERE handle_canonical IS NULL so it self-heals existing accounts on next login too plus a one-time scripts backfill-handles.mjs for the dormant NULL rows. Amends the WP-101 mutual-presence invariant handle_locked_at becomes independent NULL equals auto-assigned now equals explicit claim so assignAutoHandle is a sanctioned second disjoint writer of the handle columns. No schema migration cols exist since migration 008. The by-at-handle flows resolve with no code change. Out of scope paired follow-up the change-handle claim endpoint plus MyProfilePage field plus rewiring claimHandle guard. Zero engine determinism surface handles stay routing aliases AccountId still the trust key. Section 17 section 22 No conflict. Section 20 N A. Section 21 Library-only fns. User-visible surface at-handle reachability indirect D-24026 operator-pending needs prod backfill run. EC-535 standard two-session lane drafted 2026-08-05 not yet executed. Reserves D-24303. Hard-deps WP-101 plus WP-174 plus WP-499 D-24302)"]
        ["WP-501 ✅ Change your handle owner-profile handle edit (server plus arena-client; Track 2 follow-up. WP-500 auto-assigns every account a handle but there is no way to change it a slug collision leaves e.g. jeff2 when the user wanted jeff. Lets a signed-in user change their handle. New changeHandle accountId requestedHandle db in handle.logic.ts validateHandleFormat then UPDATE handle_canonical plus display_handle WHERE ext_id equals and handle_locked_at IS NULL freely changeable while unlocked per the D-24303 model a third disjoint writer never handle_locked_at 23505 to handle_taken reuses the existing HandleErrorCode incl handle_already_locked so NO handle.types.ts change returns handleCanonical displayHandle. New PATCH api me handle authenticated-session in handle.routes.ts plus server.mjs wiring plus MyProfilePage.vue handle-edit affordance plus ownerProfileApi changeHandle wrapper. The previous handle is released no redirect. Out of scope deferred the explicit lock claim action plus claimHandle guard rewire and change rate-limit cooldown. No schema migration no engine determinism surface. Section 17 section 22 No conflict AccountId stays the trust key friendships survive a handle change. Section 20 N A. Section 21 adds PATCH api me handle plus changeHandle Library-only. User-visible surface route me change-handle field D-24026 live-verify operator-pending. EC-536 standard two-session lane drafted 2026-08-05 not yet executed. Reserves D-24305. Hard-deps WP-101 plus WP-500 D-24303 plus WP-104)"]
        ["WP-502 ✅ Match end-of-life controls Play Again plus End Game (engine plus server plus arena-client; two player-facing play-surface controls Play Again on match end relaunch the same loadout at seat 0 the arcade insert another quarter replay plus End Game during a live match confirm-gated closes the match out for every seat when a co-op table runs out of time. Engine a new MATCH_ENDED_EARLY endgame counter evaluateEndgame checks it first to an endedEarly tie set by a new server-only endMatchEarly move never throws no-op when already over NOT a CORE_MOVE_NAME the additive optional endedEarly marker rides EndgameResult plus UIGameOverState outcome stays tie so no EndgameOutcome union change. Modeled as the counter not events.endGame because buildUIState derives gameOver from evaluateEndgame G.counters not ctx.gameover an event-end would never surface the endgame panel. An early-ended match is never scored the server rejects the submission with a new ended_early SubmissionRejectionReason before any INSERT authoritative plus the client submit-on-gameover hook skips the POST to ineligible advisory. Play Again relaunches solo via launchMatchFromComposition using the stashed loadout plus snapshot seat count. No new top-level G field so no finalStateHash PRE_WP080 re-pin runtime-only counter. Section 17 section 22 No conflict no RNG no G persistence replay-safe. Section 20 N A. Section 21 no new HTTP endpoint. User-visible surface Play Again plus End Game on play.legendary-arena.com D-24026 operator-pending post-deploy. EC-537 standard two-session lane executed 2026-08-05. Lands D-24306)"]
        ["WP-503 ✅ Doctor Octopus villain Fight draw 8 instead of 6 next hand (game-engine plus card-data BLOCKED on WP-497; implements the core spider-foes Doctor Octopus villain Fight ability When you draw a new hand of cards at the end of this turn draw eight cards instead of six core/spider-foes/doctor-octopus copies 2 surfaced as a no-handler hollow in a live Magneto match 2026-08-05 zdcAitTRmIY log 17.2.17. Adds ONE new keyword-less VillainEffectPrimitive override-next-hand-size 13 to 14 append-only D-24034 plus marker effect override-next-hand-size 8 on the Fight line the handler sets G.handSizeOverrides currentPlayer 8 at onFight. REUSES WP-497 shared hashed handSizeOverrides field AND its play-phase onBegin consumption verbatim this WP adds only the villain-side WRITER plus marker no new G field no second consumption. DISTINCT from the co2e MASTERMIND Doc Ock Octet WP-497 mastermind-tactic path same effect plus field different subsystem. Live composition unlike the mastermind Doc Ock the villain Doc Ock CAN co-occur with Magneto MAGNETO_HAND_SIZE_LIMIT trim it did Magneto cap is a Master-Strike-time discard-to-4 ORTHOGONAL to Doc Ock onBegin fill not a precedence execution adds an independence test. Determinism writes the WP-497-hashed field so re-pin only if a committed fixture fights Doc Ock none expected. Single layer engine plus card-data markers. Section 17 section 1 2 10 No conflict determinism line. Section 20 N A. Section 21 N A. User-visible surface play.legendary-arena.com D-24026 live-verify operator-pending. EC-538 villain-effect-vocab BLOCKED on WP-497 drafted 2026-08-05 not yet executed. Reserves D-24307. Hard-deps WP-497 D-24300 BLOCKED plus WP-252 D-24023 plus WP-485 D-24290 plus D-24266 plus D-24034)"]
        ["WP-504 ✅ Friend request by handle OR Account ID handle-or-UUID matching (server plus arena-client; makes the add-friend surface identifier-agnostic. POST api me friends requests accepts EITHER handle unchanged OR accountId a well-formed UUID equals the target own AccountId shared out-of-band a copy-paste capability like WP-499 match link NOT a directory you can only add an account whose ID was deliberately given to you. Exactly one of the two is required both or neither to invalid_request. New shared pure guard isWellFormedAccountId in identity.logic.ts plus new shared client discriminator parsePlayerIdentifier the accountId branch resolves via the existing findPlayerByAccountId to a new account_not_found 404 code then funnels into the SAME WP-355 block cooldown rate-limit chain plus sendFriendRequest self_friendship and already handled in the logic layer no new self guard. account_not_found joins the FriendApiErrorCode union plus FRIEND_API_ERROR_CODES array server-side AND the client mirror plus both drift tests in lockstep five sites. Client add-friend input accepts handle or Account ID. FR-2 preserved an output-redaction invariant friend responses still emit handle plus displayName only never accountId this change is input-only and emits no identifier D-24308 records the scope clarification. Why the friend-request surface not match-invites invites are friend-gated createMatchInvite to not_friends so a valid invite target is always already a friend addressable by handle UUID-matching there is redundant the paired WP-505 was drafted and dropped numbers retracted. No engine registry data persistence surface no schema migration no finalStateHash PRE_WP080 re-pin. Section 17 section 22 No conflict FR-2 note input-only. Section 20 N A. Section 21 request schema gains accountId row replaced at execution per D-11804. User-visible surface the add-friend input on play.legendary-arena.com D-24026 live-verify operator-pending. EC-539 standard two-session lane drafted 2026-08-05 not yet executed. Reserves D-24308. Hard-deps WP-350 351 plus WP-355 D-24147 plus WP-499 D-24302)"]
        ["WP-505 ✅ Captured cards under villains and mastermind board display of captured heroes and bystanders (game-engine UIState projection plus arena-client render; renders captured cards on the play board beneath city villains and the mastermind. All three capture stores are tracked in G at runtime the gap is projection plus render. One face-up heroes captured from the HQ villainAttachedHeroes already project as UICityCard attachedHeroes WP-214 and survive the audience filter but no client renders them and the client has no ext_id to image resolver so this WP adds UICityCard attachedHeroDisplay UICardDisplay array index-aligned hero display payloads and CityRow.vue renders them as face-up card art. Two face-down bystanders captured by city villains attachedBystanders project nowhere deliberately excluded D-12805 Interpretation B and D-12806 safe-skip so this WP adds count-only UICityCard attachedBystanderCount number face-down equals identity hidden per D-24311 reverses the safe-skip for the count only never onto the mastermind tile and renders an N captured badge. Three face-down bystanders captured by the mastermind mastermind attachedBystanders D-15401 already project so MastermindTile.vue swaps its text list for the same count badge no engine change. Both new UICityCard fields added via the 5-step Board-Visible Field contract both required so the enumerated UICityCard-literal sites are backfilled CityRow.test useCityRow.test mutationDetector.test plus the three uiState JSON fixtures bound satisfies UIState and the stale uiState.types.drift.test Object.keys assertion is updated to the full 8-key set repairing the WP-214 omission. Enforcement arena-client vue-tsc is the only compile-time enforcer the drift Object.keys assertion is the engine guard. Determinism projection is hash-excluded no re-pin. Corrects the stale mastermind ships empty no source today comments false since D-15401 while preserving the never flatten onto the mastermind prohibition. Completes the arena-client render WP-214 explicitly deferred. Section 17 product legibility No conflict determinism untouched NG-1 to 7 not crossed. Section 20 N A. Section 21 N A. User-visible surface play.legendary-arena.com captured hero art plus count badges under adversaries D-24026 live-verify operator-pending. EC-540 standard two-session lane drafted 2026-08-06 not yet executed. Reserves D-24311. Hard-deps WP-214 EC-246 plus WP-154 D-15401 plus WP-128 D-12805 D-12806)"]
        ["WP-498 ✅ Return-on-discard reactive hero ability Cyclops Unending Energy (game-engine plus arena-client plus card-data; implements the unimplemented Cyclops Unending Energy core/cyclops/unending-energy count 3 reactive ability If a card effect makes you discard this card you may return this card to your hand surfaced by the operator 2026-08-04 the Determination and Optic Blast discard-to-play cost force-discarded it with no return option. FULL-FAITHFUL operator-chosen ALL card-effect hand to discard sources trigger the return not just the discard-to-play cost. Adds the engine first reactive hero timing onDiscard HERO_ABILITY_TIMINGS 5 to 6 plus new HeroKeyword return-on-discard HERO_KEYWORDS 33 to 34 bare marker plus a new forced-discard chokepoint discardFromHand plus checkReturnOnDiscard no shared hand to discard mover exists today through which ALL 5 card-effect discard sites route resolveDiscardToPlay discardChoice.resolve mastermindHandlers.discardCardFromHand schemeTwistResolvers.discardHand dodgeCard guarded by a cheat-proof drift test asserting no hand to discard mutation outside the chokepoint. The reaction parks an OPTIONAL PendingReturnOnDiscard decline or cardId per the resolveOptionalPutBottomHQ template plus resolveReturnOnDiscard move client false plus block-all guards plus ai.legalMoves bot default accept strictly beneficial plus chooser-only UIPendingReturnOnDiscard plus arena-client ReturnOnDiscardPrompt.vue. Determinism pendingReturnOnDiscard lazy-init never seeded so NO PRE_WP080 or hashGameState re-pin fixture finalStateHash re-pins ONLY if a committed fixture plays and returns Unending Energy none expected. Cross-layer one WP WP-383 mirror-precedent renumbered from WP-497 after a same-number race with 1215 and 1217. Section 17 section 1 2 10 No conflict NG clear determinism line. Section 20 N A. Section 21 N A. User-visible surface play.legendary-arena.com D-24026 live-verify operator-pending. EC-533 standard two-session lane drafted 2026-08-04 not yet executed. Reserves D-24301. Hard-deps WP-383 D-24184 plus D-24139 plus WP-248 D-24019 plus WP-476 D-24284 plus D-24266)"]
        ["WP-395 ✅ Canonical villain/henchmen loadouts for gauntlet legs (registry + server; ScenarioKey is scheme::mastermind::sorted-villain-groups and villain groups are unconstrained across all 41 sets (134 of them, D-24131 'any villain groups qualify'), while PAR is calibrated per scenario key with a validator rejecting sampleSize < 500 — so across 639 scheme×mastermind leg pairs the space is 85,626 scenarios at 1p and 8,205,239,889 at 5p, about 4.1 trillion simulated games. An enumerated menu of three collapses it to 6,354 scenarios / ~3.18M games (the drafted 639/1,917 figures assumed one villain set across all player counts, which PLAYER_COUNT_SETUP forbids). Since submission fail-closes on par_not_published, free villain choice does not make the ranked surface expensive — it makes PAR unreachable. Ranked gauntlet qualification ONLY; casual play keeps free selection, and hero choice stays free since heroes are not in ScenarioKey. Migration cost is currently zero — competitive_scores is empty — and 103/111 masterminds already declare alwaysLeads. Sequence ahead of any PAR calibration work. D-24199 Active; 110 masterminds x 3 = 330 generated loadouts; migration 035 adds competitive_scores.henchman_key since henchmen are absent from ScenarioKey; board + challenge link publish the requirement; EC-435; drafted 2026-07-18, done 2026-07-20)"]
        ["WP-396 📝 bystanderLost has no producer (engine scoring; the penalty the scoring model ranks highest is hardcoded to 0 in deriveScoringInputs, so it can never fire. validateScoringConfig hard-fails any config where bystanderLost does not outweigh both villainEscaped and bystanderReward — the VISION §21 moral hierarchy is encoded in the config contract and then multiplied by a constant zero. Two defects: nothing counts bystander loss (no BYSTANDERS_LOST counter, though villainDeck.reveal.ts already measures the pile delta across the escape branch), and nothing consumes it. Design question open; no EC yet. Renumbered from WP-391 by EC-429 follow-up — that slot is Support Card Pools, merged, referenced by six ECs and two DECISIONS entries; drafted 2026-07-19)"]
        ["WP-400 ✅ Pin the Node toolchain to one committed version (infrastructure; nothing pins a Node minor, so every environment resolves \"22\" independently — two Pages logs three days apart report 22.22.0 and 22.16.0 from the same repo. Adds .node-version as the single source; 21 workflow entries read it instead of restating it)"]
        ["WP-401 ✅ Execute the Node 24 upgrade (infrastructure; Node 22 is in LTS Maintenance since 2025-10-21 with EOL 2027-04-30, Node 24 Krypton is Active LTS to 2026-10-20 and supported to 2028-04-30. WP-400 pinned the toolchain first, so the edit is .node-version plus two render.yaml envVars and zero workflow changes — the packet is the determinism evidence, not the edit)"]
        ["WP-402 ✅ LAGN 1.3.0 hero alternates (contract; adds optional setup.hero_alternates so a saved/shared loadout carries bench heroes — the operator's seven-hero shortlist, five played + two reserve. NOT extra entries in setup.heroes: hero count is exact-enforced against PLAYER_COUNT_SETUP, 3 at 1p / 5 at 2-4p / 6 at 5p, and the viewer maps setup.heroes straight into heroDeckIds, so seven entries produce an invalid draft AND a thrown Game.setup. Copies the D-24195 support_pools shape verbatim: optional, version-gated, strict superset. The version gate is load-bearing — lagnSchema is not .strict(), so an ungated bench is silently STRIPPED on parse, the worst failure mode. Fixes a latent contract defect the draft scaffold caught: the provenance gate is written === LAGN_VERSION_1_2_0, so the moment 1.3.0 joins the supported list a 1.3.0 doc carrying catalog_ref is rejected by a message reading 1.2.0 or later — measured, not reasoned; converted to an ordinal comparison, D-24211 locks version gates ordinal. LAGN_VERSION stays 1.1.0, readers accept 1.3.0, writers do not emit it here. No .max() on the bench — a cap in a published standard needs a major version; two slots are UI-only. Empirical scaffold RUN: 54→52/2, both mechanical, zero fixture breakage. First of a three-packet arc, one per layer. Reserves D-24210 + D-24211; EC-437; drafted 2026-07-20)"]
        ["WP-403 📝 MATCH-SETUP envelope hero alternates (registry; BLOCKED on WP-402. Adds optional heroAlternateIds to the envelope beside heroSelectionMode/supportPools, NOT CompositionSchema — the 9-field composition lock stays. A hard prerequisite of WP-404, and the finding that split the arc into three: every setupContract object is .strict(), so an unknown envelope key is rejected at parse, and the viewer draft is a MatchSetupDocument that cannot carry a bench until the envelope does. Its own packet because contract files are locked by code-style.md, requiring review + a D-entry, and folding it into WP-404 would cross a layer boundary inside one WP. Validation is thin — shape + identity via the existing checkArrayExtIds, no parallel resolver, plus disjointness from heroDeckIds; no count rule, since PLAYER_COUNT_SETUP governs played heroes and inventing a reserve-count rule is a prohibited AI failure pattern. AC-8 pins zero engine files changed and finalStateHash unmoved. Scaffold RUN: registry 178/178, zero fixture breakage. Reserves D-24212; EC-438; drafted 2026-07-20)"]
        ["WP-404 📝 Hero alternates in the Loadout tab + LAGN writer flip (registry-viewer; BLOCKED on WP-402 + WP-403. Two bench slots carried through the draft, the LAGN export, the ?lagn= share link, and the saved-loadout library — plus the LAGN_VERSION flip 1.1.0→1.3.0, skipping 1.2.0 for free, that makes the whole arc observable. Until the writer flips the bench cannot survive a round trip: the WP-402 version gate rejects a bench on a pre-1.3.0 document. The flip belongs here by rule — validator.ts's frozen-writer comment prescribes flipping together with the producers. Scaffold-measured blast radius, not grep-inferred: the flip breaks 5 tests all in lagn-spec/validator.test.ts and ZERO viewer tests — the two viewer 1.1.0 literals are input fixtures on an accepted read version, so the flip does not touch them; migrate.ts needs no edit. Trap: game-engine/versioning/** shares the string 1.1.0 for the engine save-version namespace and must not be touched. Reuses the single importer/exporter, no fork; empty bench omits the key rather than emitting []; import atomic. AC-5 is an explicit export→import round-trip test, the WP-291/EC-429 asymmetry. typecheck is the load-bearing gate for SFCs. §21 TRIGGERED, two rows move, replaced WHOLE per D-11804; no stored record migrates. User-visible on cards.legendary-arena.com — D-24026 required, AC-11 drives the terminal action, save then re-open, not just a rendering slot. Reserves D-24213; EC-439; drafted 2026-07-20)"]
        ["WP-405 ✅ LAGN 1.4.0 match participants + scoring profile (contract, reader-only; adds optional top-level players[] {seat, player_id, display_name?} and scoring_profile (a plain string label) so a server-emitted result LAGN describes who played and under what profile. Operator-directed with the consumer story on the table: competitive credit/scoring takes matchId only and re-executes the bgio blob server-side (D-5301 / D-24126), so both blocks are DESCRIPTIVE and NON-authoritative — never a credit, scoring, ranking, or verification input. D-24214 locks that boundary for players[] plus the privacy rule (player_id is a PUBLIC id, never AccountId); D-24215 locks it for scoring_profile (a label not authority; a plain string not an enum, since the profile set is the leaderboard's to own). Version-gated like every prior minor — lagnSchema is not .strict(), so an ungated block is silently STRIPPED on a pre-1.4.0 doc; the gate makes that a loud rejection. Reuses WP-402's isLagnVersionAtLeast ordinal helper. LAGN_VERSION is NOT flipped — reader-only like WP-394 for 1.2.0; the writer flip is a future server-producer packet. Independent of WP-404. Empirical scaffold RUN: 65→64/1, mechanical, zero fixture breakage, count gate green +2/+2. §21 TRIGGERED, POST /api/me/loadouts replaced WHOLE per D-11804. Reserves D-24214 + D-24215; EC-440; drafted 2026-07-21)"]
        ["WP-406 ✅ Result-LAGN producer + writer flip to 1.4.0 (server; BLOCKED on WP-405. The producer that gives WP-405's reader contract a concrete consumer: new GET /api/match/:matchId/result-lagn emitting a LAGN 1.4.0 result document (setup + players[]? + result + scoring_profile) for a completed match, projected read-only from the bgio blob + the legendary.match_seat_accounts roster, plus the LAGN_VERSION flip to 1.4.0. Reuses WP-361's buildMatchLagn mapping + blob-read helper, no fork. The one real design fork flagged for operator review — player_id source: no guaranteed public id exists per account (the WP-101 handle is nullable-until-claimed; public surfaces use mutable display_name). D-24216 locks the recommendation: player_id = display_handle, handleless seats OMITTED (never AccountId, never synthesized), empty roster omits players[]. Reads all fit existing carve-outs (D-24119 re-reduce / D-24153 composition / D-24169 gameover) + a domain-table roster read — no new carve-out. Descriptive-only: nothing scores/credits/ranks from it (D-24214/D-24215; wiring it in reopens D-5301). Guest-readable for completed matches (404 until gameover). §21 TRIGGERED, new row + setup emitter's stamped version moves, WHOLE per D-11804. Reserves D-24216; EC-441; drafted 2026-07-21)"]
        ["WP-407 ✅ Hall of Legends per-match result view (legends-board; BLOCKED on WP-406. The Hall of Legends' first per-match view — a MatchResultPanel showing a completed match's outcome + participant roster read from the WP-406 result LAGN. No per-match view exists today; legends-board is all aggregate panels and /api/leaderboards/scores/:replayHash is a single score by hash, not a roster — so this is net-new. D-24217 locks it as a pure result-LAGN consumer: renders only the public labels WP-406 emitted, never joins an identity table directly, omitted seats render anonymous — so the public-identity boundary lives in exactly one place (the producer). Live fetch keeps the snapshot publisher's aggregate scope unchanged. typecheck is the load-bearing SFC gate. User-visible on legends.legendary-arena.com — D-24026 required, AC-6 drives the terminal action. Reserves D-24217; EC-442; drafted 2026-07-21)"]
        ["WP-408 ✅ Portable match-LAGN download (legends-board; BLOCKED on WP-406 + WP-407. Adds a download this match as LAGN control to the WP-407 result view: one click fetches the WP-406 result LAGN and saves match-<id>.lagn.json as a client Blob, no new server surface. The self-describing-artifact half of the arc — a completed match becomes a standards-conformant record the Registry Viewer ?lagn= flow, the lagn CLI, or any third-party tool can consume. D-24218 locks the export as ONE-WAY: never re-ingested to score/credit/rank — competitive outcomes stay matchId -> blob -> re-execute -> AccountId (D-5301), so editing the file changes nothing. Failed fetch shows a visible full-sentence error, never a silent no-op. Its own packet, not folded into WP-407, because the download is a user action with its own export-only consideration; display-then-action sequencing mirrors WP-402->WP-404. §21 N/A. User-visible on legends.legendary-arena.com — D-24026 required, AC-6 downloads + re-opens the file. Reserves D-24218; EC-443; drafted 2026-07-21)"]
        ["WP-409 ✅ Hero-play synergy-effect count -> UIState (engine signal for the tiered combo cue; the ewiki Sound Effects page specs an escalating combo/synergy sting but nothing the client observes carries a how-much-did-this-play-do count — the villain side exposes FightResolvedEvent.appliedEffects, the hero side has no analog. Engine reality verified this session: a hero play does NOT re-fire other cards' hooks (no cascade); synergy is conditional gating ([hc:X]/requiresTeam/requiresKeyword), so the honest countable signal is the number of hero effects that FIRED for a play. Widens executeHeroEffects void->number (mirrors the villain VillainEffectResult[] accumulator), applyCardPlay stores it on a transient G.lastPlayEffectsFired covering playCard + playFromUndercover, the play-phase onBegin resets it, and UIState.game.lastPlayEffectsFired projects it publicly. Observability-only — nothing reads it for rules — so excluded from the finalStateHash oracle (the D-24081 messages mechanism), leaving every recorded sentinel hash byte-unchanged (no re-pin); the dual-oracle computeStateHash/PRE_WP080_HASH gate handled with a matched exclusion or a reasoned re-pin. No client audio (deferred to the future audio-layer WP; operator chose engine-signal-only), no new NotableGameEvent variant. User-visible surface = none, infrastructure only. Reserves D-24221; EC-444; drafted 2026-07-21)"]
        ["WP-410 ✅ Card-image working-set prefetch at match start (game-engine + arena-client; implements the ewiki LAGN discussion's Option C — warm every card image a match can show during the setup screen so a card paints instantly on reveal instead of blocking the turn on a round-trip to images.legendary-arena.com. Cross-layer by necessity, verified this draft: the arena-client is forbidden a runtime @legendary-arena/registry import and the live UIState projects only currently-visible cards, so the client cannot derive the full image set — but the engine already computes every match card's imageUrl in G.cardDisplayData at setup. So the engine projects the deduped set as an optional top-level UIState.matchCardImageUrls (always populated by uiState.build.ts, [] for empty; optional in the type so no hand-written UIState fixture needs a backfill — the WP-179 pattern; an additive Pick<UIState,'matchCardImageUrls'> satisfies drift pin is added, as no top-level keyset gate exists), and — the ship-blocker the copilot gate caught at draft — passed through filterUIStateForAudience (the sole engine->client boundary, which rebuilds UIState from a whitelist so a field it does not copy is silently dropped) as public for every audience. A new useCardImagePrefetch composable, mounted once at the D-16501 PlayViewport root, warms the list with bounded concurrency (PREFETCH_CONCURRENCY = 6), fail-soft (a rejected warm is skipped, covered by the CardTile.vue lazy img fallback), and idempotent (a Set so reconnect/re-render never refetches). Projection-only — nothing writes G — so no state-hash surface, every sentinel finalStateHash/PRE_WP080_HASH byte-unchanged, no re-pin. Information-safe: the SET of card designs is public from the composition, the manifest carries no face-down order. No LAGN change (embed/zip rejected; prefetch is a client delivery concern), no service worker / Cache Storage (v1 warms the HTTP cache; the durable SW is a named future WP), no first-visible priority ordering (future), no R2 Cache-Control change (the recommended companion ops change, out of tree). §21 N/A — the manifest rides the existing UIState transport, no endpoint. User-visible on play.legendary-arena.com — D-24026 required, AC-10 drives an actual reveal (warm-at-setup + paint-from-cache-on-reveal). Reserves D-24222; EC-445; drafted 2026-07-21)"]
        ["WP-411 ✅ Set ctx.gameover at end of game (top-level endIf; CRITICAL latent bug found live-verifying WP-407/408. A clean prod win (J8rw6ziU4Xg, Magneto's four tactics defeated) returned 404 match_not_finished; a DB read showed both metadata.gameover AND state.ctx.gameover null after a decisive victory. Root cause: the endgame check is a PHASE-level endIf on the play phase, which in boardgame.io only ends the phase (-> the empty end phase) and does NOT set ctx.gameover. LegendaryGame has no top-level endIf and no events.endGame() anywhere; the phase-endIf comment even asserts the wrong belief that a truthy phase-endIf return becomes ctx.gameover (true only for a top-level endIf). So NO match ever registers gameover, and every completed-match consumer is dead in prod: competitive submission (WP-338 -> competitive_scores empty), result-LAGN (WP-406), Hall of Legends (WP-407/408), match summaries (D-24169); every finished-match query returns 0 rows. No test caught it because the suite asserts the pure evaluateEndgame(G) function, never the boardgame.io wiring that sets ctx.gameover. Fix: add top-level endIf ({G}) => evaluateEndgame(G) ?? undefined; remove the redundant phase endIf; keep the four phases (lobby/setup/play/end, pinned by game.test.ts). Tests close the gap: a LegendaryGame.endIf unit assertion + an engine-runner integration test that plays to a real gameover. Determinism scaffold-gated: no G mutation so finalStateHash unchanged for an identical G; games now terminate at the win/loss point, so any fixture recorded past it is re-recorded via the canonical recorder + reasoned re-pin, never silent. No server edit (server already reads metadata.gameover). User-visible surface = none directly (engine wiring) but unblocks the completed-match pipeline; D-24026 live-verify REQUIRED, also unblocks WP-407/408's populated-roster verify. Reserves D-24223; EC-446; drafted 2026-07-21. DONE 2026-07-21: top-level endIf added, phase endIf removed; AC-3 wiring test (framework sets ctx.gameover via boardgame.io reducer InitializeGame+CreateGameReducer) realized in game.test.ts NOT engine-runner — runSimulation re-implements the turn loop + calls evaluateEndgame directly, never instantiates boardgame.io, so it has no ctx.gameover to assert (EC-446 Execution Amendment); runMatch.test.ts got an honest end-to-end termination smoke instead; engine-only, no server edit. build 0; game-engine 2053->2055/0 +AC-1+AC-3; engine-runner 19->20/0; whole-repo --no-bail clean; finalStateHash + PRE_WP080_HASH UNCHANGED (no G mutation, AC-4 re-record a verified no-op); D-24223 Active; AC-6 D-24026 live-verify operator-pending on the engine deploy)"]
        ["WP-412 ✅ Arena-client audio layer foundation (the first sound in the game; the foundational arena-client audio layer the ewiki Sound Effects design calls for. Ships Surface 1: one CC0 clip per notable game event (the six NotableGameEventType variants) via a howler.js SFX engine + autoplay-unlock gesture + persistent mute/master-volume (localStorage). A useSoundEffects consumer keeps its OWN append-only cursor over UIState.notableEvents — catch up on first frame, no history replay, one clip per newly-appended event, no overlay-style 2.5s throttle — distinct from WP-201's one-at-a-time visual queue. Pure client presentation: reads UIState only, never writes G/ctx, zero engine/determinism/replay footprint. New dep howler + @types/howler, arena-client only. Audio bytes hosted on R2 (audio/sound-effects/), never in git; tests use a mocked Howl. Deferred to follow-on WPs: the adaptive danger-meter music (separate channel), the tiered combo cue (consumes WP-409's lastPlayEffectsFired; hard-deps WP-412 + WP-409), and Surface-2/3 cues. User-visible on play.legendary-arena.com, D-24026 required. Reserves D-24224; EC-447; drafted 2026-07-21)"]
        ["WP-413 ✅ Arena-client tiered combo cue (the escalating hero-play synergy sting the ewiki Sound Effects design calls for. Consumes WP-409's UIState.game.lastPlayEffectsFired scalar and plays through the WP-412 audio engine getAudioEngine() — no new dependency, engine, control, or channel. A pure comboTierForCount maps the count to a tier: <= 0 none silent, 1 small, 2 medium, >= 3 big; three CC0 clips combo-small/medium/big.mp3 hosted on R2 audio/sound-effects/, never in git. A useComboCue consumer keeps its OWN last-seen scalar and fires once per audible value-change — catch up on first frame, no pre-mount cue; NOT an append-only cursor, the signal is a scalar. Accepted v1 limitation: two consecutive same-turn plays with the same non-zero count coalesce to one cue; the per-turn reset re-arms equal values across turns; a per-play edge signal is a deferred follow-on. Pure client presentation: reads UIState only, never writes G/ctx, zero engine/determinism/replay footprint. Wired once at the WP-412 01.5 host PlayViewport.vue beside useSoundEffects. Deferred to follow-on WPs: music separate channel, Surface-2/3 cues, the per-play edge de-coalescing. User-visible on play.legendary-arena.com, D-24026 required. Reserves D-24228; EC-448; drafted 2026-07-21)"]
        ["WP-421 ✅ Arena-client Surface-2 player-action move SFX (the tactile action-move cue the ewiki Sound Effects Surface-2 design calls for; the follow-on WP-412's Out of Scope named. The client dispatches these moves, so it plays a sound on the LOCAL action for immediate feedback, independent of the authoritative result — fired from the single App.vue submitMove closure (prop-drilled as SubmitMove), before relaying intent. Dispatch-keyed, deliberately distinct from Surface 1 useSoundEffects and the combo cue useComboCue, both PlayViewport snapshot-watch hosts: a move cue must fire on the local action because recruitHero emits NO notable event and the felt cues must not wait on a projected frame. Coverage: five dispatchable moves via a PARTIAL moveSfxManifest over UiMoveName — playCard play-card.mp3, recruitHero recruit-hero.mp3, fightVillain attack-villain.mp3, drawCards draw-cards.mp3, endTurn end-turn.mp3 (hyphenated R2 audio/sound-effects/ paths; lobby/stage/resolve moves silent, an unmapped name is a no-op). dodgeCard gap: the ewiki table's sixth row cannot fire — dodgeCard is an engine-only move (packages/game-engine/src/moves/dodgeCard.ts) with no UiMoveName dispatch path; it stays unmapped, test-pinned, a documented gap for a later UI-affordance WP. useMoveSounds(getAudioEngine()) returns playMoveSound(name) and reuses the WP-412 engine wholesale — no new dependency, engine, control, or channel; the autoplay-unlock and master mute/volume gates plus the EC-448 lazy-load-any-URL amendment apply unchanged. Pure client presentation: reads NO UIState, never writes G/ctx, zero engine/determinism/replay footprint; no runtime registry/server import. Deferred to follow-on WPs: a dodge UI affordance, Surface 3 turn-lifecycle cues, background music. User-visible on play.legendary-arena.com, D-24026 required (the five CC0 move clips are already live on R2 GET-200/audio-mpeg; operator-pending only on the deploy eyeball). Reserves D-24241; EC-456; Lightweight Lane drafted+executed 2026-07-24)"]
        ["WP-425 ✅ Apex LEGENDARY! combo tier (adds a fourth boundary to the shipped WP-413 comboTierForCount — an apex legendary tier at count >= 5, and narrows big to 3-4 — so a very large hero-ability chain plays a distinct, rarer LEGENDARY! combo sting on play.legendary-arena.com above big. The minimal extension of the D-24228 surface: ComboTier gains legendary, comboTierForCount maps >= 5 to legendary, and comboCueManifest maps it to a CC0 combo-legendary.mp3 on R2 audio/sound-effects/, never in git. The useComboCue consumer is tier-agnostic and does NOT change; audioEngine.ts EC-448 lazy-load-any-URL does NOT change. The Record<Exclude<ComboTier,none>,string> type plus the AUDIBLE_TIERS drift test enforce the union-map lockstep. Shared tier not audio-only per D-24246: the >= 5 boundary is the fourth Combo Tier Contract tier, locked for BOTH the audio sting this WP and the future visual combo-flash / synergy call-out LEGENDARY! — the VFX layer is NOT built in code yet, so this WP ships audio only and the contract locks the boundary so the future visual consumer inherits it; resolves the ewiki Combo scaling beyond T3 open question. The ewiki Combo Tier Contract wiki/visual-effects.md and narrative call-out ladder wiki/narrative-psychology.md update in lockstep, the may-not-diverge rule, so the documented contract matches the code. Pure client presentation: reads UIState only, never writes G/ctx, zero engine/determinism/replay footprint; no finalStateHash re-pin; no new dependency, engine, control, channel, or UIState field. User-visible on play.legendary-arena.com, D-24026 required — needs combo-legendary.mp3 on R2. Reserves D-24246; EC-460; standard two-session lane, drafted 2026-07-25)"]
        ["WP-422 📝 Seed PAR publication — turn on competitive submissions (registry + tooling + data; fixes the whole competitive surface being dark. Investigation 2026-07-25 found the PAR index was NEVER generated or delivered: data/par/{sim,seed}/v1/index.json was never committed, no build or CI step produces it, and no runnable generator exists, so the server gate parGate.mjs fail-closes, every submission returns par_not_published, competitive_scores stays empty, and the Legends board plus all 110 gauntlet boards cannot fill regardless of play volume — the documented sole remaining blocker on the whole competitive surface. Ships the content-driven Seed PAR, VISION section 26 and docs 12 Phase 1, the deliberate ~60-70 percent day-one tier; simulation Phase 2 is a later WP. A) add difficultyRating 1-10 to the mastermind/scheme/villain-group schema, reversing D-5508's deliberate deferral that PAR does not exist yet, and author a documented reviewed first-pass rating for every competitive item, the values being a product-judgment deliverable Jeff reviews. B) documented global default ScenarioScoringConfig weights, caps, and penalties satisfying validateScoringConfig, plus a difficulty-to-ParBaseline mapping where parValue equals computeParScore. C) a deterministic authoring-time script scripts/generate-seed-par.mjs that writes write-once data/scoring-configs and data/par/seed/v1, committed to the repo — the loadParIndex and loadRegistry local-fs delivery model, no new infra. No runtime engine or server change; the gate and leaderboards already consume the index. Section 21 N/A. Split-vs-single flagged for pre-flight. Reserves D-24242; EC-457; drafted 2026-07-24, not yet executed)"]
        ["WP-414 ✅ Bot-ally stall surfacing + restart revival (server; fixes a reported prod freeze on play.legendary-arena.com. A bot-ally match froze on the bot's turn while the operator ran concurrent sessions that merge to main, which triggers a Render redeploy that destroys the in-process BotAllyDriver mid-match. A live driver can never freeze forever (it faults within BOT_MAX_MOVE_STEPS_PER_TURN), so a lasting freeze means the driver is gone (restart) or was persisted faulted and never revived — and rehydrateBotAllyDrivers today revives only status=active rows, while the fault_message it writes is exposed by no endpoint, so the human on seat 0 waits forever with no signal. Fix, server half: a read-only GET /api/match/:matchId/bot-ally-status (guest by capability, side-table-only per D-24095, absent for a non-bot match) returning driving/status/message; the driver retries a wedged turn once from a fresh fetch before faulting; restart revival re-attaches a still-live faulted/exhausted match bounded by a new revive_count column (MAX_REVIVALS=3) so a genuinely-stuck match settles to a surfaced faulted instead of looping every deploy. Migration 036 adds revive_count, additive and idempotent; no server.mjs edit (route registers inside registerBotAllyRoutes, revival is already wired). New guest GET so api-endpoints.md gains a row. User-visible on play.legendary-arena.com, D-24026 required. Pairs with WP-415 (the client banner). Reserves D-24229 and D-24230; EC-449; drafted 2026-07-22)"]
        ["WP-415 ✅ Bot-ally stall banner (client; The client half of the bot-ally freeze fix: a useBotAllyStatus composable consumes WP-414's GET /api/match/:matchId/bot-ally-status on the play surface — probe once on mount, poll only a real bot-ally match (absent stops it), stop on a terminal status and on unmount, fail-soft on a fetch error. When the bot ally stopped abnormally (driving false, status not completed or absent) a co-op-framed BotAllyStallBanner renders the server's public-safe message verbatim plus a client-only Return-to-lobby escape (no new or destructive endpoint). A healthy match and a normally-completed match show nothing; a network blip is not a stopped bot. Mounted once at the D-16501 PlayViewport play-root, the WP-410/412 01.5 wiring precedent; no runtime registry/server import; section 23(b) co-op framing on every string. User-visible on play.legendary-arena.com, D-24026 required. Reserves D-24231; EC-450; drafted 2026-07-22)"]
        ["WP-418 ✅ New-version refresh prompt + reconnect-gap audit (client; kills the deploy-mid-match freeze. When a deploy lands mid-match, Cloudflare Pages swaps the hashed asset chunks the open tab references, so a lazy-loaded chunk 404s and the board freezes or paints blank tiles — the classic stale-SPA-after-deploy, not engine logic. The reconnect stack (D-24232, WP-311, WP-312, D-24234) re-anchors match state but cannot fix a stale JS bundle; only a page reload does. Fix, client only: A) the build emits a tiny version.json ({ gitSha }) via a Vite plugin reusing the existing gitSha capture — no second git call, no npm dep, and configureServer serves it in dev — polled cache:no-store and compared against the baked __GIT_SHA__ by the pure isNewerBuildAvailable; B) a window vite:preloadError catch flips the flag immediately and preventDefaults the white-screen. Triggers on mount, tab-focus, socket reconnect (a WP-311 connection store isConnected false-to-true edge, reused rather than a second socket listener), and a ~60s backstop. UpdateAvailableBanner.vue — read-only, role=status, glyph plus text, keyboard-reachable — renders a user-initiated Refresh now (location.reload, prop-drilled from PlayViewport) plus a dismiss; never an auto or forced reload (that would discard an in-progress action; an auto-reload-when-safe path is a deferred sub-fork). Fail-soft: a failed or missing version.json shows nothing. Reconnect-gap audit: D-24232 still covers the pure transport-drop, a stale bundle is out of resync's reach by design, and the resync stack is not refactored. Pure arena-client plus one build-config emit; no engine/server/registry change; section 21 N/A. Reserves D-24238; EC-453; drafted+executed 2026-07-24)"]
        ["WP-419 ✅ Bot-ally status liveness + strand-to-faulted (server; fixes a silent bot-ally freeze found in a live diagnostic, match vMxtCoOZDFj, frozen on the bot's turn 1 while GET /api/match/:matchId/bot-ally-status reported driving:true and status:active. The bot driver's fault machinery is bounded, so a match stuck active for 30+ minutes means no driver is actually running: the in-process BotAllyDriver was destroyed by a redeploy, plausibly WP-418's own Render redeploy, and active is merely its creation status, never overwritten. Two defects turned that driver-loss into a silent permanent freeze. One, the status endpoint derived driving from the DB active flag with no liveness check, so a dead-but-active match reported healthy and the WP-415 banner, which needs driving:false, never showed. Two, a match that burns all MAX_REVIVALS revivals without completing a turn is excluded from revival yet left active, un-revived and un-surfaced. Fix, server only: A) driving equals row.status active AND botAllyDrivers.has(matchId), where botAllyDrivers is the process live-driver registry, so a driverless active match reports driving:false and the banner surfaces; B) rehydrateBotAllyDrivers now calls settleStrandedActiveMatches, which reads status active AND revive_count at or above MAX_REVIVALS and settles each driverless row to completed or faulted with the public-safe BOT_FAULTED_MESSAGE, skipping a row this boot just revived because it has a live driver. Untouched: the revival-cap read and increment logic that the 2026-07-23 OOM-loop hotfix owns; exhausted is not stranded; recovering a genuinely-drivable capped match and a multi-instance DB heartbeat are named follow-ups, single-instance assumed. No determinism, persistence, response-shape, or auth change; section 21 N/A. Reserves D-24239; EC-454; drafted+executed 2026-07-24)"]
        ["WP-420 ✅ Deploy-aware bot-ally revival (server; the recovery follow-up WP-419 named. WP-419 surfaces a driverless bot-ally match and faults a cap-stranded one; this recovers a match that was healthy and being driven when a clean deploy destroyed its in-process driver — the observed vMxtCoOZDFj case, a match that would play fine if the driver returned but burns all MAX_REVIVALS revivals across restarts without completing a turn and then faults. Design: distinguish lost-to-a-clean-deploy, recover, from crashed or wedged, do not loop. A graceful SIGTERM shutdown in index.mjs, which is what Render sends on a redeploy, marks the matches currently in botAllyDrivers via a new additive shutdown_interrupted column, migration 037, before the pool closes, best-effort. Boot revival includes a shutdown_interrupted row regardless of the cap, revive_count < MAX_REVIVALS OR shutdown_interrupted, and clears the flag on re-registration, so a clean shutdown earns exactly one past-cap revival; a recovered drivable match then completes a turn and D-24233 resets revive_count, while a match that still cannot complete a turn and is not cleanly interrupted again strands to faulted per WP-419. An ungraceful loss, OOM or crash with no SIGTERM and so no flag, is never free-revived: it keeps WP-414's cap and WP-419's strand-to-faulted, so the 2026-07-23 OOM restart loop cannot return because its terminator is a crash with no clean shutdown, exactly the capped case. Rejected: a partial-progress reset, which would reopen the loop. Server orchestration plus an additive side-table column, D-24095 store-only; no engine or registry change; section 21 N/A. Reserves D-24240; EC-455; drafted+executed 2026-07-24. Executed: migration 037 shutdown_interrupted; index.mjs SIGTERM marks the botAllyDrivers matches before pool close, best-effort; readRevivableBotAllyMatches OR-extended with shutdown_interrupted; markBotAllyMatchRevived clears the flag on revival)"]
        ["WP-424 ✅ Stop bot-ally drivers on SIGTERM, deploy-overlap freeze (server; fixes a silent bot-ally freeze that recurs on essentially every deploy catching a bot mid-turn — live diag DBlXvBs_WXA, frozen on the bot's turn 15 right after the bot fought a villain, the turn never ended, while the status reported driving:true and status:active. The deploy log named it: after the new instance re-registered the driver, boardgame.io's Master repeatedly rejected the bot's move, invalid stateID was 273 expected 274 playerID 1 action fightVillain, again 288 and 289 fifty-eight seconds later — an off-by-one _stateID race, two writers driving the same bot seat. Root cause: the SIGTERM handler in index.mjs marks the driven matches shutdown_interrupted per WP-420 but never stops their drivers; Render's rolling deploy boots the NEW instance, which revives and drives those matches, BEFORE it SIGTERMs the OLD one, whose handler then blocks in httpServer.close draining the human's long-lived Socket.IO connection — so the old instance's drivers keep polling every 250ms and submitting the bot's moves for the entire termination-grace window until SIGKILL. Two instances driving the same seat race on _stateID; the slow chain-triggering fightVillain loses repeatedly, never lands, the turn wedges, and because a driver is still registered driving:true, WP-419's stall banner which needs driving:false never fires — a silent frozen board. Fix, server only: A) new stopAllBotAllyDrivers() snapshots botAllyDrivers.values() and calls each driver.stop(), clearing the timer, setting stopped, de-registering; B) call it in the SIGTERM handler AFTER markInProgressBotAllyMatchesInterrupted reads the driven-match keys, because stopping first would empty the map; C) an in-flight tick bails on driver.stopped, attemptBotTurn returns at the loop top and runTick skips the post-turn side-table write. The old instance ends its participation at SIGTERM; WP-420's mark still lets the new instance revive the match as the sole writer, so fightVillain lands and the turn completes. Untouched: WP-420 mark-and-revive, the status route and driving, the client banner, determinism and persistence. Deferred: a cross-instance ownership guard, a DB advisory lock or driver_owner plus heartbeat, the durable multi-instance fix that closes the residual boot-to-SIGTERM window; single-instance steady state assumed. No determinism, persistence, response-shape, or auth change; section 21 N/A. Renumbered from WP-423/EC-458/D-24243 after a concurrent session landed those numbers first, hugo-version-upgrade, PR 1000. Reserves D-24244; EC-459; Lightweight lane drafted+executed 2026-07-25. User-visible on play.legendary-arena.com, D-24026 required, operator-pending on the deploy eyeball)"]
        ["WP-426 ✅ Bot-ally driver survives a transient Postgres blip (server; another silent bot-ally freeze, this one on a build that already carries WP-424 — live diag nZn_U4QO-hr, frozen on the bot's turn while the status reported driving:true and status:active. Root cause from the server deploy log: Postgres briefly went unreachable after a deploy, bgio-store fetch for the match ECONNREFUSED on port 5432 across all 4 retries, still failing after 4 transient retries keeping the server up returning empty, immediately followed by bot-ally match failed to persist teardown status completed. The bgio store's resilientFetch returns EMPTY, not a throw, when it exhausts retries against an unreachable database, the number-930 keep-the-server-up behavior; but the driver's runTick treated an empty fetchState as match vanished from the store and tore down as completed — so a transient DB blip, a Render database restart or instability around a deploy, killed the driver mid-match, and the teardown's own status write then also failed against the same dead database, leaving the side-table row active with no live driver, a silent freeze the human sees as a frozen board, self-healing only on a later restart's revival and re-looping if the DB stays flaky. An empty fetch is AMBIGUOUS, it means either a reaped match or an unreachable DB, and the driver assumed the former on the FIRST empty. Fix, server only: a new emptyFetchPolls counter plus a tolerateEmptyFetch helper, increment on each empty, reset to 0 on any non-empty fetch, and only teardown completed once it reaches BOT_MAX_EMPTY_FETCH_POLLS, 90, about several minutes since each empty tick already carries the store's own 1.8 second retry. BOTH empty sites funnel through it, the top-of-tick state-is-null and the mid-turn driveBotTurn vanished result, split out from game-over. A DB outage now recovers with the driver still registered and resumes driving; only a match that stays empty past the whole window, genuinely reaped which is rare for a live driver whose gameover or idle-abandon teardown fires first, tears down. Untouched: WP-419 liveness driving, WP-420 mark-and-revive, WP-424 SIGTERM stop, and the store's empty-to-stay-up contract, making it throw was rejected as a wider blast radius; determinism and persistence. Flagged separately: the infra trigger, why Postgres goes ECONNREFUSED around deploys, a deploy-coupled DB restart or residual instability despite the basic-1gb move number-932, is an operational investigation not this code fix. No determinism, persistence, response-shape, or auth change; section 21 N/A. Reserves D-24247; EC-461; Lightweight lane drafted+executed 2026-07-25. User-visible on play.legendary-arena.com, D-24026 required, operator-pending on the deploy eyeball)"]
        ["WP-427 ✅ Bot resolves the put-bottom-HQ pending choices, getLegalMoves short-circuit (game engine + server; fixes a genuine bot-ally fault, live diag 660LwoUY-Yq showed the WP-415 The bot ally could not finish its turn banner, on a STABLE server and healthy DB with no deploy churn, ruling out every infra cause fixed earlier this session. Root cause: the engine has 8 block-all pending-choice types, each a hasPending guard that freezes every other move until resolved, but getLegalMoves in simulation/ai.legalMoves.ts short-circuited to a resolve move for only 6, it had no case for hasPendingOptionalPutBottomHQ, Ionic Energy's optional put-a-card or Absorb Ambient Power's mandatory put-a-card, or hasPendingPutAnyNumberBottomHQ, the multi-select Wonder Man, Sunspot, Star-Lord. When one of those fired getLegalMoves fell through to normal-move enumeration, the block-all guard then rejected whatever the bot dispatched, the driver's fault fallback endTurn then advanceStage was equally blocked, and the turn FAULTED. This is the pending-choice-with-no-resolution-path hard-freeze class and a human on such a hero would be stuck too. The triggers are non-core heroes, so core-only matches never hit it, the recurring core-hero faults this session were the infra causes, DB provisioning, deploy-overlap D-24244, DB-blip D-24247, now fixed. Fix: A) two new getLegalMoves short-circuits mirroring the existing 6, the optional form declines with decline true unless front.mandatory in which case it moves the first present HQ card via selectFirstHqCard lowest slot index, the put-any-number form submits the empty put-none selection cardIds empty array, each a length-1 list. B) the bot driver's findPendingChoiceMove in autoplay/botLoopProgress.mjs synced from its drifted 2-name list to all 8 resolve-move names, defense-in-depth, the policy fallback already dispatched the six that had a short-circuit. Boundary: getLegalMoves is a PURE AI and simulation helper, not the engine reducer, no G mutation, no move-validation-contract change, no replay or finalStateHash determinism surface, full engine suite 2069/0 unchanged no re-pin; no registry or server runtime change, the server edit is the autoplay helper only; section 21 N/A; no PAR baseline impact, PAR unpublished and these choices absent from core-hero sim fixtures. Reserves D-24248; EC-462; Lightweight lane drafted+executed 2026-07-25. User-visible on play.legendary-arena.com, D-24026 required, operator-pending on the deploy eyeball)"]
        ["WP-431 ✅ Narrate the city-entry bystander attach and the captured-hero return-on-defeat into the game log (game engine; two LOGGING GAPS found while reviewing a full co-op Magneto game log 2026-07-25 — both mechanics are CORRECT, but each moved a card with no G.messages line, so the log read as if two bugs occurred. One: every villain/henchman defeat rescued captured+1 bystanders because the MVP city-entry rule D-1701 silently attaches 1 bystander to every villain/henchman on entering the City, attachBystanderToVillain called in villainDeck.reveal.ts, and only the villain-deck bystander reveal and the Midtown-Bank twist were logged — the entry attach was silent, so the later rescued-N-bystanders line, a faithful victory-pile delta, correctly counted the silent entry bystander and read off-by-one. Two: a player played a hero they never recruited, the Skrull-Shapeshifters ambush puts the rightmost HQ hero under the villain via captureHeroFromHq and on defeat awardAttachedHeroes moves it to the DEFEATING player's discard, Fight: Gain that Hero per WP-214, and the capture was logged but the return was silent so the hero appeared in a deck with no trail. Fix: append one guarded pushLog on the city-entry attach naming the bystander and villain, snapshot the pile top before the attach mutates it, no line when the supply pile is empty; and one guarded pushLog per captured hero returned naming the hero and villain, snapshot G.villainAttachedHeroes before awardAttachedHeroes deletes the entry, none when no hero was attached. The engine's own Debuggability rule prescribes this, a human-readable G.messages entry for non-obvious behavior. Boundary: log-only, G.messages is hash-excluded per D-24081 so NO finalStateHash re-pin, full engine suite 2073/0 +4 unchanged hashes; no state, contract, schema, persistence, response-shape, or auth change, both edits stay in the Game Engine layer with no new cross-layer import; the MVP-vs-canonical faithfulness of the D-1701 entry-attach rule is unchanged and out of scope, this only narrates it. Section 21 N/A. Section 17 N/A, internal log readability. Reserves D-24252; EC-466; Lightweight lane drafted+executed 2026-07-25. User-visible on play.legendary-arena.com game log, D-24026 N/A log-text only, verified by the +4 engine tests and re-reading the motivating game log)"]
        ["WP-432 ✅ Remove the non-canonical city-entry bystander attach, supersedes D-1701 (game engine; makes the bystander flow FAITHFUL to tabletop Legendary by deleting the D-1701/WP-017 MVP rule that attached 1 bystander from G.piles.bystanders to every villain/henchman on City entry, attachBystanderToVillain in villainDeck.reveal.ts. Canonical rule confirmed against tabletop sources: a villain does NOT capture a bystander merely by entering the City; bystanders enter play ONLY via a bystander CARD revealed from the villain deck, captured by the frontmost city villain or the Mastermind if the city is empty, or via a specific Ambush / Master-Strike / Scheme-Twist / Fight capture-bystander effect. The defect: the engine ran BOTH the canonical card-reveal source AND the D-1701 entry-attach, two independent bystander populations, bystander-villain-deck-NN cards versus pile-bystander supply tokens, both scored as flat bystander VP, so it put about 2x the canonical bystanders into play, inflated bystander VP, and drained the supply pile that hero Rescue-a-Bystander abilities share, the pressure behind the D-24032 30-floor. This surfaced while reviewing a full co-op Magneto game log where every villain/henchman defeat rescued captured-plus-1 bystanders. Fix, engine: delete the entry-attach block plus the WP-431 city-entry log line plus the now-unused attachBystanderToVillain import in villainDeck.reveal.ts, the helper stays because the Midtown Bank twist and Fight capture-bystander still call it; rewrite about 3 tests that asserted the entry-attach into canonical no-attach guards and fix the Midtown scheme-twist supply-count expectations that assumed a chained-reveal attach; correct the long-standing D-18504 to D-1701 mis-citation, D-18504 is the unrelated ambush-wound-loop deletion, in the code comment, the DECISIONS D-20006 mirror and D-24252 entry, and the WP-431 artifacts, and mark D-1701 Superseded. Impact: roughly halves bystanders-in-play and bystander VP, a scoring and balance change in the faithful direction, operator-approved; relieves supply-pile pressure so hero rescues are more reliable and the D-24032 30-floor is now over-provisioned and unchanged. Determinism: removing the entry-attach changes G only for a game that reveals a villain/henchman with a non-empty supply; the one committed golden fixture sentinel-core-doom-2p reveals only mastermind-strike cards so its finalStateHash is byte-unchanged and needs NO re-pin, and PRE_WP080_HASH, an empty replay, is untouched. Engine suite 2071/0; pnpm -r build 0; pnpm -r --no-bail test green. Section 21 N/A. Section 17 faithfulness to the licensed Legendary ruleset, no revenue or non-goal conflict. Reserves D-24254; EC-467; Lightweight lane drafted+executed 2026-07-25. User-visible on play.legendary-arena.com, bystander VP totals drop toward tabletop, D-24026 observational. Hard-deps WP-017, WP-431)"]
                ["WP-430 ✅ Fluid desktop responsive scaling for the play surface (App, apps/arena-client; design approved by the operator 2026-07-25 and promoted to FULL — EC-465 authored, session prompt written, pre-flight READY, copilot PASS; ready to execute, not yet executed). Adds a fluid desktop scaling model to PlayDesktop, ADDITIVE to the D-12909 max-width 767px mobile/desktop split which stays unchanged (useViewport.ts and PlayMobile untouched). Three things, all pure CSS: (1) a centered max-width cap, --play-max-width 1600px plus margin-inline auto on .play-desktop, so ultra-wide/4K monitors gain margin not oversized cards — today .play-desktop is a plain flex column with no cap and stretches edge-to-edge; (2) fluid clamp() sizing between a 1366px comfortable desktop floor and the cap via a --play-gutter token and three --card-width tokens whose clamp floor equals the current fixed card sizes 60/90/120px, so tiles only grow never shrink and aspect-ratio 5/7 is preserved; (3) checkpoint media queries at 1440/1920/2560px for gutter/spacing tuning only, not new layouts, so the D-12901 Mastermind-top-left and D-12902 opponents-top-edge placement is preserved. The exact clamp curves are the operator-tunable knob; the locked anchors are cap 1600, floor 1366, the three checkpoints, and floor-preserving card widths. Pure CSS presentation: reads no G/UIState, writes no G/ctx, zero engine/determinism/replay footprint, no finalStateHash re-pin; no new dependency, no CSS framework, no container queries. Three-file App-layer allowlist: styles/base.css tokens, pages/PlayDesktop.vue container cap plus checkpoints, components/play/CardTile.vue fluid widths; no useViewport.ts, no PlayMobile, no packages, no skin theme.css. Section 21 N/A, client CSS with no HTTP surface. Section 17 accessibility, readability across desktop sizes, no conflict, NG-1..7 uncrossed. Reserves D-24251. User-visible on play.legendary-arena.com, D-24026 required, verified by resizing a real browser: capped and centered at 2560, no horizontal scroll at 1366, tiles scale from the floor, mobile intact at 767 and below. Resolves the ewiki Responsive Viewport Targets open question. Hard-deps WP-129, WP-007a, WP-100-era CardTile. Reserves D-24251; EC-465; standard two-session lane, drafted 2026-07-25, promoted to FULL 2026-07-25, executed 2026-07-26 — impl a8b82d9e, D-24251 Active, arena-client typecheck 0 + suite 1099/1099, pnpm -r build 0, D-24026 live-verified via browser resize (2560 capped 1600 and centered no-h-scroll, 1366 floor no-h-scroll cards at 60/90, 767-and-below PlayMobile unchanged), 3-file allowlist held; ewiki Responsive Viewport Targets Open-question update deferred to PR 1019 merge)"]
        ["WP-433 ✅ Bot-ally turn-fault observability, log the reason and pending-choice state (server; closes the silent-fault observability gap. attemptBotTurn in botAllyDriver.mjs has four fault-return sites and only the decision-threw one logged, so the other three, policy returned no move, the offered move never advanced the _stateID, and the per-turn step cap, faulted SILENTLY. A recurring bot-ally freeze, status faulted and driving false and could-not-finish-its-turn, could NOT be root-caused from server logs across three rounds of log-trading because the fault path logged nothing. From the client diagnostic alone the two candidate causes are indistinguishable and the deciding data is server-side: a getLegalMoves resolution gap where a block-all pending choice is set that has no bot-resolvable move, the WP-427 class, versus a transient store or _stateID wedge with no pending set, the WP-424/426 class; awaitingPlayerInput surfaces only 4 of the 8 pending types so the client cannot see the culprit. Fix, server log-only: a defensive summarizeBotTurnState helper emitting one line, turn, stage, stateId, available attack and recruit, hand size, and which of the nine block-all pending flags are set, plus a console.error [bot-ally] match seat FAULTED with reason and summary at each of the four fault returns; the move-did-not-advance fault names the offered move, and the step-cap fault uses a retained lastStateSnapshot since it returns after the loop out of the state scope. Why the pending set is the discriminator: pending non-empty means a getLegalMoves gap for that named choice, fix belongs in ai.legalMoves.ts the WP-427 pattern; pending none on an already-end-able turn means a store or _stateID wedge, the WP-424/426 infra class; one log line now settles it, turning a multi-round remote-diagnosis dead-end into a one-shot read. Behavior-neutral: only console.error diagnostic lines added, the fault kind and message and teardown and persisted status and revival and status route are byte-unchanged; the summarizer is fully defensive and never throws because a fault log must not crash the tick. Bot-ally driver suite 25/0, plus 2, a fault-log-capture test and a summarizeBotTurnState unit test; pnpm -r build 0. Section 21 N/A. Section 17 N/A, internal operator observability, no rule or economy or revenue change. Reserves D-24255; EC-468; Lightweight lane drafted+executed 2026-07-26. User-visible surface none, internal operator tooling, D-24026 N/A, the payoff is the NEXT live freeze being diagnosable from one log line. Hard-deps WP-375, WP-419, WP-427)"]
        ["WP-437 ✅ Bot-ally cross-instance ownership guard, deploy-overlap two-writer freeze (server; closes the residual freeze WP-424 and D-24244 explicitly deferred. WP-424 stopped the OLD instance's drivers at SIGTERM, but on a rolling Render deploy the NEW instance boots, revives a match's BotAllyDriver, and starts submitting the bot's moves BEFORE Render SIGTERMs the OLD one, so for the termination-grace window TWO instances drive the same bot seat and race on boardgame.io's _stateID, invalid stateID was N expected N+1; the bot's move never lands, the turn livelocks, and because a driver is registered the status route reports driving true status active so neither WP-419's banner which needs driving false nor WP-433's fault log fires, a silent frozen board, live match Sk1ASNTkGSz, same signature as the WP-424 case DBlXvBs_WXA. Fix, server only, D-24256: a per-match ownership LEASE in legendary.match_bot_ally, migration 038 additive driver_owner text plus heartbeat_at timestamptz, NULL-default and idempotent. A new botAllyOwnership.mjs holds SERVER_INSTANCE_ID, a per-process crypto.randomUUID, BOT_ALLY_LEASE_TTL_MS 15000, an atomic claim-or-renew acquireOrRenewBotAllyLease, claimable when unowned or mine or never-heartbeated or stale past the TTL, a fresh peer lease is not, and releaseBotAllyLeasesForOwner. The driver arbitrates COOPERATIVELY at each 250ms poll tick, top of runTick via an OPTIONAL deps.renewOrAcquireLease: it drives only while it holds the lease, YIELDS the tick to a live peer submitting nothing so no _stateID race, and skips the tick on a lease-check throw. The existing poll loop IS the retry, so the newly-revived driver takes over the instant the old owner releases, SIGTERM releaseBotAllyLeasesForOwner in index.mjs is a near-instant handoff, or its heartbeat expires, the roughly 15s crash TTL. Design, D-24256: Option B, the heartbeat lease, over Option A, a pg advisory lock, because the single shared max 10 pool cannot pin a client per driver for the driver's lifetime without starving every other server surface, and a session-scoped advisory lock requires exactly that pinned connection; Option B uses pooled queries, composes with the existing side-table and WP-420 SIGTERM machinery, and is observable, driver_owner and heartbeat_at are directly queryable and handoffs log. Behavioural NO-OP for a single instance, it always wins its own lease, and the optional dep keeps every pre-WP-437 unit test and single-instance prod byte-unchanged. TTL 15s never false-expires a live owner, which renews every tick, yet frees a crashed owner with no clean SIGTERM within a bounded window; a single turn exceeding 15s under a sustained DB outage is a documented residual, still bounded by the fault machinery and the stale-stateID reject. Untouched: the 250ms poll, fault and retry budgets, MAX_REVIVALS, WP-419 liveness, WP-420 revival, WP-424 SIGTERM-stop, WP-426 empty-tolerance; the human is NEVER gated, the lease governs only which instance drives the bot seat. Ownership state is side-table only per D-24095, never G or ctx and never the bgio blob, the same data class as revive_count and shutdown_interrupted. driver_owner is NOT surfaced on the public guest status route, it stays driving status message, so section 21 and D-11804 N/A, no HTTP shape change, ownership is observed via the DB and logs. Section 17 Vision, section 11 lifecycle and section 14 reliability, no conflict. Bot-ally suite plus 9, 4 botAllyOwnership.test.ts, instance-id and TTL shape, claim returns true, peer-fresh returns false, release by owner, and 5 driver lease cases, owner drives, peer yields, cooperative handoff, lease throw skips, no-dep back-compat; server suite 948/0 plus 158 DB-skipped; pnpm -r build 0; migration 038 applied and re-applied idempotent against a local Postgres and the claim-or-renew exercised live, claim unowned 1, peer fresh 0, stale past TTL 1, release then re-claim 1. Reserves D-24256; EC-472; standard lane drafted+executed 2026-07-26. User-visible on play.legendary-arena.com, D-24026 live-verify operator-pending, deploy twice in quick succession mid bot-ally match, the class only reproduces under real overlapping deploys. Hard-deps WP-375, WP-419, WP-420, WP-424, WP-426, WP-433)"]
                ["WP-430 📝 Fluid desktop responsive scaling for the play surface (App, apps/arena-client; design approved by the operator 2026-07-25 and promoted to FULL — EC-465 authored, session prompt written, pre-flight READY, copilot PASS; ready to execute, not yet executed). Adds a fluid desktop scaling model to PlayDesktop, ADDITIVE to the D-12909 max-width 767px mobile/desktop split which stays unchanged (useViewport.ts and PlayMobile untouched). Three things, all pure CSS: (1) a centered max-width cap, --play-max-width 1600px plus margin-inline auto on .play-desktop, so ultra-wide/4K monitors gain margin not oversized cards — today .play-desktop is a plain flex column with no cap and stretches edge-to-edge; (2) fluid clamp() sizing between a 1366px comfortable desktop floor and the cap via a --play-gutter token and three --card-width tokens whose clamp floor equals the current fixed card sizes 60/90/120px, so tiles only grow never shrink and aspect-ratio 5/7 is preserved; (3) checkpoint media queries at 1440/1920/2560px for gutter/spacing tuning only, not new layouts, so the D-12901 Mastermind-top-left and D-12902 opponents-top-edge placement is preserved. The exact clamp curves are the operator-tunable knob; the locked anchors are cap 1600, floor 1366, the three checkpoints, and floor-preserving card widths. Pure CSS presentation: reads no G/UIState, writes no G/ctx, zero engine/determinism/replay footprint, no finalStateHash re-pin; no new dependency, no CSS framework, no container queries. Three-file App-layer allowlist: styles/base.css tokens, pages/PlayDesktop.vue container cap plus checkpoints, components/play/CardTile.vue fluid widths; no useViewport.ts, no PlayMobile, no packages, no skin theme.css. Section 21 N/A, client CSS with no HTTP surface. Section 17 accessibility, readability across desktop sizes, no conflict, NG-1..7 uncrossed. Reserves D-24251. User-visible on play.legendary-arena.com, D-24026 required, verified by resizing a real browser: capped and centered at 2560, no horizontal scroll at 1366, tiles scale from the floor, mobile intact at 767 and below. Resolves the ewiki Responsive Viewport Targets open question. Hard-deps WP-129, WP-007a, WP-100-era CardTile. Reserves D-24251; EC-465; standard two-session lane, drafted 2026-07-25, promoted to FULL 2026-07-25, not yet executed)"]
        ["WP-429 📝 Reconnect / resync counters in the transport diagnostics block (App, apps/arena-client; the follow-up WP-428 deferred — it DOES touch client/bgioClient.ts. Extends the shipped WP-428 transport block with four recovery counters — reconnectResyncCount, moveAckResyncCount, spectatorStaleResyncCount, tabFocusResyncCount — one per existing auto-recovery path in the transport wrapper: the D-24232 reconnect-resync onTransportReconnect, the WP-312 move-ack-timeout onWatchdogFire, the spectator-staleness timer fire, and the tab-focus onVisibilityChange. Each increments a WP-311 connection-store counter PAST its path's cooldown gate. The two clean paths take a single increment in their own body; the spectator and tab-focus paths share the gated helper forceCooldownGatedResync, which gains a cause parameter, spectator or tabFocus, so its single past-gate increment routes to the right counter without conflating the two or counting cooldown-suppressed triggers — an independent gate audit caught that shared-helper coupling and the WP was corrected and scaffold-proven, the spectator resync tallies only spectatorStaleResyncCount not tabFocusResyncCount. buildTransportDiagnostics copies the four counts verbatim; paired with WP-428 timeSinceLastFrameMs a freeze report names both how stale the connection is and how hard the client already fought to recover. No recovery-behavior change — no timing constant, cooldown, watchdog-arming, or performResync edit, proven by a git diff timing-constant no-diff gate; the manual banner resync is not counted; the exporter DiagnosticExportButton.vue is unchanged, the counts ride the store it already reads. Pure client presentation: App layer only, framework/transport state never G and never persisted per WP-116, EC-260 diagnostics boundary held via a structural state param, zero engine/determinism/replay footprint, no finalStateHash re-pin. Six-file App-layer allowlist; no DiagnosticExportButton.vue, no packages. Section 21 N/A, client-side with zero network egress. Section 17 N/A, internal diagnostics that counts sync recovery and changes no sync behavior. User-visible surface none, internal operator tooling, no D-24026 gate, verified by a report download after a driven reconnect or resync. Closes the ewiki Play Diagnostics counters-are-a-not-yet-surfaced-follow-up note. Hard-deps WP-428, WP-311, WP-312, D-24232. Reserves D-24250; EC-464; standard two-session lane, drafted 2026-07-25, not yet executed)"]
        ["WP-428 ✅ Transport diagnostics block in the play-surface diagnostic export (App, apps/arena-client; adds a transport block to the WP-228 diagnostic report so a downloaded freeze report names the client's live connection state instead of leaving the transport layer opaque. Five fields: isConnected, lastStateId, hasEverConnected read verbatim from the WP-311 connection store; a new lastFrameAtMs, a client wall-clock stamp the store records on every subscribe frame via a defaulted atMs = Date.now() third parameter on setConnected so client/bgioClient.ts's two-argument call is UNCHANGED; and a derived timeSinceLastFrameMs, the capture clock minus lastFrameAtMs, the decisive signal for the waiting-forever-for-a-server-frame freeze class the motivating live report, match 660LwoUY-Yq with entries empty and no transport visibility, could not diagnose. Typed TransportDiagnostics not opaque unknown; the one clock subtraction lives in a pure buildTransportDiagnostics of state and capturedAtMs so buildDiagnosticReport stays pure, mirroring the shipped uiStateSnapshot and matchSetup pass-through. Pure client presentation: reads the framework/transport connection store only, never G per the WP-116 disconnect policy and never persisted; the EC-260 diagnostics-module boundary holds, no engine/registry/boardgame.io import; zero engine/determinism/replay footprint, no finalStateHash re-pin. Out: reconnect/resync/frame counters, which live as bgioClient.ts locals, a follow-up WP-A2; perf/memory capture; a transition log; buffer-durability-across-refresh; raw frame capture; server-side correlation. Five-file App-layer allowlist; no client/bgioClient.ts, no packages. Section 21 N/A, report assembled client-side with zero network egress. Section 17 N/A, internal diagnostics that reads sync observability and changes no sync logic. User-visible surface none, internal operator tooling, no D-24026 gate, verified by downloading a report and confirming the transport fields reflect live connection state. Closes the ewiki Play Diagnostics no-transport-data-captured-today Edge Case. Hard-deps WP-228, WP-311, WP-246. Reserves D-24249; EC-463; standard two-session lane, drafted 2026-07-25, not yet executed)"]
        ["WP-423 ✅ Coordinated Hugo version upgrade — ewiki build surface (infrastructure / shared tooling; bumps the engine monorepo's ONE Hugo site, apps/wiki-viewer the ewiki, from 0.135.0 to a current Hugo Extended release, recommended 0.161.1 to match sibling legendary-arena-lab's production pin, and adds a CI page-presence guard so a silent sub-page drop fails the build instead of shipping a 404. Modeled on the legendary-arena-lab outage: CI and Cloudflare Pages were pinned 0.149.0 while local dev ran 0.161.1, a newer-syntax files glob was UNKNOWN on 0.149.0 so the wiki mount silently dropped and every sub-page 404'd in production while hugo --minify exited 0 and CI stayed green; lab PRs 13 guard, 14 pin, 15 config-modernize-after-floor, 12 headers. Discovered, not assumed, is the spine and this repo differs in four load-bearing ways. One, exactly one Hugo site here, a grep of workflows, render.yaml, config files, package.json, .tool-versions, netlify.toml, and wrangler found no second site. Two, CI, the wiki-viewer.yml workflow using peaceiris/actions-hugo v3 extended, and the Render deploy, render.yaml buildCommand curling hugo_extended of cat .hugo-version, already read the same apps/wiki-viewer/.hugo-version file, so the lab's CI-versus-dashboard drift cannot recur and the only drift surface is local dev with its unpinned binary. Three, there is no Cloudflare Pages Hugo build in this repo, the Pages and dashboard-HUGO_VERSION and floor-0.146.0 material in wiki/hugo-onboarding.md documents the marketing site in the separate C:\\www\\legendary-arena-com repo whose upgrade is a companion WP there, out of scope. Four, hugo.toml uses a single-directory content projection with no Modules and no files or module.mounts keys, so the lab's exact silent-mount-drop is structurally absent and the page-presence guard is added as defense-in-depth beside the existing determinism and JS-free gates. Lockstep invariant: the pin lives in .hugo-version only, no version literal in the workflow or render.yaml. Rendered content unchanged, proven by a pre-bump baseline diff of public captured on 0.135.0 before the bump, NOT by the same-version determinism gate which only proves intra-version reproducibility and never compares the two versions; accept only cosmetic minifier drift, STOP on any content or page or structure change. Config modernization, files and locale, deferred since there is a single build target with no floor to clear. No engine, registry, server, PG, G, or ctx touched; section 21 N/A, no HTTP endpoint or apps/server/src function; section 17 internal tooling with no trigger surface; section 20 N/A. User-visible surface = none, infrastructure only, payoff is a current pin, local-to-CI-to-deploy lockstep, and a page-presence CI guard. Hard-deps WP-139, D-13808/D-13811/D-13813. Reserves D-24243; EC-458; drafted 2026-07-24, not yet executed)"]

      Friends & Ranked Trust (2026-07)
        ["WP-350 ✅ Friendships data model + status machine + mutual-clique helper (server) — new legendary.friendships table (migration 028; player_id FK CASCADE, closed status pending/accepted/declined, normalized-pair LEAST/GREATEST unique index, addressee_id/status lookup index); AccountId-keyed send/accept/decline/remove state machine + list helpers + getFriendshipStatus + areAllMutualFriends clique predicate (accepted-pair count == C(n,2); n≤1 vacuous; order/dup-independent); declined→pending is an UPDATE, removeFriend DELETEs; library-only (no endpoint/UI); EC-380; D-24142"]
        ["WP-351 ✅ Friend-request API (/api/me/friends*) — six authenticated-session-required routes over WP-350's logic (send/list-friends/list-requests/accept/decline/remove); resolves @handle→AccountId inbound (findAccountByHandle) and enriches to FriendSummary (handle+displayName, never accountId — FR-2) via one ext_id=ANY($1) round-trip; acting account needs a claimed handle (handle_required); auth-first, session-resolved actor; WP-350 contract untouched; 6 api-endpoints rows; EC-381; D-24143"]
        ["WP-352 ✅ Friends tab on the owner profile (arena client) — a Friends section on ?route=me: add-by-@handle, incoming requests (accept/decline), outgoing pending (display-only), friends list (remove) over WP-351's API; new friendsApi.ts + useFriends composable + FriendsSection.vue mounted in MyProfilePage.vue; handle-only identity on screen (never accountId — FR-2); client error-code mirror + drift test; mutate→refetch; EC-382; D-24144"]
        ["WP-353 ✅ Friend-request email notifications (Brevo transactional, server) — fire-and-forget, fail-open emails on request-received + request-accepted; adds the missing transactional path (POST /v3/smtp/email, createBrevoTransactionalSender, injectable fetch) without touching WP-293's brevoClient.types.ts; notifyFriendRequest{Received,Accepted} = single fail-open boundary (never throws; unconfigured/failed → console.warn no-op); wired void into WP-351's send/accept handlers with no endpoint-contract change; template-driven copy, no accountId in params; EC-383; D-24145"]
        ["WP-354 ✅ Ranked eligibility gate: friendship-clique check at score submission (server) — at submitCompetitiveScoreByMatchIdForRequest, readSeatAccounts + areAllMutualFriends → is_ranked_eligible on competitive_scores (migration 029, default true; n≤1 vacuous so solo stays ranked; fail-safe to Casual on any throw — submission never fails; evaluate-once/immutable FR-7); public ranked leaderboard SELECT+COUNT filter is_ranked_eligible=true, owner My-Scores unfiltered (Casual); scoring math byte-identical; lobby-invite half split to a future WP; EC-384; D-24146"]
        ["WP-355 ✅ Friend abuse controls: block list + request rate limit + re-request cooldown (server) — new legendary.player_blocks table (migration 030; separate model — blocking is never a friendship status, D-24142); blockPlayer INSERTs the block AND severs any existing friendship transactionally; symmetric isEitherBlocked + per-day cap (20) + re-request cooldown (24h) run block→cooldown→rate-limit before sendFriendRequest; 3 new POST/DELETE/GET /api/me/blocks endpoints (handle+displayName, no accountId); FriendApiErrorCode += blocked/rate_limited/request_cooldown; WP-350 contract untouched; block routes mount inside registerFriendshipRoutes (server.mjs untouched); EC-385; D-24147"]
        ["WP-357 ✅ Friend-request email opt-out preference (server) — per-account friend_request_emails boolean on legendary.player_profiles (migration 031, default true); read/written additively via OwnerProfileView/OwnerProfilePatch (12→13 keys) through the existing transactional upsertOwnerProfile; checked in WP-353's sendFriendNotification — opted-out recipient → clean no-op (no send, no warn), governing both friend emails; pref folded into resolveIdentities via LEFT JOIN + COALESCE(..., true); WP-353 signatures byte-identical; packet #6 follow-on; EC-387; D-24149"]
        ["WP-358 ✅ Match friend-invite (server) — a seated player invites an accepted friend by @handle → legendary.match_invites record (migration 032) + fail-open notifyMatchInvite email; accept returns matchId and the client joins via the existing POST /api/match/join (no server-side bgio join); friends-only by design (getFriendshipStatus==='accepted') so anti-spam + block-respecting by construction; 4 authenticated endpoints, MatchInviteView never exposes accountId; lobby-invite half of packet #5; full serialized server suite 934/934; EC-388; D-24150"]
        ["WP-359 ✅ Friend-email opt-out toggle (arena client) — a ?route=me checkbox reading/writing WP-357's friendRequestEmails via the existing owner-profile fetch/save (one ref + one PATCH field + one checkbox); client OwnerProfileView/Patch mirror gains the field; no new endpoint/composable; arena-client typecheck 0 + test 845/845; client follow-on to WP-357; EC-389; D-24151"]
        ["WP-360 ✅ Match-invite UI: invitee core (arena client) — matchInvitesApi (mirrors friendsApi) + useMatchInvites + MatchInvitesSection (?route=me: list pending invites, Accept→hand off to Lobby, Decline) mounted in MyProfilePage; handle-only identity, never accountId; mutate→refetch + client error-code drift mirror; inviter-side invite trigger + full seat-join deferred to a follow-on (LobbyView has no persistent matchId; join needs the seat/credentials flow); arena-client typecheck 0 + test 867/867; EC-390; D-24152"]
        ["WP-366 ✅ Match-invite UI: inviter trigger + join-from-invite (arena client) — completes the deferred WP-360 follow-on: new InviteFriendControl.vue mounted in PlayViewport (self-contained — reads ?match= + useAuthStore, the ViewLoadoutButton idiom; render-gated live-match+authed; never G/UIState) + additive inviteFriendToMatch wrapper & useMatchInvites().invite; MatchInvitesSection Accept upgraded to a real join via a new pure joinMatchFromInvite.ts (injected listMatches/joinMatch/navigate deps) → lobbyApi.listMatches (first open seat) + joinMatch + the joinExisting ?match&player&credentials navigate (no reimplemented join); match-absent→\"no longer available\", full→\"already full\"; handle-only identity (never accountId, FR-2); §23(b) copy; client-only, no server change; arena-client typecheck 0 + test 899/899; EC-396; D-24158"]
        ["WP-369 ✅ Pre-match waiting room: seat-aware 'Waiting for players' invite panel (arena client) — play-view waiting state (operator chose it over a dedicated new room surface): a WaitingForPlayersPanel.vue in PlayViewport that supersedes WP-366's corner InviteFriendControl (deleted), renders only for an authed live match with ≥1 open seat (auto-hides when full/gone; solo never shows), shows '{filled} of {total}' + an @handle invite (reuses useMatchInvites().invite, no new mechanic) + a Copy-join-link ${origin}/?route=lobby&match=<id> (public deep-link, no secret); seat-fill via a new useMatchSeatStatus composable that POLLS lobbyApi.listMatches (open=!seat.name) at SEAT_POLL_INTERVAL_MS=5000, cleared on full/gone/unmount, last-snapshot-preserving — no bgioClient/transport change (matchData not plumbed); LobbyView gains a minimal ?match= row highlight+order; handle-only (never accountId); client-only, no server change; arena-client typecheck 0 + test 908/908; EC-398; D-24163"]
        ["WP-370 ✅ Player-count setup table (registry source of truth) + engine composition block + villain-deck bystander fix (registry + game-engine) — a canonical PLAYER_COUNT_SETUP table (villain groups/henchmen/villain-deck bystanders/heroes = 1·1·1·3 / 2·1·2·5 / 3·1·8·5 / 3·2·8·5 / 4·2·12·6) in packages/registry as the single source of truth; the engine reads it off the CardRegistry object at setup via structural typing (no registry import — layer boundary); validateMatchSetup(input,registry,numPlayers?) BLOCKS villain-group/henchman/hero length mismatches at Game.setup (numPlayers optional so 2-arg callers + table-less mocks skip → zero fixture breaks); game.ts threads ctx.numPlayers into validateSetupData + setup(); the villainDeck.setup null-scheme in-deck bystander fallback reads the table's villainDeckBystanderCount (1/2/8/8/12) not ctx.numPlayers (D-24166 fixes 3/4/5-player under-seeding; scheme override wins); registry-side coupling ships as a pure checkPlayerCountComposition helper (setupContract byte-unchanged); supply-pile bystandersCount floor D-24032 unchanged; 2p sentinel byte-identical (no re-pin); registry 137/0 + engine 1927/0; foundation packet — unblocks WP-371/372; EC-399; D-24165/D-24166"]
        ["WP-371 ✅ Lobby player-count pre-submit check: read-only setup-requirements endpoint (server) + warn/disable-before-submit (arena client) — operator 'expand it' scope change: the drafted server composition GATE was dropped as redundant (WP-370's validateSetupData already blocks a mismatch and POST /api/match/create already propagates it as a 400). Instead a read-only GUEST GET /api/match/setup-requirements returns { requirements: PLAYER_COUNT_SETUP } (server imports @legendary-arena/registry; cacheable; guidance-only NOT a gate) + a lobby pre-check: LobbyView fetches on mount (best-effort) and computes villain-group/henchman/hero mismatches on both create paths (uploaded playerCount/composition + manual numPlayers/CSV lengths) via a new pure playerCountRequirements.ts (computePlayerCountMismatches + formatMismatchWarning), rendering a warning per mismatch and disabling Create (canSubmitFromJson + canSubmitCreate); fetchSetupRequirements shape-guarded → null (never undefined); progressive enhancement (null requirements → inert; engine 400 remains the authority); arena-client imports no registry table; POST /api/match/create/join byte-unchanged; new api-endpoints row (D-11804); arena-client 916/0 + server 797/154/0; EC-400; D-24167 reframed"]
        ["WP-372 ✅ Loadout builder: player-count required counts + warn/export-gate (registry viewer) — completes the player-count arc. The cards.legendary-arena.com Loadout tab gains two computeds off the single-source-of-truth registry table (requiredPlayerCountSetup + playerCountCompositionMismatches via checkPlayerCountComposition/getPlayerCountSetup; NO re-typed literals), a 'For a N-player match: …' required-counts readout, a full-sentence warning per villain-group/henchman/hero mismatch (mirrors the missingRequiredVillainGroupIds pattern), and the mismatch added to both export handlers + both Download :disabled bindings — the warn-in-builder/gate-export half of D-24165; authoring stays free (heroes chosen, never auto-filled); no game-engine import. Packaging refinement: WP-370 exported the table only from the node-only root barrel, so this WP re-exports it from the browser-safe setupContract barrel (playerCountSetup.ts has zero node deps) — viewer Vite build stays browser-safe. registry-viewer 127/0; D-24026 live-verified on the worktree dev server (readout + warnings + Download disabled; reactive 2→4); EC-401; consumes D-24165 (no new D-entry)"]
        ["WP-375 ✅ Solo bot-ally driver: mixed human+bot match (server) — new POST /api/match/create-with-bot + a per-match BotAllyDriver that reserves + auto-readies the bot seat(s) and drives their moves via Master.onUpdate, narrowing the WP-163 autoplay lifecycle to a mixed human+bot cooperative match (human joins seat 0 authed; bots secret-delegated with NO match_seat_accounts row per D-24120; endpoint never calls startMatchIfReady; join-before-return ordering keeps the waiting panel hidden; match tagged botSeats; no new engine variant). Driver acts only on a bot seat's turn / bot-owned pending choice, drains choices via findPendingChoiceMove, never blocks the human (endTurn→advanceStage→bot-faulted), torn down on every exit path; determinism = seeded bot PRNG only. Produces the botSeats tag WP-377 consumes; blocks WP-376. Open at execution: poll-vs-subscribe turn detection, restart re-hydration, metadata storage. Source design DESIGN-SOLO-BOT-ALLY.md; EC-404; reserves D-24170"]
        ["WP-376 ✅ Solo bot-ally lobby affordance (client) — a 'Play with a bot ally' arena-client lobby control (seat count + bot count + policy) → createMatchWithBot (POST /api/match/create-with-bot) → the human joins seat 0 via the authed joinMatch(..., authToken) (NEVER a server-returned credential — the key distinction from the autoplay spectator flow; keeps seat 0's match_seat_accounts row so WP-377's ranked guard + attribution stay correct) → play surface. Co-op copy only (§23(b)); auth-gated; botCount∈1..seats-1 client-validated; reuses buildConfig(); no WaitingForPlayersPanel logic change. Build/merge may precede WP-377 but production exposure is blocked until WP-377 Active (else the DESIGN §5b ranked-farm vector is live). Client-only; EC-405; reserves D-24171"]
        ["WP-377 ✅ Ranked eligibility: seat-count-complete roster guard (server) — hardens computeRankedEligibility so ranked ⇔ readSeatAccounts(matchId).length === seatCount AND areAllMutualFriends(roster), plus a botSeats-tag short-circuit — closes the DESIGN §5b leak where a rowless (bot/guest) seat yields a short roster that is vacuously ranked, letting a 1-human+1-bot match submit a ranked score. Predicate is !== seatCount (NOT < 2) so genuine solo stays vacuously ranked; fail-safe Casual preserved (extends the WP-354 try/catch); by-hash ?? true default untouched; guard lives only in computeRankedEligibility. Landable independently of WP-375 (the seat-count backstop is a no-op for all-human matches; the tag short-circuit activates once WP-375 writes it). No migration (is_ranked_eligible exists, WP-354/029). Gates WP-376 production exposure. Open at execution: seat-count source (ctx.numPlayers recommended / playerZones). EC-406; reserves D-24172 (amends D-24146)"]

      Next Horizons
        ["📦 Core set keyword & ability coverage — get the core set fully playable first, then add sets incrementally (in progress via the effect-authoring grind — e.g. WP-310/316/317)"]
        ["📦 Live PvP matchmaking & match-discovery UX — reconnect/desync resilience now SHIPPED (WP-116 policy + WP-311 reconnect + WP-312 move-ack watchdog); what remains is matchmaking + a match-discovery/join UX"]

      Phase 10 — Debugging, Testing & Troubleshooting
        ["Future-WP-A 📝 Placeholder — replay diff tool"]
        ["Future-WP-B 📝 Placeholder — ops histogram aggregator"]
        ["Future-WP-C 📝 Placeholder — determinism verifier"]
        ["Future-WP-D 📝 Placeholder — server error telemetry"]
        ["Future-WP-E 📝 Placeholder — engine perf profiler"]
        ["Future-WP-F 📝 Placeholder — end-to-end smoke suite"]
        ["Future-WP-G 📝 Placeholder — disconnect stress suite"]
        ["Future-WP-H 📝 Placeholder — synthetic load generator"]

      Governance Drafts
        ["WP-097 ✅ Tournament funding policy"]
        ["WP-098 ✅ Funding surface lint gate"]
        ["WP-042.1 ⏸ Blocked — PostgreSQL seeding checklists"]

      Reference (one-line pointers)
        ["docs/12-SCORING-REFERENCE.md — formula and invariants"]
        ["docs/12.1-PAR-ARTIFACT-INTEGRITY.md — hashing trust model"]
        ["cards.legendary-arena.com — registry viewer (public)"]
        [".claude/CLAUDE.md — root coordination"]
        [".claude/rules/ — 7 rule files"]
        ["EC_INDEX.md — execution checklists (range in Project Baselines)"]
        ["DECISIONS.md — D-NNNN ledger (range in Project Baselines)"]
        ["WORK_INDEX.md — authoritative per-WP audit log"]
```

---

## Progress Summary (counts only)

<!-- ROADMAP-COUNTS:START (generated by scripts/roadmap-counts.mjs — do not hand-edit) -->
| Cluster | Done | Open |
|---|---|---|
| Foundation (Foundation Prompts) | 4/4 | — |
| Phase 0 — Coordination | 9/9 | — |
| Phase 1 — Game Setup | 4/4 | — |
| Phase 2 — Core Turn Engine | 5/5 | — |
| Phase 3 — MVP Multiplayer | 6/6 | — |
| Phase 4 — Core Gameplay Loop | 9/9 | — |
| Phase 5 — Card Mechanics | 7/7 | — |
| Phase 6 — Verification & Production | 14/14 | — |
| UI Implementation Chain | 5/5 | — |
| Content Layer | 3/3 | — |
| Pre-Planning System | 5/5 | — |
| Post-Phase-6 Hygiene | 5/5 | — |
| Phase 7 — Beta, Launch & PAR | 6/6 | — |
| Scoring & PAR Pipeline | 6/7 | 1 open |
| Beta-Launch Pillar | 5/5 | — |
| Engine Hardening | 2/2 | — |
| Client Integration Cluster | 21/21 | — |
| Auth Stack & Profile Surface | 15/15 | — |
| Engine + Server Wiring & Leaderboard HTTP | 3/3 | — |
| Registry Viewer Enhancements | 27/27 | — |
| Phase 8 — Interactive Board Layout | 3/3 | — |
| G-State Extensions | 4/4 | — |
| Monetization Stack | 3/3 | — |
| Engine & Test-Harness Cleanup | 5/5 | — |
| Physical Card Pipeline | 5/5 | — |
| Domain Cutover & Infrastructure | 10/10 | — |
| Public Leaderboard (Marketing) | 2/2 | — |
| Legends Public Scoreboard | 2/2 | — |
| Villain Deck Pipeline | 5/5 | — |
| Villain & Henchman Effects | 16/19 | 3 open |
| Hero Ability Coverage & Markup Pipeline | 52/52 | — |
| Notable Events & Overlays | 4/4 | — |
| Simulation Sweep & Analytics Pipeline | 8/8 | — |
| Dashboard & Operator Analytics | 18/18 | — |
| Agent Triage Pipeline | 7/8 | 1 open |
| Admin & Route Wiring | 4/4 | — |
| Phase 9 — Profile Surface Follow-ups | 11/11 | — |
| Architecture & API Governance | 4/4 | — |
| Complete-Game Testing | 1/1 | — |
| Cross-App Infrastructure | 2/2 | — |
| Multiplayer Play & Match Durability (2026-07) | 8/8 | — |
| Hero/Villain Effects & Diagnostics (2026-07) | 5/5 | — |
| Live-Play HUD & Pending-Choice UX (2026-07) | 20/20 | — |
| Competitive Score Submission & Verification (2026-07) | 10/10 | — |
| Gauntlet Leaderboards (Legends) (2026-07) | 108/115 | 2 ⏸, 5 open |
| Friends & Ranked Trust (2026-07) | 18/18 | — |
| Next Horizons | 0/2 | 2 📦 queued |
| Phase 10 — Debugging, Testing & Troubleshooting | 0/8 | 8 📝 placeholders |
| Governance Drafts | 2/3 | 1 ⏸ |
| **Total** | **494/507 WP ✅** (+ 4/4 Foundation Prompts) | 3 ⏸, 10 open |

**Open / blocked WPs (derived from WORK_INDEX, 13):** WP-042.1 ⏸ blocked; WP-349 open; WP-391 open; WP-396 open; WP-398 open; WP-399 open; WP-368 open; WP-403 ⏸ blocked; WP-404 ⏸ blocked; WP-422 open; WP-429 open; WP-453 open; WP-512 open.
<!-- ROADMAP-COUNTS:END -->

> Counts only. Description, deps, baselines, hashes — all in the mindmap line above or in `WORK_INDEX.md`. The table inside the markers above is **generated** by `scripts/roadmap-counts.mjs` (sole writer; D-24001), derived from `WORK_INDEX.md` status × mindmap cluster membership — it is no longer hand-maintained, so it no longer drifts. Status is authoritative from `WORK_INDEX.md`; cluster membership is authoritative from the mindmap nodes above. The generator **fails loudly** on a WORK_INDEX WP with no mindmap node (D-24002), so no work packet can be silently uncounted.
>
> **Counting convention (encoded by the generator, not redefined):** each row counts the distinct `WORK_INDEX.md` work-packets homed in that cluster (combined lines like `WP-005A/B` count their members; range lines like `WP-043..047` expand to each member; the Phase-6 `WP-048..051` line is a cross-reference — any node containing `(see ` — counted once under Scoring & PAR). Foundation = 4 Foundation Prompts (not WPs), reported as a separate `+N/N` addend. `Next Horizons` (3 📦) and `Phase 10` (8 📝) are forward-looking nav placeholders rendered `0/N`, not WPs; the `Reference (one-line pointers)` cluster is navigation and is excluded from the table. Open and blocked WPs are enumerated on the generated summary line inside the markers above (single source; not restated here).

---

## Project Baselines (canonical — single source; do not restate elsewhere)

- **Phase 3 Gate:** Closed (D-1320)
- **Phase 6 Gate:** Closed 2026-04-19 — tag `phase-6-complete` at `c376467`
- **Engine test baseline:** `1872 / 0 / 0` (438 suites)
- **Registry test baseline:** `130 / 0 / 0` (14 suites)
- **Server test baseline (no-DB):** `753 / 0 / 103` (856 total; 151 suites — the 103 are DB-gated non-silent skips; with `TEST_DATABASE_URL` + migrations the full serialized suite is `856 / 0 / 0`, last run 2026-07-09 under EC-376)
- **arena-client test baseline:** `809 / 0 / 0` (115 suites)
- **Dashboard test baseline:** `409 / 0 / 0` (15 suites)
- **Registry-viewer test baseline:** `110 / 0 / 0` (23 suites)
- **legends-board test baseline:** `40 / 0 / 0` (8 suites)
- **Preplan test baseline:** `52 / 0 / 0` (7 suites)
- **lagn test baseline:** `21 / 0 / 0` (8 suites)
- **vue-sfc-loader test baseline:** `11 / 0 / 0`
- **engine-runner test baseline:** `19 / 0 / 0` (2 suites)
- **replay-producer test baseline:** `4 / 0 / 0` (2 suites)
- **DECISIONS.md range:** `D-0203..D-24139` (870 entries; extends through the 2026-07-09 gauntlet/SSO arcs)
- **EC range:** `EC-001..EC-377` (extends through WP-347)

> All twelve `pass / fail / skipped` figures above are a live test-run at HEAD (`5843b7dd`) on 2026-07-10 (`pnpm --filter <pkg> test`, fresh worktree, `pnpm -r build` first), not STATUS-derived. Per-row `post-WP-NNN` attributions are gone on purpose: suites grow between doc passes, so the HEAD sha + date above is the provenance. The D/EC ranges are derived from `DECISIONS.md` entry headings and `docs/ai/execution-checklists/` filenames at the same HEAD.

---

## Next Unblocked (ordered)

1. **WP-345 — Player-count gauntlet boards + challenge links (legends-board client)** — drafted 2026-07-09; its hard-dep WP-344 (the D-24134 server half) executed the same day. Execution-prep landed 2026-07-10 (EC-379 + session prompt) — **the next step is the execution session itself.** It renders the `entryCounts` / `legs` / `players[]` data WP-344 already publishes, plus the per-leg "Challenge this leg" links into the WP-114 registry-viewer preview.
2. **Finish core-set ability coverage** — in progress via the `/coverage`-driven effect-authoring grind on the composable-primitive substrate (D-24029): new mechanics land as data rows, not engine edits (recent examples: WP-310 / WP-316 / WP-317). Goal unchanged — the `core` set fully playable on play.legendary-arena.com, additional sets incrementally.
3. **Live PvP matchmaking & match-discovery UX** — the resilience half has shipped (WP-116 reconnect policy, WP-309 durable bgio match storage, WP-311 client reconnect/desync auto-resync, WP-312 move-ack watchdog, WP-326/327 stale-match hygiene); what remains is matchmaking plus a match-discovery/join UX.
4. **Gauntlet progress on profiles + champion badges** — backlog, to be drafted and numbered (the non-checkbox backlog bullet in `WORK_INDEX.md`; source D-24131 §8b): owner-profile per-gauntlet checklist + public-profile completed-gauntlet badges via the existing `badges[]` field.
5. **Phase 10 placeholders** — promote a candidate to a real WP only when a concrete production-debugging need motivates it.
6. **WP-042.1** — unblocks when Foundation Prompt 03 is revived.

**Recently completed (2026-07-08..09):** the competitive score-submission arc **WP-332..341** (submission endpoint → seat→account identity → faithful reducer-replay → verifier repoint → turns-native scoring → submit-by-matchId + `GET /api/me/scores` → client submit-after-match + My-Scores → co-owner hardening → session-hydration fix) and the gauntlet leaderboard arc **WP-342/343/344** (set-gauntlet boards server + client, player-count server half), plus **WP-346** (header username is the profile link) and **WP-347** (cross-subdomain Hanko SSO cookie). Per-WP detail lives in the two 2026-07 mindmap clusters above and `WORK_INDEX.md` — not restated here (the dated per-WP ladders this block used to carry drifted a month stale; cluster granularity + a pointer is the durable form).

**Blocked (cannot start):**
- ⏸ WP-042.1 — Deferred PostgreSQL seeding checklists; unblocks when Foundation Prompt 03 (seed runner + migrations) is revived.

**Pending (WP files not yet authored):**
- Gauntlet progress on profiles + champion badges (backlog bullet in `WORK_INDEX.md`; D-24131 §8b)
- Direct-into-lobby challenge prefill (named future WP in WP-345 §Out of scope)

---

## Phase Closure Records

### Phase 6 (Closed 2026-04-19)
- Tag: `phase-6-complete` @ `c376467`
- Engine baseline at close: `604 / 132 / 0`
- Server baseline at close: `124 / 0 / 54`

### Phase 3 Gate
- Closed (D-1320)

---

## WP Disambiguators

- **WP-042 vs WP-042.1** — WP-042 is intentionally scope-reduced per D-4201; the four PostgreSQL seeding checklist sections are partitioned to a sibling sequel WP-042.1 (Governance Drafts). WP-042 is **complete**; WP-042.1 is **blocked** on FP-03 revival. Not a partial undo.
- **WP-128/129/130 vs WP-131 EC slot** — WP-128/129/130 reserved EC-131/132/133 by chronological-tail ordering; WP-131 (next free WP slot) retargets to EC-134 per the locked WP-keyed-EC retarget precedent.
- **WP-207a vs WP-207b** — both backfill the arena client for the new `UIState.notableEvents` projection (WP-200/201): 207a = JSON fixtures, 207b = test backfill. Sequential halves of one client follow-up, not a renumber.
- **arena-client typecheck-drift recurrences (WP-166 → WP-207a/b → WP-227)** — three distinct WPs that each restored arena-client `vue-tsc` green after an engine field-add; each is a separate recurrence of the same pattern, not a re-do of the prior. (WP-207a/b is the 2nd; WP-227 the 3rd.)

---

*Last updated: 2026-07-10 (Project Baselines refresh — the block carried a 2026-06-09 live-run (`2e99369`): six packages, engine `1177`, server `477/0/66`, DECISIONS through D-22801, ECs through EC-260. Re-run at HEAD `5843b7dd` on a fresh worktree (`pnpm install` + `pnpm -r build` + `pnpm --filter <pkg> test`): **all twelve** test-bearing packages now listed (adds legends-board, preplan, lagn, vue-sfc-loader, engine-runner, replay-producer), all green — engine `1872/0/0`, server no-DB `753/0/103` (856 total; full DB-gated `856/0/0` per EC-376), arena-client `809/0/0`, dashboard `409/0/0`, registry-viewer `110/0/0`. Ranges re-derived: `D-0203..D-24139` (870 entries), `EC-001..EC-377`. Dropped the per-row `post-WP-NNN` attributions — suites grow between passes, so HEAD sha + date is the provenance now. Prose-only; `roadmap:counts:check` green.)*

*Prior: 2026-07-09 (Next Unblocked refresh — the section had drifted a month stale: the "Score submission HTTP wiring" item was shipped in full by WP-332..341, "Live PvP matchmaking & reconnect" had its resilience half shipped (WP-116/309/311/312/326/327), and the agent-triage "complete" note plus the 2026-06-07..10 dated per-WP ladders were history duplicating the mindmap/WORK_INDEX. Rewritten: WP-345 (now unblocked — WP-344 executed) leads the ordered list; core-set coverage reframed onto the D-24029 grind; matchmaking item narrowed to what actually remains; the D-24131 §8b profiles/badges backlog item added; Recently-completed collapsed to cluster granularity with a pointer instead of dated ladders; Pending block now names the two known unauthored WPs. Prose-only — no mermaid node or generated-table change; `roadmap:counts:check` green.)*

*Prior: 2026-07-09 (14-WP mindmap orphan backfill — `roadmap:counts:check` failed on `main` naming **WP-332..345** (none had been noded since 2026-06-20; the executing ECs' locked file sets omitted the mindmap). Added two new clusters: **Competitive Score Submission & Verification (2026-07)** (WP-332..341, all ✅) and **Gauntlet Leaderboards (Legends) (2026-07)** (WP-342/343/344 ✅ + WP-345 📝 drafted). Removed the now-shipped "Score submission HTTP wiring" 📦 Next-Horizons placeholder (WP-332/338/339/341 closed that loop). Also fixed a WORK_INDEX parser mis-key: the D-24131 §8b backlog bullet ("Gauntlet progress on profiles") was a checkbox row with no own WP id, so the generator keyed it by its first prose WP mention and falsely flipped **WP-339 → open**; the bullet is now a plain (non-checkbox) list item until drafted and numbered. Generated table: Total **339/341 WP ✅**; open = WP-345, blocked = WP-042.1.)*

*Prior: 2026-06-20 (node-icon hygiene — flipped the **WP-270 📝 → ✅** node to match its WORK_INDEX state: the consumer-half registry-viewer mechanic filter surface shipped via #415, but its EC's locked file set omitted the mindmap so the node stayed Drafted. Pure navigation hygiene; the count table is derived from WORK_INDEX and was already current, so `roadmap:counts --write` produced no table change and `roadmap:counts:check` is green. Prior update: the WP-268/269/270/271 mindmap orphan backfill — see git history.)*

*Prior: 2026-06-12 (WP-241 ✅ done — Dashboard Operator Auth + Bearer Cutover. The dashboard's mock login is replaced with real Hanko auth (mirroring `apps/arena-client`: local-copy `auth/hankoClient.ts` + the WP-160 token store + `<hanko-auth>`), and the three LIVE fetchers attach `Authorization: Bearer` via the shared `services/authToken.ts` seam instead of `credentials:'include'` — superseding D-20601's cookie posture so the client complies with the bearer-only server (D-11202). Added the **WP-241** node to Dashboard & Operator Analytics; the generated count table moved **13 → 14/14**, Total **237/238 WP ✅** (WP-241 was a latent orphan — drafted after WP-240's generator shipped — now noded). D-24003/D-24004/D-24005 Active. Operator cutover (post-merge): set `VITE_HANKO_TENANT_BASE_URL` + `VITE_API_BASE_URL=https://api.legendary-arena.com` + `VITE_USE_MOCKS=false` in CF Pages + redeploy.)*

*Prior: 2026-06-12 (WP-240 ✅ done — Roadmap Count-Table Generator. The Progress Summary count table is now **generated content** bounded by the `ROADMAP-COUNTS` start/end markers, derived by `scripts/roadmap-counts.mjs` from `WORK_INDEX.md` status × mindmap cluster membership (sole writer; hand-edits inside the markers are overwritten by the next run). Added the missing **WP-236** node (Phase 2 — Core Turn Engine) + the **WP-240** node (Domain Cutover & Infrastructure) — both were orphans the loud-fail gate (D-24002) named on the first run. The generated table corrected the WP-238 count drift the prior note flagged: Dashboard & Operator Analytics **12 → 13/13**, Total **WP ✅ 236/237** (= the raw WORK_INDEX checkbox count). Weekly cron `.github/workflows/roadmap-counts.yml` (`'0 6 * * 1'`) PRs the regenerated table on diff. D-24001/D-24002 Active.)*

*Prior: 2026-06-11 (WP-238 ✅ done — Sweep MOCK→LIVE flip. New `sweepLiveFetchers.ts` (`fetchSweepHealthLive`) mirrors the WP-206 analytics live-fetch pattern for the single object-envelope `GET /api/sweep/latest` resource (shared `isLiveModeEnabled()` gate, synchronous cached-`Ref` getter, fail-silent, `credentials:'include'` session parity, `{latest,recentRuns}` object guard); `mocks.ts` gates the existing `fetchSweepHealth` alias via the existing `liveMode` (no second env gate). `SweepHealthWidget.vue`/`PipelinePage.vue` byte-identical; MOCK stays the local-dev/test default. Added a WP-238 ✅ node under Dashboard & Operator Analytics; D-23801/D-23802 Active. Gates: dashboard test 247→274 / 0 fail, `vue-tsc --noEmit` 0, build 0. NOTE: the pre-existing Progress Summary count drift (the counting convention line still reads "WP-231..235 pending" though those nodes show ✅) was left as-is — not introduced or reconciled by this WP.)*

*Prior: 2026-06-10 (WP-235 drafted + revised: the Pipeline page sweep HEALTH trend view (cadence-aware health-rate trends) WP + EC-268 authored, reserving D-23501/D-23502/D-23503. A metric-review pass found the original aggregate anomaly-rate degenerate (`sum(anomalyCounts) === cellCount`) — and the EXISTING health-rate KPI/Architect-lane likewise ≡ 0 on live data; revised to a true health rate (`endgame-reached / cellCount`) via a narrow documented healthy-class-constant exception to D-20703 (D-23503), repairing both degenerate sites. Flipped WP-235 📦 → 📝 in the mindmap + moved it Pending → Drafted; Agent Triage Pipeline open `1 📦 → 1 📝`; Total `1 📦 → 1 📝 + 1 ⏸`. The only remaining Agent Triage Pipeline step, now authored and ready for execution.)*

*Prior: 2026-06-10 (status reconcile: WP-231/232/233/234 ✅ done — the Agent Triage Pipeline's scheduled-triage → handoff-chain → closed-loop-verify sequence plus the parallel-safe full-corpus weekly sweep expansion all landed on `origin/main`. Flipped 📦→✅ in the mindmap + bullet list; Agent Triage Pipeline cluster 1/6 → 5/6 ✅; Progress Summary **226/232 → 230/232 WP ✅**, 5 📦 → 1 📦 (only WP-235 trend view remains), 1 ⏸. Next Unblocked item 4 narrowed to WP-235.)*

*Prior: 2026-06-09 (session add: WP-230 ✅ done — Pipeline page sweep integration; the Pipeline page's agent lanes now consume nightly sweep findings via `useSweepHealth` (Inspector anomalies, Builder fatals, Architect health rate, Evaluator freshness + trend), with priority escalation on real findings. Agent Triage Pipeline cluster now 1/6 ✅; Progress Summary **226/232 WP ✅**, 0 📝, 5 📦, 1 ⏸. Next Unblocked reordered (WP-230 removed; core-set ability coverage now #1).)*

*Prior: 2026-06-09 (session add: WP-229 ✅ folded into Dashboard & Operator Analytics; new **Agent Triage Pipeline** cluster added with WP-230 📝 drafted + WP-231..235 📦 pending — the simulation-sweep-to-agent-lane pipeline. WP-230 wires existing `useSweepHealth` into the Pipeline page; WP-231..233 are sequential triage → handoff → verify; WP-234 full-corpus expansion is parallel-safe; WP-235 trend view depends on WP-230. Progress Summary updated: **225/232 WP ✅**, 1 📝, 5 📦, 1 ⏸. Next Unblocked reordered with WP-230 at #1.)*

*Prior: 2026-06-09 (**FULL reconciliation to `origin/main` HEAD `2e99369`**: folded WP-181..228 plus the pre-existing WP-015A gap into the mindmap — 49 work-packets across **4 new clusters** (Villain & Henchman Effects, Hero Ability Coverage & Markup Pipeline, Notable Events & Overlays, Simulation Sweep & Analytics Pipeline) plus extensions to Phase 4/5, Content Layer, Client Integration, Auth Stack, Registry Viewer Enhancements, Engine & Test-Harness Cleanup, and a renamed/expanded **Dashboard & Operator Analytics**. Rebuilt the Progress Summary to one row per cluster — **225/226 WP ✅, 1 ⏸** (WP-042.1). Re-derived Project Baselines from a live test-run at HEAD (engine 1177, registry 115, registry-viewer 39, server 477/0/66, arena-client 517, dashboard 191) and bumped the DECISIONS range to D-22801 + EC range to EC-260. This supersedes the 2026-06-08 staleness flag below — the mindmap is now current to HEAD.)*

*Prior: 2026-06-08 (session add: WP-227 ✅ arena-client vue-tsc green — WP-214/222 UIState/UICityCard fixture + prop backfill, the 3rd recurrence of the engine-field-add → client-typecheck-drift pattern after WP-166/207; WP-225 ✅ hero draw markup noted under Recently Completed. **Staleness flag:** this was a targeted single-session add, NOT a full catchup. The mindmap is still behind `origin/main` — the last full reconciliation was WP-180 (2026-05-26); WP-181..226 are not yet folded into the mindmap, and the Progress Summary counts (181/195) + Project Baselines remain frozen at the WP-180 state. A full catchup is a separate pass.)*

*Prior: 2026-05-26 (roadmap catchup: added 25 missing WPs to mindmap — WP-086/096/116-119/153-158/162/164/167-173/175/178-180; added Next Horizons section with 3 forward-looking strategic directions (card keyword coverage, live PvP reconnect, score submission wiring); trimmed Recently Completed to one-liners per checklist rule; total 181/195 ✅.)*
