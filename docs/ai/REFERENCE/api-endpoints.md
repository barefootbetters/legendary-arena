# HTTP API Endpoints — Authoritative Catalog

> Authoritative catalog of every HTTP endpoint exposed (or coded but not yet
> exposed) by `apps/server`, plus every library function reachable via direct
> import from `apps/server/src/**` that future request-handler WPs may
> graduate to an HTTP route. Created by
> [WP-118](../work-packets/WP-118-http-api-surface-catalog.md) (single `SPEC:`
> commit, 2026-04-30) per `D-11801`..`D-11804` in
> [`DECISIONS.md`](../DECISIONS.md).

**Primary audience.** Future Work Packet authors first; client developers may
read this directly until OpenAPI tooling lands; ops should consult
[`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs) for
ground truth on what is currently registered with the boardgame.io Koa
router. The catalog is descriptive, not prescriptive — it records the
shipped, deferred, library-only, and pending HTTP surface as of catalog
write time and is updated by every endpoint-touching WP per the D-11804
update obligation.

---

## Status Taxonomy (Closed Set)

Every catalog row carries exactly one of the following four `Status` values.
Any other string is FAIL under both lint §21 and the
[`.claude/rules/work-packets.md`](../../../.claude/rules/work-packets.md)
catalog-update rule.

| Status | Meaning |
|---|---|
| `Wired` | Route registered in `apps/server/src/server.mjs` (or via boardgame.io built-ins surfaced by `Server({games:[LegendaryGame]})`) and reachable over HTTP today. |
| `Shipped-but-unwired` | Handler code exists in `apps/server/src/**` and exports a `register*Routes(...)`-style function, but route registration is deferred per a `DECISIONS.md` entry (e.g., `D-10202` for the WP-102 profile route awaiting long-lived `pg.Pool`). |
| `Library-only` | Function exists in `apps/server/src/**` with no HTTP surface planned in the originating WP. May graduate to `Wired` via a future request-handler WP, or may remain library-only by design (e.g., WP-103 replay helpers — WP-103 §Out of Scope explicitly forbids HTTP exposure). Each `Library-only` row cites either the originating WP's intentional choice or the deferral decision. |
| `Pending` | Drafted in a future WP that has not yet executed (e.g., WP-115 leaderboard endpoints). The row names both the drafting WP and its current state at catalog-write time, formatted `Pending: WP-NNN (STATE YYYY-MM-DD)`. When the drafting WP executes, its commit replaces these rows per D-11804 replace-whole-row merge semantics. |

---

## Auth Taxonomy (Closed Set per D-9905, extended by D-15901, reduced by D-17602)

Every catalog row's `Auth` column carries exactly one of the following four
values. Any other string is FAIL under lint §21 closed-set verification.

| Auth | Meaning |
|---|---|
| `guest` | Endpoint accepts unauthenticated requests; no session token required. The current shipped surface (boardgame.io built-ins, `/health`, the deferred profile route) is `guest`. |
| `handle-required` | Endpoint requires a claimed handle (per WP-101) on the requesting account; session validation is performed by the future `requireAuthenticatedSession` middleware (WP-112 deferral). No endpoint uses this value today. |
| `authenticated-session-required` | Endpoint requires a validated Hanko session (per D-9905) on the requesting account; session validation is performed by `requireAuthenticatedSession` (WP-112). |
| `admin-session-required` | Server-validated Hanko session + `is_admin = TRUE` on the resolved `legendary.players` row. See WP-159 §A. Introduced by WP-159 (D-15901, D-15902) as the per-user-attributable successor to the retired `admin-secret` value (D-17602). Callers: WP-107 (admin profile routes), WP-176 (admin billing route). |

---

## Catalog Format (per D-11801 = A)

Each endpoint group below is a Markdown table with the following columns,
in this order: `Status`, `Method`, `Path`, `Auth`, `Request Schema (file
ref)`, `Response Schema (file ref)`, `Authorizing WP`, `Notes`. Canonical
field-name spellings (`accountId`, `handle`, `matchId`, `replayHash`, etc.)
match [`docs/ai/REFERENCE/00.2-data-requirements.md`](00.2-data-requirements.md)
exactly; any divergence is FAIL under lint §21 canonical-field-name
verification.

---

## Error Contract (per D-11802 = C)

The catalog records two distinct error-response contracts, one per
ownership domain:

1. **boardgame.io built-in lobby endpoints** — error semantics are owned by
   the upstream `boardgame.io` framework. The catalog documents them
   descriptively only and never reshapes them; any divergence is recorded
   as a `Drift:` annotation, not normalized in WP-118.
2. **Project-owned handlers** — error responses use the project-specific
   shape `{ code: string, message: string, requestId?: string }`. The
   `requestId` field is `conditional-on-server-trace-injection`: present
   on every project-owned endpoint once a future request-handler WP lands
   request-ID middleware; absent until then; never both present-on-some
   and absent-on-others within the same release.

---

## Versioning Policy (per D-11803 = B)

Endpoints live at their natural path with no `/v1/` prefix and no
header-based content negotiation. The catalog itself is the contract: every
consumer reads the relevant catalog row to learn the current shape.
Breaking changes require a coordinated client + server release plus a
`Drift:` annotation in the affected catalog row plus a corresponding
`DECISIONS.md` entry justifying the break.

---

## Update Obligation (per D-11804 = C)

Every Work Packet that adds, modifies, removes, or changes the status of an
HTTP endpoint or a library function reachable via direct import from
`apps/server/src/**` MUST update this catalog in the same commit, replacing
the **entire** affected row (no partial-column updates). Status transitions
(e.g., a `Pending` placeholder row → `Wired` when the drafting WP executes)
are full-row replacements, never field-level edits. Enforcement is
duplicated across:

- [`docs/ai/REFERENCE/00.3-prompt-lint-checklist.md §21`](00.3-prompt-lint-checklist.md) — catches at WP-draft time during the Lint Gate.
- [`.claude/rules/work-packets.md`](../../../.claude/rules/work-packets.md) — catches during execution.

Both gates encode the replace-whole-row constraint per D-11804 merge
semantics. A future API-touching WP that slips past one gate is still
caught by the other.

---

## `Wired` — Reachable Over HTTP Today

### Server-Registered Routes

| Status | Method | Path | Auth | Request Schema (file ref) | Response Schema (file ref) | Authorizing WP | Notes |
|---|---|---|---|---|---|---|---|
| `Wired` | `GET` | `/health` | `guest` | (none — empty body) | `{ status: 'ok' }` literal at [`apps/server/src/server.mjs:31-33`](../../../apps/server/src/server.mjs) | WP-004 (server bootstrap) | Render health-check + `pnpm check` target. Registered via `registerHealthRoute(server.router)` at [`apps/server/src/server.mjs:120`](../../../apps/server/src/server.mjs); definition at [`apps/server/src/server.mjs:30-34`](../../../apps/server/src/server.mjs). |
| `Wired` | `GET` | `/api/version` | `guest` | (none — empty body) | `{ version: string, gitSha: string, buildTimestamp: string }` literal at [`apps/server/src/version.mjs`](../../../apps/server/src/version.mjs) | WP-180 (build-time version stamping) | Read-only deployment-freshness diagnostics. Values cached at process start (no per-request recompute). `buildTimestamp` is process-start time per D-18001 (NOT Vite build time). `gitSha` is 7-char short SHA via `execSync('git rev-parse --short HEAD')` with fallback to `'unknown'`. Registered inline in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs). No `Cache-Control` header (endpoint is informational, caching is acceptable). Status-code domain locked to `{200}` for this handler. |
| `Wired` | `GET` | `/health/legends-publisher` | `guest` | (none — empty body) | `LegendsPublisherHealthState` `{ status: 'ok' \| 'error' \| 'idle', lastSuccessAt: string \| null, lastErrorAt: string \| null, lastErrorMessage: string \| null, intervalMs: number }` per WP-142 contract at [`apps/server/src/legends/legends.types.ts`](../../../apps/server/src/legends/legends.types.ts) | WP-142 (legends snapshot publisher) | Legends publisher operational health. Returns the publisher's last-success/last-error timestamps and current status. No auth — returns operational metrics only (timestamps + status), no PII, no game state, no user data. Identical no-auth posture to `GET /health`. Registered via `registerLegendsPublisherRoutes(server.router)` in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs). Status-code domain locked to `{200}` for this handler. |
| `Wired` | `GET` | `/api/leaderboards/scenarios` | `guest` | (none — empty body) | `{ scenarioKeys: string[] }` per [`apps/server/src/leaderboards/leaderboard.routes.ts`](../../../apps/server/src/leaderboards/leaderboard.routes.ts); on uncaught error returns `500` with body `{ "error": "internal_error" }` (project-owned envelope per D-11802 = C; literal shape locked at WP-115 v1.0 §Locked contract values) | WP-115 (public leaderboard endpoints) | Discoverability seam — returns the deduplicated set of `scenarioKey` values present in `legendary.competitive_scores` so clients can enumerate available leaderboards without prior registry knowledge. Wraps WP-054 `listScenarioKeys(database)` at [`apps/server/src/leaderboards/leaderboard.logic.ts:281`](../../../apps/server/src/leaderboards/leaderboard.logic.ts). Registered via `registerLeaderboardRoutes(server.router, pool, parGate)` in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs). `Cache-Control: no-store` set as the first statement of every response (including 500 error paths per WP-115 v1.1 Patch 8). Status-code domain locked to `{200, 500}` for this handler. |
| `Wired` | `GET` | `/api/leaderboards/scenarios/:scenarioKey` | `guest` | `:scenarioKey` path param (required, non-empty); query params `limit` (default 25, range 1–100, integer) and `offset` (default 0, range 0–10000, integer); duplicate query params (Koa `string[]`) accepted only for arrays of length 1 per WP-115 v1.1 Patch 5 | `ScenarioLeaderboard` `{ scenarioKey, entries: PublicLeaderboardEntry[], totalEligibleEntries }` per WP-054 contract at [`apps/server/src/leaderboards/leaderboard.types.ts:107-111`](../../../apps/server/src/leaderboards/leaderboard.types.ts); on missing path param or malformed `limit` / `offset` returns `400` with body `{ "error": "invalid_query", "message": <sentence> }` (per WP-115 v1.1 Patches 4 / 5); on uncaught error returns `500` with body `{ "error": "internal_error" }` (project-owned envelope per D-11802 = C; literal shapes locked at WP-115 v1.0 §Locked contract values) | WP-115 (public leaderboard endpoints) | Per-scenario score list with PAR fail-closed semantics inherited from WP-054. Wraps `getScenarioLeaderboard(options, database, deps)` at [`apps/server/src/leaderboards/leaderboard.logic.ts:151`](../../../apps/server/src/leaderboards/leaderboard.logic.ts) with explicit `deps: { checkParPublished: parGate.checkParPublished }` injection at every call site (WP-054 `PRODUCTION_DEPENDENCIES` default fail-closes scenarios to empty by design). Empty leaderboard (no PAR or no eligible scores) returns `200` with `entries: []`, never `404`. `Cache-Control: no-store` set as the first statement of every response (including 400 / 500 error paths per WP-115 v1.1 Patch 8). Status-code domain locked to `{200, 400, 500}` for this handler. Sensitive fields (`accountId`, `submissionId`, `email`, `authProvider`, `authProviderId`, `stateHash`, `scoreBreakdown`) are stripped at the WP-054 type boundary (`PublicLeaderboardEntry`) per D-5201 and never re-introduced at the route layer. |
| `Wired` | `GET` | `/api/leaderboards/scores/:replayHash` | `guest` | `:replayHash` path param (required, non-empty; verbatim spelling per `00.2-data-requirements.md`) | `PublicLeaderboardEntry` per WP-054 contract at [`apps/server/src/leaderboards/leaderboard.types.ts:84-94`](../../../apps/server/src/leaderboards/leaderboard.types.ts); on miss returns `404` with body `{ "error": "score_not_found" }`; on missing path param returns `400` with body `{ "error": "invalid_query", "message": "Replay hash is required." }`; on uncaught error returns `500` with body `{ "error": "internal_error" }` (project-owned envelope per D-11802 = C; literal shapes locked at WP-115 v1.0 §Locked contract values) | WP-115 (public leaderboard endpoints) | Single-score detail via cryptographic replay-hash permalink. Wraps `getPublicScoreByReplayHash(replayHash, database)` at [`apps/server/src/leaderboards/leaderboard.logic.ts:238`](../../../apps/server/src/leaderboards/leaderboard.logic.ts); WP-054 returns `null` on miss, this route translates to `404` with the locked envelope. Visibility filter (`visibility IN ('public', 'link')`) inherited from WP-054 — private replays never surface. Returned entry's `rank` field is `0` per WP-054 single-record sentinel (callers wanting rank context call `getScenarioLeaderboard` instead). `Cache-Control: no-store` set as the first statement of every response (including 400 / 404 / 500 error paths per WP-115 v1.1 Patch 8). Status-code domain locked to `{200, 400, 404, 500}` for this handler. |
| `Wired` | `GET` | `/api/leaderboards/themes/:themeId` | `guest` | `:themeId` path param (required, non-empty; kebab-case per `theme.schema.ts:108` regex `^[a-z0-9]+(-[a-z0-9]+)*$`); query params `limit` (default 25, range 1–100, integer) and `offset` (default 0, range 0–10000, integer); duplicate query params (Koa `string[]`) accepted only for arrays of length 1 per WP-115 v1.1 Patch 5 | `ThemeLeaderboard` `{ themeId, entries: PublicLeaderboardEntry[], totalEligibleEntries }` per WP-150 contract at [`apps/server/src/leaderboards/leaderboard.types.ts`](../../../apps/server/src/leaderboards/leaderboard.types.ts); entry shape is `PublicLeaderboardEntry` reused verbatim (9 fields per D-5201); on unknown themeId returns `404` with body `{ "error": "theme_not_found" }` (new envelope value introduced by WP-150); on missing path param or malformed `limit` / `offset` returns `400` with body `{ "error": "invalid_query", "message": <sentence> }`; on uncaught error returns `500` with body `{ "error": "internal_error" }` (project-owned envelope per D-11802 = C) | WP-150 (leaderboard theme + global aggregation endpoints) | Theme-grouped leaderboard across every scenario that belongs to a given `themeId` per WP-055 `ThemeDefinition.themeId`. Wraps `getThemeLeaderboard(options, database, deps)` at [`apps/server/src/leaderboards/leaderboard.logic.ts`](../../../apps/server/src/leaderboards/leaderboard.logic.ts) with explicit `deps: { checkParPublished, getScenarioKeysForTheme }` injection at every call site (D-15002 — single deps bundle). The themeId → scenarioKey[] mapping is built at server startup in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs) from `content/themes/*.json` using `validateThemeFile` (registry export) + the engine's `buildScenarioKey` helper (D-15001) — the leaderboards module never imports the registry directly (layer boundary preserved). PAR-eligibility filter applied per scenario via `deps.checkParPublished` (D-5306 fail-closed); a known theme whose scenarios have no PAR-published scores surfaces `200` with `entries: []` and `totalEligibleEntries: 0`, NOT 404. Sort order byte-identical to `getScenarioLeaderboard` (`final_score ASC, created_at ASC`). `Cache-Control: no-store` set as the first statement of every response (including 400 / 404 / 500 error paths per WP-115 v1.1 Patch 8). Status-code domain locked to `{200, 400, 404, 500}` for this handler. Sensitive fields (`accountId`, `submissionId`, `email`, `authProvider`, `authProviderId`, `stateHash`, `scoreBreakdown`) are stripped at the WP-054 type boundary per D-5201 and never re-introduced. |
| `Wired` | `GET` | `/api/leaderboards/top` | `guest` | Query params `limit` (default 25, range 1–100, integer) and `offset` (default 0, range 0–10000, integer); duplicate query params (Koa `string[]`) accepted only for arrays of length 1 per WP-115 v1.1 Patch 5 | `GlobalTopLeaderboard` `{ entries: PublicLeaderboardEntry[], totalEligibleEntries }` per WP-150 contract at [`apps/server/src/leaderboards/leaderboard.types.ts`](../../../apps/server/src/leaderboards/leaderboard.types.ts); entry shape is `PublicLeaderboardEntry` reused verbatim (9 fields per D-5201); on malformed `limit` / `offset` returns `400` with body `{ "error": "invalid_query", "message": <sentence> }`; on uncaught error returns `500` with body `{ "error": "internal_error" }` (project-owned envelope per D-11802 = C) | WP-150 (leaderboard theme + global aggregation endpoints) | Global Top-N leaderboard across every PAR-published scenario (lowest `final_score` first). Wraps `getGlobalTopLeaderboard(options, database, deps)` at [`apps/server/src/leaderboards/leaderboard.logic.ts`](../../../apps/server/src/leaderboards/leaderboard.logic.ts). PAR-eligibility derivation per D-15003: `listScenarioKeys(database)` → filter via `deps.checkParPublished` → `cs.scenario_key = ANY($1)` SELECT filter. No 404 surface — when no PAR-published scenarios exist, the route returns `200` with `entries: []` and `totalEligibleEntries: 0`. Sort order byte-identical to `getScenarioLeaderboard` (`final_score ASC, created_at ASC`). Each entry's `scenarioKey` field is the per-row disambiguator the UI uses to group / filter. `Cache-Control: no-store` set as the first statement of every response (including 400 / 500 error paths per WP-115 v1.1 Patch 8). Status-code domain locked to `{200, 400, 500}` for this handler. Sensitive fields stripped at the WP-054 type boundary per D-5201. |
| `Wired` | `GET` | `/api/me/profile` | `authenticated-session-required` | (none — empty body) | `OwnerProfileView` `{ accountId, displayName, handleCanonical, avatarUrl, aboutMe, avatarVisibility, aboutMeVisibility, linksVisibility, friendRequestEmails, links, updatedAt, teamAffiliations, badges }` (13 keys) per WP-104 + WP-109 + WP-105 + WP-305 + WP-357 contracts at [`apps/server/src/profile/ownerProfile.types.ts`](../../../apps/server/src/profile/ownerProfile.types.ts). WP-305 / D-24089 added the owner's own identity: `accountId` (opaque `ext_id` UUID, always present), `displayName` (`legendary.players.display_name`, NOT NULL), `handleCanonical` (`legendary.players.handle_canonical`, lowercased canonical form, `null` before the handle is claimed — same wire name + form as `PublicProfileView`). On auth failure returns `401` with body `{ "error": "<SessionValidationErrorCode>" }` for `'missing_token'` / `'invalid_token'` / `'expired_token'` / `'unknown_account'` (closed-set per WP-112) — `'unknown_account'` is **401, not 403**, per the account-existence-probe defense locked in WP-104; on `'lookup_failed'` returns `500` with body `{ "error": "lookup_failed" }` | WP-104 (owner profile read); WP-305 (identity fields) | Owner self-service read of the authenticated owner's own profile. Wraps `getOwnerProfile(accountId, database)` at [`apps/server/src/profile/ownerProfile.logic.ts`](../../../apps/server/src/profile/ownerProfile.logic.ts). The identity fields (`accountId` / `displayName` / `handleCanonical`) resolve from the always-present `legendary.players` row and surface on every return path including the synthesized default (WP-305 / D-24089). Read-no-mutate invariant: when no `legendary.player_profiles` row exists, the helper synthesizes the most-private default profile view (per D-10403 + Vision §3 fail-closed posture) and no INSERT fires on the read path; the first PATCH is the sole row-creation site via the `INSERT ... ON CONFLICT (player_id) DO UPDATE` upsert pattern. `Cache-Control: no-store` set as the first statement of every response (including error paths per WP-115 D-11504 lock). Registered via `registerOwnerProfileRoutes(server.router, pool, { requireAuthenticatedSession, verifier, accountResolver })` in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs). Genuinely authenticated as of WP-131 / EC-134. Production wiring constructs the Hanko verifier at startup from `HANKO_TENANT_BASE_URL` + `HANKO_EXPECTED_AUDIENCE`. |
| `Wired` | `PATCH` | `/api/me/profile` | `authenticated-session-required` | Sparse partial JSON body per D-10406 RFC 7396 — every field optional; key absence leaves unchanged; explicit `null` clears the nullable field; the literal four-character string `"null"` is treated as the literal string. Recognized fields: `displayName` (string; trimmed 1–64, no control chars; NOT nullable — cannot be cleared; WP-305 / D-24090), `avatarUrl` (string \| null), `aboutMe` (string \| null), `avatarVisibility` ('private' \| 'public'), `aboutMeVisibility` ('private' \| 'public'), `linksVisibility` ('private' \| 'public'), `friendRequestEmails` (boolean; NOT nullable — `true` to receive friend-request emails, `false` to opt out; WP-357 / D-24149). `handleCanonical` and `accountId` are NOT accepted (immutable / non-editable). | Updated `OwnerProfileView` per WP-104 + WP-305 + WP-357 contract (13 keys; the returned `displayName` is the POST-write value on a rename); on validator failure returns `400` with body `{ "error": "invalid_request" }` / `{ "error": "invalid_avatar_url" }` / `{ "error": "invalid_display_name" }` (closed-set `OwnerProfileErrorCode` per WP-104 + WP-305); on auth failure returns `401` / `500` per the locked `SessionValidationErrorCode` mapping table (`'unknown_account'` is **401, not 403**); on uncaught error returns `500` with body `{ "error": "internal_error" }` | WP-104 (owner profile edit); WP-305 (editable display name) | Owner self-service edit of the authenticated owner's own profile. Wraps `upsertOwnerProfile(accountId, patch, database)` at [`apps/server/src/profile/ownerProfile.logic.ts`](../../../apps/server/src/profile/ownerProfile.logic.ts). Three-state input discrimination via `Object.hasOwn` per the WP-104 locked pattern (key absent vs. value `null` vs. value string); inline ternaries returning `T \| undefined` are forbidden under `exactOptionalPropertyTypes`. WP-305 / D-24090: a present `displayName` is validated against the identity-layer rules (mirrors `identity.logic.ts`; new code `invalid_display_name` → 400) and written to `legendary.players.display_name` via `UPDATE ... RETURNING display_name` inside ONE `BEGIN/COMMIT` transaction with the `player_profiles` upsert — both writes land or neither (a SQL fault `ROLLBACK`s and surfaces as `500 { error: 'internal_error' }` via `Promise.reject`); the response reflects the post-write name. Absent `displayName` → no `legendary.players` write. The `player_profiles` upsert remains `INSERT ... ON CONFLICT (player_id) DO UPDATE`; the first PATCH on a never-edited account creates the row, subsequent PATCHes update it. `updated_at` advances on every successful PATCH. No companion `PUT /api/me/profile` (locked under WP-104 D-10406 — would invite full-row nulling on partial submissions). `Cache-Control: no-store` set as the first statement of every response. Genuinely authenticated as of WP-131 / EC-134. Production wiring constructs the Hanko verifier at startup from `HANKO_TENANT_BASE_URL` + `HANKO_EXPECTED_AUDIENCE`. |
| `Wired` | `PUT` | `/api/me/links` | `authenticated-session-required` | Full-replace JSON body per D-10407 — `{ links: OwnerProfileLink[] }`. Each `OwnerProfileLink` is `{ provider, url, isPublic, displayOrder }`. `provider` is a closed-set value from the 6-entry allowlist `'twitter' \| 'github' \| 'twitch' \| 'discord' \| 'youtube' \| 'website'` (D-10404). `url` is HTTPS-only any-host (D-10405) ≤2048 chars. `isPublic` is boolean. `displayOrder` is supplied for symmetry with the read shape but ignored on the write path — the server uses the array index (0-based) as the authoritative `display_order` value. Maximum 10 links per account; over-cap returns `400` with `{ "error": "too_many_links" }`. | Updated `OwnerProfileView` per WP-104 contract; on validator failure returns `400` with body `{ "error": "invalid_request" }` / `{ "error": "invalid_link_url" }` / `{ "error": "too_many_links" }` (closed-set `OwnerProfileErrorCode` per WP-104); on auth failure returns `401` / `500` per the locked `SessionValidationErrorCode` mapping table | WP-104 (owner links replace) | Owner self-service replace of the full link list. Wraps `replaceOwnerLinks(accountId, links, database)` at [`apps/server/src/profile/ownerProfile.logic.ts`](../../../apps/server/src/profile/ownerProfile.logic.ts). Single PostgreSQL transaction (`BEGIN; DELETE; INSERT...; COMMIT;`) so partial state is never visible to a concurrent reader. `display_order` is the loop index, preserving the order the client sent. The read-back path applies `ORDER BY display_order ASC, link_id ASC` so `OwnerProfileView.links` is always returned ASC-sorted by `displayOrder` (the WP-104 ordering invariant lock — clients MUST NOT defensively re-sort). `Cache-Control: no-store` set as the first statement of every response. Genuinely authenticated as of WP-131 / EC-134. Production wiring constructs the Hanko verifier at startup from `HANKO_TENANT_BASE_URL` + `HANKO_EXPECTED_AUDIENCE`. |
| `Wired` | `POST` | `/api/me/avatar` | `authenticated-session-required` | `multipart/form-data` with single file field `avatar`. Multer limits: `fileSize: 5 * 1024 * 1024` (5 MB), `files: 1`, `fields: 0`. Accepted MIME types (validated via magic-byte sniffing, NOT Content-Type header): `image/jpeg`, `image/png`, `image/webp` (closed set per D-10601). | `200 → { avatarUrl: string }` — the new CDN URL (`https://images.legendary-arena.com/avatars/{accountId}.webp`). Error responses use D-11802 shape `{ code, message }`: `400 { code: 'invalid_mime_type' }` — upload is not jpeg/png/webp per magic-byte sniff; `400 { code: 'file_too_large' }` — exceeds 5 MB or 20 megapixels; `429 { code: 'rate_limited' }` — user exceeded 1 upload per 60 seconds; `500 { code: 'upload_failed' }` — R2 PUT or DB update failed; `401 { code: 'unauthorized' }` — session validation failed. | WP-106 (avatar upload pipeline) | Server-side avatar upload: validates MIME via magic bytes (not multer mimetype), checks in-memory rate limit (1 per 60s per user keyed by accountId), processes via `sharp(buffer, { limitInputPixels: 20_000_000 }).rotate().resize(256, 256, { fit: 'cover', position: 'centre' }).webp({ quality: 80 })` (EXIF stripped by NOT calling `.withMetadata()`), PUTs to R2 at `avatars/{accountId}.webp` with `Content-Type: image/webp` + `Cache-Control: public, max-age=300`, then UPDATEs `legendary.player_profiles.avatar_url`. Compensating delete: if DB UPDATE fails after R2 PUT succeeds, the R2 object is DELETEd (if compensating DELETE also fails, logs structured error and still returns 500). Side effect: updates `avatar_url` column to the canonical CDN URL. Supersedes D-10405 for `avatar_url` only — `PATCH /api/me/profile` now enforces closed-origin allowlist per D-10601 (only the user's own canonical R2 URL accepted). `Cache-Control: no-store` set as the first statement of every response. Registered via `registerAvatarUploadRoutes(server.router, pool, deps)` in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs). Genuinely authenticated as of WP-131 / EC-134. Avatar CDN host unified to `images.legendary-arena.com` per D-24083 / WP-296 — same `legendary-images` R2 bucket and Cloudflare custom domain as card images; `AVATAR_CDN_BASE` and the closed-origin `validateAvatarUrl` allowlist both target the new host, and migration 021 rewrote existing `avatar_url` rows. |
| `Wired` | `POST` | `/api/me/loadouts` | `authenticated-session-required` | JSON body `{ name, lagn }`. `name` is a 1–80-char string (trimmed before validation and persisted trimmed). `lagn` is a LAGN document validated server-side via `@legendary-arena/lagn` `validate`. **Accepted versions: `1.0.0`, `1.1.0`, `1.2.0`, `1.3.0`, `1.4.0`** (`LAGN_SUPPORTED_VERSIONS`). **Narrowed by WP-394 / D-24198:** a pre-`1.2.0` body carrying a provenance block (`catalog_ref`, `registry_ref`, or `effect_snapshot`) is **rejected** with `400 { "error": "invalid_lagn" }` where it previously returned `201` — the schema does not call `.strict()`, so such a key used to be silently stripped and the document accepted; provenance on a `1.2.0`+ body is accepted and persisted verbatim. **Further narrowed by WP-402 / D-24210 / D-24211:** a pre-`1.3.0` body carrying `setup.hero_alternates` (the bench of reserve heroes) is likewise **rejected** with `400 { "error": "invalid_lagn" }` where it previously returned `201`, for the same silent-strip reason; a bench on a `1.3.0` body is accepted and persisted verbatim. **Further narrowed by WP-405 / D-24214 / D-24215:** a pre-`1.4.0` body carrying the root `players[]` (match participants) or `scoring_profile` (a descriptive label) is likewise **rejected** with `400 { "error": "invalid_lagn" }` where it previously returned `201`, for the same silent-strip reason; both blocks on a `1.4.0` body are accepted and persisted verbatim. (A saved loadout would never legitimately carry `players[]`, but the endpoint validates the whole LAGN, so the acceptance envelope still moves.) Both blocks are **descriptive and non-authoritative** — never a scoring, credit, ranking, or verification input (D-24214 / D-24215). This endpoint persists the client's document verbatim and never re-stamps its version; the server's own LAGN write value (`LAGN_VERSION` at [`packages/lagn-spec`](../../../packages/lagn-spec)) is `1.4.0` as of WP-406. | `201 → SavedLoadoutView` `{ id, name, visibility, shareSlug, createdAt, updatedAt, lagn }` per WP-301 contract at [`apps/server/src/profile/loadoutLibrary.types.ts`](../../../apps/server/src/profile/loadoutLibrary.types.ts); on validation failure returns `400` with body `{ "error": "invalid_name" }` / `{ "error": "invalid_lagn" }`; over the 50-per-account cap returns `409` with `{ "error": "loadout_limit_reached" }`; on auth failure returns `401` with `{ "error": "unauthorized" }`; on uncaught error `500` with `{ "error": "internal_error" }` (closed-set `LoadoutLibraryErrorCode` per WP-301) | WP-301 (profile loadout library) | Create a saved loadout scoped to the caller's account. Wraps `createLoadout(accountId, input, database)` at [`apps/server/src/profile/loadoutLibrary.logic.ts`](../../../apps/server/src/profile/loadoutLibrary.logic.ts). Server-side LAGN validation is mandatory (client validation is never the gate). New rows are `private` with `share_slug = null` until the owner makes them public. Per-account cap `MAX_SAVED_LOADOUTS_PER_ACCOUNT = 50` (free-tier quota; premium-unlimited is a future hook). Saved loadouts are decorative (Vision §19b) — never a competitive-submission path. `Cache-Control: no-store` set as the first statement of every response. Registered via `registerLoadoutLibraryRoutes(server.router, pool, { requireAuthenticatedSession, verifier, accountResolver })` in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs). |
| `Wired` | `GET` | `/api/me/loadouts` | `authenticated-session-required` | (none — empty body; bearer session token via Authorization header per WP-112 contract) | `200 → { loadouts: SavedLoadoutView[] }` ordered `updated_at DESC` (most-recently-touched first); each element carries the full `lagn` document (no summary projection); on auth failure `401 { "error": "unauthorized" }`; on uncaught error `500 { "error": "internal_error" }` | WP-301 (profile loadout library) | List the caller's own saved loadouts. Wraps `listLoadouts(accountId, database)`. Scoped by the resolved `player_id` (cross-account isolation). Returns each row's full `lagn` so the client can render "view as cards" without a second fetch; bounded by the 50-cap. `Cache-Control: no-store` set as the first statement of every response. |
| `Wired` | `PATCH` | `/api/me/loadouts/:id` | `authenticated-session-required` | `:id` path param (loadout UUID). JSON body `{ name?, visibility? }` — both optional. `name` is a 1–80-char string (trimmed). `visibility` is `'private' \| 'public'`. A body with neither field is rejected `empty_update`. | `200 → SavedLoadoutView` (updated); a no-field PATCH returns `400 { "error": "empty_update" }`; a malformed `visibility` value returns `400 { "error": "invalid_request" }`; an invalid `name` returns `400 { "error": "invalid_name" }`; a malformed / cross-account / missing `:id` returns `404 { "error": "not_found" }` (no existence leak); on auth failure `401`; on uncaught error `500 { "error": "internal_error" }` | WP-301 (profile loadout library) | Rename and/or toggle visibility of one of the caller's loadouts. Wraps `updateLoadout(accountId, id, patch, database)`. Locked visibility→slug transitions: `private→public` mints an opaque `share_slug` (`crypto.randomBytes(16)` → `base64url`, ≥128 bits, 22 chars, collision-retried), `public→public` preserves the existing slug (so already-shared links keep working), `public→private` clears it. Any real change sets `updated_at = now()`. Scoped by the resolved `player_id`. `Cache-Control: no-store` set as the first statement of every response. |
| `Wired` | `DELETE` | `/api/me/loadouts/:id` | `authenticated-session-required` | `:id` path param (loadout UUID). | `204` (no body) on success; a malformed / cross-account / missing `:id` returns `404 { "error": "not_found" }` (no existence leak); on auth failure `401`; on uncaught error `500 { "error": "internal_error" }` | WP-301 (profile loadout library) | Delete one of the caller's loadouts by id. Wraps `deleteLoadout(accountId, id, database)`. Scoped by the resolved `player_id` — account B deleting account A's id gets `404`. `Cache-Control: no-store` set as the first statement of every response. |
| `Wired` | `GET` | `/api/loadouts/:shareSlug` | `guest` | `:shareSlug` path param (opaque server-minted base64url slug). | `200 → { name, lagn, displayHandle }` for a **public** loadout only (allowlisted projection); a missing slug or a private loadout returns `404 { "error": "not_found" }` — the body NEVER carries `accountId` / `ext_id` and NEVER a private loadout; on uncaught error `500 { "error": "internal_error" }` | WP-301 (profile loadout library) | Guest read of a public shared loadout. No auth. Wraps `getPublicLoadoutBySlug(shareSlug, database)`. The `visibility = 'public'` filter plus the null-slug-on-private invariant guarantee a private loadout is unreachable via this endpoint; `displayHandle` falls back to the owner's `display_name` when no handle is claimed (mirrors the public-profile fallback). `Cache-Control: no-store` set as the first statement of every response (a public loadout can be flipped private, so a cached copy must never keep serving it). |
| `Wired` | `POST` | `/api/me/gauntlet-runs` | `authenticated-session-required` | JSON body — the entire body is an untrusted WP-440 gauntlet identity pack `{ pack_version, gauntlet: { setAbbr, mastermindSlug, division, playerCount } }` validated server-side via `@legendary-arena/registry` `validateGauntletPack` (`./gauntletPack` subpath). `division` is `'fixed' \| 'open'`; `playerCount` is `1..5`. | `201 → GauntletRunView` `{ id, setAbbr, mastermindSlug, division, playerCount, legPicks, createdAt, updatedAt, firstCompletedAt }` per WP-445 contract at [`apps/server/src/gauntlet/gauntletRun.types.ts`](../../../apps/server/src/gauntlet/gauntletRun.types.ts) (new active run, `legPicks = {}`); **`200 → GauntletRunView`** for the **idempotent attach** to an existing active run of the same identity (D-24264 — the migration-039 partial-unique conflict is caught and resolved to the existing run, NEVER a 409/500); on invalid pack shape returns `400 { "error": "invalid_pack" }`; on a gauntlet / player-count with no approved loadout menu returns `422 { "error": "unknown_gauntlet" }`; on a suspended account returns `403 { "error": "account_suspended" }`; on auth failure returns `401 { "error": "unauthorized" }`; on uncaught error `500 { "error": "internal_error" }` (closed-set `GauntletRunErrorCode` per WP-445) | WP-445 (gauntlet run import + run-CRUD API) | Import a WP-440 identity pack as an account-local gauntlet run. Sixth WP of the Mastermind Gauntlets: download → import → build → track epic. Auth chain `requireAuthenticatedSession` → `requireUnsuspendedAccount`. Wraps `importGauntletRun(accountId, input, database, resolveGauntletExistence)` at [`apps/server/src/gauntlet/gauntletRun.logic.ts`](../../../apps/server/src/gauntlet/gauntletRun.logic.ts). Gauntlet existence is INJECTED (D-24264) via a resolver built from the startup `gauntletCatalog` — the logic layer never imports the registry catalog. INSERTs an active run with empty `leg_picks`; a re-import of the same active identity returns the existing run (`200`). NEVER writes `first_completed_at` (every run created is active); a fresh POST after `first_completed_at` is set opens a new active run. Stores NOTHING derived (D-24262). Scoped by the resolved `player_id`. `Cache-Control: no-store` set as the first statement of every response. Registered via `registerGauntletRunRoutes(server.router, pool, { requireAuthenticatedSession, verifier, accountResolver, requireUnsuspendedAccount, resolveGauntletExistence })` in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs). |
| `Wired` | `GET` | `/api/me/gauntlet-runs` | `authenticated-session-required` | (none — empty body; bearer session token via Authorization header per WP-112 contract) | `200 → { runs: GauntletRunProgressView[] }` — the caller's runs, each the raw `GauntletRunView` fields `{ id, setAbbr, mastermindSlug, division, playerCount, legPicks, createdAt, updatedAt, firstCompletedAt }` **PLUS** a DERIVED progression block computed at read time (WP-446 / D-24265): `status` (the 5-state `GauntletRunStatus` — `needs-heroes` \| `ready` \| `playing` \| `all-legs-cleared` \| `champion`, with `all-legs-cleared` DISTINCT from `champion`), `pool` (sorted union of all `legPicks` hero ids), `budgetHeadroom` (`budget − pool.length`, negative allowed), `heroCount`, `budget` (`heroCount + 2`), `isChampion`, and `legs: { schemeId, schemeName, cleared, hasFullPicks, lastPlayedAt }[]`. Per-leg `cleared` reuses `qualifiesAsLegClear` and `isChampion` reuses `findBestPoolAssignment` from `gauntletTruth.logic.ts` (the SAME truth the leaderboard's `getGauntletStandings` uses); `lastPlayedAt` is the ISO `max(created_at)` over the caller's qualifying `legendary.competitive_scores` for that leg. Stores **NOTHING** — every field is derived per read (D-24262). Ordered active runs first (`first_completed_at IS NULL` DESC), then `updated_at DESC, id ASC`; on auth failure `401 { "error": "unauthorized" }`; on suspended account `403 { "error": "account_suspended" }`; on uncaught error `500 { "error": "internal_error" }` | WP-446 (gauntlet run derived-progression read) | List the caller's own gauntlet runs with derived progression. Wraps `listGauntletRunProgress(accountId, database, resolveGauntletRunProgressInputs, leaderboardDependencies)` at [`apps/server/src/gauntlet/gauntletRunProgress.logic.ts`](../../../apps/server/src/gauntlet/gauntletRunProgress.logic.ts). Reuses `listGauntletRuns` for the caller's raw runs, then runs one caller-scoped `competitive_scores` read per run. Caller-centric aggregation by `team_key` (D-24265 §4); for a solo run the champion verdict coincides with the caller's presence in `getGauntletStandings`' fixed division (cross-checked). The derivation inputs (`legs`, `approvedLoadouts`, `poolBudget`, `heroCount`, `boardName`) + the leaderboard dependency are INJECTED from the startup `gauntletCatalog` + `PLAYER_COUNT_SETUP` in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs) — the logic layer never imports the registry. READ-ONLY: no write path (never writes `first_completed_at`). Scoped by the resolved `player_id` (cross-account isolation). `Cache-Control: no-store` set as the first statement of every response. |
| `Wired` | `PATCH` | `/api/me/gauntlet-runs/:id` | `authenticated-session-required` | `:id` path param (run UUID). JSON body `{ legPicks }` — an object mapping each scheme slug to an array of `heroDeckIds` (`Record<schemeSlug, string[]>`). Validated for STRUCTURAL shape only — NO gameplay / registry validity check on hero ids (that is launch-time, a later WP). | `200 → GauntletRunView` (updated; `updated_at` advances); a malformed `legPicks` (non-object, array, or a non-string-array value) returns `400 { "error": "invalid_leg_picks" }`; a malformed / cross-account / missing `:id` returns `404 { "error": "not_found" }` (no existence leak; `:id` UUID-guarded before any `::uuid` cast); on auth failure `401`; on uncaught error `500 { "error": "internal_error" }` | WP-445 (gauntlet run import + run-CRUD API) | Edit one of the caller's runs' per-leg hero picks. Wraps `updateGauntletRunLegPicks(accountId, id, patch, database)`. `leg_picks` is the single authoritative hero state; a real PATCH sets `updated_at = now()`. Scoped by the resolved `player_id`. `Cache-Control: no-store` set as the first statement of every response. |
| `Wired` | `DELETE` | `/api/me/gauntlet-runs/:id` | `authenticated-session-required` | `:id` path param (run UUID). | `204` (no body) on success; a malformed / cross-account / missing `:id` returns `404 { "error": "not_found" }` (no existence leak; `:id` UUID-guarded before any `::uuid` cast); on auth failure `401`; on uncaught error `500 { "error": "internal_error" }` | WP-445 (gauntlet run import + run-CRUD API) | Delete one of the caller's runs by id. Wraps `deleteGauntletRun(accountId, id, database)`. Scoped by the resolved `player_id` — account B deleting account A's id gets `404`. `Cache-Control: no-store` set as the first statement of every response. |
| `Wired` | `POST` | `/api/me/friends/requests` | `authenticated-session-required` | JSON body `{ handle }` — the target player's `@handle` (canonicalized `trim().toLowerCase()` before `findAccountByHandle`). | `201 → FriendSummary` `{ handle, displayName, status, direction, requestedAt, respondedAt }` (NO `accountId` / `ext_id` / `player_id` — FR-2); `400 { "error": "invalid_request" }` (missing/empty handle); `404 { "error": "handle_not_found" }` (target unresolved); `409 { "error": "handle_required" }` (acting account has no claimed handle); `409 { "error": "self_friendship" \| "already_pending" \| "already_friends" }`; **(WP-355)** `403 { "error": "blocked" }` (either party has blocked the other); `429 { "error": "request_cooldown" }` (re-send within 24h of a decline) `\| { "error": "rate_limited" }` (≥ 20 outgoing pending requests in the trailing 24h); `401 { "error": "unauthorized" }`; `500 { "error": "internal_error" }` | WP-351 (friend-request API); WP-355 (block/cooldown/rate-limit guards) | Send a friend request by `@handle`. Resolves the target handle → `AccountId` (`findAccountByHandle`), requires the acting account to have a claimed handle. **WP-355 (D-24147):** three abuse-control guards run in the locked order **block → cooldown → rate limit** BEFORE `sendFriendRequest` — `isEitherBlocked` (symmetric) → `blocked`; `mostRecentDeclineAgainst` within `REREQUEST_COOLDOWN_HOURS = 24` → `request_cooldown`; `countOutgoingPendingSince(24h) ≥ MAX_OUTGOING_PENDING_PER_DAY = 20` → `rate_limited`. Then wraps `sendFriendRequest(pool, actingAccountId, targetAccountId)` at [`apps/server/src/friendships/friendships.logic.ts`](../../../apps/server/src/friendships/friendships.logic.ts). Acting identity is session-resolved, never body-supplied. `Cache-Control: no-store` first statement. Registered via `registerFriendshipRoutes(server.router, pool, { requireAuthenticatedSession, verifier, accountResolver })` in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs). |
| `Wired` | `GET` | `/api/me/friends` | `authenticated-session-required` | (none — empty body; bearer session token via Authorization header) | `200 → { friends: FriendSummary[] }` — the caller's accepted friendships, each `{ handle, displayName, status: 'accepted', direction, requestedAt, respondedAt }` (NO `accountId`); `401 { "error": "unauthorized" }`; `500 { "error": "internal_error" }` | WP-351 (friend-request API) | List the caller's accepted friendships. Wraps `listFriends(pool, accountId)` and enriches each `FriendshipView` via a single `WHERE ext_id = ANY($1)` round-trip (no N+1) to swap the friend's `AccountId` for `display_handle` + `display_name`. `Cache-Control: no-store` first statement. |
| `Wired` | `GET` | `/api/me/friends/requests` | `authenticated-session-required` | (none — empty body) | `200 → { incoming: FriendSummary[], outgoing: FriendSummary[] }` — pending requests the caller received (`direction: 'incoming'`) vs sent (`direction: 'outgoing'`), enriched to `handle` + `displayName` (NO `accountId`); `401 { "error": "unauthorized" }`; `500 { "error": "internal_error" }` | WP-351 (friend-request API) | List the caller's pending friend requests both directions. Wraps `listIncomingRequests` + `listOutgoingRequests` (WP-350), each enriched via one `ext_id = ANY($1)` round-trip. `Cache-Control: no-store` first statement. |
| `Wired` | `POST` | `/api/me/friends/requests/:handle/accept` | `authenticated-session-required` | `:handle` path param — the requester's `@handle`. | `200 → FriendSummary` (`status: 'accepted'`, `direction: 'incoming'`, NO `accountId`); `404 { "error": "handle_not_found" }` (requester unresolved) / `{ "error": "no_pending_request" }` (no pending row); `403 { "error": "not_addressee" }` (caller is not the request's addressee); `409 { "error": "handle_required" }`; `401 { "error": "unauthorized" }`; `500 { "error": "internal_error" }` | WP-351 (friend-request API) | Accept a pending incoming request. Resolves the requester `:handle` → `AccountId`, then wraps `acceptFriendRequest(pool, addresseeAccountId, requesterAccountId)` (WP-350) — only the original addressee may accept. `Cache-Control: no-store` first statement. |
| `Wired` | `POST` | `/api/me/friends/requests/:handle/decline` | `authenticated-session-required` | `:handle` path param — the requester's `@handle`. | `200 → FriendSummary` (`status: 'declined'`, NO `accountId`); `404 { "error": "handle_not_found" }` / `{ "error": "no_pending_request" }`; `403 { "error": "not_addressee" }`; `409 { "error": "handle_required" }`; `401 { "error": "unauthorized" }`; `500 { "error": "internal_error" }` | WP-351 (friend-request API) | Decline a pending incoming request. Resolves the requester `:handle` → `AccountId`, then wraps `declineFriendRequest(pool, addresseeAccountId, requesterAccountId)` (WP-350). A later send re-opens the pair (`declined → pending` UPDATE). `Cache-Control: no-store` first statement. |
| `Wired` | `DELETE` | `/api/me/friends/:handle` | `authenticated-session-required` | `:handle` path param — the friend's `@handle`. | `204` (no body) on success; `404 { "error": "handle_not_found" }` (friend unresolved) / `{ "error": "not_friends" }` (no accepted friendship); `409 { "error": "handle_required" }`; `401 { "error": "unauthorized" }`; `500 { "error": "internal_error" }` | WP-351 (friend-request API) | Remove an accepted friendship (either party may). Resolves the friend `:handle` → `AccountId`, then wraps `removeFriend(pool, accountId, otherAccountId)` (WP-350) which DELETEs the row (symmetric; re-friending is a fresh request). `Cache-Control: no-store` first statement. |
| `Wired` | `POST` | `/api/me/blocks` | `authenticated-session-required` | JSON body `{ handle }` — the `@handle` to block. | `201 → { handle, displayName }` of the blocked account (NO `accountId` — FR-2); `400 { "error": "invalid_request" }` (missing/empty handle); `404 { "error": "handle_not_found" }` (target unresolved); `409 { "error": "self_block" \| "already_blocked" }`; `401 { "error": "unauthorized" }`; `500 { "error": "internal_error" }` | WP-355 (friend abuse controls) | Block a player by `@handle`. Wraps `blockPlayer(pool, blockerAccountId, blockedAccountId)` at [`apps/server/src/friendships/playerBlocks.logic.ts`](../../../apps/server/src/friendships/playerBlocks.logic.ts), which INSERTs the block **and** severs any existing friendship in one transaction (D-24147). Blocking is a separate model from friendship (`legendary.player_blocks`, migration 030 — never a friendship `status`, per D-24142). The block guard on `POST /api/me/friends/requests` treats it symmetrically. `Cache-Control: no-store` first statement. |
| `Wired` | `DELETE` | `/api/me/blocks/:handle` | `authenticated-session-required` | `:handle` path param — the blocked `@handle` to unblock. | `204` (no body) on success; `404 { "error": "handle_not_found" }` (target unresolved) / `{ "error": "not_blocked" }` (no block exists); `401 { "error": "unauthorized" }`; `500 { "error": "internal_error" }` | WP-355 (friend abuse controls) | Unblock a player. Wraps `unblockPlayer(pool, blockerAccountId, blockedAccountId)` — deletes the directed block row (does NOT restore any severed friendship; re-friending is a fresh request). `Cache-Control: no-store` first statement. |
| `Wired` | `GET` | `/api/me/blocks` | `authenticated-session-required` | (none — the account is the session) | `200 → { blocked: [{ handle, displayName }] }` (the accounts this player has blocked, most-recent first; NO `accountId`); `401 { "error": "unauthorized" }`; `500 { "error": "internal_error" }` | WP-355 (friend abuse controls) | List the caller's blocked accounts. Wraps `listBlocks(pool, accountId)`. Identity on the wire is `@handle` + `displayName` only (FR-2). `Cache-Control: no-store` first statement. |
| `Wired` | `POST` | `/api/match/invites` | `authenticated-session-required` | JSON body `{ matchId, handle }` — the match to invite into + the friend's `@handle`. | `201 → MatchInviteView` `{ matchId, inviterHandle, inviterDisplayName, status, createdAt }` (NO `accountId` — FR-2); `400 { "error": "invalid_request" }` (missing/empty `matchId` or `handle`); `404 { "error": "handle_not_found" }` (target unresolved); `403 { "error": "not_in_match" }` (inviter not seated in the match) / `{ "error": "not_friends" }` (invitee is not an accepted friend); `409 { "error": "self_invite" \| "already_invited" }`; `401 { "error": "unauthorized" }`; `500 { "error": "internal_error" }` | WP-358 (match friend-invite) | Invite an **accepted friend** into a match you have joined. Resolves the target `@handle` → `AccountId` (`findAccountByHandle`), then wraps `createMatchInvite(pool, inviterAccountId, inviteeAccountId, matchId)` at [`apps/server/src/match/matchInvites.logic.ts`](../../../apps/server/src/match/matchInvites.logic.ts) — guards self-invite, inviter-seated (`readSeatAccounts`, WP-333), and friends-only (`getFriendshipStatus === 'accepted'`, anti-spam + block-respecting by construction). Fires a fail-open invite email fire-and-forget (WP-353 sender; `BREVO_MATCH_INVITE_TEMPLATE_ID`). `Cache-Control: no-store` first statement. Registered via `registerMatchInviteRoutes(server.router, pool, deps)` in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs). |
| `Wired` | `GET` | `/api/me/match-invites` | `authenticated-session-required` | (none — the account is the session) | `200 → { invites: MatchInviteView[] }` — the caller's pending incoming match invites, newest first, each `{ matchId, inviterHandle, inviterDisplayName, status: 'pending', createdAt }` (NO `accountId`); `401 { "error": "unauthorized" }`; `500 { "error": "internal_error" }` | WP-358 (match friend-invite) | List the caller's pending incoming match invites. Wraps `listIncomingMatchInvites(pool, accountId)` — one JOIN to `legendary.players` for the inviter's `display_handle` + `display_name`. `Cache-Control: no-store` first statement. |
| `Wired` | `POST` | `/api/me/match-invites/:matchId/accept` | `authenticated-session-required` | `:matchId` path param. | `200 → { matchId }` — the caller then joins via the existing `POST /api/match/join` (NO server-side bgio join); `404 { "error": "invite_not_found" }` (no pending invite to that match for the caller); `401 { "error": "unauthorized" }`; `500 { "error": "internal_error" }` | WP-358 (match friend-invite) | Accept a pending match invite. Wraps `acceptMatchInvite(pool, accountId, matchId)` (marks the invite `accepted`). Returns the `matchId` for the client to join through the existing join endpoint. `Cache-Control: no-store` first statement. |
| `Wired` | `POST` | `/api/me/match-invites/:matchId/decline` | `authenticated-session-required` | `:matchId` path param. | `204` (no body) on success; `404 { "error": "invite_not_found" }` (no pending invite to that match); `401 { "error": "unauthorized" }`; `500 { "error": "internal_error" }` | WP-358 (match friend-invite) | Decline a pending match invite. Wraps `declineMatchInvite(pool, accountId, matchId)` (marks the invite `declined`; a later re-invite re-opens it via `declined → pending` UPDATE). `Cache-Control: no-store` first statement. |
| `Wired` | `POST` | `/api/teams` | `authenticated-session-required` | JSON body `{ name, cohortLabel, teamSize, startDate, endDate, visibility, founders }` per `CreateTeamInput` at [`apps/server/src/teams/team.types.ts`](../../../apps/server/src/teams/team.types.ts). `teamSize` is a closed literal union `3 \| 4 \| 5` (WP-109 §6 — Legendary's three meaningful cooperative formats). `visibility` is `'public' \| 'friends' \| 'private'`. `founders` enumerates the initial members + substitutes (the captain is the API caller and is appended automatically). | `Team` per WP-109 contract at [`apps/server/src/teams/team.types.ts`](../../../apps/server/src/teams/team.types.ts); on validator failure returns `400` / `409` per the closed-set `TeamErrorCode` mapping table at [`apps/server/src/teams/team.routes.ts`](../../../apps/server/src/teams/team.routes.ts); on auth failure returns `401` / `500` per the locked `SessionValidationErrorCode` mapping table (`'unknown_account'` is **401, not 403**) | WP-109 (team affiliation) | Caller becomes captain + first member automatically. Single-transaction multi-row write per D-10907 + EC-115 Guardrail 15 (BEGIN; INSERT teams; INSERT team_member_events × N; INSERT team_audit_log; COMMIT). Validator runs before any SQL: `validateTeamSize` rejects sizes outside `{3,4,5}`; `validateRoster` rejects under-strength founding rosters per WP-109 §8.2. `team_id` is server-generated UUID v4 (D-10908 brand-cast at exactly one site). `Cache-Control: no-store` set as the first statement of every response. Registered via `registerTeamRoutes(server.router, pool, { requireAuthenticatedSession, verifier, accountResolver })` in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs). Genuinely authenticated as of WP-131 / EC-134. Production wiring constructs the Hanko verifier at startup from `HANKO_TENANT_BASE_URL` + `HANKO_EXPECTED_AUDIENCE`. |
| `Wired` | `GET` | `/api/teams/:teamId` | `handle-required` | `:teamId` path param (UUID v4 per `toTeamId` validator at [`apps/server/src/teams/team.logic.ts`](../../../apps/server/src/teams/team.logic.ts)). Authentication is optional for `'public'`-visibility teams; required for `'friends'` / `'private'` (visibility filter applied server-side). | `Team` per WP-109 contract; on miss returns `404` with body `{ "error": "team_not_found" }`; on visibility-filter denial returns **`404` with body `{ "error": "team_not_visible" }`** (NOT 403, per Hard Stop #20 — avoids leaking team existence to viewers without permission); on malformed `teamId` returns `400` with body `{ "error": "invalid_request" }` | WP-109 (team affiliation) | Visibility-filtered read: `'public'` teams return to all (anonymous viewers permitted); `'friends'` and `'private'` teams require authentication AND that the viewer is a current/historical member. The `'friends'`-without-friend-graph case collapses to `'private'` semantics per OQ-1 = (a) / D-10901. `Cache-Control: no-store` set as the first statement of every response. Genuinely authenticated as of WP-131 / EC-134. Production wiring constructs the Hanko verifier at startup from `HANKO_TENANT_BASE_URL` + `HANKO_EXPECTED_AUDIENCE`. |
| `Wired` | `PATCH` | `/api/teams/:teamId` | `authenticated-session-required` | `:teamId` path param + JSON body `{ name?, visibility? }` (sparse partial — `teamSize` is intentionally NOT in scope per EC-115 Guardrail 9 immutability lock). Per `Object.hasOwn` discrimination: key absent → leave unchanged; key present → validate + apply. | Updated `Team` per WP-109 contract; on validator failure returns `400` (closed-set `TeamErrorCode`); on captain-mismatch returns `403` with body `{ "error": "not_team_captain" }`; on auth failure per the locked `SessionValidationErrorCode` mapping | WP-109 (team affiliation) | Captain-only mutation. The route layer's `assertCallerIsCaptain` helper compares `team.captainPlayerId` to the resolved caller `player_id` (lookup against `legendary.players.ext_id`). Audit-log entries fire per field changed: `'rename'` action when name changes, `'visibility_change'` action when visibility changes. `Cache-Control: no-store` set as the first statement of every response. Genuinely authenticated as of WP-131 / EC-134. Production wiring constructs the Hanko verifier at startup from `HANKO_TENANT_BASE_URL` + `HANKO_EXPECTED_AUDIENCE`. |
| `Wired` | `POST` | `/api/teams/:teamId/members` | `authenticated-session-required` | `:teamId` path param + JSON body `{ playerId: string, role: 'member' \| 'substitute' }`. | Updated `Team` per WP-109 contract; on closed-set `TeamErrorCode` failure returns `400` / `403` / `404` / `409` per the locked mapping; specifically `'duplicate_active_membership'` returns `409` (same-size cohort exclusivity per WP-109 §8.5 + EC-115 Guardrail 12). | WP-109 (team affiliation) | Captain-only mutation. `validateSameSizeExclusivity` issues a single SELECT against `legendary.team_member_events` before the INSERT; the partial UNIQUE index `uq_team_member_events_active_size` is the concurrency-safe backstop. The `team_size` column on the new event row is INSERT-time copied from `legendary.teams.team_size` per OQ-4 = (a) / D-10906; structurally immutable post-INSERT. Audit-log entry: `'member_add'` action. `Cache-Control: no-store` set as the first statement of every response. Genuinely authenticated as of WP-131 / EC-134. Production wiring constructs the Hanko verifier at startup from `HANKO_TENANT_BASE_URL` + `HANKO_EXPECTED_AUDIENCE`. |
| `Wired` | `PATCH` | `/api/teams/:teamId/members/:playerId` | `authenticated-session-required` | `:teamId` and `:playerId` path params (no JSON body required for the substitute → member promotion path — the only role change WP-109 supports). | Updated `Team` per WP-109 contract; on `'invalid_request'` returns `400` (target is not currently a substitute); on captain-mismatch returns `403` `{ "error": "not_team_captain" }`. | WP-109 (team affiliation) | Captain-only mutation. Promotion is the SECOND of the two-event flow per WP-109 §8.3 + EC-115 Guardrail 4 — a substitute does not auto-promote when a member's `leftAt` is set. The captain (or operator) records both the departing member's `leftAt` (via DELETE below) AND the substitute's role change (here) as separate actions. Internally: SEAL the substitute's open event row (set `left_at = now()` per the seal-via-UPDATE lifecycle pattern; the partial UNIQUE index requires the prior open row to be sealed before a new same-`(player, size)` row can be opened) + INSERT a new event row with `role = 'member'`. Audit-log entry: `'role_change'` action with payload `{ fromRole: 'substitute', toRole: 'member' }`. `Cache-Control: no-store` set as the first statement of every response. Genuinely authenticated as of WP-131 / EC-134. Production wiring constructs the Hanko verifier at startup from `HANKO_TENANT_BASE_URL` + `HANKO_EXPECTED_AUDIENCE`. |
| `Wired` | `DELETE` | `/api/teams/:teamId/members/:playerId` | `authenticated-session-required` | `:teamId` and `:playerId` path params. | Updated `Team` per WP-109 contract; on captain-mismatch (when caller is neither the captain nor the target self) returns `403` `{ "error": "not_team_captain" }`; on validator failure returns `400` (`'monotonic_violation'` for impossible timeline). | WP-109 (team affiliation) | Captain or self mutation: the route layer permits the leave when the caller's `player_id` equals the target `player_id` (self-leave) OR when the caller is the team captain. Internally: SEAL the open event row (set `left_at = now()` per the seal-via-UPDATE lifecycle pattern). Per WP-109 §8.3 + EC-115 Guardrail 4, this does NOT auto-promote any substitute — the captain must issue a separate `PATCH /api/teams/:teamId/members/:playerId` for the substitute to promote. Audit-log entry: `'member_leave'` action. `Cache-Control: no-store` set as the first statement of every response. Genuinely authenticated as of WP-131 / EC-134. Production wiring constructs the Hanko verifier at startup from `HANKO_TENANT_BASE_URL` + `HANKO_EXPECTED_AUDIENCE`. |
| `Wired` | `PATCH` | `/api/teams/:teamId/captain` | `authenticated-session-required` | `:teamId` path param + JSON body `{ newCaptainPlayerId: string }`. | Updated `Team` per WP-109 contract; on `'captain_must_be_member'` returns `400` (target is not a current member); on captain-mismatch returns `403` `{ "error": "not_team_captain" }`. | WP-109 (team affiliation) | Captain-only mutation. `validateCaptainInvariant` enforces the captain MUST be a current `member` (role `'member'`, leftAt unset) per WP-109 §6 + EC-115 Guardrail 11; substitutes, former members, and non-members are all rejected. Atomic UPDATE on `legendary.teams.captain_player_id` — exactly one captain per team at all times; no transient null-captain or two-captain state. Audit-log entry: `'captain_reassign'` action with payload `{ oldCaptainPlayerId, newCaptainPlayerId }`. `Cache-Control: no-store` set as the first statement of every response. Genuinely authenticated as of WP-131 / EC-134. Production wiring constructs the Hanko verifier at startup from `HANKO_TENANT_BASE_URL` + `HANKO_EXPECTED_AUDIENCE`. |
| `Wired` | `POST` | `/api/teams/:teamId/status` | `authenticated-session-required` | `:teamId` path param + JSON body `{ status: 'completed' \| 'retired' }`. The `'active'` value is intentionally rejected (no path back from terminal states; would also conflict with the `'team_not_active'` precondition check). | Updated `Team` per WP-109 contract; on `'team_not_active'` returns `409` (team is already in a terminal state); on captain-mismatch returns `403` `{ "error": "not_team_captain" }`. | WP-109 (team affiliation) | Captain-only mutation. Per WP-109 §10 the two terminal states are observationally distinct — `'completed'` is a natural end (cohort finished its planned arc); `'retired'` is a premature / administrative termination. Both freeze the roster (no further membership mutations permitted). EC-115 Guardrail 14 forbids adding a `'paused'` / recovery state without a DECISIONS.md override. Audit-log entry: `'status_change'` action with payload `{ oldStatus, newStatus }`. `Cache-Control: no-store` set as the first statement of every response. Genuinely authenticated as of WP-131 / EC-134. Production wiring constructs the Hanko verifier at startup from `HANKO_TENANT_BASE_URL` + `HANKO_EXPECTED_AUDIENCE`. |
| `Wired` | `GET` | `/api/me/entitlements` | `authenticated-session-required` | (none — empty body; bearer session token via Authorization header per WP-112 contract) | `200 → { entitlements: Entitlement[] }` per WP-132 contract at [`apps/server/src/entitlements/entitlements.types.ts`](../../../apps/server/src/entitlements/entitlements.types.ts); on auth failure returns `401` with body `{ "code": "unauthorized" }` for the closed-set 401-mapped session validation codes (`'missing_token'` / `'invalid_token'` / `'expired_token'` / `'unknown_account'` collapse to a single client-facing value to defeat the account-existence probe per WP-104 D-10403); on dev-mode unconfigured-verifier returns `500` with body `{ "code": "session_verifier_not_configured" }` (only reachable when `NODE_ENV != 'production'` per D-13101); on operational fault (orchestrator `'lookup_failed'` or helper `'lookup_failed'`) returns `500` with body `{ "error": "internal_error" }` (project-owned envelope per D-11802 = (C)) | WP-132 (entitlements data model + read API) | Result ordering is `grantedAt` ASC (PUBLIC CONTRACT — consumers MAY rely on stable oldest-first ordering for deterministic rendering). Revoked entitlements (`revokedAt IS NOT NULL`) are EXCLUDED by contract — the response NEVER includes a row with non-null `revokedAt`. Timestamps are ISO-8601 UTC strings (Postgres `timestamptz` JSON serialization). `EntitlementKey` is the closed-set allowlist locked under D-13203 (six cosmetic-only members at WP-132 close — adding a key is a data edit + WP + DECISIONS.md entry, never a runtime toggle); `source` is the closed-set value `'stripe' \| 'admin_grant' \| 'comp'` locked under D-13204. The `legendary.entitlements (player_id, entitlement_key) WHERE revoked_at IS NULL` partial-unique-index is the idempotency primitive consumed by WP-134's webhook retries. `Cache-Control: no-store` set as the first statement of every response (including 401 / 500 error paths per WP-115 D-11504 lock). Status-code domain locked to `{200, 401, 500}` for this handler; `403` / `404` / `422` are out of scope per D-13205 envelope-split lock. Registered via `registerEntitlementRoutes(server.router, pool, { requireAuthenticatedSession, verifier, accountResolver })` in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs). Genuinely authenticated as of WP-131 / EC-134. Production wiring constructs the Hanko verifier at startup from `HANKO_TENANT_BASE_URL` + `HANKO_EXPECTED_AUDIENCE`. |
| `Wired` | `POST` | `/api/billing/checkout-session` | `authenticated-session-required` | `{ priceId: string }` — exactly one field; any extra field (including `successUrl`, `cancelUrl`, `redirectUri`) returns `400` with body `{ "code": "invalid_request" }` per WP-133 §Locked contract values (the redirect-manipulation defense locked under D-13309 — `successUrl` / `cancelUrl` are server-derived from `BillingConfig.publicBaseUrl` and never sourced from request input). | `200 → { checkoutUrl: string, sessionId: string }` per WP-133 contract at [`apps/server/src/billing/billing.types.ts`](../../../apps/server/src/billing/billing.types.ts); on disallowed `priceId` returns `400` with body `{ "code": "invalid_price" }` (allowlist gate fires BEFORE any Stripe SDK call per EC-136 §3 invariant); on auth failure returns `401` with body `{ "code": "unauthorized" }` (the closed-set 401-mapped codes — `'missing_token'` / `'invalid_token'` / `'expired_token'` / `'unknown_account'` — collapse to a single client-facing value to defeat the account-existence probe per WP-104 D-10403); on Stripe SDK fault returns `500` with body `{ "code": "stripe_error" }`; on dev-mode unconfigured-verifier returns `500` with body `{ "code": "session_verifier_not_configured" }`; on `billingConfig === undefined` returns `503` with body `{ "code": "billing_not_configured" }` (non-production missing-env path); on uncaught operational fault returns `500` with body `{ "error": "internal_error" }` (project-owned envelope per D-11802 = (C)). | WP-133 (Stripe checkout session + webhook ingestion) | `priceId` validated against the env-configured `STRIPE_PRICE_ALLOWLIST` (parsed at startup into `ReadonlyMap<string, EntitlementKey>` per D-13305) BEFORE any Stripe API call — a client submitting an arbitrary price ID gets `400` with NO Stripe call made. `successUrl` and `cancelUrl` are server-derived from `${BillingConfig.publicBaseUrl}/billing/success?session_id={CHECKOUT_SESSION_ID}` and `${BillingConfig.publicBaseUrl}/billing/cancel` respectively per D-13309. Stripe Checkout `mode: 'payment'` per D-13307 (one-time payments only at MVP; subscriptions deferred to a future WP). Stripe Customer creation deferred per D-13308 — WP-133 passes `customer_email` only (resolved from `legendary.players.email` for the validated `accountId`); the future Customer Portal WP introduces eager Customer creation. INSERTs a row into `legendary.stripe_checkout_sessions` with `intent_status = 'open'` (the ONLY value WP-133 ever writes; WP-134 owns transitions). `Cache-Control: no-store` set as the first statement of every response (including error paths per WP-115 D-11504 lock). Status-code domain locked to `{200, 400, 401, 500, 503}` for this handler; `403` / `404` / `422` are out of scope. Registered via `registerBillingRoutes(server.router, pool, { requireAuthenticatedSession, verifier, accountResolver, billingConfig, stripeClient, resolveCustomerEmail })` in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs). Genuinely authenticated as of WP-131 / EC-134. WP-134 does not replace this row. |
| `Wired` | `POST` | `/api/billing/webhook/stripe` | `guest` | Raw bytes (NOT JSON) — Stripe sends the event payload as the verbatim body; the `Stripe-Signature` header IS the auth (per the WP-115 `guest`-with-server-side-validation precedent). Route-level raw-body middleware captures `ctx.request.rawBody` BEFORE any global JSON parser per D-13304 (`koa-body` raw mode at the route scope, `jsonLimit: '1mb'`); JSON parse + restringify would invalidate the HMAC signature. | `200 → { received: true, duplicate: boolean, processed: boolean, reason: string \| null }` per the WP-134 D-13402 extension — `processed: true` for every `Result.ok` outcome reached without error (including the terminal-no-op reasons `'fulfilled'` / `'duplicate'` / `'unhandled_event_type'` / `'unpaid_session'` / `'already_processed'`); `processed: false` only on `Result.fail` from `processStripeEvent` OR on the duplicate-delivery skip branch (existing row's `processed_at` non-NULL); `reason` carries the `FulfillmentSuccessReason` closed-set string on success (`'fulfilled'` / `'duplicate'` / `'unhandled_event_type'` / `'unpaid_session'` / `'already_processed'`), the `FulfillmentErrorCode` closed-set string on `Result.fail` (`'session_lookup_failed'` / `'cross_validation_failed'` / `'price_not_in_allowlist'` / `'entitlement_insert_failed'` / `'event_update_failed'` / `'session_update_failed'`), or `null` on the duplicate-delivery skip branch only. The invariant `processed: true ↔ reason ≠ null` holds by construction. On signature verification failure returns `400` with body `{ "code": "invalid_signature" }`; on database fault at the `recordStripeEvent` INSERT returns `500` with body `{ "error": "internal_error" }`; on the row-absent edge case (the post-INSERT SELECT returns no row) returns `500` with body `{ "error": "internal_error" }` per D-13404 (the SOLE exception to the always-200 posture). | WP-133, WP-134 | Stripe signature verifies the raw body via `stripe.webhooks.constructEvent(rawBody, sig, billingConfig.webhookSecret)`; idempotent ingestion via `ON CONFLICT (event_id) DO NOTHING` (per D-13306). The `payload jsonb` column stores the FULL Stripe event envelope serialized via `JSON.stringify(event)` — `api_version` is preserved as the forensic signal for Stripe-side API version drift. Zero `event.type` filter on the ingestion path — `processStripeEvent` is the sole classifier per D-13403; the unhandled-event-type early-return guard maps non-`checkout.session.completed` types to `Result.ok({ reason: 'unhandled_event_type' })`. Per D-13401 the webhook handler invokes `processStripeEvent` synchronously after `recordStripeEvent` and BEFORE returning 200; processing budget is ~50–200 ms per row, well under Stripe's 30s timeout. The shared local helper `loadStripeEventRecordByEventId(pool, eventId)` re-fetches the inserted row by `event_id` (PS-1 path (a) re-fetch via SELECT — `recordStripeEvent` returns `BillingResult<{ inserted: boolean }>` only and is NOT modified). The duplicate-delivery branch (Stripe at-least-once retry) inspects `existingRow.processed_at`: if NULL the handler dispatches `processStripeEvent` against the existing row (self-heal); if non-NULL the handler skips processing and returns `{ duplicate: true, processed: false, reason: null }`. Always-200 on signature-verified events per D-13404 (the row-absent 500 is the SOLE exception); fulfillment errors land in `legendary.stripe_events.process_error` for forensic review and the recovery script `scripts/process-stripe-events.mjs` (per D-13405) drains the backlog. The `EntitlementKey` allowlist sourcing — `BillingConfig.priceAllowlist` parsed at startup from `STRIPE_PRICE_ALLOWLIST` per D-13305 — feeds the cross-validation axes; the closed-set `EntitlementKey` union locked under WP-132 D-13203 is the structural NG-1 defense. `Cache-Control: no-store` set as the first statement of every response. Status-code domain locked to `{200, 400, 500}` for this handler. Registered via `registerBillingRoutes(...)` adjacent to the checkout-session handler. |
| `Wired` | `GET` | `/api/me/billing/history` | `authenticated-session-required` | (none — empty body; bearer session token via Authorization header per WP-112 contract) | `200 → { history: BillingHistoryEntry[] }` per WP-108 contract at [`apps/server/src/billing/billing.types.ts`](../../../apps/server/src/billing/billing.types.ts); on auth failure returns `401` with body `{ "code": "unauthorized" }` (closed-set 401-mapped session validation codes collapse to a single client-facing value per WP-104 D-10403); on operational fault returns `500` with body `{ "error": "internal_error" }` (project-owned envelope per D-11802 = (C)) | WP-108 (profile billing & funding history UI) | Read-only billing history for the authenticated owner's Stripe checkout sessions. Two-query pattern mirrors WP-132 `getEntitlementsForAccount`: Step 1 existence check against `legendary.players` by `ext_id`; Step 2 SELECT from `legendary.stripe_checkout_sessions` by `account_id` with `ORDER BY created_at DESC LIMIT 100`. `Cache-Control: no-store` set as the first statement of every response (including 401 / 500 error paths per WP-115 D-11504 lock). Status-code domain locked to `{200, 401, 500}` for this handler. Registered via `registerBillingRoutes(...)` adjacent to the checkout-session and webhook handlers in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs). Genuinely authenticated as of WP-131 / EC-134. |
| `Wired` | `GET` | `/api/players/:handle/profile` | `guest` | `:handle` path param (string matching `HANDLE_REGEX` per WP-101 — `^[a-z][a-z0-9_]{2,23}$`) | `PublicProfileView` shape `{ handleCanonical, displayHandle, displayName, publicReplays, teamAffiliations, badges }` per WP-102 + WP-109 + WP-105 contracts; on miss returns `404` with body `{ "error": "player_not_found" }` (project-specific shape per D-11802) | WP-102, WP-152 | Route live. Registered via `registerProfileRoutes(server.router, pool)` in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs). Guest endpoint — no auth middleware. Closes D-10202 deferral (WP-102 route registration deferred to future request-handler WP) and D-11505 reaffirmation (WP-115). Handler code unchanged from WP-102; `profile.routes.ts`, `profile.logic.ts`, `profile.types.ts` are zero-byte diff. `Cache-Control: no-store` set as the first statement of every response (including 404 error path). Status-code domain locked to `{200, 404, 500}` for this handler. |
| `Wired` | `GET` | `/api/admin/billing/history` | `admin-session-required` | (none — empty body; bearer session token via Authorization header per WP-112 contract) | `200 → { entries: AdminBillingEntry[] }` per WP-110 contract at [`apps/server/src/billing/adminBilling.types.ts`](../../../apps/server/src/billing/adminBilling.types.ts); `AdminBillingEntry` has 6 fields: `accountId`, `sessionId`, `entitlementKey`, `intentStatus` (closed 4-value union: `'open' \| 'completed' \| 'expired' \| 'canceled'`), `createdAt`, `completedAt` (string \| null). On auth failure returns `401` with body `{ "code": "unauthorized", "reason": "<string>" }` or `403` with body `{ "code": "forbidden", "reason": "<string>" }` (closed-union `'unauthorized' \| 'forbidden'` per D-15901); on `requireAdminSession` `'lookup_failed'` returns `500` with body `{ "code": "internal_error" }` (distinct from operational-fault 500 below); on operational fault returns `500` with body `{ "error": "internal_error" }` (project-owned envelope per D-11802 = C). | WP-110 (admin billing visibility), WP-176 (auth cutover) | Read-only cross-account billing visibility surface for authorized internal users. Queries `legendary.stripe_checkout_sessions` without `account_id` filter; `ORDER BY created_at DESC LIMIT 250`. Admin gate via `requireAdminSession` (WP-159 §A — Hanko session + `is_admin = TRUE` per D-15901 + D-15902), replacing the WP-110 shared-secret gate per D-17601. `adminGate.ts` deleted (D-17603). `price_id` deliberately excluded from `AdminBillingEntry` per D-11002 — Stripe dashboard lookup via `sessionId` suffices for dispute correlation. `Cache-Control: no-store` set as the first statement of the handler body per WP-115 D-11504 lock. Status-code domain locked to `{200, 401, 403, 500}` for this handler. Registered via `registerAdminBillingRoutes(server.router, pool, { requireAdminSession, verifier, accountResolver })` in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs). |
| `Wired` | `GET` | `/api/dash/metrics/billing/health` | `admin-session-required` | `range` query parameter, closed set `'7d' \| '14d' \| '30d' \| '90d'` (required; absent or invalid → `400 { code: 'invalid_request' }`); bearer session token via Authorization header per WP-112 contract. | `200 → { data: BillingHealth }` where `BillingHealth` has 8 fields — `windowStart` (string, ISO-8601), `windowEnd` (string, ISO-8601), `webhookFailureRate` (number in `[0, 1]`), `webhookFailureCount` (number), `webhookTotalCount` (number), `intentAbandonmentRate` (number in `[0, 1]`), `intentAbandonedCount` (number), `intentTotalCount` (number) — at [`apps/server/src/dashboard/dashboardBilling.types.ts`](../../../apps/server/src/dashboard/dashboardBilling.types.ts). A zero-total window yields rate `0`, never `NaN`. On auth failure `401 { code: 'unauthorized', reason }` or `403 { code: 'forbidden', reason }` (closed union per D-15901); `requireAdminSession` `'lookup_failed'` and any handler fault both collapse to `500 { code: 'internal_error' }`. | WP-373 (dashboard billing + revenue) | Read-only billing-health feed over `legendary.stripe_events` (webhook failures) + `legendary.stripe_checkout_sessions` (intent abandonment). First `/api/dash/*` sub-surface. Response byte-mirrors the dashboard `BillingHealth` (D-19603 forward contract; drift source of truth `apps/dashboard/src/types/index.ts`). Admin gate via `requireAdminSession` (WP-159 §A — Hanko session + `is_admin = TRUE`); D-19603 finance/admin gate resolves to admin (no `finance` role exists). `Cache-Control: no-store` first statement per D-11504. Bare `{ data: T }` envelope per D-20503. Status-code domain locked to `{200, 400, 401, 403, 500}`. Registered via `registerDashboardBillingRoutes(server.router, pool, { requireAdminSession, verifier, accountResolver })` in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs). Cites: D-24168, D-19603, D-20503, D-11504, D-11804. |
| `Wired` | `GET` | `/api/dash/metrics/billing/health/sparklines` | `admin-session-required` | `range` query parameter, closed set `'7d' \| '14d' \| '30d' \| '90d'` (required; absent or invalid → `400 { code: 'invalid_request' }`); bearer session token via Authorization header. | `200 → { data: BillingHealthSparklines }` where `BillingHealthSparklines` is `{ webhook: BillingHealthSparklinePoint[], intent: BillingHealthSparklinePoint[] }` and `BillingHealthSparklinePoint` is `{ date: string (YYYY-MM-DD, UTC), rate: number }` — one point per UTC day across the window, gaps filled with rate `0` — at [`apps/server/src/dashboard/dashboardBilling.types.ts`](../../../apps/server/src/dashboard/dashboardBilling.types.ts). Auth failures `401 { code: 'unauthorized', reason }` / `403 { code: 'forbidden', reason }`; `'lookup_failed'` + handler fault → `500 { code: 'internal_error' }`. | WP-373 (dashboard billing + revenue) | Per-day webhook-failure + intent-abandonment sparklines accompanying the aggregate `BillingHealth`. Drift source of truth `apps/dashboard/src/services/billingHealthMocks.ts`. Same admin gate, `no-store`, and `{ data: T }` envelope as `GET /api/dash/metrics/billing/health`. Status-code domain locked to `{200, 400, 401, 403, 500}`. Registered via `registerDashboardBillingRoutes(server.router, pool, { requireAdminSession, verifier, accountResolver })` in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs). Cites: D-24168, D-19603, D-20503, D-11504, D-11804. |
| `Wired` | `GET` | `/api/dash/revenue` | `admin-session-required` | (no query parameters — `range` is NOT consulted; bearer session token via Authorization header). | `200 → { data: RevenueRecord[] }` where `RevenueRecord` is `{ id: string, date: string, amount: number, source: string, currency: string }` — `amount` is whole currency units (Stripe `amount_total` cents ÷ 100), `currency` is upper-cased, `source` is the joined checkout session's `entitlement_key` when present else the `'stripe'` fallback; events whose `amount_total` is null are skipped — at [`apps/server/src/dashboard/dashboardBilling.types.ts`](../../../apps/server/src/dashboard/dashboardBilling.types.ts). Most-recent 50 (query-level `LIMIT 50`). Auth failures `401 { code: 'unauthorized', reason }` / `403 { code: 'forbidden', reason }`; `'lookup_failed'` + handler fault → `500 { code: 'internal_error' }`. | WP-373 (dashboard billing + revenue) | Read-only recent-purchase revenue records over `legendary.stripe_events` (completed-checkout amounts) joined to `legendary.stripe_checkout_sessions` for `source`. Response byte-mirrors the dashboard `RevenueRecord`. Takes NO range (unlike the three metrics routes). Same admin gate, `no-store`, `{ data: T }` envelope. Status-code domain locked to `{200, 401, 403, 500}`. Registered via `registerDashboardBillingRoutes(server.router, pool, { requireAdminSession, verifier, accountResolver })` in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs). Cites: D-24168, D-19603, D-20503, D-11504, D-11804. |
| `Wired` | `GET` | `/api/dash/metrics/revenue` | `admin-session-required` | `range` query parameter, closed set `'7d' \| '14d' \| '30d' \| '90d'` (required; absent or invalid → `400 { code: 'invalid_request' }`); bearer session token via Authorization header. | `200 → { data: DailyMetric[] }` where `DailyMetric` is `{ date: string (YYYY-MM-DD, UTC), value: number }` — completed-purchase amounts (whole currency units) summed per UTC day across the window, gaps filled with `0` — at [`apps/server/src/dashboard/dashboardBilling.types.ts`](../../../apps/server/src/dashboard/dashboardBilling.types.ts). Auth failures `401 { code: 'unauthorized', reason }` / `403 { code: 'forbidden', reason }`; `'lookup_failed'` + handler fault → `500 { code: 'internal_error' }`. | WP-373 (dashboard billing + revenue) | Read-only daily-revenue time series (whole currency units per UTC day) for the dashboard revenue chart. Response byte-mirrors the dashboard `DailyMetric`. Same admin gate, `no-store`, `{ data: T }` envelope as the other metrics routes. Status-code domain locked to `{200, 400, 401, 403, 500}`. Registered via `registerDashboardBillingRoutes(server.router, pool, { requireAdminSession, verifier, accountResolver })` in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs). Cites: D-24168, D-19603, D-20503, D-11504, D-11804. |
| `Wired` | `GET` | `/api/dash/matches` | `admin-session-required` | (no query parameters; bearer session token via Authorization header). | `200 → { data: MatchRecord[] }` where `MatchRecord` is `{ id: string, startedAt: string (ISO), duration: number (seconds), playerCount: number, scheme: string, mastermind: string, outcome: 'villain_wins' \| 'hero_wins' \| 'in_progress' }` at [`apps/server/src/dashboard/dashboardGameplay.types.ts`](../../../apps/server/src/dashboard/dashboardGameplay.types.ts). Most-recent 50 by `updated_at`. Auth failures `401 { code: 'unauthorized', reason }` / `403 { code: 'forbidden', reason }`; handler fault → `500 { code: 'internal_error' }`. | WP-374 (dashboard gameplay + KPIs) | Read-only **match-summary projection** of the `bgio.matches` blob (**D-24169 carve-out**): `initial_state.G.matchConfiguration` (scheme/mastermind ext_ids resolved to display names via the startup registry) + `ctx.numPlayers` + `metadata.{createdAt,updatedAt,gameover}`. **Projection-only** — never reads `state`/`log`, never writes; `metadata.gameover` absent → `in_progress`, `heroes-win` → `hero_wins`, `scheme-wins` → `villain_wins`; rows with null `initial_state` skipped. Same admin gate, `no-store`, `{ data: T }` envelope. Status-code domain `{200, 401, 403, 500}`. Registered via `registerDashboardGameplayRoutes(server.router, pool, { requireAdminSession, registry, verifier, accountResolver })` in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs). Cites: D-24169, D-24153, D-24095, D-20503, D-11504, D-11804. |
| `Wired` | `GET` | `/api/dash/players` | `admin-session-required` | (no query parameters; bearer session token via Authorization header). | `200 → { data: PlayerRecord[] }` where `PlayerRecord` is `{ id: string (ext_id), name: string, email: string, matchesPlayed: number, winRate: number (0-1), lastActive: string (ISO), status: 'active' \| 'inactive' \| 'banned' }` at [`apps/server/src/dashboard/dashboardGameplay.types.ts`](../../../apps/server/src/dashboard/dashboardGameplay.types.ts). Most-recent 100 by `created_at`. Auth failures as above; handler fault → `500 { code: 'internal_error' }`. | WP-374 (dashboard gameplay + KPIs) | Read-only `legendary.players` LEFT JOIN aggregated `legendary.competitive_scores` (by internal `player_id`): `matchesPlayed` = score count, `winRate` = `heroes-win` fraction (0 when 0 scores — not null), `status` = `is_suspended` → `banned` else `active`/`inactive` by a 30-day recency threshold, `lastActive` = **approximate** (`max(competitive_scores.created_at)` ?? `players.created_at` — no per-account activity log exists). Same admin gate, `no-store`, `{ data: T }`. Status-code domain `{200, 401, 403, 500}`. Cites: D-24169, D-20503, D-11504, D-11804. |
| `Wired` | `GET` | `/api/dash/kpis` | `admin-session-required` | (no query parameters; bearer session token via Authorization header). | `200 → { data: KpiSnapshot[] }` where `KpiSnapshot` is `{ id: string, label: string, value: number, previousValue: number, unit: string, trend: 'up' \| 'down' \| 'flat', target?: number, tolerance?: number, direction?: 'higher-is-better' \| 'lower-is-better' }` at [`apps/server/src/dashboard/dashboardGameplay.types.ts`](../../../apps/server/src/dashboard/dashboardGameplay.types.ts). Auth failures as above; handler fault → `500 { code: 'internal_error' }`. | WP-374 (dashboard gameplay + KPIs) | Read-only **derivable** KPI set — `total_players`, `new_players_30d`, `total_matches`, `revenue_30d` (reuses WP-373 `getRevenueDaily`), `hero_win_rate_30d` — each with a prior-equal-window `previousValue` and `up`/`down`/`flat` trend; `target`/`tolerance`/`direction` omitted (no threshold defined, D-19802 → no status chip). **DAU and any activity-derived KPI are OMITTED** (no activity signal exists — not fabricated). Same admin gate, `no-store`, `{ data: T }`. Status-code domain `{200, 401, 403, 500}`. Cites: D-24169, D-19802, D-20503, D-11504, D-11804. |
| `Wired` | `GET` | `/api/dash/system/runtime` | `admin-session-required` | (no query parameters; bearer session token via Authorization header). | `200 → { data: RuntimeHealthSnapshot }` where `RuntimeHealthSnapshot` is `{ capturedAt: string (ISO), uptimeSeconds: number, cpuCount: number, cpuPercent: number \| null, eventLoopDelayMs: { mean: number, p50: number, p99: number, max: number }, memoryRssMb: number, webConcurrency: number \| null }` at [`apps/server/src/dashboard/dashboardRuntime.types.ts`](../../../apps/server/src/dashboard/dashboardRuntime.types.ts). Auth failures `401 { code: 'unauthorized', reason }` / `403 { code: 'forbidden', reason }`; handler fault → `500 { code: 'internal_error' }`. | WP-439 (dashboard runtime-health signal) | Live snapshot of THIS server process's runtime health, sampled from `process`/`perf_hooks`/`os` (event-loop delay via `monitorEventLoopDelay`, CPU % via `process.cpuUsage()` delta ÷ cpuCount, RSS, uptime, `WEB_CONCURRENCY`). **Reads NO database, NO registry, NO engine** — the pool is threaded only for the admin gate. `cpuPercent` is machine-capacity percent (`null` on the first read); event-loop percentiles cover the window since the previous read (the histogram resets each read). Decision-support for "is one Node process CPU-saturated — is clustering worth it yet?". Same admin gate (`requireAdminSession`), `no-store`, `{ data: T }` envelope. Status-code domain `{200, 401, 403, 500}`. Registered via `registerDashboardRuntimeRoutes(server.router, pool, { requireAdminSession, verifier, accountResolver })` in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs). Cites: D-24258, D-20503, D-11504, D-11804. |
| `Wired` | `GET` | `/api/admin/players/:handle/integrity` | `admin-session-required` | `:handle` path param (string; canonicalized server-side via `trim().toLowerCase()` per WP-101 lookup pattern; required non-empty) | `200 → AdminProfileResponse` `{ accountId: AccountId; handle: string; isSuspended: boolean; recentAuditLog: AuditLogEntry[] }` per WP-107 contract at [`apps/server/src/profile/admin/adminProfile.types.ts`](../../../apps/server/src/profile/admin/adminProfile.types.ts). 4-field surface (admin-only by design); `AuditLogEntry` is `{ actionId: string; actingAccountId: AccountId; actionType: 'suspend' \| 'unsuspend'; reason: string; createdAt: string /* ISO-8601 */ }`. Audit-log tail bounded at `LIMIT 100`, `ORDER BY created_at DESC, action_id DESC` (action_id tiebreaker resolves same-millisecond collisions deterministically). On `requireAdminSession` failure returns `401 { code: 'unauthorized', reason }` / `403 { code: 'forbidden', reason }` / `500 { code: 'lookup_failed', reason }` per the WP-159 closed-union mapping. On missing handle row returns `404 { code: 'not_found', reason }`. On uncaught error returns `500 { code: 'internal_error' }`. | WP-107 (profile integrity / anti-cheat surface) | Read-only; admin-attributed via `requireAdminSession` (WP-159) as the FIRST statement in the handler body (no inline `is_admin` check permitted per WP-159 contract). Profile + audit-log SELECTs share a single `BEGIN ISOLATION LEVEL REPEATABLE READ ... COMMIT` transaction so the response cannot show stale suspension state alongside fresh audit rows. Public / owner profile composition (display name, badges, replays, team affiliations) is reached by admins via the existing WP-102 / WP-104 endpoints — composing those into this response is out of scope to keep the locked admin surface narrow. `Cache-Control: no-store` set as the first statement of every response (including error paths per WP-115 D-11504 lock). Status-code domain locked to `{200, 400, 401, 403, 404, 500}` for this handler. Registered via `registerAdminProfileRoutes(server.router, pool, { requireAdminSession, verifier, accountResolver })` in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs). WP-107 is the FIRST caller of `requireAdminSession`. |
| `Wired` | `POST` | `/api/admin/players/:handle/suspend` | `admin-session-required` | `:handle` path param + JSON body `{ reason: string }` per `AdminActionRequest` at [`apps/server/src/profile/admin/adminProfile.types.ts`](../../../apps/server/src/profile/admin/adminProfile.types.ts). `reason` is `.trim()`-normalized BEFORE validation; trimmed length must be 1-500 chars (whitespace-only rejected with `400 { code: 'invalid_request' }`; over-cap rejected with same 400 — the DB `CHECK (length(reason) BETWEEN 1 AND 500)` in migration 015 is defense-in-depth). | `200 → AdminActionResponse` `{ ok: true; actionId: string }` per WP-107 contract; `actionId` is the bigserial PK of the newly-written `legendary.admin_actions` row serialized as a string at the JSON boundary. On `requireAdminSession` failure returns `401` / `403` / `500` per WP-159's closed-union mapping. On missing handle row returns `404 { code: 'not_found', reason }`. On self-action (`actingAccountId === targetAccountId` after handle resolution) returns `400 { code: 'invalid_request', reason: 'Admins cannot suspend their own account.' }` with **zero audit rows written**. On invalid reason returns `400 { code: 'invalid_request', reason }`. On transaction failure (UPDATE or audit INSERT throws) returns `500 { code: 'internal_error' }`. | WP-107 (profile integrity / anti-cheat surface) | Writes one row to `legendary.admin_actions`. Idempotent at the column (re-suspending an already-suspended account is a no-op on `is_suspended`) but **observable at log** (the audit row IS still written to capture admin intent + reason). The transactional envelope `BEGIN -> UPDATE legendary.players SET is_suspended = TRUE WHERE ext_id = $2 -> INSERT legendary.admin_actions ... RETURNING action_id -> COMMIT` lives in `adminProfile.logic.ts`; the audit INSERT completes BEFORE COMMIT; any step failure rolls back BOTH the column update and the audit write. UPDATE is unconditional set (NOT read-modify-write) — concurrent duplicate writes are acceptable; idempotency is DB-enforced. `Cache-Control: no-store` set as the first statement of every response. Status-code domain locked to `{200, 400, 401, 403, 404, 500}` for this handler. |
| `Wired` | `POST` | `/api/admin/players/:handle/unsuspend` | `admin-session-required` | `:handle` path param + JSON body `{ reason: string }` per `AdminActionRequest` (same shape + same `.trim()` + 1-500 char validation as `/suspend`). | `200 → AdminActionResponse` `{ ok: true; actionId: string }` (same shape as `/suspend`). Same closed-set error mapping: `401` / `403` / `500` on auth, `404` on missing handle, `400` on self-action (verbatim reason `'Admins cannot suspend their own account.'` — same sentence applies to both routes by design so callers see identically-framed errors), `400` on invalid reason, `500 { code: 'internal_error' }` on transaction failure. | WP-107 (profile integrity / anti-cheat surface) | Writes one row to `legendary.admin_actions`. Idempotent at the column (re-unsuspending an already-unsuspended account is a no-op on `is_suspended`) but observable at log (audit row IS still written). Same transactional envelope as `/suspend` with `is_suspended = FALSE` and `action_type = 'unsuspend'`. Same self-action guard, same race-safety lock (unconditional UPDATE), same `Cache-Control: no-store` first-statement lock. Status-code domain locked to `{200, 400, 401, 403, 404, 500}` for this handler. |
| `Wired` | `POST` | `/api/match/autoplay/:matchId/pause` | `guest` | `:matchId` path param (required, non-empty); no request body (bodyless POST — no `koaBody()`) | `AutoplayControlResponse` `{ ok: boolean, paused: boolean, historyLength: number, cursor: number, mode: 'live' \| 'paused', speedMode: '1x' \| '2x' \| '4x' \| 'max', gameOver: boolean, aborted: boolean, uiState?, error?, abortReason? }` via `buildResponse` at [`apps/server/src/autoplay/autoplay.mjs`](../../../apps/server/src/autoplay/autoplay.mjs); `200` on success (no `uiState`); `404 { ok:false, ..., aborted:false, error }` when no autoplay match is running for `:matchId`; `500` envelope on unexpected fault. `mode` present on every response, read only from `controller.getMode()` (D-16304). Pause does NOT reset `speedMode`. `aborted: boolean` is ALWAYS present (`false` during normal play and on a natural game over; `true` after an abnormal bot-loop stop); `abortReason?: string` is present as an OWN key ONLY when `aborted === true` and is a PUBLIC-SAFE full sentence — never raw exception text, stack, DB error, URL, secret, internal id, or path (WP-261, D-24037). | WP-163 (autoplay playback controls — server), EC-200 (speed + game-over), WP-261 (abort surfacing) | Gates the bot loop at the next move boundary. Controller is the per-match `createPlaybackController` instance in the `autoplayControllers` map; rewind buffer + the `aborted` / `abortReason` flags are Class 1 Runtime State, never persisted (D-16306). Registered via `registerAutoplayRoutes(server.router, context)` in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs). |
| `Wired` | `POST` | `/api/match/autoplay/:matchId/resume` | `guest` | `:matchId` path param (required, non-empty); optional JSON body `{ speedMode?: '1x' \| '2x' \| '4x' }` (uses `koaBody()`). When body absent or `speedMode` omitted, preserves the prior speed mode. `'max'` is not a valid client-sent value (server-only). | `AutoplayControlResponse` (see `/pause` row); `200` with `mode: 'live'`, no `uiState`; `404` / `500` envelopes as above. `speedMode` in response reflects the applied speed. Carries `aborted: boolean` (always) + `abortReason?: string` (public-safe full sentence, own key only when `aborted === true`) per WP-261 / D-24037. | WP-163, EC-200 (speed control), WP-261 (abort surfacing) | Releases the pause gate and restores the speed-appropriate inter-move cadence. If `speedMode` was `'max'` (from a prior go-to-end), resume resets it to `'1x'` at base delay. |
| `Wired` | `POST` | `/api/match/autoplay/:matchId/step-forward` | `guest` | `:matchId` path param (required, non-empty); no request body. Optional headers: `X-Player-ID`, `X-Credentials` (when present and valid, rewind `uiState` is filtered for the player's audience instead of spectator) | `AutoplayControlResponse`; `200` — `uiState` present ONLY on the cursor branch (cursor behind live edge), audience-filtered (player when identity headers are valid; spectator otherwise); the live-move branch (cursor at live edge) releases the gate for exactly one real move and returns no `uiState`, `mode: 'paused'` (D-16302 / D-16304); `404` / `500` envelopes as above. Carries `aborted: boolean` (always) + `abortReason?: string` (public-safe full sentence, own key only when `aborted === true`) per WP-261 / D-24037. | WP-163, WP-177 (autoplay rewind requester audience), WP-261 (abort surfacing) | The live-move branch signals the bot loop to advance one move; it does not itself submit a move. Cursor-branch `uiState` is audience-filtered via `resolveRequesterAudience` (D-17701, scoping D-16303). |
| `Wired` | `POST` | `/api/match/autoplay/:matchId/step-back` | `guest` | `:matchId` path param (required, non-empty); no request body. Optional headers: `X-Player-ID`, `X-Credentials` (when present and valid, rewind `uiState` is filtered for the player's audience instead of spectator) | `AutoplayControlResponse`; `200` with audience-filtered `uiState` (player when identity headers are valid; spectator otherwise) and `mode: 'paused'`; `409 { ok:false, ..., error }` when already at the first captured state (`cursor === 0`); `404` / `500` envelopes as above. Carries `aborted: boolean` (always) + `abortReason?: string` (public-safe full sentence, own key only when `aborted === true`) per WP-261 / D-24037. | WP-163, WP-177 (autoplay rewind requester audience), WP-261 (abort surfacing) | Visual rewind one snapshot back over captured history. `uiState` is `filterUIStateForAudience(buildUIState(snapshot.G, ctx), audience)` where `audience` is derived by `resolveRequesterAudience` (D-17701, scoping D-16303); never mutates boardgame.io state. |
| `Wired` | `POST` | `/api/match/autoplay/:matchId/restart` | `guest` | `:matchId` path param (required, non-empty); no request body. Optional headers: `X-Player-ID`, `X-Credentials` (when present and valid, rewind `uiState` is filtered for the player's audience instead of spectator) | `AutoplayControlResponse`; `200` with audience-filtered `uiState` (player when identity headers are valid; spectator otherwise) of the first captured snapshot and `mode: 'paused'`; `409` when no history has been captured yet; `404` / `500` envelopes as above. Carries `aborted: boolean` (always) + `abortReason?: string` (public-safe full sentence, own key only when `aborted === true`) per WP-261 / D-24037. | WP-163, WP-177 (autoplay rewind requester audience), WP-261 (abort surfacing) | Jumps the cursor to the first captured snapshot. `uiState` audience derived by `resolveRequesterAudience` (D-17701, scoping D-16303). Visual only. |
| `Wired` | `POST` | `/api/match/autoplay/:matchId/go-to-end` | `guest` | `:matchId` path param (required, non-empty); no request body | `AutoplayControlResponse`; `200` with `mode: 'live'`, no `uiState`; `404` / `500` envelopes as above. Carries `aborted: boolean` (always) + `abortReason?: string` (public-safe full sentence, own key only when `aborted === true`) per WP-261 / D-24037. | WP-163 (autoplay playback controls — server), WP-261 (abort surfacing) | Jumps the cursor to the live edge, resumes the loop, and fast-forwards at `playbackDelayOverride = 10` ms (D-16307). Client awaits the live broadcast rather than a rewind overlay. |
| `Wired` | `GET` | `/api/match/autoplay/:matchId/status` | `guest` | `:matchId` path param (required, non-empty); no request body (bodyless GET — no `koaBody()`) | `AutoplayControlResponse` metadata envelope `{ ok, paused, historyLength, cursor, mode, speedMode, gameOver, aborted, abortReason? }` via `buildResponse` — NO `uiState` (status is metadata only); `200` when a controller is registered for `:matchId` (regardless of `historyLength`), INCLUDING an aborted-but-still-registered controller during its review window (`aborted: true` + `abortReason`, NOT a 404); `404 { ok:false, paused:false, historyLength:0, cursor:-1, mode:'live', speedMode:'1x', gameOver:false, aborted:false, error }` when no autoplay match is running (incl. a torn-down post-game controller, D-16308); `500` envelope on unexpected fault. `mode` read only from `controller.getMode()` (D-16304). `aborted: boolean` is ALWAYS present; `abortReason?: string` is an OWN key ONLY when `aborted === true` and is a PUBLIC-SAFE full sentence (never raw exception text, stack, DB error, URL, secret, internal id, or path) per WP-261 / D-24037. | WP-165 (autoplay status endpoint — server), WP-261 (abort surfacing) | Side-effect-free read probe — strictly read-only, never mutates controller state. Reuses WP-163's `getController` / `buildResponse` / `handlePlaybackRequest`. The WP-164 client probes this once on mount to tell an autoplay match (`200` → show the playback bar, seed initial state) from a normal live match (`404` → hide the bar) without a URL marker or a side-effectful POST (D-16501). The WP-262 client will poll this to surface the `aborted` state as a "Bot match stopped" banner. Rewound-ness (`cursor < historyLength - 1`) and the transient-404-during-init retry are client-side (WP-164). Registered via `registerAutoplayRoutes(server.router, context)` in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs). |
| `Wired` | `POST` | `/api/analytics/events` | `guest` | JSON body — single-event `AnalyticsEventCapturePayload` `{ event_type: AcquisitionEventType, user_id: string \| null, session_id: string, timestamp: number, properties?: Record<string, string \| number \| boolean \| null \| object> }` OR batch `AnalyticsEventBatchPayload` `{ events: AnalyticsEventCapturePayload[] }` per WP-205 §Locked contract values at [`apps/server/src/analytics/analytics.types.ts`](../../../apps/server/src/analytics/analytics.types.ts). `event_type` is the closed 9-value union (`'direct' \| 'search' \| 'referral' \| 'paid' \| 'signup-start' \| 'signup-complete' \| 'first-match-started' \| 'first-match-completed' \| 'retention-return'`). `session_id` non-empty ≤ 128 chars; `user_id` ≤ 512 chars pre-hash (or null for anonymous); `timestamp` finite number ∈ `[0, currentServerTime + 5 * 60 * 1000]`; `properties` optional object (root must be object, NOT array), depth ≤ 5, leaf values `string \| number \| boolean \| null` only (Date / Map / Set / Function / class instances / BigInt / Symbol REJECTED per D-20501). Body size cap: 8 KB single / 100 KB batch; max 50 events per batch (exceeding → 413). | `202 → { accepted: number }` per WP-205 §Locked contract values; `400 → { code: 'invalid_request' }` on closed-set rejection, malformed payload, depth / leaf-type violation, length-bound violation, or out-of-bounds timestamp; `413 → { code: 'payload_too_large' }` on body cap or batch-event cap; `429 → { code: 'rate_limited' }` when per-IP token bucket exhausted (per-EVENT semantics — a batch of N consumes N tokens; full batch dropped before any parsing / hashing / INSERT per D-20503 tightening); `500 → { code: 'internal_error' }` on operational fault (D-11802 = C). | WP-205 (analytics_events server) | Always-open `guest` capture endpoint per D-20503; per-IP in-memory token bucket (60 events/min) keyed by `ctx.request.ip` (process-local — multi-instance lifecycle deferred); 8 KB single / 100 KB batch body cap; max 50 events/batch. NOT idempotent per D-20503 — duplicate POSTs produce duplicate rows; clients own deduplication. Raw `user_id` is hashed at the route boundary via `hashUserId(user_id, analyticsUserIdSalt)` (SHA-256 of `${user_id}\|${salt}`) BEFORE any INSERT per D-20502; persisted column is `user_id_hash` CHAR(64) (NULL for anonymous events). Salt loaded once at startup via `getAnalyticsUserIdSalt()` — production loud-fails if `ANALYTICS_USER_ID_SALT` env var is unset OR empty; test/dev returns the EC-233 fixed salt + one-shot `console.warn`. INSERTed `ts` is the client-supplied `timestamp` (server clock used ONLY as the validator upper-bound anchor per D-20503). Leakage gate (D-20502 tightening): raw `user_id` MUST NOT appear in any console.{log,info,warn,error} call site or any 4xx error response body — error messages reference field NAMES, not values. Carry-forward: D-9905 (Auth taxonomy), D-11504 (Cache-Control first-statement lock), D-11802 = C (error envelope). `Cache-Control: no-store` set as the literal first statement of every response (including 400 / 413 / 429 / 500 paths). Status-code domain locked to `{202, 400, 413, 429, 500}` for this handler. Registered via `registerAnalyticsRoutes(server.router, pool, { requireAuthenticatedSession, verifier, accountResolver, analyticsUserIdSalt })` in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs). |
| `Wired` | `GET` | `/api/analytics/traffic-sources` | `authenticated-session-required` | Query param `range` — closed `'7d' \| '14d' \| '30d' \| '90d'` (anything else → 400). | `200 → { data: readonly TrafficSource[] }` per WP-205 §Locked contract values at [`apps/server/src/analytics/analytics.types.ts`](../../../apps/server/src/analytics/analytics.types.ts). Each `TrafficSource` is `{ channel: 'direct' \| 'search' \| 'referral' \| 'paid', date: 'YYYY-MM-DD', visitorCount: number, signupCount: number }`. Rows SQL-pre-sorted ASC by `(channel, date)` per D-20501; route MUST NOT call `Array.sort(...)`. NO `source` / `updatedAt` fields per D-20503 — dashboard's future MOCK→LIVE flip wrapper adds those at the call site. `400 → { code: 'invalid_request' }` on range absent / invalid; `401 → { code: 'unauthorized' }` on session failure (closed-set `SessionValidationErrorCode` collapse per D-10403 account-existence-probe defense — 'missing_token' / 'invalid_token' / 'expired_token' / 'unknown_account' all surface as 'unauthorized'); `500 → { code: 'internal_error' }` on operational fault. | WP-205 (analytics_events server) | Operator-only acquisition channel attribution view feeding the WP-203 dashboard `TrafficSourcesWidget`. Wraps `getTrafficSources(database, range)` at [`apps/server/src/analytics/analytics.logic.ts`](../../../apps/server/src/analytics/analytics.logic.ts). Per-session channel attribution rule (D-20501 tightening): the FIRST `(ts ASC)` channel event wins; subsequent channel events IGNORED; no-channel sessions EXCLUDED entirely (NO `direct` fallback bucket). SQL implementation uses `ROW_NUMBER() OVER (PARTITION BY session_id ORDER BY ts ASC)` window function — `MIN(ts)` subquery + GROUP BY with implicit tie-breaking is HARD FAIL. UTC date bucket via `(ts AT TIME ZONE 'UTC')::date`. `visitorCount = COUNT(DISTINCT session_id)` per (channel, date); `signupCount = COUNT(DISTINCT user_id_hash)` joining attributed-channel sessions to subsequent `signup-complete` events in the SAME session. Carry-forward: D-9905, D-10403 (auth collapse), D-11504 (Cache-Control lock), D-11802 = C (error envelope). `Cache-Control: no-store` set as the literal first statement of every response (including 400 / 401 / 500 paths). Status-code domain locked to `{200, 400, 401, 500}` for this handler. |
| `Wired` | `GET` | `/api/analytics/activation-funnel` | `authenticated-session-required` | Query param `range` — closed `'7d' \| '14d' \| '30d' \| '90d'` (anything else → 400). | `200 → { data: readonly ActivationFunnelStep[] }` per WP-205 §Locked contract values. Each `ActivationFunnelStep` is `{ step: 'signup-start' \| 'signup-complete' \| 'first-match-started' \| 'first-match-completed', date: 'YYYY-MM-DD', count: number }`. Rows SQL-pre-sorted ASC by `(step, date)` per D-20501; route MUST NOT re-sort. NO `source` / `updatedAt` fields per D-20503. `400 → { code: 'invalid_request' }` on range invalid / absent; `401 → { code: 'unauthorized' }` per D-10403 collapse; `500 → { code: 'internal_error' }` on operational fault. | WP-205 (analytics_events server) | Operator-only activation funnel view feeding the WP-203 dashboard `ActivationFunnelWidget`. Wraps `getActivationFunnel(database, range)` at [`apps/server/src/analytics/analytics.logic.ts`](../../../apps/server/src/analytics/analytics.logic.ts). Per-step × per-day `COUNT(DISTINCT user_id_hash)` aggregation; anonymous events (NULL `user_id_hash`) excluded from DISTINCT user-id-hash counts via SQL `WHERE user_id_hash IS NOT NULL`. UTC date bucket via `(ts AT TIME ZONE 'UTC')::date`. Carry-forward: D-9905, D-10403, D-11504, D-11802 = C. `Cache-Control: no-store` first-statement lock; status-code domain `{200, 400, 401, 500}`. |
| `Wired` | `GET` | `/api/analytics/retention-cohorts` | `authenticated-session-required` | Query param `cohortCount` — integer in `[1, 26]`; defaults to 8 if absent (out-of-range → 400). | `200 → { data: readonly RetentionCohort[] }` per WP-205 §Locked contract values. Each `RetentionCohort` is `{ cohortWeek: 'YYYY-Www' (ISO 8601), cohortSize: number, day1ReturnCount: number, day7ReturnCount: number }`. Rows SQL-pre-sorted ASC by `cohortWeek` per D-20501 (Unicode code-unit comparison byte-identical to ISO-week lexical order); route MUST NOT re-sort. NO `source` / `updatedAt` fields per D-20503. `400 → { code: 'invalid_request' }` on cohortCount out of range / non-integer; `401 → { code: 'unauthorized' }` per D-10403 collapse; `500 → { code: 'internal_error' }` on operational fault. | WP-205 (analytics_events server) | Operator-only retention cohort view feeding the WP-203 dashboard `RetentionCohortsWidget`. Wraps `getRetentionCohorts(database, cohortCount)` at [`apps/server/src/analytics/analytics.logic.ts`](../../../apps/server/src/analytics/analytics.logic.ts). Weekly cohorts via ISO 8601 `to_char(date_trunc('week', ts), 'IYYY-"W"IW')`; `cohortSize = COUNT(DISTINCT user_id_hash)` from `signup-complete` events. Day-N return = `COUNT(DISTINCT user_id_hash)` of cohort members with ANY `event_type != 'signup-complete'` event in `[signup_ts + (N-1) days, signup_ts + N days)` per the v1 coarse return definition (D-20501) — channel events, activation events, AND `retention-return` events all count; only `signup-complete` itself is excluded by definition. Per-class filtering (e.g., "only `first-match-*` counts as a real return") is a future tuning WP. Carry-forward: D-9905, D-10403, D-11504, D-11802 = C, D-19908 (cohort week ISO label discipline). `Cache-Control: no-store` first-statement lock; status-code domain `{200, 400, 401, 500}`. |
| `Wired` | `POST` | `/api/sweep/runs` | `guest` | JSON body — `SweepRunPayload` `{ runId: string (non-empty, ≤ 128 chars), startedAt: string (ISO-8601 parseable by `new Date(value)`), cellCount: number (integer, 0 ≤ n ≤ 10000), anomalyCounts: Record<SweepAnomalyClass, number>, manifestBlob?: unknown }` per WP-209 §Locked Type Contracts at [`apps/server/src/sweep/sweep.types.ts`](../../../apps/server/src/sweep/sweep.types.ts). `SweepAnomalyClass` is the closed 4-value union (`'endgame-reached' \| 'not-endgame' \| 'escaped-villain-cap' \| 'fatal'`) imported from `@legendary-arena/game-engine` per WP-195 D-19502 — server validator rejects `anomalyCounts` whose keys fall outside `SWEEP_ANOMALY_CLASSES` with `400`. Body size cap: 5 MB; cell-count cap: 10000 (defense-in-depth before INSERT). `X-Sweep-Token` header MUST equal `process.env.SWEEP_SUBMIT_TOKEN` byte-for-byte via `node:crypto.timingSafeEqual` (preceded by a `Buffer.byteLength` length-equality precheck — Node's `timingSafeEqual` throws `RangeError` on unequal-length buffers) per D-20702. | `201 → { data: { runId: string, accepted: true } }` per WP-209 §Locked Type Contracts; `400 → { data: [], error: 'invalid_request' }` on payload shape violation (bad runId / startedAt / cellCount / out-of-set anomaly key); `401 → { data: [], error: 'unauthorized' }` on missing or mismatched `X-Sweep-Token` header (fires BEFORE any DB I/O); `409 → { data: [], error: 'conflict' }` on duplicate `run_id` (PRIMARY KEY collision — existing row UNCHANGED, no UPSERT, no `ON CONFLICT DO NOTHING` per D-20701); `413 → { data: [], error: 'payload_too_large' }` on body size cap (5 MB) or cell-count cap (10000) exceeded; `500 → { data: [], error: 'internal_error' }` on operational fault. | WP-209 | CI-only submission endpoint for the WP-194 setup-matrix sweep + WP-195 manifest anomaly oracle pipeline. Auth via shared-secret header (`X-Sweep-Token`, NOT a Bearer scheme); production loud-fails at startup when `SWEEP_SUBMIT_TOKEN` env var is unset or empty (mirrors `ANALYTICS_USER_ID_SALT` precedent — `apps/server/src/server.mjs` `loadSweepSubmitToken` raises a full-sentence error). Token sourced from Render env var `SWEEP_SUBMIT_TOKEN` `sync: false` (operator-set in dashboard) + GitHub Actions secret of the same name. Idempotent by `run_id` PRIMARY KEY: duplicate POSTs return 409 with the existing row byte-identical pre/post (the GitHub Actions retry path consumes this contract). INSERT statement enumerates the 5 non-default columns explicitly (`(run_id, started_at, cell_count, anomaly_counts, manifest_blob)`); `submitted_at` is OMITTED so the column DEFAULT `NOW()` populates the submission wall-clock anchor server-side. `manifest_blob` is the raw `ManifestClassification` JSON (forensic only; nullable — smaller payloads may omit). `Cache-Control: no-store` set as the literal first statement of the handler body per D-11504 lock. Status-code domain locked to `{201, 400, 401, 409, 413, 500}` for this handler. Registered via `registerSweepRoutes(server.router, pool, { requireAuthenticatedSession, verifier, accountResolver, sweepSubmitToken })` in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs). Carry-forward: D-9905 (Auth taxonomy), D-11504 (Cache-Control first-statement lock), D-11802 = C (error envelope), D-11804 (catalog update obligation), D-19502 (sweep anomaly 4-class closed taxonomy). Cites: D-20701, D-20702, D-20704. |
| `Wired` | `GET` | `/api/sweep/latest` | `authenticated-session-required` | (no body; bearer session token via Authorization header per WP-112 contract; **query parameters are IGNORED in v1** — `?limit`, `?since`, `?runId`, etc. do not alter the response shape per WP-209 §Non-Negotiable Constraints) | `200 → { data: { latest: SweepRunSummary \| null, recentRuns: readonly SweepRunSummary[] } }` where `SweepRunSummary` is `{ runId: string, submittedAt: string (ISO-8601 UTC), startedAt: string (ISO-8601 UTC), cellCount: number, anomalyCounts: Record<SweepAnomalyClass, number> }` per WP-209 §Locked Type Contracts at [`apps/server/src/sweep/sweep.types.ts`](../../../apps/server/src/sweep/sweep.types.ts). `recentRuns.length ≤ 30`, ordered `submitted_at DESC` per the SQL pre-sorted invariant (`ORDER BY submitted_at DESC LIMIT 30` literal); when the table is non-empty `latest === recentRuns[0]` (invariant — handler builds both from the same SQL result). `SweepRunSummary` deliberately EXCLUDES `manifestBlob` — the blob is forensic-only and never shipped on the operator dashboard read path. `data` is an OBJECT (NOT the WP-205 `data: readonly T[]` array shape) because the endpoint serves two semantically distinct payloads — `latest` summary + up to 30 `recentRuns` — in one response. `401 → { data: [], error: 'unauthorized' }` on session failure (closed-set `SessionValidationErrorCode` collapse per D-10403 — 'missing_token' / 'invalid_token' / 'expired_token' / 'unknown_account' all surface as 'unauthorized'); `500 → { data: [], error: 'internal_error' }` on operational fault. | WP-209 | Operator-only read of the latest sweep run + last 30 runs feeding the WP-210 `SweepHealthWidget` on the dashboard `/system` page. Reuses the WP-115 `pg.Pool` + WP-112 `requireAuthenticatedSession` orchestrator. "Latest" ordering dimension is greatest `submitted_at` (NOT `started_at`) so back-fills and out-of-order submissions sort by submission wall-clock — answers the operator question "what's the most recent thing the dashboard knows about?". Backed by the BTREE index `sweep_runs_submitted_at_desc_idx`. `Cache-Control: no-store` set as the literal first statement of the handler body per D-11504 lock. Status-code domain locked to `{200, 401, 500}` for this handler. Registered via `registerSweepRoutes(...)` in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs). Carry-forward: D-9905, D-10403 (auth collapse), D-11504, D-11802 = C, D-11804, D-19502. Cites: D-20701, D-20702. |
| `Wired` | `POST` | `/api/inspection/reports` | `guest` | JSON body — `InspectionReportPayload` `{ reportId: string (non-empty, ≤ 160 chars; form <sweepRunId>-<generatedAtIsoCompact>), sweepRunId: string (non-empty, ≤ 128 chars), generatedAt: string (ISO-8601 parseable by `new Date(value)`), verdict: 'PASS' \| 'FAIL' (IGNORED — server recomputes), findings: InspectionFinding[] (0..500; each `{ severity: 'P0'\|'P1'\|'P2', anomalyClass: string, cellId: string\|null, description: string ≤ 1000 chars, route: 'Builder'\|'Architect' }`) }` per WP-231 §Locked Type Contracts at [`apps/server/src/inspection/inspection.types.ts`](../../../apps/server/src/inspection/inspection.types.ts). `anomalyClass` is an OPAQUE string (no engine `SweepAnomalyClass` import — D-23103). Body size cap: 5 MB; findings cap: 500 (bounds runaway LLM output before INSERT). `X-Inspection-Token` header MUST equal `process.env.INSPECTION_SUBMIT_TOKEN` byte-for-byte via the shared `validateSharedSecret` helper (`node:crypto.timingSafeEqual` after a `Buffer.byteLength` length-equality precheck) per D-23101. | `201 → { data: { reportId: string, accepted: true } }`; `400 → { data: [], error: 'invalid_request' }` on payload/finding shape violation (bad reportId / sweepRunId / generatedAt / out-of-set severity or route / findings > 500); `401 → { data: [], error: 'unauthorized' }` on missing or mismatched `X-Inspection-Token` (fires BEFORE any DB I/O); `409 → { data: [], error: 'conflict' }` on duplicate `report_id` (PRIMARY KEY collision — existing row UNCHANGED, no UPSERT); `413 → { data: [], error: 'payload_too_large' }` on body size cap (5 MB) exceeded; `500 → { data: [], error: 'internal_error' }` on operational fault. | WP-231 | CI-only submission endpoint for the nightly Inspector triage. The SERVER is the sole authority for the derived fields — `verdict` AND the stored `p0/p1/p2` counts are RECOMPUTED from `findings` at insert time and any client-supplied derived values are IGNORED (not compared, not stored, NO 400 on disagreement) per D-23101; the durable verdict (`deriveVerdict`: FAIL iff any P0/P1) is reproducible from the stored findings even though the LLM findings themselves are nondeterministic (D-23102). Auth via shared-secret header (`X-Inspection-Token`, NOT a Bearer scheme); production loud-fails at startup when `INSPECTION_SUBMIT_TOKEN` is unset (mirrors the `SWEEP_SUBMIT_TOKEN` / `ANALYTICS_USER_ID_SALT` precedent — `apps/server/src/server.mjs` `loadInspectionSubmitToken`). Token sourced from Render env var `INSPECTION_SUBMIT_TOKEN` `sync: false` + GitHub Actions secret of the same name. Idempotent by `report_id` PRIMARY KEY; re-triage across runs gets a fresh `generatedAt` → fresh `report_id` → distinct row (no 409). INSERT enumerates the 8 non-default columns explicitly; `submitted_at` is OMITTED (DEFAULT `NOW()`). `Cache-Control: no-store` first statement per D-11504. Status-code domain locked to `{201, 400, 401, 409, 413, 500}`. Registered via `registerInspectionRoutes(server.router, pool, { requireAuthenticatedSession, verifier, accountResolver, inspectionSubmitToken })` in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs). Carry-forward: D-9905, D-11504, D-11802 = C, D-11804. Cites: D-23101, D-23102. |
| `Wired` | `GET` | `/api/inspection/latest` | `authenticated-session-required` | (no body; bearer session token via Authorization header per WP-112 contract; **query parameters are IGNORED in v1** — they do not alter the response shape) | `200 → { data: { latest: InspectionReportSummary \| null, recentReports: readonly InspectionReportSummary[] } }` where `InspectionReportSummary` is `{ reportId: string, sweepRunId: string, submittedAt: string (ISO-8601 UTC), generatedAt: string (ISO-8601 UTC), verdict: 'PASS' \| 'FAIL', counts: { p0: number, p1: number, p2: number }, findings: readonly InspectionFinding[] }` per WP-231 §Locked Type Contracts at [`apps/server/src/inspection/inspection.types.ts`](../../../apps/server/src/inspection/inspection.types.ts). `recentReports.length ≤ 30`, ordered `submitted_at DESC` (`ORDER BY submitted_at DESC LIMIT 30` literal); when non-empty `latest === recentReports[0]` (handler builds both from the same SQL result). `data` is an OBJECT (NOT `data: readonly T[]`) because the endpoint serves two payloads in one response (same deviation WP-209's `GET /api/sweep/latest` justified). `401 → { data: [], error: 'unauthorized' }` on session failure (`SessionValidationErrorCode` collapse per D-10403); `500 → { data: [], error: 'internal_error' }` on operational fault. | WP-231 | Operator-only read of the latest triage report + last 30, feeding the future dashboard triage surface (paired follow-up WP, mirrors WP-209 → WP-210). Reuses the WP-115 `pg.Pool` + WP-112 `requireAuthenticatedSession`. "Latest" ordering dimension is greatest `submitted_at`. Backed by the BTREE index `inspection_reports_submitted_at_desc_idx`. `Cache-Control: no-store` first statement per D-11504. Status-code domain locked to `{200, 401, 500}`. Registered via `registerInspectionRoutes(...)` in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs). Carry-forward: D-9905, D-10403, D-11504, D-11802 = C, D-11804. Cites: D-23101. |
| `Wired` | `POST` | `/api/handoffs/sync` | `guest` | Empty JSON body (`{}` — any body is ignored; the endpoint always syncs the latest inspection report). `X-Handoff-Token` header MUST equal `process.env.HANDOFF_SUBMIT_TOKEN` byte-for-byte via the shared `validateSharedSecret` helper (`node:crypto.timingSafeEqual` after a `Buffer.byteLength` length-equality precheck) per D-23203. | `200 → { data: HandoffSyncSummary }` where `HandoffSyncSummary` is `{ reportId: string \| null, findingCount: number, created: number, unchanged: number }` per WP-232 §Locked Type Contracts at [`apps/server/src/handoff/handoff.types.ts`](../../../apps/server/src/handoff/handoff.types.ts); `created + unchanged === findingCount` (empty `inspection_reports` → `reportId: null` + all-zero counts). `401 → { data: [], error: 'unauthorized' }` on missing or mismatched `X-Handoff-Token` (fires BEFORE any DB I/O); `413 → { data: [], error: 'payload_too_large' }` on the 64 KB body cap; `500 → { data: [], error: 'internal_error' }` on operational fault. | WP-232 | CI-only handoff-queue materializer for the nightly Inspector triage. Reads the latest inspection report via the exported `fetchLatestInspectionReport(database)` ONLY — this module issues NO direct SQL against `legendary.inspection_reports` (one source of truth — D-23203) — and inserts one `open` handoff row per finding via `INSERT ... ON CONFLICT (handoff_id) DO NOTHING` (idempotent + status-preserving: a re-sync of a report that already has rows is a normal no-op — a `claimed` row stays `claimed` — the deliberate opposite of the inspection POST's no-UPSERT-409 posture). `handoffId` is the deterministic PK `<reportId>#<findingIndex>`. Single-latest-report sync window: intermediate reports between two syncs are not guaranteed to be materialized (by design). Auth via shared-secret header (`X-Handoff-Token`, NOT a Bearer scheme); production loud-fails at startup when `HANDOFF_SUBMIT_TOKEN` is unset (`apps/server/src/server.mjs` `loadHandoffSubmitToken`). Token sourced from Render env var `HANDOFF_SUBMIT_TOKEN` `sync: false` + GitHub Actions secret of the same name (the `inspection-nightly.yml` `handoffs:sync` trailing step). PLUMBING ONLY (D-23202): the server records lifecycle state; it runs no autonomous agent. `Cache-Control: no-store` first statement per D-11504. Status-code domain locked to `{200, 401, 413, 500}`. Registered via `registerHandoffRoutes(server.router, pool, { requireAuthenticatedSession, verifier, accountResolver, handoffSubmitToken })` in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs). Carry-forward: D-9905, D-11504, D-11802 = C, D-11804. Cites: D-23203, D-23201. |
| `Wired` | `POST` | `/api/handoffs/transition` | `guest` | JSON body — `HandoffTransitionPayload` `{ handoffId: string (non-empty, ≤ 320 chars; travels in the BODY, NOT the path), toStatus: HandoffStatus (`'open'\|'claimed'\|'fix-proposed'\|'escalated'\|'resolved'\|'wont-fix'` — a `HANDOFF_STATUSES` member), branchRef?: string\|null (REQUIRED non-empty ≤ 200 chars when `toStatus === 'fix-proposed'`), amendmentRequest?: string\|null (REQUIRED non-empty ≤ 2000 chars when `toStatus === 'escalated'`) }` per WP-232 §Locked Type Contracts at [`apps/server/src/handoff/handoff.types.ts`](../../../apps/server/src/handoff/handoff.types.ts). Body size cap: 64 KB. `X-Handoff-Token` header MUST equal `process.env.HANDOFF_SUBMIT_TOKEN` byte-for-byte via the shared `validateSharedSecret` helper per D-23202. | `200 → { data: { handoff: HandoffRecord } }` (the updated row) per WP-232 §Locked Type Contracts; `400 → { data: [], error: 'invalid_request' }` on payload shape violation (bad `handoffId` / out-of-set `toStatus` / missing `branchRef` on `fix-proposed` / missing `amendmentRequest` on `escalated`); `401 → { data: [], error: 'unauthorized' }` on missing or mismatched `X-Handoff-Token` (fires BEFORE any DB I/O); `404 → { data: [], error: 'not_found' }` on an unknown `handoffId`; `409 → { data: [], error: 'conflict' }` on an off-table transition OR a concurrent transition that already moved the row (status UNCHANGED); `413 → { data: [], error: 'payload_too_large' }` on the 64 KB body cap; `500 → { data: [], error: 'internal_error' }` on operational fault. | WP-232 | Applies one server-validated, concurrency-safe lifecycle transition to a single handoff. The server is the SOLE authority for `status` + `updated_at` (D-23202): `status` changes only via a transition the locked table permits (`open→claimed`; `claimed→fix-proposed\|escalated\|wont-fix`; `fix-proposed→resolved\|claimed`; `escalated→claimed\|resolved\|wont-fix`; `resolved`/`wont-fix` terminal); an off-table pair is a 409, not a silent overwrite. Validation-before-read: auth (401) → body parse/size (413/400) → shape (400) → conditional-required (400) all complete BEFORE any DB access. The mutation is a single guarded `UPDATE ... WHERE handoff_id = $1 AND status = $expectedStatus RETURNING *`; if it affects 0 rows (a parallel transition moved the row) the handler re-reads once and maps absent → 404 / different-status → 409 (no lost update; no `FOR UPDATE` / advisory lock / multi-statement txn). Field persistence: `branch_ref` written only on `→ fix-proposed`, `amendment_request` only on `→ escalated`; both PRESERVED (never nulled) on every other transition (auditability). PLUMBING ONLY (D-23202): `branchRef` / `amendmentRequest` are references the server STORES, never actions it performs — it creates no git branch, opens no PR, edits no WP spec. Auth via shared-secret header (`X-Handoff-Token`). `Cache-Control: no-store` first statement per D-11504. Status-code domain locked to `{200, 400, 401, 404, 409, 413, 500}`. Registered via `registerHandoffRoutes(...)` in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs). Carry-forward: D-9905, D-11504, D-11802 = C, D-11804. Cites: D-23202, D-23201. |
| `Wired` | `GET` | `/api/handoffs/latest` | `authenticated-session-required` | (no body; bearer session token via Authorization header per WP-112 contract; **query parameters are IGNORED in v1** — they do not alter the response shape) | `200 → { data: { reportId: string \| null, handoffs: readonly HandoffRecord[], counts: { open, claimed, fixProposed, escalated, resolved, wontFix } } }` where `HandoffRecord` is `{ handoffId, reportId, sweepRunId, findingIndex, severity: 'P0'\|'P1'\|'P2', route: 'Builder'\|'Architect', anomalyClass: string (opaque — no engine union, D-23103), cellId: string\|null, description: string, status: HandoffStatus, branchRef: string\|null, amendmentRequest: string\|null, createdAt, updatedAt }` per WP-232 §Locked Type Contracts at [`apps/server/src/handoff/handoff.types.ts`](../../../apps/server/src/handoff/handoff.types.ts). The "latest report" is resolved deterministically via `ORDER BY created_at DESC, report_id DESC LIMIT 1` (greatest `created_at`, tie-broken by lexicographically greatest `report_id`); that report's rows are returned `ORDER BY finding_index ASC, handoff_id ASC` with a query-level `LIMIT 500`; `counts` is computed over the returned rows and sums to `handoffs.length`. `reportId` is `null` when `finding_handoffs` is empty. `401 → { data: [], error: 'unauthorized' }` on session failure (`SessionValidationErrorCode` collapse per D-10403); `500 → { data: [], error: 'internal_error' }` on operational fault. | WP-232 | Operator + Builder/Architect read of the latest report's handoff-chain state, feeding the future dashboard "status of each finding through the chain" view (paired follow-up). Reuses the WP-115 `pg.Pool` + WP-112 `requireAuthenticatedSession`. `data` is an OBJECT (NOT `data: readonly T[]`) — the latest report's handoffs + per-status counts in one response (same deviation WP-209's `GET /api/sweep/latest` justified). `Cache-Control: no-store` first statement per D-11504. Status-code domain locked to `{200, 401, 500}`. Registered via `registerHandoffRoutes(...)` in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs). Carry-forward: D-9905, D-10403, D-11504, D-11802 = C, D-11804. Cites: D-23201. |
| `Wired` | `POST` | `/api/handoffs/verify` | `guest` | Empty JSON body (`{}` — any body is ignored; the endpoint always verifies the latest inspection report). Body size cap: 64 KB. `X-Handoff-Token` header MUST equal `process.env.HANDOFF_SUBMIT_TOKEN` byte-for-byte via the shared `validateSharedSecret` helper (`node:crypto.timingSafeEqual` after a `Buffer.byteLength` length-equality precheck) per D-23302 (REUSED — no new token, no `render.yaml` / `.env.example` change). | `200 → { data: HandoffVerifySummary }` where `HandoffVerifySummary` is `{ reportId: string \| null, verified: number, regressed: number, skipped: number }` per WP-233 §Locked Type Contracts at [`apps/server/src/handoff/handoff.types.ts`](../../../apps/server/src/handoff/handoff.types.ts); the accounting invariant is `verified + regressed + skipped <= ` the count of `fix-proposed` handoffs read at the start (the delta is the concurrent misses the per-transition catch excludes, reported in no counter; empty `inspection_reports` → `reportId: null` + all-zero). `401 → { data: [], error: 'unauthorized' }` on missing or mismatched `X-Handoff-Token` (fires BEFORE any DB I/O); `413 → { data: [], error: 'payload_too_large' }` on the 64 KB body cap; `500 → { data: [], error: 'internal_error' }` on operational fault. | WP-233 | CI-only closed-loop verifier — the pipeline's first autonomous-ACTION surface (D-23302): it actively transitions handoffs without a human, but writes NO code, opens NO PR, edits NO spec. Reads the latest (re-sweep) inspection report via the exported `fetchLatestInspectionReport(database)` ONLY — this module issues NO direct SQL against the inspection-reports table (one source of truth — D-23203 carry-forward) — loads every `fix-proposed` handoff (a `WHERE status = 'fix-proposed'` SELECT against its own `finding_handoffs` table), and for each whose origin `reportId` DIFFERS from the latest report's `reportId` (the load-bearing newer-report-only guard — a genuine re-sweep has run since the fix) diffs its snapshotted `(cellId, anomalyClass)` against the latest findings: anomaly ABSENT ⇒ transition `fix-proposed → resolved` (verified); anomaly PRESENT ⇒ transition `fix-proposed → claimed` (regression — the re-open edge WP-232 reserved). Matching is strict `===` (a run-level finding/handoff carries `cellId: null`; `null === null` matches a run-level anomaly; NO coercion / `String()` / `"null"`) and existential (≥ 1 match ⇒ regressed, 0 ⇒ verified — multiplicity never weighted); the LLM-nondeterministic finding `description` is never compared (D-23102 carry-forward) and `anomalyClass` stays an opaque string (no engine union — D-23103). Same-report handoffs are `skipped` (left `fix-proposed`). Reuses the WP-232 6-status lifecycle + transition table VERBATIM — NO new state (operator Q4 lock — D-23301) — and transitions go through the existing `applyHandoffTransition` guarded UPDATE, so the diff is server-authoritative and concurrency-safe: a row a parallel transition advanced makes `applyHandoffTransition` throw (`HandoffTransitionError` / `HandoffNotFoundError`), which the verify loop catches and excludes from every counter (never a lost update, double-act, or 500). Idempotent: a second run against the same latest report finds no eligible `fix-proposed` rows (all-zero); a run against an unchanged report only `skips`. Auth via shared-secret header (`X-Handoff-Token`, REUSED from WP-232 — no new token); the additive trailing `handoffs:verify` step in `inspection-nightly.yml` (after `handoffs:sync`, `if: success()`) calls it on post-merge / next-nightly timing (D-23302). `Cache-Control: no-store` first statement per D-11504. Status-code domain locked to `{200, 401, 413, 500}`. Registered via `registerHandoffRoutes(server.router, pool, { requireAuthenticatedSession, verifier, accountResolver, handoffSubmitToken })` in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs) (signature unchanged — additive route registration). Carry-forward: D-9905, D-11504, D-11802 = C, D-11804. Cites: D-23301, D-23302. |
| `Wired` | `GET` | `/api/sweep/runs/latest` | `guest` | (no body; `X-Sweep-Token` shared-secret header MUST equal `process.env.SWEEP_SUBMIT_TOKEN` byte-for-byte via the shared `validateSharedSecret` helper — the CI triage consumer reuses the sweep token family, no third secret; **query parameters are IGNORED**) | `200 → { data: { run: { runId: string, submittedAt: string (ISO-8601 UTC), startedAt: string (ISO-8601 UTC), cellCount: number, anomalyCounts: Record<string, number>, manifestBlob: unknown \| null } \| null } }` — the latest sweep run INCLUDING its forensic `manifestBlob` (the operator dashboard `GET /api/sweep/latest` stays blob-free and byte-unchanged); `run` is `null` only when `sweep_runs` is empty. `401 → { data: [], error: 'unauthorized' }` on missing or mismatched `X-Sweep-Token` (fires BEFORE any DB I/O); `500 → { data: [], error: 'internal_error' }` on operational fault. | WP-231 | CI-only blob-read endpoint realizing WP-209's explicitly-deferred `GET /api/sweep/runs/:runId` blob retrieval (D-23103), scoped to the Inspector triage consumer (`scripts/inspection-fetch.mjs`). Additive to the sweep module; the existing `POST /api/sweep/runs` + `GET /api/sweep/latest` handlers are behavior-unchanged (the POST's only change is routing its token check through the shared `validateSharedSecret` helper). `Cache-Control: no-store` first statement per D-11504. Status-code domain locked to `{200, 401, 500}`. Registered via `registerSweepRoutes(...)` in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs). Carry-forward: D-9905, D-11504, D-11802 = C, D-11804. Cites: D-23103, D-20702. |
| `Library-only` | `n/a` | `processStripeEvent` (call site under `apps/server/src/billing/processStripeEvent.logic.ts`) | (n/a — internal library function) | `args: { eventRecord: StripeEventRecord; billingConfig: BillingConfig; database: DatabaseClient }` per the WP-134 §A locked signature. | `Promise<FulfillmentResult>` per the locked `Result<T>` shape: `{ ok: true; value: FulfillmentSuccess }` carries `entitlementGranted: boolean`, `entitlementKey: EntitlementKey \| null`, `sessionId: string \| null`, and a `FulfillmentSuccessReason` closed-set string; `{ ok: false; reason: string; code: FulfillmentErrorCode }` carries the failure-class code with a full-sentence diagnostic. | WP-134 | Called synchronously by the webhook handler post-WP-134; also reachable via `scripts/process-stripe-events.mjs` for recovery. Pure DB-mutation path: zero outbound Stripe API calls, zero `process.env` reads, zero engine / registry / preplan imports. Phase 0a structural type guard narrows `payload: unknown` BEFORE any field access (no type-cast escapes per the EC-140 §6 grep gate). Phase 2 five-axis cross-validation: `intent_status === 'open'` + session row presence + `client_reference_id` ↔ `account_id` + `metadata.entitlementKey` ↔ `entitlement_key` + `priceAllowlist[priceId]` ↔ `entitlement_key`. Phase 3 transactional fulfillment write (BEGIN → INSERT entitlement → UPDATE session → UPDATE event → COMMIT); step 10 (event row's `processed_at = now()`) is the LAST write so a partial-write crash leaves `processed_at = NULL` and the recovery script re-picks the row. The `accountId → player_id` resolution at Phase 3 step 6 mirrors the WP-104 / EC-135 `ownerProfile.logic.ts:123` two-query precedent — `legendary.entitlements.player_id` is `bigint` FK to `legendary.players(player_id)`, NOT `text` to `ext_id`. Failure-class lock: every `Result.fail` writes `process_error` (soft-cap 2000 chars) + leaves `processed_at = NULL` so the recovery script's `WHERE processed_at IS NULL` selector continues to surface the row. |

### boardgame.io Built-In Lobby Endpoints

These endpoints are registered by `Server({games:[LegendaryGame]})` at
[`apps/server/src/server.mjs:110-118`](../../../apps/server/src/server.mjs).
Their request and response shapes are owned by the upstream `boardgame.io`
framework; this catalog documents them descriptively only. The CLI clients
under `apps/server/scripts/{create-match,list-matches,join-match}.mjs` are
the de-facto contract today (per `.claude/skills/legendary-server/SKILL.md` §"CLI Scripts" —
they are clients of the catalog, not catalog entries).

| Status | Method | Path | Auth | Request Schema (file ref) | Response Schema (file ref) | Authorizing WP | Notes |
|---|---|---|---|---|---|---|---|
| `Wired` | `POST` | `/games/legendary-arena/create` | `authenticated-session-required` | boardgame.io match-create payload (`{ numPlayers, setupData: MatchSetupConfig }` per [`docs/ai/ARCHITECTURE.md` §"Match Lifecycle"](../ARCHITECTURE.md)) | `{ matchID }` per boardgame.io defaults | WP-011 (match creation / lobby flow); WP-308 (hard gate) | Error semantics owned by boardgame.io (per D-11802 = C). The `setupData` payload is the 9-field `MatchSetupConfig` per `00.2 §8.1`; canonical field names (`schemeId`, `mastermindId`, etc.) match `00.2` verbatim. **WP-308 (hard gate — D-24094):** an app-layer Koa guard (`nativeLobbyGuard.ts`), mounted before the bgio lobby router, rejects this native POST with `401` unless the request carries a valid authenticated session **or** the process-local internal-delegation secret. The arena-client reaches match creation via the guarded `POST /api/match/create`; the server-internal delegation (matchGate + autoplay loopback) is admitted via the secret. Supersedes the D-24093 soft-gate posture (route no longer `guest`/open). |
| `Wired` | `GET` | `/games/legendary-arena` | `guest` | Query params per boardgame.io defaults | `{ matches: [...] }` per boardgame.io defaults | WP-012 (match list / join / reconnect) | List of active matches. Error semantics owned by boardgame.io (per D-11802 = C). Consumed by `apps/server/scripts/list-matches.mjs` and the arena-client lobby. |
| `Wired` | `POST` | `/games/legendary-arena/{matchID}/join` | `authenticated-session-required` | `{ playerID, playerName }` per boardgame.io defaults | `{ playerCredentials }` per boardgame.io defaults | WP-011 (match creation), WP-012 (join / reconnect); WP-308 (hard gate) | Players join via this built-in. Error semantics owned by boardgame.io (per D-11802 = C). After join the match begins in the `lobby` phase per [`docs/ai/ARCHITECTURE.md`](../ARCHITECTURE.md). **WP-308 (hard gate — D-24094):** the same `nativeLobbyGuard.ts` rejects this native POST with `401` unless it carries a valid authenticated session **or** the process-local internal-delegation secret. The arena-client reaches join via the guarded `POST /api/match/join`; the autoplay loop and matchGate loopback are admitted via the secret. Supersedes the D-24093 soft-gate posture. |
| `Wired` | `POST` | `/api/match/create` | `authenticated-session-required` | `{ numPlayers, setupData: MatchSetupConfig }` (9-field composition per `00.2 §8.1`) | `{ matchID }` | WP-307 (multiplayer-play auth gate) | Guarded front door to the native lobby create (D-24092 policy, D-24093 mechanism). Runs `requireAuthenticatedSession` first — `401` with a full-sentence `{ error }` on an absent/invalid session — then delegates server-internal to `/games/legendary-arena/create` (carrying the WP-308 internal-delegation secret). No game logic (server wires, engine decides). Since WP-308 / D-24094 the native route is itself gated, so the account requirement now holds by every path — this is the official signed-in front door, not merely the enforced one. |
| `Wired` | `POST` | `/api/match/join` | `authenticated-session-required` | `{ matchID, playerID, playerName }` | `{ playerCredentials }` | WP-307 (multiplayer-play auth gate); seat→account side-effect WP-333 | Guarded front door to the native lobby join. `matchID` travels in the request body (the native route carries it in the URL path). `401` on an absent/invalid session, `400` on a missing `matchID`, then delegates to `/games/legendary-arena/{matchID}/join`. **Side-effect (WP-333 / D-24119 / D-24120):** on a successful join, the server-verified session `accountId` is recorded against `(matchID, playerID)` in `legendary.match_seat_accounts` (server-only — never stamped into bgio `player.data`/`setupData`, which are client-exposed) so the faithful-replay capture step can attribute the finished match. The recorded `accountId` is the session value, never a client-supplied field; the write is idempotent (UPSERT) and best-effort (a record failure is logged and does NOT fail the join). Request/response schemas + status codes are unchanged by WP-333. |
| `Wired` | `POST` | `/api/match/create-with-bot` | `authenticated-session-required` | `{ numPlayers: 2..5, botCount: 1..(numPlayers-1), policy: 'competent' \| 'random', setupData: MatchSetupConfig }` (9-field composition per `00.2 §8.1`) | `200 → { matchId }`; `400` invalid body (`numPlayers`/`botCount` out of range, or `botCount >= numPlayers`); `401` unauthenticated; `500` server fault | WP-375 (solo bot-ally driver) | Creates a **mixed human + bot `cooperative` match**: a signed-in human plays seat `"0"`; the server secret-joins + auto-readies the bot seats `"1"…"botCount"` and drives them via `Master.onUpdate` with a per-match `BotAllyDriver`. Runs `requireAuthenticatedSession` first. Server-side, BEFORE returning `matchId`: loopback native `create` (WP-308 internal-delegation secret) → secret-join ONLY the bot seats (**never seat 0**; **no `match_seat_accounts` row for a bot** — D-24120) → persist the `botSeats` tag to `legendary.match_bot_ally` (migration 033) → auto-ready the bot seats (**never starts the match** — the human readying seat 0 does, `lobby.validate.ts:63`). The human then joins seat 0 through the existing authed `/api/match/join` (WP-376, client). The non-empty `botSeats` tag is the durable Casual-never-ranked signal WP-377's ranked guard consumes (DESIGN §5b/§5c). No game logic (server wires, engine decides). Decisions locked in D-24170 (poll turn-detection at 250ms, boot re-registration, side-table metadata). |
| `Wired` | `GET` | `/api/match/:matchId/bot-ally-status` | `guest` | `:matchId` path param (required, non-empty); no request body (bodyless GET — no `koaBody()`) | `200 → { driving: boolean, status: 'active' \| 'faulted' \| 'abandoned' \| 'exhausted' \| 'completed' \| 'absent', message: string \| null }`; `500 { error: 'internal_error' }` on a DB fault. Status-code domain `{200, 500}`. `Cache-Control: no-store`. `driving === (status === 'active')`; `message` is the row's public-safe `fault_message` surfaced **verbatim** when `status === 'faulted'`, else `null`; `absent` (+ `driving:false`, `message:null`) is a **`200`** when there is NO `legendary.match_bot_ally` row (i.e. not a bot-ally match) — never a `404`. | WP-414 (bot-ally stall surfacing + restart revival) | Read-only status surface the play surface (WP-415) polls so a human is never silently frozen on a stopped bot ally. **`guest` by capability** — the unguessable `matchId` is the credential; the response carries no identity, no `G`/`ctx`, no seat credentials (D-24229). **Side-table-only (D-24095):** reads ONLY `legendary.match_bot_ally` (`status` + `fault_message`) — NEVER the bgio blob, never interprets `G`/`ctx`. `fault_message` is surfaced verbatim (already public-safe per WP-261 / D-24037). Registered inside `registerBotAllyRoutes(server.router, context)` in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs) (no new wiring call). |
| `Wired` | `GET` | `/api/match/setup-requirements` | `guest` | (none) | `200 → { requirements: Record<'1'..'5', { villainGroupCount, henchmenGroupCount, villainDeckBystanderCount, heroCount }> }` | WP-371 (lobby player-count pre-check) | Read-only projection of the `PLAYER_COUNT_SETUP` table (`@legendary-arena/registry`, D-24165) so the lobby can **warn + disable Create before submit** on a player-count/composition mismatch (D-24167). Public game-rules data — no auth; `Cache-Control: public, max-age=3600`. **Guidance only, not a gate:** the authoritative composition block stays at the engine (WP-370 `validateSetupData`, surfaced as the create `400`). No game logic (server wires). |
| `Wired` | `GET` | `/api/match/:matchId/lagn` | `authenticated-session-required` | `:matchId` path param (required, non-empty); no request body | `200 → { lagn: LAGN }` — the match's setup as a **Tier-1 LAGN** (`@legendary-arena/lagn`; the stamped `lagn_version` is **`1.4.0`** as of the WP-406 writer flip — it reads `LAGN_VERSION`; `lagn_version`/`game_id`/`variant`/`player_count`/`setup` with `mastermind`/`scheme`/`villain_groups`/`henchmen_groups`/`heroes` + the four supply counts; setup-only, no `card_catalog`/`replay`/`result`). `{ lagn }` is a single top-level key. Errors use the uniform `{ error: <code> }` envelope: `401 <SessionValidationCode>`; `403 { error: 'not_a_participant' }`; `404 { error: 'match_not_found' }` (an unknown match AND an unprojectable one — null `initial_state` — are indistinguishable, to avoid a match-existence oracle); `500 { error: 'lagn_projection_failed' }` (the built document failed `validate()` — a blob-shape regression, e.g. a corrupt `numPlayers`) or `{ error: 'internal_error' }` (uncaught). Status domain `{200,401,403,404,500}`. `Cache-Control: no-store` first statement. | WP-361 (current-match loadout as LAGN) | Read-only projection of a match's setup as a Tier-1 LAGN, consumed by the WP-363 in-match "View loadout in Registry Viewer" link (→ the WP-362 viewer `?lagn=` ingest). Auth chain: WP-112 `requireAuthenticatedSession` → a **participant gate** (`readSeatAccounts(matchId)` must contain the session `accountId`, WP-333). Reads the 9-field composition + `numPlayers` from the `bgio.matches` blob (`initial_state.G.matchConfiguration` / `ctx.numPlayers`) via a thin `SELECT initial_state`-only read — the **D-24153 blob-read carve-out** (a derived, read-only Tier-1 LAGN projection extending D-24095/D-24119; never written back, never a save-game). The construction-only `buildMatchLagn` mapper (`officersCount → shield_officers_count`; `variant` `solo`/`cooperative` by seat count; ext_id → registry display name or the id verbatim) is `validate()`d **once** in the route before the `200`. Registered via `registerMatchLagnRoutes(server.router, pool, { requireAuthenticatedSession, verifier, accountResolver, registry })` in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs). |
| `Wired` | `GET` | `/api/match/:matchId/result-lagn` | `guest` | `:matchId` path param (required, non-empty); no request body | `200 → { lagn: LAGN }` — a **completed** match's result as a **LAGN 1.4.0** document: the Tier-1 `setup` (reused from `buildMatchLagn`) + `scoring_profile` (a descriptive label, default `"classic"`) + optional `players[]` (the participant roster) + optional `result` (`{ outcome }`). `{ lagn }` is a single top-level key. Errors use `{ error: <code> }`: `404 { error: 'not_found' }` (unknown / unprojectable match — null `initial_state`); `404 { error: 'match_not_finished' }` (the match has no `metadata.gameover` — still in progress); `500 { error: 'lagn_projection_failed' }` (the built document failed `validate()`) or `{ error: 'internal_error' }` (uncaught). Status domain `{200,404,500}`. `Cache-Control: no-store` first statement. | WP-406 (result-LAGN producer) | Read-only projection of a **finished** match's result as a LAGN 1.4.0 document, the source the WP-407 Hall-of-Legends match view and the WP-408 portable download consume. **Guest-readable** — a completed match's result is public (Hall-of-Legends material), so there is no session or participant gate; the privacy surface is which handles appear, decided in `buildResultPlayers`. **Descriptive only (D-24214 / D-24215):** nothing scores, credits, ranks, or verifies from the document — competitive credit stays `matchId → blob → re-execute → AccountId` (D-5301 / D-24126). `players[].player_id` is the account's **claimed public handle** (`legendary.players.display_handle`), **never** the internal `AccountId`; a seat whose account has not claimed a handle is **omitted** (when none qualify, `players[]` is omitted entirely, not `[]`). Bots/guests have no `match_seat_accounts` row, so `players.length ≤ player_count` by construction. Reads fit **existing** carve-outs — composition via **D-24153** (reused `readMatchConfigurationForLagn`), outcome via `metadata.gameover` (**D-24169**), roster via the `legendary.match_seat_accounts` domain table — **no new blob-read carve-out** (D-24216). The construction-only `buildResultMatchLagn` is `validate()`d **once** before the `200`. Registered via the same `registerMatchLagnRoutes(...)` call in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs). |
| `Wired` | (boardgame.io built-ins as surfaced by `Server({...})` — `leave`, `playAgain`, etc. if present) | (per `Server({...})` defaults) | `guest` | per boardgame.io defaults | per boardgame.io defaults | boardgame.io built-in (no project WP authorized them explicitly) | Any additional built-ins surfaced by the verification grep (Step 2) at execution time of an endpoint-touching WP are catalogued descriptively under this row when discovered. boardgame.io error semantics apply. |

---

## `Shipped-but-unwired` — Handler Code Exists, Route Registration Deferred

*No `Shipped-but-unwired` rows remain. The sole row previously in this section (`GET /api/players/:handle/profile`, deferred per D-10202) was graduated to `Wired` by WP-152 (2026-05-15) and now appears in the Server-Registered Routes table above.*

---

## `Library-only` — Function Reachable Via Direct Import, No HTTP Surface Today

Cataloging a `Library-only` function does not promise it will graduate to
HTTP. Each row cites either the originating WP's intentional choice (e.g.,
WP-103 §Out of Scope explicitly forbids HTTP exposure of `storeReplay` /
`loadReplay`) or the deferral decision (e.g., D-10202 for the WP-102
profile route awaiting long-lived `pg.Pool`).

| Status | Method | Path | Auth | Request Schema (file ref) | Response Schema (file ref) | Authorizing WP | Notes |
|---|---|---|---|---|---|---|---|
| `Library-only` | (n/a) | (n/a — function `claimHandle`) | (n/a — caller-injected `DatabaseClient`) | Function args per [`apps/server/src/identity/handle.logic.ts:157`](../../../apps/server/src/identity/handle.logic.ts) — `(accountId, displayHandle, database)` | `Result<HandleClaim, HandleErrorCode>` per WP-101 | WP-101 (handle claim flow) | No HTTP layer planned in originating WP. Pure library function consumed by future request-handler WPs that will wire the claim endpoint. The locked claim SQL is the SOLE writer for the three handle columns on `legendary.players`. |
| `Library-only` | (n/a) | (n/a — function `findAccountByHandle`) | (n/a — caller-injected `DatabaseClient`) | `(handleCanonical, database)` per [`apps/server/src/identity/handle.logic.ts:260`](../../../apps/server/src/identity/handle.logic.ts) | `Result<{ accountId, handleCanonical, displayHandle, handleLockedAt } \| null, HandleErrorCode>` per WP-101 | WP-101 (handle claim flow) | No HTTP layer planned in originating WP. Pure library function used by WP-102 `getPublicProfileByHandle` and any future surface that resolves handle → `AccountId`. Per `DESIGN-RANKING.md` lines 485-487, handles are presentation aliases — every consumer dereferences to `AccountId` at point of use. |
| `Library-only` | (n/a) | (n/a — function `getHandleForAccount`) | (n/a — caller-injected `DatabaseClient`) | `(accountId, database)` per [`apps/server/src/identity/handle.logic.ts:298`](../../../apps/server/src/identity/handle.logic.ts) | `Result<{ handleCanonical, displayHandle, handleLockedAt } \| null, HandleErrorCode>` per WP-101 | WP-101 (handle claim flow) | No HTTP layer planned in originating WP. Pure library function used by WP-102 to populate the case-preserved `displayHandle` field on `PublicProfileView`. |
| `Library-only` | (n/a) | (n/a — function `storeReplay`) | (n/a — caller-injected `DatabaseClient`) | `(replayInput, database)` per [`apps/server/src/replay/replay.logic.ts:60`](../../../apps/server/src/replay/replay.logic.ts) | `Result<{ replayHash }, ReplayErrorCode>` per WP-103 | WP-103 (replay storage / loader) | **Route-less by design.** WP-103 §Out of Scope explicitly forbids HTTP exposure of replay storage; this function is consumed only by future internal pipelines (replay-producer CLI, future submission flows). Any future WP that would expose this over HTTP must explicitly supersede the WP-103 §Out of Scope clause and update this catalog row. |
| `Library-only` | (n/a) | (n/a — function `loadReplay`) | (n/a — caller-injected `DatabaseClient`) | `(replayHash, database)` per [`apps/server/src/replay/replay.logic.ts:103`](../../../apps/server/src/replay/replay.logic.ts) | `Result<ReplayBlob \| null, ReplayErrorCode>` per WP-103 | WP-103 (replay storage / loader) | **Route-less by design** (same as `storeReplay` — WP-103 §Out of Scope). Future leaderboard score-detail surfaces (WP-115 `GET /api/leaderboards/scores/:replayHash`) read replay metadata through their own SQL projection rather than calling this function across the HTTP boundary. |
| `Library-only` | (n/a) | (n/a — function `reduceMatchToFinalState`) | (n/a — pure; `{ initialState, log }`) | `({ initialState, log })` per [`apps/server/src/replay/matchReplay.logic.ts`](../../../apps/server/src/replay/matchReplay.logic.ts) | `{ finalState: LegendaryGameState, stateHash, turnCount }` | WP-334 (mechanism); WP-336 (`turnCount` + multi-turn faithfulness fix) | **Route-less by design.** The D-24119 faithful reducer-replay: re-executes a completed match's persisted `bgio.matches` `initialState + log` through boardgame.io's own reducer (`CreateGameReducer`, `isClient: false`, imported from `boardgame.io/dist/cjs/internal.js` — the resolvable dist entry; the bare `/internal` subpath does not resolve under tsx and the root pulls in react) to reproduce the exact live final `G` + its canonical `computeStateHash`. **Re-dispatches ONLY player `MAKE_MOVE` entries; skips every `GAME_EVENT`** — the reducer regenerates all move-triggered phase/turn transitions inline and re-evaluates `endIf`, so re-dispatching the logged `GAME_EVENT` consequence would double-apply it. (WP-334 originally skipped only `automatic === true`, but play-phase `endTurn` events are logged `automatic: false`, so that skip double-advanced every turn past the first — faithful for a 0-turn lobby game, divergent for every multi-turn match; **fixed under D-24124**.) `turnCount` (WP-336 / D-24123) = completed play turns = the max play-phase `turn` in the log − `FIRST_PLAY_TURN (2)`, floored at 1 — the PAR-calibrated `rounds` input, read from the log's live-recorded per-entry `turn` (NOT the reduced `ctx.turn`). Consumed by the WP-335 capture step + (via `reduceReplayByHash`) the WP-336 competitive verifier. |
| `Library-only` | (n/a) | (n/a — function `readMatchForReplay`) | (n/a — caller-injected `DatabaseClient`) | `(matchId, database)` per [`apps/server/src/replay/matchReplay.logic.ts`](../../../apps/server/src/replay/matchReplay.logic.ts) | `{ initialState, log, metadata } \| null` | WP-334 (server-layer faithful reducer-replay) | **Route-less by design.** Reads a completed match's replay artifact from the WP-309 `bgio.matches` store (the D-24095 replay carve-out authorizes this server-layer read: a derived, read-only projection). Returns `null` when the row is absent OR its `initial_state` is null (a setState-upsert-created row is not replayable). |
| `Library-only` | (n/a) | (n/a — function `captureMatch`) | (n/a — caller-injected `DatabaseClient`) | `(matchId, database)` per [`apps/server/src/replay/matchCapture.logic.ts`](../../../apps/server/src/replay/matchCapture.logic.ts) | `{ matchId, replayHash, scenarioKey, seatsOwned, skipped }` | WP-335 (live-match capture harvester) | **Route-less by design; invoked by the background `startCaptureHarvester` scan (no HTTP surface).** Captures one finished match: `readMatchForReplay` → `reduceMatchToFinalState` → derive `scenarioKey` (set-abbr stripped → `buildScenarioKey`) → durably store the `{ initialState, log }` artifact in `bgio.replay_artifacts` keyed by `replayHash` (also the `replayHash → matchId` mapping + `scenario_key`) → `assignReplayOwnership` per authenticated seat (best-effort) → stamp `bgio.matches.captured_at`. SUBMITTABLE-ONLY: does not score, does not gate on PAR, does not flip visibility, does not call `storeReplay` (D-24122). |
| `Library-only` | (n/a) | (n/a — function `readReplayArtifactByHash`) | (n/a — caller-injected `DatabaseClient`) | `(replayHash, database)` per [`apps/server/src/replay/matchReplay.logic.ts`](../../../apps/server/src/replay/matchReplay.logic.ts) | `{ initialState, log } \| null` | WP-336 (competitive verifier repoint) | **Route-less by design.** Reads a submitted replay's durable artifact from the WP-335 `bgio.replay_artifacts` store by `replay_hash` (`SELECT initial_state, log`). Returns `null` when no artifact with that hash exists (an unknown / uncaptured replay). The `bgio` schema is a derived replay projection (D-24122 / D-24095), never the `legendary.*` domain. |
| `Library-only` | (n/a) | (n/a — function `reduceReplayByHash`) | (n/a — caller-injected `DatabaseClient`) | `(replayHash, database)` per [`apps/server/src/replay/matchReplay.logic.ts`](../../../apps/server/src/replay/matchReplay.logic.ts) | `{ finalState, stateHash, turnCount } \| null` | WP-336 (competitive verifier repoint) | **Route-less by design.** Composes `readReplayArtifactByHash` + `reduceMatchToFinalState`: looks up the durable artifact by `replayHash` and reduces it to its final state, or `null` when the artifact is absent. This is the faithful-path replacement for the WP-053 verifier's old `loadReplay`(`replay_blobs`) + `replayGame`(determinism harness) pair; it is the production `reduceReplay` dependency injected into `submitCompetitiveScoreImpl`. The verifier maps `null` to `replay_verification_failed` (does not throw). |
| `Library-only` | (n/a) | (n/a — function `readSeatAccounts`) | (n/a — caller-injected `DatabaseClient`) | `(matchId, database)` per [`apps/server/src/match/seatAccount.logic.ts`](../../../apps/server/src/match/seatAccount.logic.ts) | `{ playerId, accountId }[]` | WP-335 (live-match capture harvester) | **Route-less by design.** Reads the authenticated seats recorded at join by WP-333 `recordSeatAccount` (`legendary.match_seat_accounts`), for the capture step to assign ownership per seat. Bots/guests have no row (D-24120) and are simply absent. |
| `Wired` | `POST` | `/api/competition/scores` | `authenticated-session-required` | `{ matchId: string }` (`CompetitiveSubmissionRequest`) per [`apps/server/src/competition/competition.types.ts`](../../../apps/server/src/competition/competition.types.ts); the finished match is referenced by `matchId` — the client never sends a `replayHash` (it cannot compute `computeStateHash`), so the server resolves it (WP-338) | `200` → `{ record: CompetitiveScoreRecord, wasExisting: boolean }` (record shape per `competition.types.ts`). Errors use the uniform `{ error: <code> }` envelope: `400 invalid_request` (missing/empty `matchId`); `401 <SessionValidationCode>`; `403 { error: 'forbidden' }` (suspended) and `403 { error: 'not_owner' \| 'visibility_not_eligible' }`; `404 { error: 'replay_not_found' }`; `409 { error: 'match_not_finished' }` (match has not reached gameover); `422 { error: 'par_not_published' \| 'replay_verification_failed' }`; `500 { error: 'internal_error' }`. Status domain `{200,400,401,403,404,409,422,500}` | WP-332 (endpoint); WP-338 (submit-by-matchId + on-demand capture + auto-publish) | **WP-338 (WP-5a) made this submittable from `matchId` alone** — the arena-client's post-match consumer. `submitCompetitiveScoreByMatchIdForRequest`: guest guard → **gameover gate** (`isMatchFinished`; unfinished → `409 match_not_finished`, never captured, scoring is end-of-match only D-4804) → **resolve `replayHash` by `match_id`** (`readReplayHashByMatchId`, capturing **on-demand** via `captureMatch` when the 5-min harvester scan hasn't run — the WP-335 reaper capture-guard keeps the finished match's row alive) → confirm the caller's ownership (`findReplayOwnershipForAccount`, a by-account lookup so a 2-authenticated-seat co-owner isn't mis-rejected by `findReplayOwnership`'s LIMIT-1 row) → **auto-publish** that ownership `private → public` (submission = consent-to-publish, D-24126) → delegate to `submitCompetitiveScoreForRequest` (WP-053/336 verify+score). Auth chain: WP-112 `requireAuthenticatedSession` → WP-107 `requireUnsuspendedAccount` (`'suspended'` → 403, `'lookup_failed'` → 500) → `findPlayerByAccountId` → non-guest `PlayerAccount`. **WP-340:** the delegated verifier's own owner check now also uses the by-account lookup (`findReplayOwnershipForAccount`) — so a co-owner of a 2-authenticated-seat match is eligible from either seat; `404 replay_not_found` still means no account owns the replay, `403 not_owner` means it is owned but not by the caller. The server still reduces + hash-verifies (`reduced.stateHash === replayHash`, D-5301) and scores off the reduced final `G` with `rounds = turnCount` (D-24123). Idempotent (`UNIQUE (player_id, replay_hash)`); `Cache-Control: no-store` first. Registered via `registerCompetitionRoutes(server.router, pool, { requireAuthenticatedSession, verifier, accountResolver, requireUnsuspendedAccount, checkParPublished })` in [`server.mjs`](../../../apps/server/src/server.mjs). |
| `Wired` | `GET` | `/api/me/scores` | `authenticated-session-required` | (none — the account is the session) | `200` → `{ scores: CompetitiveScoreRecord[] }` (newest first; `[]` when the account has no submissions). Each `CompetitiveScoreRecord` carries 14 keys including `playerCount: number \| null` and (WP-354 / D-24146) `isRankedEligible: boolean` — whether the run's authenticated human roster was a mutual-friend clique at submission (solo runs are vacuously `true`; a `false` run is Casual, still returned on this owner read but excluded from the public ranked board). Errors: `401 <SessionValidationCode>`; `403 { error: 'forbidden' }` (suspended); `500 { error: 'internal_error' }`. Status domain `{200,401,403,500}` | WP-338 (my-scores read); WP-354 (`isRankedEligible` field) | The authenticated player's submitted competitive scores — the "my scores" read the arena-client profile (WP-339) renders. This owner read is NOT filtered by ranked eligibility (the player sees their own Casual runs); the public leaderboard read applies the `is_ranked_eligible = true` filter. Same auth chain as the POST (WP-112 → WP-107). Backed by `listPlayerCompetitiveScores(accountId, database)` ([`competition.logic.ts`](../../../apps/server/src/competition/competition.logic.ts)). `Cache-Control: no-store` first (a personalized authenticated read is not cacheable). Registered in the same `registerCompetitionRoutes` call as the POST. |
| `Library-only` | (n/a) | (n/a — function `submitCompetitiveScoreByMatchIdForRequest`) | (n/a — caller-injected `DatabaseClient` + `{ checkParPublished }`) | `(identity, matchId, database, productionDeps)` per [`apps/server/src/competition/competition.logic.ts`](../../../apps/server/src/competition/competition.logic.ts) | `SubmissionResult` | WP-338 | **Route-less by design** (the `POST /api/competition/scores` handler calls it). Orchestrates gameover-gate → resolve-by-matchId (on-demand capture) → confirm caller ownership → auto-publish → delegate to the unchanged `submitCompetitiveScoreForRequest`. Never throws for an expected failure. |
| `Library-only` | (n/a) | (n/a — function `readReplayHashByMatchId`) | (n/a — caller-injected `DatabaseClient`) | `(matchId, database)` per [`apps/server/src/replay/matchReplay.logic.ts`](../../../apps/server/src/replay/matchReplay.logic.ts) | `string \| null` | WP-338 | **Route-less by design.** `SELECT replay_hash FROM bgio.replay_artifacts WHERE match_id = $1` — resolves a finished match's canonical `replayHash` for the submit-by-matchId flow; `null` when the match has not been captured yet (the caller then captures on-demand). |
| `Library-only` | (n/a) | (n/a — function `isMatchFinished`) | (n/a — caller-injected `DatabaseClient`) | `(matchId, database)` per [`apps/server/src/replay/matchReplay.logic.ts`](../../../apps/server/src/replay/matchReplay.logic.ts) | `boolean` | WP-338 | **Route-less by design.** Whether a match's `bgio.matches` metadata carries a `gameover` (`metadata ? 'gameover'`) — the submit-by-matchId gameover gate; `false` for an absent or in-progress match. |
| `Library-only` | (n/a) | (n/a — function `findReplayOwnershipForAccount`) | (n/a — caller-injected `DatabaseClient`) | `(accountId, replayHash, database)` per [`apps/server/src/identity/replayOwnership.logic.ts`](../../../apps/server/src/identity/replayOwnership.logic.ts) | `ReplayOwnershipRecord \| null` | WP-338 | **Route-less by design.** A specific account's ownership row for a replay (`WHERE p.ext_id = $1 AND ro.replay_hash = $2`) — resolves the SUBMITTING account's own row for a co-owned (2-authenticated-seat) replay, where `findReplayOwnership`'s LIMIT-1 returns an arbitrary owner. |
| `Library-only` | (n/a) | (n/a — function `requireAuthenticatedSession`) | (n/a — caller-injected dependencies) | `(req, options)` per [`apps/server/src/auth/sessionToken.logic.ts:157`](../../../apps/server/src/auth/sessionToken.logic.ts) — `req` is a `{ headers }` shape (per D-11202 — bearer header only); `options` is `{ verifier?: SessionVerifier; accountResolver?: AccountResolver; database: DatabaseClient }` | `Result<AccountId, SessionValidationErrorCode>` per WP-112 (closed-union codes: `'missing_token'`, `'invalid_token'`, `'expired_token'`, `'unknown_account'`, `'session_verifier_not_configured'`, `'lookup_failed'`) | WP-112 (session token validation middleware) | **Ships fail-closed unconfigured.** No HTTP layer in WP-112; future request-handler WP wires `requireAuthenticatedSession` into authenticated route handlers. Implements the WP-099 §A "Session Validation Middleware" policy contract; F-1..F-7 Future-Auth Gates PASS by construction (no `'hanko'` literal, no broker-specific code, no new npm dep). Per D-11204, the orchestrator returns `Result.fail({ code: 'session_verifier_not_configured' })` on every authenticated request until production wiring calls `configureSessionValidation({ verifier, accountResolver, database })` exactly once at startup; the broker-specific `SessionVerifier` implementation is deferred to WP-126 per D-11201. |
| `Library-only` | (n/a) | (n/a — function `requireAdminSession`) | (n/a — caller-injected dependencies) | `(request, options)` per [`apps/server/src/auth/adminSession.ts`](../../../apps/server/src/auth/adminSession.ts) — `request: SessionTokenRequest`; `options: RequireAuthenticatedSessionOptions` (re-exports from `./sessionToken.types.js`); both imported via WP-112's locked surface, never directly from `../identity/identity.types.js` | `Promise<AdminSessionResult>` per WP-159; success branch `{ ok: true; accountId }`; failure branch `{ ok: false; code: 'unauthorized' \| 'forbidden' \| 'lookup_failed'; reason: string }` (closed union `AdminSessionErrorCode` + canonical static `reason` strings per WP-159 §A) | WP-159 (admin session gate) | **Composition over WP-112 + single-column DB read.** Composes `requireAuthenticatedSession` with a fresh `SELECT is_admin FROM legendary.players WHERE ext_id = $1` against the resolver-returned `AccountId` (per D-15901). Single source of truth for admin authorization — a repo-wide grep gate enforces that `adminSession.ts` is the ONLY file issuing the admin-flag SELECT (per D-15902 forward-compatibility to a future `player_roles` join). Fail-closed at every branch: upstream session rejection → `'unauthorized'`; DB throw / zero rows / multi-row / non-boolean flag → `'lookup_failed'`; strict `=== true` is the only success path. No HTTP layer in WP-159; callers: WP-107 (profile integrity / anti-cheat surface, first caller), WP-176 (admin billing auth cutover, second caller — replaced `requireAdminSecret` per D-17601; `adminGate.ts` deleted per D-17603). |
| `Library-only` | (n/a) | (n/a — function `findAccountByAuthProviderSub`) | (n/a — caller-injected dependencies) | `(authProvider, authProviderSub, database)` per [`apps/server/src/auth/accountLookup.logic.ts:120`](../../../apps/server/src/auth/accountLookup.logic.ts) — positional args (per D-11203); `database: DatabaseClient` | `Result<{ accountId, authProvider, authProviderId } \| null, AccountLookupErrorCode>` per WP-112 (closed-union codes: `'lookup_failed'`); `Result.ok(null)` on clean no-match (the resolver translates this to `'unknown_account'` at the orchestrator boundary) | WP-112 (session token validation middleware) | **Read-only `legendary.players` SELECT.** Single locked projection: `SELECT ext_id, auth_provider, auth_provider_id FROM legendary.players WHERE auth_provider = $1 AND auth_provider_id = $2 LIMIT 1`. The translation from the verifier-side `authProviderSub` (OIDC nomenclature) to the canonical wire-level `authProviderId` field happens at exactly one site — the SQL parameter binding inside this helper. No INSERT / UPDATE / DELETE in scope. Performs no input canonicalization (mirrors WP-101 `findAccountByHandle` minimal-canonicalization posture for opaque verifier-emitted subjects). |
| `Library-only` | (n/a) | (n/a — function `productionAccountResolver`) | (n/a — caller-injected dependencies) | `(claim: VerifiedSessionClaim, database: DatabaseClient)` per [`apps/server/src/auth/accountResolver.logic.ts`](../../../apps/server/src/auth/accountResolver.logic.ts) | Returns `Promise<Result<AccountId \| null>>`; failure-payload `code` is `'lookup_failed'` (forwarded verbatim from `findAccountByAuthProviderSub` or `provisionPlayerAccount`) | WP-131 + WP-174 (authenticated routes production wiring + first-sign-in auto-provisioning) | **Production wiring of the WP-112 `AccountResolver` interface with WP-174 read-or-create extension.** Consumed at startup by `tryConstructHankoVerifier()` in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs). Lookup-first: delegates to WP-112 `findAccountByAuthProviderSub`; `Result.ok(hit)` → `Result.ok(hit.accountId)`. On no-match: if the claim carries a usable email (defined, non-empty after trim, contains `@`), calls `provisionPlayerAccount` (WP-174 §E — `INSERT ... ON CONFLICT (auth_provider, auth_provider_id) DO NOTHING` + re-SELECT) to auto-provision a `legendary.players` row. On successful provisioning → `Result.ok(newAccountId)`. On `duplicate_email` (same email, different auth provider) → `Result.ok(null)` (the orchestrator translates to `'unknown_account'`; account linking intentionally deferred). On missing/invalid email → `Result.ok(null)` (preserves pre-WP-174 behavior). `Result.fail({ code: 'lookup_failed', reason })` forwarded verbatim on DB faults. No-throw discipline per D-11203 — every failure surface is a typed `Result`. |
| `Library-only` | (n/a) | (n/a — function `provisionPlayerAccount`) | (n/a — caller-injected `DatabaseClient`) | `(input: { email, displayName, authProvider, authProviderId }, database, idProvider?)` per [`apps/server/src/auth/accountProvisioning.logic.ts`](../../../apps/server/src/auth/accountProvisioning.logic.ts) | Returns `Promise<Result<ProvisionedAccount>>` where `ProvisionedAccount` is `{ accountId, email, displayName, authProvider, authProviderId }`; failure-payload `code` is `'duplicate_email'` (SQLSTATE 23505 on email UNIQUE constraint) or `'lookup_failed'` (other DB faults) | WP-174 (first-sign-in auto-provisioning) | **Race-safe account provisioning.** Single INSERT statement with `ON CONFLICT (auth_provider, auth_provider_id) DO NOTHING RETURNING ...`; if RETURNING yields zero rows (conflict), re-SELECTs by `(auth_provider, auth_provider_id)` to fetch the winning row. Idempotency guarantee: same `(auth_provider, auth_provider_id)` pair always resolves to the same `accountId`. Does NOT call `createPlayerAccount` from `identity.logic.ts` (WP-052 contract file; locked). `accountId` generated via `node:crypto.randomUUID()` (injectable for tests). No-throw discipline — every failure path returns a typed `Result`. |
| `Library-only` | (n/a) | (n/a — function `getEntitlementsForAccount`) | (n/a — caller-injected `DatabaseClient`) | `(accountId: AccountId, database: DatabaseClient)` per [`apps/server/src/entitlements/entitlements.logic.ts:52`](../../../apps/server/src/entitlements/entitlements.logic.ts) | Returns `Promise<EntitlementsResult<Entitlement[]>>`; failure-payload `code` is `'lookup_failed'` (Step 1 zero-row OR Step 1 / Step 2 DB fault) | WP-132 (entitlements data model + read API) | Two-query pattern (mirrors [`apps/server/src/profile/ownerProfile.logic.ts:123`](../../../apps/server/src/profile/ownerProfile.logic.ts)): Step 1 maps `AccountId` (= `ext_id text` per D-5201) → `player_id bigint` via `SELECT player_id FROM legendary.players WHERE ext_id = $1 LIMIT 1`; Step 2 reads active entitlements via `SELECT entitlement_key, source, source_ref, granted_at, revoked_at FROM legendary.entitlements WHERE player_id = $1 AND revoked_at IS NULL ORDER BY granted_at ASC`. Step 1 strictly precedes Step 2 (no parallelization — the AccountId-not-found branch must remain distinguishable from the no-entitlements branch). No-throw discipline; every failure path returns `Result.fail({ code: 'lookup_failed' })`. Read-only invariant: the file contains zero mutation site against `legendary.entitlements` by WP-132 construction; WP-134 (Stripe webhook + fulfillment) owns the row-creation site, and revocation is a future-WP responsibility. Consumed by [`apps/server/src/entitlements/entitlements.routes.ts`](../../../apps/server/src/entitlements/entitlements.routes.ts) `registerEntitlementRoutes(...)` for the `GET /api/me/entitlements` handler. |
| `Library-only` | (n/a) | (n/a — function `createHankoSessionVerifier`) | (n/a — caller-injected dependencies) | Factory-config arg `HankoVerifierConfig` per [`apps/server/src/auth/hanko/hankoVerifier.logic.ts:230`](../../../apps/server/src/auth/hanko/hankoVerifier.logic.ts) — `{ tenantBaseUrl, expectedAudience, jwksRefreshIntervalMs?, fetcher? }` (all `readonly`). `tenantBaseUrl` is the tenant-scoped origin per Hanko Cloud's `/{tenant_id}/.well-known/jwks.json` shape; the verifier appends `/.well-known/jwks.json` programmatically. | Returns `SessionVerifier` (WP-112 interface — re-imported from `../sessionToken.types.js`, never redeclared). The returned `verify(token)` closure returns `Promise<Result<VerifiedSessionClaim>>` (single-parameter `Result<T>` per the shipped contract at `identity.types.ts:139-141`); failure-payload `code` field carries `SessionVerificationErrorCode` values (`'invalid_token' \| 'expired_token' \| 'unknown_provider' \| 'verification_failed'`) translated by the orchestrator at `sessionToken.logic.ts:191-193`. | WP-126 (external authentication integration) | **Ships fail-closed unwired.** No HTTP layer in WP-126; production wiring deferred to a future request-handler WP that calls `configureSessionValidation({ verifier: createHankoSessionVerifier(config), accountResolver, database })` exactly once at startup (per D-11204 + D-11201 staging). Implements the WP-099 §A "Session Validation Middleware" policy contract; F-1..F-7 Future-Auth Gates PASS by construction (D-12601 = built-ins-only — RS256 via Node v22 `node:crypto`, no top-level `jsonwebtoken` / `jose` / `jwks-rsa`; F-1 no `'hanko'` literal in any `auth_provider` value path; F-2 every `@teamhanko/*` import / `hanko.io` URL contained under `apps/server/src/auth/hanko/`; F-3 verifier never generates `AccountId`). Federated-IdP mapping per D-12604 reads Hanko's documented `amr` (Authentication Method References) array claim through the closed-set `HANKO_IDP_TO_AUTH_PROVIDER` lookup (`'ext:google' → 'google'`, `'ext:discord' → 'discord'`, native methods `'pwd' \| 'passkey' \| 'otp' \| 'totp' \| 'security_key' → 'email'`); federated values take precedence over native via two-pass priority scan; unknown values resolve to `'unknown_provider'`. JWKS endpoint is `${tenantBaseUrl}/.well-known/jwks.json`, refreshed every 300_000 ms (D-12603 default); per-instance cache, single-flight, one-shot retry, failed-refresh preserves cache, aliasing-defended via `Object.freeze` at insertion. |
| `Library-only` | (n/a) | (n/a — function `applyOperatorOverride`) | (n/a — caller-injected `DatabaseClient` + admin-auth out of scope) | `(operatorAccountId, teamId, action, reason, payload, database)` per [`apps/server/src/teams/team.logic.ts`](../../../apps/server/src/teams/team.logic.ts) — `action` is closed union `'reassign_captain' \| 'force_retire' \| 'amend_membership'`; `reason` is non-empty (validator rejects empty per EC-115 Guardrail 5) | `Result<Team, TeamErrorCode>` per WP-109 (closed-union codes per `TEAM_ERROR_CODES`) | WP-109 (team affiliation) | **Route-less by design — admin-auth WP gates HTTP exposure** (mirrors WP-104's deferred admin surface). Operator override paths exist in `team.logic.ts` for amendment-pattern corrections, captain reassignment when the captain becomes unreachable, and force-retire of dissolved cohorts. Every override row carries `is_operator = true` AND a non-empty `reason` text, distinguishable from captain-driven rows in the audit log per WP-109 §9 + EC-115 Guardrail 5. A future admin-auth WP wires this function into an `/api/admin/teams/...` surface; until then it is reachable only via direct import (and via the team logic test file). |
| `Library-only` | (n/a) | (n/a — function `composeTeamAffiliationsForProfile`) | (n/a — caller-injected `DatabaseClient` + optional `FriendGraphService`) | `(database, subjectPlayerId, viewerPlayerId, friendGraphService)` per [`apps/server/src/teams/team.logic.ts`](../../../apps/server/src/teams/team.logic.ts); `viewerPlayerId` is `null` for unauthenticated public reads; `friendGraphService` is `undefined` until a future friend-graph WP lands | `readonly TeamAffiliation[]` per WP-109; ordering invariant `ORDER BY joined_at ASC, team_id ASC` per pre-flight PS-13 + EC-115 Locked Values; clients MUST NOT defensively re-sort | WP-109 (team affiliation) | **Read-no-mutate composer.** Single visibility-filtered SELECT joining `legendary.team_member_events` to `legendary.teams` with the four-branch WHERE per WP-109 §11 + OQ-1 = (a) / D-10901 + EC-115 Guardrail 6 ('public' / 'friends'-with-graph / 'friends'-without-graph-fallback / 'private'). Consumed by both `getPublicProfileByHandle` (public profile read; viewer = null) AND `getOwnerProfile` (owner /me read; viewer = subject) per pre-flight PS-3 = YES / D-10904. No HTTP layer of its own — the data reaches the wire via the `teamAffiliations[]` projection on `PublicProfileView` / `OwnerProfileView`. |
| `Library-only` | (n/a) | (n/a — function `requireUnsuspendedAccount`) | (n/a — caller-injected `DatabaseClient`) | `(database: DatabaseClient, accountId: AccountId)` per [`apps/server/src/auth/requireUnsuspendedAccount.ts`](../../../apps/server/src/auth/requireUnsuspendedAccount.ts) | `Promise<RequireUnsuspendedAccountResult>` per WP-107; success branch `{ ok: true }` (no payload — the success signal IS the absence of a failure code); failure branch `{ ok: false; code: 'suspended' \| 'lookup_failed'; reason: string }` (closed union `RequireUnsuspendedAccountErrorCode` + canonical static `reason` strings per WP-107 §Locked contract values) | WP-107 (profile integrity / anti-cheat surface) | **Ships fail-closed unwired.** First caller deferred to the future score-submission request-handler WP that introduces the HTTP route over WP-053's `submitCompetitiveScore` library function (the score-submission route does not exist at HEAD — `submitCompetitiveScore` is itself `Library-only` per the row above; mirrors WP-053 / D-10202 deferral precedent). Lives under `apps/server/src/auth/` (NOT `apps/server/src/profile/admin/`) because the intake check is broker-agnostic and reusable; profile-admin-owned would invert the dependency direction (the future score-submission route would import from profile-admin). HTTP error mapping locked here as a caller-contract for the future request-handler WP: `'suspended'` -> HTTP 403 `{ code: 'forbidden', reason: 'Account is suspended.' }`; `'lookup_failed'` -> HTTP 500 `{ code: 'internal_error' }`. Single locked projection: `SELECT is_suspended FROM legendary.players WHERE ext_id = $1` against the `is_suspended BOOLEAN NOT NULL DEFAULT FALSE` column added by migration 015. Strict triple-equals on the flag value (no truthy coercion, mirrors WP-159 `adminSession.ts` pattern); fail-closed on every fault branch (DB throw / zero rows / multi-row / non-boolean flag all collapse to `'lookup_failed'`). No-throw discipline — every fault category returns a typed `Result`. |

---

## `Pending` — Drafted In A Future WP, Not Yet Executed

Each `Pending` row names both the drafting WP and its current state at
catalog-write time. When the drafting WP executes, its commit replaces
these rows per D-11804 replace-whole-row merge semantics — the row is
re-written wholesale (status → `Wired`, file refs filled in, etc.), never
edited column-by-column.

*No `Pending` rows at catalog-write time. The three forward-reference
placeholder rows previously seeded by WP-118 for the WP-115 leaderboard
endpoints were graduated to `Wired` in the WP-115 execution commit
(2026-05-01) per D-11804 single-row-graduation semantics; the underlying
network surface is now fully described by the three new `Wired` rows in
the `Server-Registered Routes` section above.*

---

## Footer

Cataloging a `Library-only` function does not promise it will graduate to
HTTP. Each `Library-only` row cites either the originating WP's intentional
choice (e.g., WP-103 §Out of Scope) or the deferral decision (e.g.,
D-10202 for the WP-102 profile route). Graduation, when it happens, is the
wiring WP's responsibility under the D-11804 catalog-update obligation —
the wiring WP replaces the affected row wholesale (status → `Wired`,
columns re-populated for the HTTP shape) in the same commit that lands
the new route registration in [`apps/server/src/server.mjs`](../../../apps/server/src/server.mjs).

*Last updated: WP-373 / EC-402 (dashboard billing + revenue endpoints, 2026-07-13). Added 4 new `Wired` rows to Server-Registered Routes — `GET /api/dash/metrics/billing/health`, `GET /api/dash/metrics/billing/health/sparklines`, `GET /api/dash/revenue`, `GET /api/dash/metrics/revenue` — all `admin-session-required`, the first `/api/dash/*` sub-surface. No Auth Taxonomy change (reuses `admin-session-required`). Per D-24168 (endpoint set), D-19603 (billing-health forward contract), D-20503 (bare `{ data: T }` envelope), D-11504 (`no-store`).*

*Previously: WP-176 / EC-198 (admin billing auth cutover, 2026-05-24). Replaced `GET /api/admin/billing/history` row wholesale: Auth `admin-secret` → `admin-session-required`; status-code domain `{200, 401, 500}` → `{200, 401, 403, 500}`; error body gains `reason` field per D-15901 closed-union. Auth Taxonomy reduced from 5 to 4 values — `admin-secret` row removed (D-17602; no remaining callers after `adminGate.ts` deleted per D-17603). Updated `admin-session-required` taxonomy row to cite WP-107 + WP-176 as callers. Updated `requireAdminSession` Library-only row to cite WP-176 as second caller.*

*Previously: WP-174 / EC-196 (first-sign-in auto-provisioning, 2026-05-24). Updated `productionAccountResolver` row to reflect WP-174 read-or-create extension (lookup-first, then auto-provision on no-match when claim carries usable email). Added 1 new `Library-only` row for `provisionPlayerAccount` (`apps/server/src/auth/accountProvisioning.logic.ts`). No new HTTP routes — all changes are library-only.*

*Previously: WP-107 / EC-195 (profile integrity / anti-cheat surface, 2026-05-24). Added 3 new `Wired` rows to Server-Registered Routes (`GET /api/admin/players/:handle/integrity`, `POST /api/admin/players/:handle/suspend`, `POST /api/admin/players/:handle/unsuspend`) — all `admin-session-required`. Added 1 new `Library-only` row for `requireUnsuspendedAccount` (`apps/server/src/auth/requireUnsuspendedAccount.ts`); first caller deferred to a future score-submission request-handler WP per the WP-053 / D-10202 deferral precedent. WP-107 is the FIRST caller of `requireAdminSession` (WP-159).*

*Previously: WP-159 / EC-173 (admin session gate, 2026-05-17). Added `Library-only` row for `requireAdminSession` (`apps/server/src/auth/adminSession.ts`) under the auth cluster. Extended Auth Taxonomy from 4 to 5 values (`admin-session-required` added per D-15901 + D-15902). Cutover of WP-110's existing `admin-secret` routes to the new gate is a deliberately separate follow-up WP.*

*Previously: WP-110 / EC-163 (admin billing visibility, 2026-05-15) added `GET /api/admin/billing/history` row to Server-Registered Routes and extended the Auth Taxonomy from 3 to 4 values (`admin-secret` added per D-11001).*
