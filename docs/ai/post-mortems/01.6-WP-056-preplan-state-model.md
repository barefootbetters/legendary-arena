# 01.6 Post-Mortem — WP-056 / EC-056 Pre-Planning State Model & Lifecycle

**Work Packet:** WP-056
**Execution Checklist:** EC-056
**Execution Date:** 2026-04-20
**Pre-Flight Date:** 2026-04-20 (Commit A0 `f2af0f3`)
**Primary Layer:** Pre-Planning (Non-Authoritative, Per-Client) — new `preplan` code category (D-5601)
**WP Class:** Contract-Only (types-only; no runtime code)
**Execution HEAD at session start:** `311c8fe` (A0 `f2af0f3` + WP-081 01.7 copilot-check merge `06125e9`/`311c8fe` — both SPEC, zero WP-056 file overlap)
**Test Baseline:** `536 passing / 0 failing` (start) → `536 passing / 0 failing` (end) — UNCHANGED

---

## Section 1 — Overview

WP-056 establishes the pre-planning state contract as types-only scaffolding
in a new non-authoritative package (`packages/preplan/`). Four public types —
`PrePlan`, `PrePlanSandboxState`, `RevealRecord`, `PrePlanStep` — are
exported from a single source file with JSDoc lifecycle invariants inline.
Zero runtime code, zero tests, zero wiring into the game engine. The package
consumes `CardExtId` from `@legendary-arena/game-engine` via a type-only
import and carries the engine as a workspace peer — no runtime dependency.
This contract is the foundation for WP-057 (sandbox execution) and WP-058
(disruption detection), which will own the first runtime consumers of the
closed unions (`status`, `invalidationReason.effectType`) and therefore own
the canonical readonly arrays + drift-detection tests per Finding #4
deferral lock.

---

## Section 2 — Final Baseline

| Package | Tests | Suites | Failing | Delta |
|---|---|---|---|---|
| `@legendary-arena/registry` | 13 | 2 | 0 | unchanged |
| `@legendary-arena/vue-sfc-loader` | 11 | — | 0 | unchanged |
| `@legendary-arena/game-engine` | 436 | 109 | 0 | unchanged |
| `apps/server` | 6 | — | 0 | unchanged |
| `apps/replay-producer` | 4 | — | 0 | unchanged |
| `apps/arena-client` | 66 | — | 0 | unchanged |
| `@legendary-arena/preplan` (new) | 0 | 0 | 0 | added 0 tests (RS-2 zero-test lock) |
| **Repo-wide** | **536** | — | **0** | **UNCHANGED** |

`pnpm --filter @legendary-arena/preplan build` exits 0. `dist/` contains
`index.js`, `index.d.ts`, `preplan.types.js`, `preplan.types.d.ts`, plus
sourcemaps. TypeScript declaration output is valid.

`pnpm -r build` at HEAD (before and after WP-056) fails inside
`packages/registry` with `dist/cards.json not found — run 'pnpm normalize'
first`. This is a **pre-existing registry build-pipeline bootstrapping
condition** that the parallel WP-081 session is explicitly chartered to fix.
Verified by stashing WP-056 work and re-running `pnpm -r build` on HEAD:
same registry failure reproduces with zero WP-056 code present. Not a
WP-056-introduced regression. See §8 for full reconciliation.

---

## Section 3 — Allowlist Conformance

`git diff --name-only HEAD` (Commit A staging target):

```
packages/preplan/package.json                              NEW
packages/preplan/tsconfig.json                             NEW
packages/preplan/src/preplan.types.ts                      NEW
packages/preplan/src/index.ts                              NEW
pnpm-lock.yaml                                             MODIFIED (scoped)
docs/ai/post-mortems/01.6-WP-056-preplan-state-model.md    NEW (this file)
```

All six files match the §Files Expected to Change allowlist exactly. Zero
scope expansion. Zero "while I'm here" edits. Zero contract files modified
(engine / registry / server / apps).

**Commit B governance-close targets (staged separately):**
- `docs/ai/STATUS.md`
- `docs/ai/work-packets/WORK_INDEX.md`
- `docs/ai/execution-checklists/EC_INDEX.md`

**Unchanged tripwires verified:**
- `pnpm-workspace.yaml` — no diff (PS-3 correction held)
- `packages/game-engine/src/index.ts` — no diff
- `packages/game-engine/src/state/zones.types.ts` — no diff
- `packages/game-engine/src/types.ts` — no diff
- `packages/game-engine/src/matchSetup.types.ts` — no diff
- `packages/registry/**` — no diff
- `apps/**` — no diff
- Root `package.json` — no diff
- `.claude/worktrees/` — untouched (parallel WP-081 session state preserved)
- `stash@{0..2}` — intact (no pops)

**Inherited dirty-tree items** (never staged into any WP-056 commit):
- `M docs/ai/invocations/session-wp079-label-replay-harness-determinism-only.md`
- `?? docs/ai/REFERENCE/01.3-ec-mode-commit-checklist.oneliner.md`
- `?? docs/ai/invocations/forensics-move-log-format.md`
- `?? docs/ai/invocations/session-wp048-par-scenario-scoring.md`
- `?? docs/ai/invocations/session-wp067-uistate-par-projection-and-progress-counters.md`
- `?? docs/ai/invocations/session-wp068-preferences-foundation.md`
- `?? docs/ai/post-mortems/01.6-applyReplayStep.md`
- `?? docs/ai/session-context/session-context-forensics-move-log-format.md`
- `?? docs/ai/session-context/session-context-wp067.md`

---

## Section 4 — Pre-Flight / Copilot-Check Reconciliation

**Copilot Check (01.7) disposition at pre-flight:** CONFIRM — 30/30 PASS after
five scope-neutral FIXes landed in Commit A0 (`f2af0f3`):

| FIX | Target | Status at execution |
|---|---|---|
| PS-1 | EC-056 authored + registered in `EC_INDEX.md` (Status: Draft) | Verified — EC-056 file present; EC_INDEX row at line 131 |
| PS-2 | D-5601 authored (new top-level `preplan` code category) + registered in `DECISIONS_INDEX.md` + `02-CODE-CATEGORIES.md` | Verified — D-5601 at `DECISIONS.md:6034`; DECISIONS_INDEX row at line 562; `02-CODE-CATEGORIES.md` carries the `preplan` row |
| PS-3 | WP-056 §Files Expected to Change corrected — `pnpm-workspace.yaml` removed; `pnpm-lock.yaml` delta explicitly scoped | Verified — `pnpm-workspace.yaml` shows no diff post-install; lockfile delta confined to `importers['packages/preplan']` |
| Finding #4 | Closed-union canonical-array deferrals documented on `status` → WP-057 and `effectType` → WP-058 | Verified — both JSDoc citations present in `preplan.types.ts` |
| Finding #10 | `\| string` open-union advisory rationale on `RevealRecord.source` + `PrePlanStep.intent` | Verified — both JSDoc rationales present, `CoreMoveName` deliberate-NOT citation present on `PrePlanStep.intent` |

All five FIXes remained in force throughout execution. Zero additional
RISK items surfaced during implementation.

---

## Section 5 — Dependency Contract Verification

- **`CardExtId` re-export location:** `packages/game-engine/src/index.ts:5` — verified at read-time; matches pre-flight PS-4 reality-reconciliation.
- **`CardExtId` canonical definition:** `packages/game-engine/src/state/zones.types.ts:23` — `export type CardExtId = string;` — verified.
- **`PlayerZones` shape:** `hand` / `deck` / `discard` / `inPlay` / `victory` — verified in `zones.types.ts`. `PrePlanSandboxState` intentionally omits `victory` per DESIGN-CONSTRAINT #9 (no information leakage — the victory pile is not player-visible during normal play). The JSDoc on `PrePlanSandboxState` carries this rationale inline.
- **Import statement in `preplan.types.ts`:** `import type { CardExtId } from '@legendary-arena/game-engine';` — type-only, matches Locked Values verbatim. Zero other imports. Verification Step 8 grep (`| grep -v "import type"`) returns zero non-type-only engine import hits.
- **No runtime engine coupling:** Zero bare `import { ... } from '@legendary-arena/game-engine'` anywhere under `packages/preplan/**`. Zero `boardgame.io` imports. Zero registry imports. Zero `apps/*` imports.
- **Lockfile delta:** +10 lines, a single new `importers['packages/preplan']` block declaring the `@legendary-arena/game-engine` workspace link + `typescript` devDep. No cross-importer churn. P6-44 / PS-3 lockfile discipline held.

---

## Section 6 — Hidden-Coupling / Aliasing Audit

**N/A at the types layer.** WP-056 introduces no function returns, no spread
copies, no projections, no mutable-array handoffs. The aliasing risk
surface is inherited by WP-057 when sandbox execution begins constructing
snapshots from real player state — at that point §6 becomes load-bearing
(spread-copy mutable sources; never hand back references to authoritative
state). WP-056 pre-emptively defends against this downstream risk by:

1. Making `revealLedger` the **sole** authority for deterministic rewind
   (DESIGN-CONSTRAINT #3; JSDoc-pinned on `RevealRecord`). Rewind logic
   that inspects `sandboxState` directly is defined as invalid up front,
   so aliasing of `sandboxState` fields cannot cross the rewind boundary.
2. Encoding the `victory` omission as a structural invariant on
   `PrePlanSandboxState`, not as a runtime check. Hidden-state leakage is
   impossible by construction — the type does not carry the field.
3. Declaring `PrePlanStep.intent` and `RevealRecord.source` as
   **advisory-open** unions with explicit "never executable" and
   "descriptive-only" language. Consumers are structurally prevented from
   treating these as dispatch keys, which pre-empts a common class of
   coupling bug where UI hints drift into execution paths.

No hidden coupling introduced. No enum or union widened unintentionally.
No ordering assumptions not explicitly documented (`revision` monotonicity,
`revealIndex` monotonicity, lifecycle transition list all JSDoc-pinned).

---

## Section 7 — Invariants Held vs Broken

| Invariant | Source | Status |
|---|---|---|
| DESIGN-CONSTRAINT #1 (Explicit representation) | `DESIGN-CONSTRAINTS-PREPLANNING.md` | Held — `PrePlan` is a first-class exported type |
| DESIGN-CONSTRAINT #2 (Advisory, non-binding) | `DESIGN-CONSTRAINTS-PREPLANNING.md` | Held — no write path to authoritative state; JSDoc declares "never commits game actions, never writes to authoritative game state" |
| DESIGN-CONSTRAINT #3 (Full rewind capability) | `DESIGN-CONSTRAINTS-PREPLANNING.md` | Held — `revealLedger` declared sole rewind authority in `RevealRecord` JSDoc |
| DESIGN-CONSTRAINT #9 (No information leakage) | `DESIGN-CONSTRAINTS-PREPLANNING.md` | Held — `victory` omitted from `PrePlanSandboxState`; counter constraint JSDoc-pinned |
| DESIGN-CONSTRAINT #10 (Single-turn scope) | `DESIGN-CONSTRAINTS-PREPLANNING.md` | Held — `appliesToTurn = ctx.turn + 1` invariant JSDoc-pinned on `PrePlan` type and on `appliesToTurn` field |
| Revision monotonicity | WP-056 §B | Held — JSDoc on `revision` declares `starts at 1`, `increments on any mutation`, `new instance post-rewind starts at 1 with new prePlanId` |
| NON-GUARANTEE clause on `baseStateFingerprint` | WP-056 §B | Held — clause preserved verbatim; divergence hint, not correctness authority |
| Closed-union deferral (status → WP-057) | Finding #4 | Held — JSDoc cites `PREPLAN_STATUS_VALUES` deferred to WP-057 |
| Closed-union deferral (effectType → WP-058) | Finding #4 | Held — JSDoc cites `PREPLAN_EFFECT_TYPES` deferred to WP-058 |
| Open-union advisory rationale (source, intent) | Finding #10 | Held — `\| string` fallback preserved; descriptive-not-executable language present; `CoreMoveName` deliberate-NOT citation present |
| Null semantics (missing PrePlan vs empty planSteps) | WP-056 §F | Held — JSDoc type-level on `PrePlan` distinguishes the two |
| Lifecycle transition list | WP-056 §F | Held — `'active' → 'invalidated' → discarded` / `'active' → 'consumed' → discarded` JSDoc-pinned |
| P6-50 paraphrase discipline | `01.4-pre-flight-invocation.md` | Held — zero hits for `G` / `LegendaryGameState` / `LegendaryGame` / `boardgame.io` in JSDoc prose (verified via Grep on file) |
| RS-2 zero-test baseline | Session prompt Locked Values | Held — zero `.test.ts` files under `packages/preplan/` |
| `pnpm-workspace.yaml` UNCHANGED (PS-3) | WP amendment | Held — `git diff pnpm-workspace.yaml` returns empty |
| `pnpm-lock.yaml` delta scope (P6-44) | WP amendment | Held — delta = 10 lines, one new importer block only |

Zero invariants broken. Zero non-compliance findings.

---

## Section 8 — Reality-Reconciliation vs Session Prompt Locked Values

Expected: zero drift findings, given pre-flight caught PS-1/PS-2/PS-3 +
Findings #4/#10 before execution. Observed: **one pre-existing repo
condition** surfaced during Task 8 that the session prompt did not
anticipate.

### Finding 8.1 — `pnpm -r build` pre-existing registry failure

**What happened.** Task 8 Verification Step 3 calls for `pnpm -r build` to
exit 0. It exits 1 with `packages/registry build: ❌ dist/cards.json not
found — run 'pnpm normalize' first`. The registry `build` script chains
`tsc -p tsconfig.build.json && node scripts/build-dist.mjs`; `build-dist.mjs`
reads `dist/cards.json` which is produced by a separate `pnpm normalize`
step that must be run once before `pnpm -r build` will succeed.

**Is it WP-056's fault?** No. Reproduced the failure on clean HEAD (`311c8fe`)
by stashing WP-056 work (`git stash push -u -- packages/preplan pnpm-lock.yaml`,
stash `415798f`), re-running `pnpm install` + `pnpm -r build`. Same registry
failure reproduces with zero WP-056 code present. This is a pre-existing
registry build-pipeline bootstrapping condition that predates the WP-056
branch.

**Why the session prompt didn't catch it.** The session prompt Verification
Step 3 was written under the implicit assumption that `pnpm -r build` is
green at HEAD. The assumption is empirically false today and has been
false since at least the WP-081 planning cycle — the parallel WP-081
session (`.claude/worktrees/`) is explicitly chartered to fix the registry
build-pipeline bootstrapping issue (`WP-081-registry-build-pipeline-cleanup.md`).
Neither pre-flight audit for WP-056 nor the 01.7 copilot check reality-
reconciled against `pnpm -r build`; both assumed the baseline `pnpm test`
repo-wide green implied the build was also green.

**Resolution.** Skip the repo-wide build gate for WP-056 and substitute the
package-scoped build: `pnpm --filter @legendary-arena/preplan build` exits
0, produces valid `dist/*.js` + `*.d.ts` + sourcemaps. This is the correct
scope because WP-056 only introduces `packages/preplan/` and must not touch
registry build artifacts (which would be a scope violation — `packages/registry/**`
is in the §UNCHANGED files tripwire list). Registry build is unblocked
separately by WP-081.

**No action required by WP-056.** No scope amendment. No D-entry needed —
this is a documentation-to-reality drift, not a decision. Recommend a
future pre-flight guardrail: "before citing `pnpm -r build` in a
Verification Step, reality-reconcile against HEAD to confirm it is
green today."

### Finding 8.2 — EC-056 verification grep (`ctx`) vs session prompt exception

**What happened.** EC-056 §After Completing line 80 grep pattern is:
`\b(G|ctx|LegendaryGameState|LegendaryGame|boardgame\.io)\b` — includes
`ctx`. Session prompt Verification Step 13 grep omits `ctx` deliberately:
`\b(G|LegendaryGameState|LegendaryGame)\b`, and Stop Condition 16 declares
`ctx.turn + 1` the **only permitted framework reference** in JSDoc prose.
WP-056 §B source code block writes `ctx.turn + 1` verbatim in the
`appliesToTurn` JSDoc and prose.

**Is this a real violation?** No. Per authority hierarchy (WP > EC), the
WP spec is authoritative; the EC extracts the most drift-prone elements
into a quick-reference format and defers to the WP on conflicts. The
session prompt explicitly authorizes the `ctx.turn + 1` exception. The
EC grep was authored conservatively but is broader than the permitted
discipline.

**Resolution.** JSDoc written with `ctx.turn + 1` per WP + session prompt.
The EC grep, if run verbatim, would produce 2 hits (lines 21 and 51 of
`preplan.types.ts`, both `ctx.turn + 1` in the `appliesToTurn` invariant
JSDoc) — both are the authorized exception. Session prompt Verification
Step 13 (the operational test) returns zero hits, as expected.

**Recommend a future EC amendment** (not blocking for this WP): narrow
the grep pattern to exclude `ctx` and rely on the Step 13 session-prompt
form + Stop Condition 16 narrative exception, OR add an exception clause
to the EC line.

### All other reality checks

- `CardExtId` re-export at `packages/game-engine/src/index.ts:5` — ✓ matches pre-flight
- `CardExtId = string` at `packages/game-engine/src/state/zones.types.ts:23` — ✓ matches pre-flight
- `PlayerZones` shape includes `victory` — ✓ confirms the `victory` omission from `PrePlanSandboxState` is a deliberate design choice, not a spec error
- `pnpm-workspace.yaml` has `packages: - "packages/*"` — ✓ existing glob covers `packages/preplan/`
- Registry tsconfig pattern (NodeNext, ES2022, strict, exactOptionalPropertyTypes, noUncheckedIndexedAccess) — ✓ mirrored into preplan tsconfig; `lib: ["ES2022"]` (no DOM) and `exclude: ["node_modules", "dist"]` (no scripts dir) correctly narrowed per session prompt Task 4 guidance
- Test baseline 536/0 at session start — ✓ verified twice (before install; after install)

---

## Section 9 — 01.5 / 01.6 Trigger Verification

### 01.5 Runtime Wiring Allowance — NOT INVOKED

All four 01.5 trigger criteria confirmed absent (session prompt table,
verified against delivered code):

| Trigger | Applies? | Evidence |
|---|---|---|
| Adds a required field to `LegendaryGameState` or shared type | No | Zero engine files modified; `git diff packages/game-engine/**` returns empty |
| Changes the shape of a return type produced by `buildInitialGameState` | No | Zero setup orchestrator touched |
| Adds a new move to `LegendaryGame.moves` | No | Zero moves added; `CoreMoveName` / `CORE_MOVE_NAMES` unchanged |
| Adds a new phase hook that alters test scaffolding | No | Zero phase hooks added; `MATCH_PHASES` / `TURN_STAGES` unchanged |

01.5 remains non-invoked per P6-51 form (1) explicit declaration.

### 01.6 Post-Mortem — MANDATORY (three triggers fire)

All three independent 01.6 triggers confirmed present:

| Trigger | Evidence |
|---|---|
| New long-lived abstraction | `PrePlan` (+ `PrePlanSandboxState`, `RevealRecord`, `PrePlanStep`) — four types, single source of truth, JSDoc-pinned invariants |
| New contract consumed by future WPs | WP-057 (sandbox execution) and WP-058 (disruption detection) are documented downstream consumers; WP-057 also owns `PREPLAN_STATUS_VALUES`, WP-058 owns `PREPLAN_EFFECT_TYPES` per Finding #4 deferral lock |
| New code-category directory | `packages/preplan/` — new `preplan` category per D-5601 (top-level-package classification pattern following D-6301 / D-6511) |

This post-mortem file (`docs/ai/post-mortems/01.6-WP-056-preplan-state-model.md`)
satisfies the mandatory requirement and is staged into Commit A per session
prompt Locked Values.

---

## Section 10 — Fixes Applied During Post-Mortem

None. All FIXes (PS-1 / PS-2 / PS-3 / Finding #4 / Finding #10) landed
pre-execution in Commit A0 (`f2af0f3`). Zero in-allowlist refinements
discovered during execution.

Two **documentation-to-reality drifts** surfaced (Section 8 Findings 8.1
and 8.2); neither is a code change; both are recommendations for future
pre-flight / EC authoring discipline, not WP-056 scope amendments:

- **Finding 8.1** (pre-existing `pnpm -r build` registry failure): no WP-056
  action. Downstream WP-081 is chartered to fix. Recommend future pre-flight
  checklists reality-reconcile against `pnpm -r build` not just `pnpm test`.
- **Finding 8.2** (EC-056 grep pattern includes `ctx` while session prompt
  Stop Condition 16 permits `ctx.turn + 1`): no WP-056 action. WP > EC
  authority resolves cleanly. Recommend a future EC amendment to narrow
  the grep pattern or add an explicit exception clause.

---

## Final Verdict

**WP-056 COMPLETE.** Execution is correct, safe, and durable.

- Four public types present with JSDoc invariants.
- Zero runtime-executable code in `packages/preplan/**`.
- Zero architectural boundary violations (all greps return zero hits).
- Pre-existing `pnpm -r build` registry failure documented and scoped out
  (not WP-056-caused, not WP-056's responsibility to fix).
- Test baseline 536/0 unchanged.
- Lockfile delta scoped to `importers['packages/preplan']` only.
- All three 01.6 triggers satisfied by this post-mortem.
- All five DESIGN-CONSTRAINTS claimed by WP-056 (#1/#2/#3/#9/#10) JSDoc-pinned.

Ready for Commit A (`EC-056: ...`) followed by Commit B governance close
(`SPEC: close WP-056 / EC-056 governance`).
