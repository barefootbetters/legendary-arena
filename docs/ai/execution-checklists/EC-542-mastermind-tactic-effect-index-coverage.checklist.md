# EC-542 — Mastermind-Tactic Coverage in the Effect-Implementation Index / `/debug/effects` (Execution Checklist)

**Source:** docs/ai/work-packets/WP-507-mastermind-tactic-effect-index-coverage.md
**Layer:** Registry (schema) + Shared Tooling (generator) + App (dashboard)

## Before Starting
- [ ] Baseline: re-confirm `origin/main` HEAD; record it. Record the registry +
      dashboard suite pass counts before editing; if they moved, re-record.
- [ ] Confirm the current closed enum: `EFFECT_INDEX_ENTRY_SCOPES = ["hero",
      "villain"]` and `EffectImplementationSummarySchema.byScope` is a
      `.strict()` `{ hero, villain }` (`packages/registry/src/schema.ts`).
- [ ] Confirm the index generator reads ONLY the two ledgers + the registry
      schema today (`scripts/build-effect-implementation-index.mjs`) — the
      tactic feed is additive and must stay engine-free (card data + committed
      JSON only).
- [ ] Confirm the dashboard reader path: `useEffectIndex.ts`
      (`ScopeFilter = EffectIndexEntryScope | 'all'`, generic scope-equality
      filter — flows the new value through), `scopeLabel` switch,
      `EffectsPage.vue` byScope line + filter chips + `.fx-scope-*` CSS. The
      bundled `apps/dashboard/src/data/effect-implementation-index.json` is
      GITIGNORED / regenerated by `prebuild:effect-index` — do NOT hand-edit it.
- [ ] Confirm WP-506 is landed (`[x]` in WORK_INDEX) — WP-507 is **BLOCKED on
      WP-506 execution** (its `resolveCrushingShockwave` handler must exist
      before the provenance overlay can mark it `executable`, per verbatim
      honesty). If WP-506 is still `[ ]`, STOP.
- [ ] **Scaffold the enum widening first** (a closed-enum add can break an
      exhaustive `switch` or a drift test). Prototype the schema change, then
      **`pnpm --filter @legendary-arena/registry build` BEFORE** the dashboard
      typecheck/suite (the dashboard imports the enum from `registry/dist` — a
      stale dist is a false green), then run the registry + dashboard suites;
      fold any observed break into scope.

## The six `{ hero, villain }` literals — ALL gain `mastermind` (lockstep)
Widening `byScope` to a required `mastermind` key breaks these hardcoded
literals; three are hard failures, three are consistency/cosmetic:
- `schema.ts` `EffectImplementationSummarySchema.byScope` `.strict()` object — the type add.
- `schema.ts` `superRefine` `const scopeTally = { hero: 0, villain: 0 }` — **MUST-FIX or `NaN` fails validation.**
- `scripts/build-effect-implementation-index.mjs` `buildSummary` `const byScope = { hero: 0, villain: 0 }` — **MUST-FIX or `byScope.mastermind = NaN`.**
- `apps/dashboard/src/composables/useEffectIndex.ts` `EMPTY_INDEX.byScope` — **MUST-FIX or `vue-tsc` compile break.**
- `schema.effectImplementationIndex.test.ts` + `useEffectIndex.test.ts` `byScope` fixtures — fix or the tests fail on the widening.
- `build-effect-implementation-index.mjs` `main()` console.log + `apps/dashboard/scripts/build-effect-index.mjs` `EMPTY_INDEX_STUB` — cosmetic/consistency only.

## Locked Values (do not re-derive)
- New scope member: `"mastermind"` (added to `EFFECT_INDEX_ENTRY_SCOPES`, the
  `byScope` object, AND the `superRefine` `scopeTally` literal — see the six-
  literals section; the comparison loop iterates the array but the accumulator
  is a hardcoded literal, so `mastermind: 0` there is REQUIRED, not "verify").
- Tactic ext_id grammar: `${setAbbr}-mastermind-${slug}-${tacticSlug}`
  (`mastermind.setup.ts:240`); tactic cards = `masterminds[].cards[]` with
  `tactic: true` in `data/cards/*.json`.
- Per-tactic row: `mechanic` = the tactic **slug**; `name` = card `name`;
  `set` = the **file-level `abbr`** (NOT a per-mastermind field — `mm.setAbbr`
  is `undefined`); one row per tactic.
- Provenance file: `scripts/coverage/tactic-provenance.json`, keyed by tactic
  ext_id → `{ status, handler, wp, decision }`; only IMPLEMENTED tactics listed
  (WP-497 Octet → `co2e-mastermind-doctor-octopus-octet-of-valence-electrons`;
  WP-506 Crushing Shockwave → `core-mastermind-magneto-crushing-shockwave`,
  handler `packages/game-engine/src/rules/tacticHandlers.ts#...`).
- Default for an unmapped tactic: `status: "unmarked"`, `handler`/`wp`/`decision` = `""`.

## Guardrails
- Generator stays **engine-free** — no `packages/game-engine` import; card data
  + committed JSON only (mirrors the existing "reads NO engine dist" note).
- **Verbatim honesty (WP-493 / AC-5):** never fabricate a handler path, WP, or
  decision; a tactic absent from the provenance map is `unmarked` + blanks.
- Enum + array + every dependent consumer move in **lockstep** (drift rule):
  the schema enum, the byScope object, the byScope tally refinement, the
  dashboard `scopeLabel` case, the filter chip list, and the badge CSS all gain
  `mastermind` together.
- `mastermind` is a **closed-enum** member — no consumer treats scope as an
  open string; every `switch`/label handles it explicitly.
- No engine / `G` / determinism / persistence surface; NO card-data change.
- Commit the regenerated `data/metadata/effect-implementation-index.json`;
  `effect-index:check` must pass. Confirm the dashboard bundle regenerates via
  `prebuild:effect-index` — never hand-edit the gitignored copy.

## Required `// why:` Comments
- The `tactic-provenance.json` overlay rationale: identity is enumerated from
  card data so all tactics appear automatically; only status/handler/wp/decision
  are curated (the WP-493 generated-identity / curated-provenance split).
- `mechanic = tactic slug` — why each tactic is its own `card × mechanic` row.
- Generator stays engine-free — reads card data + committed JSON, mirroring the
  existing self-validation-only registry import.
- The new `mastermind` scope member — a closed-enum widening, a registry
  contract change (D-24313), kept in lockstep with byScope + the dashboard.
- Refresh the two now-stale generator comments — the header docblock ("Joins
  the TWO committed mechanic ledgers") and the `buildCards` "hero and villain
  ledgers use disjoint id-spaces" note — to include the card-data tactic feed.

## Files to Produce
- `packages/registry/src/schema.ts` — `+ "mastermind"` in the scopes array +
  `+ mastermind` in `byScope`; `+ mastermind: 0` in the `superRefine` `scopeTally`
  literal (required — see the six-literals section). Rebuild `dist`.
- `packages/registry/src/schema.effectImplementationIndex.test.ts` — valid
  fixture gains a `scope: "mastermind"` entry + `cards{}` + `byScope.mastermind`
  (exercises the `superRefine` tally); add a "wrong `byScope.mastermind` count
  rejects" case mirroring the existing hero reject; fix the existing `byScope`
  fixtures (`{ hero: 1, villain: 1 }`, `byScope.hero = 99`) for the new key.
- `scripts/coverage/tactic-provenance.json` — **NEW** — implemented-tactic overlay.
- `scripts/build-effect-implementation-index.mjs` — add the tactic feed
  (`readMastermindTactics` + merge into collect/cards/summary), engine-free;
  `+ mastermind: 0` in `buildSummary`'s `byScope` literal.
- `scripts/build-effect-implementation-index.test.ts` — **NEW `.test.ts`** (run
  via `node --import tsx --test`; the repo forbids `.test.mjs`) — tactic-feed
  entries + provenance resolution (executable vs unmarked) + `byScope.mastermind`
  tally.
- `package.json` — add `"effect-index:test"` script (mirrors `roadmap:counts:test`).
- `.github/workflows/ci.yml` — add a `run: pnpm effect-index:test` step beside
  the existing `pnpm roadmap:counts:test` (else the generator test never runs).
- `data/metadata/effect-implementation-index.json` — regenerated.
- `apps/dashboard/src/composables/useEffectIndex.ts` — `scopeLabel` mastermind case.
- `apps/dashboard/src/composables/useEffectIndex.test.ts` — scopeLabel + filter.
- `apps/dashboard/src/pages/debug/EffectsPage.vue` — byScope count + Mastermind
  filter chip + `.fx-scope-mastermind` CSS.
- The `EffectsPage` test (if present) — filter/summary render.
- Governance: `docs/ai/DECISIONS.md` (D-24313 Drafted→Active, edit in place);
  `WORK_INDEX.md`; `EC_INDEX.md`; `docs/05-ROADMAP-MINDMAP.md` `📝`→`✅` +
  `pnpm roadmap:counts:write`; `docs/ai/STATUS.md`.

## After Completing
- [ ] `pnpm --filter @legendary-arena/registry build` 0; schema test green.
- [ ] `pnpm effect-index` then `pnpm effect-index:check` 0; spot-check Octet +
      Crushing Shockwave `executable`, a random other tactic `unmarked`.
- [ ] `pnpm --filter @legendary-arena/dashboard test` + `build` 0; `pnpm -r build` 0.
- [ ] `git diff --name-only` = the allowlist + regenerated artifact + governance.
- [ ] D-24313 Active; `WORK_INDEX` + `EC_INDEX` flipped; mindmap `✅` + counts
      refreshed; `STATUS.md` updated.
- [ ] D-24026 live-verify (operator-pending): `/debug/effects` Mastermind filter
      shows core Magneto's four tactics, Crushing Shockwave + Octet executable.

## Common Failure Smells
- Registry test still passes but the dashboard `scopeLabel` throws / shows a
  blank chip → a consumer switch was not widened (enum/consumer drift).
- `effect-index:check` fails → the regenerated artifact was not committed, or
  the tactic feed is non-deterministic (sort order / set iteration).
- A tactic shows `executable` with no handler, or a fabricated handler → the
  provenance overlay/default logic is wrong; unmapped MUST be `unmarked` + blanks.
- `byScope.mastermind` mismatch → the schema tally refinement was not updated /
  the count is computed off a stale entry list.
- The gitignored dashboard `src/data/…json` got hand-edited → revert; it must
  come from `prebuild:effect-index`.
