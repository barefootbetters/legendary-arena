# EC-431 — Loki Hypno-Thrall Zone (Execution Checklist)

**Source:** docs/ai/work-packets/WP-398-loki-hypno-thrall-zone.md
**Layer:** Game Engine

## Before Starting
- [ ] **Shares `rules/mastermindHandlers.{ts,test.ts}` with WP-397 — NOT
      parallel-safe.** If WP-397 landed first, re-record the baseline pass
      count against current `main`
- [ ] `resolveLokiStrike` exists and its no-Strength-Hero path is the logged
      no-op this WP replaces (WP-388); `G.cardTraits` carries `heroClass`
      (WP-179); else STOP: abort and report
- [ ] Enumerate EVERY `MastermindState` construction site — expect four in
      `mastermind.setup.ts` (three degenerate early-returns + the computed
      path). Different count = STOP; a missed site is an `undefined` zone
- [ ] Confirm both oracles: the sentinel under `src/test/fixtures/games/`
      (regenerated by `record-game-fixture.mjs`) and `PRE_WP080_HASH` in
      `replay/replay.execute.test.ts`
- [ ] Exact target file set (any edit outside = FAIL; surface as a blocker
      first): `mastermind/mastermind.types.ts`,
      `mastermind/mastermind.setup.{ts,test.ts}`,
      `rules/mastermindHandlers.{ts,test.ts}`,
      `replay/replay.execute.test.ts`, the recorded sentinel fixture, plus
      `docs/ai/STATUS.md`, `docs/ai/DECISIONS.md`,
      `docs/ai/work-packets/WORK_INDEX.md`,
      `docs/ai/execution-checklists/EC_INDEX.md`,
      `docs/05-ROADMAP-MINDMAP.md`
- [ ] `pnpm --filter @legendary-arena/game-engine` build + test exit 0 —
      observed draft baseline (`origin/main` @ `01498ac1`): **2028 pass / 473
      suites / 0 fail**

## Locked Values (do not re-derive)
- Field: `hypnoThralls: CardExtId[]` on `MastermindState`, append-only, `[]`
  at EVERY construction site. Non-grey:
  `gameState.cardTraits?.[extId]?.heroClass != null`
- Selection extends `selectLowestCostHero` by a **plain discriminator**, NOT a
  callback (`code-style.md` §Functions)
- Branch order: Strength-Hero discard → Thrall stack → logged no-op. A player
  with neither gets the no-op; NEVER a substituted Wound
- Hand removal uses the WP-382 / D-24183 idiom with BOTH returned arrays
  assigned back (`moveCardFromZone` returns `{ from, to, found }`)
- Player iteration: `Object.keys(gameState.playerZones).sort()`
- Thralls attach to the MASTERMIND (`gameState.mastermind.hypnoThralls`),
  never to `playerZones` — the card stacks them next to Loki

## Guardrails
- **The sentinel fixture is REGENERATED by `scripts/record-game-fixture.mjs`
  and never hand-edited.** A hand-written hash is indistinguishable from a
  masked regression
- **Explain the delta before re-pinning.** Both oracles move for exactly ONE
  reason: `MastermindState` gained a field. **The fixture stores no serialized
  `G`** (keys: `name`, `input`, `meta`,
  `expected{finalStateHash, messages, outcome, snapshotPerTurn}`) — do NOT
  look for a `hypnoThralls: []` addition, it cannot exist. Pass condition:
  **only `expected.finalStateHash` changes**; `name`/`input`/`meta`/
  `expected.messages`/`.outcome`/`.snapshotPerTurn` byte-identical. Re-record
  by pinning the original metadata (read the seed / created-at /
  engine-version / `input` out of the existing fixture) so `meta` does not
  churn and self-trip this gate; check `record-game-fixture.mjs --help` for
  current flags rather than reciting an invocation
- `sim:runtime-observed:check` must stay current with NO regeneration (the
  matrix is `core/dr-doom`); if it regenerates, the change reached further
  than intended — STOP
- No removal path, no scoring/VP. No RNG, no `Math.random()`, no wall-clock
- No new `RuleEffect` type, move, or phase; **no `UIState` field** (WP-399 —
  adding it here breaks arena-client `vue-tsc` outside this allowlist). No
  `boardgame.io` / registry import
- Card conservation load-bearing: `hand + discard + thralls` before ≡ after

## Required `// why:` Comments
- The `hypnoThralls` field: append-only mastermind-adjacent zone mirroring
  D-15401 `attachedBystanders`; no removal path because no printed text
  returns a Thrall
- Why a new field is unavoidable when Doom's Omens were not: Omens are
  counted; Thralls are specific cards needing durable identification
- The Thrall branch: D-24192's gap closed; discard-first ordering is the
  tabletop "or" resolution — the second branch fires only when the first cannot
- The non-grey predicate: rulebook "Grey Heroes" = no Hero Class, so non-grey
  ⟺ `heroClass != null`; Wounds are grey and never stacked
- At the re-pin sites: the delta's single cause (`MastermindState` gained a
  field), so a future reader can tell an explained re-pin from a masked one

## Files to Produce
- `mastermind/mastermind.types.ts` — **modified** — `hypnoThralls` field
- `mastermind/mastermind.setup.ts` — **modified** — `[]` at all four sites
- `mastermind/mastermind.setup.test.ts` — field present at every site
- `rules/mastermindHandlers.{ts,test.ts}` — Thrall branch + AC-1..AC-7
- `replay/replay.execute.test.ts` — **modified** — `PRE_WP080_HASH` re-pin
- the recorded sentinel fixture — **modified** — REGENERATED by script
- `docs/ai/STATUS.md`, `docs/ai/DECISIONS.md` (D-24201 Active),
  `WORK_INDEX.md`, `EC_INDEX.md`, `docs/05-ROADMAP-MINDMAP.md` — governance

## After Completing
- [ ] `pnpm --filter @legendary-arena/game-engine` build + test exit 0 after
      the re-pins; record the pass count
- [ ] `pnpm -r build` exits 0
- [ ] Sentinel fixture diff inspected — ONLY the `hypnoThralls: []` addition
      and the recomputed hash; both hash values recorded in the close-out with
      the one-line reason
- [ ] `pnpm sim:runtime-observed:check` exits 0, **no regeneration**
- [ ] **Control run:** revert the Thrall branch, record the observed failure
      count; a passing suite means the tests are vacuous
- [ ] `git diff --name-only` on STAGED changes = exactly the allowlist
- [ ] `docs/ai/STATUS.md` states **"No user-observable change — infrastructure
      only; the Thrall zone is not visible until WP-399"**
- [ ] `docs/ai/DECISIONS.md` D-24201 Active **and D-24192 amended inline —
      Loki half closed, which with WP-397 completes its recorded fidelity
      gap**; `WORK_INDEX.md` + `EC_INDEX.md`
      flipped with date; mindmap node `📝`→`✅` + `roadmap:counts:write`
- [ ] WP-399 confirmed queued as the required follow-up

## Common Failure Smells
- A `MastermindState` is built without `hypnoThralls` → a construction site
  was missed; the degenerate early-returns are the ones that get forgotten
- Anything beyond `expected.finalStateHash` differs in the fixture → the
  re-record churned `meta` or the change reached further; do not re-pin,
  investigate
- A Wound or a grey starter appears in `hypnoThralls` → the predicate inverted
