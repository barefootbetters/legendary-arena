# EC_INDEX.md — Execution Checklist Index

> **Purpose:** Tracks all Execution Checklists and their readiness status.  
> Subordinate to `docs/ai/work-packets/WORK_INDEX.md` — if status conflicts, WORK_INDEX.md wins.

> **Execution Rule:**  
> No implementation work may begin on a Work Packet unless a corresponding Execution Checklist (EC) exists in this index.

> **Status legend:**
> - `Done` — WP fully executed and validated; EC preserved for reference and regression  
> - `Draft` — EC generated and ready for execution  
> - `Blocked` — EC exists but WP dependencies are not met

> **Note:** Phase 0 ECs may be marked `Done` earlier than others due to documentation-only scope.

---

## Governing Documents

- **EC Template:** `docs/ai/execution-checklists/EC-TEMPLATE.md`
- **EC Workflow:** `docs/ai/REFERENCE/01.1-how-to-use-ecs-while-coding.md`
- **Bug Handling:** `docs/ai/REFERENCE/01.2-bug-handling-under-ec-mode.md`

All bugs must be handled through the bug handling protocol once any EC
is active. See 01.2 for the clause-driven diagnosis sequence.

---

## Foundation Prompts

| EC      | Source    | Layer                  | Execution Scope                                      | Status |
|---------|-----------|------------------------|------------------------------------------------------|--------|
| EC-FP01 | FP-01     | Server / Infrastructure | Render.com backend, rules loader, schema, render.yaml | Done   |

---

## Phase 0 — Coordination & Contracts

| EC     | Source WP | Layer                          | Execution Scope                                      | Status |
|--------|-----------|--------------------------------|------------------------------------------------------|--------|
| EC-001 | WP-001    | Documentation / Coordination   | Repo-as-memory system, REFERENCE docs                | Done   |
| EC-002 | WP-002    | Game Engine                    | boardgame.io Game skeleton, LegendaryGame            | Draft  |
| EC-003 | WP-003    | Registry                       | Card registry defect fixes, smoke test               | Draft  |
| EC-004 | WP-004    | Server                         | Server bootstrap, registry + rules wiring            | Draft  |
| EC-043 | WP-043    | Registry / Contracts (Docs)    | Data contracts reference migration                   | Draft  |
| EC-044 | WP-044    | Coordination / Quality Gate    | Prompt lint governance alignment                     | Draft  |
| EC-045 | WP-045    | Coordination / Execution Gate  | Connection health check alignment                    | Draft  |
| EC-046 | WP-046    | Coordination / Execution Gate  | R2 validation governance alignment                   | Draft  |
| EC-047 | WP-047    | Coordination / Quality Gate    | Code style reference alignment                       | Draft  |

---

## Phase 1 — Game Setup Contracts & Determinism

| EC      | Source WP | Layer                    | Execution Scope                                      | Status |
|---------|-----------|--------------------------|------------------------------------------------------|--------|
| EC-005A | WP-005A   | Game Engine / Contracts  | MatchSetupConfig (9 fields), validateMatchSetup      | Draft  |
| EC-005B | WP-005B   | Game Engine / Setup      | shuffleDeck, buildInitialGameState, Game.setup()     | Draft  |
| EC-006A | WP-006A   | Game Engine / Contracts  | PlayerZones (5), GlobalPiles (4), validators         | Draft  |
| EC-006B | WP-006B   | Game Engine / Setup      | buildPlayerState, buildGlobalPiles                   | Draft  |

---

## Phase 2 — Core Turn Engine

| EC      | Source WP | Layer                    | Execution Scope                                      | Status |
|---------|-----------|--------------------------|------------------------------------------------------|--------|
| EC-007A | WP-007A   | Game Engine / Contracts  | MATCH_PHASES, TURN_STAGES, getNextTurnStage          | Draft  |
| EC-007B | WP-007B   | Game Engine / Turn Loop  | advanceTurnStage, G.currentStage, play phase         | Draft  |
| EC-008A | WP-008A   | Game Engine / Contracts  | MoveResult/MoveError, CORE_MOVE_NAMES, stage gating  | Draft  |
| EC-008B | WP-008B   | Game Engine / Moves      | drawCards, playCard, endTurn, zoneOps.ts             | Draft  |

---

## Phase 3 — MVP Multiplayer Infrastructure

| EC     | Source WP | Layer                    | Execution Scope                                      | Status |
|--------|-----------|--------------------------|------------------------------------------------------|--------|
| EC-009A| WP-009A   | Game Engine / Contracts  | HookDefinition, RULE_TRIGGER_NAMES, RULE_EFFECT_TYPES| Draft  |
| EC-009B| WP-009B   | Game Engine / Rules Runtime | ImplementationMap, executeRuleHooks, applyRuleEffects| Draft  |
| EC-010 | WP-010    | Game Engine / Endgame    | evaluateEndgame, ENDGAME_CONDITIONS, endIf wiring    | Draft  |
| EC-011 | WP-011    | Server + Game Engine     | G.lobby, match creation, lobby moves, CLI script     | Draft  |
| EC-012 | WP-012    | Server / Match Access    | list-matches, join-match CLI scripts                 | Draft  |
| EC-013 | WP-013    | Architecture / Data Lifecycle | PERSISTENCE_CLASSES, MatchSnapshot, ARCHITECTURE.md | Draft  |

---

## Phase 4 — Core Gameplay Loop

| EC     | Source WP | Layer                    | Execution Scope                                      | Status |
|--------|-----------|--------------------------|------------------------------------------------------|--------|
| EC-014A | WP-014A  | Game Engine / Core Gameplay | Reveal pipeline, trigger emission, REVEALED_CARD_TYPES (no deck construction) | Draft  |
| EC-015 | WP-015    | Game Engine / Board Zones| G.city, G.hq, villain movement, escapes              | Draft  |
| EC-016 | WP-016    | Game Engine / Core Actions | fightVillain, recruitHero moves                      | Draft  |
| EC-017 | WP-017    | Game Engine / Zones + Effects | G.ko, G.attachedBystanders, wounds                 | Draft  |
| EC-018 | WP-018    | Game Engine / Economy    | G.turnEconomy, G.cardStats, parseCardStatValue       | Draft  |
| EC-019 | WP-019    | Game Engine / Boss Fight | G.mastermind, tactics deck, fightMastermind          | Draft  |
| EC-020 | WP-020    | Game Engine / Scoring    | computeFinalScores, VP constants                     | Draft  |

---

## Phase 5 — Card Mechanics & Abilities

| EC     | Source WP | Layer                    | Execution Scope                                      | Status |
|--------|-----------|--------------------------|------------------------------------------------------|--------|
| EC-021 | WP-021    | Game Engine / Rules (Contracts) | HeroAbilityHook, HeroKeyword, G.heroAbilityHooks  | Draft  |
| EC-022 | WP-022    | Game Engine / Rules Execution | 4 unconditional hero keywords execution            | Draft  |
| EC-023 | WP-023    | Game Engine / Rules Execution | 4 condition types, AND logic evaluation            | Draft  |
| EC-024 | WP-024    | Game Engine / Rules Execution | Scheme twist + mastermind strike handlers          | Draft  |
| EC-025 | WP-025    | Game Engine / Board Keywords | BoardKeyword (patrol/ambush/guard), G.cardKeywords| Draft  |
| EC-026 | WP-026    | Game Engine / Setup      | SchemeSetupInstruction, city modifiers               | Draft  |

---

## Phase 6 — Verification, UI & Production

| EC     | Source WP | Layer                    | Execution Scope                                      | Status |
|--------|-----------|--------------------------|------------------------------------------------------|--------|
| EC-027 | WP-027    | Engine Infrastructure    | ReplayInput, replayGame, verifyDeterminism           | Draft  |
| EC-028 | WP-028    | Engine / UI Boundary     | UIState contract, buildUIState pure function         | Draft  |
| EC-029 | WP-029    | Engine / UI Boundary     | UIAudience, filterUIStateForAudience                 | Draft  |
| EC-030 | WP-030    | Game Orchestration       | Campaign/Scenario framework, external to engine      | Draft  |
| EC-031 | WP-031    | Engine Safety            | 5 invariant categories, assertInvariant              | Draft  |
| EC-032 | WP-032    | Multiplayer Safety       | ClientTurnIntent, desync detection                   | Draft  |
| EC-033 | WP-033    | Game Engine / Content    | validateContent, validateContentBatch                | Draft  |
| EC-034 | WP-034    | Game Engine / Versioning | 3 version axes (`EngineVersion` semver, `DataVersion` integer, `ContentVersion` integer), `VersionedArtifact<T>` wrapper, `checkCompatibility` (pure decision; never throws), `migrateArtifact` (forward-only; throws on no path per D-0802 load-boundary exception), `stampArtifact` (single permitted wall-clock read in subtree per D-3401 sub-rule), MVP `migrationRegistry = Object.freeze({})`. Five new files under `packages/game-engine/src/versioning/` (D-3401 engine code category). 9 new tests in one `describe('versioning (WP-034)')` block; engine 427→436, repo 517→526. P6-43 paraphrase pass caught six JSDoc/grep collisions at first verification gate run — first empirical demonstration of cross-session precedent-log value. 01.5 NOT INVOKED. 01.6 post-mortem MANDATORY (new long-lived abstraction `VersionedArtifact<T>` + new code-category directory D-3401) — verdict WP COMPLETE. See `EC-034-versioning.checklist.md` + `docs/ai/invocations/session-wp034-versioning-save-migration.md`. Executed 2026-04-19 at commit `5139817`. | Done   |
| EC-035 | WP-035    | Operations / Release     | Release artifacts, 4 environments (`dev` → `test` → `staging` → `prod` sequential promotion), rollback (revert engine + content together; never roll `dataVersion` forward; re-apply last known good; no data loss — D-0902), P0–P3 incident severity ladder with D-0802 vs D-1234 mapping, four-field incident-record contract. Six new files (3 ops docs under `docs/ops/` + 1 new engine type file at `packages/game-engine/src/ops/ops.types.ts` under D-3501 engine code category + 2 modified re-exports in `types.ts` / `index.ts`). Pure type definitions only — `OpsCounters`, `DeploymentEnvironment`, `IncidentSeverity` with no runtime instance anywhere in the engine (RS-1 option (a)). Zero new tests (RS-2 lock); engine baseline UNCHANGED at 436 / 109 / 0 fail; repo-wide 526 / 0 fail. 01.5 NOT INVOKED. 01.6 post-mortem MANDATORY (new long-lived abstraction `OpsCounters` + new code-category directory D-3501) — verdict WP COMPLETE with zero mid-execution fixes (pre-flight paraphrase-discipline Locked Value prevented the P6-43 collision class that surfaced in WP-034). See `EC-035-release-ops.checklist.md` + `docs/ai/invocations/session-wp035-release-deployment-ops-playbook.md`. Executed 2026-04-19 at commit `d5935b5`. | Done   |
| EC-042 | WP-042    | Server / Operations      | Deployment checklists — R2 full (§A.1–§A.7) + PostgreSQL scope-reduced to §B.1 / §B.2 / §B.6 / §B.7 (§B.3 / §B.4 / §B.5 / §B.8 deferred to WP-042.1 per D-4201 — `scripts/seed-from-r2.mjs` has never existed). Seven-file allowlist (two new checklists under `docs/ai/deployment/` + ARCHITECTURE.md one-line cross-reference + RELEASE_CHECKLIST.md two back-pointer blocks + D-4202 P6-51 form-(2) legacy §C UI-rendering-layer exclusion back-pointer + D-4203 P6-51 form-(1) Documentation-class invariant for WP-042 + mandatory 01.6 post-mortem). Zero runtime code / zero new scripts / zero `package.json` edits / zero new tests; engine baseline UNCHANGED at 436 / 109 / 0 fail; repo-wide 526 / 0 fail. Documents the three real Foundation Prompt 02 migrations (`001_server_schema.sql`, `002_seed_rules.sql`, `003_game_sessions.sql`) plus the six real env vars exposed by `validate.ts` (`METADATA_DIR`, `SETS_DIR`, `HEALTH_OUT`, `R2_BASE_URL`, `SKIP_IMAGES`, `IMAGE_DELAY_MS` — post-mortem §8 flags the drift from the session prompt's RS-3 `CARDS_DIR` lock). Paraphrase discipline per P6-50 (`Konva` / `canvas` / `boardLayout` / `CARD_TINT` / `game-engine` / framework name / `LegendaryGame` / framework-context grep all zero). 01.5 NOT INVOKED. 01.6 post-mortem MANDATORY (first concrete consumer of WP-035's back-pointer pattern + two canonical deployment-pillar checklist surfaces) — verdict WP COMPLETE with zero mid-execution fixes (three reality-reconciliation findings resolved during authoring, not after tests). See `EC-042-deployment-checklists.checklist.md` + `docs/ai/invocations/session-wp042-deployment-checklists.md`. Executed 2026-04-19 at commit `c964cf4`. | Done   |
| EC-048 | WP-048    | Engine Scoring + Server  | PAR scoring, ScenarioKey, LeaderboardEntry contract  | Draft  |
| EC-055 | WP-055    | Registry / Content Contracts | Theme Data Model (v2): `ThemeDefinitionSchema` + sub-schemas (`ThemeSetupIntentSchema`, `ThemePlayerCountSchema`, `ThemePrimaryStoryReferenceSchema`, `ThemeMusicAssetsSchema`) in `packages/registry/src/theme.schema.ts`; `validateTheme` (sync) + `validateThemeFile` (async, filename-to-themeId alignment, four stable error-path labels `'file'`/`'json'`/`'themeId'`/`<zod-path>`) in `theme.validate.ts`; 10 `node:test` cases wrapped in one `describe('theme schema (WP-055)')` block in `theme.schema.test.ts` (registry 3→13 / 1→2 / 0 fail; repo-wide 526→536; engine 436/109/0 UNCHANGED). `setupIntent` mirrors WP-005A `MatchSetupConfig` ID fields verbatim (count fields excluded — themes describe composition, not pile sizing). Three optional music fields added at v2 per D-5509 (`musicTheme`, `musicAIPrompt`, `musicAssets` with eight optional `*Url` sub-fields). 74-file allowlist: `theme.schema.ts` / `theme.validate.ts` / `theme.schema.test.ts` + `content/themes/minimal-example.json` + mandatory 01.6 post-mortem NEW + 68 shipped `content/themes/*.json` v1→v2 migration per D-5509 (migration staged during 2026-04-19 v2 design pass; committed under WP-055 allowlist) + `packages/registry/src/index.ts` public-surface extension MODIFIED (8 additive export lines per WP-055 §E; no existing export reordered/renamed/removed). §D.8 directory-scan test iterates `content/themes/index.json` manifest (NOT `readdir`) so aggregate artifacts (`00-ALL_THEMES_COMBINED.json`, `01-ALL_THEMES_COMBINED.json`, `index.json` itself) are excluded. Registry-layer only — zero `game-engine` / `boardgame.io` / `apps/server/` / `apps/registry-viewer/` imports (grep confirmed); immutable WP-003 files (`schema.ts`, `shared.ts`, `impl/localRegistry.ts`) untouched; WP-005A `matchSetup.types.ts` untouched; `apps/registry-viewer/` remains quarantined in `stash@{0}` per PS-4. 01.5 NOT INVOKED. 01.6 post-mortem MANDATORY — verdict WP COMPLETE with zero post-mortem fixes and one mid-execution paraphrase-token self-catch (`packages/game-engine/src/matchSetup.types.ts` literal path reference removed from a `// why:` comment at Verification Step 4, documented in post-mortem §8). Three-commit topology A0 `aaba66d` (SPEC pre-flight bundle) + A `dc7010e` (EC-055 execution) + B (this commit, SPEC governance close). See `EC-055-theme-data-model.checklist.md` + `docs/ai/invocations/session-wp055-theme-data-model.md`. Executed 2026-04-20 at commit `dc7010e`. | Done   |
| EC-056 | WP-056    | Pre-Planning (new `preplan` category, D-5601) | Pre-Planning State Model & Lifecycle (Read-Only Core): types-only Contract WP creating the new `packages/preplan/` package (four new files: `package.json`, `tsconfig.json`, `src/preplan.types.ts`, `src/index.ts`) plus `pnpm-lock.yaml` delta scoped to a new `importers['packages/preplan']` block (+10 lines, zero cross-importer churn per P6-44). Four public types: `PrePlan` (with `prePlanId`, `revision`, `playerId`, `appliesToTurn = ctx.turn + 1`, closed-union `status: 'active' \| 'invalidated' \| 'consumed'`, optional `invalidationReason` with closed-union `effectType: 'discard' \| 'ko' \| 'gain' \| 'other'`, `baseStateFingerprint` NON-GUARANTEE clause preserved verbatim), `PrePlanSandboxState` (player-visible zones only — `hand`/`deck`/`discard`/`inPlay`/`counters`; `victory` omitted by design per DESIGN-CONSTRAINT #9), `RevealRecord` (sole rewind authority per DESIGN-CONSTRAINT #3; open `source` union with `\| string` fallback), `PrePlanStep` (open `intent` union with `\| string` fallback — advisory/descriptive, intentionally NOT `CoreMoveName`). Canonical readonly arrays + drift-detection tests for `status` / `effectType` closed unions **deferred to WP-057 / WP-058** (first runtime consumers). `pnpm-workspace.yaml` NOT modified — existing `packages/*` glob already covers the new package (PS-3 correction of WP spec). Test baseline 536 passing / 0 failing UNCHANGED (zero tests added — RS-2 zero-test lock). Layer-boundary integrity: zero `boardgame.io` / runtime engine / registry / `apps/**` / `Math.random` / `require(` / `.reduce(` hits under `packages/preplan/`; zero `G` / `LegendaryGameState` / `LegendaryGame` / `boardgame.io` tokens in JSDoc prose (P6-50 paraphrase discipline) — only permitted framework reference is `ctx.turn + 1` in `appliesToTurn` invariant JSDoc (session prompt Stop Condition 16 explicit exception). 01.5 NOT INVOKED (four criteria all absent). 01.6 post-mortem MANDATORY (three triggers fire: new long-lived abstraction `PrePlan` + contract consumed by WP-057/058 + new code-category directory D-5601) — verdict WP COMPLETE with zero in-code fixes (one pre-existing `pnpm -r build` registry failure documented in §8 Finding 8.1 — orthogonal to WP-056, addressed by parallel WP-081; one EC/WP grep-pattern drift documented in §8 Finding 8.2 — `ctx` narrowing recommended for future EC amendment, non-blocking). Commits use `EC-056:` prefix (never `WP-056:` per P6-36). Three-commit topology: A0 `f2af0f3` (SPEC pre-flight bundle: EC-056 + D-5601 + 02-CODE-CATEGORIES.md + DECISIONS_INDEX.md + WP-056 amendments + pre-flight doc) → A `eade2d0` (EC-056 execution: preplan package + 01.6 post-mortem) → B (this commit, SPEC governance close: WORK_INDEX / STATUS / EC_INDEX). See `EC-056-preplan-state-model.checklist.md` + `docs/ai/invocations/session-wp056-preplan-state-model.md`. Executed 2026-04-20 at commit `eade2d0`. | Done   |
| EC-057 | WP-057    | Pre-Planning (continuing `preplan` category, D-5601) | Pre-Plan Sandbox Execution — first runtime consumer of WP-056's `PrePlan` contract. Nine new files under `packages/preplan/src/` (four implementation — `speculativePrng.ts`, `preplanSandbox.ts`, `speculativeOperations.ts`, `preplanStatus.ts` — plus four `*.test.ts` files; 01.6 post-mortem as the tenth). Two modified implementation files: `index.ts` (type-only → mixed runtime/type export surface per RS-2 — authorized progression from WP-056's Contract-Only surface) and `package.json` (adds `"test": "node --import tsx --test src/**/*.test.ts"` script + `"tsx": "^4.15.7"` devDep per PS-3; mirrors `packages/registry/package.json:19, 34` exactly). `pnpm-lock.yaml` delta scoped to `importers['packages/preplan']` devDep block only (P6-44 tripwire). Ten public function signatures locked verbatim in EC-057 §Locked Values: `createSpeculativePrng` / `speculativeShuffle` / `generateSpeculativeSeed` (Fisher-Yates PRNG; `Date.now()` permitted exactly once at `generateSpeculativeSeed` per DESIGN-PREPLANNING §3 — Step 17 grep gate expects exactly one hit); `createPrePlan` / `computeStateFingerprint`; five speculative operations (`speculativeDraw` / `speculativePlay` / `updateSpeculativeCounter` / `addPlanStep` / `speculativeSharedDraw`) — all five return `null` when `prePlan.status !== 'active'` (uniform null-on-inactive RS-8, test 12 proves 5×2 matrix); `revision` `+1` on every successful mutation / `0 delta` on every null-return path (test 13 proves 5×2 branch matrix); spread-copy discipline on every returned `sandboxState.hand` / `deck` / `discard` / `inPlay` / `counters` plus `revealLedger` / `planSteps` (WP-028 aliasing precedent — post-mortem §6 aliasing trace MANDATORY, line-by-line per return statement); `appliesToTurn = snapshot.currentTurn + 1` unconditionally (DESIGN-CONSTRAINT #10); reveal ledger monotonicity — every successful `speculativeDraw` appends one `RevealRecord` with `source: 'player-deck'` and strictly-increasing `revealIndex` from zero, every successful `speculativeSharedDraw` appends one with caller-supplied source. Canonical readonly array: `PREPLAN_STATUS_VALUES = ['active', 'invalidated', 'consumed'] as const` + `PrePlanStatusValue = typeof PREPLAN_STATUS_VALUES[number]` derived type + compile-time exhaustive-check (`PrePlanStatusValue extends PrePlan['status'] ? PrePlan['status'] extends PrePlanStatusValue ? true : never : never`) + runtime drift-detection test (`preplanStatus.test.ts`) — deferred from WP-056 per EC-056 Locked Value line 32 (PS-2). `PREPLAN_EFFECT_TYPES` remains WP-058 scope — must NOT be pulled forward. `PlayerStateSnapshot` type locked verbatim (six fields: `playerId`, `hand`, `deck`, `discard`, `counters`, `currentTurn`). `computeStateFingerprint` non-goals locked: deterministic + content-sensitive only; NOT cryptographic, NOT collision-resistant, NOT cross-process stable; trivial FNV-1a / djb2 over sorted-keys stringification suffices; `node:crypto` / hashing libraries forbidden. Shuffle fixture discipline (RS-11): ≥8-card deck + proven non-identity seed with `// why:` comment. Test baseline: preplan `0/0/0 → 23/4/0` across four test files each wrapped in exactly one top-level `describe()` block (bare `test()` forbidden per WP-031 precedent); engine UNCHANGED at `436/109/0` (WP-057 touches zero engine code); registry `13/2/0` / vue-sfc-loader `11/?/0` / server `6/?/0` / replay-producer `4/?/0` / arena-client `66/?/0` all unchanged; repo-wide `536 → 559 passing / 0 failing`. `packages/preplan/src/preplan.types.ts` immutable in this WP (`git diff` empty tripwire); `packages/preplan/tsconfig.json` unchanged; `pnpm-workspace.yaml` unchanged (existing `packages/*` glob already covers preplan). Layer-boundary integrity — 24 verification-step greps all zero-or-expected: no `boardgame.io` imports (escaped dot per P6-22); no runtime engine imports (`import type` permitted); no `@legendary-arena/registry` / `pg` / `apps/**` imports; no `Math.random` / `ctx.random` anywhere; no `require(` (ESM only); no `.reduce(` (code-style invariant extends to preplan); `Date.now` exactly one hit at `speculativePrng.ts:generateSpeculativeSeed`. P6-50 paraphrase discipline: zero `G` / `LegendaryGameState` / `LegendaryGame` / `boardgame.io` tokens in code or JSDoc prose; `ctx` appears only in the inherited `ctx.turn + 1` invariant carve-out from `preplan.types.ts:54` (WP-056 carve-out preserved). 01.5 Runtime Wiring Allowance NOT INVOKED — four criteria all absent (no `LegendaryGameState` field added; no `buildInitialGameState` shape change; no new `LegendaryGame.moves` entry; no new phase hook). 01.6 Post-Mortem MANDATORY — three triggers fire (new long-lived abstractions: `speculativePrng` / sandbox factory / five speculative operations / `PREPLAN_STATUS_VALUES`; first runtime consumer of `PrePlan.status` closed union; contract consumed by WP-058 disruption pipeline). Copilot Check (01.7): CONFIRM — 30/30 PASS after re-run 2026-04-20 following three HOLD FIXes (Date.now grep gate added; test 12 uniform null-on-inactive parameterized over 5 operations × 2 non-active statuses; test 13 revision-increment discipline parameterized over 5 operations × 2 branches). Commits use `EC-057:` prefix on code-changing commits (never `WP-057:` per P6-36 — commit-msg hook rejects); `SPEC:` on governance / pre-flight / governance-close commits. Three-commit topology: A0 `SPEC:` pre-flight bundle (this landing: EC-057 + WP-057 amendments + pre-flight + session-context + EC_INDEX row + session prompt) → A `EC-057:` execution (10 implementation files + `pnpm-lock.yaml` + 01.6 post-mortem + any D-5701 entries) → B `SPEC:` governance close (STATUS + WORK_INDEX + EC_INDEX status flip). Staging by exact filename only — never `git add .` / `git add -A` / `git add -u` (P6-27 / P6-44). Inherited dirty-tree items (10 unrelated untracked files + `.claude/worktrees/`) untouched and never staged; quarantine stashes `stash@{0..2}` intact and never popped. See `EC-057-preplan-sandbox-execution.checklist.md` + `docs/ai/invocations/session-wp057-preplan-sandbox-execution.md` + `docs/ai/invocations/preflight-wp057-preplan-sandbox-execution.md` + `docs/ai/session-context/session-context-wp057.md`. Executed 2026-04-20 at commit `8a324f0`. | Done   |
| EC-058 | WP-058    | Pre-Planning (continuing `preplan` category, D-5601) | Pre-Plan Disruption Pipeline (Detect → Invalidate → Rewind → Notify) — first runtime consumer of WP-056's `invalidationReason.effectType` closed union + first implementation of DESIGN-CONSTRAINT #3 "reveal ledger is sole rewind authority". Eight new files under `packages/preplan/src/`: `disruption.types.ts` (four public types consolidated per PS-3: `PlayerAffectingMutation`, `DisruptionNotification`, `SourceRestoration`, `DisruptionPipelineResult`); `disruptionDetection.ts` (`isPrePlanDisrupted`); `disruptionPipeline.ts` (`invalidatePrePlan`, `computeSourceRestoration`, `buildDisruptionNotification`, internal `buildNotificationMessage`, `executeDisruptionPipeline`); `preplanEffectTypes.ts` (canonical `PREPLAN_EFFECT_TYPES` readonly array + `PrePlanEffectType` + compile-time drift check using `NonNullable<PrePlan['invalidationReason']>['effectType']` per PS-2); three `*.test.ts` files each wrapped in exactly one top-level `describe()` block. One modified file: `packages/preplan/src/index.ts` (additive WP-058 export block; WP-056 + WP-057 blocks unchanged). `packages/preplan/package.json` and `pnpm-lock.yaml` explicitly NOT in allowlist — `tsx` devDep + test script inherited from WP-057. Five public function signatures locked verbatim in EC-058 §Locked Values. Null-on-inactive convention extended from WP-057 RS-8: `isPrePlanDisrupted` returns `false`; `invalidatePrePlan` / `executeDisruptionPipeline` return `null`. Programming-error throw ONLY at `buildDisruptionNotification` when `status !== 'invalidated'` (caller misuse; `executeDisruptionPipeline` is the sole expected caller). `PrePlan.revision` MUST NOT increment in `invalidatePrePlan` (status + `invalidationReason` are not revision-bumping fields per `preplan.types.ts:36-38`) — test asserts `invalidatedPlan.revision === prePlan.revision`. Full-spread 42/42 discipline on `invalidatePrePlan` return (every `sandboxState.hand` / `deck` / `discard` / `inPlay` / `counters` + `revealLedger` + `planSteps` fresh copy even though invalidation doesn't semantically change them — WP-028 aliasing precedent + WP-057 post-mortem §6 pattern; 01.6 §6 aliasing trace MANDATORY). Conditional-assignment pattern at two `affectedCardExtId` sites (RS-4): `invalidatePrePlan` → `invalidationReason.affectedCardExtId` and `buildDisruptionNotification` → returned `DisruptionNotification.affectedCardExtId` — required under `exactOptionalPropertyTypes: true` (`packages/preplan/tsconfig.json:8`). Ledger-sole rewind authority (DESIGN-CONSTRAINT #3): `computeSourceRestoration` reads only `revealLedger`, never `sandboxState` — Copilot Issue 11 test asserts by passing a plan whose sandbox disagrees with the ledger and verifying restoration follows the ledger. `executeDisruptionPipeline` reads `prePlan.revealLedger` (not `invalidatedPlan.revealLedger`) with required `// why:` comment (RS-8) — semantically equivalent because invalidation doesn't mutate ledger. `DisruptionPipelineResult.requiresImmediateNotification: true` is a literal type (not `boolean`) — type-level Constraint #7 enforcement per amendment A-058-05 (Copilot Issue 15). First-mutation-wins semantics (WP-058 §Architectural Placement lines 120-125) enforced mechanically via status guard in `isPrePlanDisrupted` + `invalidatePrePlan` — no caller dedup logic. Test baseline: preplan `23 / 4 / 0 → 52 / 7 / 0` across 3 new suites (detection 5 + pipeline 23 [invalidation 4 + restoration 6 including ledger-sole Issue 11 + notification 5 + full pipeline 7 + acceptance 1] + effect-type drift 1 = 29 tests); engine UNCHANGED at `436 / 109 / 0` (WP-058 touches zero engine code); registry / vue-sfc-loader / server / replay-producer / arena-client all unchanged; repo-wide `559 → 588 passing / 0 failing`. Layer-boundary integrity — verification-step greps all zero-or-expected: no `boardgame.io` imports (escaped dot per P6-22); no runtime engine imports (`import type { CardExtId }` only); no `@legendary-arena/registry` / `pg` / `apps/**` imports; no `Math.random` / `ctx.random` anywhere; no `Date.now` in new WP-058 files (pre-existing single hit at `speculativePrng.ts:79` WP-057 carve-out must remain the only hit — amendment A-058-03 per Copilot Issue 2); no `require(` (ESM only); no `.reduce(` (code-style invariant extends to preplan; `computeSourceRestoration` uses explicit `for...of`). P6-50 paraphrase discipline: zero `G` / `LegendaryGameState` / `LegendaryGame` / `boardgame.io` tokens in code or JSDoc prose in new files; `ctx` appears only in the inherited `ctx.turn + 1` invariant carve-out at `preplan.types.ts:21, :51` (WP-056 carve-out preserved). WP-056 immutable (`preplan.types.ts` `git diff` empty tripwire) + WP-057 immutable (`preplanStatus.ts` / `speculativePrng.ts` / `preplanSandbox.ts` / `speculativeOperations.ts` all `git diff` empty). `PREPLAN_EFFECT_TYPES` canonical readonly array deferred from WP-056 per `preplan.types.ts:101-106` JSDoc (WP-057 picked up `PREPLAN_STATUS_VALUES`; WP-058 picks up `PREPLAN_EFFECT_TYPES`); compile-time drift-check pattern uses `NonNullable<>` accommodation required because `invalidationReason` is optional on `PrePlan`. One `describe()` per test file convention (WP-031 / WP-057 precedent) — `describe('preplan disruption detection (WP-058)')` / `describe('preplan disruption pipeline (WP-058)')` / `describe('preplan effect-type drift (WP-058)')`. 01.5 Runtime Wiring Allowance NOT INVOKED — four criteria all absent (no `LegendaryGameState` field added; no `buildInitialGameState` shape change; no new `LegendaryGame.moves` entry; no new phase hook). 01.6 Post-Mortem MANDATORY — four triggers fire (new long-lived abstractions: detection / invalidation / restoration / notification / pipeline orchestration; first runtime consumer of `invalidationReason.effectType` closed union; first implementation of DESIGN-CONSTRAINT #3 "reveal ledger is sole rewind authority"; first full-spread 42/42 pattern applied to a status-transition operation — `invalidatePrePlan` — not a sandbox-mutation operation as in WP-057). Copilot Check (01.7): initial HOLD 2026-04-20 with three FIXes (Date.now grep gate / ledger-sole restoration test / `requiresImmediateNotification` JSDoc + `// why:` upgrade) — all three FIXes applied in-place via amendments A-058-03/04/05 before session-prompt generation; expected CONFIRM on re-run. Three pre-session PS actions resolved by Commit A0 SPEC bundle: PS-1 (author EC-058) + PS-2 (`PREPLAN_EFFECT_TYPES` canonical array + drift test amendment) + PS-3 (consolidate all four public types in `disruption.types.ts` instead of split between `disruption.types.ts` + `disruptionPipeline.ts`). Commits use `EC-058:` prefix on code-changing commits (never `WP-058:` per P6-36 — commit-msg hook rejects); `SPEC:` on governance / pre-flight / governance-close commits. Three-commit topology: A0 `SPEC:` pre-flight bundle (EC-058 + WP-058 amendments A-058-01 through A-058-05 + pre-flight + copilot check + session-context + EC_INDEX row) → A `EC-058:` execution (8 implementation files + `index.ts` modify + 01.6 post-mortem + any D-5801 entries) → B `SPEC:` governance close (STATUS + WORK_INDEX + EC_INDEX status flip). Staging by exact filename only — never `git add .` / `git add -A` / `git add -u` (P6-27 / P6-44). Inherited dirty-tree items (untracked files + `.claude/worktrees/` + one modified `session-wp079-*.md`) untouched and never staged; quarantine stashes `stash@{0..2}` intact and never popped. See `EC-058-preplan-disruption-pipeline.checklist.md` + `docs/ai/invocations/preflight-wp058-preplan-disruption-pipeline.md` + `docs/ai/session-context/session-context-wp058.md`. Executed 2026-04-20 at commit `bae70e7`. | Done   |
| EC-066 | WP-066    | Client UI / Registry     | Registry viewer: image-to-data toggle, localStorage. Three new + two modified files under `apps/registry-viewer/src/` (`composables/useCardViewMode.ts` + `components/ViewModeToggle.vue` + `components/CardDataDisplay.vue`; `App.vue` + `components/CardDetail.vue`). `viewMode: 'image' \| 'data'` literal union persisted to `localStorage.cardViewMode` with REQ-1 discriminated narrowing (`stored === 'data' ? 'data' : 'image'`; self-heal write-back on mount) + REQ-2 setItem swallow (iOS Safari private mode / quota — `// why:` per 00.6 Rule 11). Public API is exactly `{ viewMode, toggleViewMode }` — `setViewMode` deliberately not exposed (no known caller; architecture.md §"Prohibited AI Failure Patterns"). 13 locked display labels rendered via AND-semantics (`v-if` guards on non-null/non-empty); three EC-listed fields absent on FlatCard today (`victoryPoints` / `recruiterText` / `attackerText`) documented as contract-aware placeholders in `CardDataDisplay.vue` JSDoc — naturally omitted until a future registry WP surfaces them. Empty-field designer choice: OMIT (not em-dash) for cleaner printable output. `@media print` styles on the data view for WCAG-friendly PDF export. Viewer has no automated test harness at baseline (REQ-3); verification is manual per EC §After Completing items 4-9 (DevTools → Application → Local Storage key check; toggle real-time update; state preservation across toggle; search/filter non-reset; print preview clean). Baseline gates `pnpm --filter registry-viewer {typecheck,lint,build}` all exit 0 errors (lint 174→184 warnings, 10 cosmetic in new files). Forbidden-imports grep gates (`boardgame.io`, `node:`, `pg`, `@legendary-arena/{game-engine,preplan,server}`, `Math.random`, `Date.now`) all zero for new/modified files. 01.5 NOT INVOKED — all four criteria absent (WP-030 precedent). 01.6 post-mortem OPTIONAL but authored — verdict WP COMPLETE with zero in-execution fixes; one non-blocking discovery documented (Section 10 Note 1: EC/FlatCard field-name gap resolved in-component via JSDoc mapping table, no EC/WP amendment, no scope expansion). REQ-4 pre-existing `02-CODE-CATEGORIES.md` governance gap (registry-viewer unclassified) flagged and deferred per precedent (WP-060/082/083/EC-103). Two-commit topology: A `8c5f28f` (EC-066 execution: 5 allowlist files) → B (this commit, SPEC governance close + post-mortem). See `EC-066-registry-viewer-data-toggle.checklist.md` + `docs/ai/invocations/session-wp066-registry-viewer-data-toggle.md` + `docs/ai/post-mortems/01.6-WP-066-registry-viewer-data-toggle.md`. Executed 2026-04-22 at commit `8c5f28f`. | Done   |
| EC-081 | WP-081    | Registry / Build Tooling | Registry build pipeline cleanup: delete 3 broken scripts (`normalize-cards.ts`, `build-dist.mjs`, `standardize-images.ts`) and their `package.json` / ci.yml / `docs/03-DATA-PIPELINE.md` / `README.md` references; trim `scripts.build` to `tsc` only. Subtractive only — zero new code, zero new tests, zero deps, zero `packages/registry/src/**` diff, zero `pnpm-lock.yaml` diff, zero `version` bump. Registers D-8101 (delete-not-rewrite; no monorepo consumer of the old `dist/*.json` artifacts — runtime path is `metadata/sets.json` + `metadata/{abbr}.json` from R2) + D-8102 (`registry:validate` is the single CI validation step; build and validate responsibilities remain separate). Test baseline UNCHANGED (engine `436 / 109 / 0 fail`; repo-wide `536 / 0 fail`). Three-commit topology: PS-2 `9fae043` (SPEC amendment adding README §F.6 anchor for "How to Standardize Images" section — closes negative-guarantee AC gap) → PS-3 `aab002f` (SPEC amendment adding §G anchor deleting `docs/03-DATA-PIPELINE.md` "Legacy Scripts" subsection — closes session-invocation Step 5 grep expectation gap + Step 6 OOS-match documentation) → A `ea5cfdd` (EC-081 execution: 3 deletions + 4 modifications + D-8101/D-8102 + DECISIONS_INDEX rows). Known OOS follow-ups (separate operator-tooling cleanup WP): `packages/registry/.env.example` lines 13-17 orphaned; `upload-r2.ts` docstring + closing `console.log` still reference deleted JSON artifacts. 01.5 NOT INVOKED; 01.6 post-mortem OPTIONAL (doc-only subtractive WP — WP-079 precedent). First green `pnpm --filter @legendary-arena/registry build` since WP-003 landed. See `EC-081-registry-build-pipeline-cleanup.checklist.md`. Executed 2026-04-20 at commit `ea5cfdd`. | Done   |

---

## Phase 7 — Beta, Launch & Live Ops

| EC     | Source WP | Layer                    | Execution Scope                                      | Status |
|--------|-----------|--------------------------|------------------------------------------------------|--------|
| EC-036 | WP-036    | Game Engine / Simulation | AI Playtesting & Balance Simulation Framework — four new files under `packages/game-engine/src/simulation/` (D-3601 engine code category, ninth precedent in the D-2706 / D-2801 / D-3001 / D-3101 / D-3201 / D-3301 / D-3401 / D-3501 chain): `ai.types.ts` (four pure type contracts — `AIPolicy`, `LegalMove`, `SimulationConfig`, `SimulationResult`; no runtime values), `ai.random.ts` (`createRandomPolicy(seed)` backed by file-local djb2 + mulberry32, neither exported; zero-legal-moves fallback returns endTurn intent per RS-6), `ai.legalMoves.ts` (`getLegalMoves(G, context): LegalMove[]` + 8-entry `SIMULATION_MOVE_NAMES` tuple + RS-13 enumeration order lock `playCard` → `recruitHero` → `fightVillain` → `fightMastermind` → `revealVillainCard` → `drawCards` → `advanceStage` → `endTurn`, stage-gated), `simulation.runner.ts` (`runSimulation(config, registry: CardRegistryReader)` + static 8-entry `MOVE_MAP` dispatch per D-2705 + local `SimulationMoveContext` structural interface per D-2801 + 200-turn safety cap per RS-7 + Fisher-Yates shuffle driven by run-level mulberry32 + post-endgame stats sourced from `UIState.progress.escapedVillains` + sum of `UIPlayerState.woundCount` per RS-12; degenerate inputs return zeroed `SimulationResult` without throwing). `simulation.test.ts` ships exactly 8 tests in one `describe('simulation framework (WP-036)')` block using `node:test` + `node:assert` only; test #7 uses canonical RS-14 assertion `assert.equal(player1.handCards, undefined, ...)`. Two modified files: `types.ts` (re-export four simulation types) + `index.ts` (public API for four types + `createRandomPolicy` + `getLegalMoves` + `runSimulation` + `SimulationLifecycleContext`). Engine baseline shift `436 / 109 / 0 → 444 / 110 / 0 fail` (+8 tests / +1 suite); repo-wide `588 / 0 → 596 / 0`. Layer-boundary integrity — 13 verification-step greps all zero-or-expected: no `boardgame.io` imports (escaped-dot grep); no runtime engine gameplay imports modified (targeted `git diff --name-only` against `moves/` / `rules/` / `setup/` / `turn/` / `ui/` / `scoring/` / `endgame/` / `villainDeck/` / `network/` / `replay/` / `game.ts`); no `@legendary-arena/registry` imports in simulation files; no `Math.random(` / `.reduce(` / `require(` in simulation code; `package.json` / `pnpm-lock.yaml` / `packages/game-engine/package.json` untouched (P6-44). DECISIONS.md adds four entries: D-3601 (Simulation Code Category; landed in A0 `4e340fd`), D-3602 (AI Uses the Same Pipeline as Humans; landed in A `04c53c0`), D-3603 (Random Policy Is the MVP Balance Baseline; landed in A `04c53c0`), D-3604 (Simulation Seed Reproducibility: Two Independent PRNG Domains; landed in A `04c53c0`). Amendments: A-036-01 landed in A0 (PS-2: `CardRegistry` → `CardRegistryReader` signature correction per WP-025/026 D-2504 precedent); A-036-02 lands in B (session-prompt pseudocode used flat `ClientTurnIntent` field names but the authoritative shape in `network/intent.types.ts:35` is nested — implementation follows the session prompt's binding "Copy WP-032's shape verbatim" instruction; scope-neutral reconciliation). 01.5 NOT INVOKED (four criteria all absent). 01.6 post-mortem MANDATORY (four triggers fire: new long-lived abstraction `AIPolicy` + new code-category directory D-3601 + new contract consumed by future Phase 7 WPs + D-2704 capability-gap pattern documented) — verdict WP COMPLETE with zero mid-execution fixes beyond the four reconciliation findings documented in post-mortem §5 (all session-prompt pseudocode vs. real-type divergences, all resolved in favor of the authoritative types per the session prompt's binding instructions). Commits use `EC-036:` prefix on code-changing commits (never `WP-036:` per P6-36 — commit-msg hook rejects); `SPEC:` on governance / pre-flight / governance-close commits. Three-commit topology: A0 `4e340fd` (SPEC pre-flight bundle: DECISIONS.md D-3601 + 02-CODE-CATEGORIES.md update + WP-036 §D signature + §Amendments A-036-01 + EC-036 amendment note + pre-flight file + session prompt + session-context bridge) → A `04c53c0` (EC-036 execution: 4 new simulation files + 1 test file + types.ts re-export + index.ts public API + DECISIONS.md D-3602/3/4) → B (this commit, SPEC governance close: STATUS + WORK_INDEX WP-036 `[ ]` → `[x]` + EC_INDEX EC-036 Draft → Done + WP-036 §Amendments A-036-02 + mandatory 01.6 post-mortem). Staging by exact filename only — never `git add .` / `git add -A` / `git add -u` (P6-27 / P6-44). Inherited dirty-tree items (untracked files + `.claude/worktrees/` + one modified `session-wp079-*.md`) untouched and never staged; quarantine stashes `stash@{0..2}` intact and never popped. See `EC-036-ai-playtesting.checklist.md` + `docs/ai/invocations/session-wp036-ai-playtesting-balance-simulation.md` + `docs/ai/invocations/preflight-wp036-ai-playtesting-balance-simulation.md` + `docs/ai/session-context/session-context-wp036.md` + `docs/ai/post-mortems/01.6-WP-036-ai-playtesting-balance-simulation.md`. Executed 2026-04-21 at commit `04c53c0`. | Done   |
| EC-037 | WP-037    | Release Strategy (Docs) + Game Engine (Contract-Only) | **WP-037 — Public Beta Strategy.** Contract-Only + Documentation bundle. Five Commit-A files: `packages/game-engine/src/beta/beta.types.ts` (new; D-3701 engine code category — 10th precedent in the D-2706 / D-2801 / D-3001 / D-3101 / D-3201 / D-3301 / D-3401 / D-3501 / D-3601 chain); `packages/game-engine/src/types.ts` + `index.ts` (additive re-export blocks only — no `LegendaryGameState` shape change, no pre-existing re-export modified); `docs/beta/BETA_STRATEGY.md` + `docs/beta/BETA_EXIT_CRITERIA.md` (new strategy-document pair). Three type contracts: `BetaFeedback` (6 required + 1 optional fields in EC-037 §Locked-Values order — `sessionId`, `buildVersion`, `category`, `description`, `severity`, `reproductionReplayId?`), `BetaCohort` (closed 3-member literal union `'expert-tabletop' \| 'general-strategy' \| 'passive-observer'`), `FeedbackCategory` (closed 5-member literal union `'rules' \| 'ui' \| 'balance' \| 'performance' \| 'confusion'`). `BetaFeedback.category` inline union is a deliberate verbatim duplicate of `FeedbackCategory` — not a DRY refactor opportunity; lock-step review discipline per EC-037. Required `// why:` module-header comment (EC-037 line 65) verbatim: *"feedback tied to build version for traceability; replay reference enables reproduction."* `BETA_STRATEGY.md` has 8 sections (Objectives 4+4 non-goals, Feature Scope included/excluded, User Cohorts with signal targets, Access Control invitation-only + hard cap + unique build ID + opt-in diagnostics, Feedback Collection Model referencing `BetaFeedback`, Timeline closed-alpha → invite-beta → open-beta, Exit Criteria Summary, Related Documents) plus three DoD rationale paragraphs (invitation-only signal quality; three-cohort signal targets; same release gates as production). `BETA_EXIT_CRITERIA.md` is a binary pass/fail gate across 4 categories (Rules correctness / UX clarity / Balance perception / Stability) with 12 criteria total, every one citing a specific source signal (`BetaFeedback` records, `OpsCounters` deltas, `verifyDeterminism` output, `runSimulation` output, deployment logs); includes the three Vision §4 multiplayer criteria (reconnection round-trips, late-joining semantics, no-desync in final 2 weeks). Category 3 anchored to D-0702 (Balance Changes Require Simulation); Category 4 anchored to D-0902 (Rollback Is Always Possible) + Vision §4. Test baseline UNCHANGED at engine `444 / 110 / 0 fail` (RS-2 zero-new-tests lock honored); repo-wide `596 / 0` (unchanged). Layer-boundary integrity — 13 verification-step greps all zero-or-expected: no `boardgame.io` imports (P6-22 escaped-dot pattern); no `@legendary-arena/registry` / `apps/server/` / `apps/` imports in `src/beta/`; no `Math.random` / `Date.now` / `performance.now` / `new Date()` in `src/beta/`; no cross-subdirectory engine imports (`from '(\.\./)+(versioning\|scoring\|…\|simulation)/'`); no I/O (`node:fs` / `node:net` / `node:http` / `process.env`); no `.reduce()`; no `require(` (RS-7 `-SimpleMatch`); `git diff --name-only` against all 24 engine subdirectories + `matchSetup.*` + `game.ts` returns empty output; `BetaFeedback` never a field of `LegendaryGameState` (types.ts line 269/273/277 re-export block only; `LegendaryGameState` begins at 361); no `.test.ts` leaked into `src/beta/`; `pnpm-lock.yaml` absent from diff (P6-44). `DECISIONS.md` adds four entries: D-3701 (Beta Types Code Category; landed in A0 `a4f5574`) + D-3702 / D-3703 / D-3704 (three DoD decisions: invitation-only signal quality / three-cohort signal targets / same release gates as production; landed in B). Reality reconciliation: `beta.types.ts` module-header JSDoc softened pre-commit to cite D-3701 for the forbidden-token list rather than restate tokens inline (the original prose tripped Verification Steps 5b and 6 as false positives against narrative text; zero structural risk; zero governance content lost — D-3701 enumerates the forbidden tokens exhaustively). Required EC-037 line-65 `// why:` comment preserved verbatim; recommended per-type commentary preserved verbatim. 01.5 NOT INVOKED (four criteria all absent — no `LegendaryGameState` widening, no setup-orchestrator signature change, no new move, no new phase hook). 01.7 Copilot Check verdict CONFIRM (30 / 30 PASS) honored literally. 01.6 post-mortem MANDATORY (P6-35; two triggers fire: new long-lived abstraction `BetaFeedback` + `BETA_STRATEGY.md`/`BETA_EXIT_CRITERIA.md` template pair + new code-category directory D-3701) — verdict WP COMPLETE with one pre-commit fix (reality reconciliation above) documented in post-mortem §8 and §10; all 18 verification steps pass. Commits use `EC-037:` prefix on code-changing commits (never `WP-037:` per P6-36 — commit-msg hook rejects); `SPEC:` on governance / pre-flight / governance-close commits. Three-commit topology: A0 `a4f5574` (SPEC pre-flight bundle: DECISIONS.md D-3701 + 02-CODE-CATEGORIES.md §engine subdirectory row) → A `160d9b9` (EC-037 execution: 3 new files + 2 additive re-exports + 01.6 post-mortem) → B (this commit, SPEC governance close: STATUS + WORK_INDEX WP-037 `[ ]` → `[x]` + EC_INDEX EC-037 Draft → Done + DECISIONS.md D-3702 / D-3703 / D-3704). Staging by exact filename only — never `git add .` / `git add -A` / `git add -u` (P6-27 / P6-44). Inherited dirty-tree items resolved by prior SPEC commits before session open; `.claude/worktrees/` + `docs/audits/` untracked directories untouched; quarantine stashes `stash@{0..3}` intact and never popped. Commit-body Vision trailer: `Vision: §3, §4, §5, §13, §14, §18, §22, §24, NG-1, NG-3`. See `EC-037-beta-strategy.checklist.md` + `docs/ai/invocations/session-wp037-public-beta-strategy.md` + `docs/ai/post-mortems/01.6-WP-037-public-beta-strategy.md`. Executed 2026-04-22 at commit `160d9b9`. | Done   |
| EC-038 | WP-038    | Operations / Launch      | 4 readiness gates, launch day procedure              | Done   |
| EC-039 | WP-039    | Operations / Observability | **WP-039 — Post-Launch Metrics & Live Ops.** Documentation-only strategy document (`docs/ops/LIVE_OPS_FRAMEWORK.md`). Path A reuses landed `IncidentSeverity` and `OpsCounters` from `ops.types.ts` (D-3501) rather than defining parallel types — resolves all three v1 pre-flight blockers by construction (duplicate severity type, severity-semantic contradiction with `INCIDENT_RESPONSE.md:33`, parallel counter container). Framework doc has 11 top-level sections (§1 Purpose — §11 Summary) with 8 foundational constraints, 6 data collection rules, 6 success criteria, 9 non-goals. §3 Severity Taxonomy cross-links to `INCIDENT_RESPONSE.md` (does not restate table; Verification Step 9 grep for "Immediate rollback" returned 0); §4 Observability Surface cross-links to `ops.types.ts` `OpsCounters` (does not redefine four fields). Replay desync classified P1 per `INCIDENT_RESPONSE.md:33`. §5 metric labels (System Health / Gameplay Stability / Balance Signals / UX Friction) are organizational prose only — not a typed union. Live-ops cadence locked: daily / weekly / monthly with out-of-cadence review permitted only for P0/P1. 01.5 NOT INVOKED (all four trigger criteria absent). 01.6 post-mortem MANDATORY (one new long-lived abstraction document) — delivered at `docs/ai/post-mortems/01.6-WP-039-post-launch-metrics-live-ops.md`. Verification: all 15 steps pass; zero TS files modified; zero parallel `MetricPriority` / `MetricSeverity` / `MetricCategory` / `MetricEntry` anywhere (one pre-Commit-A reality reconciliation: `MetricCategory` meta-prose paraphrased to "code-level union" so Step 5 returns empty); `INCIDENT_RESPONSE.md` and `ops.types.ts` untouched; framework doc cross-links all three authority identifiers (47 matches); replay desync + P1 paired explicitly; zero subjective-language tokens; zero forbidden-token enumeration (cites D-0901 per §18). Test baseline UNCHANGED at engine 444/110/0 + repo-wide 596/0 (zero new tests). Three-commit topology: A0 SPEC pre-flight bundle (`9e7d9bd`) → A `EC-039:` content + 01.6 post-mortem (`4b1cf5c`) → B SPEC governance close (this commit: STATUS + WORK_INDEX WP-039 `[ ]` → `[x]` + EC_INDEX EC-039 Draft → Done + one new DECISIONS.md entry documenting the Path A reuse decision). Staging by exact filename only — never `git add .` / `git add -A` / `git add -u` (P6-27 / P6-44). Inherited `.claude/worktrees/` untouched and never staged. Commits use `EC-039:` prefix on content commit; `SPEC:` on pre-flight bundle and governance close (never `WP-039:` per P6-36 — commit-msg hook rejects). Commit-body Vision trailer: `Vision: §3, §5, §13, §14, §22, §24`. See `EC-039-live-ops.checklist.md` + `docs/ai/invocations/session-wp039-post-launch-metrics-live-ops.md` + `docs/ai/invocations/preflight-wp039-post-launch-metrics-live-ops-v2.md` + `docs/ai/invocations/copilot-wp039-post-launch-metrics-live-ops.md` + post-mortem. Executed 2026-04-23 at commit `4b1cf5c`. | Done   |
| EC-040 | WP-040    | Product Governance       | **WP-040 — Growth Governance & Change Budget.** Contract-Only + Documentation bundle: `docs/governance/CHANGE_GOVERNANCE.md` (new — reader-facing prose with seven top-level sections including the category-to-layer mapping table per Copilot Issue 26 FIX, the `versionImpact` → version-axis mapping table per Copilot Issue 4 FIX, and the `exactOptionalPropertyTypes` omit-don't-undefined authoring pattern per Copilot Issue 5 FIX); `packages/game-engine/src/governance/governance.types.ts` (new under **D-4001** — ninth engine subdirectory classification after D-2706 / D-2801 / D-3001 / D-3101 / D-3201 / D-3301 / D-3401 / D-3501) exporting `ChangeCategory` closed union of five literals (`ENGINE` / `RULES` / `CONTENT` / `UI` / `OPS`), `ChangeBudget` six-field budget struct, `ChangeClassification` classification metadata with optional `immutableSurface` literal union; all fields `readonly` per D-2802 / D-3501 aliasing-prevention discipline. Path A reuses landed version axes (`EngineVersion` / `DataVersion` / `ContentVersion` per D-0801) and landed ops surfaces (`IncidentSeverity` / `OpsCounters` per D-3501) via cross-link rather than parallel types — four Path A findings (four-metric-categories paraphrase, D-3901 inheritance, eight AUTHORITATIVE surfaces, post-mortem gap) and three pre-session actions (D-4001 code-category classification, P6-51 form (2) DECISIONS.md placement, P6-52 paraphrase discipline) all resolved at pre-flight level before Commit A opened. Additive re-exports in `types.ts` + `index.ts` only — no `LegendaryGameState` field added (verified: all five `ChangeCategory|ChangeBudget|ChangeClassification` matches in `types.ts` are on re-export lines, not field declarations). Governance types are out-of-band metadata only — engine never constructs or mutates instances, never stored in `G`, never persisted, never transmitted in replay logs, never used to branch runtime gameplay. Required `// why: change budgets prevent entropy during growth (D-1001)` comment sits immediately above `ChangeBudget`; D-3901 reuse-verification header `// why:` comment records the 4/4 PASS genuinely-novel result from pre-flight v2. 01.5 NOT INVOKED (all four trigger criteria absent: no `LegendaryGameState` field, no `buildInitialGameState` shape change, no new `LegendaryGame.moves` entry, no new phase hook). 01.7 Copilot Check 27/30 PASS with 3 scope-neutral RISK FIXes (Issues 4 / 5 / 26) folded into session-prompt §Locked Values and applied verbatim during authoring. 01.6 post-mortem mandatory triggers fired (new long-lived abstraction document + new code-category directory + new type contracts) — delivered at `docs/ai/post-mortems/01.6-WP-040-growth-governance-change-budget.md`. All 17 Verification Steps pass on first run — zero mid-execution fixes, zero grep-guard trips, zero forbidden-token enumeration in `CHANGE_GOVERNANCE.md` (Step 10 CLEAN), zero subjective-language hits (Step 9 CLEAN), zero `require(` in `packages/game-engine/src/governance/` (Step 5 CLEAN), zero engine gameplay files modified (Step 6 CLEAN). Test baseline UNCHANGED at engine 444/110/0 + repo-wide 596/0 (zero new tests; RS-2 lock honored). Engine build exits 0. Four-commit topology: **A0a** SPEC precedent-land (`a6be850` — P6-52 + P6-53 in 01.4 + back-sync of 00-INDEX / 05-ROADMAP / 05-ROADMAP-MINDMAP); **A0** SPEC pre-flight bundle + D-4001 (`5e1a0fa` — v1 + v2 preflights, copilot check, session prompt, Path A rewrites of WP-040 + EC-040, D-4001 additions to DECISIONS.md + DECISIONS_INDEX.md + 02-CODE-CATEGORIES.md); **A** `EC-040:` content + 01.6 post-mortem (`6faaf3b` — 5 files: `CHANGE_GOVERNANCE.md` + `governance.types.ts` + `types.ts` + `index.ts` + post-mortem); **B** SPEC governance close (this commit: STATUS.md + WORK_INDEX.md WP-040 `[ ]` → `[x]` + EC_INDEX.md EC-040 Draft → Done + three back-pointer DECISIONS.md entries **D-4002** (Change Classification back-pointer) / **D-4003** (Growth Vectors back-pointer) / **D-4004** (Immutable Surfaces back-pointer) per P6-51 form (2) + three matching DECISIONS_INDEX rows). Staging by exact filename only — never `git add .` / `git add -A` / `git add -u` (P6-27 / P6-44). Inherited `.claude/worktrees/` untouched and never staged. Commits use `EC-040:` prefix on content commit; `SPEC:` on precedent-land, pre-flight bundle, and governance close (never `WP-040:` per P6-36 — commit-msg hook rejects). Commit-body Vision trailer on both Commit A and Commit B: `Vision: §5, §13, §14, §22, §24`. See `EC-040-growth-governance.checklist.md` + `docs/ai/invocations/session-wp040-growth-governance-change-budget.md` + `docs/ai/invocations/preflight-wp040-growth-governance-change-budget-v2.md` + `docs/ai/invocations/copilot-wp040-growth-governance-change-budget.md` + post-mortem. Executed 2026-04-23 at commit `6faaf3b`. | Done   |
| EC-041 | WP-041    | Core Architecture (Docs) | **WP-041 — System Architecture Definition & Authority Model.** Pure documentation certification pass over `docs/ai/ARCHITECTURE.md`. Three Commit-A surfaces: ARCHITECTURE.md (4 edits — version stamp at top, Document override hierarchy block updated from stale 4-entry chain to 7-entry authoritative chain, single clarifying sentence inserted above Field Classification Reference table body disambiguating Class column semantics, stale `*Last updated: WP-014 review*` footer refreshed to reference WP-041) + DECISIONS.md (two new entries — D-4101 Resolved Transcription Inconsistency for footer refresh; D-4102 Rules-Architecture Drift Log consolidating three drift items between `.claude/rules/architecture.md` and ARCHITECTURE.md) + DECISIONS_INDEX.md (new "Architecture Certification & Audit (WP-041)" section with two index rows) + 01.6 post-mortem at `docs/ai/post-mortems/01.6-WP-041-architecture-audit.md`. Field Classification Reference table verified complete: all 20 G-class Runtime fields established by WP-005B through WP-026 present (`selection` / `playerZones` / `piles` / `villainDeck` / `villainDeckCardTypes` / `hookRegistry` / `currentStage` / `lobby` / `messages` / `counters` / `city` / `hq` / `ko` / `attachedBystanders` / `turnEconomy` / `cardStats` / `mastermind` / `heroAbilityHooks` / `cardKeywords` / `schemeSetupInstructions`); all names match `LegendaryGameState` in `packages/game-engine/src/types.ts:375` verbatim; `matchConfiguration` (Class 2) and `activeScoringConfig` (WP-067) intentionally excluded per WP-041 §Out of Scope. Authority chain locks `docs/01-VISION.md` between `docs/ai/ARCHITECTURE.md` and `.claude/rules/*.md` (per WP-041 §B). Architecture and engine versions intentionally synchronized at 1.0.0 — version stamp value matches `CURRENT_ENGINE_VERSION_VALUE` at `packages/game-engine/src/versioning/versioning.check.ts:29`. Pre-flight bundle resolved two governance findings before execution: PS-1 (BLOCKING — EC-041 locked field count corrected from 19 to 20, `selection` added at position #1 per introduction-order canonical lock per PS-4); PS-2 (NON-BLOCKING — WP-041 Assumes range refreshed from D-0001..D-1102 to D-0001..D-4004 to match current DECISIONS.md tail). Three session-prompt guardrails encoded as PS-3a/b/c: §B is an UPDATE not an ADD; clarifying sentence is single-sentence not column restructure; `activeScoringConfig` (WP-067) is out of scope. 01.5 NOT INVOKED (all four trigger criteria absent — no `LegendaryGameState` field added, no `buildInitialGameState` shape change, no new move, no new phase hook). 01.7 Copilot Check verdict CONFIRM after PS-1/PS-2/PS-4 — 30/30 categorically PASS (28 PASS + 2 RISK both fixed by guardrails before execution + 0 BLOCK). 01.6 post-mortem (recommended-but-optional for documentation packets) authored anyway per WP-040 / WP-042 / WP-066 / WP-081 Phase 7 precedent — verdict WP COMPLETE; captures session-context lineage gap (no `session-context-wp041.md` ever generated; lineage jumped WP-040 → WP-042) and the precedent observation that pre-flight + copilot check caught the EC-041 "Exactly 19" miscount before execution. Test baseline UNCHANGED at engine 444/110/0 + repo-wide 596/0 (zero new tests; documentation only). Layer-boundary integrity — `git diff --name-only packages/ apps/` returns empty across all three commits (no engine, registry, server, or app file touched); `git diff --name-only .claude/rules/` returns empty across all three commits (no rules-file modification — drift logged in D-4102 is for separate correction pass). Three-commit topology: A0 SPEC pre-flight bundle (`6cc2541` — preflight + copilot check + session prompt + PS-1/PS-2 corrections to WP-041 + EC-041) → A `EC-041:` content + 01.6 post-mortem (`0e8e8b1` — ARCHITECTURE.md + DECISIONS.md + DECISIONS_INDEX.md + post-mortem) → B SPEC governance close (this commit: STATUS.md + WORK_INDEX.md WP-041 `[ ]` → `[x]` + EC_INDEX.md EC-041 Draft → Done). Staging by exact filename only — never `git add .` / `git add -A` / `git add -u` (P6-27 / P6-44). Inherited `.claude/worktrees/` untouched and never staged. Commits use `EC-041:` prefix on content commit; `SPEC:` on pre-flight bundle and governance close (never `WP-041:` per P6-36 — commit-msg hook rejects). Commit-body Vision trailer on Commit A: `Vision: §7, §8, §13, §14, §15`. See `EC-041-architecture-audit.checklist.md` + `docs/ai/invocations/preflight-wp041-system-architecture-audit.md` + `docs/ai/invocations/copilot-wp041-system-architecture-audit.md` + `docs/ai/invocations/session-wp041-architecture-audit.md` + `docs/ai/post-mortems/01.6-WP-041-architecture-audit.md`. Executed 2026-04-23 at commit `0e8e8b1`. | Done   |
| EC-049 | WP-049    | Tooling / Simulation     | T2 CompetentHeuristic, PAR aggregation, policy tiers. Three-file Commit-A code bundle (`simulation/ai.tiers.ts` + `simulation/ai.competent.ts` + `simulation/par.aggregator.ts`) plus two test files (`simulation/ai.competent.test.ts` with 10 tests, `simulation/par.aggregator.test.ts` with 17 tests) plus three modifications (`types.ts` re-exports, `index.ts` public API exports, `DECISIONS.md` entries D-4901..D-4911 covering T2 sole PAR authority, 55th-percentile nearest-rank, neutral hero pool, N >= 500 validation-only, five T2 heuristics, first-class loss treatment, server-layer gate, seed-set canonicalization, immutable Raw Score surface, module-level needsMoreSamples thresholds, severity-tagged ParValidationResult). Engine baseline: 444/110/0 → 471/112/0 (+27 tests / +2 suites); repo-wide 596/123/0 → 623/125/0. `generateScenarioPar` replicates per-game loop from WP-036 simulation.runner.ts (RS-10) using engine primitives only; WP-036 + WP-048 + WP-020 contract files byte-identical pre vs post. `registry: CardRegistryReader` signature (PS-2). Typed `ParAggregationError` with `'EMPTY_DISTRIBUTION' \| 'PERCENTILE_OUT_OF_RANGE'` discriminated union; tests assert `instanceof` + `.code`. Test #15 asserts both byte-identity AND JSON-roundtrip via `deepStrictEqual(JSON.parse(JSON.stringify(r)), ...)`. Every reproducibility test injects `config.generatedAtOverride` (RS-11). Cluster-based multimodality smell test (replaced initial peak-distance version). Added `MAX_MOVES_PER_GAME = 2000` move-level stall cap for deterministic-policy trap cases (strict in-allowlist refinement per WP-031 precedent; no 01.5 invocation). 01.6 post-mortem delivered at `docs/ai/post-mortems/01.6-WP-049-par-simulation-engine.md` covering aliasing, JSON-roundtrip, `// why:` completeness, reproducibility protocol, per-game loop replication audit, layer-boundary audit, contract-surface verification. Three-commit topology: A0 SPEC pre-flight (`67927f1`) → A `EC-049:` execution (`021555e`, 8 files) → B SPEC governance close (this commit: STATUS.md + WORK_INDEX.md WP-049 `[ ]` → `[x]` + EC_INDEX.md EC-049 Draft → Done + post-mortem). Commits use `EC-049:` on code; `SPEC:` on governance (never `WP-049:` per P6-36). Executed 2026-04-23 at commit `021555e`. | Done 2026-04-23 |
| EC-050 | WP-050    | Tooling / Data           | **WP-050 — PAR Artifact Storage & Indexing.** Dual source-class storage (`data/par/seed/{v}/` + `data/par/sim/{v}/` versioned independently) with `sourceClassRoot` as single choke-point for `seed/` vs `sim/` directory names. Two-file Commit-A code bundle: `simulation/par.storage.ts` (13 exported functions — `scenarioKeyToFilename`, `scenarioKeyToShard`, `sourceClassRoot`, `computeArtifactHash`, `writeSimulationParArtifact`, `readSimulationParArtifact`, `writeSeedParArtifact`, `readSeedParArtifact`, `buildParIndex`, `lookupParFromIndex`, `resolveParForScenario`, `validateParStore`, `validateParStoreCoverage` — plus `ParStoreReadError` class and `PAR_ARTIFACT_SOURCES` canonical readonly array drift-pinned against `ParArtifactSource` union) + `simulation/par.storage.test.ts` (exactly 34 tests in one describe: 6 path + 7 sim I/O + 7 seed I/O + 4 index + 5 resolver + 4 validation + 1 hashing). Three modifications: `types.ts` re-exports 10 storage types, `index.ts` public API adds 13 functions + class + constant + typed re-exports, `DECISIONS.md` entries D-5002..D-5010 (dual source classes, simulation-over-seed precedence single-resolver lock, file-based not database, scheme-slug 2-char sharding, canonical sorted-key JSON for determinism, atomic index temp+rename, overwrite refusal as immutability enforcement, SHA-256 `artifactHash` via `node:crypto` with self-hash exclusion, non-T2 policy tier guard). Engine baseline: 471/112/0 → 505/113/0 (+34 tests / +1 suite); repo-wide 623/125/0 → 657/126/0. `writeSeedParArtifact` four-parameter signature `(artifact, scoringConfig, basePath, parVersion)` per PS-5 — writer runs `parValue === computeParScore(scoringConfig with parBaseline)` consistency guard at write time. Both writers refuse overwrite via `fs.access` pre-check + full-sentence `Error` (never `ParStoreReadError` — that's read-only). Both writers always compute `artifactHash` themselves — caller-supplied hashes ignored. `resolveParForScenario` is the ONLY sanctioned cross-class reader: sim first, seed second, `null` otherwise; no optional `preferSource` override; throws `ParStoreReadError` on malformed indices (no silent fall-through). `validateParStore` is read-only audit (no silent repair); flags non-T2 simulation artifacts + cross-class `source` drift + hash mismatch + completeness / exclusivity. D-5001 IO carve-out boundary grep-enforced: only `par.storage.ts` and `par.storage.test.ts` import `node:fs` / `node:fs/promises` in `src/simulation/`. WP-036 / WP-048 / WP-049 contract files byte-identical pre vs post (`git diff main -- ` 15 files returns zero). 01.5 NOT INVOKED (zero `LegendaryGameState` field, zero `buildInitialGameState` shape change, zero moves, zero phase hooks). 01.6 post-mortem MANDATORY per four triggers (new long-lived abstraction, new contract consumed by future WPs, new canonical readonly array, first filesystem carve-out) — delivered at `docs/ai/post-mortems/01.6-WP-050-par-artifact-storage.md` covering aliasing, JSON-roundtrip, `// why:` completeness (19 comments vs 10 required), determinism, per-source-class isolation, layer-boundary + D-5001 carve-out audit, hash integrity. `// why: ` self-hash exclusion cited above `computeArtifactHash`; `// why: ` distinction between seed-set djb2 (WP-049) and tamper-detection SHA-256 (WP-050) cited per PS-4. Four-commit topology: A0a SPEC pre-flight bundle (`3552fc2` — session context, pre-flight report, session prompt) → A0b SPEC PS-1..PS-6 resolution (`cd7965a` — EC drift fix to 34-test count + dual source classes, D-5001 IO carve-out landed, `ScenarioKey` wording, `node:crypto` citation, four-parameter seed writer, `node:fs/promises` production lock) → A `EC-050:` execution (`ccdf44e` — 5 files, 2284 insertions) → B SPEC governance close (this commit: STATUS.md + WORK_INDEX.md WP-050 `[ ]` → `[x]` + EC_INDEX.md EC-050 Draft → Done + post-mortem). Staging by exact filename only — never `git add .` / `git add -A` / `git add -u` (P6-27). Inherited `.claude/worktrees/` and unrelated WP-087 draft files untouched and never staged. Commits use `EC-050:` prefix on code; `SPEC:` on pre-flight and governance close (never `WP-050:` per P6-36 — commit-msg hook rejects). See `EC-050-par-artifact-storage.checklist.md` + `docs/ai/invocations/session-wp050-par-artifact-storage.md` + `docs/ai/invocations/preflight-wp050-par-artifact-storage.md` + `docs/ai/session-context/session-context-wp050.md` + post-mortem + D-5001..D-5010. Executed 2026-04-23 at commit `ccdf44e`. | Done 2026-04-23 |
| EC-051 | WP-051    | Server / Enforcement     | **WP-051 — PAR Publication & Server Gate Contract.** Server-layer enforcement of the pre-release PAR gate. Two new files under `apps/server/src/par/`: `parGate.mjs` (exports `checkParPublished(simulationIndex, seedIndex, scenarioKey)` — pure synchronous lookup with D-5003 / D-5101 sim-over-seed precedence, returns fresh object literal `{parValue, parVersion, source}` per copilot #17 aliasing guard, returns `null` on absent-from-both or dual-null fail-closed; `createParGate(basePath, parVersion)` — async factory loading both source-class indices once at startup via `Promise.all` on two `loadParIndex` calls, catches `ParStoreReadError` per class via `handleParLoadError` helper with full-sentence warn-log + null-for-that-class D-5101 graceful degradation; non-`ParStoreReadError` re-throws so infrastructure failures surface loudly) + `parGate.test.ts` (exactly 13 tests in one describe block: 3 `loadParIndex` smoke + 3 `checkParPublished` base + 3 `createParGate` integration including zero-fs-at-request-time invariant via post-construction `rm -rf` + 3 dual-class precedence including both-present-sim-wins + 1 aliasing guard). Two modifications: `apps/server/src/server.mjs` (surgical import + Promise.all extension adding `createParGate('data/par', process.env.PAR_VERSION ?? 'v1')` as third independent startup task; returned gate captured via destructuring with explicit `void parGate;` marker documenting held-for-WP-053/WP-054 binding; no other lines changed) + `apps/server/package.json` (test glob expanded from `scripts/**/*.test.ts` to `scripts/**/*.test.ts src/**/*.test.ts` — unquoted form matches proven pre-WP-051 precedent; quoted form from Locked Values was refined mid-execution because pnpm-on-Windows routes test scripts through cmd.exe which does not honor single quotes and silently matched zero files, verified root cause in post-mortem §3.8). Test baseline: apps/server `6/2/0` → `19/3/0` (+13 tests, +1 suite); repo-wide `658/126/0` → `671/127/0`; engine `506/113/0` unchanged (A1 amendment already in A0-engine commit). Zero `node:fs` imports in `parGate.mjs` (grep-verified; PAR file IO delegated entirely to engine's `loadParIndex` per D-5001 line 8937). Zero `boardgame.io` / `LegendaryGame` / `ctx.` / `.reduce(` with branching / `Math.random` / `Date.now` / `require(` / `writeFile`/`mkdir`/`unlink`/`rename`/`truncate` references in `parGate.mjs` (grep-verified). Tests may import `node:fs/promises` for fixture setup — fs-free boundary applies to production file only. Active PAR version from `PAR_VERSION` env var with `?? 'v1'` fallback per D-5102 — read-once at startup; stable for process lifetime; no runtime reload; no SIGHUP; no format validation (fail-soft via missing-index warnings). Existence-based trust per D-5103 — server does not reimplement `validateParStore` or recompute `artifactHash`; hash / coverage validation is CI-time only. Dual-class precedence D-5101 — sim queried first; hit returns `source: 'simulation'`; else seed hit returns `source: 'seed'`; else `null`. Fresh-object-literal return load-bearing for WP-053 / WP-054 leaderboard records. 01.5 NOT INVOKED (zero `LegendaryGameState` field, zero `buildInitialGameState` shape change, zero new moves, zero new phase hooks — the only existing files modified are in the EC-051 explicit allowlist). 01.6 post-mortem MANDATORY per three triggers (new long-lived abstraction `createParGate` factory, new contract consumed by future WPs for WP-053 / WP-054 `{parValue, parVersion, source}` return shape, first server-layer consumer of `data/par/` tree with partial filesystem surface delegated entirely to engine) — delivered at `docs/ai/post-mortems/01.6-WP-051-par-publication-server-gate.md` covering all 8 mandatory checks (layer-boundary, aliasing, fail-closed, no-fs-at-request-time, precedence, config, `// why:` completeness, test-glob including cmd.exe + pnpm root-cause analysis). Four-commit topology: **A0-engine** `5e468a7` (`EC-050:` — A1 amendment landing `loadParIndex` + drift test; split from governance because the commit-msg hook blocks `SPEC:` when `packages/` or `apps/` code is staged); **A0-governance** `db83d9a` (`SPEC:` — D-5101/5102/5103, WP-050/EC-050 amend notes, WP-051/EC-051 amend, session-context-wp051, preflight, session prompt); **A** `EC-051:` content + 01.6 post-mortem (`ce3bffb` — 4 files: `parGate.mjs`, `parGate.test.ts`, `server.mjs`, `package.json`); **B** SPEC governance close (this commit: STATUS.md + WORK_INDEX.md WP-051 `[ ]` → `[x]` + EC_INDEX.md EC-051 Draft → Done + post-mortem). Staging by exact filename only — never `git add .` / `git add -A` / `git add -u` (P6-27). Inherited `.claude/worktrees/` and unrelated WP-087 / WP-088 / WP-089 / WP-090 / session-context-wp087 / preflight-wp051-input draft files untouched and never staged during any WP-051 commit. Commits use `EC-051:` prefix on code commit; `SPEC:` on governance pre-flight and close (never `WP-051:` per P6-36 — commit-msg hook rejects). See `EC-051-par-publication-server-gate.checklist.md` + `docs/ai/invocations/session-wp051-par-publication-server-gate.md` + `docs/ai/invocations/preflight-wp051-par-publication-server-gate.md` + `docs/ai/session-context/session-context-wp051.md` + post-mortem + D-5101 / D-5102 / D-5103. Executed 2026-04-23 at commit `ce3bffb`. | Done 2026-04-23 |
| EC-087 | WP-087    | Game Engine / Types      | **WP-087 — Engine Type Hardening (scope-narrowed execution).** `PlayerId` string alias added to `packages/game-engine/src/types.ts` with required `// why:` comment citing the boardgame.io 0.50.x player-index convention (`"0" \| "1" \| …`) and the non-branded rationale (D-8701). Three `Record<string, …>` → `Record<PlayerId, …>` swaps landed at the three canonical sites: `LegendaryGameState.playerZones` (`types.ts`), `GameStateShape.playerZones` (`state/zones.types.ts`), and `PersistableMatchConfig.playerNames` (`persistence/persistence.types.ts` — WP/EC mislabeled as `MatchSnapshot.playerNames`; the file has exactly one `playerNames` field at the verified line 58, so the operational target was unambiguous). `ruleRuntime.ordering.test.ts` factory-refactored: `hookRegistry: createHookRegistry(hooks)` now constructed via object spread in the factory's returned literal, eliminating the sole non-setup `hookRegistry` assignment (grep invariant's only pre-change hit). **Scope deviation from WP-087 §Locked Values:** the three `readonly` modifiers on `LegendaryGameState.{hookRegistry, schemeSetupInstructions, heroAbilityHooks}` were **reverted mid-session** after applying them produced seven TS errors in four production-code files outside the 4-file allowlist (`game.ts:237,252`, `hero/heroConditions.evaluate.ts:71`, `hero/heroEffects.execute.ts:146`, `villainDeck/villainDeck.reveal.ts:203,218,233`). Per session prompt §AI Agent Warning #1 and generic-ripple Hard Stop, the `readonly` tightening belongs in a follow-up WP that first widens the consumer signatures (`HookRegistry` type alias + two hero-hook param types) before tightening the state-type. See D-8702 for the full deferral rationale and the follow-up WP scope. Test baseline `671 / 127 / 0` unchanged. `pnpm -r build`, `pnpm -r test`, `pnpm --filter @legendary-arena/game-engine exec tsc --noEmit` all exit 0. Narrowed production grep `(G\|gameState)\.(hookRegistry\|schemeSetupInstructions\|heroAbilityHooks)\s*(=\|\.push\|\.pop\|\.splice\|\.shift\|\.unshift)` over `packages/game-engine/src/**/*.ts` excluding `*.test.ts`: zero matches (per PS-1 resolution in pre-flight). Test-file grep of same field-level pattern: zero matches (factory refactor eliminated the lone pre-change hit at `ordering.test.ts:56`). `export type PlayerId`: 1 match; `import type { PlayerId }`: 1 match per zones + persistence; `Record<PlayerId`: 3 matches (one per locked site). 01.5 NOT INVOKED (zero `LegendaryGameState` field added, zero `buildInitialGameState` shape change, zero moves, zero phase hooks). 01.6 OPTIONAL — skipped (no new long-lived abstraction, no new contract consumed by future WPs, no new canonical readonly array, no new IO surface; session drift captured in D-8702 instead of a dedicated post-mortem). Two-commit topology: **A** `EC-087:` code-only (`73aeada` — 4 files: `types.ts`, `state/zones.types.ts`, `persistence/persistence.types.ts`, `rules/ruleRuntime.ordering.test.ts`); **B** SPEC governance close (this commit: `DECISIONS.md` adds D-8701..D-8704 — non-branded alias rationale, `readonly` deferral, `MatchSetupConfig` non-applicability, `heroDeckIds` communal-pool semantic; `STATUS.md` adds WP-087 current-state block with the scope-deviation note; `WORK_INDEX.md` flips WP-087 `[ ]` → `[x]` with date + `73aeada`; `EC_INDEX.md` flips EC-087 Draft → Done). Staging by exact filename only — never `git add .` / `git add -A` / `git add -u` (P6-27). In-flight WP-089/WP-090 `WORK_INDEX.md` additions + EC-090 `EC_INDEX.md` addition from a separate workstream stashed before Commit A; restored via `git stash pop` after Commit B. Commits use `EC-087:` prefix on code commit; `SPEC:` on governance close (never `WP-087:` per P6-36 — commit-msg hook rejects). See `EC-087-engine-type-hardening.checklist.md` + `docs/ai/invocations/session-wp087-engine-type-hardening.md` + `docs/ai/invocations/preflight-wp087-engine-type-hardening.md` + `docs/ai/session-context/session-context-wp087.md` + D-8701..D-8704. Executed 2026-04-23 at commit `73aeada`. | Done 2026-04-23 |
| EC-088 | WP-088    | Game Engine / Setup      | **WP-088 — Setup Module Hardening: `buildCardKeywords` Runtime Guards, Villain Pre-Index, Output Ordering.** Single-file internal-hardening pass on `packages/game-engine/src/setup/buildCardKeywords.ts` — seven numbered in-file edits, zero caller touches, zero new tests, byte-identical output for well-formed registry data. Adds private `isKeywordSetData(x: unknown): x is KeywordSetData` type guard + per-iteration `typeof` / `Array.isArray` shape guards with `continue` on mismatch (no `try/catch`). Replaces `findFlatCardForVillainCard` O(V·F) rescan with a local `Set<string>` pre-index built once before the `listSets()` loop (O(V+F)); `Set` strictly function-local per **D-8802** — never placed in `G`, returned, or exported. Replaces push-and-break ordering with local boolean flags + canonical emission order `['patrol', 'ambush', 'guard']` — byte-identical to `BOARD_KEYWORDS` at `boardKeywords.types.ts:24` per **D-8801** (one canonical order across the engine, not two); each `result[extId]` is a freshly-constructed `BoardKeyword[]` per D-8802 (WP-028 `cardKeywords` aliasing precedent). Deletes `KeywordSetData.abbr` and `KeywordSetData.henchmen` (unused); preserves `KeywordVillainCardEntry`, `KeywordVillainGroupEntry`, `KeywordFlatCard`, `CardKeywordsRegistryReader` verbatim. Tightens `buildCardKeywords` JSDoc return-contract sentence (local / contractual; no longer asserts caller behavior). Whitespace-tolerance deferral (no `trimStart()` on Ambush prefix-match) locked by **D-8803** with explicit reversal conditions — the existing 304-occurrence verified-invariant `// why:` comment stands verbatim. No `boardgame.io` or `@legendary-arena/registry` imports; no `.reduce(`; no `try/catch`. Function signature byte-identical pre/post: `export function buildCardKeywords(registry: unknown, _matchConfig: MatchSetupConfig): Record<CardExtId, BoardKeyword[]>`. Caller at `packages/game-engine/src/setup/buildInitialGameState.ts:173` untouched. Locked test baseline: engine `506 / 113 / 0` (post-WP-087 at `73aeada`) + repo-wide `671 / 127 / 0` (post-WP-051 at `ce3bffb`); no test-count change permitted. Required `// why:` comments on `isKeywordSetData`, villain pre-index (cite D-8802), and canonical emission order (cite D-8801 + D-8802). Aliasing self-audit at `## After Completing` traces every `result[<extId>]` assignment for fresh-array identity. Self-compliant `## Vision Alignment` block cites `§1, §2`. A0 SPEC bundle complete 2026-04-23 (D-8801 / D-8802 / D-8803 + WP-088 `## Appendix A` 00.3 lint PASS + WORK_INDEX + EC_INDEX registration). See [EC-088-build-card-keywords-hardening.checklist.md](EC-088-build-card-keywords-hardening.checklist.md) + [WP-088](../work-packets/WP-088-build-card-keywords-hardening.md) + D-8801 / D-8802 / D-8803. Executed 2026-04-23 at commit `d183991` (A0 SPEC `88580a9`). Adjusted test baseline `507 / 114 / 0` engine + `672 / 128 / 0` repo-wide (rebased from invocation's pre-drift numbers per post-WP-087 A1 amendment `d5880d2`); no test count change caused by WP-088. | Done 2026-04-23 |
| EC-089 | WP-089    | Game Engine / Runtime Wiring | **WP-089 — Engine PlayerView Wiring (UIState Projection).** Single-file runtime-wiring pass on `packages/game-engine/src/game.ts` that registers `playerView: buildPlayerView` on the `LegendaryGame` object so every state frame boardgame.io pushes to a connected client is an audience-filtered `UIState` instead of raw `LegendaryGameState`. Adds one new top-level named function `buildPlayerView(gameState, ctx, playerID)` positioned adjacent to `setup` / `moves` / `phases` per EC-089 §Guardrails; composes `buildUIState` (WP-028) + `filterUIStateForAudience` (WP-029) — both locked contracts, untouched by this packet. Audience derivation (locked): `typeof playerID === 'string'` → `{ kind: 'player', playerId: playerID }`; `null` or `undefined` → `{ kind: 'spectator' }`. Projection order (locked): build `UIBuildContext` from `ctx.phase` / `ctx.turn` / `ctx.currentPlayer` → `buildUIState(gameState, uiBuildContext)` → derive audience → `filterUIStateForAudience(fullUIState, audience)` → return. `buildPlayerView` is pure: no RNG, no wall-clock, no I/O, no mutation of `G` or `ctx`, no `G.messages` append, never throws. Type-assertion lock (RS-3): `Game<LegendaryGameState, …>.playerView` declares `(G, Ctx, PlayerID) => G`; reshape to `UIState` resolved via a single narrowly-scoped `as unknown as Game<LegendaryGameState>['playerView']` cast at the `playerView:` assignment site only — no `Game<…>` generic change, no wrapper Game, no parallel projection, no `PlayerView.STRIP_SECRETS`. `UIAudience` construction is inline (not imported into `game.ts`) because the type lives at `uiAudience.types.ts` and is not re-exported from `uiState.filter.ts` (RS-1). Test fixture pattern is dual-context (RS-2): `makeMockCtx` supplies the `SetupContext` for `buildInitialGameState`; an inline `ctxLike` object literal (`phase`, `turn`, `currentPlayer`) supplies the runtime `Ctx` subset passed into `LegendaryGame.playerView!(…)`. Required `// why:` comments: `null`/`undefined` → spectator mapping; `playerView: buildPlayerView` field as sole engine→client projection boundary; local type assertion rationale. Locked test baseline anchor: engine `507 / 114 / 0` (post-WP-088 at `57327c2`) + repo-wide `672 / 128 / 0`; expected delta `+6 / +1 / 0` yielding `513 / 115 / 0` engine + `678 / 129 / 0` repo-wide. Six new tests in `packages/game-engine/src/game.playerView.test.ts` wrapped in a single `describe('LegendaryGame.playerView (WP-089)')` block: delegation-correctness (seated player), `playerID === null` → spectator, `playerID === undefined` → spectator, determinism (two calls deep-equal), no-mutation of `gameState` (JSON.stringify before/after), no-mutation of `ctx`. No `boardgame.io/testing` import; no modification of `makeMockCtx`. Lifecycle prohibition: `buildPlayerView` consumed exclusively via `LegendaryGame.playerView` — never from phase hooks, turn hooks, or moves (WP-028 lifecycle-isolation precedent). 01.5 INVOKED per new `LegendaryGame` top-level field (single-file scope confined to `game.ts`). 01.6 post-mortem MANDATORY per new long-lived abstraction (engine→client projection boundary) + new contract consumed by WP-090 live-match client wiring. DECISIONS.md adds **D-8901** per WP-089 canonical language — `playerView` reshapes client-visible state to `UIState`; `filterUIStateForAudience` is the audience authority; boardgame.io `STRIP_SECRETS` intentionally not used. Commits use `EC-089:` prefix on code-changing commits; `SPEC:` on governance / pre-flight / governance-close commits (never `WP-089:` per P6-36 — commit-msg hook rejects). Three-commit topology at execution: A0 SPEC pre-flight bundle (register WP-089 + EC-089 governance rows per PS-1/PS-2; session-context, pre-flight, copilot check, session prompt) → A `EC-089:` content (2 files: `game.ts` modified, `game.playerView.test.ts` new) + optional DECISIONS.md D-8901 + optional 01.6 post-mortem → B SPEC governance close (STATUS.md + WORK_INDEX.md WP-089 `[ ]` → `[x]` + EC_INDEX.md EC-089 Draft → Done + DECISIONS.md D-8901 if not landed in A + post-mortem if not landed in A). Staging by exact filename only — never `git add .` / `git add -A` / `git add -u` (P6-27). Parallel workstream `apps/registry-viewer/src/registry/shared.ts` (Vue v-for key fix) + untracked WP-090 / WP-059 / EC-090 / EC-059 / `session-context-wp089.md` files + `.claude/worktrees/` directory untouched and never staged during any WP-089 commit. Drafted 2026-04-23; registered in EC_INDEX + WORK_INDEX 2026-04-24. See [EC-089-engine-playerview-wiring.checklist.md](EC-089-engine-playerview-wiring.checklist.md) + [WP-089](../work-packets/WP-089-engine-playerview-wiring.md) + `docs/ai/session-context/session-context-wp089.md` + `docs/ai/invocations/preflight-wp089-engine-playerview-wiring.md` + `docs/ai/invocations/copilot-wp089-engine-playerview-wiring.md`. | Draft  |
| EC-085 | WP-085    | Governance / Audit Tooling | **WP-085 — Vision Alignment Audit (Detection, Classification & Gating).** Pre-execution checklist, EC-TEMPLATE-conformant (exactly 60 content lines). Three-file Commit-A budget: `scripts/audit/vision/run-all.mjs` (new orchestrator invoking all four domain greps read-only with single PASS/FAIL exit code); `scripts/audit/vision/determinism.greps.mjs` (modified — comment-aware filter on DET-001 only, other RULES untouched); `docs/audits/vision-alignment-{YYYY-MM-DD}.md` (first audit report). Two-channel DET-001 model locked: script-channel executable count = `0` after the comment-aware filter; orchestrator-channel allowlist verification = exactly six doc-comment matches at `packages/game-engine/src/` pairs `moves/coreMoves.impl.ts:10`, `moves/zoneOps.ts:5`, `setup/shuffle.ts:5`, `simulation/ai.legalMoves.ts:9`, `simulation/ai.random.ts:9`, `simulation/simulation.runner.ts:10`. DET-007 remains single-channel by design with four-pair allowlist at `persistence/persistence.types.ts:75`, `persistence/snapshot.create.ts:86`, `persistence/snapshot.create.ts:90`, `versioning/versioning.stamp.ts:59`. Calibrated baseline (6 / 4 / 0 / 0 / 0) captured at INFRA `24996a9` on `main` is consumed as a locked contract per WP-085 Scope (In) §D with named constants `EXPECTED_DET_001 = 6`, `EXPECTED_DET_007 = 4`, `EXPECTED_MONETIZATION = 0`, `EXPECTED_REGISTRY = 0`, `EXPECTED_ENGINE_BOUNDARY = 0`. Two required `// why:` comments: comment-filter asymmetry with DET-007, and orchestrator's same-day re-run refusal explaining audit-history immutability. Commit prefix `EC-085:` at execution (never `WP-085:` per P6-36). Vision trailer `Vision: §3, §13, §14, §22, §24`. Self-compliant `## Vision Alignment` block present in WP-085. Three-commit topology at execution: A0 SPEC pre-flight → A `EC-085:` code (three files above) → B SPEC governance close (STATUS.md + WORK_INDEX.md checkbox + DECISIONS.md with two new entries: Path B operational claim "§17 Vision Alignment is enforced by WP-085 audit tooling" + two-channel DET-001 model as reusable governance pattern). D-8501 anchors WP-085 as the queued §17 enforcement instrument pre-execution (landed at SPEC `2e88aa7`); Path B entries and the two-channel model mechanics decision are deferred to Commit B — pre-execution DECISIONS entries would assert operational reality that does not yet exist. Commit A landed at `c836b29` (three files: `run-all.mjs`, `determinism.greps.mjs` comment-aware filter, first audit report `vision-alignment-2026-04-22.md`); verdict PASS, baseline matched exactly (6 / 4 / 0 / 0 / 0). Commit B lands STATUS.md + WORK_INDEX.md checkbox + EC_INDEX.md row flip + three DECISIONS.md entries (D-8502 calibrated baseline source-of-truth, D-8503 two-channel DET-001 / single-channel DET-007 filter asymmetry, D-8504 same-day audit overwrite refusal as audit-history immutability). Vision trailer `Vision: §3, §13, §14, §22, §24` on Commit A. See `EC-085-vision-alignment-audit.checklist.md` + WP-085 + D-8501 / D-8502 / D-8503 / D-8504. Executed 2026-04-22 at commit `c836b29`. | Done   |

---

## Summary

| Status   | Count |
|----------|-------|
| Done     | 16    |
| Draft    | 47    |
| Blocked  | 0     |
| **Total**| **63**|

---

## Registry Hygiene (R-EC Series)

| EC | Scope | Execution Scope | Status |
|---|---|---|---|
| R-EC-01 | Registry / R2 Data | Fix `[object Object]` abilities in R2 metadata (msmc, bkpt, msis) | Deferred |
| R-EC-02 | Registry / Card Data | Fix missing vp fields in mgtg masterminds | Done |
| R-EC-03 | Registry / R2 Images | Resolve 5 missing images (slug mismatch or not uploaded) | Deferred |

---

## Shared Tooling (EC-060+ Series)

Execution Checklists for shared internal tooling packages under
`packages/*` that are consumed by app test scripts only (not by the
game engine, registry, or server at runtime). Numbered 060+ to keep
the range separate from phase-driven game-engine ECs and viewer ECs.

| EC | Source WP | Layer | Execution Scope | Status |
|---|---|---|---|---|
| EC-065 | WP-065 | Shared Tooling (`packages/vue-sfc-loader/`) | Node `module.register()` loader + `compileVue` pure helper; enables `node:test` to import `.vue` SFCs; hard prerequisite for WP-061, WP-062, WP-064 and future client UI WPs. See `EC-065-vue-sfc-loader.checklist.md`. | Draft |
| EC-067 | WP-061 | Client UI (`apps/arena-client/`) | Gameplay client bootstrap: Vue 3 + Vite + Pinia SPA; `useUiStateStore()` with `snapshot: UIState \| null` + `setSnapshot`; typed fixture loader with `satisfies UIState` discipline; `<BootstrapProbe />` wiring smoke; dev-only `?fixture=` URL harness behind `import.meta.env.DEV` (DCE-guarded); jsdom + `@vue/test-utils` test path via `vue-sfc-loader/register`. No HUD, no networking, no runtime engine import. **Note:** EC-061 was consumed by the registry-viewer Rules Glossary (commit `1b923a4`); WP-061 uses EC-067 — the next free slot. See `EC-067-gameplay-client-bootstrap.checklist.md`. Executed 2026-04-17 at commit `2e68530`. | Done |
| EC-068 | WP-067 | Game Engine (`packages/game-engine/src/ui/`) | UIState projection bridge between WP-048 (PAR scoring types) and WP-062 (Arena HUD consumer): adds `UIProgressCounters { bystandersRescued, escapedVillains }` as non-optional `UIState.progress`; adds optional `UIGameOverState.par: UIParBreakdown { rawScore, parScore, finalScore, scoringConfigVersion }` populated only at `phase === 'end'` with a defined `G.activeScoringConfig`; projection-time aggregation of bystanders from victory piles (no new G counter); drift test pins every new field name for WP-062 aria-label stability; includes three WP-061 fixture type-conformance edits (single `progress` key addition per fixture) so `pnpm -r test` stays green across the contract change. 01.5 NOT INVOKED. 01.6 post-mortem MANDATORY. See `EC-068-uistate-par-projection-and-progress-counters.checklist.md`. Executed 2026-04-17 at commit `1d709e5`. | Done |
| EC-069 | WP-062 | Client UI (`apps/arena-client/src/components/hud/`) | Arena HUD & Scoreboard: seven-file Vue 3 component tree (`ArenaHud`, `TurnPhaseBanner`, `SharedScoreboard`, `ParDeltaReadout`, `PlayerPanelList`, `PlayerPanel`, `EndgameSummary`) + `hudColors.ts` palette helper; six `node:test` component tests under jsdom via `vue-sfc-loader/register`. `ArenaHud.vue` is the sole `useUiStateStore` consumer (container/presenter split); every subcomponent takes its UIState sub-slice via props. `aria-label`s bind to literal leaf field names pinned by WP-067's drift test (`bystandersRescued`, `escapedVillains`, `twistCount`, `tacticsRemaining`, `tacticsDefeated`, `finalScore`). `<SharedScoreboard />` renders unconditionally (`progress` is required on every UIState). `<ParDeltaReadout />` renders em-dash when `!('par' in gameOver)` (D-6701 dominant runtime case); zero is a valid engine value and renders as `0`. `base.css` gains five HUD tokens with numeric contrast ratios. `ArenaHud.vue`, `PlayerPanel.vue`, `PlayerPanelList.vue`, `ParDeltaReadout.vue`, and `EndgameSummary.vue` use the `defineComponent({ setup() { return {...} } })` form per D-6512/P6-30 — under vue-sfc-loader's separate-compile pipeline, template bindings beyond props must be returned from `setup()` and imported child components must be registered via the `components: {...}` option; `TurnPhaseBanner.vue` + `SharedScoreboard.vue` remain in `<script setup>` (props-only templates). **Note:** EC-062–EC-064 unused; EC-065–EC-068 historically bound; following the EC-061 → EC-067 and EC-066 → EC-068 retargeting precedent, WP-062 uses EC-069 — the next free slot. 01.5 NOT INVOKED. 01.6 post-mortem MANDATORY (new long-lived abstraction + new contract consumption). See `EC-069-arena-hud-scoreboard.checklist.md`. Executed 2026-04-18 at commit `<pending — gatekeeper session>`. | Done |
| EC-070 | WP-068 | Client UI (`apps/registry-viewer/src/prefs/`) | Preferences Foundation: Pinia-backed preferences subsystem with three shared-tier sections (`appearance`, `accessibility`, `advancedBase`). Adds `pinia ^2.1.7` dep + `tsx` devDep + `test` script to `apps/registry-viewer/package.json`; `createPinia()` wiring + side-effect `registerSections` import in `src/main.ts`. Nine new source files (four `*.schema.ts` + `sectionRegistry.ts` + `persistence.ts` + `createPreferencesStore.ts` + `usePreferences.ts` + `registerSections.ts`) + four `*.test.ts` (schema defaults + purity guard + registry duplicate-id + store round-trip/corrupt-blob backup). Defaults calibrated to plan §2.3 verbatim so a fresh visitor sees bit-identical UI to pre-packet (§5.7 "defaults-match-today"). Option A path locked per D-1414; `packages/ui-preferences/` not created. Schema tier is Node-consumable (no `vue` / `@vue/` / `document.` / `window.` imports in any `*.schema.ts` — enforced by `_schema-purity.test.ts`) so the eventual Option-A → Option-B hoist remains a pure file-move. Persistence is corruption-safe (missing vs corrupt JSON distinguished; corrupt blob copied to `BACKUP_KEY`). **Note:** Drafted alongside execution per P6-36 (the commit-msg hook rejects `WP-###:` prefixes; an EC-### commit prefix requires an EC file). 01.5 NOT INVOKED. 01.6 post-mortem MANDATORY (new long-lived abstraction + new runtime wiring). See `EC-070-preferences-foundation.checklist.md`. Executed 2026-04-18 at commit `bbd58b0` (+ `8ec6ced` structural refinement). | Done |
| EC-071 | WP-063 | Game Engine (`packages/game-engine/src/replay/`) + CLI Producer App (`apps/replay-producer/`) | Replay Snapshot Producer: adds engine type `ReplaySnapshotSequence { version: 1, snapshots: readonly UIState[], metadata? }` + pure helper `buildSnapshotSequence({ setupConfig, seed, playerOrder, moves, registry, metadata? })` that wraps WP-080's `applyReplayStep` step-level API, calls WP-028's `buildUIState` at each step, and returns a frozen sequence (no I/O, no logging, no wall clock, no RNG, no `boardgame.io` import). Adds NEW CLI app `apps/replay-producer/` (first `cli-producer-app` per D-6301) with `produce-replay --in --out --match-id --produced-at` flags, documented exit codes 0–4 (named constants `EXIT_OK` / `EXIT_INVALID_ARGS` / `EXIT_INPUT_PARSE` / `EXIT_ENGINE` / `EXIT_OUTPUT_WRITE` behind a single `// why:` block), sorted top-level JSON keys (D-6302), optional-field omission (D-6303 — never `"metadata": undefined` / `null`), sourcemaps via `process.setSourceMapsEnabled(true)` at entry, and a committed `three-turn-sample` golden artifact triplet. Execution surfaced **D-6305** (`ReplayInputsFile` field named `moves: readonly ReplayMove[]` to match WP-027 canonical naming, not `inputs: readonly ReplayInput[]` as the WP literally phrased it; helper requires explicit `playerOrder` + `registry` parameters beyond the WP's 3-field signature; full rationale in `docs/ai/post-mortems/01.6-WP-063-replay-snapshot-producer.md` §D-6305). **Note:** EC-062–EC-064 unused; EC-065–EC-070 historically bound; following the EC-061 → EC-067, EC-066 → EC-068, EC-062 → EC-069 retargeting precedent, WP-063 uses EC-071 — the next free slot. Commits use `EC-071:` prefix (never `WP-063:` per P6-36). 01.5 NOT INVOKED. 01.6 post-mortem MANDATORY (new long-lived abstraction + new code category) — delivered in-session at `docs/ai/post-mortems/01.6-WP-063-replay-snapshot-producer.md`; verdict WP COMPLETE. Pre-commit review ran in separate gatekeeper session per P6-35; no P6-42 deviation. See `EC-071-replay-snapshot-producer.checklist.md`. Executed 2026-04-19 at commit `97560b1`. | Done |
| EC-072 | WP-080 | Game Engine (`packages/game-engine/src/replay/`) | Replay Harness Step-Level API: adds named export `applyReplayStep(gameState, move, numPlayers): LegendaryGameState` to `packages/game-engine/src/replay/replay.execute.ts` (Q1 = Option A — single function, minimum surface); refactors `replayGame`'s internal loop to delegate to the new export so `MOVE_MAP` remains the single source of truth for dispatch (Q3 = Option A; regression guard via byte-identical `computeStateHash` on the existing `verifyDeterminism` fixture). `MOVE_MAP`, `buildMoveContext`, and `ReplayMoveContext` remain file-local (Q4 — not exported). State-ownership contract: mutate-and-return-same-reference (Q2 = Option A; consumers wanting historical snapshots project via `buildUIState`, not `G` copies). `ReplayInputsFile` is OUT OF SCOPE (Q5 — WP-063's concern). RNG semantics unchanged; D-0205 remains in force. New test file `replay.execute.test.ts` asserts identity, `replayGame` regression, and unknown-move warning-and-skip routing. Adds exactly one new export line under the existing WP-027 block in `packages/game-engine/src/index.ts`. **Note:** EC-062–EC-064 unused; EC-065–EC-071 historically bound; following the EC-061 → EC-067, EC-066 → EC-068, EC-062 → EC-069, and EC-063 → EC-071 retargeting precedent, WP-080 uses EC-072 — the next truly free slot. Commits use `EC-072:` prefix (never `WP-080:` per P6-36). Hard upstream: WP-079 / EC-073 (JSDoc narrowing on `replay.execute.ts` must land first; both packets touch the same file; WP-080 inherits WP-079 verbatim). 01.5 NOT INVOKED. 01.6 post-mortem MANDATORY (new long-lived abstraction — `applyReplayStep` becomes the canonical step-level dispatch surface for every future replay consumer). See `EC-072-replay-harness-step-level-api.checklist.md`. Executed 2026-04-19 at commit `dd0e2fd`. | Done |
| EC-073 | WP-079 | Game Engine / Documentation (`packages/game-engine/src/replay/`) | Label Replay Harness as Determinism-Only: doc-only JSDoc + module-header rewrite on `packages/game-engine/src/replay/replay.execute.ts` and `packages/game-engine/src/replay/replay.verify.ts` per D-0205's single follow-up action. Zero runtime behavior change; zero signature change; zero export change; zero test change (test count IDENTICAL to starting commit). Forbidden phrases ("replays live matches", "replays a specific match", "reproduces live-match outcomes") grep to zero; required phrases ("determinism-only" ≥ 2 in execute / ≥ 1 in verify; D-0205 xref in both; `MOVE_LOG_FORMAT.md` Gap #4 xref in execute). Existing `// why:` comments preserved verbatim (reverse-shuffle `replay.execute.ts:118–124`; events no-op `:110–117`; two-run rationale `replay.verify.ts:43–45`). Hard downstream: WP-080 / EC-072 (both WPs touch `replay.execute.ts`; WP-079 lands first and WP-080 inherits the narrowing verbatim). **Note:** EC-062–EC-064 unused; EC-065–EC-072 historically bound; following the EC-061 → EC-067, EC-066 → EC-068, EC-062 → EC-069, EC-063 → EC-071, and EC-080 → EC-072 retargeting precedent, WP-079 uses EC-073 — the next truly free slot. Commits use `EC-073:` prefix (NEVER `WP-079:` per P6-36 — the WP body's trailing note saying "use `WP-079:` prefix" predates P6-36 and is superseded by EC-073's Locked Values). NO 01.6 post-mortem required (doc-only; no new long-lived abstraction; no new code category — both P6-35 triggers absent). See `EC-073-label-replay-harness-determinism-only.checklist.md`. Executed 2026-04-19 at commit `1e6de0b`. | Done |
| EC-074 | WP-064 | Client UI (`apps/arena-client/src/replay/` + `src/components/log/` + `src/components/replay/` + `src/fixtures/replay/`) | Game Log & Replay Inspector: four new SFCs (`<GameLogPanel />`, `<ReplayInspector />`, `<ReplayFileLoader />`, plus `loadReplay.ts` parser) that consume `ReplaySnapshotSequence` from WP-063 and drive `useUiStateStore().setSnapshot` on index changes. **Consumer-side `version === 1` assertion per D-6303** carries three locked full-sentence error templates mirroring the WP-063 CLI wording (invalid version / missing `snapshots` / empty `snapshots`). **`<ReplayInspector />` and `<ReplayFileLoader />` are non-leaf** under `@legendary-arena/vue-sfc-loader` and use `defineComponent({ setup() {...} })` form per P6-30 / P6-40 (`<ReplayFileLoader />` was promoted from `<script setup>` mid-execution after the first test run revealed its template references non-prop bindings — same vue-sfc-loader separate-compile failure WP-061's `<BootstrapProbe />` and WP-062's HUD containers documented per D-6512 / P6-30); `<GameLogPanel />` stays in `<script setup>` (props-only template). Keyboard controls `←` / `→` / `Home` / `End` with `tabindex="0"` root + listeners-on-root — locks **D-6401** as first repo precedent (confirmed via WP-062 HUD review: no prior keyboard-stepper art). Fixture is 8 snapshots produced by the committed WP-063 CLI using 3 `advanceStage` moves (for `G.currentStage` stage transitions: start→main between snapshots 0-1, main→cleanup between 2-3) + 4 unknown-move records (for `UIState.log` growth via `applyReplayStep`'s warning-and-skip at `replay.execute.ts:162–166`); **phase transitions unreachable via producer per D-0205, rescoped to stage transitions** per the WP-064 2026-04-19 amendment. Fixture path `apps/arena-client/src/fixtures/replay/three-turn-sample.{json,inputs.json,cmd.txt}`; byte-identical regeneration confirmed twice. Sourcemaps inherit Vite defaults (WP-061). 01.5 NOT INVOKED. 01.6 post-mortem MANDATORY (new long-lived abstraction `parseReplayJson` + new keyboard focus precedent D-6401) delivered in-session at `docs/ai/post-mortems/01.6-WP-064-log-replay-inspector.md` — verdict WP COMPLETE. One in-allowlist refinement applied during post-mortem: `<ReplayInspector />`'s `currentLog` computed now spread-copies `snapshot.log` before passing to `<GameLogPanel />` (WP-028 / D-2802 aliasing-prevention). Pre-commit review ran in separate gatekeeper session per P6-35 default; no P6-42 deviation. Verification (12 of 12 pass): build/typecheck/test exit 0; arena-client 35→66 (+31); repo-wide 486→517 (+31); no runtime engine/registry/boardgame.io import; no engine move/hook names leaked; no `Math.random`/`Date.now`/`performance.now` (P6-43 paraphrase form); no `.reduce()` in rendering; engine/registry/vue-sfc-loader/server/replay-producer/registry-viewer untouched; `pnpm-lock.yaml` absent (P6-44); both stashes intact; EC-069 placeholder not backfilled; WP-080 post-mortem not staged. **Note:** EC-062–EC-064 unused; EC-065–EC-073 historically bound; following the EC-061 → EC-067, EC-066 → EC-068, EC-062 → EC-069, EC-063 → EC-071, EC-080 → EC-072, and EC-079 → EC-073 retargeting precedent, WP-064 uses EC-074 — the next free slot. Commits use `EC-074:` prefix (never `WP-064:` per P6-36). See `EC-074-log-replay-inspector.checklist.md` + `docs/ai/preflight-wp064.md` (READY verdict 2026-04-19). Executed 2026-04-19 at commit `76beddc`. | Done |

---

## Registry Viewer (EC-101+ Series)

Ad-hoc viewer-scoped ECs for CI, tooling, a11y, performance, and
deployment hardening of `apps/registry-viewer`. Numbered 101+ to keep
the range clearly separate from game-engine WP-backed ECs.

| EC | Scope | Execution Scope | Status |
|---|---|---|---|
| EC-101 | Viewer / CI + Tooling + A11y | Fix CI duplicate `on.push` triggers; scaffold viewer `typecheck` + `lint` scripts + `.eslintrc.cjs`; add `aria-live` status announcements in `App.vue` | Draft |
| EC-102 | Viewer / Type Consolidation + Cosmetic Lint | Consolidate viewer on `types-index.ts` (wide/live FlatCard); retire stale `types/index.ts` as canonical source for browser.ts/httpRegistry.ts/shared.ts; null-safe field mappings + stringify attack/recruit in shared.ts; silence 2 cosmetic ESLint rules; `prefer-const` fix. **CI gating deferred to EC-103** (vue-tsc exclude bug + 29 real a11y errors surfaced by eslint-plugin-vuejs-accessibility) | Draft |
| EC-103 | Viewer / A11y Cleanup + CI Gating | Resolve EC-102's two blockers: (1) add `@types/node` to unblock `localRegistry.ts` typecheck + re-point its types to `types-index.ts`; (2) fix 29 real a11y errors across 8 Vue SFCs (`no-static-element-interactions` ×13, `click-events-have-key-events` ×10, `form-control-has-label` ×3, `no-redundant-roles` ×1) — prefer `<div @click>` → `<button>` semantic swaps over ARIA. Then wire `Lint viewer` + `Typecheck viewer` CI steps into `build-viewer` job. Includes manual keyboard smoke test. | Done |
| EC-104 | Viewer / Debug Surface (Dev-Only) | Add unified dev-only debug gate `DEBUG_VIEWER` (`import.meta.env.DEV && ?debug`) in new `src/lib/debugMode.ts`; rewrite `devLog.ts` to locked categorical signature `(category, message, fields?)`; instrument `registryClient.ts` / `themeClient.ts` with `load start/complete/failed` events; add conditional `.debug-section` inside `HealthPanel.vue` with optional `debugState` prop fed inline from `App.vue`; tighten ESLint with `no-console: ['error', { allow: ['warn','error'] }]` + overrides for `devLog.ts` / `debugMode.ts`. DCE hard-gate verified: zero `DEBUG_VIEWER` / `debugMode` references in `dist/`. | Done |
| EC-105 | Viewer / A11y Interaction Tracing (Dev-Only) | Add `"a11y"` category to `devLog` plus `traceSource()` helper; instrument backdrop-close / activation / resize / cross-link handlers across `CardDetail.vue`, `GlossaryPanel.vue`, `ImageLightbox.vue`, `HealthPanel.vue`, `ThemeDetail.vue`, and `App.vue` `handleKeydown`. Log-only, zero UI, zero behavior changes, all gated by `DEBUG_VIEWER`. **Trigger:** execute only after WP-065 (Vue SFC Test Transform Pipeline) lands — rationale in checklist §Before Starting. See `EC-105-viewer-a11y-interaction-tracing.checklist.md`. | Deferred |
| EC-106 | WP-060 | Registry Viewer (`apps/registry-viewer/src/lib/` + `src/composables/`) + Content / Data (`data/metadata/`) | Keyword & Rule Glossary Data Migration: serialize the hardcoded `KEYWORD_GLOSSARY` (113 entries) and `RULES_GLOSSARY` (20 entries) Maps from `useRules.ts` into `data/metadata/keywords-full.json` + `data/metadata/rules-full.json` (alphabetical by `key`; token markup preserved verbatim); upload both files to `images.barefootbetters.com/metadata/` (operator-step with R2 credentials — STOP+escalate if unavailable); add new `src/lib/glossaryClient.ts` singleton fetcher mirroring `themeClient.ts` structure (module-scope `_promise` holders, `devLog("glossary", ...)` instrumentation, non-blocking). **Lock: `lookupKeyword` / `lookupRule` algorithmic bodies preserved byte-for-byte** — only the backing Map source changes (40-line matcher with exact / space-hyphen-stripped / prefix / suffix / substring branches must survive retargeting). `HERO_CLASS_GLOSSARY` (5 entries), `RuleEntry`, `parseAbilityText`, `lookupHeroClass`, `AbilityToken`, `TokenType` all preserved verbatim in `useRules.ts`. `useGlossary.ts` scope expansion (reactive `allEntries` ref + exported `rebuildGlossaryEntries()` called from `App.vue` post-fetch) authorized under viewer analog of `docs/ai/REFERENCE/01.5-runtime-wiring-allowance.md`. `App.vue` gains one glossary-load block inside existing `onMounted` try, parallel to the `getThemes()` pattern. `docs/03.1-DATA-SOURCES.md` §Registry Metadata gains two new rows. Baseline 588 / 0 preserved; zero new tests required (optional `glossaryClient.ts` tests follow `themeClient.ts` precedent if authored — one `describe()` per file per WP-031/057 precedent). 01.5 NOT INVOKED as an engine-contract clause (no engine wiring); viewer analog cited only for `useGlossary.ts` scope expansion. 01.6 post-mortem NOT TRIGGERED (`glossaryClient.ts` is a new instance of an existing abstraction `themeClient.ts`, not a new long-lived abstraction; viewer `src/lib/` is pre-classified). **Note:** `EC-060-keyword-glossary.checklist.md` slot consumed by commit `6a63b1c` (earlier viewer-scope glossary tooltips + mobile set-pill fix); following the EC-061 → EC-067, EC-066 → EC-068, EC-062 → EC-069, EC-063 → EC-071, EC-080 → EC-072, EC-079 → EC-073, and EC-064 → EC-074 retargeting precedent, WP-060 uses **EC-106** — the next free slot in the registry viewer 101+ series. Commits use `EC-106:` prefix (never `WP-060:` per P6-36). See `EC-106-keyword-rule-glossary-data.checklist.md` + `docs/ai/invocations/preflight-wp060-keyword-rule-glossary-data.md` (DO NOT EXECUTE YET → five pre-session actions → READY on resolution). | Done |
| EC-107 | WP-082 | Registry (`packages/registry/src/schema.ts` + `src/index.ts`) + Registry Viewer (`apps/registry-viewer/src/**`) + Content / Data (`data/metadata/`) — **WP-082 — Keyword & Rule Glossary Schema, Labels, and Rulebook Deep-Links.** Adds `KeywordGlossaryEntrySchema` / `KeywordGlossarySchema` / `RuleGlossaryEntrySchema` / `RuleGlossarySchema` + inferred types to `packages/registry/src/schema.ts` — **first `.strict()` schemas in the file**, author-facing-strict vs loader-permissive per WP-033 / D-3303. Re-exports four schemas + two types from `packages/registry/src/index.ts`. Backfills required `label` + optional `pdfPage` on all 123 entries in `data/metadata/keywords-full.json` (count drifted from WP-060's 113 to 123 pre-authoring — 10 new keys awaiting label/pdfPage); backfills optional `pdfPage` on 20 entries in `data/metadata/rules-full.json` with an RS-3 pre-flight gate (`git diff data/metadata/rules-full.json` — whitespace → revert + re-apply; content → STOP). Uploads the Marvel Legendary Universal Rulebook v23 PDF (44 MB) to R2 at `images.barefootbetters.com/docs/legendary-universal-rules-v23.pdf` with `Content-Type: application/pdf` + `Cache-Control: max-age=31536000, immutable`. Adds `rulebookPdfUrl` config field. `glossaryClient.ts` gains `.safeParse(...)` at fetch boundary (`.parse(...)` forbidden) with full-sentence `console.warn` + empty-Map fallback; widens keyword Map value shape to `{ label, description }` per locked option (α); parallel `Map<string, number>` for keyword `pdfPage` owned by `glossaryClient.ts` (option (β) widening past `{ label, description }` forbidden). `useRules.ts` widens `setGlossaries` / `getKeywordGlossaryMap` / `lookupKeyword` signatures; `lookupKeyword` algorithmic body preserved byte-for-byte except three identifier-only `.description` suffix adds at lines 160/164/198 (exact / space-hyphen-stripped / prefix / suffix / substring branching intact per EC-106 lock). Widens `RuleEntry` with `pdfPage?: number`. Adds `HERO_CLASS_LABELS` Map with 5 hardcoded entries. `HERO_CLASS_GLOSSARY` preserved verbatim. Deletes `titleCase()` heuristic from `useGlossary.ts` (broke rulebook capitalization in five WP-060-audited cases: `Chooseavillaingroup` / `Shieldclearance` / `Greyheroes` / `Half-points` + all punctuation-bearing names) plus the villainousweapons dedup block. Extends `GlossaryEntry` with `pdfPage?: number`. Renders conditional rulebook deep-link anchor in `GlossaryPanel.vue` with mandatory `@click.stop` + `target="_blank"` + `rel="noopener"` + `v-if="entry.pdfPage !== undefined && rulebookPdfUrl"`; href `${rulebookPdfUrl}#page=${entry.pdfPage}` (RFC 3778 §3). `App.vue` gains exactly one ref + one prop — no other edit. Prepends Authority Notice blockquote to `docs/legendary-universal-rules-v23.md` (5262-line `pdftotext -layout` extract committed alongside the raw `docs/Marvel Legendary Universal Rules v23.txt`). Updates `apps/registry-viewer/CLAUDE.md` with verbatim sentence "Do not infer labels from keys under any circumstance." Updates `docs/03.1-DATA-SOURCES.md` §Registry Metadata (113 → 123 + schema-reference notes + new rulebook PDF row). Six DECISIONS.md entries in Commit B including drift-detection note (`.strict()` enforces drift for editorial metadata vs canonical-array parity per WP-033 / D-3303) and D-6001 symmetric supersession back-pointers. `rulebookPdfUrl` absence is a supported configuration — silent anchor omission, no fallback UI. Browser-native `#page=N` PDF viewer only (no PDF.js). Test baseline UNCHANGED at **596 / 0 failing** (no new tests). No new dependencies; `pnpm-lock.yaml` unchanged. 01.5 engine clause NOT INVOKED; viewer analog invoked (explicit authorization) for 5 viewer files per WP-060 / D-6007. 01.6 post-mortem NOT TRIGGERED. Three-commit topology: A0 `SPEC:` → A `EC-107:` → B `SPEC:`. R2 PDF upload + JSON republish are separate operator steps between A and B. Pre-flight + 01.7 copilot check (30/30 PASS CONFIRM after HOLD → fix cycle resolving RS-1..RS-12 + PS-1..PS-4 + RISK FIX #1..#4). Commit prefix `EC-107:` at execution (never `WP-082:` per P6-36). See `EC-107-keyword-rule-glossary-schema-and-labels.checklist.md` + `docs/ai/invocations/preflight-wp082-keyword-rule-glossary-schema-and-labels.md` + `docs/ai/invocations/session-wp082-keyword-rule-glossary-schema-and-labels.md` + `docs/ai/session-context/session-context-wp082.md`. **Executed 2026-04-21 at commit `3da6ac3`; three amendments landed at Commit B: A-082-01 formalizes three beyond-allowlist Vite-resolution additions (viewer `"@legendary-arena/registry": "workspace:*"` dep + registry `"./schema"` subpath export + 3-line `pnpm-lock.yaml` workspace-link delta); A-082-02 records the RS-3 diff-gate STOP and path-1 quarantine of the pre-session `rules-full.json` summary rewrite to `stash@{0}`; A-082-03 records the R2 operator sequence including the `.md` → `.pdf` upload iteration.** | Done |
| EC-108 | WP-083 | Registry (`packages/registry/src/schema.ts` + `src/index.ts` + `packages/registry/package.json`) + Registry Viewer (`apps/registry-viewer/src/lib/registryClient.ts` + `src/lib/themeClient.ts` + `CLAUDE.md`) | **WP-083 — Fetch-Time Schema Validation for Registry-Viewer Clients.** Adds `ViewerConfigSchema` + `ThemeIndexSchema` + inferred types `ViewerConfig` / `ThemeIndex` to `packages/registry/src/schema.ts`; `ViewerConfigSchema` is `.strict()` (first object schema in the file carrying `.strict()` beyond EC-107's glossary additions); `ThemeIndexSchema` is an array schema with per-element `/\.json$/` regex. `RegistryConfigSchema` body preserved byte-for-byte — only adjacent comment updated to disambiguate from `ViewerConfigSchema` (naming collision lock per D-8302). General-schema re-export block in `src/index.ts` extended with two new exports + two inferred types; **theme-schema re-export block at lines ~47–57 NOT modified**. **Amendment A-083-04** adds `./theme.schema` subpath export to `packages/registry/package.json` (mirrors A-082-01 `./schema` precedent; barrel import would break the viewer's Vite/Rollup build per `glossaryClient.ts:20–28`); D-8305 locks this as reusable precedent. `registryClient.ts` retrofitted: inline `RegistryConfig` interface deleted; imports `ViewerConfigSchema` + `ViewerConfig` from `@legendary-arena/registry/schema`; `.safeParse(...)` at fetch boundary with full-sentence `[RegistryConfig] Rejected ...` throw on failure. `themeClient.ts` retrofitted with split-subpath imports: `ThemeIndexSchema` from `@legendary-arena/registry/schema` AND `ThemeDefinitionSchema` + `ThemeDefinition` from `@legendary-arena/registry/theme.schema`; four inline interfaces (`ThemeSetupIntent` / `ThemePlayerCount` / `ThemePrimaryStoryReference` / `ThemeDefinition` at lines ~12–47) deleted; index validation **throws** on failure; individual theme validation **warns + skips** on failure (`Promise.allSettled` + null-filter + sort-by-name tail preserved byte-for-byte). Error rendering locked to first-Zod-issue `path.join('.') + ' — ' + message` with `[RegistryConfig]` / `[Themes]` category tag; `.format()` dumps and multi-issue arrays forbidden (mirrors EC-107 `[Glossary] Rejected` pattern). `.parse(...)` forbidden at all fetch boundaries — `.safeParse(...)` only. **Severity policy (D-8303):** throw for hard dependencies (viewer config + theme index); warn + skip for isolated batch entries (individual theme files). `theme.schema.ts` + `theme.validate.ts` locked (D-5504 / D-5509 / EC-055); `git diff` must be empty post-execution. Viewer's local `src/registry/schema.ts` duplicate is a known architectural smell (out of scope). Auxiliary metadata schemas (`CardTypeEntrySchema` / `HeroClassEntrySchema` / `HeroTeamEntrySchema` / `IconEntrySchema` / `LeadsEntrySchema`) remain offline-only per D-8304 — future runtime fetcher requires its own WP. `apps/registry-viewer/CLAUDE.md` updated to note fetch-boundary Zod validation on all four R2 fetchers (registry config, themes, keywords, rules). `docs/03.1-DATA-SOURCES.md` update is optional (doc does not currently list viewer public config or themes as source rows). 5 new DECISIONS.md entries at Commit B: D-8301 (viewer fetches validate at boundary) / D-8302 (naming distinction) / D-8303 (severity policy) / D-8304 (auxiliary schemas offline-only) / D-8305 (`./theme.schema` subpath precedent, introduced by A-083-04). Test baseline UNCHANGED at **596 / 0 failing** (inherited from WP-082/EC-107 close at commit `3da6ac3`); optional `packages/registry/src/schema.test.ts` schema-parse tests authorized by executor judgment per copilot-check Finding #11 (one `describe()` per schema — if authored, adds +3 tests / +2 suites to baseline and must be re-declared in session prompt before Commit A). `ThemeDefinition` type shift is strict widening for optional fields AND narrowing of `themeSchemaVersion` from `number` to `z.literal(2)` — Verification Step 7.1 grep-gate confirms zero consumers outside the deleted inline interface. `themeSchemaVersion` narrowing is safe because viewer only reads the field. No new dependencies (`zod` already in both packages); `pnpm-lock.yaml` unchanged (additive `exports` entry only; workspace link already exists from A-082-01). 01.5 engine clause NOT INVOKED (no `LegendaryGameState` field, no `buildInitialGameState` change, no `LegendaryGame.moves` entry, no phase hook); viewer analog NOT INVOKED (retrofit is within already-allowlisted files). 01.6 post-mortem NOT TRIGGERED (new schemas are instances of existing abstraction per WP-082 precedent; `./theme.schema` subpath is a new instance of A-082-01 abstraction; no new code category; zero engine touch). Three-commit topology: A0 `SPEC:` (governance bundle — WORK_INDEX row + EC_INDEX row + session-context + WP-083 A-083-01 through A-083-04 amendments + EC-108 + pre-flight v2 + copilot-check v2 + session prompt) → A `EC-108:` (code + package.json + viewer + docs) → B `SPEC:` (STATUS/WORK_INDEX flip + EC_INDEX flip + DECISIONS). No R2 JSON republish needed (validation-retrofit only; data unchanged). Pre-flight v2 READY TO EXECUTE after PS-1/2/3 resolution + A-083-04 amendment; copilot-check v2 CONFIRM 30/30 PASS. Commit prefix `EC-108:` at execution (never `WP-083:` per P6-36). See `EC-108-fetch-time-schema-validation.checklist.md` + `docs/ai/invocations/preflight-wp083.md` (v1 DO NOT EXECUTE YET → v2 READY) + `docs/ai/invocations/copilot-check-wp083.md` (v1 SUSPEND → v2 CONFIRM) + `docs/ai/invocations/session-wp083-fetch-time-schema-validation.md` + `docs/ai/session-context/session-context-wp083.md`. **Executed 2026-04-21 at commit `601d6fc`** under EC-108; no executor-time amendments needed; verification steps 1–22 + 7.1 + 7.2 + 7.3 all pass; baseline preserved 596/0 failing; offline schema smokes 23d–23h + bonus regex all behave as locked; dev server + production preview both serve HTTP 200 with retrofit strings present and zero `__vite-browser-external` leaks; five governance entries D-8301..D-8305 land at Commit B. | Done |
| EC-109 | WP-084 | Registry (`packages/registry/src/schema.ts` + `src/index.ts` + `scripts/validate.ts`) + Content / Data (`data/metadata/`) | **WP-084 — Delete Unused Auxiliary Metadata Schemas and Files.** Delete-only subtractive packet. Removes five unused auxiliary Zod schemas (`CardTypeEntrySchema`, `HeroClassEntrySchema`, `HeroTeamEntrySchema`, `IconEntrySchema`, `LeadsEntrySchema`), five corresponding JSON files in `data/metadata/`, the orphan `card-types-old.json`, and the Phase 2 metadata-validation block in `validate.ts` (renumbers former Phases 3/4/5 → 2/3/4). Zero runtime consumers per 2026-04-21 audit; four surviving metadata files (`keywords-full.json`, `rules-full.json`, `sets.json`) and all surviving schemas are LOCKED byte-for-byte. No engine / server / viewer-runtime / pre-plan / R2 changes. **Independent of WP-082 and WP-083.** Six pre-flight STOP gates defined (branch/baseline; usage-audit greps; contract/doc sanity incl. `00.2` active-vs-historical distinction; `validate.ts` phase-number safety; git-file health; deletion-intent confirmation). Commit prefix `EC-109:` at execution (never `WP-084:` per P6-36). See `EC-109-delete-unused-auxiliary-metadata.checklist.md` + `docs/ai/invocations/preflight-wp084.md` (Pending verdict → READY in execution session 2026-04-21 after Gate 2/3/4/5/6 re-runs). **Executed 2026-04-21 at commit `b250bf1`** under EC-109; A-084-01 amendment landed in A0 SPEC bundle expanding scope with viewer dead-code deletion + 00.2 rewrite + current-state docs sweep + legacy `Validate-R2-old.ps1` deletion + registry JSDoc cleanup; verification steps 1–24 all pass; baseline preserved 596/0 failing; seven DECISIONS.md governance entries D-8401..D-8407 + D-6002 historical-neighbor note land at Commit B. | Done |
| EC-110 | (no WP — ad-hoc INFRA) | Registry build tooling (`packages/registry/scripts/validate.ts`) + CI (`.github/workflows/ci.yml`) | Validate Registry CI path fix: `validate.ts` defaults resolve via `fileURLToPath(import.meta.url)` instead of `process.cwd()` so `pnpm --filter @legendary-arena/registry validate` works (CI filter sets CWD to `packages/registry/`). Removes dead `CARDS_DIR` / `DATA_VERSION` env block from the validate step — script reads `SETS_DIR`, not `CARDS_DIR`, and `DATA_VERSION` is only consumed by `upload-r2.ts`. Env overrides still win. `HEALTH_OUT` intentionally remains CWD-relative so `packages/registry/dist/registry-health.json` artifact-upload path keeps resolving under `pnpm --filter`. Surfaced two pre-existing data errors hidden by the path bug (msp1 sentinel `-1` ids; shld numeric `attack`/`recruit` where schema wants strings) — repaired upstream in `C:\Users\jjensen\bbcode\modern-master-strike\src\data\patches\{msp1,shld}.patch.json` and regenerated via `convert-cards-v15.mjs`; data commits carry `INFRA:` prefix (no `packages/`/`apps/` files). Baseline 588 / 0 preserved. See `EC-110-validate-registry-ci-path-fix.checklist.md`. | Done |

---

## Rules

- EC status tracks **independently** from WP completion status in `WORK_INDEX.md`
- `Draft` means the EC is generated and structurally correct
- Move to `Done` **only** when the corresponding WP is completed in `WORK_INDEX.md`
- `Blocked` applies when a WP's dependencies are known to be unmet
- This index is subordinate to `WORK_INDEX.md` — it does **not** determine execution order
- **All bug handling must follow** `docs/ai/REFERENCE/01.2-bug-handling-under-ec-mode.md` once any EC is active

---

**Last updated:** 2026-04-23 (EC-040 executed at commit `6faaf3b` — WP-040 Growth Governance & Change Budget lands the growth-governance framework as a Contract-Only + Documentation bundle: one new reader-facing prose document (`docs/governance/CHANGE_GOVERNANCE.md`), one new types file (`packages/game-engine/src/governance/governance.types.ts` under D-4001 — ninth engine subdirectory classification) exporting three metadata types (`ChangeCategory`, `ChangeBudget`, `ChangeClassification`; all fields `readonly`), two additive re-export edits (`types.ts` + `index.ts`), one new 01.6 post-mortem. Four-commit topology: A0a SPEC precedent-land (`a6be850`) → A0 SPEC pre-flight bundle + D-4001 (`5e1a0fa`) → A `EC-040:` content + post-mortem (`6faaf3b`) → B SPEC governance close (this commit — prepends STATUS.md Phase 7 closure, checks off WP-040, flips EC-040 Draft → Done, lands D-4002 / D-4003 / D-4004 back-pointer DECISIONS.md entries per P6-51 form (2) + three matching DECISIONS_INDEX rows). Five categories map one-to-one to the five-layer partition per D-4002; five immutable surfaces require major version bump + migration path + DECISIONS.md entry per D-4004 / D-1002; CONTENT + UI primary growth vectors per D-4003 / D-1003; ENGINE restricted with architecture review; RULES secondary at-most-1-per-release under simulation (D-0702). Path A reuses landed version axes (D-0801) and landed ops surfaces (D-3501) via cross-link rather than parallel types. All 17 Verification Steps pass on first run — zero mid-execution fixes, zero grep-guard trips, zero forbidden-token enumeration in `CHANGE_GOVERNANCE.md`. Test baseline UNCHANGED at engine 444/110/0 + repo-wide 596/0 (zero new tests; RS-2 lock honored). 01.5 NOT INVOKED. 01.7 Copilot Check 27/30 PASS with 3 scope-neutral RISK FIXes (Issues 4 / 5 / 26 — versionImpact axis-mapping table, `exactOptionalPropertyTypes` omit-don't-undefined pattern, category-to-layer mapping table) folded into session-prompt §Locked Values and applied verbatim. 01.6 post-mortem mandatory triggers fired (new long-lived abstraction document + new code-category directory + new type contracts). Commit prefix `EC-040:` at execution; `SPEC:` on precedent-land, pre-flight bundle, and governance close (never `WP-040:` per P6-36). Vision trailer `Vision: §5, §13, §14, §22, §24`. Summary counts: Done 16 → 17 / Draft 48 → 47 / Total 83 (unchanged). **Unblocks WP-041** (System Architecture Definition & Authority Model). Prior EC-085 execution close footer retained below as historical context — EC-085 **registered** pre-execution — WP-085 Vision Alignment Audit (Detection, Classification & Gating) pre-execution checklist added under Phase 7 Draft; EC-TEMPLATE-conformant at exactly 60 content lines; pairs with WP-085 draft at commit `2e88aa7` and D-8501 queued-instrument decision. Registration is governance-only: no execution claim, no file change under `scripts/audit/vision/`, `docs/audits/`, or any engine / registry / server / UI path. Commit A execution remains gated on a separate EC-085 session with its own pre-flight artifact. Summary counts: Done 10 / Draft 49 → 50; Total 59 → 60. Prior EC-037 execution close footer retained below as historical context — EC-037 executed at commit `160d9b9` — WP-037 Public Beta Strategy lands the controlled-public-beta pillar as a Contract-Only + Documentation bundle: `packages/game-engine/src/beta/beta.types.ts` new under D-3701 (10th engine subdirectory classification); `packages/game-engine/src/types.ts` + `index.ts` gain additive re-export blocks only; `docs/beta/BETA_STRATEGY.md` + `docs/beta/BETA_EXIT_CRITERIA.md` new strategy-document pair. Three JSON-serializable type contracts (`BetaFeedback`, `BetaCohort`, `FeedbackCategory`) + two strategy docs locking the four binary pass/fail exit categories (Rules correctness / UX clarity / Balance perception / Stability). Test baseline UNCHANGED at engine 444/110/0 and repo-wide 596/0 — RS-2 zero-new-tests lock honored; 18 verification steps all pass. Beta runs the same deterministic engine as production (no "beta mode"), uses the same release gates as production (no shortcuts), and inherits all rollback capabilities from WP-035 / D-0902. 01.5 NOT INVOKED; 01.7 Copilot Check 30/30 CONFIRM; 01.6 post-mortem mandatory and complete at `docs/ai/post-mortems/01.6-WP-037-public-beta-strategy.md`. Three commits — A0 `a4f5574` SPEC pre-flight (D-3701 + 02-CODE-CATEGORIES.md), A `160d9b9` EC-037 code + post-mortem, B (this commit) SPEC governance close with three DoD decisions D-3702 / D-3703 / D-3704. Commit-body Vision trailer: `§3, §4, §5, §13, §14, §18, §22, §24, NG-1, NG-3`. Unblocks WP-038 (launch readiness) and WP-039 (post-launch metrics / live ops). Prior EC-106 execution close footer retained below as historical context — EC-106 executed at commit `412a31c` — WP-060 Keyword & Rule Glossary Data Migration lands the registry-viewer's first content-class fetch migration that is not themes: two new bare-array JSONs under `data/metadata/` (`keywords-full.json` with 113 `{ key, description }` entries at 22,867 bytes; `rules-full.json` with 20 `{ key, label, summary }` entries at 4,302 bytes; both alphabetical by `key` via `localeCompare`; token markup `[icon:X]`, `[hc:X]`, `[keyword:N]`, `[rule:N]`, smart quotes `“ ”`, em dash `—` all preserved verbatim), both uploaded to `images.barefootbetters.com/metadata/` (operator-step; HTTP 200 confirmed with matching Content-Length on both URLs before Commit A landed). New `apps/registry-viewer/src/lib/glossaryClient.ts` mirrors `themeClient.ts:49–113` structure verbatim: module-scope `_keywordPromise` / `_rulePromise` singleton cache; two IIFE-style accessors (`getKeywordGlossary(metadataBaseUrl)` / `getRuleGlossary(metadataBaseUrl)`) with early-return on cached promise; `devLog("glossary", ...)` instrumentation on entry + complete + failed; `throw` inside the IIFE on HTTP !ok so `App.vue` can `console.warn` + continue (non-blocking at the boundary); `resetGlossaries()` nullifies both promises for tests. `useRules.ts` Map bodies removed (`KEYWORD_GLOSSARY` / `RULES_GLOSSARY` `export const` declarations deleted entire); replaced with module-scope `_keywordGlossary` / `_ruleGlossary` holders populated by new exported `setGlossaries(keywords, rules)` setter called from `App.vue` after both fetches resolve. New exported `getKeywordGlossaryMap()` / `getRuleGlossaryMap()` getters added so `useGlossary.ts` can iterate the installed Maps (returns `null` before the fetch resolves — callers null-guard). **Algorithm preservation lock (hard):** `lookupKeyword` body (exact-lowercase → space-hyphen-stripped → prefix / suffix / substring matcher with longest-match tracking) and `lookupRule` body (exact + slugified fallback iterator) preserved byte-for-byte with only the `KEYWORD_GLOSSARY` → `_keywordGlossary` / `RULES_GLOSSARY` → `_ruleGlossary` identifier substitution and a one-line `null`-guard at each function top. Every existing `// why:` comment preserved verbatim (`exact match covers most tokens`, `some keywords have hyphens/spaces stripped`, `card data uses parameterized forms`). `HERO_CLASS_GLOSSARY` (5 entries), `RuleEntry`, `parseAbilityText`, `lookupHeroClass`, `AbilityToken`, `TokenType` preserved verbatim per D-6005. Algorithm preservation confirmed by Verification Step 10 (branch markers grep ≥ 3; actual 4) and manual smoke 13c (all seven modifier keywords — "Ultimate Abomination", "Double Striker", "Triple Empowered", "Focus 2", "Patrol the Bank", "Danger Sense 3", "Cross-Dimensional Hulk Rampage" — returned their correct tooltip text). `useGlossary.ts` scope expansion authorized under the viewer analog of `docs/ai/REFERENCE/01.5-runtime-wiring-allowance.md`: `allEntries` converted from module-eval `const` to reactive `ref<GlossaryEntry[]>([])`; new exported `rebuildGlossaryEntries()` called once from `App.vue` after the async fetch resolves; `buildAllEntries()` retargeted to read via `getKeywordGlossaryMap()` / `getRuleGlossaryMap()` + `null`-guards; dedup check (`// why: some keywords appear twice in the map (villainousweapons). Skip duplicates.`) preserved verbatim. `App.vue` `onMounted` try block gains a glossary-load block parallel to the `getThemes()` pattern: `Promise.all([getKeywordGlossary(), getRuleGlossary()])` → `setGlossaries()` → `rebuildGlossaryEntries()`; `catch` `console.warn("[Glossary] Load failed (non-blocking):", ...)` + continue. Three new imports added. `apps/registry-viewer/src/lib/devLog.ts` `Category` union extended with `"glossary"` — one-line amendment authorized by EC-106 §Out of Scope note (required so the `devLog` calls in `glossaryClient.ts` typecheck against the strict union; 10th file in Commit A's allowlist rather than 9). `apps/registry-viewer/CLAUDE.md` updated: Architecture & Data Flow block gains `getKeywordGlossary()` + `getRuleGlossary()` sections alongside `getThemes()`; Key Files table gains `glossaryClient.ts` + `useGlossary.ts` rows; "Keyword & Rule Glossary" section rewritten from hardcoded-Maps narrative to R2-fetched flow. `docs/03.1-DATA-SOURCES.md` §Registry Metadata Files table gains two new rows matching the `sets.json` / `card-types.json` shape. Seven new `docs/ai/DECISIONS.md` entries (D-6001 display-only status / no Zod; D-6002 `data/metadata/` co-location; D-6003 R2 hosting alongside cards and themes; D-6004 `[keyword:N]` render-time resolution via `parseAbilityText`; D-6005 hero-class descriptions stay hardcoded; D-6006 canonical baseline is the in-repo 113/20 Maps, not upstream 102/18 `modern-master-strike` JSON; D-6007 `useGlossary.ts` wiring scope expansion + lookup algorithm lock). Test baselines UNCHANGED: repo-wide `588 passing / 0 failing`; engine `436 / 109 / 0 fail`; registry `13 / 2 / 0`; vue-sfc-loader `11 / 0 / 0`; server `6 / 2 / 0`; replay-producer `4 / 2 / 0`; preplan `52 / 7 / 0`; arena-client `66 / 0 / 0` (WP-060 is data + viewer-wiring class; RS-2 holds with zero new tests). Layer-boundary integrity: zero `packages/game-engine/` imports (P6-22 escaped-dot grep confirmed), zero `boardgame.io` imports, zero `preplan` / `server` / `pg` / `registry` runtime imports in any touched file, zero `require()` calls, zero `.reduce()` in migration output or lookup bodies. No new test infrastructure. `package.json` / `pnpm-lock.yaml` untouched (P6-44). R2 upload verified via `curl -sI` HEAD returning HTTP 200 with matching Content-Length (22867 / 4302) before Commit A landed (§Verification Step 11); manual DEV smoke 13a–13g and PROD smoke 14a–14c all passed, including the critical 13c modifier-keyword regression check (byte-for-byte `lookupKeyword` preservation confirmed end-to-end) and the 13g negative test (bad `metadataBaseUrl` produces `console.warn` + card view still renders). 01.5 NOT INVOKED as an engine-contract clause (no engine surface touched: no `LegendaryGameState` field, no `buildInitialGameState` shape change, no `LegendaryGame.moves` entry, no phase hook); viewer-scope analog cited for `useGlossary.ts` only per D-6007. 01.6 post-mortem NOT TRIGGERED (`glossaryClient.ts` is a new *instance* of the `themeClient.ts` abstraction locked by WP-055, not a new long-lived abstraction type; viewer `src/lib/` is pre-classified; `KeywordGlossary` / `RuleGlossary` aliases are viewer-local with no cross-package consumer; zero engine involvement). Commit prefix `EC-106:` per the seven-row EC-slot retargeting precedent chain (EC-061 → EC-067, EC-066 → EC-068, EC-062 → EC-069, EC-063 → EC-071, EC-080 → EC-072, EC-079 → EC-073, EC-064 → EC-074, **EC-060 → EC-106** — first use of the registry-viewer 101+ series retargeting form; the original `EC-060-keyword-glossary.checklist.md` slot was consumed by commit `6a63b1c` for earlier viewer-scope glossary tooltips + mobile set-pill fix, unrelated to WP-060). `WP-060:` / `EC-060:` rejected by `.githooks/commit-msg` per P6-36. Three commits: `0654a4c` SPEC pre-flight bundle (EC-106 file + EC_INDEX row + WP-060 amendments + pre-flight file + copilot-check 30/30 PASS + session prompt); `412a31c` EC-106 execution (10 files: 2 JSONs + `glossaryClient.ts` + `useRules.ts` + `useGlossary.ts` + `App.vue` + `devLog.ts` + viewer `CLAUDE.md` + `03.1-DATA-SOURCES.md` + DECISIONS.md); this SPEC governance close. Branch `wp-060-keyword-rule-glossary` cut fresh from `0654a4c`. Fresh topic branch isolates WP-060 from the post-Phase-6 governance trunk. Both R2 JSON artifacts served via Cloudflare (`Server: cloudflare`). `stash@{0..2}` intact (quarantine respected); none of the 11 inherited dirty-tree items staged or committed; mystery untracked `docs/ai/ideas/audio-stingers-sketch.md` observed during pre-flight (per WP-030 precedent) flagged in execution summary and NOT touched. Summary counts: Done 5 → 6 / Draft 54 → 53 / Total 59 (unchanged). **Unblocks:** downstream registry-viewer WPs that want to reference glossary data from R2 via a singleton (Phase 5 keyword-union WPs may now validate card data against the R2-served `keywords-full.json` during content authoring without re-embedding the definitions in code). Prior EC-042 execution close footer retained below as historical context — EC-042 executed at commit `c964cf4` — WP-042 Deployment Checklists lands two canonical deployment-pillar verification surfaces plus their cross-reference plumbing: `docs/ai/deployment/r2-data-checklist.md` (full seven-section R2 data verification: §A.1 Validation script usage across local + R2 modes with the six real env vars from `packages/registry/scripts/validate.ts` — `METADATA_DIR`, `SETS_DIR`, `HEALTH_OUT`, `R2_BASE_URL`, `SKIP_IMAGES`, `IMAGE_DELAY_MS` — plus the five-phase pipeline description from the script's own header; §A.2 Registry manifest (`data/metadata/sets.json` — 40 entries); §A.3 Metadata files table with the six locked minimum-entry counts from `registry/scripts/validate.ts` — `sets.json` (40), `card-types.json` (37), `hero-classes.json` (5), `hero-teams.json` (25), `icons-meta.json` (7), `leads.json` (50); §A.4 Image assets naming convention + Phase 5 spot-checks at three URLs per set; §A.5 Cross-reference checks — `alwaysLeads` resolution + duplicate-slug detection; §A.6 R2 bucket configuration — `legendary-images` bucket, CORS `GET`/`HEAD` from `cards.barefootbetters.com` + `localhost:5173`, cache-control `immutable` for images / `max-age=3600` for JSON, `rclone` remote `legendary-r2` verification; §A.7 New set upload procedure as seven ordered steps); `docs/ai/deployment/postgresql-checklist.md` (scope-reduced PostgreSQL checklist with a top-of-file "Deferred sections" pointer citing D-4201 and enumerating §B.3 / §B.4 / §B.5 / §B.8 as awaiting WP-042.1; §B.1 Pre-conditions — Node v22+, `pg` package resolvable in `apps/server`, `DATABASE_URL` points at the target environment, operator-level database-name eye-check since `scripts/migrate.mjs` does not read the `EXPECTED_DB_NAME` env var the session prompt assumed, `psql "$env:DATABASE_URL" -c "select 1"` health probe; §B.2 Migration execution — the three real Foundation Prompt 02 migrations at commit `ac8486b` (`001_server_schema.sql`, `002_seed_rules.sql`, `003_game_sessions.sql`) with expected `pnpm migrate` output on first and subsequent runs + `public.schema_migrations` contents enumeration; §B.6 Rules data seeding verification — `legendary.rules` count > 0 + FK join with `legendary.rule_docs` = zero unmatched rules + representative code lookup `where code = 'shards'` + `rule_docs.search_tsv @@ to_tsquery('english', 'wound')` FTS path + `legendary.source_files` provenance row; §B.7 Schema-structure verification — B.7.1 tables exist (`pg_tables` query with exactly nine expected rows: seven `legendary.*` + `public.game_sessions` + `public.schema_migrations`); B.7.2 columns + types for `legendary.rules` / `legendary.rule_docs` (including the generated `search_tsv tsvector`) / `public.game_sessions`; B.7.3 four FK constraints all `ON DELETE CASCADE`; B.7.4 unique constraints; B.7.5 eight indexes including `rule_docs_search_idx` GIN on the generated `search_tsv`; B.7.6 `set_updated_at()` function + `game_sessions_set_updated_at` trigger). All table references under `legendary.*` (except intentional `public.game_sessions` per D-1201 / D-1202 and `public.schema_migrations` per the migration runner's convention). Cross-reference plumbing: `docs/ai/ARCHITECTURE.md` §Section 2 Server Startup Sequence gains a one-line cross-reference pointing at `docs/ai/deployment/`; `docs/ops/RELEASE_CHECKLIST.md` Gate 2 and §Relationship to runtime invariant checks each gain a back-pointer block citing the R2 and PostgreSQL checklists respectively (first concrete consumer of WP-035's `RELEASE_CHECKLIST.md` back-pointer pattern — integration pattern locked for future pillar WPs to follow). Governance: **D-4202** (P6-51 form-(2) back-pointer for legacy 00.2b §C UI-rendering-layer verification exclusion — UI is a separate layer from Server / Ops per `ARCHITECTURE.md §Layer Boundary (Authoritative)`; UI-implementation volatility makes library-specific deployment checks brittle; `RELEASE_CHECKLIST.md` Gate 5 already covers UI contract correctness at release time). **D-4203** (P6-51 form-(1) discrete entry codifying WP-042 as Documentation-class under Server / Operations layer as a load-bearing invariant — no new runtime code, no new `scripts/` files (explicitly no `scripts/seed-from-r2.mjs`), no `package.json` entries, no new migrations, no new tests, no new npm dependencies. Preserves test-baseline invariance, three-commit topology, and Layer Boundary stability. Future deployment-pillar Documentation WPs may cite as precedent). D-4201 (WP-042 scope reduction) landed at pre-flight commit `cbb6476`. Test baselines: engine UNCHANGED at 436 / 109 / 0 fail; repo-wide UNCHANGED at 526 / 0 fail (WP-042 is documentation class; RS-2 lock holds). Paraphrase discipline per P6-50: `Select-String` over the produced checklists for `Konva|canvas|boardLayout|CARD_TINT|game-engine|boardgame\.io|LegendaryGame|ctx\.|Math\.random|Date\.now|performance\.now|seed-from-r2|pnpm seed` returns zero matches. 01.5 NOT INVOKED (no types added, no moves added, no phase hooks added). 01.6 post-mortem mandatory triggers fired (new long-lived abstractions + first concrete back-pointer-pattern consumer) — verdict **WP COMPLETE** with zero mid-execution fixes; post-mortem §8 documents three reality-reconciliation findings (`SETS_DIR` not `CARDS_DIR`; no `EXPECTED_DB_NAME` env var in the runner; §B.7 verifies the tables the three shipped migrations actually create, not the deferred seed-dependent tables) surfaced during authoring and resolved by documenting reality in the checklist prose rather than documenting the pre-amendment paper-spec values. Follow-up work item **WP-042.1** added to `WORK_INDEX.md` blocked on Foundation Prompt 03 revival — will extend `postgresql-checklist.md` in place by converting the top-of-file "deferred sections" pointer into four full sections once the seed runner and its `package.json` wiring + complementary migrations exist. Both retained stashes (`stash@{0}`, `stash@{1}`) intact; EC-069 `<pending — gatekeeper session>` placeholder NOT backfilled (owned by a separate SPEC session); `pnpm-lock.yaml` absent from diff (no new dep). Three commits: `cbb6476` SPEC pre-flight (D-4201 + WP/EC amendments + session prompt); `c964cf4` EC-042 code + D-4202 + D-4203 + post-mortem; this SPEC governance close. **Unblocks WP-042.1** (deferred PostgreSQL seeding checklist sections) pending Foundation Prompt 03 revival. Prior EC-035 execution close footer retained below as historical context — EC-035 executed at commit `d5935b5` — WP-035 Release, Deployment & Ops Playbook lands the first operational-playbook surface for the project: three new documents under `docs/ops/` (`RELEASE_CHECKLIST.md` with seven binary pass/fail release gates citing D-0602 / D-0801 / D-0802 / D-0902; `DEPLOYMENT_FLOW.md` with the four-environment sequential promotion path + no-hot-patching rule citing D-1002 + four rollback triggers + four rollback rules implementing D-0902 at the deployment boundary; `INCIDENT_RESPONSE.md` with the P0–P3 severity ladder + D-0802 vs D-1234 mapping explanation + four-field incident-record contract) plus the engine-side type surface `packages/game-engine/src/ops/ops.types.ts` under D-3501 (eighth engine subdirectory classification after D-2706 / D-2801 / D-3001 / D-3101 / D-3201 / D-3301 / D-3401) exporting `OpsCounters` (four `readonly number` fields in locked fail-loud-before-fail-soft order: `invariantViolations`, `rejectedTurns`, `replayFailures`, `migrationFailures`), `DeploymentEnvironment` (closed union in promotion order), `IncidentSeverity` (closed union in descending urgency). RS-1 option (a) locked at pre-flight: pure type definitions with no runtime instance anywhere in the engine — the engine does not auto-heal; monitoring is passive; humans decide actions. Additive re-exports in `types.ts` and `index.ts` only — no `LegendaryGameState` shape change, no other engine subdirectory touched, zero new tests (RS-2 lock). Engine baseline UNCHANGED at 436 / 109 / 0 fail; repo-wide 526 / 0 fail. `pnpm-lock.yaml` absent (no new dep); both retained stashes intact. 01.5 NOT INVOKED. 01.6 post-mortem mandatory triggers fired (new long-lived abstraction `OpsCounters` + new code-category directory D-3501) — verdict WP COMPLETE with zero mid-execution fixes (the pre-flight paraphrase-discipline Locked Value prevented the P6-43 collision class that surfaced in WP-034's first verification gate run). Three commits: `4b6b60b` SPEC pre-flight (D-3501 + 02-CODE-CATEGORIES.md + session prompt); `d5935b5` EC-035 code + post-mortem; this SPEC governance close. **Unblocks WP-042** (Deployment Checklists); WP-036 (AI Playtesting) dependency also green. Prior EC-034 execution close footer retained below as historical context — EC-034 executed at commit `5139817` — WP-034 Versioning & Save Migration Strategy lands the first persistence-versioning surface for the engine: `EngineVersion` semver + `DataVersion` / `ContentVersion` integer axes, `VersionedArtifact<T>` wrapper, `checkCompatibility` pure decision function, `migrateArtifact` forward-only with load-boundary throw exception per D-0802, `stampArtifact` save-time embed with the single permitted wall-clock read in the subtree per D-3401 sub-rule. Five new files under `packages/game-engine/src/versioning/` (D-3401 engine code category, seventh instance of the established directory-classification pattern after replay/ui/campaign/invariants/network/content); additive re-exports in `types.ts` and `index.ts` only — no `LegendaryGameState` shape change, no other engine subdirectory touched. MVP `migrationRegistry = Object.freeze({})` is the long-lived seam. 9 new tests in one `describe('versioning (WP-034)')` block per P6-19 / P6-25 suite-count discipline; engine 427→436 (+9; 108→109 suites), repo-wide 517→526 (+9). `pnpm-lock.yaml` absent (P6-44); both retained stashes intact. 01.5 NOT INVOKED. 01.6 post-mortem mandatory triggers fired (new long-lived abstraction `VersionedArtifact<T>` + new code-category directory D-3401) — verdict WP COMPLETE; zero in-allowlist refinements applied during post-mortem. **Meta-finding:** P6-43 (JSDoc + grep collision precedent authored from WP-064 execution and committed at `0c741c6`) caught six initial JSDoc-vs-grep collisions at the first verification gate run; all fixed via paraphrase form before re-test. First empirical demonstration that the precedent log is load-bearing across sessions. Three commits: `c587f74` SPEC pre-flight (D-3401 + 02-CODE-CATEGORIES.md + session prompt); `5139817` EC-034 code + post-mortem; this SPEC governance close. Prior EC-074 execution close footer retained below as historical context — EC-074 executed at commit `76beddc` — WP-064 Game Log & Replay Inspector lands four new client surfaces consuming WP-063's `ReplaySnapshotSequence` artifact: `parseReplayJson` D-6303 site, `<GameLogPanel />` leaf, `<ReplayInspector />` non-leaf with `tabindex="0"` + listeners-on-root keyboard pattern locked as first repo precedent **D-6401**, `<ReplayFileLoader />` non-leaf — promoted from `<script setup>` mid-execution after the same vue-sfc-loader separate-compile failure WP-061 / WP-062 documented. Fixture `three-turn-sample.{json,inputs.json,cmd.txt}` at 8 snapshots, byte-identical regeneration confirmed; arena-client tests 35→66, repo-wide 486→517; engine/registry/vue-sfc-loader/server/replay-producer/registry-viewer all untouched; `pnpm-lock.yaml` absent from diff per P6-44; both inherited stashes preserved. 01.6 post-mortem mandatory triggers fired (new long-lived abstraction + new keyboard precedent) — verdict WP COMPLETE; one in-allowlist refinement applied (`currentLog` spread-copy in `<ReplayInspector />` per WP-028 / D-2802 aliasing-prevention pattern). Prior EC-071 execution close footer retained below as historical context — EC-071 executed at commit `97560b1` with repo-wide tests 464 → 486.)