# EC-113 — Engine-Server Registry Wiring + Validator Alignment (Execution Checklist)

**Source:** [docs/ai/work-packets/WP-113-engine-server-registry-wiring-and-validator-alignment.md](../work-packets/WP-113-engine-server-registry-wiring-and-validator-alignment.md)
**Layer:** Server (`apps/server/`) + Game Engine (`packages/game-engine/`)

## Before Starting
- [ ] WP-100 complete; D-10006 → D-10013 all landed in main
- [ ] Pre-flight 2026-04-27 READY TO EXECUTE (conditional on PS-1..8 — all resolved into this EC + WP-113 body)
- [ ] Q1 LOCKED (PS-3): validator strategy is option (a) — validator delegates to builder-owned slug sources. `CardRegistryReader` widened in-place to `{ listCards, listSets, getSet }`; `EMPTY_REGISTRY` in `game.ts` widened to satisfy.
- [ ] Q2 LOCKED (PS-1): hero-deck builder file path is `packages/game-engine/src/setup/heroAbility.setup.ts`. The "hero-deck builder" is `buildHeroAbilityHooks`; its slug-extractor is `extractHeroSlug` (currently local — to be exported).
- [ ] Q3 LOCKED (PS-4): per-builder diagnostic emission site is **orchestration-side for all four builders** in `packages/game-engine/src/setup/buildInitialGameState.ts`. Forced by builder signatures — none of the four builders receives `G`. The Uniformity Rule (one mode globally) is satisfied by orchestration-only emission. Exported registry-reader guards drive detection.
- [ ] D-10014 reservation noted in DECISIONS.md (entry created at Commit B; pre-promotion drafts may reference it)
- [ ] `pnpm --filter @legendary-arena/game-engine build` exits 0
- [ ] `pnpm --filter @legendary-arena/game-engine test` exits 0 (baseline `524 / 116 / 0`)
- [ ] `pnpm --filter @legendary-arena/server test` exits 0 (baseline `47 / 7 / 0` pass; 16 skipped DB-tests preserved)
- [ ] `pnpm --filter @legendary-arena/arena-client test` exits 0 (baseline `182 / 17 / 0`)
- [ ] PS-8 — re-probe slug collisions at session start; record measured counts (hero/mastermind/villain group/scheme) in the D-10014 entry. Do NOT repeat the WP's stated "11/584 mastermind" figure verbatim — that probe-shape number is unverified at session start. The determinism argument stands on hero collisions alone (23 / 279 verified at pre-flight 2026-04-27).

## Locked Values (do not re-derive)
- Server wiring sequence: `loadRegistry()` resolves → `setRegistryForSetup(registry)` → registry/rules/PAR Promise.all → `Server({ games, origins })` → `server.run({ port })`
- `setRegistryForSetup` import path: `@legendary-arena/game-engine` (top-level export)
- `setRegistryForSetup` registry guard: do NOT remove `if (gameRegistry) { ... }` at `game.ts:201-210` — server wiring is the runtime fix, not the engine guard
- **Set-qualified ID format (LOCKED):** all five entity-ID fields on `MatchSetupConfig` use `<setAbbr>/<slug>`. Bare slugs, display names, and flat-card keys are ALL rejected. Format-error messages name the canonical form + concrete example.
- **Qualified IDs are exclusive (LOCKED):** no fallback / compatibility / lenient / best-effort path for bare slugs, display names, or flat-card keys exists anywhere in the validator or builders. Reviewers reject any PR that introduces such a path regardless of how isolated it appears.
- Empirical collision evidence (2026-04-27 probe): 23 hero slugs, 11 mastermind slugs, 4 villain group slugs, 2 scheme slugs collide across loaded sets. Bare slugs are nondeterministic by construction; qualified format is mandatory regardless of present-day disambiguation.
- `parseQualifiedId(input)` rejects empty / no-slash / multiple-slash / empty-part inputs. Validator emits a format error before existence check on parse failure.
- Validator distinguishes "set not loaded" vs "slug not in that set" in error messages.
- **Authority lock (LOCKED):** the per-builder slug-source helpers are the ONLY authoritative definition of acceptable slug semantics. `matchSetup.validate.ts` MUST NOT independently parse, normalize, or reinterpret slug formats beyond delegating to these helpers (and to `parseQualifiedId` for the surrounding envelope).
- **Slug-source helper classes (PS-2 LOCKED):** "slug-source" semantics are NOT uniform across the four builders — split into two classes:
  - **Class A — slug key-decoders** (decode `{setAbbr}-{cardType}-{slug}-{cardSlug}` flat-card keys):
    - `extractVillainGroupSlug` — promote to export from `villainDeck.setup.ts` (currently local). Type-stable, no signature change.
    - `extractHeroSlug` — promote to export from `setup/heroAbility.setup.ts` (currently local). Type-stable, no signature change.
  - **Class B — set-data slug iterators** (enumerate `setData.{masterminds|schemes|henchmen}[].slug`):
    - `listMastermindSlugsInSet(setData: unknown): string[]` — new exported helper in `mastermind.setup.ts`.
    - `listSchemeSlugsInSet(setData: unknown): string[]` — new exported helper in `setup/buildSchemeSetupInstructions.ts`.
    - `listHenchmanGroupSlugsInSet(setData: unknown): string[]` — new exported helper in `villainDeck.setup.ts` (henchmen are co-located with villain-deck construction; preserve current location). Each returns `[]` on a malformed `setData` shape — no throws.
- **Registry-reader guard exports (PS-4 LOCKED):** the four type guards must be promoted to named exports for orchestration-side use:
  - `isVillainDeckRegistryReader` — export from `villainDeck.setup.ts`.
  - `isMastermindRegistryReader` — export from `mastermind.setup.ts`.
  - `isSchemeRegistryReader` — export from `setup/buildSchemeSetupInstructions.ts`.
  - `isHeroAbilityRegistryReader` — export from `setup/heroAbility.setup.ts`.
- **`CardRegistryReader` widening (PS-3 LOCKED):** widen the existing `CardRegistryReader` interface in `matchSetup.validate.ts` in-place (Option (i)) to `{ listCards(), listSets(), getSet(abbr): unknown }`. `EMPTY_REGISTRY` in `game.ts:56-58` must add `listSets: () => []` and `getSet: () => undefined`. Real `CardRegistry` already satisfies the wider shape structurally (verified — `VillainDeckRegistryReader` reads the same shape).
- **Builder filtering order (LOCKED, hard constraint):** parse `<setAbbr>/<slug>` → filter cards by `setAbbr` first → match by `<slug>` within that set's cards only. Builders that parse the qualified ID but match across all sets (e.g., global `findCardBySlug` after parse) are in violation of the determinism contract. Reviewers reject regardless of test pass/fail.
- **Builder internal-iterator updates (PS-7 LOCKED):** the existing internal iterators that consume bare slugs from `MatchSetupConfig` today must be updated to accept `(setAbbr, slug)` and iterate ONLY the named set's data. Affected functions:
  - `villainDeck.setup.ts`: `filterVillainCardsByGroupSlug` (filter by `setAbbr` first, then group slug); `findHenchmanGroupSlug` (constrain to named set); `findSchemeSlug` (constrain to named set).
  - `mastermind.setup.ts`: `findMastermindCards` (constrain to named set).
  - `buildSchemeSetupInstructions.ts`: any internal scheme lookup (constrain to named set).
  - `heroAbility.setup.ts`: hero-card filter inside `buildHeroAbilityHooks` (filter by `setAbbr` first, then hero slug from `extractHeroSlug`).
  - `economy/economy.logic.ts` (mid-execution amendment, 2026-04-27): `buildCardStats()` parses set-qualified IDs and filters by `setAbbr` first, slug second, for hero / villain / henchman cardStats derivation. Three iterators at lines 179, 195, 224. Discovered post-EC-promotion; included to satisfy the WP's locked acceptance criterion "G.cardStats populated for the chosen cards." Authorized inline per D-3103 precedent.
  Each modification is signature-stable at the builder's top-level entry — only internal helpers change.
- **Diagnostic emission site (Q3, PS-4 LOCKED):** orchestration-side, in `setup/buildInitialGameState.ts`, for ALL FOUR builders. None of the four builders receives `G` — emission inside the builder is structurally impossible without a forbidden signature change. The orchestration site builds a local `setupMessages: string[]` accumulator BEFORE constructing `baseState`; runs each exported `isXRegistryReader` guard against the registry; on `false`, pushes a full-sentence diagnostic naming (a) which builder was skipped, (b) why (registry-reader interface incomplete / registry not injected by server), (c) how to fix (verify `setRegistryForSetup(registry)` was called at server startup OR that the test mock implements the full reader interface). Final assignment: `baseState.messages = setupMessages`. Builders' internal `isXRegistryReader → empty` paths remain unchanged for defense-in-depth.
- **Uniformity rule (LOCKED):** all four setup builders MUST use the SAME failure-reporting *mode*. PS-4 picks `G.messages` diagnostics globally (the only feasible mode given builder signatures). Mixing modes across helpers is forbidden; mixing within one helper is also forbidden.
- 01.5 NOT INVOKED: no new `LegendaryGameState` field, no `buildInitialGameState` shape change, no new `LegendaryGame.moves` entry, no new phase hook. Verified four times: (a) `G.messages` is preexisting, (b) `baseState` shape unchanged (only the `messages` field's content differs from `[]` to a populated accumulator), (c) `LegendaryGame.moves` untouched, (d) no phase hook added.

## Guardrails
- **Server change is minimal diff (PS-5):** `import { setRegistryForSetup }` addition + destructure rename in `Promise.all` (currently `[, , parGate]`; becomes `[registry, , parGate]` to capture the resolved registry) + `setRegistryForSetup(registry)` call site immediately after `await Promise.all(...)` + `// why:` comment. No `startServer()` structural reorganization, no Promise.all sequencing change, no other server file modified.
- Validator option (a) only: validator consumes builder-owned slug-source helpers (Class A + Class B per Locked Values). Pivot to option (b) requires WP body amendment + new pre-flight.
- `// why:` comments referencing D-10014 land in the SAME commit set as the D-10014 entry (Commit B).
- Existing test fixtures using flat-card keys for slug fields are updated, NOT preserved as drift sources. Each fixture change documented in test JSDoc with D-10014 reference.
- **`buildKnownExtIds` deprecation marker (Finding 24):** the existing `buildKnownExtIds` function in `matchSetup.validate.ts` becomes consumer-less after WP-113 (no entity-ID field uses flat-card-key validation any more). Retain in this WP for one release cycle as a forensic-debugging aid; add a `// why:` comment citing D-10014 + "deprecated; remove in follow-up cleanup WP — no current consumers." Do NOT remove the function in this WP.
- **`MatchSetupConfig` field-doc update (Finding 10):** add a one-line JSDoc on each of the five entity-ID fields in `matchSetup.types.ts` (or wherever the five fields are declared) documenting the locked `<setAbbr>/<slug>` format and citing D-10014. Documentation-only change inside an in-scope file; if the field type lives in a separate file not currently in scope, add that file to §Files to Produce.
- **Validator structural split (Finding 30):** preserve the existing `// --- Shape validation: ... ---` comment-block sub-structure in `validateMatchSetup`. As the per-field qualified-ID validation grows, extract `validateShape(input, errors)` and `validateExistence(input, registry, errors)` into named sub-functions inside `matchSetup.validate.ts` to stay under the 30-line limit per `00.6 §5`. No new abstractions — same conventions already partially present.
- No changes to `apps/arena-client/`, `LegendaryGame.moves`, lobby phase config, or any UI surface.
- **Hard cap: 17 modified-or-created files** (raised from 16 → 17 mid-execution per the WP §Mid-Execution Amendment block to accommodate the `economy.logic.ts` PS-7 spec-gap fix; authorized inline per D-3103 precedent). PS-1..8 grew scope from 12 to ~14-15; the amendment adds one more file. Anything beyond 17 is scope creep — STOP and escalate.

## Required `// why:` Comments
- `apps/server/src/server.mjs` `setRegistryForSetup(registry)` call site: D-10014 + WP-100 smoke-test discovery
- `packages/game-engine/src/matchSetup.validate.ts` per-field validation loop: D-10014 + slug-vs-key contract
- `packages/game-engine/src/matchSetup.validate.ts` `CardRegistryReader` widening (PS-3): D-10014 + "validator needs `listSets`/`getSet` to build per-field qualified-ID sets"
- `packages/game-engine/src/matchSetup.validate.ts` `buildKnownExtIds` deprecation marker (Finding 24): D-10014 + "deprecated; remove in follow-up cleanup WP — no current consumers"
- `packages/game-engine/src/game.ts` `EMPTY_REGISTRY` widening (PS-3): D-10014 + "satisfies the wider CardRegistryReader for test-context skip path"
- Each promoted Class A slug key-decoder export site (`extractVillainGroupSlug` in `villainDeck.setup.ts`, `extractHeroSlug` in `setup/heroAbility.setup.ts`): D-10014 + "single source of truth — flat-card-key decoder"
- Each new Class B set-data slug iterator export site (`listMastermindSlugsInSet`, `listSchemeSlugsInSet`, `listHenchmanGroupSlugsInSet`): D-10014 + "single source of truth — set-data slug enumerator"
- Each promoted registry-reader guard export site (4 guards in 4 files): D-10014 + "orchestration-side diagnostic detection seam"
- Single orchestration-side diagnostic emission block in `setup/buildInitialGameState.ts` (Q3 LOCKED orchestration-only per PS-4): D-10014 + "Uniformity Rule + builder signatures don't accept G + remediation pointer to setRegistryForSetup"
- Each `MatchSetupConfig` entity-ID field JSDoc (Finding 10, 5 fields): D-10014 + locked `<setAbbr>/<slug>` format
- Each builder-internal-iterator update site (PS-7): D-10014 + "Builder Filtering Order — iterate named set only"

## Files to Produce (executed in this order)
1. `packages/game-engine/src/villainDeck/villainDeck.setup.ts` — **modified** — promote `extractVillainGroupSlug` (Class A) + `isVillainDeckRegistryReader` (guard) to exports; add new exported `listHenchmanGroupSlugsInSet` (Class B); update `filterVillainCardsByGroupSlug` and `findHenchmanGroupSlug` and `findSchemeSlug` to accept `(setAbbr, slug)` and iterate the named set only (PS-7)
2. `packages/game-engine/src/mastermind/mastermind.setup.ts` — **modified** — promote `isMastermindRegistryReader` (guard) to export; add new exported `listMastermindSlugsInSet` (Class B); update `findMastermindCards` to accept `(setAbbr, slug)` and iterate the named set only (PS-7)
3. `packages/game-engine/src/setup/buildSchemeSetupInstructions.ts` — **modified** — promote `isSchemeRegistryReader` (guard) to export; add new exported `listSchemeSlugsInSet` (Class B); update internal scheme lookup (if any added) to accept `(setAbbr, slug)` (PS-7)
4. `packages/game-engine/src/setup/heroAbility.setup.ts` — **modified — Q2 LOCKED** — promote `extractHeroSlug` (Class A) + `isHeroAbilityRegistryReader` (guard) to exports; update hero-card filter inside `buildHeroAbilityHooks` to filter by `setAbbr` first then hero slug (PS-7)
5. `packages/game-engine/src/matchSetup.validate.ts` — **modified** — widen `CardRegistryReader` to `{ listCards, listSets, getSet }` (PS-3); add `parseQualifiedId(input)`; add five `buildKnownXxxQualifiedIds` helpers consuming Class A + Class B slug sources; restructure per-field validation loops (format-error before existence check); preserve `buildKnownExtIds` with deprecation `// why:` (Finding 24); structural sub-function split if length grows (Finding 30)
6. `packages/game-engine/src/matchSetup.types.ts` — **modified — Finding 10** — add JSDoc to each of the five entity-ID fields documenting `<setAbbr>/<slug>` format + D-10014 reference. (If field declarations live in a different file, add THAT file to this list and renumber.)
7. `packages/game-engine/src/game.ts` — **modified — PS-3** — widen `EMPTY_REGISTRY` to satisfy widened `CardRegistryReader` (`listSets: () => []`, `getSet: () => undefined`); add `// why:` per Required `// why:` Comments above. Do NOT modify the `gameRegistry` guard at lines 201-210.
8. `packages/game-engine/src/setup/buildInitialGameState.ts` — **modified — PS-4 / Q3 LOCKED** — orchestration-side diagnostic accumulator: build `setupMessages: string[]` BEFORE `baseState`; run each of the four exported `isXRegistryReader` guards; on `false`, push full-sentence diagnostic; assign `baseState.messages = setupMessages` (replacing the `messages: []` literal). Preserve all other `baseState` fields and ordering.
9. `packages/game-engine/src/matchSetup.validate.test.ts` — **modified** — ≥25 new field validation tests (5 fields × accept-qualified + reject-bare-slug + reject-display-name + reject-flat-card-key + reject-cross-set-collision) + ≥5 parse-error tests + fixture migrations (each documented in test JSDoc with D-10014 reference)
10. `packages/game-engine/src/{villainDeck,mastermind,rules}/*setup.test.ts` (3 files) — **modified — fixture migration only** — `villainDeck.setup.test.ts`, `mastermind.setup.test.ts`, and `rules/heroAbility.setup.test.ts` carry the bare-slug → set-qualified fixture migrations per the qualified-ID contract; each change is documented in test JSDoc with a D-10014 reference. **Diagnostic-presence regression tests for all four builders are consolidated into `setup/buildInitialGameState.loadout.test.ts` (item 11)** — co-located with the orchestration-site code-under-test where the diagnostics actually emit. Same four-builder coverage matrix; single test file. No `buildSchemeSetupInstructions.test.ts` is modified or created. (Originally specced under this item as 4 builder test files split one-per-builder; consolidation reflected here per pre-commit review Nit B; matches post-mortem §10.)
11. `packages/game-engine/src/setup/buildInitialGameState.loadout.test.ts` — **new** — end-to-end loadout integration test (non-empty G across all four subsystems + zero "skipped" diagnostics; substring match on each of the four diagnostic prefixes to assert NONE appear with a real registry). **Mid-execution amendment: also asserts (a) `Object.keys(G.cardStats).length > 0` with expected hero/villain/henchman extIds present (POSITIVE), and (b) no "buildCardStats" / "cardStats empty" diagnostic in `G.messages` (NEGATIVE). Locks the buildCardStats spec-gap fix as a permanent regression guard.**

11a. `packages/game-engine/src/economy/economy.logic.ts` — **modified — mid-execution amendment** — PS-7 qualified-ID parsing for `buildCardStats()`. Three iterators at lines 179, 195, 224 (heroDeckIds, villainGroupIds, henchmanGroupIds). Filter `setAbbr` first, slug second; internal helpers (`filterHeroCardsByDeckSlug`, `findVillainGroupCards`, `findHenchmanGroupVAttack`) take `(setAbbr, slug)`. Local `parseQualifiedIdForSetup` helper duplicated from validator (NOT imported — keeps economy.logic.ts free of validator coupling). Graceful-skip on parse failure (validator is the authoritative format-error reporter).

11b. `packages/game-engine/src/economy/economy.logic.test.ts` — **modified — mid-execution amendment** — regression tests for qualified-ID `buildCardStats()` (≥3 tests: accept-qualified populates stats; reject-bare-slug yields empty; reject-cross-set-collision filters correctly).
12. `apps/server/src/server.mjs` — **modified — PS-5 minimal diff** — `import { setRegistryForSetup }` from `@legendary-arena/game-engine`; rename `Promise.all` destructure from `[, , parGate]` to `[registry, , parGate]`; call `setRegistryForSetup(registry)` immediately after `await Promise.all(...)`; add `// why:` comment per Required `// why:` Comments above. No other modifications to `server.mjs`.
13. `apps/server/src/server.mjs.test.ts` — **new or modified** — wiring-ordering test (asserts `setRegistryForSetup` was called during `startServer()` execution and prior to the function returning); use `mock.module` or a re-exported test seam to spy the call; do NOT require deep boardgame.io constructor spying

**Sequencing rule:** steps 1-4 (extractor/guard/iterator exports) MUST complete before step 5 (validator imports them). Step 5 MUST complete before step 8 (orchestration imports the guards). Step 8 MUST complete before step 11 (loadout test asserts orchestration behavior). Steps 12-13 are independent of steps 1-11 and may be done in parallel; they MUST land in the same commit set as Commit A per `01.3-commit-hygiene-under-ec-mode.md` to avoid ship-broken-engine-with-disconnected-server states.

## After Completing
- [ ] `pnpm --filter @legendary-arena/game-engine build` exits 0
- [ ] `pnpm --filter @legendary-arena/game-engine test` exits 0; baseline `524 → ~555 / 117 / 0` (+~31 tests, +1 suite per PS-6 — ≥25 new field-validation tests + ≥5 parse-error tests + 4 diagnostic-presence regression tests + 1 loadout integration test = ≥35; absolute minimum is 524+35=559, the ~555 figure assumes some fixture-migration tests that already counted as separate tests are folded). The exact final count is whatever the executor measures; the floor is 524+35.
- [ ] `pnpm --filter @legendary-arena/server test` exits 0; baseline `47 → ~48 / 7 / 0` pass (+1 wiring test); 16 skipped DB-tests preserved
- [ ] `pnpm --filter @legendary-arena/arena-client test` exits 0; baseline `182 / 17 / 0` UNCHANGED
- [ ] `pnpm -r build` exits 0
- [ ] Manual smoke test (`scripts/Start-SmokeTest.ps1 -KillStaleListeners`), **bare-slug case**: match creation FAILS with a validator format error naming the qualified-ID form + an example. (Successful match creation here is a contract violation, NOT an acceptable outcome.)
- [ ] Manual smoke test, **set-qualified case**: match creation succeeds; clicking Reveal in `start` produces a villain or henchman in the City; fightVillain defeats the target; fightMastermind decrements tactics
- [ ] `git diff --name-only -- apps/arena-client packages/registry` returns no output
- [ ] `docs/ai/STATUS.md` updated — match creation now produces non-empty matches end-to-end
- [ ] `docs/ai/DECISIONS.md` updated — D-10014 (engine-server registry wiring + slug-vs-key contract per field, with per-builder diagnostic emission sites)
- [ ] `docs/ai/work-packets/WORK_INDEX.md` WP-113 row Done with date
- [ ] `docs/ai/execution-checklists/EC_INDEX.md` EC-113 row Draft → Done with date
- [ ] 01.6 post-mortem at `docs/ai/post-mortems/01.6-WP-113-engine-server-registry-wiring-and-validator-alignment.md` (mandatory triggers: new contract surface — slug-set helpers; new code seam — server registry wiring; new long-lived abstraction — setup-diagnostic surfacing pattern)

## Common Failure Smells
- "Validator now rejects every loadout" → step 5 ran before steps 1-4 (extractors/guards/iterators not yet exported). Re-order; the 13-step sequence is load-bearing.
- "Validator can't import `listSets`" → step 5 ran before step 7 (`game.ts` `EMPTY_REGISTRY` widening). Or `CardRegistryReader` widening was skipped. Per PS-3, the widening is in step 5 itself; verify the interface declaration.
- Diagnostic appears in `G.messages` on every match (even with registry wired) → diagnostic emission ran unconditionally instead of inside the `isXRegistryReader === false` branch at the orchestration site. Loadout integration test catches this. Per PS-4, the orchestration-side push only fires when the guard returns false.
- arena-client test count drifted → WP-113 touched a UI file. STOP and revert; this WP is engine + server only.
- Bare slug / display name / flat-card key fixture rejected by validator → **expected.** Update fixture to `<setAbbr>/<slug>` qualified format per D-10014 and document each migration in test JSDoc. Do NOT add the bare slug to a fallback list to "make the test pass."
- Validator returns "set 'wwhk' not loaded" or "format invalid: expected `<setAbbr>/<slug>`, got 'black-widow'" → expected when fixture omits the `<setAbbr>/` prefix. Format error fires before existence check on parse failure; "set not loaded" fires before "slug not in that set" — the two error kinds carry different remediations.
- "Cannot read properties of undefined (G.messages)" inside a builder → builder shouldn't be touching `G.messages` at all. Per Q3 LOCKED orchestration-side, all four diagnostic pushes live in `setup/buildInitialGameState.ts`. If you find yourself adding a `messages` parameter to a builder, **STOP — that's a forbidden signature change.**
- Two helpers throw, two helpers push to `G.messages` → **uniformity violation.** Per Q3 LOCKED, all four orchestration-side pushes use the SAME pattern (export guard, run guard at orchestration, push diagnostic on `false`). Tests asserting "diagnostic present" should fail in exactly the same way for all four guards, or fail in exactly the same way for none.
- Validator parses `<setAbbr>/<slug>` with its own ad-hoc `split('/')` instead of calling `parseQualifiedId`, OR re-implements bare-slug normalization → **authority lock violation.** Validator delegates to `parseQualifiedId` + builder-owned slug-source helpers (Class A key-decoders + Class B set-data iterators); it does not own slug grammar.
- A "lenient mode" / "legacy compatibility" / "best-effort" / "try-bare-slug-as-fallback" branch appears anywhere in validator or builders → **exclusivity violation.** No fallback paths exist; bare-slug input is a hard reject. Delete the branch; do not gate it behind a flag.
- "I'd like to add `extractMastermindSlug` / `extractSchemeSlug` for symmetry with `extractVillainGroupSlug`" → **STOP — read PS-2.** Mastermind and scheme slugs are NOT decoded from flat-card keys; they are matched against `setData.{masterminds|schemes}[].slug` directly. The Class B helpers (`listMastermindSlugsInSet`, `listSchemeSlugsInSet`) are the right shape — they enumerate set-data slugs. Inventing a fictitious key-decoder is contract drift.
- Engine test count grew by ~10 instead of ~30 → executor wrote ≤10 tests where ≥30 are required. Per PS-6, the floor is 524+35=559 engine tests. Audit the per-field validation suite: each of 5 fields needs ≥5 tests (accept + reject-bare + reject-display + reject-flat + reject-cross-set-collision); the parse-error suite needs ≥5 tests; per-builder regression needs 4 tests; loadout integration needs ≥1.
- New file `packages/game-engine/src/setup/<heroDeckBuilder>.ts` appears in git status → **STOP — Q2 LOCKED.** The hero-deck "builder" is `setup/heroAbility.setup.ts`'s `buildHeroAbilityHooks`. There is no separate hero-deck-construction file. Delete the new file; modify `setup/heroAbility.setup.ts` instead.
- WP-100 retrospective JSON fixture (`docs/ai/invocations/session-wp100-*.md`) appears in git diff → out of scope per WP-113 §Files Expected to Change retrospective note. Do NOT update WP-100 spec; the smoke-test artifact is scratchpad-by-default and self-correcting.
