# EC-058 — Pre-Plan Disruption Pipeline (Execution Checklist)

**Source:** docs/ai/work-packets/WP-058-preplan-disruption-pipeline.md
**Layer:** Pre-Planning (Non-Authoritative, Per-Client) — `preplan` code category (D-5601, Status: Immutable)
**Pre-Flight:** docs/ai/invocations/preflight-wp058-preplan-disruption-pipeline.md (verdict: conditional READY; PS-1/2/3 resolved by the same Commit A0 `SPEC:` bundle that lands this EC + copilot-check HOLD fixes applied in-place)

## Before Starting
- [ ] WP-056 complete — executed 2026-04-20 at commit `eade2d0` (types surface shipped; `PrePlan` / `PrePlanSandboxState` / `RevealRecord` / `PrePlanStep` exported from `packages/preplan/src/index.ts`; `invalidationReason.effectType` closed union at `preplan.types.ts:108`)
- [ ] WP-057 complete — executed 2026-04-20 at commit `8a324f0` (governance close `7414656`); preplan baseline `23 / 4 / 0`; `PREPLAN_STATUS_VALUES` + drift test shipped at `packages/preplan/src/preplanStatus.ts`; spread-copy 42/42 discipline established by `speculativeOperations.ts`
- [ ] WP-008B complete — `CORE_MOVE_NAMES` locked (engine does not know preplan exists; WP-058 consumes no engine runtime)
- [ ] `DESIGN-PREPLANNING.md §11` (disruption pipeline single-workflow constraint) and `DESIGN-CONSTRAINTS-PREPLANNING.md` #3 / #4 / #6 / #7 / #9 / #11 / #12 read — these are WP-058 scope
- [ ] `pnpm -r build` exits 0 at session start
- [ ] `pnpm -r --if-present test` exits 0 — baseline **559 passing / 0 failing** (preplan 23 / engine 436 / registry 13 / vue-sfc-loader 11 / server 6 / replay-producer 4 / arena-client 66)
- [ ] `packages/preplan/src/preplan.types.ts` matches WP-056 spec verbatim — **immutable in this WP**; any perceived need to add a field is scope creep, stop and escalate
- [ ] `packages/preplan/src/preplanStatus.ts`, `speculativePrng.ts`, `preplanSandbox.ts`, `speculativeOperations.ts` match WP-057 spec verbatim — **immutable in this WP**
- [ ] Inherited dirty-tree items from `session-context-wp058.md` lines 162-190 (untracked files + `.claude/worktrees/` + one modified `session-wp079-*.md`) untouched — never `git add .` / `git add -A` (P6-27 / P6-44)
- [ ] Quarantine stashes `stash@{0}` / `stash@{1}` / `stash@{2}` remain intact and are NOT popped

## Locked Values (do not re-derive)
- Five public function signatures (verbatim — from WP-058 §C, §D, §E, §F, §G):
  - `isPrePlanDisrupted(prePlan: PrePlan | null, mutation: PlayerAffectingMutation): boolean`
  - `invalidatePrePlan(prePlan: PrePlan, mutation: PlayerAffectingMutation): PrePlan | null`
  - `computeSourceRestoration(revealLedger: readonly RevealRecord[]): SourceRestoration`
  - `buildDisruptionNotification(invalidatedPlan: PrePlan, mutation: PlayerAffectingMutation): DisruptionNotification`
  - `executeDisruptionPipeline(prePlan: PrePlan, mutation: PlayerAffectingMutation): DisruptionPipelineResult | null`
- Internal helper (verbatim — from WP-058 §F): `buildNotificationMessage(mutation: PlayerAffectingMutation): string` (non-exported)
- Four public types consolidated in `packages/preplan/src/disruption.types.ts` (PS-3 resolution — WP-058 §H source of truth):
  - `PlayerAffectingMutation` — `{ sourcePlayerId: string; affectedPlayerId: string; effectType: 'discard' | 'ko' | 'gain' | 'other'; effectDescription: string; affectedCardExtId?: CardExtId }`
  - `DisruptionNotification` — `{ prePlanId: string; affectedPlayerId: string; sourcePlayerId: string; message: string; affectedCardExtId?: CardExtId }`
  - `SourceRestoration` — `{ playerDeckReturns: CardExtId[]; sharedSourceReturns: Record<string, CardExtId[]> }`
  - `DisruptionPipelineResult` — `{ invalidatedPlan: PrePlan; sourceRestoration: SourceRestoration; notification: DisruptionNotification; requiresImmediateNotification: true }`
- `PREPLAN_EFFECT_TYPES` canonical readonly array (PS-2, deferred from WP-056 per `preplan.types.ts:101-106` JSDoc): `export const PREPLAN_EFFECT_TYPES = ['discard', 'ko', 'gain', 'other'] as const;` + `export type PrePlanEffectType = typeof PREPLAN_EFFECT_TYPES[number];` lives in new file `packages/preplan/src/preplanEffectTypes.ts`. Compile-time drift check uses `NonNullable<PrePlan['invalidationReason']>['effectType']` because `invalidationReason` is optional. Drift-detection test in `preplanEffectTypes.test.ts` asserts runtime set-equality against `{'discard', 'ko', 'gain', 'other'}`
- **Null-on-inactive convention (carries from WP-057 RS-8):** `isPrePlanDisrupted` returns `false` on null plan or `status !== 'active'`; `invalidatePrePlan` returns `null` on `status !== 'active'`; `executeDisruptionPipeline` returns `null` when `invalidatePrePlan` returns `null`. `computeSourceRestoration` never fails — empty ledger produces `{ playerDeckReturns: [], sharedSourceReturns: {} }`
- **Programming-error throw (only one in WP-058):** `buildDisruptionNotification` throws exactly when `invalidatedPlan.status !== 'invalidated'`. Error message format verbatim from WP-058 §F lines 400-403: `` `Cannot build notification for PrePlan ${invalidatedPlan.prePlanId}: status is '${invalidatedPlan.status}', expected 'invalidated'.` ``. Reserved for caller misuse; `executeDisruptionPipeline` is the only expected caller and always passes an invalidated plan
- **`PrePlan.revision` discipline:** `invalidatePrePlan` MUST NOT increment `revision`. Per `preplan.types.ts:36-38`, revision increments only on mutation of `sandboxState` / `revealLedger` / `planSteps` — status + `invalidationReason` are not those fields. Test asserts `invalidatedPlan.revision === prePlan.revision`
- **Full-spread 42/42 discipline for `invalidatePrePlan` (RS-3, mirrors WP-057 post-mortem §6):** every field of the returned `PrePlan` is a fresh copy — `sandboxState.hand`, `sandboxState.deck`, `sandboxState.discard`, `sandboxState.inPlay`, `sandboxState.counters`, `revealLedger`, `planSteps` — even though invalidation does not semantically change them. The `{ ...prePlan, status: 'invalidated', invalidationReason: {...} }` partial-spread shortcut (WP-058 §D skeleton lines 276-286) is **too loose** — executor must expand to full-field fresh copies. 01.6 §6 aliasing trace MANDATORY
- **Conditional-assignment for optional `affectedCardExtId` (RS-4):** under `exactOptionalPropertyTypes: true` (`packages/preplan/tsconfig.json:8`), assigning `undefined` to a `field?: CardExtId` slot fails typecheck. Build the base object without `affectedCardExtId`, then assign in an `if (mutation.affectedCardExtId !== undefined)` block. Applies at two sites: (a) `invalidatePrePlan` → `invalidationReason.affectedCardExtId`; (b) `buildDisruptionNotification` → returned `DisruptionNotification.affectedCardExtId`
- **Ledger-sole rewind authority (DESIGN-CONSTRAINT #3, `preplan.types.ts:162-168`):** `computeSourceRestoration` reads only `revealLedger: readonly RevealRecord[]`. It never inspects `PrePlan.sandboxState`. Copilot-check Issue 11 FIX test asserts this by passing a plan whose `sandboxState` disagrees with the ledger and verifying restoration follows the ledger
- **Source partitioning semantics (WP-058 §E lines 351-360):** `record.source === 'player-deck'` → `playerDeckReturns.push(record.cardExtId)`; every other value → `sharedSourceReturns[record.source]` (initialized to `[]` on first encounter, then pushed). Iteration via `for (const record of revealLedger)` preserves insertion order. No `.reduce()`
- **Pipeline ledger source (RS-8):** `executeDisruptionPipeline` passes `prePlan.revealLedger` (not `invalidatedPlan.revealLedger`) to `computeSourceRestoration` — equivalent because invalidation does not mutate ledger, and avoids depending on `invalidatePrePlan`'s spread-copy semantics. Required `// why:` comment locks the rationale
- **`requiresImmediateNotification: true` literal (WP-058 §G lines 457-466):** type-level encoding of Constraint #7; required `// why:` comment explains that removing the field would delete the type-level enforcement mechanism. Test asserts the field is `true` on every successful pipeline result
- **Import discipline:** `import type { CardExtId } from '@legendary-arena/game-engine'` is the only permitted engine reference; no runtime engine import; no `boardgame.io`; no `@legendary-arena/registry`; no `pg`; no `apps/**`
- **Test baseline lock (target; executor confirms exact counts at session close):** preplan `23 / 4 / 0 → 52 / 7 / 0` (+29 tests: detection 5 + invalidation 4 + restoration 6 [5 per WP §I + 1 ledger-sole per Copilot Issue 11] + notification 5 + full pipeline 7 + acceptance 1 + effect-type drift 1); engine `436 / 109 / 0` **UNCHANGED**; registry `13 / 2 / 0` unchanged; vue-sfc-loader `11 / 0` unchanged; server `6 / 2 / 0` unchanged; replay-producer `4 / 2 / 0` unchanged; arena-client `66 / 0` unchanged; **repo-wide `559 / 0 → 588 / 0`**
- **Test wrapping convention:** each new test file wraps its tests in exactly one top-level `describe()` block — `describe('preplan disruption detection (WP-058)')`, `describe('preplan disruption pipeline (WP-058)')`, `describe('preplan effect-type drift (WP-058)')`. Bare top-level `test()` calls forbidden (WP-031 / WP-057 precedent)
- **01.5 Runtime Wiring Allowance: NOT INVOKED** — four criteria absent: no `LegendaryGameState` field added; no `buildInitialGameState` shape change; no new `LegendaryGame.moves` entry; no new phase hook. Session prompt must declare NOT INVOKED explicitly per WP-030 / WP-055 / WP-056 / WP-057 precedent
- **01.6 Post-Mortem: MANDATORY** — four triggers fire: new long-lived abstractions (`isPrePlanDisrupted`, `invalidatePrePlan`, `computeSourceRestoration`, `buildDisruptionNotification`, `executeDisruptionPipeline`); first runtime consumer of `invalidationReason.effectType` closed union; first implementation of DESIGN-CONSTRAINT #3 "reveal ledger is sole rewind authority"; first full-spread 42/42 pattern applied to a status-transition (not a sandbox-mutation) operation. §6 aliasing trace MANDATORY
- **Commit prefix: `EC-058:`** for the execution commit; `WP-058:` is **forbidden** (P6-36). `SPEC:` for pre-flight bundle and governance close
- **Three-commit topology:** A0 `SPEC:` (this EC + WP-058 §Scope amendments for PS-2/PS-3 + pre-flight + copilot check) → A `EC-058:` (execution: 8 new files + index.ts modify + 01.6 post-mortem + any D-entries) → B `SPEC:` (STATUS + WORK_INDEX + EC_INDEX governance close)

## Guardrails
- Pre-Planning layer only — no imports from `packages/game-engine/**` runtime, `packages/registry/**`, `apps/server/**`, any `apps/**`, `pg`, or `boardgame.io`
- Type-only engine import (`import type { CardExtId } from '@legendary-arena/game-engine'`); bare `import { ... }` from `@legendary-arena/game-engine` forbidden
- No `Math.random()` anywhere (WP-058 consumes no randomness); no `ctx.random.*` — no `ctx` exists in this layer
- No `Date.now()` anywhere in WP-058 new files. The pre-existing single hit at `packages/preplan/src/speculativePrng.ts:79` (WP-057 carve-out) must remain the **only** hit in `packages/preplan/src/`
- No `.reduce()` anywhere — `computeSourceRestoration` uses explicit `for (const record of revealLedger)` with a mutable accumulator (code-style invariant extends to preplan)
- No wiring into `game.ts`, `LegendaryGame.moves`, phase hooks, or any engine lifecycle point (WP-028 lifecycle-prohibition precedent)
- No new move added to `CORE_MOVE_NAMES`; `CoreMoveName` union untouched
- No write path to `G` — there is no access path and none must be invented
- No in-place mutation of input `PrePlan` or any of its arrays/objects; every returned envelope carries fresh nested arrays/objects (RS-3 full-spread discipline)
- No increment of `PrePlan.revision` in `invalidatePrePlan` — status + `invalidationReason` are not revision-bumping fields
- No throw outside `buildDisruptionNotification`'s single programming-error guard
- No edit to `packages/preplan/src/preplan.types.ts` (WP-056 immutable), `preplanStatus.ts` / `speculativePrng.ts` / `preplanSandbox.ts` / `speculativeOperations.ts` (WP-057 immutable)
- No edit to `packages/preplan/package.json` or `pnpm-lock.yaml` — no devDep delta anticipated; if one surfaces it is a P6-44 tripwire (stop and investigate)
- P6-50 paraphrase discipline: JSDoc in new files must not reference engine runtime concepts by name — `G`, `ctx`, `LegendaryGameState`, `LegendaryGame`, `boardgame.io` are forbidden tokens in prose (use escaped grep patterns `boardgame\.io`, `\\bG\\b`, `\\bctx\\b` per P6-22 to avoid over-match); the inherited `ctx.turn + 1` references at `preplan.types.ts:21, :51` are the only permitted `ctx` occurrences
- Allowlist staging only — stage files by exact name; never `git add .` / `git add -A` / `git add -u`; never `--no-verify` / `--no-gpg-sign`
- DESIGN-CONSTRAINT #3 (ledger-sole rewind) is a hard invariant — `computeSourceRestoration` must read **only** `revealLedger`, never `sandboxState`; test covers this

## Required `// why:` Comments
- `disruptionDetection.ts` `isPrePlanDisrupted` — at the null-plan guard and the `status !== 'active'` guard: "pre-plan is advisory and only mutates while active; null-return signals non-active status to the caller without throwing (WP-057 null-on-inactive convention extended to detection; binary per-player detection per DESIGN-CONSTRAINT #4)"
- `disruptionPipeline.ts` `invalidatePrePlan` — at the `status !== 'active'` guard: "null-return signals non-active status without throwing; first-mutation-wins semantics for multiple mutations per move (WP-058 §Architectural Placement lines 120-125) is enforced mechanically here, not by caller dedup"
- `disruptionPipeline.ts` `invalidatePrePlan` — at the full-spread return (every nested array/object): "fresh copy prevents aliasing — invalidation does not semantically change sandboxState/revealLedger/planSteps, but returning them by reference would let a consumer mutate the input PrePlan through the output envelope (WP-028 aliasing precedent, WP-057 post-mortem §6 42/42 pattern)"
- `disruptionPipeline.ts` `invalidatePrePlan` — at the conditional-assignment for `invalidationReason.affectedCardExtId`: "exactOptionalPropertyTypes forbids explicit undefined assignment on optional field; build base object without affectedCardExtId, assign in an if-block only when mutation.affectedCardExtId is defined (session-context-wp058 Lesson 1; D-2901 preserveHandCards precedent)"
- `disruptionPipeline.ts` `computeSourceRestoration` — at the `for (const record of revealLedger)` loop: "reveal ledger is the sole authority for deterministic rewind (DESIGN-CONSTRAINT #3, preplan.types.ts:162-168); sandboxState must not be inspected here — any rewind logic that reads sandboxState is invalid"
- `disruptionPipeline.ts` `buildDisruptionNotification` — at the throw on non-invalidated plan: "programming-error throw — executeDisruptionPipeline is the only expected caller and always passes an invalidated plan; any other caller is misusing the API (WP-057 convention: throws reserved for programming errors, null returns for expected failure paths)"
- `disruptionPipeline.ts` `buildDisruptionNotification` — at the conditional-assignment for returned `affectedCardExtId`: same rationale as the invalidatePrePlan site (exactOptionalPropertyTypes + optional field)
- `disruptionPipeline.ts` `executeDisruptionPipeline` — at the `computeSourceRestoration(prePlan.revealLedger)` call (reading from prePlan, not invalidatedPlan): "invalidation does not mutate the ledger; reading from the pre-invalidation plan is equivalent to reading from invalidatedPlan and avoids depending on invalidatePrePlan's spread-copy semantics (RS-8)"
- `disruptionPipeline.ts` `DisruptionPipelineResult` declaration — at the `requiresImmediateNotification: true` literal field: "encodes Constraint #7 (immediate notification before further planning interaction) in the type system; callers check this field to confirm they are handling the notification requirement; removing the field would delete a type-level enforcement mechanism (Copilot Issue 15)"
- `disruption.types.ts` header or at each type declaration — brief note that the four public types are co-located here rather than beside their implementations so the WP-058 §H export surface is the single source of truth (PS-3 resolution)
- `index.ts` — augment the existing header comment to note the additive WP-058 export block (five functions + four types + `PREPLAN_EFFECT_TYPES` + `PrePlanEffectType`) sits below the WP-057 block; no edit to the WP-057 block
- `preplanEffectTypes.ts` at the `as const` on `PREPLAN_EFFECT_TYPES`: "canonical readonly array paired with PrePlan.invalidationReason.effectType closed union; drift-detection test enforces parity at build time (deferred from WP-056 per preplan.types.ts:101-106 JSDoc — WP-058 is the first runtime consumer of the effectType union)"

## Files to Produce
- `packages/preplan/src/disruption.types.ts` — **new** — four public types: `PlayerAffectingMutation`, `DisruptionNotification`, `SourceRestoration`, `DisruptionPipelineResult` (PS-3 consolidation — source of truth for all WP-058 public types; `disruptionPipeline.ts` imports them via `import type`)
- `packages/preplan/src/disruptionDetection.ts` — **new** — pure helper: `isPrePlanDisrupted`; imports `type { PrePlan } from './preplan.types.js'` + `type { PlayerAffectingMutation } from './disruption.types.js'`
- `packages/preplan/src/disruptionPipeline.ts` — **new** — pure helpers: `invalidatePrePlan`, `computeSourceRestoration`, `buildDisruptionNotification`, internal non-exported `buildNotificationMessage`, `executeDisruptionPipeline`; imports `type { CardExtId } from '@legendary-arena/game-engine'` + intra-package types only
- `packages/preplan/src/preplanEffectTypes.ts` — **new (PS-2)** — `PREPLAN_EFFECT_TYPES` readonly array + `PrePlanEffectType` derived type + compile-time drift check using `NonNullable<PrePlan['invalidationReason']>['effectType']`
- `packages/preplan/src/disruptionDetection.test.ts` — **new** — 5 tests in one `describe('preplan disruption detection (WP-058)')` block (owner match → true; other-player → false; null plan → false; already-invalidated → false; already-consumed → false)
- `packages/preplan/src/disruptionPipeline.test.ts` — **new** — 23 tests in one `describe('preplan disruption pipeline (WP-058)')` block: invalidation 4 (active→'invalidated'; reason populated; null on non-active; input not mutated including revision stays same) + source restoration 6 (empty ledger; player-deck returns; shared-source returns; mixed sources; order preserved; **ledger-sole per Copilot Issue 11 — sandbox disagrees with ledger, restoration follows ledger**) + notification 5 (correct ids; message includes effect; includes card when set; omits card when absent; throws on non-invalidated) + full pipeline 7 (active+matching → full result with `requiresImmediateNotification: true`; active+non-matching → null per `isPrePlanDisrupted`; non-active → null; multiple mutations same player → first result then null; result's `invalidatedPlan.status === 'invalidated'`; result's `sourceRestoration` matches ledger; result's `notification.message` includes cause) + acceptance scenario 1 (create → speculative draw/play → execute disruption → verify invalidated + restoration + notification + fresh `createPrePlan` succeeds from updated state)
- `packages/preplan/src/preplanEffectTypes.test.ts` — **new (PS-2)** — 1 test in one `describe('preplan effect-type drift (WP-058)')` block: runtime set-equality between `new Set(PREPLAN_EFFECT_TYPES)` and `new Set(['discard', 'ko', 'gain', 'other'])`
- `packages/preplan/src/index.ts` — **modified** — add WP-058 additive export block: four types from `./disruption.types.js` (`PlayerAffectingMutation`, `DisruptionNotification`, `SourceRestoration`, `DisruptionPipelineResult`); `isPrePlanDisrupted` from `./disruptionDetection.js`; `invalidatePrePlan`, `computeSourceRestoration`, `buildDisruptionNotification`, `executeDisruptionPipeline` from `./disruptionPipeline.js`; `PREPLAN_EFFECT_TYPES` + `PrePlanEffectType` from `./preplanEffectTypes.js`. Keep WP-056 + WP-057 blocks unchanged
- `docs/ai/post-mortems/01.6-WP-058-preplan-disruption-pipeline.md` — **new** — 10-section audit; MANDATORY per four triggers; §6 aliasing trace covers every returned envelope from `invalidatePrePlan` / `computeSourceRestoration` / `buildDisruptionNotification` / `executeDisruptionPipeline`; staged into Commit A
- `docs/ai/DECISIONS.md` — **modified** (if new D-entries authored) — candidates from WP-058 §Governance (lines 671-686): (1) binary per-player detection per Constraint #4; (2) single-workflow pipeline (detect/invalidate/restoration/notification not separable); (3) ledger-sole rewind (WP-056 invariant); (4) pipeline produces instructions + events; caller applies them; (5) `PlayerAffectingMutation` produced by integration-layer adapters, not engine. Group as `### D-5801` (or split across 5801/5802/5803) at executor discretion; `DECISIONS_INDEX.md` row mandatory for every new D-entry

## After Completing
- [ ] `pnpm install` — no-op expected; any `pnpm-lock.yaml` delta is a scope violation (stop and investigate)
- [ ] `pnpm -r build` — exits 0 (preplan `dist/` contains `.js` + `.d.ts` for all new files)
- [ ] `pnpm -r --if-present test` — exits 0; preplan delta `23 / 4 / 0 → 52 / 7 / 0`; engine UNCHANGED at `436 / 109 / 0`; repo-wide `559 → 588`
- [ ] `git grep -nE "from ['\"]boardgame\.io" packages/preplan/` — zero hits (escaped dot per WP-031 P6-22)
- [ ] `git grep -nE "from ['\"]@legendary-arena/game-engine['\"]" packages/preplan/src/` returns only lines beginning `import type` — no runtime engine import
- [ ] `git grep -nE "from ['\"]@legendary-arena/registry" packages/preplan/` — zero hits
- [ ] `git grep -nE "from ['\"]pg" packages/preplan/` — zero hits
- [ ] `git grep -nE "from ['\"]apps/" packages/preplan/` — zero hits
- [ ] `git grep -nE "Math\.random" packages/preplan/` — zero hits
- [ ] `git grep -nE "ctx\.random" packages/preplan/` — zero hits
- [ ] `git grep -nE "Date\.now" packages/preplan/src/` — **exactly one hit** at `packages/preplan/src/speculativePrng.ts:79` inside `generateSpeculativeSeed` (WP-057 carve-out); any additional hit is an unauthorized wall-clock read introduced by WP-058 (Copilot Issue 2)
- [ ] `git grep -nE "require\(" packages/preplan/` — zero hits (ESM only)
- [ ] `git grep -nE "\.reduce\(" packages/preplan/` — zero hits
- [ ] `git grep -nE "\\b(LegendaryGameState|LegendaryGame)\\b" packages/preplan/src/` — zero hits in new WP-058 files (code + JSDoc prose)
- [ ] `git grep -nE "boardgame\\.io" packages/preplan/src/` — only the type-only import line in files that use `CardExtId` via `import type { CardExtId } from '@legendary-arena/game-engine'` (note: that import doesn't contain the string `boardgame.io`, so this grep expects zero hits in WP-058 new files)
- [ ] `git grep -nE "\\bG\\b" packages/preplan/src/` — zero hits (P6-50 paraphrase; letter-`G`-as-engine-state forbidden)
- [ ] `git grep -nE "\\bctx\\b" packages/preplan/src/` — only the inherited `ctx.turn + 1` invariant references at `preplan.types.ts:21, :51` (WP-056 carve-out); any other hit in WP-058 new files is a paraphrase violation
- [ ] `git diff --name-only` — exactly the 8 allowlisted files under `packages/preplan/src/` + governance files (DECISIONS.md / DECISIONS_INDEX.md if D-entries authored; 01.6 post-mortem); no other files modified; `package.json` / `pnpm-lock.yaml` unchanged
- [ ] `packages/preplan/src/preplan.types.ts` unchanged from WP-056 (`git diff packages/preplan/src/preplan.types.ts` empty)
- [ ] `packages/preplan/src/preplanStatus.ts` / `speculativePrng.ts` / `preplanSandbox.ts` / `speculativeOperations.ts` unchanged from WP-057 (all four `git diff` empty)
- [ ] Aliasing trace completed in 01.6 post-mortem §6 — every returned `PrePlan` from `invalidatePrePlan` has fresh `sandboxState.{hand,deck,discard,inPlay,counters}` + `revealLedger` + `planSteps`; every returned `SourceRestoration` / `DisruptionNotification` / `DisruptionPipelineResult` has fresh arrays/objects; no input reference reachable through any output
- [ ] Revision-non-increment verified — `invalidatedPlan.revision === prePlan.revision` asserted in invalidation test
- [ ] Ledger-sole rewind verified — Copilot Issue 11 test asserts `computeSourceRestoration` ignores `sandboxState` mismatch; §6 post-mortem confirms no `sandboxState` read path in `computeSourceRestoration`
- [ ] `requiresImmediateNotification: true` assertion present in full-pipeline test; JSDoc + `// why:` comment explain the type-level enforcement rationale (Copilot Issue 15)
- [ ] Conditional-assignment pattern applied at both `affectedCardExtId` sites; tests cover with-card and without-card branches
- [ ] `docs/ai/DECISIONS.md` — any new D-entries present with title headings; `DECISIONS_INDEX.md` rows match
- [ ] `docs/ai/work-packets/WORK_INDEX.md` — WP-058 checked off with date + commit hash (Commit B)
- [ ] `docs/ai/STATUS.md` — WP-058 status bumped (Commit B)
- [ ] `docs/ai/execution-checklists/EC_INDEX.md` — EC-058 row present (Commit A0) and status flipped Draft → Done with commit hash (Commit B)
- [ ] Commit A message prefix `EC-058:`; zero `WP-058:` prefix occurrences anywhere in commit history

## Common Failure Smells
- `invalidatePrePlan` returns `{ ...prePlan, status: 'invalidated', invalidationReason: {...} }` with no explicit `sandboxState` / `revealLedger` / `planSteps` spread → aliasing; inputs mutable through output; violates full-spread 42/42 discipline (WP-057 post-mortem §6 precedent). Test "Input plan is not mutated" catches the flat case but not nested aliasing — §6 trace is the backstop
- `invalidatePrePlan` increments `revision` → violates `preplan.types.ts:36-38` invariant (revision only bumps on sandboxState/revealLedger/planSteps mutation; status is not one of those)
- `invalidationReason: { ..., affectedCardExtId: mutation.affectedCardExtId }` assigned unconditionally → TS compile fails under `exactOptionalPropertyTypes: true` when `mutation.affectedCardExtId` is `undefined` (session-context-wp058 Lesson 1); use conditional-assignment pattern
- `DisruptionNotification` returned with `affectedCardExtId: mutation.affectedCardExtId` unconditionally → same TS compile failure; conditional-assignment at the return site
- `computeSourceRestoration` reads `prePlan.sandboxState` for any reason → violates DESIGN-CONSTRAINT #3 (ledger-sole rewind authority); Copilot Issue 11 test catches it
- `buildDisruptionNotification` called with an active plan somewhere other than through `executeDisruptionPipeline` → throws with the locked error template; caller is misusing the API, not a runtime recoverable condition
- `executeDisruptionPipeline` passes `invalidatedPlan.revealLedger` to `computeSourceRestoration` instead of `prePlan.revealLedger` → semantically equivalent today (invalidation doesn't mutate ledger) but couples to spread-copy semantics; lock to `prePlan.revealLedger` with `// why:` comment (RS-8)
- `executeDisruptionPipeline` dispatches multiple mutations from a single caller without relying on the status guard → caller-side dedup logic is scope creep; WP-058 §Architectural Placement lines 120-125 locks the first-mutation-wins semantics at the mechanical level (status guard in `isPrePlanDisrupted` + `invalidatePrePlan`)
- `DisruptionPipelineResult` declared with `requiresImmediateNotification: boolean` instead of `requiresImmediateNotification: true` → widens the type, deletes the type-level Constraint #7 encoding; must be the literal `true` (not the primitive)
- `disruptionPipeline.ts` re-declares `SourceRestoration` or `DisruptionPipelineResult` → PS-3 consolidates all four public types in `disruption.types.ts`; `disruptionPipeline.ts` imports via `import type` only
- `preplanEffectTypes.ts` uses `PrePlan['invalidationReason']['effectType']` (without `NonNullable<>`) in the drift check → `invalidationReason` is optional; without `NonNullable<>` the indexed type is `undefined`-contaminated and the check compiles but asserts the wrong set
- `computeSourceRestoration` uses `.reduce()` to partition records → `.reduce()` forbidden in preplan; use `for (const record of revealLedger)` with a mutable accumulator
- Test asserts specific `DisruptionNotification.message` string exactly (e.g., `"Player 0's force discard."`) → brittle; use substring assertions (RS-13 — source id appears; effect description appears; card appears iff `affectedCardExtId` set)
- `disruptionPipeline.test.ts` has multiple `describe()` blocks (one per subsection) → breaks the one-describe-per-file convention (WP-031 / WP-057 precedent); collapse into a single `describe('preplan disruption pipeline (WP-058)')` block and the suite count stays at +1
- `packages/preplan/package.json` modified in `git diff --name-only` → no devDep needed for WP-058; any edit here is scope creep or a `tsx` version mismatch signal (stop and investigate)
- `pnpm-lock.yaml` appears in `git diff --name-only` → no lockfile delta expected for WP-058; presence indicates an accidental `pnpm install` with pending changes (P6-44 tripwire)
- Commit message starts with `WP-058:` → P6-36 violation; use `EC-058:` for execution commit and `SPEC:` for A0 / B commits
- Any file under `packages/game-engine/`, `packages/registry/`, `apps/**` appears in `git diff --name-only` → layer violation; WP-058 touches only `packages/preplan/src/` + governance docs
- `docs/ai/post-mortems/01.6-WP-058-preplan-disruption-pipeline.md` absent at session end → 01.6 MANDATORY triggers not satisfied; blocks Commit A
- `preplanEffectTypes.test.ts` asserts only a runtime check without the compile-time pattern (or vice versa) → weaker than WP-057 `preplanStatus.ts` precedent; include both the compile-time exhaustive check in `preplanEffectTypes.ts` AND the runtime set-equality assertion in the test file
