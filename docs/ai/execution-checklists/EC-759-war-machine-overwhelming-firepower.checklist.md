# EC-759 — War Machine "Overwhelming Firepower" onDefeat reward (marker-only)

**WP:** WP-722 · **Decision:** D-24543 · **Lane:** Lightweight (single session)

## Before Starting

- [ ] WP-656 / D-24467 is on `main`: `defeated-villain-or-mastermind` marker→condition
      branch (`setup/heroAbility.setup.ts`), `resolveDeferredHeroGrants`
      (`hero/heroEffects.execute.ts`), `G.villainOrMastermindDefeatedSinceResolve` set at
      the `fightVillain` / `fightMastermind` tails.
- [ ] `draw` and `rescue` are in `HERO_KEYWORDS` + `HANDLED_KEYWORDS` with handlers;
      `rescue` ∈ `NO_MAGNITUDE_KEYWORDS`.
- [ ] `rvlt.json` ability line for `war-machine/overwhelming-firepower` idx 0 matches the
      map `abilityText` exactly.
- [ ] `pnpm --filter @legendary-arena/registry --filter @legendary-arena/game-engine build`.

## Locked Values

- Three apply entries (order = condition, draw, rescue) for `rvlt` /
  `war-machine` / `overwhelming-firepower` / `abilityIndex 0`:
  `[keyword:defeated-villain-or-mastermind]`, `[keyword:draw:1]`, `[keyword:rescue:1]`.
- Marked line: `Whenever you defeat a Villain or Mastermind this turn, draw a card and rescue a Bystander. [keyword:defeated-villain-or-mastermind] [keyword:draw:1] [keyword:rescue:1]`
- Parsed hook: `conditions:[{type:'defeatedVillainOrMastermindThisTurn',value:'1'}]`,
  `effects:[{type:'draw',magnitude:1},{type:'rescue',magnitude:1}]`, `unresolvedMarkers` empty/absent.

## Guardrails

- [ ] NO `packages/game-engine/src/**` source edit except the new test file — no new
      handler, keyword, condition type, or drift array.
- [ ] Remove the `war-machine/overwhelming-firepower` `_deferred` entry (do not leave it).
- [ ] Card data is regenerated by `apply-hero-ability-markers.mjs`, never hand-typed.
- [ ] `finalStateHash` must stay byte-unchanged — no sentinel/fixture re-pin (no committed
      fixture plays rvlt War Machine). If a sentinel moves, STOP and investigate.
- [ ] Moon Knight golden-ankh + Werewolf track-the-captives stay `_deferred`.
- [ ] Test: `node:test`/`node:assert` only; no boardgame.io; drives the real parsed hook.

## Required Comments (`// why:`)

- [ ] In the test: why the marked-ability constant is the exact generated line; why the
      condition defers on play and fires per defeat (cite WP-656 / D-24467).

## Files to Produce

- [ ] `scripts/convert-cards/inputs/hero-ability-markers.json` — +3 apply entries, −1 `_deferred`.
- [ ] `data/cards/rvlt.json` — generated (apply script).
- [ ] `docs/ai/coverage/hero-mechanic-ledger.json` / `.csv` — generated.
- [ ] `data/metadata/effect-implementation-index.json` — generated.
- [ ] `data/metadata/card-mechanics.json` — generated.
- [ ] `packages/game-engine/src/hero/warMachineOverwhelmingFirepower.test.ts` — new.

## After Completing

- [ ] `cards:check`, `ledger:heroes:check`, `effect-index:check`, `mechanics:metadata:check`,
      `sim:coverage --check`, `sim:runtime-observed:check` all exit 0.
- [ ] Full game-engine suite 0 fail; the new file's 4 tests pass.
- [ ] STATUS.md, DECISIONS.md (D-24543 Active), WORK_INDEX (WP-722 `[x]`), EC_INDEX (`Done`),
      mindmap node + `roadmap:counts:check` 0, NUMBER-LEDGER reservations.
- [ ] D-24026 live-verify on `play.legendary-arena.com` (post-deploy).

## Common Failure Smells

- A hollow after regen (`unresolvedMarkers` non-empty) → a token typo; re-check the three forms.
- `cards:check` fail → line was hand-edited, not applied; re-run the apply script.
- A sentinel `finalStateHash` diff → something other than data changed; STOP.
