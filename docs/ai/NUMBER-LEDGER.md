# Number Ledger — WP / EC / D allocation lock

The append-only reservation ledger that stops two concurrent sessions from
allocating the **same** WP / EC / D number (the failure that renumbered WP-419 →
WP-421). See **D-24245** (this mechanism; renumbered from D-24242 — see the D
section below) and the allocation protocol in
[`01.0a-wp-drafting-phase.md`](REFERENCE/01.0a-wp-drafting-phase.md).

## How it works

Each space (`WP` / `EC` / `D`) has a **`high-water`** — the highest number that
was already allocated when this ledger was adopted (2026-07-25). Numbers at or
below the high-water are grandfathered (they live in WORK_INDEX / EC_INDEX /
DECISIONS and are stable). **Every new allocation above the high-water gets one
reservation line here**, newest last:

```
- WP-422 — <kebab-slug or short title> (YYYY-MM-DD, <branch-or-PR>)
```

**The protocol (reserve first):**

1. `node scripts/check-number-ledger.mjs --next wp` (or `ec` / `d`) → the next free number.
2. Append its reservation line under the matching `## ` section **in your SPEC
   commit**, and get that tiny append merged **first** — claim the number before
   the bulky work.
3. `node scripts/check-number-ledger.mjs --check` must pass (CI runs it too).

**Why this holds under concurrency:**

- `.gitattributes` marks this file `merge=union`, so two sessions reserving
  *different* numbers auto-merge with **no conflict** on local rebase/merge (the
  big prose indices do not — that is why reservations live in this minimal file).
- If two sessions reserve the **same** number, union-merge keeps both identical
  lines and `--check` **fails loudly** (`DUPLICATE reservation`) — the collision
  surfaces early in CI, not silently at merge time. One session renumbers.
- `--check` also fails on **drift**: a number used in an index above the
  high-water with no reservation here (`UNRESERVED`).

Union-merge is a *local* git driver (it does not run on GitHub's server-side
squash) — that is fine: the duplicate check is the real safety net and catches a
same-number race however the merge happened.

---

## WP

high-water: 422

<!-- reservations (WP-423 and up), newest last -->

- WP-423 — hugo-version-upgrade (2026-07-24, spec/wp-423-hugo-version-upgrade)
- WP-424 — bot-ally stop-drivers-on-sigterm (2026-07-25, fix/bot-ally-stop-drivers-on-sigterm)
- WP-425 — apex-legendary-combo-tier (2026-07-25, spec/wp-425-apex-combo-tier)
- WP-426 — bot-ally survive-db-blip (2026-07-25, fix/bot-ally-survive-db-blip)
- WP-428 — diagnostic-transport-block (2026-07-25, spec/wp-428-transport-diagnostics)
- WP-427 — bot-resolve-putbottomhq (2026-07-25, fix/bot-resolve-putbottomhq)
- WP-429 — transport-reconnect-resync-counters (2026-07-25, spec/wp-429-transport-counters)
- WP-430 — fluid-desktop responsive scaling (2026-07-25, spec/wp-429-fluid-desktop-scaling)
- WP-431 — log-entry-bystander-and-hero-return (2026-07-25, fix/log-entry-bystander-and-hero-return)
- WP-432 — remove-noncanonical-entry-bystander (2026-07-25, fix/remove-noncanonical-entry-bystander)
- WP-433 — bot-ally-fault-observability (2026-07-26, fix/bot-ally-fault-observability)
- WP-434 — log-outcome-engine-contract (WP-B.3a: G.messages/UIState.log string[]→LogEntry[] + LOG_OUTCOMES enum + pushLog outcome arg; implements D-24253; renumbered from WP-432 after a landed collision; 2026-07-26, spec/wp432-log-outcome-engine-contract)
- WP-435 — log-outcome-client-colour (WP-B.3b: GameLogPanel colours each log line by LogEntry.outcome + non-colour glyph/aria signal + export tag policy; arena-client only; implements D-24253; 2026-07-26, spec/wpb3b-log-colour)
- WP-436 — effectprovenance-outcome-retire (WP-B.3c: effectProvenance recentlyPlayedCards[].outcome reads the authoritative LogEntry.outcome instead of string-matching; awaitingPlayerInput + hollowEffects reads kept; arena-client only; implements D-24253 §Fork F; 2026-07-26, spec/wpb3c-effectprovenance-retire)
- WP-437 — bot-ally-ownership-guard (cross-instance driver ownership lease — driver_owner + heartbeat_at side-table columns + tick-level lease arbitration so only ONE instance drives a bot seat; closes the WP-424/D-24244-deferred deploy-overlap two-writer window; server only; 2026-07-26, worktree-wp-bot-ally-ownership-guard)
- WP-438 — logentry-card-field (structured LogEntry.card{?} so effectProvenance stops parsing 'played X' ext-id prose; retires PLAYED_LABEL_EXTID + the B.3c (extId) substring; realizes D-24253 §14; 2026-07-27, spec/wp-logentry-card)
- WP-439 — dashboard-runtime-health (server process CPU%/event-loop-lag/mem/uptime signal on a new admin-gated GET /api/dash/runtime + a dashboard runtime-health tile; answers do-we-need-to-cluster with data; server + dashboard; 2026-07-27, worktree-wp-dashboard-runtime-health)
- WP-440 — gauntlet-pack-contract (identity-only GauntletPack Zod schema + buildGauntletPack/validateGauntletPack in packages/registry, strict per-pack_version validation with a major-version reject gate; first WP of the Mastermind Gauntlets download→import→build→track epic; registry only; 2026-07-27, claude/wp440-gauntlet-pack)
- WP-441 — legends-gauntlet-download (legends-board pin core/magneto to the top of the gauntlet index + a "Download Mastermind Gauntlet" control with a player-count 1..5 / division fixed|open selector defaulting to solo+fixed that Blob/anchor-downloads gauntlet-<set>-<mm>-<div>-p<N>.gauntlet.json; builds the WP-440 pack INLINE via a type-only contract import to preserve the vue-sole-runtime-dep / zero-API invariant; second WP of the Mastermind Gauntlets epic; apps/legends-board only; 2026-07-27, claude/wp441-legends-download)
- WP-442 — gauntlet-truth-helper (behavior-preserving server refactor: extract the leg-clear qualification predicate + findBestPoolAssignment out of apps/server/src/legends/gauntlet.logic.ts into a new pure, data-injected, layer-locked gauntletTruth.logic.ts; getGauntletStandings rewired to consume it with ZERO behavior change so gauntlet.logic.test.ts passes unchanged; the shared truth helper WP-5's per-run read reuses so tracker + leaderboard cannot drift; third WP of the Mastermind Gauntlets epic; apps/server only; NO new D-entry — inherits the existing layer lock; 2026-07-27, claude/wp442-truth)
- WP-443 — gauntlet-run-persistence (fourth WP of the Mastermind Gauntlets epic; new migration 039_create_player_gauntlet_runs.sql — one legendary.player_gauntlet_runs table storing account-local run identity + per-leg hero picks leg_picks jsonb + audit timestamps, NOTHING derived: no status/hero_pool/child/history/flag column, partial-unique active-run index WHERE first_completed_at IS NULL; optional minimal gauntletRun.types.ts + a DB-gated migration test; reserves the load-bearing D-24262 derived-progression lock; server/persistence only; parallel-safe with WP-440/441; 2026-07-27, claude/wp443-run-persistence)
- WP-444 — registry-viewer-gauntlet-pack-import (fifth WP of the Mastermind Gauntlets epic — the cards-side consumer of the WP-440 identity pack; added after a live product test where the downloaded pack errored with 5 schema issues in the cards.legendary-arena.com builder; a new "Load Gauntlet Pack (paste or file)" importer validates the pack via validateGauntletPack, shows a leg (scheme) picker, resolves the approved variant-0 composition from GAUNTLET_LOADOUT_MENUS + playerCount and prefills the builder draft with heroDeckIds left empty; zero-API client-side resolution from the bundled registry; unknown-gauntlet / unoffered-count → friendly message not a schema wall; apps/registry-viewer only; reserves D-24263; hard-deps WP-440 + WP-395; 2026-07-28, claude/wp444-cards-consumer)
- WP-445 — gauntlet-run-import-api (sixth WP of the Mastermind Gauntlets epic — the play-side consumer of the WP-440 identity pack; SPLIT off the plan's "import + run API" #5: this WP is import + run CRUD only — POST /api/me/gauntlet-runs validates a WP-440 pack via validateGauntletPack + confirms the gauntlet exists in the registry + INSERTs an active run with empty leg_picks, IDEMPOTENTLY attaching to an existing active run on the partial-unique conflict instead of erroring; GET returns the caller's raw stored runs (identity + leg_picks + timestamps, NO derived projection); PATCH edits leg_picks; DELETE removes; mirrors loadoutLibrary.{routes,logic,types}.ts for auth/player_id-scoping/no-store/typed-errors; reuses WP-443 gauntletRun.types.ts + migration 039; server.mjs wiring + api-endpoints.md catalog + DB-gated tests; reserves D-24264 the import-idempotency + run-API-shape lock; the DERIVED-progression read GET — 5-state status/pool/headroom/per-leg-cleared/last-played via gauntletTruth.logic.ts — is the queued follow-on WP-446 NOT drafted here; apps/server only; hard-deps WP-440 + WP-442 + WP-443; 2026-07-28, claude/wp445-run-api)
- WP-446 — gauntlet-run-derived-progression (seventh WP of the Mastermind Gauntlets epic — the DERIVED read WP-445 deferred; enhances GET /api/me/gauntlet-runs so each run carries its DERIVED progression computed at read time, NOTHING stored per D-24262: legs = the mastermind's home-set schemes (injected), per-leg cleared via qualifiesAsLegClear over the caller's competitive_scores, pool = union of leg_picks hero ids, budget headroom = heroCount+2 minus pool size, champion via findBestPoolAssignment over the caller's winning teams, a 5-state status needs-heroes|ready|playing|all-legs-cleared|champion with all-legs-cleared DISTINCT from champion, and per-leg last-played = max(created_at); REUSES the WP-442 gauntletTruth.logic.ts helper as the SINGLE source of truth so tracker + leaderboard cannot diverge, mirrors getGauntletStandings' injected inputs (approvedLoadouts + published-version + leaderboardDeps + heroCount) scoped to one caller; new gauntletRunProgress.logic.ts + test, additive GauntletRunProgressView types in gauntletRun.types.ts, GET-row api-endpoints.md replace-whole-row, server.mjs wiring; caller-scoped, authenticated-session-required, no-store; DB-gated tests over all 5 states + champion + per-leg-cleared + headroom + a cross-check that the run read agrees with getGauntletStandings on shared solo fixtures; reserves D-24265 the single-run-status-derivation-semantics lock; apps/server only; hard-deps WP-442 + WP-443 + WP-445; 2026-07-28, claude/wp446-progress)
- WP-447 — villain-scry-ko-own-deck (first self-deck scry mechanic in the villain/henchman effect vocabulary — closes the dead Doombot Legion Fight "Look at the top two cards of your deck. KO one of them and put the other back." Jeff reported from a live Magneto match, for which D-24266 shipped only the unmarked-ability breadcrumb; appends ONE no-param primitive scry-ko-own-deck to the closed VillainEffectPrimitive union + VILLAIN_EFFECT_PRIMITIVES array at position 6 append-only with the drift test moved together, extends parseParameterizedEffect to accept it as no-param, adds a villainEffectScryKoOwnDeck handler + a deterministic selectScryKoTarget worst-first Wound → starting-SHIELD → lex-lowest that KOs one of min(2,deck.length) of the current player's deck and leaves the other on top, empty deck reachable no-op; marks Doombot Legion (core + co2e only — msp1's look-2/KO-1 card is a differently-named henchman with a [keyword:Fight]: prefix, deferred) via villain-effect-markers.json + the script's local primitives array, regenerated onto data/cards/{core,co2e}.json by apply-effect-markers.mjs; AUTO-RESOLVED — no pending-choice/UIState/prompt/End-Turn-gate/bot-getLegalMoves change, the interactive pick is the queued follow-on per the koHeroCurrentPlayer WP-185→WP-242 path; determinism-adjacent, no ctx.random/I/O/new-G-field, replay-hash regen-with-note only if a fixture defeats a Doombot; reserves D-24267 the primitive + auto-resolve decision; game-engine + card-data only; hard-deps D-24266 + WP-252; 2026-07-28, spec/wp-447-scry-ko-mechanic)
- WP-449 — profile-gauntlet-tracker-ui (final WP of the Mastermind Gauntlets download → import → build → track epic — the play-side `?route=me` gauntlet tracker; adds a Gauntlet Runs section to `apps/arena-client/src/pages/MyProfilePage.vue` + a new never-throw client API module `lib/api/gauntletRunApi.ts` consuming WP-445's four /api/me/gauntlet-runs endpoints; import via file+drag-drop+paste-JSON → POST; renders the WP-446 GauntletRunProgressView 5-state status with all-legs-cleared vs champion VISIBLY DISTINCT, pool + budget headroom, per-leg rows, derived "where you left off"; per-leg hero-pick PATCH + "Play this leg" assembling a MatchSetupConfig from the picked heroes + a server-supplied launch block and launching via WP-448 launchMatchFromComposition; completed history + delete/reset. CROSS-LAYER: arena-client CANNOT import registry, so villains/henchmen (approved variant 0, D-24199) + the four supply counts (no registry source) come from a minimal ADDITIVE server extension of the WP-446 derived read — GauntletRunProgressView gains a `launch: GauntletRunLaunch | null` block (server/apps/server touch, no new endpoint, no migration); §21 replaces the GET /api/me/gauntlet-runs catalog row. Reserves D-24269 (tracker UX contract + Play-this-leg composition-assembly resolution + operator forks); hard-deps WP-440 + WP-445 + WP-446 + WP-448; 2026-07-28, claude/wp449-tracker-ui)
- WP-448 — composition-to-match-launch-primitive (eighth WP of the Mastermind Gauntlets epic — the reusable composition→match launch primitive the WP-7 tracker will call; behavior-preserving arena-client refactor. Extracts the create-and-join-from-a-composition chain — createMatch(config, playerCount, token) → persistMatchSetup → joinMatch(seat 0) → navigate ?match=<id>&player=0&credentials=<c> — out of apps/arena-client/src/lobby/LobbyView.vue's submitFromJson AND the identical submitCreate chain into a NEW reusable module apps/arena-client/src/lobby/useCreateMatchFromComposition.ts exporting a never-throw async launchMatchFromComposition(input) that returns a typed { ok:true, matchID } | { ok:false, message } result, and rewires LobbyView to consume it as the SINGLE source with ZERO behavior change; existing arena-client lobby suite passes UNCHANGED is the correctness gate. createWithBotAlly (bot API, different endpoint + message), joinExisting (join-only), startAutoplay (autoplay endpoint) stay out with documented rationale. Also delivers the launch mechanism the long-deferred WP-303 "Load into lobby / play a saved loadout" placeholder needs — reserves D-24268 recording that supersession; a future WP-303 becomes a call site, not a launch-chain build. No new endpoint, no server change, no createMatch/joinMatch contract change, no ?loadout= deep-link. New useCreateMatchFromComposition.ts + .test.ts + modified LobbyView.vue; apps/arena-client only; hard-dep none (parallel-safe with the epic); enables WP-7; 2026-07-28, claude/wp448-match-primitive)
- WP-450 — villain-gain-attached-hero (D-24266 breadcrumb false-positive fix — `Fight: Gain that Hero` villain lines are flagged unmarked-ability even though the effect fires via the generic WP-431 awardAttachedHeroes at the fight site, not a hook; appends ONE no-param primitive gain-attached-hero to the closed VILLAIN_EFFECT_PRIMITIVES union + array, append-only, position resolved against main per the WP-447 ordering coordination, drift test moved together; a villainEffectGainAttachedHero handler that is a deliberate no-op returning targets:[] and mutating nothing — the handler exists only to classify the line reachable/applied so the breadcrumb stops; marks the 3 lines core skrull-queen-veranke + core skrull-shapeshifters + rvlt klaw via villain-effect-markers.json + the apply-effect-markers.mjs local array, regenerated onto data/cards/{core,rvlt}.json; NO gameplay change, awardAttachedHeroes untouched; DETERMINISM: marking suppresses a G.diagnostics hollow write which IS hashed (computeStateHash serializes whole G; finalStateHash oracle excludes only messages+logMeta not diagnostics), so hash-relevant where the 3 villains are fought — re-pin-free ONLY because no pinned fixture defeats them, executor confirms by running the suite (not "no-op ⇒ no re-pin"); Klaw's Ambush class+cost-filtered capture stays unimplemented + keeps its breadcrumb, out of scope; reserves D-24270; game-engine + card-data only; hard-deps D-24266 + WP-431 + WP-252, soft-coordinates WP-447; NOTE ledger --next returned WP-449 but that is claimed by open PR #1070 so this took WP-450; 2026-07-28, spec/wp-450-gain-attached-hero)
- WP-451 — exclude-diagnostics-from-state-hash (closes the observability-hash footgun found while drafting WP-447/450: G.diagnostics — runtime-only observation, WP-257 never-gameplay-input — is serialized into BOTH state-hash surfaces so marking a villain / the D-24266 breadcrumb firing silently shifts finalStateHash; excludes diagnostics from hashGameState the fixture-golden oracle (load-bearing, stops golden-fixture churn) AND computeStateHash the production whole-G competitive/determinism oracle (decouples the competitive replayHash + step-9 anti-tamper compare from observability drift); corrects the now-false hash comments (real stale rationale is buildInitialGameState.ts ~566-568 comment-only, + game.ts full-G caveat + hashGameState.ts diagnostics why-comment); diagnostics-invariance tests + exclusion-set pin; expected NO re-pin (no fixture materializes a hollow record); RISK operator-sign-off: computeStateHash half changes the competitive replayHash algorithm — accepted scores immutable+never-reverified (immune), score math never reads diagnostics, only exposure is a one-time captured-pre/submitted-post transition window (rare/recoverable); recommended default (copilot + analysis) = hashGameState-only (zero competitive risk, fixes fixture churn), both surfaces available if operator wants the computeStateHash exclusion in one shot; no DB/server change; reserves D-24271; game-engine only; hard-deps WP-257 + D-24081/D-24221; 2026-07-29, spec/wp-451-diagnostics-hash-exclusion)
- WP-453 — simulation-setup-shuffle-fidelity (the WP-452 follow-up for the flagged turn-0 SCHEME_LOSS on midtown-bank-robbery / negative-zone-prison-breakout; root cause is NOT scheme setup — evaluateEndgame(G)=null at init for all schemes — but a simulation reverse-shuffle artifact: buildVillainDeck lexically sorts the deck before shuffling, scheme-twist-* ids sort LAST, and the sim/fixture setup context uses makeMockCtx's (d)=>[...d].reverse() → all 8 twists stack on top of the villain deck; the two schemes whose twist resolver CHAINS reveals cascade through the cluster in one reveal → schemeTwistCount hits lossThreshold:8 (D-24178) at turn 0; every scheme's twists are front-loaded, depressing ALL sim win rates incl. the WP-452 0.0% baseline; production unaffected (real boardgame.io PRNG shuffle); fix = point the 5 sim/fixture setup sites (runner simulateOneGame/simulateOneCoopGame/simulateOneGameAndCaptureMoves, par.aggregator, runFixture) at each file's EXISTING seeded shuffleWithPrng(deck,nextRandom); makeMockCtx NOT touched (~190 importers + PRE_WP080_HASH pin); recorder↔replay lockstep, sentinel-core-doom-2p.replay.json re-recorded (new finalStateHash), PRE_WP080_HASH byte-identical; no PAR re-pin (no committed artifact/gate); registry-free regression test; reserves D-24273; game-engine only; standard two-session lane; hard-deps WP-452 + WP-036 + WP-411; 2026-07-29, claude/kind-austin-dde1e0)
- WP-454 — gauntlet-loadout-qualification-guard (RENUMBERED from WP-453 after PR #1083 landed a different WP-453/EC-488/D-24273 (simulation-setup-shuffle-fidelity) first; a build-time UX guard on the cards.legendary-arena.com Loadout Builder closing the wrong-villains/henchmen footgun — WP-444 prefills approved adversaries but leaves them editable and a hand-built loadout has no guard, so a non-qualifying match is only caught server-side post-game matchesApprovedLoadout or rejected at submission par_not_published; adds a NEW pure sibling helper gauntletQualificationCheck.ts checkGauntletQualification comparing the draft villainGroupIds+henchmanGroupIds+playerCount against the mastermind approved GAUNTLET_LOADOUT_MENUS by EXACT set equality on full set-qualified ext_ids (spread-copy sort not in-place; stricter than the server bare-slug segment, sound per D-24131 same-set + exact henchman_key) returning a discriminated status not-a-gauntlet/unoffered-count/qualifies+variantIndex/not-qualifying+approvedVariantCount rendered as a pre-play qualification BADGE, PLUS a pack-sourced adversary LOCK after onPickGauntletLeg with an explicit Unlock adversaries opt-out; advisory only never blocks build/export/launch, server stays sole adjudicator; hero-pool D-24187 out of scope; zero-API no new registry import; reserves D-24274; standard two-session lane apps/registry-viewer only; hard-deps WP-444 + WP-395; 2026-07-29, claude/wp454-gauntlet-loadout-qualification-guard)
- WP-455 — local-workindex-row-check (a LOCAL authoring-time mirror of the CI-only WORK_INDEX row-format drift guard; surfaced by the WP-453/454 arc where a draft row written `**Drafted 2026-07-29; not yet executed**` — not the canonical `**Draft <date>**` — broke the dashboard workIndexRowPattern.test.ts drift guard, failed the whole Dashboard Gates CI job, and was twice mis-diagnosed as a coverage failure; adds a root scripts/check-workindex-rows.mjs exposing pure extractCanonicalPattern(generatorSource) + findUnparsedWpRows(content, pattern) + a guarded main(), reusing the SINGLE-SOURCE WORK_INDEX_ROW_PATTERN extracted from apps/dashboard/scripts/build-governance-snapshot.mjs exactly as the dashboard test does (no third regex copy), validating docs/ai/work-packets/WORK_INDEX.md and exiting non-zero with offending rows listed; new scripts/check-workindex-rows.test.ts (node:test on the pure helpers incl. the exact WP-453 Drafted-row regression) + root package.json workindex:rows:check / workindex:rows:test scripts mirroring roadmap:counts:check/:test; NO git-hook framework (none exists — out of scope), NO apps/dashboard change, NO CI-workflow edit (CI enforcement already exists via Dashboard Gates); reserves D-24275; standard two-session lane, infra/root-tooling only; hard-deps none; 2026-07-29, claude/wp455-local-workindex-row-check)
- WP-456 — legends-mastermind-details-reveal (a CLIENT-ONLY apps/legends-board reveal on legends.legendary-arena.com — each gauntlet in the index gains a clickable Show-details expandable card showing the mastermind's schemes/legs + the approved villain and henchmen groups per player count 1..5; the data is ALREADY published in the gauntlet-index snapshot GauntletIndexEntry.legs + .approvedLoadouts WP-395/D-24199 and already parsed by the board — the reveal renders what is already in hand, completing the D-24199 board-must-SHOW-the-requirement promise D-24186/D-24190; surfaced when a 2p core/magneto challenge-link run had only Brotherhood auto-added and the WP-454 badge flagged it but the board offered no way to learn the 2p variant-0 needs Brotherhood + Enemies of Asgard; new pure buildGauntletDetails helper + tests in gauntletDisplay reusing formatApprovedLoadout/listApprovedLoadouts + GauntletIndexPanel.vue reveal; ZERO-API preserved, vue-only runtime NO registry import, NO publisher/server/snapshot-contract change; reserves D-24276; standard two-session lane, apps/legends-board only; hard-deps WP-395 + WP-343/345; 2026-07-29, claude/wp456-legends-mastermind-details-reveal)
- WP-457 — challenge-link-count-consistency (a CLIENT-ONLY apps/legends-board fix: the gauntlet INDEX Challenge-arrow CTA firstLegChallengeUrl pins the SOLO approved loadout via selectApprovedLoadout(gauntlet) with no count but emits NO playerCount, so the cards builder defaults to DEFAULT_PLAYER_COUNT=2 and evaluates a solo composition at 2 players — the needs-2-villains-has-1 mismatch an operator hit; fix threads the row's selected player count selectionFor(board).playerCount into BOTH selectApprovedLoadout AND buildChallengeUrl so the pinned villains/henchmen and the URL count agree and the link opens a qualifying draft, matching GauntletBoardPanel per-leg links which already do this; the registry-viewer consumer is ALREADY correct via D-24190 applyPreviewToDraft so it is untouched; new pure buildRowChallengeUrl helper + tests in gauntletDisplay + GauntletIndexPanel.vue wiring; zero-API, vue-only, NO registry/server/snapshot change; reserves D-24277; standard two-session lane, apps/legends-board only; hard-deps WP-345 + WP-395 + WP-387; 2026-07-30, claude/wp457-challenge-link-count-consistency)
- WP-458 — collapse-approved-loadouts-to-one (collapse the approved gauntlet villain/henchmen menu from THREE configurations per mastermind to ONE — variant 0, the canonical rotation the challenge links/pack import/badge already default to — so the ONLY ranked-qualification variable is the heroes; casual play free selection is untouched; single-lever change scripts/generate-gauntlet-loadouts.mjs VARIANTS_PER_MASTERMIND 3→1 + regenerate gauntletLoadouts.generated.ts to 110×1 + update the registry test (exactly-one, remove the distinctness test) + comment-only fixes in gauntletLoadouts.ts and apps/server/src/legends/gauntlet.logic.ts; qualification logic matchesApprovedLoadout untouched (one approved config not three); ZERO migration cost because competitive_scores is empty — the cheapest this will ever be; supersedes D-24199 menu-of-three (core rule intact); reserves D-24278; standard two-session lane, registry/tooling; hard-deps WP-395; 2026-07-30, claude/wp458-459-collapse-and-reveal-layout)
- WP-459 — compact-details-reveal-layout (make the WP-456 legends Show-details reveal compact: lay the five player-count blocks in a responsive multi-column grid instead of one tall stacked column, tighten the Villains/Henchmen spacing, keep the Schemes line full-width above the grid — fixes the ~40-row single-column whitespace/scroll complaint; presentation-only CSS + minor markup regrouping in apps/legends-board GauntletIndexPanel.vue, rendered text unchanged, buildGauntletDetails/data untouched; vue-only zero-API, responsive single-column no-overflow on mobile; pairs with WP-458 which cuts each count to one config but does NOT hard-depend on it; NO D-entry — UI polish; standard two-session lane, apps/legends-board only; hard-deps WP-456; 2026-07-30, claude/wp458-459-collapse-and-reveal-layout)
- WP-460 — divider-below-reveal (a one-line scoped-CSS refinement of WP-459: remove the .gauntlet-details border-top so the horizontal divider is no longer ABOVE the Show-details reveal panel — the .gauntlet-row border-bottom already sits BELOW the expanded reveal WP-459 flex-wrap and becomes the sole correctly-placed divider; operator visual request 2026-07-30; presentation-only apps/legends-board GauntletIndexPanel.vue, rendered content unchanged, vue-only zero-API; NO D-entry UI polish; lightweight draft+exec one PR, browser-verified; hard-deps WP-459; 2026-07-30, claude/wp460-divider-below-reveal)
- WP-461 — legends-set-details-publish (server producer half of the operator-requested "Show set details" reveal: the legends publisher emits a self-describing per-set roster into gauntlet-index.json — every mastermind/scheme/villain/henchman with authoritative registry names, plus a usedByGauntlets flag per villain/henchman computed server-side as whether its setAbbr/slug appears in any approved config across the set's masterminds at any count; the coverage gap is only REPORTED not closed, operator chose transparency-now; legends module stays registry-free, rosters injected via server.mjs→index.mjs→legends.scheduler.ts→publishAllBoards like gauntletCatalog; additive optional GauntletIndexSnapshot.sets field; touches legends.types.ts + gauntlet.logic.ts + legends.publisher.ts + wiring; reserves D-24279; hard-deps WP-395 + WP-458; paired with WP-462 client; 2026-07-30, claude/draft-set-details-reveal)
- WP-462 — legends-set-details-reveal (client consumer half: the legends board renders a per-set-group "Show set details" reveal from WP-461's sets roster — Masterminds/Schemes/Villains/Henchmen with each villain/henchman marked fought or unused, text-labelled not colour-only; mirrors the additive snapshot types in snapshotClient.ts never importing the server module, vue-only zero-API, renders the usedByGauntlets flag verbatim no client recompute; degrades to no-reveal on a pre-WP-461 snapshot; touches snapshotClient.ts + gauntletDisplay.ts + GauntletIndexPanel.vue; NO D-entry client mirror of D-24279; hard-deps WP-461; 2026-07-30, claude/draft-set-details-reveal)
- WP-463 — villain-zone-restricted-each-ko (closes the last onAmbush hollow from a live Magneto match 2026-07-30 gitSha b685eff: Juggernaut's "Ambush: Each player KOs two Heroes from their discard pile." — a source-zone-restricted each-player KO the generic ko-hero:each:N never covered because its resolver falls through discard→hand→inPlay while Juggernaut is discard-only; adds an optional zone?:'discard'|'hand' field to VillainEffectDescriptor + the 4-token grammar ko-hero:each:N:zone in the parser and the apply-effect-markers.mjs local validator — NO new primitive so VILLAIN_EFFECT_PRIMITIVES + drift test untouched; new koHeroesFromZoneForPlayer KOs up to magnitude heroes from ONLY the named zone reusing selectKoHeroTarget Wounds-excluded starter-first-then-lex-asc, no cross-zone fallback, short/empty zone reachable no-op, wired into villainEffectKoHero each branch behind descriptor.zone; descriptorKey deliberately OMITS zone so the zone-bearing descriptor reverse-maps to koHeroEachPlayerMag2 and narrates per-target for free WP-316 with the frozen 10-keyword surface untouched; marks Juggernaut core brotherhood/juggernaut ambush ko-hero:each:2:discard + escape ko-hero:each:2:hand regenerated onto data/cards/core.json core-only the other 11 corpus lines carry a filter/choice clause deferred; auto-resolved each-player KO stays auto D-18902; determinism-adjacent no ctx.random/I/O/new-G-field a real hashed KO so replay-hash regen-with-note only if a fixture reveals/escapes Juggernaut; reserves D-24280; game-engine + card-data only; hard-deps D-24266 + WP-202 + WP-252; 2026-07-30, SPEC-onambush-juggernaut)
- WP-465 — score-submit-rejection-copy (honest per-reason copy for the post-match score-submission banner: today useCompetitiveSubmitOnGameover blanket-maps every non-200 from POST /api/competition/scores to `'failed'` with the message "Couldn't submit your score. It may still be counted shortly." — misleading because the hasSubmitted guard means there is NO retry, and the dominant real cause is `par_not_published` (an ad-hoc non-ranked-gauntlet match is permanently ineligible, not a transient failure). SubmitScoreResult.error ALREADY carries the server rejection reason (readErrorCode), so the fix is client-only, zero-API: add a new SubmissionStatus `'ineligible'`, map result.error === 'par_not_published' → 'ineligible' (else 'failed'), add its honest copy + a neutral/info CSS variant in PlayViewport.vue, drop the false "may still be counted shortly" promise from the 'failed' copy, and harden SUBMISSION_MESSAGES to Record<Exclude<SubmissionStatus,'idle'>,string> so a new status forces its message; touches useCompetitiveSubmitOnGameover.ts + .test.ts + PlayViewport.vue; finer per-reason copy for the rare permanent reasons (not_owner/visibility_not_eligible/replay_not_found/match_not_finished) deferred, they keep 'failed'; NO server change, NO D-entry presentation-only like WP-459; hard-deps WP-339 (submit-after-match); 2026-07-30, SPEC-score-submit-copy)
- WP-466 — core-coverage-matrix-compact (operator-directed presentation refinement of the WP-464 Core coverage matrix on legends.legendary-arena.com, goal = remove horizontal scroll: (1) render each mastermind ONCE as a group-header row above its 8 scheme-rows instead of repeating the mastermind name on every row, so the row-label column holds only the scheme name; (2) rotate the villain/henchman column headers 90° via writing-mode:vertical-rl so each of the 11 adversary columns is ~1 line-height wide; (3) let a long scheme name wrap to 2 lines (drop nowrap + cap width). Single-file GauntletIndexPanel.vue — template regroup + scoped CSS; buildCoverageMatrix data UNCHANGED (grouping via the existing isMatrixMastermindStart); vue-only zero-API, no data/contract change, NO D-entry; lightweight draft+exec one PR like WP-460; hard-deps WP-464; 2026-07-30, claude/wp466-matrix-compact)
- WP-467 — matrix-scheme-wrap-fix (bug-fix completing the WP-466 scheme-name wrap on the Core coverage matrix, which did NOT take effect on the deployed page — operator reported the long scheme name stayed one line and bled into the ✓ cells; two root causes: the general .coverage-matrix-table th white-space:nowrap OUT-specifies the .coverage-matrix-rowhead class on the same th, and max-width on a th is ignored under auto table-layout; fix = wrap the scheme name in an inner inline-block span carrying display:inline-block + max-width:8.5rem + white-space:normal + overflow-wrap so the cell sizes to the bounded span; single-file GauntletIndexPanel.vue scoped-CSS + one span, no data/grouping/heading change, vue-only zero-API, NO D-entry; verified at real intrinsic geometry — long span 28px/2-lines bounded 136px, short 14px/1-line, 0px overlap into the ✓ cells; lightweight draft+exec one PR; hard-deps WP-466; 2026-07-30, claude/wp467-scheme-wrap-fix)
- WP-476 — magneto-master-strike-reveal-or-discard (RESERVED: Magneto's Master Strike "Each player reveals an [team:x-men] Hero or discards down to four cards" is only half-implemented — it always auto-discards, never checks the X-Men reveal alternative, and gives no discard choice [live Magneto 1p bug, 2026-07-31]. WP implements the mastermind-strike reveal-or-discard-to-N conditional [reveal-check is HAND-ONLY — the strike fires at start-of-turn so inPlay is empty; deliberately NOT the D-24281 hand+inPlay scope — gates the discard] + an INTERACTIVE discard-to-4 choice mirroring the WP-242/243 KO-a-Hero + WP-470 scry-KO pending-choice machinery; cross-layer engine + arena-client; reserves D-24284; 2026-07-31, claude/spec-bug2-master-strike)
- WP-477 — turnactionbar-discard-gate (RESERVED: WP-476 follow-on. Wire the `hasPendingDiscardChoice` gate — already added to `useTurnActions` at execution — into `TurnActionBar.vue`'s End-Turn / Pass-Priority buttons so they disable-with-tooltip while a Magneto discard-to-limit choice is pending, matching every other pending-choice's UX. `TurnActionBar.vue` was outside the WP-476 allowlist, so the gate is inert today; the engine block-all guard is already authoritative [End Turn no-ops server-side], this is UX parity only. arena-client only [TurnActionBar.vue + PlayDesktop/PlayMobile prop wiring]; no engine change, no contract, no determinism impact; lightweight-lane; 2026-07-31, claude/wp477-turnactionbar-discard-gate)
- WP-471 — per-scheme-gauntlet-registry-loader (arc 1/5: replace the per-mastermind GENERATED gauntlet approved-loadouts with a fully-authored YEAR-KEYED JSON data/gauntlet-configs.json — year→set→mastermind→scheme→{villains[],henchmen[]} ordered pools scaled by REQUIRED_GROUP_COUNTS; seed from today's GAUNTLET_LOADOUT_MENUS so non-Core is byte-identical + the confirmed Core per-scheme swaps; Zod validator + a per-(set×mastermind×scheme) loader getGauntletConfig + getActiveYear; additive foundation, nothing consumes it yet; registry layer; hard-deps WP-458; paired with WP-472+WP-473+WP-474; RENUMBERED off #1118 which took WP-470/EC-505/D-24282; 2026-07-31, claude/spec-per-scheme-gauntlet-arc)
- WP-472 — per-scheme-gauntlet-server-leaderboard (arc 2/5: migrate the server SHARED-TRUTH + leaderboard + publisher — matchesApprovedLoadout/qualifiesAsLegClear gain an ADDITIVE per-scheme lookup [absent-scheme → per-mastermind fallback so WP-473/474 keep building], getGauntletStandings passes the leg scheme, buildSetDetailsCatalog D-24279 coverage recomputes per-scheme, buildPublishedApprovedLoadouts emits per-leg onto GauntletIndexLeg; LANDS D-24283; server layer registry-free; hard-deps WP-471; 2026-07-31, claude/spec-per-scheme-gauntlet-arc)
- WP-473 — per-scheme-gauntlet-run-tracker-launch (arc 3/5: migrate the run-tracker consumers — deriveGauntletRunProgress leg-clear passes the leg scheme into WP-472's additive qualifiesAsLegClear, and the per-leg LAUNCH composition re-keys GauntletRunLaunch from one per-run block to a per-leg map [gauntletRunProgress.logic deriveGauntletRunLaunch + server.mjs resolveGauntletRunProgressInputs + gauntletRun.types] so "Play this leg" fields the leg's adversaries not the mastermind default — closes the leg-can-never-qualify functional break; NO D-entry [consumes D-24283]; server layer registry-free; hard-deps WP-472; 2026-07-31, claude/spec-per-scheme-gauntlet-arc)
- WP-474 — per-scheme-gauntlet-consumers (arc 4/5: legends-board snapshot mirror re-key in lockstep + buildCoverageMatrix per-leg ✓ patterns + reveal/challenge per-leg, and cards-builder checkGauntletQualification [WP-454 badge — add schemeId] + resolveGauntletLegLoadout [WP-444 pack — USE schemeId not echo] + LoadoutBuilder.vue thread schemeId into the badge+prefill call sites; apps/legends-board + apps/registry-viewer; NO D-entry [consumes D-24283]; hard-deps WP-471+WP-472; 2026-07-31, claude/spec-per-scheme-gauntlet-arc)
- WP-475 — per-scheme-gauntlet-arena-client (arc 5/5: migrate the arena-client Play this leg consumer of the per-leg launch map WP-473 added additively to GauntletRunLaunch — gauntletRunApi.ts mirrors the per-leg legLaunch map, MyProfilePage.vue playLeg selects run.launch.legLaunch[leg.schemeSlug] with the per-run block as old-snapshot fallback; closes the primary-play-surface launch break the gates caught; apps/arena-client client tier mirror-not-import; NO D-entry [consumes D-24283]; hard-deps WP-473; 2026-07-31, claude/spec-per-scheme-gauntlet-arc)
- WP-470 — doombot-scry-interactive-choice (the deferred WP-447 phase-2 interactive follow-on Jeff requested from a live Magneto match: Doombot Legion's "Fight: Look at the top two cards of your deck. KO one of them and put the other back." currently AUTO-resolves (villainEffectScryKoOwnDeck KOs the deterministic worst via selectScryKoTarget), giving no player agency — line 172 KO'd a real "Dangerous Rescue" when both top cards were heroes. Upgrades scry-ko to an INTERACTIVE choice mirroring the WP-242/243 KO-a-Hero pending-choice infrastructure: villainEffectScryKoOwnDeck parks a new PendingScryKoChoice{choiceType:'scry-ko', playerID, revealedCardIds:[top≤2]} onto a G.pendingScryKoChoices FIFO queue when deck≥2 (1 → auto-KO, 0 → no-op, mirroring WP-242's 0/1/≥2); a resolveScryKoChoice bgio move (client:false, registered in game.ts) validates front playerID/choiceType + cardId∈revealedCardIds, KOs the chosen card leaving the other on top, front-pops; a hasPendingScryKoChoice block-all guard added to every action-move guard site (game.ts, coreMoves.impl, dodgeCard, fightMastermind, …) + the End-Turn/advanceStage gates; a UIState projection pendingScryKoChoice (+ the WP-249/D-24020 filtered private variant) showing the ≤2 revealed cards; the bot/sim path (ai.legalMoves + simulation.runner + par.aggregator) resolves DETERMINISTICALLY via selectScryKoTarget so par/replay are faithful; and the arena-client PendingScryKoChoicePrompt.vue (mirrors PendingKoHeroChoicePrompt.vue) + useTurnActions + uiMoveName wiring + PlayViewport mount. Cross-layer (game-engine + arena-client), engine-then-client execution order. Determinism: bot-driven fixtures fighting a Doombot now park→resolve (2 moves vs 1 auto-KO) + a new hashed G.pendingScryKoChoices field, so a fixture re-pin is LIKELY + game.test.ts move-set/count +1; reserves D-24282; hard-deps WP-447 (auto scry-ko) + WP-242/WP-243 (KO-a-Hero pending-choice pattern); 2026-07-30, SPEC-doombot-scry-interactive)
- WP-469 — villain-reveal-or-wound (closes the last onFight hollow from a live Magneto match: core/brotherhood/sabretooth's "Fight: Each player reveals an [team:x-men] Hero or gains a Wound." + "Escape: Same effect." — a CONDITIONAL each-player reveal-or-wound, the single most common unimplemented villain mechanic (41 corpus lines). Adds a new primitive `reveal-or-wound` at VILLAIN_EFFECT_PRIMITIVES position 8 (append-only + drift test → 8) parameterized by a predicate {requireKind:'team'|'hero-class', requireValue} via grammar reveal-or-wound:<team|hc>:<value> in the parser + apply-effect-markers.mjs validator (hc→hero-class; requireValue normalized to the cardTraits slug space via normalizeTraitSlug); a villainEffectRevealOrWound handler that for EACH player (sorted) scans their HAND for a hero whose G.cardTraits {team,heroClass} matches (reusing the D-24076 cardTraits lookup, hand-only — narrower than playerMeetsDefeatRequirement hand+inPlay because the printed reveal is a hand action) and gainWounds only on NO match (mirroring gain-wound:each's woundsDrawn bump for the current player); auto-resolved, self-narrates via pushLog (keyword-less → descriptorKey NOT extended, frozen surface untouched). Marks CORE only — 5 cards / 8 markers (sabretooth fight+escape team:x-men, enemies-of-asgard/frost-giant fight+escape hc:ranged, enemies-of-asgard/ymir-frost-giant-king ambush hc:ranged, masters-of-evil/ultron escape hc:tech, radiation/zzzax fight+escape hc:strength — EVERY core "Same effect." Escape marked) + removes stale _unassigned rows; the ~35 cross-set instances + conditional/compound variants deferred. Determinism-adjacent: no ctx.random/I/O/new-G-field, a REAL hashed Wound so re-pin is LIKELY across these common core villains; reserves D-24281; game-engine + card-data only; hard-deps D-24266 + D-24076 + WP-252; RENUMBERED from WP-468 after a concurrent collision (#1114 took WP-468/EC-503 for matrix-dropdown-collapse); 2026-07-30, SPEC-sabretooth-reveal-or-wound)
- WP-468 — matrix-dropdown-collapse (two operator-directed presentation changes to the legends gauntlet index: (1) .download-select <option> text was the dark-theme light colour so it was invisible on the native light dropdown popup until the blue hover highlight — give options explicit dark-on-light #111827/#fff so values read when not highlighted, covers the download count/division selects + the coverage-matrix count select, closed control keeps its light-on-dark styling; (2) collapsible masterminds — each mastermind's 8 scheme rows collapse under a clickable full-width toggle button in the group-header th, aria-expanded + caret, scheme tr v-if on isMastermindExpanded, DEFAULT COLLAPSED so the matrix opens compact and the operator expands one at a time avoiding the 32-row scroll and keeping the rotated headings lined up. Single-file GauntletIndexPanel.vue scoped-CSS + template + a small reactive expandedMasterminds record, buildCoverageMatrix + data untouched, vue-only zero-API, NO D-entry; lightweight draft+exec one PR; hard-deps WP-464 + WP-466 + WP-467; 2026-07-30, claude/wp468-matrix-dropdown-collapse)
- WP-464 — core-coverage-matrix (operator-requested collapsible Core-Set gauntlet-coverage matrix at the bottom of the Core Set group on legends.legendary-arena.com: a TRANSPOSED matrix — rows = the set's 4 masterminds × 8 schemes (32 rows, grouped by mastermind), columns = the 7 villains + 4 henchmen — where a cell shows a checkmark hyperlink to that scheme-leg's cards-builder challenge (approved loadout pinned) iff the villain/henchman is in the mastermind's approved config at the selected player count; a player-count selector defaults to 2p, switchable 1-5; presentation-only client, reads ALREADY-published data (each gauntlet's approvedLoadouts + legs + WP-461 sets roster + existing buildChallengeUrl), zero-API vue-only, no new snapshot/contract; Core-only pilot but the pure builder is generic; NO D-entry presentation-only like WP-459; touches gauntletDisplay.ts + GauntletIndexPanel.vue; hard-deps WP-461 + WP-462 + WP-395; 2026-07-30, claude/draft-core-coverage-matrix)
- WP-452 — bot-ally-coop-winrate-harness (first WP of the Bot Ally Strengthening epic; a deterministic co-op win-rate + loss-cause harness over a fixed (config,seed) matrix, built on the existing runSimulation / evaluateEndgame path, so bot-ally strength changes become a measured NUMBER; classifyCoopOutcome taxonomy = win / loss-scheme-completed / loss-villains-escaped / loss-tie / inconclusive-turn-cap; registry threaded as an explicit CardRegistryReader param (engine imports the type only; the .mjs supplies the reader via createRegistryFromLocalFiles) — the copilot-gate BLOCK fix; game-engine simulation module + a scripts/ entrypoint; reserves D-24272; standard two-session lane; RENUMBERED from WP-444 after a landed collision with the gauntlet cards-consumer; 2026-07-29, claude/wp452-bot-ally-winrate-harness)
- WP-478 — empty-deck-reshuffle-reveal-scry (RESERVED: reveal/look-at-top effects must reshuffle the discard into the deck when the deck is exhausted mid-effect, matching the standard Legendary rule "whenever you need to draw OR reveal cards and your deck is empty, shuffle the discard to form a new deck" and the existing `drawCardsIntoHand` precedent [live Magneto 1p bug, 2026-07-31: Amazing Spider-Man reveal-top-3 at step 15.2.6 and Doombot Legion look-at-top-two-KO-one at 19.2.37 both silently no-op'd on an empty deck while 8 cards sat in the discard]. Fixes BOTH the hero reveal handler `heroEffectReveal` [reverses D-21502's deferred no-reshuffle] AND the villain scry handler `villainEffectScryKoOwnDeck` [reverses WP-447's scry no-reshuffle stance], reusing a shared append-reshuffle helper + the deterministic shuffleDeck; villain side threads `random` through executeVillainAbilities per the villainDeck.reveal.ts RevealContext precedent. Game-engine only; determinism-adjacent [reshuffle consumes ctx.random.Shuffle → fixture finalStateHash regen where a fixture hits the path, no new G field]; LANDS D-24285, SUPERSEDES D-21502; standard two-session lane; Bug 2 "put the rest back in any order" is a SEPARATE follow-on WP; 2026-07-31, claude/wp478-empty-deck-reshuffle-reveal-scry)
- WP-479 — reveal-remainder-reorder (RESERVED: The Amazing Spider-Man's *"Put the rest back in any order"* is not offered — after the reveal-3 draws the cost≤2 cards, the non-drawn remainder is silently left on top in its original order, with no player choice [live Magneto 1p bug, 2026-07-31; the deferred Bug 2 companion to WP-478's Bug 1]. WP adds an INTERACTIVE reorder of the reveal remainder [the revealed-but-not-drawn cards, which sit on top of the deck] via a new PendingReorderChoice + resolveReorderChoice move + block-all guard + UIState projection [face-up remainder] + client ReorderChoicePrompt + deterministic bot/sim default [identity order], mirroring the WP-470 scry-KO + WP-476 discard-to-N pending-choice machinery. Opt-in via a new HeroEffectDescriptor.reorderRemainder flag set from a card marker on Amazing Spider-Man; auto-skips when the remainder is < 2 [nothing to order]. Marks Amazing Spider-Man only; other reveal-and-reorder cards are a data follow-on. Cross-layer engine + arena-client; determinism-adjacent [new hashed pendingReorderChoices G field → finalStateHash + sentinel dual re-pin LIKELY, no ctx.random]; reserves D-24286; standard two-session lane; 2026-08-01, claude/wp479-reveal-remainder-reorder)
- WP-480 — turnactionbar-reorder-gate (RESERVED: WP-479 follow-on, the deferred End-Turn UX gate. WP-479 shipped the reveal-remainder reorder prompt + engine block-all but deferred the client button gate, so the End-Turn / Pass-Priority buttons stay enabled while a reorder is pending [clicking no-ops server-side via the authoritative engine block-all]. WP adds a `hasPendingReorderChoice` param + canEndTurn/canPassPriority gate to `useTurnActions` [reason "Choose the order to put the cards back on top before taking another action."], wires it through `TurnActionBar.vue` + `PlayDesktop`/`PlayMobile`, so the buttons disable-with-tooltip like every other pending choice. arena-client only; no engine change, no contract, no determinism impact; NO D-entry; lightweight lane; 2026-08-01, claude/wp480-turnactionbar-reorder-gate)
- WP-481 — mystique-escape-scheme-twist (RESERVED: Mystique's printed "Escape: Mystique becomes a Scheme Twist that takes effect immediately." is unimplemented — she escaped twice in a live Magneto match [2026-08-01, game-log-magneto.txt lines 180/196] and each escape only left a D-24266 no-handler breadcrumb, costing the player two free Scheme Twists. First villain effect that triggers the SCHEME-TWIST rule pipeline from the escape path. New `become-scheme-twist` VILLAIN_EFFECT_PRIMITIVE [append-only union + array + drift; no-param parse] marked on Mystique's Escape line [core only, 2 copies], with a NO-OP executor handler [breadcrumb suppression only — the executor can't reach the rule pipeline]; the real mechanic is a FIRE-SITE BRIDGE in villainDeck.reveal.ts's escape branch that, when the escaped card carries the marker, runs executeRuleHooks('onSchemeTwistRevealed',{cardId}) + applyRuleEffects [the SAME pipeline the reveal path uses for a revealed scheme-twist card], so the active scheme's twist resolver runs + schemeTwistCount increments + the loss threshold is checked. Mystique still escapes normally [the twist fires her EFFECT, she is not routed into a twist pile — resolvers use twistCardId only for the schemeTwistResolved notableEvent]. Determinism-adjacent: no new G field, but a REAL hashed schemeTwistCount increment + a schemeTwistResolved notableEvent from a new trigger path → finalStateHash + PRE_WP080 sentinel re-pin LIKELY where a fixture escapes Mystique; resolver may consume ctx.random [threaded via the RevealContext]. Contract change [new primitive] → reserves D-24287; standard two-session lane [operator pre-authorized draft+execute]; hard-deps D-24266 [breadcrumb] + WP-252 [parameterized villain-effect vocabulary]; 2026-08-01, claude/wp481-mystique-escape-scheme-twist)
- WP-482 — dococt-reveal-eight-topup (RESERVED: Doctor Octopus's Master Strike reveal-eight branch [co2e/doctor-octopus, "reveal the top 8 cards, discard all non-grey Heroes, put the rest back in random order"] reveals a SHORT deck as-is instead of reshuffling the discard to reveal a full 8 — the standard Legendary reveal-reshuffle rule. WP-397/D-24200 deferred the top-up citing "a reshuffle would need an interaction model this work does not introduce" + "matching the D-21502 empty-deck no-op"; WP-478 later shipped the `reshuffleDiscardIntoDeck` helper, so the blocker is moot. This WP adds the top-up to `resolveDoctorOctopusReveal` [mastermindHandlers.ts]: when deck.length < 8 and discard is non-empty, reshuffle the discard into the deck [reusing reshuffleDiscardIntoDeck, wrapping the handler's shuffleFunction as a ShuffleProvider] before computing revealCount = min(8, deck.length). The remainder still returns in RANDOM order [the printed text says "random order", NOT "any order" — so NO player reorder; D-24200's shuffle-back is faithful and unchanged]. REVERSES D-24200's "short deck revealed as-is" clause AND the D-24285 reveal-eight carve-out → reserves D-24288. game-engine only [mastermindHandlers.ts + test]; determinism-adjacent [the top-up consumes an extra ctx.random.Shuffle when the deck is short → finalStateHash re-pin if a fixture hits a short-deck reveal-eight; D-24200 notes the pinned dr-doom fixture does NOT reach this branch, so likely none — confirm empirically]; DRAFT-ONLY this session [operator asked to draft; execution is a later session]; standard two-session lane; hard-deps WP-397/D-24200 + WP-478/D-24285 [the reshuffle helper]; 2026-08-01, claude/wp482-dococt-reveal-eight-topup)
- WP-483 — per-scheme-gauntlet-cards-consumers (RESERVED: the registry-viewer/cards half that WP-474 split off [arc 4/5 of the per-scheme gauntlet variety; WP-474 shipped the legends-board half only because the WP-471 `getGauntletConfig` loader is Node-only — `packages/registry/src/gauntletConfigs.ts` `readFileSync`s `data/gauntlet-configs.json` at module load and is reachable only via the registry root barrel, which breaks the Vite browser build]. This WP [1] adds a BROWSER-SAFE per-scheme registry export mirroring the `gauntletLoadouts.generated.ts` precedent — a generated `gauntletConfigs.generated.ts` [the parsed config baked to a TS literal by a new `scripts/generate-gauntlet-configs.mjs`, `--check` freshness gate like `gauntlet:loadouts:check`], `gauntletConfigs.ts` refactored to import the literal instead of `node:fs` [preserving `getGauntletConfig`/`getActiveYear`/`validateGauntletConfigs` signatures + the undefined→menu-fallback semantics], and a new `./gauntletConfigs` subpath export; and [2] threads `schemeId` through the cards consumers — adds `schemeId` to `GauntletQualificationInput` + an injected per-scheme composition [resolved by the LoadoutBuilder orchestrator via the new browser-safe `getGauntletConfig`, menu-fallback when undefined], makes `resolveGauntletLegLoadout` USE `input.schemeId` [today it echoes but selects scheme-blind], and threads `schemeId` through `LoadoutBuilder.vue`'s `checkGauntletQualification` + prefill call sites. Cross-layer Registry producer + App consumer [downward, allowed]; consumes D-24283, NO new D-entry; standard two-session lane; hard-deps WP-471 + WP-472 + WP-474; User-Visible Surface = cards.legendary-arena.com [D-24026 live-verify]; 2026-08-01, claude/eager-goldwasser-7565b9)
- WP-484 — effect-implementation-index (RESERVED: first slice of the generated Effect Implementation Index [the ewiki wiki/debug-effects.md direction] — a derived JOIN of the two committed mechanic ledgers [hero + villain] into one published, schema-validated, dual-scope `data/metadata/effect-implementation-index.json` carrying per card×mechanic {scope,status,handler,wp,decision}. BOTH ledgers ALREADY derive handler=`<file>#<primitive>`+wp+decision for resolved rows [hero 230/647 rows carry a handler, villain likewise]; `""` only on unsupported/unmarked rows [both scopes] — verbatim pass-through, no backfill, no fabrication. New `EffectImplementationIndexSchema` in packages/registry/src/schema.ts [data-only zod, /schema subpath] + `effect-index(:check)` scripts + a `hero-effect-coverage` CI freshness step, mirroring WP-269/card-mechanics.json exactly. NO engine touch, NO /debug/effects viewer, NO runtime tracing [all future WPs]. User-Visible Surface = none — infrastructure [D-24026 inverted]. Contract [new derived-artifact shape] → reserves D-24289; standard two-session lane [operator asked to DRAFT; execution is a later session]; hard-deps WP-269/D-24046 [feed precedent] + WP-271 [villain ledger]; 2026-08-01, claude/wp484-effect-implementation-index)
- WP-485 — villain-effect-vocab-tier-a (RESERVED: Tier-A batch of the Core villain-effect-vocabulary extension — implements 3 currently-unmarked ("unmarked-ability"/no-handler, D-24266) Core villain Fight abilities needing NEW AUTO-RESOLVE primitives [no interactivity, no city-space, no determinism/persistence]: Enchantress "Fight: Draw three cards" [current-player draw], Destroyer "Fight: KO all your [team:shield] Heroes" [KO-all current heroes by trait, hand+in-play], Baron Zemo "Fight: For each of your [team:avengers] Heroes, rescue a Bystander" [dynamic magnitude = team-hero count]. Follow-on tiers mapped in WP §Context: Maestro + Viper + ymir/melter/paibok/hydra-kidnappers [interactive / victory-pile predicate]; whirlwind/abomination/the-lizard [city-space state-shape]; endless-armies/the-leader [recursive villain-deck play]; doctor-octopus [cleanup-draw override, determinism]. game-engine [villainEffects.execute + villainAbility.types + setup + tests] + card-data markers [villain-effect-markers.json → apply-effect-markers.mjs → core.json]; new closed-union primitives → reserves D-24290; standard two-session lane; surfaced by a live Magneto/Spider-Foes gauntlet game 2026-08-01 [Doc Ock hollow]; 2026-08-01, claude/wp485-villain-effect-vocab-tier-a)
- WP-486 — instant-defeat-with-bystander (RESERVED: implements the unimplemented hero keyword "Defeat a Villain or Mastermind that has a Bystander" — Silent Sniper [core/msp1/3dtc black-widow], surfaced by a live Magneto/Thor 2p game 2026-08-01 where the ability did nothing [unmarked → invisible to the per-hero ledger, no runtime breadcrumb]. NEW hero keyword `defeat-with-bystander` [HERO_KEYWORDS + drift array/union + MVP_KEYWORDS + HERO_EFFECT_HANDLERS + apply-hero-ability-markers.mjs VALID_TOKEN_PATTERN + hero-ability-markers.json for the 3 reprints → card-data regen + hero-ledger/effect-index/provenance regen]; an INTERACTIVE pending-choice target selection [PendingDefeatChoice + resolveDefeatChoice move + block-all guards + UIState projection + D-24011 filter + deterministic bot/sim default + arena-client prompt], defeating the chosen Villain / Mastermind-tactic WITHOUT attack via the existing defeat path; mandatory-if-able, auto-resolve when exactly 1 legal target, no-op at 0. game-engine + card-data + arena-client; new pending-choice + keyword contract → reserves D-24291; standard two-session lane [operator asked to DRAFT]; templates WP-479 [pending-choice] + WP-485 [keyword/vocab]; 2026-08-01, claude/wp486-instant-defeat)
- WP-487 — debug-effects-viewer (RESERVED: piece 3/3 of the ewiki wiki/debug-effects.md recommended direction [piece 1 = the generated Effect Implementation Index, shipped WP-484; piece 2 = runtime effect tracing, a separate future WP] — a READ-ONLY `/debug/effects` viewer on the DASHBOARD [apps/dashboard, where the ewiki explicitly places it, beside the shipped /coverage + /debug] that consumes the committed dual-scope `data/metadata/effect-implementation-index.json` [WP-484 / D-24289] via a build-time bundle copy [the /coverage `build-coverage-ledger.mjs` precedent, NOT R2, NOT mock-mode], validates it with `EffectImplementationIndexSchema` [@legendary-arena/registry/schema], and renders a searchable per-card / per-mechanic view with the status + handler + wp + decision columns, mirroring CoveragePage.vue/useCoverageLedger.ts. Consumes the WP-484 index VERBATIM; runtime traces + descriptor→handler granularity are OUT of scope [not built]. App-layer only [apps/dashboard], additive; NO engine/registry/server touch, NO G/determinism surface. User-Visible Surface = dashboard /debug/effects [D-24026 live-verify]. Reserves D-24292 [host + read-only-derived + trace-deferred lock]; standard two-session lane [operator asked to DRAFT; execution is a later session]; hard-deps WP-484/D-24289 [the index] + WP-269/D-24046 [feed-consumer precedent]; renumbered from WP-485 on 2026-08-01 [WP-485/EC-520/D-24290 taken by a parallel villain-effect-vocab session, #1157]; 2026-08-01, claude/wp-debug-effects-viewer)
- WP-488 — effect-runtime-tracing (RESERVED: piece 2/3 of the ewiki wiki/debug-effects.md direction [piece 1 = the Effect Implementation Index shipped WP-484; piece 3 = the /debug/effects viewer shipped WP-487] — a runtime-only STRUCTURED EFFECT TRACE emitted when a card effect descriptor is actually dispatched during play, recording the primitive + params + which handler-map entry ran + which zone helpers mutated state + any SECONDARY fire site [the Mystique/escape-Twist case D-24287, where the villain executor handler is a no-op but the real Scheme Twist fires at the villainDeck.reveal escape site]. Extends the existing hollow-effect detector plumbing [hollowEffect.record.ts] to make "handler reached but no mutation" and "handler never reached" both visible in ONE trace. game-engine [villain/hero effect executors + diagnostics]; determinism-CRITICAL: the trace is runtime-only, NEVER part of the hashed G [G stays JSON-serializable; diagnostics hash-excluded per the WP-451 posture] so it should NOT re-pin the finalStateHash/PRE_WP080 oracles [confirm at draft]. SCOPE FORK to resolve at draft [pending engine-surface research]: trace as a hash-excluded G.diagnostics field vs a pure module-level side channel. Reserves D-24294; standard two-session lane [operator asked to DRAFT; execution is a later session]; hard-deps WP-257/D-24034 [hollow detector] + D-24287 [escape-site case]; 2026-08-02, claude/wp-effect-tracing)
- WP-489 — villain-effect-vocab-tier-b-city-spaces (LANDED 2026-08-03: Tier-B batch of the Core villain-effect-vocabulary extension [follow-on to WP-485/D-24290 Tier A] — implements the NAMED-CITY-SPACE-conditional Core villain Fight abilities. Adds `CITY_SPACE_NAMES` as a PURE ENGINE CONSTANT (no new G field) + threads the fought `cityIndex?` from fightVillain to the handlers (trailing-optional, shuffleContext? precedent) + a universal `requireCitySpaces` descriptor gate (@<space> marker suffix). 2 AUTO-RESOLVE cards reusing existing primitives (NO new VillainEffectPrimitive): Abomination capture-bystander:3@streets+bridge (adds a count) + the Lizard gain-wound:each-other@sewers (adds an each-other target). Whirlwind DEFERRED to Tier D [interactive KO-your-heroes]. Markers land in the hashed villainAbilityHooks so re-pin only if a committed fixture deck includes these 2 cards [none today]. game-engine + card-data markers; reserves D-24295; standard two-session lane; index->name binding operator-confirmed at execution; renumbered from WP-486->488->489 (#1160 instant-defeat took 486, #1169 effect-runtime-tracing took 488) on 2026-08-02; 2026-08-02, claude/wp486-villain-city-spaces)
- WP-490 — destroyer-shield-ko-basic-cards (RESERVED: fixes the WP-485/D-24290 Destroyer surfaced hollow-in-practice by the D-24026 live-verify [Loki/Thor 2p, 2026-08-03: `Fight effect: KO'd 0 of your shield Hero(es)` while holding a S.H.I.E.L.D. Trooper + Agent]. The three basic S.H.I.E.L.D. cards [starting-shield-agent, starting-shield-trooper, pile-shield-officer] are synthetic components with no G.cardTraits entry, so ko-heroes-current-by-trait:team:shield matched zero. Narrow fix [operator ruling 2026-08-03]: widen the KO handler ONLY [koHeroMatchesTraitOrBasicShield] to name those three ext_ids for a team:shield predicate; the shared cardTraitMatches/playerHasHeroMatchingTrait/countPlayerHeroesMatchingTrait UNCHANGED [65 corpus synergies + Baron Zemo rescue-count untouched]. Relocates SHIELD_AGENT/TROOPER_EXT_ID -> pilesInit.ts [cycle-free leaf]. No new G field/primitive/card-data/ledger; no re-pin. Reserves D-24296; lightweight lane [drafted + executed same session]; hard-deps WP-485/D-24290; 2026-08-03, claude/wp490-destroyer-shield-ko)
- WP-492 — villain-effect-vocab-tier-d-whirlwind (LANDED 2026-08-03: Tier D [first interactive card] of the Core villain-effect-vocabulary arc — implements Whirlwind "Fight: If you fight Whirlwind on the Rooftops or Bridge, KO two of your Heroes." Reuses WP-489's universal requireCitySpaces location gate [Rooftops=2 / Bridge=4; NO gate-engine change] + the WP-242 interactive KO-a-Hero multi-KO queue [pendingKoHeroChoices already supports N entries: UIState projects remaining=queue length WP-243, the resolve move front-pops N times, the arena-client prompt already renders "remaining>1", the bot default drains the front repeatedly — so NO arena-client / resolve-move / projection / bot change]. The ONE engine change: generalize the ko-hero target:'current' handler to a magnitude M [park only when eligible>M, else auto-KO min(eligible,M); M=1 stays byte-identical to koHeroCurrentPlayer]. NO new VillainEffectPrimitive [reuse ko-hero with the existing magnitude? field], NO new pending-choice type, NO new G field. New marker grammar ko-hero:current:N [N>=2] + Whirlwind marker ko-hero:current:2@rooftops+bridge. Determinism: markers land in the hashed villainAbilityHooks + the KO pushes to the hashed pendingKoHeroChoices, so re-pin only if a committed fixture deck includes core/masters-of-evil [Whirlwind's group; verify at execution — none expected]. game-engine + card-data markers [single layer, Tier A/B shape]; reserves D-24298; standard two-session lane [interactive → NOT lightweight]; deferred from WP-489/D-24295 Tier B; hard-deps WP-489/D-24295 [location gate] + WP-242/D-24007 [interactive KO queue] + WP-252/D-24023 + D-24266 + D-24034; 2026-08-03, claude/wp492-whirlwind-tierd)
- WP-491 — hero-ledger-per-design-granularity (RESERVED: the hero mechanic ledger [scripts/hero-mechanic-ledger.mjs buildLedger] merges every hero card DESIGN under one hero extId, so /debug/effects + /coverage cannot say WHICH of a hero's ~4 physical card designs carries each effect — Black Widow [core] shows attack-per-count / defeat-with-bystander / draw / optional-ko-reward / rescue all keyed to `core/black-widow`, when the source data cleanly attributes them per design [mission-accomplished = draw + rescue; dangerous-rescue = optional-ko-reward; covert-operation = attack-per-count; silent-sniper = defeat-with-bystander]. Villains ALREADY carry per-card granularity [villain ledger rows key cardName + a per-card key]; ADDITIVE ATTRIBUTION (not a row re-partition): the ledger keeps ONE row per [extId x mechanic] and gains a `designs` array [slug-sorted {slug,name} of the carrying design(s); [] for hero-level unmarked], so the ledger summary + /coverage metrics + card-mechanics.json stay byte-identical and no row key collides [a per-design-row split would make 246 duplicate rows]. Flows an additive optional `designs:{slug,name}[]` through the effect-implementation-index [build-effect-implementation-index.mjs + EffectImplementationIndexSchema additive field] into a new Design column on BOTH the /debug/effects AND /coverage viewers. Touches: scripts/hero-mechanic-ledger.mjs -> hero-mechanic-ledger.{json,csv} regen [ledger:heroes:check] -> build-effect-implementation-index.mjs + packages/registry EffectImplementationIndexSchema -> effect-implementation-index.json regen [effect-index:check] -> apps/dashboard /debug/effects [useEffectIndex + view]. Multi-artifact, crosses generated-artifact + registry-schema + dashboard surfaces + regenerates TWO CI-gated artifacts -> NOT lightweight lane; standard two-session lane [operator asked to DRAFT + chose full heroes->villain parity]. Reserves D-24297; hard-deps WP-484/D-24289 [effect index] + WP-487/D-24292 [/debug/effects viewer]; surfaced by operator inspecting /debug/effects 2026-08-03; 2026-08-03, claude/wp491-hero-ledger-per-design)
- WP-494 — villain-effect-vocab-tier-d-viper (LANDED 2026-08-03: Tier D of the Core villain-effect-vocabulary arc — implements Viper [core/hydra/viper] "Fight: Each player without another HYDRA Villain in their Victory Pile gains a Wound." + "Escape: Same effect." A CONDITIONAL each-player wound gated on a per-player VICTORY-PILE villain-group predicate [NOT interactive]. Adds ONE new keyword-less VillainEffectPrimitive gain-wound-unless-victory-villain-group [count 12→13, append-only union+array+drift per D-24034] + an additive descriptor field victoryVillainGroup + the grammar gain-wound-unless-victory-villain-group:<groupSlug>. Handler mirrors villainEffectRevealOrWound [sorted per-player iteration, supply-bound, woundsDrawn current-only, self-narrate]: wound each player UNLESS their victory pile holds another villain of the named group. PATH B [no new G field, no broad re-pin]: the group ext_id prefix is DERIVED from the fought card's own ext_id [split on the unambiguous `-villain-` infix → setAbbr; prefix `${setAbbr}-villain-${groupSlug}-`], excluding the fought cardId [`another`]; victory-pile villains are matched by startsWith. Marked on BOTH fight+escape [Escape: Same effect]; the onEscape fire site already runs executeVillainAbilities. game-engine + card-data markers [single layer, Tier A/B shape]; reserves D-24299; standard two-session lane [new primitive + contract change]; deferred from WP-489 Tier B; unmarked-ability today [D-24266]. Determinism: new primitive → markers land in the hashed villainAbilityHooks so re-pin only if a committed fixture deck includes core/hydra [none expected; verify at execution]; NO new G field. Hard-deps WP-469/D-24281 [reveal-or-wound conditional-each-player precedent] + WP-202/D-20203 [each-player iteration] + WP-252/D-24023 [descriptor vocab] + WP-489/D-24295 [Tier B fire-site + self-narration] + D-24266 + D-24034; 2026-08-03, claude/wp494-viper-tierd)
- WP-493 — mechanic-provenance-backfill (RESERVED: backfills scripts/coverage/mechanic-provenance.json with 17 traced mechanic→{wp,decision} attributions for effects that are EXECUTABLE with a live handler but render blank WP/Decision on /debug/effects + /coverage — surfaced by operator inspecting /debug/effects 2026-08-03 [Warriors Three `draw`]. Root cause: the provenance map [read VERBATIM by the hero+villain mechanic ledgers → effect-implementation-index] was only ever seeded from later mechanic-implementing WPs; the foundational MVP keywords + 4 recent villain primitives were never attributed, so the ledgers pass "" through and the viewer shows blank. Backfills 13 hero mechanics [draw→WP-022/D-2201, rescue→WP-215/D-21501, undercover→WP-282/D-24060, size-changing→WP-290/D-24074, gain-wound-self/each→WP-364/D-24156, ko-wound-reward→WP-382/D-24183, discard-to-play→WP-383/D-24184, victory-villain-attack→WP-285/D-24099, and 4 decision-only mechanics (wp:"" — INFRA/bug-fix, no formal WP) put-any-number-bottom-hq→D-24132 / put-bottom-hq-icon-reward→D-24133 / optional-put-bottom-hq→D-24130 / return-zero-cost-discard→D-24139] + 4 villain primitives [scry-ko-own-deck→WP-447/D-24267, reveal-or-wound→WP-469/D-24281, become-scheme-twist→WP-481/D-24287, gain-attached-hero→WP-450/D-24270]. DATA-ONLY: 1 source edit [the provenance map] + regenerated hero-mechanic-ledger.{json,csv} + villain-mechanic-ledger.{json,csv} + data/metadata/effect-implementation-index.json [ledger:heroes:check + ledger:villains:check + effect-index:check]. NO code/engine/schema/dashboard change; cites only EXISTING historical decisions [reserves NO new D]; verbatim-passthrough fills the ledgers+index automatically. Determinism N/A [coverage artifacts, hash-excluded]. Regenerates THREE CI-gated artifacts → NOT lightweight lane [WP-491 precedent]; standard two-session lane. Coordinates with WP-491 [additive designs column on the SAME artifacts — orthogonal fields; whoever lands 2nd regenerates]. Hard-deps WP-484/D-24289 [effect index] + the mechanic-implementing WPs cited above; 2026-08-03, claude/wp493-provenance-backfill)
- WP-495 — provenance-decision-backfill (RESERVED: WP-493 follow-on. Fills the DECISION column for the remaining executable rows on /debug/effects + /coverage that have a WP but a blank Decision [75 rows across 5 rendered mechanics]. WP-493 filled the never-attributed mechanics; this fills the 6 provenance-map entries that carry wp but decision:"" — the WP-185 villain fight-effect keywords + the two count-scaled hero frameworks. Backfills koHeroCurrentPlayer→D-18503 [its specific resolution decision, mirroring captureBystander→D-18506], gainWoundEachPlayer / gainWoundCurrentPlayer / heroDeckTopToEscape→D-18502 [the WP-185 five-keyword vocabulary-lock decision that defines them; no mechanic-specific D], attack-per-count→WP-247/D-24016, optional-ko-reward→WP-248/D-24019. DATA-ONLY: 6 decision values in scripts/coverage/mechanic-provenance.json [wp already present, unchanged] + regenerated hero + villain ledgers + effect index [ledger:heroes:check + ledger:villains:check + effect-index:check]. NO code/schema/dashboard change; cites only EXISTING historical decisions [reserves NO new D]; verbatim-passthrough fills the ledgers+index automatically. Row identity untouched — only the decision cell of 75 executable rows changes; summaries + card-mechanics.json byte-identical. Determinism N/A [coverage artifacts, hash-excluded]. Standard two-session lane [regenerates 3 CI-gated artifacts, WP-491/WP-493 precedent]. Hard-deps WP-493/EC-528 [the backfill it extends] + WP-484/D-24289 [effect index]; 2026-08-03, claude/wp495-provenance-decision-backfill)
- WP-496 — coverage-decision-column (RESERVED: LIGHTWEIGHT LANE. Adds a Decision column to the /coverage by-card table so it shows the SAME WP+Decision provenance /debug/effects does. The /coverage by-card table already renders Card/Design/Set/Mechanic/Status/WP/Handler but OMITS Decision, while the by-mechanic table on the same page + /debug/effects both show it; LedgerRow already carries `decision` and the build-time coverage bundle is a full byte-copy of the committed ledger so `row.decision` is already present in the viewer — the only change is rendering it. Purely additive Vue display column [th + td] in apps/dashboard/src/pages/coverage/CoveragePage.vue [+ a test assertion]; NO schema/type change [LedgerRow.decision exists], NO generated-artifact change, NO engine/data touch, NO new decision. App-layer only, zero determinism. Follow-on to WP-493/WP-495 [which filled the decision values]. Hard-deps WP-259/D-24035 [the /coverage viewer] + WP-493 + WP-495 [the filled decisions]; 2026-08-04, claude/wp496-coverage-decision-column)
- WP-497 — mastermind-tactic-onfight-framework (RESERVED: game-engine. First WP of the Mastermind-Tactic-Fight arc. Wires defeatMastermindTacticCore to dispatch a per-tactic onFight resolver [mirrors the WP-386/388 per-mastermind mastermindStrikeHandler dispatcher, NOT a data-driven marker vocabulary — operator ruling 2026-08-04], and implements the first faithful tactic: co2e/doctor-octopus "Octet of Valence Electrons" = a per-turn hand-size override [draw 8 next hand instead of HAND_SIZE 6]. Adds a new OPTIONAL, lazily-created hashed G field [per-player next-onBegin-fill override; NOT seeded in buildInitialGameState] consumed + cleared at the play-phase onBegin fill in place of HAND_SIZE [this engine has NO end-of-turn cleanup draw, so "your new hand this turn" ≡ the defeating player's next onBegin fill — Magneto MAGNETO_HAND_SIZE_LIMIT + HAND_SIZE precedent]. Determinism: lazy-create means no committed fixture [empty-registry PRE_WP080, core/dr-doom sentinel] ever creates the key → NO re-pin expected [verify both oracles at execution; drift = STOP, never blind-re-pin per reference_hashed_g_field_dual_repin]; onFight uses ctx.random.* only where a resolver reveals/shuffles [Octet does neither]. No Magneto-cap interaction [one mastermind per match]. Follow-on tactics [Octal Octyls scry-draw-1, High Octane hero-deck-reveal-gain, Absolute Octarchy each-player-reveal-or-wound] are separate WPs hard-dep on this one. The `// tactic text effects are WP-024` comment in fightMastermind.ts is STALE [WP-024 did strike/scheme exec; tactic Fight was scoped OUT of WP-316/386/388]. Reserves D-24300; standard two-session lane [new G field + determinism surface]. Hard-deps WP-019 [tactics MVP] + WP-024 [strike dispatcher pattern] + WP-386/388/D-24188/D-24192 [per-mastermind resolver precedent] + WP-236 [HAND_SIZE onBegin draw]; surfaced live 2026-08-04 co2e Doc Ock co-op match KdHnMXaOPin; 2026-08-04, claude/wp497-tactic-onfight)
- WP-498 — hero-return-on-discard-ability (RESERVED: implements the unimplemented Cyclops "Unending Energy" [core/cyclops/unending-energy, count 3] reactive ability "If a card effect makes you discard this card, you may return this card to your hand" — surfaced by operator [Determination/Optic Blast discard-to-play cost force-discarded it with no return option, 2026-08-04]. Renumbered from a WP-497 draft that lost a same-number race to #1215/#1217 [mastermind-tactic-onfight]. FULL-FAITHFUL [operator-chosen 2026-08-04]: ALL card-effect hand→discard sources trigger the return, not just the discard-to-play cost. Core work: [1] new HeroKeyword `return-on-discard` [bare marker [keyword:return-on-discard], union+HERO_KEYWORDS array lockstep, drift 33→34] + the FIRST reactive HeroAbilityTiming `onDiscard` [HERO_ABILITY_TIMINGS 5→6; every current timing is play-initiated]; [2] a NEW forced-discard CHOKEPOINT discardFromHand[+checkReturnOnDiscard] — no shared hand→discard mover exists today — through which ALL 5 enumerated card-effect discard sites route [resolveDiscardToPlay, discardChoice.resolve, mastermindHandlers.discardCardFromHand, schemeTwistResolvers.discardHand, dodgeCard], guarded by a cheat-proof drift test asserting no hand→discard mutation outside the chokepoint; [3] an OPTIONAL "you may" pending choice PendingReturnOnDiscard [{decline:true}|{cardId} per the resolveOptionalPutBottomHQ template; NOT the mandatory resolveDiscardToPlay shape] + resolveReturnOnDiscard move [client:false] + block-all guards + ai.legalMoves bot default [accept=strictly beneficial] + UIPendingReturnOnDiscard chooser-only projection + arena-client ReturnOnDiscardPrompt.vue. Determinism: pendingReturnOnDiscard lazy-init [never seeded] → NO PRE_WP080/hashGameState re-pin; sentinel fixture finalStateHash re-pins ONLY if a recorded fixture plays+returns Unending Energy [none expected; verify sentinel-core-doom-2p at execution]. Cross-layer [engine+arena-client+card-data], one WP [WP-383 discard-to-play mirror-precedent]; standard two-session lane. Reserves D-24301; hard-deps WP-383/D-24184 [discard-to-play site + keyword substrate] + D-24139 [return-zero-cost-discard pending precedent] + WP-248/D-24019 [optional-decline pending template] + WP-476/D-24284 [mastermind reveal-or-discard discard site]; 2026-08-04, claude/wp498-return-on-discard)
- WP-499 — join-by-match-id (LANDED 2026-08-04 — arena-client 1174 pass, no re-pin: standard two-session lane [reserve-time lightweight guess upgraded at draft — proper UI coverage touches LobbyView.test.ts, a 6th code/test file, and the surface is multiplayer-join-adjacent, both failing lightweight eligibility]. Adds a lobby "Join by match ID or link" input to apps/arena-client so a player can paste a raw match ID or a full invite link [?route=lobby&match=<id>] and join directly — identity-agnostic, mirroring the already-working WP-369 copy-link flow, and the reliable path to play with a brand-new friend while the never-wired handle-claim system [claimHandle has zero non-test call sites; surfaced by operator 2026-08-04] blocks by-@handle match invites. Parses the reference [pure matchReference util], fetches that single match [read-only GET /games/legendary-arena/:id via a new lobbyApi.fetchMatch — works for unlisted matches the public list omits], and joins the first OPEN seat via the EXISTING joinMatch [POST /api/match/join; no server change, no new endpoint]. App-layer only [apps/arena-client], additive UX + input-validation, zero determinism/persistence/identity/sync-protocol surface. Reserves D-24302; hard-deps WP-307/D-24092 [authenticated match-gate join] + WP-369 [copy-join-link ?match deep link]; surfaced by operator "friend username invite errors" 2026-08-04; 2026-08-04, claude/join-by-match-id)
- WP-500 — auto-assign-handles (LANDED 2026-08-05 — server suite exit 0, assignAutoHandle DB-gated + backfill verified against local Postgres, no re-pin: server. Track 2 CORE of the friend-invite fix — makes every account reachable by @handle so by-@handle invites + friend-requests actually resolve. Auto-assign a CHANGEABLE handle derived from display_name at first-sign-in provisioning [new pure deriveHandle() slug helper + assignAutoHandle() idempotent collision-safe assign; sets handle_canonical+display_handle, leaves handle_locked_at NULL = unlocked/changeable] + a one-time backfill script for the existing NULL-handle universe [claimHandle was never wired → ALL rows NULL; D-24302/WP-499 context]. AMENDS the WP-101 handle mutual-presence invariant [handle_locked_at now INDEPENDENT: NULL=auto-assigned/changeable, now()=explicit claim/locked]. No schema migration [handle cols exist since migration 008]. OUT: the change-handle endpoint + profile UI [paired follow-up] + rewiring claimHandle's IS-NULL guard [rides the follow-up]. Reserves D-24303; hard-deps WP-101 [handle contract] + WP-174 [provisioning] + WP-499/D-24302 [join-by-id + diagnosis]; standard two-session lane [identity + contract change]; 2026-08-05, claude/wp500-auto-assign-handles)
- WP-501 — change-handle (LANDED 2026-08-05 — handle logic+route 28/28, arena-client 1188/0, no migration/type change: cross-layer [server + arena-client]. Track 2 follow-up — lets a signed-in user CHANGE their @handle [the WP-500 auto-assigned slug, e.g. operator's `jeff2`→`jeff` if free]. New changeHandle(accountId, requestedHandle, db) [reuses validateHandleFormat; UPDATE handle_canonical+display_handle WHERE ext_id=$ AND handle_locked_at IS NULL — FREELY CHANGEABLE while unlocked, the D-24303 model; a THIRD disjoint writer of the two columns, NEVER handle_locked_at; 23505→handle_taken; empty RETURNING + locked → new handle_locked code, else unknown_account] + PATCH /api/me/handle [authenticated-session] + MyProfilePage handle-edit field + ownerProfileApi. Freely-changeable per operator's auto-assign-but-changeable choice [D-24303]; old handle released [no redirect]. OUT: explicit lock/claim action + rewiring claimHandle's IS-NULL guard [deferred — nobody is locked, so change works for all today]; change rate-limit/cooldown [defer]. Touches the WP-101 handle.types.ts closed union [+handle_locked, drift lockstep] → needs D. Reserves D-24305; hard-deps WP-101 [handle contract + validateHandleFormat] + WP-500/D-24303 [auto-assign + changeable model] + WP-104 [owner profile edit surface]; standard two-session lane; surfaced by operator wanting to change auto-assigned `jeff2` 2026-08-05; 2026-08-05, claude/wp501-change-handle)

- WP-502 — match-endgame-controls (LANDED 2026-08-05 — engine 2330 + arena-client 1195 + server competition suites green, arena-client typecheck 0, no re-pin [runtime-only counter; initial G unchanged]. Two player-facing play-surface controls: "Play Again" [relaunch the same loadout at seat 0 on match end — arcade replay] + "End Game" [confirm-gated; latches the new MATCH_ENDED_EARLY endgame counter so the match ends for EVERY seat]. An early-ended match is NEVER scored [additive endedEarly marker on EndgameResult/UIGameOverState; server rejects the submission with a new ended_early reason + client skips the POST]. Engine: MATCH_ENDED_EARLY counter [evaluateEndgame checks it FIRST → endedEarly 'tie'] + endMatchEarly server-only move [NOT a CORE_MOVE_NAME] + UIState endedEarly projection. Cross-layer [engine+server+arena-client]; standard two-session lane. Reserves D-24306; 2026-08-05, claude/wp-502-match-endgame-controls)
- WP-503 — doc-ock-villain-fight-hand-size (RESERVED: BLOCKED on WP-497. Implements the core spider-foes Doctor Octopus VILLAIN Fight ability "Fight: When you draw a new hand of cards at the end of this turn, draw eight cards instead of six." [core/spider-foes/doctor-octopus, copies 2] — surfaced live 2026-08-05 as a no-handler hollow [Magneto match, Doc Ock fought at 17.2.18: unmarked-ability at onFight]. DISTINCT from the co2e MASTERMIND Doc Ock "Octet of Valence Electrons" [WP-497]: same "draw 8" effect but a VILLAIN-effect-vocabulary card fired from villainEffects.execute onFight, NOT the mastermind-tactic dispatcher. REUSES WP-497's shared hashed field G.handSizeOverrides?:Record<string,number> + its play-phase onBegin consumption [fill target = handSizeOverrides[player] ?? HAND_SIZE, then clear]. Adds ONE new keyword-less VillainEffectPrimitive override-next-hand-size [count 13→14, append-only union+array+drift per D-24034] + the marker grammar override-next-hand-size:<N> [N=8] on the Doc Ock Fight line; handler sets G.handSizeOverrides[currentPlayer]=magnitude at onFight [lazy-init], self-narrated. Determinism: writes the WP-497-hashed field so re-pin only if a committed fixture fights core/spider-foes/doctor-octopus [verify]; NO new G field [reuses WP-497's]. LIVE COMPOSITION vs Magneto: unlike the co2e mastermind Doc Ock, the villain Doc Ock CAN co-occur with any mastermind [it did in the surfacing match — Magneto + spider-foes]; the Magneto post-draw hand trim [MAGNETO_HAND_SIZE_LIMIT=4] + this override both resolve at WP-497's single onBegin point — verify order/interaction at execution. game-engine + card-data markers [single layer, villain-effect-vocab Tier A/B shape]; reserves D-24307; standard two-session lane. HARD-DEP WP-497/D-24300 [handSizeOverrides field + onBegin consumption — currently DRAFTED not executed → this WP is BLOCKED until WP-497 lands] + WP-252/D-24023 [villain descriptor vocab] + WP-485/D-24290 [Tier A auto-resolve fire-site] + D-24266 + D-24034; 2026-08-05, claude/wp503-doc-ock-villain-fight)
- WP-505 — captured-cards-under-adversaries (RESERVED: render captured cards on the PLAY BOARD beneath city villains and the mastermind. All THREE capture stores are already tracked in G; the gap is projection+render only. (1) Villain-captured HEROES from HQ [G.villainAttachedHeroes, FACE UP] already project as UICityCard.attachedHeroes [WP-214] and survive the audience filter [uiState.filter.ts] — client-only: CityRow renders each as a face-up hero image under its villain. (2) Villain-captured BYSTANDERS [G.attachedBystanders, FACE DOWN] project NOWHERE — deliberately excluded [uiState.build "do NOT flatten G.attachedBystanders"; D-12805 Interp-B / D-12806 safe-skip]; add a NEW count-only UICityCard.attachedBystanderCount:number via the 5-step Board-Visible Field contract [type→build→filter passthrough→filter test→diagnostics snapshot], client renders an "N captured" badge. (3) Mastermind-captured BYSTANDERS [G.mastermind.attachedBystanders, FACE DOWN] already project as mastermind.attachedBystanders [WP-154] — client-only: MastermindTile swaps its text <li> list for the same count badge. Face-down bystanders show as COUNT ONLY [Jeff 2026-08-06: identity stays hidden = face-down semantics]; face-up heroes show as card art. Reverses the D-12806 city-villain safe-skip [reserves D-24311]. Cross-layer game-engine[UIState projection]+arena-client[render]; the required-field add backfills UICityCard fixtures in both packages [project_arena_client_uistate_backfill_recurrence]. Standard two-session lane. Hard-deps WP-214/EC-246 [attachedHeroes projection+filter] + WP-154 [mastermind.attachedBystanders] + WP-128/D-12805/D-12806 [audience-filter matrix + the safe-skip this reverses]; 2026-08-06, claude/captured-cards-under-adversaries)
- WP-504 — friend-request-by-account-id (LANDED 2026-08-06 — server friendships routes 23/23 + isWellFormedAccountId units pass [DB-gated, serialized]; arena-client 1219/1219 + vue-tsc 0; pnpm -r build 0; D-24308 Active; no engine surface so no re-pin. account_not_found across all FIVE drift sites; FR-2 no-accountId-on-the-wire test green. full-stack handle-or-UUID matching on the friend-request surface. POST /api/me/friends/requests accepts EITHER handle OR accountId [a well-formed v4 UUID = the invitee's own AccountId, shared out-of-band — a copy-paste capability like WP-499's match link]; exactly one of the two must be present. New shared pure guard isWellFormedAccountId in identity.logic.ts + new shared client helper parsePlayerIdentifier [handle-vs-UUID discriminator]; accountId path resolves via findPlayerByAccountId → new account_not_found [404] code [FriendApiErrorCode union+FRIEND_API_ERROR_CODES array server-side + client mirror + both drift tests, lockstep], then the SAME WP-355 block/cooldown/rate-limit chain + sendFriendRequest [self_friendship/already_* handled in the logic layer]. Client add-friend input accepts "@handle or Account ID". FR-2 UNTOUCHED [output redaction — friend responses still emit handle+displayName only, never accountId; this is INPUT only, emits no identifier]. Cross-layer server+arena-client; standard two-session lane. Reserves D-24308; hard-deps WP-350/351 [friend API + FR-2] + WP-355/D-24147 [abuse controls] + WP-499/D-24302 [copy-paste-identifier trust model]; 2026-08-05, claude/wp504-friend-request-by-uuid)
- WP-506 — magneto-tactic-crushing-shockwave (RESERVED: second resolver in the WP-497/D-24300 mastermind-tactic-Fight arc [after WP-497 Doc Ock Octet]. Implements core Magneto's tactic Fight ability "Fight: Each other player reveals an [team:x-men] Hero or gains two Wounds." [core-mastermind-magneto-crushing-shockwave]. Surfaced live 2026-08-06 [Magneto co-op match: tactics Bitter Captor/Crushing Shockwave/Xavier's Nemesis all defeated at ZERO effect because dispatchTacticOnFight has only the Octet resolver — every other tactic id falls through to the silent no-op]. New resolveCrushingShockwave in rules/tacticHandlers.ts + a dispatch branch keyed by the tactic ext_id; NO-CHOICE deterministic per-player resolver mirroring mastermindHandlers.resolveCo2eMagnetoStrike [reveal-X-Men-or-penalty] but three deltas: REVEAL not discard [an X-Men holder loses nothing], TWO Wounds not one, and EACH OTHER player [exclude ctx.currentPlayer, the defeating player]. Reuses the X-Men-in-hand scan + gainWound wound-supply logic [supply-empty = logged no-op, moves never throw]. Determinism: mutates wounds pile + player discard, but only when a Magneto Crushing Shockwave tactic is defeated — no committed fixture does so → finalStateHash N/A [same reasoning as WP-497 Octet]. Single-layer game-engine; standard two-session lane. Reserves D-24312; hard-dep WP-497/D-24300 [dispatchTacticOnFight dispatcher this extends] + D-24192/D-24284 [deterministic-auto-resolve precedent over a blocking multi-player pending-choice]; 2026-08-06, claude/wp506-507-magneto-tactic-fight)
- WP-507 — mastermind-tactic-effect-index-coverage (RESERVED: closes the observability gap that hid the WP-497 arc. Mastermind TACTIC cards appear NOWHERE in data/metadata/effect-implementation-index.json [the /debug/effects surface] because that index is a join of the hero+villain mechanic ledgers ONLY and tactics are in NEITHER ledger — so /debug/effects shows just 2 Magneto rows [hero dodge + mastermind-villain unmarked] and never the four tactic Fight abilities, leaving an unimplemented tactic invisible until played [as reported 2026-08-06]. Adds a tactic feed to scripts/build-effect-implementation-index.mjs enumerating each mastermind's tactic cards [ext_id ${setAbbr}-mastermind-${slug}-${tacticSlug} from the mastermind card data], with status/handler/WP derived from the tacticHandlers.ts dispatch table [executable when a resolver exists, unmarked/unsupported otherwise] — same status vocabulary as the villain feed. CI-gated regen [effect-index:check]. Scope: index generator + regenerated JSON artifact + the tactic rows [four core Magneto + one co2e Doc Ock Octet + the remaining unimplemented tactics]. Single-layer tooling/data; standard two-session lane. Reserves D-24313; hard-dep WP-497/D-24300 [the tactic dispatcher whose coverage this surfaces]; 2026-08-06, claude/wp506-507-magneto-tactic-fight)
- WP-508 — escaped-pile-carry-away-midtown-loss (RESERVED: first WP of the resource-loss-scheme-fidelity epic [D-24178 records the 6 resource-loss schemes as unmodeled, losing on a twist-count doom-clock proxy]. Fixes Midtown Bank Robbery's printed Evil Wins "8 Bystanders carried away by escaping Villains" — unmodeled today, and the escape path RETURNS captured bystanders to G.piles.bystanders [resolveEscapedBystanders "prevent memory leaks"], the opposite of "carried away", so the condition is untracked. Surfaced live 2026-08-07 [core Magneto/Midtown co-op ended scheme-wins at twist 8 with an EMPTY escaped pile + dominant hero board]. Scope: [1] carry escaped villains' captured bystanders into G.escapedPile not supply [D-24314]; [2] new data-only SchemeTwistConfig.resourceLossCondition {kind:'escaped-pile-count',cardType,threshold} + pure evaluator countEscapedPileByType/applyEscapedPileResourceLoss that sets SCHEME_LOSS when escapedPile holds >= threshold entries of cardType [classified via G.villainDeckCardTypes], checked in the escape path — evaluateEndgame stays counter-only [D-24315]; [3] suppress the twist-count proxy for any scheme declaring a resourceLossCondition; [4] wire Midtown [bystander >= 8]. Out of scope: Negative Zone + ESCAPE_LIMIT-per-scheme [next WP], stack-depletion schemes, conversion schemes. Determinism: carry-away + SCHEME_LOSS are hashed; re-pins only a fixture that escapes a villain WITH captured bystanders — none committed → finalStateHash + PRE_WP080 byte-identical expected [drift = STOP]. No new G field. Single-layer game-engine; standard two-session lane. Reserves D-24314 + D-24315; hard-deps WP-200/D-24178 [twist config framework this extends] + WP-153 [escapedPile]; 2026-08-07, claude/escaped-pile-resource-loss)
- WP-515 — civil-war-2p-hero-deck-sizing (RESERVED: SEVENTH + FINAL WP of the resource-loss-scheme-fidelity epic — Super Hero Civil War's printed setup "If only 2 players, use only 4 Heroes in the Hero Deck." WP-510 shipped the hero-deck-depletion loss (D-24318) but flagged 2p-full-deck may under-loss because at 2p the 5-hero deck is too big to run out; this WP adds the missing setup sizing so the loss is reachable at 2p. Mirrors WP-511/D-24321's resolveEffectiveWoundsCount pattern EXACTLY: a new resolveEffectiveHeroDeckIds in setup/schemeSetupSizing.ts returns config.heroDeckIds.slice(0,4) for Civil War at exactly 2 players (drops one hero group; deterministic first-4), else unchanged; applied POST-VALIDATION at the buildHeroDeck call in buildInitialGameState. No loss change (D-24318 already ships the depletion loss). ~3-4 file game-engine WP. Determinism: gated to Civil War + 2p → non-Civil-War + 3-5p Civil War byte-identical; sentinel is not Civil War → finalStateHash/PRE_WP080 byte-identical expected (STOP + deliberate re-pin only if a committed Civil War 2p fixture exists). Reserves D-24328; hard-dep WP-510/D-24318 [merged]; 2026-08-09, claude/wp515-civilwar-2p)
- WP-514 — secret-invasion-skrull-conversion (RESERVED: SIXTH WP of the resource-loss-scheme-fidelity epic; second CONVERSION scheme (Secret Invasion of the Skrull Shapeshifters), reuses WP-513's converted-card overlay adding origin 'skrull'. The HARD conversion: setup shuffles 12 random Heroes from the Hero Deck INTO the Villain Deck (determinism-sensitive cross-deck manipulation — no hook exists; sequence against the two independent shuffles); Heroes-in-villain-deck count as Skrull Villains with attack = Hero VP+2 (new resolveFightCost mode reading G.cardVictoryPoints); "if you defeat that Hero, you gain it" (defeat-to-gain — converted hero routes to player discard not victory pile, modifies shared defeatCityVillainCore); Twist: highest-cost HQ Hero → Sewers (city[0]) as a Skrull Villain (new resolver reusing HQ-cost selection + refillHqSlot); Evil Wins: 6 Heroes in the escaped pile (escaped-converted-count origin 'skrull' threshold 6, reuses WP-513's kind). Reserves D-24326 + D-24327; hard-dep WP-513/D-24324 [merged]; 2026-08-09, claude/wp514-secret-invasion)
- WP-513 — killbots-converted-villains (RESERVED: fifth WP of the resource-loss-scheme-fidelity epic; first CONVERSION scheme (Killbots-first, self-contained — Secret Invasion is WP-514). Models "Replace Earth's Leaders with Killbots": the 18 villain-deck Bystanders (already data-driven via villainDeckBystanderCount:18) convert to Killbot Villains — typed 'villain' in villainDeckCardTypes for NATIVE reveal/fight/escape routing (RevealedCardType closed union NOT extended) + a new G.convertedVillainOrigins overlay marking origin 'killbot' (identity for count/attack/display) [D-24324]. Killbot attack = a per-scheme "twists next to this Scheme" counter (G.counters, seeded 3, +1 per killbots twist) via a new resolveFightCost converted-attack mode; loss = escaped-converted-count origin 'killbot' threshold 5 (reuses WP-508 escape-branch call site, counts by overlay not by 'villain' which would include real villains); twist proxy suppressed [D-24325]. ~14-file game-engine WP (types + setup + rules + resolver + economy + UIState); over-size split flagged for the gates. Reserves D-24324 + D-24325; hard-dep WP-508 [merged]; 2026-08-08, claude/wp512-conversion-schemes-draft)
- WP-511 — legacy-virus-wound-depletion-loss (RESERVED: fourth WP of the resource-loss-scheme-fidelity epic. Models Legacy Virus printed Evil-Wins "If the Wound stack runs out" by reusing WP-510's pile-depleted kind widened to pile:'wounds' (G.piles.wounds.length), AND introduces the epic's first scheme-specific SETUP SIZING: Legacy Virus's wound stack built at 6xnumPlayers (its printed "6 Wounds per player") instead of the flat 30. Sizing is a POST-VALIDATION engine override at the buildGlobalPiles call (compute in buildInitialGameState via a pure resolveEffectiveWoundsCount helper; the 30 woundsCount floor D-24032 is NOT changed — it governs the requested config, the scheme rule governs the built pile which is below the floor by design). Both ship together (loss without sizing = unreachable; sizing without loss = still on proxy). No tie interaction (final-turn latch ignores wounds). DELIBERATE hash re-pin of the SOLE Legacy Virus sentinel fixture sentinel-core-doom-2p.replay.json finalStateHash (30->12 wounds; regenerate via record-game-fixture.mjs --input); PRE_WP080_HASH is NOT Legacy Virus and MUST stay ec64506a. 9-file game-engine allowlist (incl. new setup/schemeSetupSizing.ts + setup-layer buildInitialGameState.ts). Civil War 4-heroes-2p sizing -> WP-513; conversion schemes later. Reserves D-24320 + D-24321; hard-dep WP-510 [merged]; 2026-08-07, claude/wp511-legacy-virus-wound-depletion)
- WP-510 — civil-war-hero-deck-depletion-loss (RESERVED: third WP of the resource-loss-scheme-fidelity epic. Models Super Hero Civil War printed Evil-Wins "If the Hero Deck runs out". Introduces a NEW resourceLossCondition kind {kind:'pile-depleted',pile:'heroDeck'} on the WP-508 framework + a new applyPileDepletionResourceLoss helper wired to the play-phase turn.onMove hook [beside latchFinalTurnIfDeckExhausted — central per-move chokepoint that catches twist-driven hero-deck depletion, not only recruitHero]. Civil War's own twist [KO all HQ heroes] drains the hero deck so the loss is reachable at default size. Tie-override is FREE: SCHEME_LOSS precedes FINAL_TURN_TIE in evaluateEndgame + the tie-resolution guard, so no finalTurn.logic.ts change. Civil War ONLY — Legacy Virus deferred to WP-511 [needs per-player wound-stack sizing 6/player else unlosable]; conversion schemes WP-513. 7-file game-engine allowlist [incl. game.ts 01.5 wiring]; standard two-session lane. Reserves D-24318 + D-24319; hard-dep WP-508/WP-509 [merged]; 2026-08-07, claude/wp510-civil-war-hero-deck-loss)
- WP-509 — negative-zone-villain-escape-loss (RESERVED: second WP of the resource-loss-scheme-fidelity epic. Models Negative Zone Prison Breakout printed Evil-Wins "If 12 Villains escape" AND retires the generic escapedVillains>=ESCAPE_LIMIT[8] loss from evaluateEndgame. REUSES the WP-508 escaped-pile-count kind with cardType 'villain' [villains-only per rulebook §Escaped Villains — henchmen excluded; escaped villains are typed 'villain' in villainDeckCardTypes]; NO new kind, NO schemeResourceLoss.ts/types change. evaluateEndgame stays counter-only [escape loss now flows through SCHEME_LOSS in the escape path]. coopOutcome keeps its 'loss-villains-escaped' category + escapedVillains>=ESCAPE_LIMIT branch [reworded to a loss-cause heuristic — NegZone losses carry escapedVillains>=12]; ESCAPED_VILLAINS counter + ESCAPE_LIMIT constant retained. 6-file game-engine allowlist; standard two-session lane. Reserves D-24316 + D-24317; hard-dep WP-508/D-24314/D-24315 [merged]; 2026-08-07, claude/wp509-negative-zone-villain-loss)
- WP-512 — coverage-backdrop-portals (RESERVED: the D-24322 coverage-backdrop follow-up. WP-511 switched the runtime-observed hollows sweep [runtime-observed-hollows.mjs] + co-op yardstick [coop-winrate.mjs] off Legacy Virus [deck-dependent loss stopped terminating the solo sweep] onto Cosmic Cube, costing coverage [artifact ~16->10 distinct mechanics, dashboard totalObs 178->163]. Switches both backdrops to core/portals-to-the-dark-dimension — a FAITHFUL true-twist-loss scheme [printed "Twist 7: Evil Wins!"] that loses at twist 7 via the existing MVP_SCHEME_TWIST_THRESHOLD fallback [NO engine change] and whose Dark-Portal twists buff the board rather than adding Wounds to hero decks, so the competent bot plays more abilities. Scaffolded at draft: 10->12 mechanics [+rooftops+streets], dashboard totalObs 163->188 [above the pre-WP-511 178], resolvedObs 59 unchanged, percentResolved 36.2->31.4; 312/312 games terminate ~3s. Rejects the explicit-lossThreshold engine option [a SCHEME_TWIST_CONFIGS entry needs a required resolverId — contract-touching for zero behavioral gain]. ~16 NOT recoverable [Legacy-Virus-specific, now deck-dependent; Portals plateaus at 12 across 8/16/24 seeds] so SEEDS_PER_BOARD stays 8. Adds a fast deterministic-termination guard [throw ProbeFailure if any swept game did not reach endgame] so a future non-terminating backdrop fails loudly, not via the WP-511 silent timeout. Coop yardstick switched in lockstep [cosmetic — 0% bot win-rate is backdrop-independent, a Bot-Ally-epic concern]. No packages/** change; no finalStateHash/PRE_WP080 interaction. 4-file allowlist [scripts x2 + coverage artifact + dashboard test]; standard two-session lane. NOTE: WP-510/511 prose loosely forward-referenced "WP-512" for the scheme-fidelity epic's continuation — narrative pointers, not ledger reservations [and they disagree]; the deferred scheme-fidelity items stay unreserved. Reserves D-24323; hard-dep WP-511/D-24322 [merged]; 2026-08-08, claude/priceless-morse-ed9637)
- WP-516 — ymir-fight-ko-wounds (RESERVED: wire the unmarked Fight of core Enemies-of-Asgard villain Ymir, Frost Giant King [D-24266 unmarked-ability, surfaced in a 2p Secret Invasion + Magneto live-verify 2026-08-09]. New fifteenth VillainEffectPrimitive 'ko-wounds-current-hand-and-discard' [append-only D-24034, 14->15] — keyword-less no-param auto-resolve [WP-485/WP-503 Tier-A shape]: the current [fighting] player KOs every Wound [WOUND_EXT_ID 'pile-wound'] from their own hand + discard to G.ko. "Choose a player" collapses to current player + "any number" to KO-all in solo/co-op [rational chooser, pure upside]; hand+discard ONLY [not in-play — Wounds are never played, unlike ko-heroes-current-by-trait]. No new parser arm [no-param parses via the generic terminal branch]. Marker on Ymir Fight line only [Destroyer/Enchantress/Frost-Giant already marked — NOT a cluster]. No ctx.random; no committed fixture includes/fights enemies-of-asgard so finalStateHash/PRE_WP080 unchanged. Single-layer game-engine + card-data + ewiki-vocab; lightweight lane [one branch, two-commit topology, one PR]. Reserves D-24329; no hard-dep; 2026-08-09, claude/clever-blackburn-5a6ccf)
- WP-517 — dr-readiness-dashboard-tile (RESERVED: an operational-health tile on dashboard.legendary-arena.com showing DR-drill readiness — last drill date + result, next due, overdue flag — derived from the "DR drill due" GitHub issues the dr-drill-reminder workflow opens/closes [ops-health surface, NOT /vision]. CI+server+dashboard: amend dr-drill-reminder.yml to add a "- [ ] Drill passed" checkbox (PASS/FAIL source) + a new apps/server /api/dash/dr-readiness feed reading the drill issues via the GitHub API + a mock-first apps/dashboard tile. Reserves D-24330; hard-dep dr-drill-reminder workflow [#1298, merged]; single session, two-commit topology; renumbered from WP-516 after Ymir shipped under 516/551/329 [#1300/#1301]; 2026-08-09, claude/spec-dr-readiness-dashboard)
- WP-518 — skrull-gain-fight-overlay-narration (RESERVED: game-engine. Secret Invasion "If you defeat that Hero, you gain it" fires correctly in the engine — the defeated Skrull routes to the defeating player's discard [WP-514/D-24327, fightVillain.ts defeatCityVillainCore] — but the center-screen fight overlay narrative [composeFightNarrative → the hashed notableEvents fightResolved narrative] says only `Fought "X"`, identical to an ordinary villain kill, so the gain lands silently and players read it as "the gain isn't happening" [surfaced live 2026-08-10, core Magneto/Secret-Invasion co-op TYSkhUJwNsZ]. Threads a `skrullGained` boolean [captured in defeatCityVillainCore before the convertedVillainOrigins delete] into composeFightNarrative, appending ` and gained the Hero into the active player's discard pile`. Additive: empty for every non-Skrull defeat → non-Skrull narratives byte-identical. Determinism: notableEvents narrative IS hashed [no observation-channel exclusion], but the clause fires only on Skrull defeat and NO committed fixture defeats a Skrull [sentinel = core/brotherhood + dr-doom, PRE_WP080 = synthetic; the sentinel replay fires no fight event] → sentinel/PRE_WP080 hashes UNCHANGED [verify at execution; drift = STOP]. No new G field, no card data, no marker pipeline. Reserves D-24331; hard-dep WP-514/D-24327 [merged] + WP-319/D-24105 [composeFightNarrative overlay contract]; single session, two-commit topology; 2026-08-10, claude/wp518-skrull-gain-narrative)
- WP-519 — melter-fight-ko-cullable-deck-top (RESERVED: wire the unmarked Fight of core Masters-of-Evil villain Melter [D-24266 unmarked-ability, surfaced in a 2p co-op Secret Invasion vs Dr. Doom live-verify 2026-08-10, match 25b12dd6, both copies -00/-01]. Printed Fight: "Each player reveals the top card of their deck. For each card, you choose to KO it or put it back." New sixteenth VillainEffectPrimitive 'ko-cullable-each-deck-top' [append-only D-24034, 15->16] — keyword-less no-param auto-resolve: each player [Object.keys(G.playerZones).sort()] reveals their deck top [reshuffling discard when empty, scry-ko precedent D-24285], and the fighting player's rational-cooperative choice collapses to auto-KO only CULLABLE cards [Wound / basic starting S.H.I.E.L.D. Agent|Trooper — the selectScryKoTarget tiers-1-2 "worst-worthy" set, D-24267] to G.ko, KEEPING every real Hero/Officer on top. Never KOs a real Hero, so it dodges the WP-470 scry-ko agency bug — no pending choice, no client change. Melter marker only [Baron Zemo/Ultron/Whirlwind already executable — NOT a cluster; the co2e MoE all-unmarked set is a separate whole-set gap]. Uses ctx.random via reshuffle + adds a hashed Fight hook to the masters-of-evil group, which IS referenced by scoring fixtures — finalStateHash/PRE_WP080/sentinel hash impact MUST be verified at execution and re-recorded via record-game-fixture.mjs if shifted. Single-layer game-engine + card-data + ewiki-vocab; standard two-session lane [two-commit topology, one PR]. Reserves D-24332; no hard-dep; 2026-08-10, claude/amazing-lewin-ce0830)
- WP-520 — co2e-masters-of-evil-mark-covered-lines (RESERVED: card-data-only marking WP for the co2e [Legendary 2nd-edition] Masters-of-Evil villain group [all four villains currently unmarked — the whole-set gap flagged by WP-519]. Marks ONLY the three timing lines that reduce to EXISTING villain primitives [WP-185 conservatism-over-coverage curation]: co2e Melter Fight -> ko-cullable-each-deck-top [verbatim core Melter text; HARD-DEP WP-519/D-24332]; co2e Ultron Escape "Each player reveals a [hc:tech] Hero or gains a Wound" -> reveal-or-wound:hc:tech [existing, WP-469/D-24281]; co2e Whirlwind Fight "If you fight Whirlwind on the Rooftops or Bridge, KO one of your Heroes" -> ko-hero:current@rooftops+bridge [bare current + universal @location gate D-24295, magnitude-1 IMPLICIT — ko-hero:current:1 is REJECTED by the parser, D-24298]. NO engine change; adds a co2e block to inputs/villain-effect-markers.json + regen co2e.json + ledger:villains/effect-index. DEFERRED to a follow-up epic [each needs a NEW VillainEffectPrimitive, enumerated in D-24333]: Baron Zemo Ambush [capture 1 + 1 per avengers Hero in HQ — dynamic HQ-team-count capture]; Ultron Fight [choose an HQ tech Hero, KO it or give it to a player — interactive HQ-hero KO-or-gift]; Whirlwind Ambush [two city Villains swap spaces — board manipulation]; plus the two variable-attack passives [Baron Zemo +1 atk per attached Bystander, Ultron +1 atk per HQ tech Hero — a resolveFightCost class, NOT hollow timing lines]. Card-data-only, standard two-session lane. Reserves D-24333; hard-dep WP-519/D-24332 [#1306]; 2026-08-10, claude/co2e-moe-marking-draft)
- WP-521 — co2e-baron-zemo-ambush-capture-per-hq-avengers (RESERVED: first of the three co2e Masters-of-Evil deferred-primitive WPs [the D-24333 epic]. co2e Baron Zemo Ambush "Baron Zemo captures a Bystander. Then he captures another Bystander for each [team:avengers] Hero in the HQ." New auto-resolve VillainEffectPrimitive: attach ONE Bystander to Baron Zemo then ONE more for each hero in G.hq whose team trait is avengers — a NEW HQ-by-trait COUNT scan [no existing primitive counts HQ heroes by team; capture-bystander:N is a FIXED count, rescue-bystanders-current-by-trait-count counts YOUR in-play heroes and RESCUES]. Attach via attachBystanderToVillain at the Ambush fire site [award deferred to defeat, the existing capture-bystander Ambush semantics], supply-bounded on G.piles.bystanders; deterministic, no ctx.random, self-narrates [keyword-less]. game-engine + a co2e baron-zemo ambush marker row [added to the WP-520 co2e MoE block, or creates it if absent]. Reserves D-24334; hard-dep none; 2026-08-10, claude/co2e-moe-new-primitives-draft)
- WP-522 — co2e-ultron-fight-hq-tech-hero-ko-or-gift (RESERVED: second of the three co2e MoE deferred-primitive WPs [the D-24333 epic]. co2e Ultron Fight "Choose a [hc:tech] Hero from the HQ. Either KO that Hero or choose a player to gain it." New VillainEffectPrimitive. FIDELITY FORK [locked in the WP]: (A) auto-resolve collapse [RECOMMENDED, the WP-519 Melter precedent] — in co-op, gifting an HQ hero into a player's DISCARD strictly dominates KO'ing it [the player gains the card AND the HQ refills], so a rational cooperative chooser always gifts the highest-value [hc:tech] HQ hero to the current player; deterministic, no pending choice, small handler; (B) interactive pending-choice [the scry-ko WP-470 shape] — park which tech hero + KO-vs-gift + which player; engine + UIState projection + client prompt + resolve move. Gift routes to the player DISCARD [the WP-514/D-24327 defeat-to-gain path]; KO to G.ko; HQ slot refills [captureHeroFromHq/refillHqSlot, hc:tech-filtered highest-cost selection]. game-engine [+ client if fork B] + a co2e ultron fight marker. Reserves D-24335; hard-dep none; 2026-08-10, claude/co2e-moe-new-primitives-draft)
- WP-523 — co2e-whirlwind-ambush-swap-two-city-villains (RESERVED: third of the three co2e MoE deferred-primitive WPs [the D-24333 epic]. co2e Whirlwind Ambush "Two Villains in the city swap spaces." New VillainEffectPrimitive and the FIRST City board-position manipulation [no existing primitive moves villains between City spaces]. The card names NO chooser and NO specific two villains, so the WP LOCKS a deterministic swap rule [candidates from the City structure: swap the two frontmost-toward-escape occupied villain spaces / frontmost-with-rearmost / etc. — the most faithful "disrupt the board" reading, decided at draft]. Swaps positions of two VILLAIN-typed occupants of G.city [henchmen excluded via G.villainDeckCardTypes]; no-op when fewer than two city villains. Ambush fire site [cityIndex is undefined for Ambush; the swap is space-relative not fought-space-relative]. Determinism via the positional rule [no ctx.random; a tie among equal candidates uses a deterministic order]. game-engine + a co2e whirlwind ambush marker. Reserves D-24336; hard-dep none; 2026-08-10, claude/co2e-moe-new-primitives-draft)
- WP-525 — secret-invasion-six-hero-play-lobby-projection (RESERVED: the play-lobby half of the Secret Invasion "6 Heroes" split epic [pairs with WP-524; the two SHIP AS A SET so play.legendary-arena.com's create flow never regresses]. WP-524 makes the ENGINE + registry-viewer builder require 6 hero groups for core/secret-invasion-of-the-skrull-shapeshifters; the arena-client PLAY lobby is scheme-BLIND — it compares the loadout against a flat GET /api/match/setup-requirements projection [PLAYER_COUNT_SETUP verbatim, no schemeId] and DISABLES Create on any mismatch [playerCountRequirements.ts / LobbyView.vue], so with WP-524 alone a correct 6-hero Secret Invasion loadout is BLOCKED by the play lobby AND a 5-hero one is rejected by the engine -> the scheme becomes un-creatable via the play lobby. This WP makes the requirement projection scheme-aware end-to-end: the server GET /api/match/setup-requirements gains an optional schemeId input [projecting resolveEffectiveHeroCount per scheme], the arena-client lobby threads the selected schemeId into fetchSetupRequirements + computePlayerCountMismatches, and the server autoplay default-loadout sizing [autoplay.mjs heroDeckIds.slice(0, heroCount)] becomes scheme-aware so a Secret Invasion autoplay/bot-ally build sizes to 6. Mirrors the WP-370/371/372 player-count-setup layer-split. server + arena-client [+ api-endpoints.md if the endpoint shape changes]. Reserves D-24338; hard-dep WP-524/D-24337; 2026-08-11, claude/wp524-525-secret-invasion-heroes)
- WP-526 — url-preview-setup-requirement-display (RESERVED: LIGHTWEIGHT LANE. Follow-up to the Secret Invasion "6 Heroes" epic surfaced from a live cards.legendary-arena.com test: a shared deep-link URL [?schemeId=...] loads a read-only PREVIEW [useSetupFromUrl, WP-114/EC-116/D-114XX — "Edit this loadout" is the only draft-promotion path], but the preview does NOT show the scheme's setup requirement, so a shared Secret Invasion link reads "needs 5 heroes" [from the empty editor draft below] until you click Edit. WP-524's scheme-aware count is correct but the preview surface never got taught about it. This WP adds a scheme-aware setup-requirement line to LoadoutPreview.vue — mirrors the builder's "For a N-player match: X villain groups, Y henchmen groups, Z heroes, W villain-deck bystanders." using getPlayerCountSetup(previewDocument.playerCount) + resolveEffectiveHeroCount(previewDocument.composition.schemeId, playerCount, baseHeroCount) [both already on the browser-safe @legendary-arena/registry/playerCountSetup subpath from WP-524]. previewDocument already carries playerCount + schemeId, so it is fully contained in LoadoutPreview.vue [+ a new LoadoutPreview.test.ts]. Respects the locked read-only preview-first design [purely additive display]. registry-viewer only; additive UX; no new contract, no D-entry [applies D-24337 to the preview surface]. Reserves EC-561; hard-dep WP-524/D-24337 [landed]; 2026-08-11, claude/wp526-preview-requirement)
- WP-524 — secret-invasion-six-hero-setup-requirement (RESERVED: Secret Invasion of the Skrull Shapeshifters prints "Setup: 8 Twists. 6 Heroes. Skrull Villain Group required." but the setup pipeline enforces only the standard per-player-count table [PLAYER_COUNT_SETUP, heroCount 5 at 2-4p]; WP-514/D-24326 deliberately scoped only the 12-hero conversion + combat and DROPPED the "6 Heroes" clause, so live 2p matches run with 5 hero groups. This WP makes the hero-count REQUIREMENT scheme-aware — the OPPOSITE class from the two shipped schemeSetupSizing overrides [Legacy Virus wounds, Civil War 4-hero at 2p], which post-validation DOWNSIZE a built pile; "6 Heroes" is a requirement INCREASE, so the operator must actually select 6 groups and every requirement checker must demand 6 not 5. Adds a scheme-aware resolveEffectiveHeroCount[schemeId, numPlayers, baseHeroCount] in packages/registry [sibling to PLAYER_COUNT_SETUP] threaded through checkPlayerCountComposition [registry] + validatePlayerCountComposition [game-engine matchSetup.validate, the Game.setup gate] + the server create-gate + the registry-viewer/arena-client lobby requirement display. Locked design [review points in the WP §Context]: FLAT 6 for this scheme wherever base<6 [2/3/4p 5->6; 5p already 6; solo-1p 3->6 flagged]; scope to the hero-count fix ONLY ["Skrull Villain Group required" was already satisfied in the reported match -> Out-of-scope follow-up]. Determinism: alters the hero-deck build for this scheme only; no committed fixture uses Secret Invasion [D-24327 grep], executor confirms suite-green, non-scheme games byte-identical. Cross-layer -> standard two-session lane. Reserves D-24337; hard-dep WP-514/D-24326 [landed] + WP-370/D-24165 [PLAYER_COUNT_SETUP]; 2026-08-10, claude/wp524-secret-invasion-six-heroes)
- WP-527 — dashboard-money-widget-source-badge (RESERVED: fix the Monetization page's mislabeled source/freshness badges. PaidActionErrorsWidget hardcodes a MOCK source badge [ref<'MOCK'>('MOCK') + literal <span class="source">MOCK</span>] on LIVE billing-health data [fetchBillingHealth is live when VITE_USE_MOCKS off; observed 2026-08-10 on /monetization showing real 0/0 data], defeating the operator's mock-vs-live trust signal. Fix: derive the badge from ServiceResponse.source [D-19804], mirroring the already-correct RevenueChartWidget [v-if=sourceLabel; live bare-{data} envelope → source undefined → no badge]. Also harden useDataFreshness so a non-finite/absent updatedAt → 'Never' [not 'NaNh ago'] — the live /api/dash/* bare {data} envelope carries no updatedAt. NetRevenueChartWidget NOT touched [its MOCK is the intentional D-19602 deduction-placeholder posture; its NaNh ago fixed transitively]. App-layer only [apps/dashboard]; 3 files [PaidActionErrorsWidget.vue + useDataFreshness.{ts,test.ts}]; no server/route/endpoints change; no new D [enforces D-19804, preserves D-19602]. Standard two-session lane, two-commit topology. User-Visible Surface = dashboard.legendary-arena.com [D-24026 live-verify]. Surfaced while live-verifying WP-517. No hard-dep; 2026-08-10, claude/wp527-dashboard-source-badge)
- WP-528 — bystander-lost-penalty-producer (RESERVED: wire the bystanderLost PenaltyEventType producer — the second of five to go live after villainEscaped. Both deriveScoringInputs [parScoring.logic.ts, real-match] and deriveScoringInputsFromFinalState [par.aggregator.ts, PAR-calibration] safe-skip bystanderLost to 0 [D-4801] so a match that loses civilians is not scored on that loss, even though validateScoringConfig ranks bystanderLostWeight above villainEscapedWeight AND above bystanderReward. The engine already models bystander carry-away [carryEscapedBystandersToPile / D-24314 routes a captured Bystander into G.escapedPile on Villain escape from both the reveal and scheme-twist escape paths], so the producer is a PURE end-of-match derivation: count G.escapedPile entries whose villainDeckCardTypes is 'bystander' — the mirror of the existing bystandersRescued victory-zone count. Wired in BOTH derivation copies kept symmetric so calibrated PAR is not easier than the live score; flips wiki/scoring.md status. No new G field/counter [avoids the finalStateHash dual re-pin]; derivation only; no double-count of villains. schemeTwistNegative + the other two producers out of scope; re-anchoring per-config weights to the 4:3:1 rulebook ratio out of scope [no production ScenarioScoringConfig exists yet]. Scaffold-first. game-engine [parScoring.logic + par.aggregator + tests] + wiki; standard two-session lane [PAR/scoring barred from lightweight lane]. Renumbered from WP-527 after #1339 took that number. Reserves D-24339; hard-dep WP-048 + D-24314; 2026-08-11, claude/wp-penalty-producers)
- WP-529 — scheme-twist-negative-penalty-producer (RESERVED: wire the schemeTwistNegative PenaltyEventType producer [third after villainEscaped + bystanderLost/WP-528]. Both deriveScoringInputs [real-match] and deriveScoringInputsFromFinalState [par.aggregator PAR-calibration] safe-skip schemeTwistNegative to 0 [D-4801]. Every scheme twist increments the durable G.counters.schemeTwistCount [schemeHandlers.ts buildGenericTwistEffects], and in Legendary every scheme twist advances the villain's scheme so is player-negative by definition [the community/rulebook -3 x twists], so the faithful producer is a PURE end-of-match derivation: schemeTwistNegative = G.counters.schemeTwistCount ?? 0 — NO twist-polarity classification, NO new G field [existing counter -> finalStateHash + PRE_WP080 byte-identical]. Mirror of the villainEscaped derivation [counter read] and sibling to WP-528. Symmetric across both derivations; flips wiki/scoring.md status. Re-anchoring weights to the 4:3:1 rulebook ratio out of scope [no production ScenarioScoringConfig exists yet]. game-engine [parScoring.logic + par.aggregator + tests] + wiki; standard two-session lane [PAR/scoring barred from lightweight lane]. Reserves D-24340; hard-dep WP-048 + D-24178; 2026-08-11, claude/wp-scheme-twist-negative)
- WP-530 — heal-button-pending-choice-gate-parity (RESERVED: LIGHTWEIGHT LANE. The arena-client Heal-Wounds button gate canHealWounds [useTurnActions.ts] checked only 6 of the engine healWounds move's 11 block-all pending-choice guards, so while a triggered-effect choice was unresolved [discardToPlay D-24184, discardChoice D-24284, reorderChoice D-24286, defeatChoice D-24291, returnOnDiscard D-24301] the Heal-Wounds button rendered ENABLED but clicking it silently no-op'd at the engine — a live-but-dead click, the getLegalMoves↔move-guard divergence class. Fix restores the 5 missing guards to canHealWounds and threads the already-declared hasPendingReturnOnDiscard prop [position 19] into TurnActionBar.vue healGate() [declared but never passed]. The reported path is confirmed correct and unchanged — healing after merely playing cards / triggering effects works because hasActedThisTurn is set ONLY by fightVillain/fightMastermind/recruitHero, never by playCard. arena-client only; additive gate guards + one prop thread; no engine/contract/determinism surface; no new D-entry [applies existing D-24184/24284/24286/24291/24301]. Reserves no D; hard-dep none. (Renumbered from WP-528/EC-563 after #1340 took those numbers for bystanderLost.) 2026-08-11, claude/heal-gate-parity)
- WP-531 — penalty-weight-4-3-1-rulebook-anchor (RESERVED: re-anchor the three rulebook penalty weights [bystanderLost : schemeTwistNegative : villainEscaped] to the community/rulebook 4:3:1 ratio [Legendary Leagues Total Score VP - 4*bystanders - 3*twists - 1*escapes], now that both producers are live [WP-528 bystanderLost + WP-529 schemeTwistNegative]. Centesimal mapping villainEscaped 100 [1.00] : schemeTwistNegative 300 [3.00] : bystanderLost 400 [4.00] — escape is the rulebook 1-unit [alternative scale 800/600/200 rejected as non-minimal]. Updates the frozen scoring surface docs/12-SCORING-REFERENCE.md default-weights table [currently 200/400/500 = 2:4:5, off-ratio] + the test scoring config data/scoring-configs/test-*.json [currently 100/50/300, off-ratio] with a scoringConfigVersion bump 1->2, satisfying validateScoringConfig structural invariants [bystanderLost 400 > villainEscaped 100 AND > bystanderReward 200/300]. SAFE-KNOBS bars scoring as a direct config knob [line 220 frozen surface] so this is a governed WP + D-entry. mastermindTacticUntaken + scenarioSpecificPenalty are NOT in the rulebook triple -> unchanged. Scaffold-first [tests asserting old-weight scores break]. No engine logic change [weights are data]; scoring/PAR barred from the lightweight lane -> standard two-session lane. Reserves D-24342; hard-dep WP-048 + WP-528 + WP-529; 2026-08-12, claude/wp-penalty-weight-anchor)
- WP-532 — paibok-fight-give-hq-hero-each-player (RESERVED: [renumbered from WP-531/EC-566/D-24342 -> WP-532/EC-567/D-24343 after the penalty-weight reserve (#1352) landed on main first; first-to-main-wins, D-24245] fix the core Skrulls villain Paibok the Power Skrull "Fight: Choose a Hero in the HQ for each player. Each player gains that Hero." — currently an UNMARKED/no-handler hollow [D-24266 breadcrumb, tracked (unmarked) in the villain-mechanic-ledger], reported live from a Dr. Doom / Secret Invasion match [turn 36, onFight fired nothing]. New INTERACTIVE VillainEffectPrimitive give-hq-hero-each-player [append-only D-24034, the 20th]: for EACH player [Object.keys(G.playerZones).sort()], remove one HQ Hero, push it to that player's discard [WP-514/D-24327 defeat-to-gain path], and refill the HQ slot from G.heroDeck [captureHeroFromHq/refillHqSlot, the WP-522/D-24335 give-hq-hero-by-trait-to-current mechanics minus the trait filter]. FIDELITY [locked, Jeff 2026-08-11]: HUMAN players pick which HQ Hero interactively [new pending-choice type + resolve move + block-all guard + ai.legalMoves short-circuit + bot-loop wiring + UIState projection + arena-client prompt, the WP-476 PendingDiscardChoice shape — current player parks, non-current players auto-resolve]; BOT/auto-driven players auto-pick the HIGHEST-COST HQ Hero. Ships pending-choice + projection + prompt together [project_pending_choice_no_ux_freeze invariant]. Marks core.json Paibok Fight only [co2e Paibok twin OUT — fast follow-up]. Cross-layer [game-engine + arena-client + server bot-loops + card-data]; standard two-session lane. Reserves EC-567 + D-24343; hard-dep WP-242/D-24007 + WP-243/D-24010 + WP-476/D-24284 + WP-522/D-24335; 2026-08-11, claude/wp-paibok-give-hq-hero)
- WP-533 — co2e-paibok-fight-give-hq-hero-each-player (RESERVED: LIGHTWEIGHT LANE. The co2e Paibok twin deferred by WP-532 [same card, Legendary 2nd-edition]. co2e Skrulls [group slug `skulls`, a co2e-source typo] Paibok the Power Skrull Fight "Choose a Hero from the HQ for each player. Each player gains that Hero." is currently UNMARKED/no-handler hollow [D-24266]; mark it with the EXISTING `give-hq-hero-each-player` primitive [WP-532/D-24343]. CARD-DATA ONLY: add a co2e paibok fight row to villain-effect-markers.json + regen co2e.json/ledger:villains/effect-index [core provenance row already exists — net-new? NO, primitive already provenance'd by WP-532]. NO engine/client/server change; NO new primitive; NO new D [applies D-24343]. The co2e-only "+1[icon:attack] for each Hero Class among Heroes in the HQ" passive is a variable-attack line [resolveFightCost class, NOT a timing line], OUT of scope. Determinism: adds a Fight hook to the co2e Paibok hashed villainAbilityHooks — re-pin only if a committed fixture fights co2e/skulls Paibok [none expected; verify at exec]. Reserves EC-568; no D; hard-dep WP-532/D-24343 [landed]; 2026-08-12, claude/paibok-co2e)
- WP-534 — preferences-foundation-registry-viewer (RESERVED: resurrects the never-merged WP-068 "Preferences Foundation" — the invisible Pinia + Zod preferences schema/store/section-registry under apps/registry-viewer/src/prefs/, NO user-visible UI. The original WP-068/EC-070/D-1414 + the docs/14-PREFERENCES-PANEL-IMPLEMENTATION-PLAN.md design + the ~909-line tested implementation were drafted 2026-04 but NEVER merged; they survive only on retired local commit 8ec6ceda [former branch wp-068-preferences-foundation]. SCAFFOLD-VERIFIED against current main: the src/prefs/ code typechecks clean [0 prefs-file errors] and its 17 tests pass — the registry-viewer build's @legendary-arena/registry/* resolution errors are the pre-existing fresh-worktree registry-dist trap, NOT prefs. Brings the foundation forward under fresh numbers: reserve D-24344 to re-lock the Option-A path [everything under registry-viewer/src/prefs, no new workspace package — D-1414 is not on main]; port the design plan doc + the impl [from 8ec6ceda] + Pinia init in main.ts + the package.json pinia dep. Locked values [EC-070]: PREFERENCES_SCHEMA_VERSION=2, storage key legendary-arena.preferences, 3 shared-tier sections appearance/accessibility/advancedBase, schema-purity [no vue/dom in *.schema.ts], store never throws. Client-UI only [apps/registry-viewer]; standard two-session lane [new subsystem + Zod-schema contract files]. Reserves EC-569 + D-24344; hard-dep none; 2026-08-12, claude/wp534-preferences-foundation)
- WP-535 — rogue-copy-powers-hero-ability (RESERVED: implement Rogue's unimplemented "Copy Powers" hero card [surfaced from Jeff's live 2p Secret Invasion game — the 6th hero was Rogue; Copy Powers logs the generic "[blocked] a play condition was not met", grep-confirmed no `copy-powers` in the engine]. Card: "Play this card as a copy of another Hero you played this turn. This card is both [hc:covert] and the color you copy." [core/rogue/copy-powers, cost 5, NO printed attack/class]. New `copy-powers` HeroKeyword [union+array `heroKeywords.ts` + HANDLED_KEYWORDS + MVP_KEYWORDS + card-data marker via inputs/hero-ability-markers.json — extend the closed markupToken set + regen data/cards/core.json] + a handler that PARKS an interactive pending-choice [the WP-532 give-HQ-Hero pattern — the ~15 touch-points: pending state field + park + hasPending + getEligible/selectDefault + resolve move + block-all guards on EVERY action move + move registration + game.test move-count + ai.legalMoves + UIState build+FILTER pass-through + client prompt] letting the human pick one Hero played this turn [`playerZones.inPlay` minus self, real heroes only] whose on-play ability is then RE-FIRED via the reentrant `executeHeroEffects(G, ctx, playerID, chosenHeroId)` [Q5: the resolve move must thread the FULL MoveContext incl. `random`, a first for resolve moves] and Copy Powers is granted covert + the copied class for `[hc:X]` synergy counting [runtime-chosen dual-class → a new mutable per-match G map, lazy-materialized to avoid the finalStateHash+PRE_WP080 dual re-pin]. FORKS locked-to-recommendation for operator review: (1) interactive [recommend] vs auto [poor fit — no dominant "which hero" choice]; (2) copy = re-fire ABILITY [recommend] not stats [Copy Powers has no printed attack]; (3) dual-class full faithful [covert static + copied-class dynamic/hashed field] vs simplified [covert-only static, drop the copied-color synergy]; (4) 0 eligible heroes → clean no-op [not "blocked-condition"], 1 eligible → auto vs park. game-engine [large: pending-choice + reentrant exec + dual-class + new keyword] + arena-client [prompt] + card-data. Steal Abilities [rogue's other copy card] is a related follow-up, OUT of scope. Standard two-session lane. Reserves EC-570 + D-24345; hard-dep WP-532/D-24343 [give-HQ-Hero pending-choice precedent] + WP-251/D-24022 [HERO_EFFECT_HANDLERS]; 2026-08-12, claude/wp535-copy-powers-draft)
- WP-536 — effects-viewer-set-filter (RESERVED: LIGHTWEIGHT LANE. Add a Set filter to the /debug/effects Effect Implementation Index viewer [apps/dashboard EffectsPage.vue] so the whole-game effect worklist can be scoped to one card set — the enabling step for driving Core-set completion off a single filtered board [Jeff 2026-08-12, "complete the entire Core set and have people play it"]. Every index entry already carries a `set` field [data/metadata/effect-implementation-index.json]; add a SetFilter type + a set-aware branch to filterEntries [useEffectIndex.ts] + a Set dropdown control in EffectsPage.vue populated from the distinct sets present in the loaded index, default 'all', composing with the existing scope/status/handler-only/search controls. Additive UI only: no schema / generated-artifact / engine / determinism change; no new D-entry. Scope OUT: /coverage [hero-only ledger board — stays the cross-set mechanic-grind surface per Jeff's recommendation]; the loadout-picker set filter [EC-420, an unrelated lobby surface — slug-word near-collision only]. apps/dashboard only; single layer. Reserves EC-571; no D; hard-dep WP-487/D-24292 [the /debug/effects viewer]; 2026-08-12, claude/effects-set-filter)
- WP-537 — core-loki-master-strike (RESERVED: implement core Loki Master Strike “Each player reveals a [hc:strength] Hero or gains a Wound” — currently HOLLOW: core/loki takes NO branch in mastermindStrikeHandler [mastermindHandlers.ts ~840-855], falling through to generic bystander-capture+strike-counter bookkeeping [the existing resolveLokiStrike is the co2e/loki face, DIFFERENT printed text]. Verified via the Core mastermind-coverage audit 2026-08-13 [2 of 4 core Master Strikes fire: Magneto+Red Skull; Doom+Loki hollow]. AUTO-RESOLVE reveal-or-Wound-by-class over ALL players [Object.keys(G.playerZones).sort()]: hand holds a [hc:strength] Hero -> reveal, no penalty; else gainWound [thread the returned array]. Mirrors resolveRedSkullStrike [auto all-players loop] + resolveCo2eMagnetoStrike [reveal/discard-or-Wound] + schemeTwistResolvers reveal-or-punish; class read via the same cardTraits/hc mechanism the team resolvers use. New resolveCoreLokiStrike + MASTERMIND_CORE_LOKI=‘core/loki’ constant + dispatch branch in mastermindStrikeHandler; NO new G field; NO pending-choice/UIState/client [auto]; NO card-data marker [strike is keyed by mastermind selection, not a card marker] and NO effect-index/ledger regen [strikes are not in the index]. Determinism: reads G + deterministic gainWound, no ctx.random/IO; re-pin only if a committed fixture reveals a strike under core/loki [none expected — verify at exec]. game-engine only [+ test]. Standard two-session lane [engine mechanic + D]. Reserves EC-572 + D-24346; hard-dep WP-024 [strike execution] + WP-388/D-24192 [per-mastermind strike-dispatch pattern]; PAIRED with WP-538 [core Doom strike, shares the dispatch surface]; 2026-08-13, claude/core-master-strikes)
- WP-538 — core-dr-doom-master-strike (RESERVED: implement core Dr. Doom Master Strike “Each player with exactly 6 cards in hand reveals a [hc:tech] Hero or puts 2 cards from their hand on top of their deck” — currently HOLLOW: core/dr-doom takes NO branch [existing resolveDoctorDoomStrike is the co2e/doctor-doom face, DIFFERENT text]. Verified via the Core mastermind-coverage audit 2026-08-13. Per-player: gate on hand.length===6; if the 6-card hand holds a [hc:tech] Hero -> reveal, no penalty; else the player puts 2 cards from hand on top of their deck. INTERACTIVE [operator ruling 2026-08-13, mirrors WP-476 Magneto discard-to-4 + WP-532 Paibok]: the CURRENT player [resolveCurrentPlayer(strikeContext)] parks a NEW put-2-on-top pending-choice [pick which 2 cards + selection order = top order]; NON-current + BOT players auto-pick 2 deterministically [cheapest-first, lex-tiebreak — the resolveRedSkull/Magneto auto precedent]. NO existing put-on-top pending-choice to reuse [Magneto=discard, Doc Ock=shuffle] -> NEW PendingPutCardsOnDeckChoice type + resolve move + block-all guards on every action move + move registration + game.test move-count + ai.legalMoves short-circuit + UIState build+types+FILTER projection [hand-sourced, per-chooser] + arena-client prompt in PlayDesktop+PlayMobile; ships pending+projection+prompt together [no-UX-freeze invariant]. New resolveCoreDoomStrike + MASTERMIND_CORE_DR_DOOM=‘core/dr-doom’ constant + dispatch branch. Hashed-G-field caution: the pending field is gameplay-affecting -> hashed; lazy-materialize to avoid the finalStateHash+PRE_WP080 dual re-pin [reference_hashed_g_field_dual_repin]; verify at exec. game-engine + arena-client [cross-layer]. Standard two-session lane. Reserves EC-573 + D-24347; hard-dep WP-024 + WP-476/D-24284 [interactive-strike pending-choice precedent] + WP-242/D-24007 [pending-choice infra] + WP-537 [shares the strike-dispatch surface — land 537 first]; 2026-08-13, claude/core-master-strikes)
- WP-539 — portals-scheme-twist (RESERVED: implement the Core scheme “Portals to the Dark Dimension” — the ONLY truly-HOLLOW Core scheme [2026-08-13 scheme-coverage audit]: no SCHEME_TWIST_CONFIGS entry, schemeHandlers logs “No resolver configured” + counter-increment only, and it loses at twist 7 ONLY BY COINCIDENCE [MVP fallback threshold 7 == printed loss-twist]. Printed: Setup 7 Twists [each a Dark Portal]; Twist 1 = Dark Portal above the Mastermind, Mastermind +1[icon:attack]; Twists 2-6 = Dark Portal in the leftmost portal-less city space, Villains in that space +1[icon:attack]; Twist 7 = Evil Wins. IMPLEMENT: [1] config entry resolverId ‘portals’ + lossThreshold 7, NO resourceLossCondition [TRUE twist-loss like Cosmic Cube; makes the twist-7 loss correct-by-design, not coincidental]; [2] new DARK_PORTAL_COUNT counter [mirrors KILLBOT_TWISTS_NEXT_TO_SCHEME, seeded 0] + a `portals` resolver incrementing it + logging placement; [3] VILLAIN positional buff in resolveFightCost [economy.resolve.ts, the single villain-attack source consumed by fightVillain/uiState.build/ai.legalMoves] — +1 when scheme is portals AND the villain’s city-space index has a Dark Portal [derive from the counter: leftmost=Bridge=index 4 first per DESIGN-BOARD-LAYOUT.md + WP-489/D-24295 city binding, so space K portal’d iff count>=6-K; verify direction at exec]; [4] MASTERMIND buff — extract resolveMastermindFightCost(G) [mastermind fightCost is read INLINE as G.cardStats[baseCardId].fightCost at fightMastermind + uiState.build + ai.legalMoves; centralize + add +1 when count>=1], consumed at all 3 sites. Determinism: reads G/counters, no ctx.random/new-persistent-shape [counter only]; re-pin only if a committed fixture reaches a portals twist [none expected]. game-engine only [+ tests]. Standard two-session lane [combat-economy + mastermind-cost refactor + scheme + a D]. Reserves EC-574 + D-24348; hard-dep WP-513/D-24325 [killbots dynamic-attack precedent] + WP-489/D-24295 [city-space binding] + D-24178 [twist-loss vs doom-clock model]; 2026-08-13, claude/wp-portals-scheme)
- WP-540 — partial-core-scheme-twist-fidelity (RESERVED: fix the TWO partial Core scheme twists [2026-08-13 scheme-coverage audit] — both fire but diverge from printed text, both param-driven fixes on the existing resolvers [game-engine only]. (A) SUPER HERO CIVIL WAR: printed Twist “KO all the Heroes in the HQ” but config params koCount:2 KOs only two [schemeTwistConfigs.ts]. Fix: add a koAll?:boolean param to the koFromHq resolver [KO ALL eligible HQ heroes + refill, reusing its existing all-eligible-when-fewer path] + set the config params to {koAll:true}. Loss [hero-deck-empty, WP-510] + 2p sizing [WP-515] already handled — OUT of scope. (B) UNLEASH THE COSMIC CUBE: printed escalation [nothing twists 1-4, each player +1 Wound twists 5-6, +3 Wounds twist 7, twist 8 Evil Wins] but config woundCount:1 applies a FLAT 1 Wound EVERY twist [woundAll resolver]. Fix: add a DATA-DRIVEN escalation param to woundAll [{escalation:[{atOrAfterTwist:5,woundCount:1},{atOrAfterTwist:7,woundCount:3}]}]; when present the resolver computes the current twist as (counters.schemeTwistCount??0)+1 [the resolver runs BEFORE the increment EFFECT is applied — confirmed schemeHandlers dispatch order] and applies the highest matching threshold woundCount [0 if none]. Loss [lossThreshold 8] already correct — OUT of scope. Determinism: reads G/counters, no ctx.random/new-persistent-shape [config data + existing counters]; re-pin only if a committed fixture reaches a Civil-War/Cosmic-Cube twist [none]. game-engine only [+ tests]. Standard two-session lane [two card fidelity fixes + a D]. Reserves EC-575 + D-24349; hard-dep D-24178 [twist-loss/doom-clock model] + WP-510/WP-515 [Civil War loss+sizing, unblock the KO-effect fix]; 2026-08-13, claude/partial-schemes)
- WP-541 — core-villain-fight-reward-effects (RESERVED: mark 3 hollow Core villain/henchman Fight abilities that REWARD the defeating player [2026-08-13 audit; Blob already handled via the require-to-defeat setup subsystem, NOT hollow]. All 3 are unconditional, magnitude-1, single-target-current-player auto-resolve gains [fit the WP-185 v1 curation discipline]. (1) Hand Ninjas [henchman core/hand-ninjas] Fight “You get +1[icon:recruit]” -> NEW gain-recruit-current primitive [G.turnEconomy.recruit += N], marker gain-recruit-current:1. (2) HYDRA Kidnappers [villain core/hydra/hydra-kidnappers] Fight “You may gain a S.H.I.E.L.D. Officer” -> NEW gain-officer-current primitive [move 1 from G.piles.officers to the current player discard; empty pile = logged no-op], marker gain-officer-current [D-24350 rules the beneficial “may” as auto-take, no interactive choice]. (3) Savage Land Mutates [henchman core/savage-land-mutates] Fight “draw an extra card next hand” -> REUSE existing override-next-hand-size primitive [WP-497 handSizeOverrides], marker override-next-hand-size:7 [HAND_SIZE 6 + 1]. Adds 2 append-only VILLAIN_EFFECT_PRIMITIVES [D-24034; union+array+handlers+registry+drift], 3 markers in villain-effect-markers.json, regenerated data/cards/core.json + ledger:villains + effect-index. Keyword-less, self-narrating, NO pending-choice/UIState/client. Determinism: reads/mutates G [recruit econ / officers pile / handSizeOverrides], no ctx.random; re-pin only if a committed fixture fights these [none]. game-engine + card-data. Standard two-session lane. Reserves EC-576 + D-24350; hard-dep WP-485/D-24290 [villain-effect primitive precedent] + WP-497/D-24300 [override-next-hand-size]; FIRST slice of the villain/henchman Fight batch [follow-ons: Maestro counted self-KO magnitude>1; Endless-Armies/The-Leader recursive villain-deck play; Supreme HYDRA dynamic piercing]; 2026-08-13, claude/villain-fights)

## EC

high-water: 457

<!-- reservations (EC-458 and up), newest last -->

- EC-458 — hugo-version-upgrade (2026-07-24, spec/wp-423-hugo-version-upgrade)
- EC-459 — bot-ally stop-drivers-on-sigterm (2026-07-25, fix/bot-ally-stop-drivers-on-sigterm)
- EC-460 — apex-legendary-combo-tier (2026-07-25, spec/wp-425-apex-combo-tier)
- EC-461 — bot-ally survive-db-blip (2026-07-25, fix/bot-ally-survive-db-blip)
- EC-463 — diagnostic-transport-block (2026-07-25, spec/wp-428-transport-diagnostics)
- EC-462 — bot-resolve-putbottomhq (2026-07-25, fix/bot-resolve-putbottomhq)
- EC-464 — transport-reconnect-resync-counters (2026-07-25, spec/wp-429-transport-counters)
- EC-465 — fluid-desktop responsive scaling (2026-07-25, spec/wp-429-fluid-desktop-scaling)
- EC-466 — log-entry-bystander-and-hero-return (2026-07-25, fix/log-entry-bystander-and-hero-return)
- EC-467 — remove-noncanonical-entry-bystander (2026-07-25, fix/remove-noncanonical-entry-bystander)
- EC-468 — bot-ally-fault-observability (2026-07-26, fix/bot-ally-fault-observability)
- EC-469 — log-outcome-engine-contract (WP-434/WP-B.3a; renumbered from EC-467 after a landed collision; 2026-07-26, spec/wp432-log-outcome-engine-contract)
- EC-470 — log-outcome-client-colour (WP-435/WP-B.3b; 2026-07-26, spec/wpb3b-log-colour)
- EC-471 — effectprovenance-outcome-retire (WP-436/WP-B.3c; 2026-07-26, spec/wpb3c-effectprovenance-retire)
- EC-472 — bot-ally-ownership-guard (WP-437; 2026-07-26, worktree-wp-bot-ally-ownership-guard)
- EC-473 — logentry-card-field (WP-438; 2026-07-27, spec/wp-logentry-card)
- EC-474 — dashboard-runtime-health (WP-439; 2026-07-27, worktree-wp-dashboard-runtime-health)
- EC-475 — gauntlet-pack-contract (WP-440; 2026-07-27, claude/wp440-gauntlet-pack)
- EC-476 — legends-gauntlet-download (WP-441; 2026-07-27, claude/wp441-legends-download)
- EC-477 — gauntlet-truth-helper (WP-442; 2026-07-27, claude/wp442-truth)
- EC-478 — gauntlet-run-persistence (WP-443; 2026-07-27, claude/wp443-run-persistence)
- EC-479 — registry-viewer-gauntlet-pack-import (WP-444; 2026-07-28, claude/wp444-cards-consumer)
- EC-480 — gauntlet-run-import-api (WP-445; 2026-07-28, claude/wp445-run-api)
- EC-481 — gauntlet-run-derived-progression (WP-446; 2026-07-28, claude/wp446-progress)
- EC-482 — villain-scry-ko-own-deck (WP-447; 2026-07-28, spec/wp-447-scry-ko-mechanic)
- EC-483 — composition-to-match-launch-primitive (WP-448; 2026-07-28, claude/wp448-match-primitive)
- EC-484 — profile-gauntlet-tracker-ui (WP-449; 2026-07-28, claude/wp449-tracker-ui)
- EC-485 — villain-gain-attached-hero (WP-450; NOTE --next returned EC-484 but that is claimed by open PR #1070 so this took EC-485; 2026-07-28, spec/wp-450-gain-attached-hero)
- EC-486 — exclude-diagnostics-from-state-hash (WP-451; 2026-07-29, spec/wp-451-diagnostics-hash-exclusion)
- EC-487 — bot-ally-coop-winrate-harness (WP-452; RENUMBERED from EC-479 after a landed collision; 2026-07-29, claude/wp452-bot-ally-winrate-harness)
- EC-488 — simulation-setup-shuffle-fidelity (WP-453; 2026-07-29, claude/kind-austin-dde1e0)
- EC-489 — gauntlet-loadout-qualification-guard (WP-454; RENUMBERED from EC-488 after a landed collision; 2026-07-29, claude/wp454-gauntlet-loadout-qualification-guard)
- EC-490 — local-workindex-row-check (WP-455; 2026-07-29, claude/wp455-local-workindex-row-check)
- EC-491 — legends-mastermind-details-reveal (WP-456; 2026-07-29, claude/wp456-legends-mastermind-details-reveal)
- EC-492 — challenge-link-count-consistency (WP-457; 2026-07-30, claude/wp457-challenge-link-count-consistency)
- EC-493 — collapse-approved-loadouts-to-one (WP-458; 2026-07-30, claude/wp458-459-collapse-and-reveal-layout)
- EC-494 — compact-details-reveal-layout (WP-459; 2026-07-30, claude/wp458-459-collapse-and-reveal-layout)
- EC-495 — divider-below-reveal (WP-460; 2026-07-30, claude/wp460-divider-below-reveal)
- EC-496 — legends-set-details-publish (WP-461; 2026-07-30, claude/draft-set-details-reveal)
- EC-497 — legends-set-details-reveal (WP-462; 2026-07-30, claude/draft-set-details-reveal)
- EC-498 — villain-zone-restricted-each-ko (WP-463; 2026-07-30, SPEC-onambush-juggernaut)
- EC-499 — core-coverage-matrix (WP-464; 2026-07-30, claude/draft-core-coverage-matrix)
- EC-500 — score-submit-rejection-copy (WP-465; 2026-07-30, SPEC-score-submit-copy)
- EC-501 — core-coverage-matrix-compact (WP-466; 2026-07-30, claude/wp466-matrix-compact)
- EC-502 — matrix-scheme-wrap-fix (WP-467; 2026-07-30, claude/wp467-scheme-wrap-fix)
- EC-503 — matrix-dropdown-collapse (WP-468; 2026-07-30, claude/wp468-matrix-dropdown-collapse)
- EC-504 — villain-reveal-or-wound (WP-469; RENUMBERED from EC-503 after the WP-468/EC-503 concurrent collision; 2026-07-30, SPEC-sabretooth-reveal-or-wound)
- EC-505 — doombot-scry-interactive-choice (WP-470; 2026-07-30, SPEC-doombot-scry-interactive)
- EC-506 — per-scheme-gauntlet-registry-loader (WP-471; 2026-07-31, claude/spec-per-scheme-gauntlet-arc)
- EC-507 — per-scheme-gauntlet-server-leaderboard (WP-472; 2026-07-31, claude/spec-per-scheme-gauntlet-arc)
- EC-511 — magneto-master-strike-reveal-or-discard (WP-476; 2026-07-31, claude/spec-bug2-master-strike)
- EC-512 — turnactionbar-discard-gate (WP-477; 2026-07-31, claude/wp477-turnactionbar-discard-gate)
- EC-508 — per-scheme-gauntlet-run-tracker-launch (WP-473; 2026-07-31, claude/spec-per-scheme-gauntlet-arc)
- EC-509 — per-scheme-gauntlet-consumers (WP-474; 2026-07-31, claude/spec-per-scheme-gauntlet-arc)
- EC-510 — per-scheme-gauntlet-arena-client (WP-475; 2026-07-31, claude/spec-per-scheme-gauntlet-arc)
- EC-513 — empty-deck-reshuffle-reveal-scry (WP-478; 2026-07-31, claude/wp478-empty-deck-reshuffle-reveal-scry)
- EC-514 — reveal-remainder-reorder (WP-479; 2026-08-01, claude/wp479-reveal-remainder-reorder)
- EC-515 — turnactionbar-reorder-gate (WP-480; 2026-08-01, claude/wp480-turnactionbar-reorder-gate)
- EC-516 — mystique-escape-scheme-twist (WP-481; 2026-08-01, claude/wp481-mystique-escape-scheme-twist)
- EC-517 — dococt-reveal-eight-topup (WP-482; 2026-08-01, claude/wp482-dococt-reveal-eight-topup)
- EC-518 — per-scheme-gauntlet-cards-consumers (WP-483; 2026-08-01, claude/eager-goldwasser-7565b9)
- EC-519 — effect-implementation-index (WP-484; 2026-08-01, claude/wp484-effect-implementation-index)
- EC-520 — villain-effect-vocab-tier-a (WP-485; 2026-08-01, claude/wp485-villain-effect-vocab-tier-a)
- EC-521 — instant-defeat-with-bystander (WP-486; 2026-08-01, claude/wp486-instant-defeat)
- EC-522 — debug-effects-viewer (WP-487; renumbered from EC-520 [taken by #1157]; 2026-08-01, claude/wp-debug-effects-viewer)
- EC-523 — effect-runtime-tracing (WP-488; 2026-08-02, claude/wp-effect-tracing)
- EC-524 — villain-effect-vocab-tier-b-city-spaces (WP-489; renumbered from EC-521 [taken by #1160]; 2026-08-02, claude/wp486-villain-city-spaces)
- EC-525 — destroyer-shield-ko-basic-cards (WP-490; lightweight lane; 2026-08-03, claude/wp490-destroyer-shield-ko)
- EC-526 — hero-ledger-per-design-granularity (WP-491; 2026-08-03, claude/wp491-hero-ledger-per-design)
- EC-527 — villain-effect-vocab-tier-d-whirlwind (WP-492; 2026-08-03, claude/wp492-whirlwind-tierd)
- EC-528 — mechanic-provenance-backfill (WP-493; 2026-08-03, claude/wp493-provenance-backfill)
- EC-529 — villain-effect-vocab-tier-d-viper (WP-494; 2026-08-03, claude/wp494-viper-tierd)
- EC-530 — provenance-decision-backfill (WP-495; 2026-08-03, claude/wp495-provenance-decision-backfill)
- EC-531 — coverage-decision-column (WP-496; lightweight lane; 2026-08-04, claude/wp496-coverage-decision-column)
- EC-532 — mastermind-tactic-onfight-framework (WP-497; 2026-08-04, claude/wp497-tactic-onfight)
- EC-533 — hero-return-on-discard-ability (WP-498; cross-layer engine+arena-client+card-data; two-session lane; renumbered from EC-532 after a same-number race; 2026-08-04, claude/wp498-return-on-discard)
- EC-534 — join-by-match-id (WP-499; lightweight lane; 2026-08-04, claude/join-by-match-id)
- EC-535 — auto-assign-handles (WP-500; server; standard two-session lane; 2026-08-05, claude/wp500-auto-assign-handles)
- EC-536 — change-handle (WP-501; cross-layer server+arena-client; standard two-session lane; 2026-08-05, claude/wp501-change-handle)

- EC-537 — match-endgame-controls (WP-502; cross-layer engine+server+arena-client; standard two-session lane; 2026-08-05, claude/wp-502-match-endgame-controls)
- EC-538 — doc-ock-villain-fight-hand-size (WP-503; game-engine + card-data markers; villain-effect-vocab; BLOCKED on WP-497; standard two-session lane; 2026-08-05, claude/wp503-doc-ock-villain-fight)
- EC-539 — friend-request-by-account-id (WP-504; cross-layer server+arena-client; standard two-session lane; 2026-08-05, claude/wp504-friend-request-by-uuid)
- EC-540 — captured-cards-under-adversaries (WP-505; cross-layer game-engine UIState projection + arena-client render; adds count-only UICityCard.attachedBystanderCount via the 5-step Board-Visible Field contract, renders face-up attachedHeroes + face-down bystander count badges under city villains and the mastermind; standard two-session lane; 2026-08-06, claude/captured-cards-under-adversaries)
- EC-541 — magneto-tactic-crushing-shockwave (WP-506; single-layer game-engine; adds resolveCrushingShockwave + dispatch branch to tacticHandlers.ts [reveal-X-Men-or-two-Wounds, each OTHER player]; standard two-session lane; 2026-08-06, claude/wp506-507-magneto-tactic-fight)
- EC-542 — mastermind-tactic-effect-index-coverage (WP-507; single-layer tooling/data; adds a tactic feed to build-effect-implementation-index.mjs so mastermind tactic Fight coverage surfaces on /debug/effects; standard two-session lane; 2026-08-06, claude/wp506-507-magneto-tactic-fight)
- EC-543 — escaped-pile-carry-away-midtown-loss (WP-508; single-layer game-engine; carry escaped villains' captured bystanders into G.escapedPile + data-only SchemeTwistConfig.resourceLossCondition [escaped-pile-count] + evaluator setting SCHEME_LOSS + twist-proxy suppression, wired for Midtown [bystander >= 8]; standard two-session lane; 2026-08-07, claude/escaped-pile-resource-loss)
- EC-550 — civil-war-2p-hero-deck-sizing (WP-515; game-engine; new resolveEffectiveHeroDeckIds in setup/schemeSetupSizing.ts [Civil War @2p → heroDeckIds.slice(0,4), else unchanged] applied at the buildHeroDeck call in buildInitialGameState; mirrors WP-511's wound-sizing; no loss change [D-24318 ships it]; standard two-session lane; 2026-08-09, claude/wp515-civilwar-2p)
- EC-549 — secret-invasion-skrull-conversion (WP-514; game-engine; reuse WP-513 converted-card overlay + 'skrull' origin; cross-deck 12-hero shuffle at setup; VP+2 dynamic attack (cardVictoryPoints); defeat-to-gain routing; HQ-highest-cost→Sewers twist resolver; escaped-converted-count origin skrull threshold 6; standard two-session lane; 2026-08-09, claude/wp514-secret-invasion)
- EC-548 — killbots-converted-villains (WP-513; single-layer game-engine; converted-villain overlay [G.convertedVillainOrigins, cards typed 'villain' + origin 'killbot'] + escaped-converted-count loss (threshold 5) + per-scheme twist counter driving killbot attack + Killbots config/resolver; ~14 files, over-size split flagged for the gates; standard two-session lane; 2026-08-08, claude/wp512-conversion-schemes-draft)
- EC-546 — legacy-virus-wound-depletion-loss (WP-511; single-layer game-engine; widen pile-depleted to 'wounds' + Legacy Virus resourceLossCondition + new setup/schemeSetupSizing.ts resolveEffectiveWoundsCount [6xplayers] wired into buildInitialGameState post-validation + DELIBERATE sentinel fixture re-pin [PRE_WP080 stays ec64506a]; standard two-session lane; 2026-08-07, claude/wp511-legacy-virus-wound-depletion)
- EC-545 — civil-war-hero-deck-depletion-loss (WP-510; single-layer game-engine; new 'pile-depleted'/heroDeck resourceLossCondition kind + applyPileDepletionResourceLoss wired to play-phase turn.onMove + Civil War config; tie-override via SCHEME_LOSS precedence [no finalTurn.logic change]; standard two-session lane; 2026-08-07, claude/wp510-civil-war-hero-deck-loss)
- EC-544 — negative-zone-villain-escape-loss (WP-509; single-layer game-engine; Negative Zone escaped-pile-count villain/12 [reuse WP-508 kind] + remove evaluateEndgame generic escape branch + reword coopOutcome comment; standard two-session lane; 2026-08-07, claude/wp509-negative-zone-villain-loss)
- EC-547 — coverage-backdrop-portals (WP-512; shared-tooling sim scripts + coverage artifact + dashboard test, NO packages change; switch coverage sweep + coop yardstick backdrop Cosmic Cube -> Portals to the Dark Dimension [faithful "Twist 7: Evil Wins!", loses via MVP fallback — no engine change], recover artifact 10->12 mechanics + dashboard totalObs 163->188, add a deterministic-termination guard; standard two-session lane; 2026-08-08, claude/priceless-morse-ed9637)
- EC-551 — ymir-fight-ko-wounds (WP-516; single-layer game-engine + card-data + ewiki; new keyword-less no-param auto-resolve VillainEffectPrimitive 'ko-wounds-current-hand-and-discard' [14->15] + handler [KO current player's Wounds from hand+discard to G.ko] + Ymir Fight marker + drift/handler/parse tests; regen core.json/ledger:villains/effect-index/provenance; NO parser arm, NO hash re-pin; lightweight lane [two-commit topology, one PR]; 2026-08-09, claude/clever-blackburn-5a6ccf)
- EC-552 — dr-readiness-dashboard-tile (WP-517; CI+server+dashboard: dr-drill-reminder.yml "Drill passed" checkbox + apps/server /api/dash/dr-readiness feed [DR-drill-issue-derived: last drill / next due / overdue] + apps/dashboard mock-first ops-health tile; single session, two-commit topology; renumbered from EC-551 after Ymir shipped under 516/551/329; 2026-08-09, claude/spec-dr-readiness-dashboard)
- EC-553 — skrull-gain-fight-overlay-narration (WP-518; game-engine only: thread `skrullGained` from defeatCityVillainCore into composeFightNarrative, append ` and gained the Hero into the active player's discard pile`; additive/byte-identical for non-Skrull defeats; notableEvents narrative is hashed but no committed fixture defeats a Skrull → sentinel/PRE_WP080 unchanged [verify]; 4 files [compose + fightVillain + 2 tests]; single session, two-commit topology; 2026-08-10, claude/wp518-skrull-gain-narrative)
- EC-554 — melter-fight-ko-cullable-deck-top (WP-519; single-layer game-engine + card-data + ewiki; new keyword-less no-param auto-resolve VillainEffectPrimitive 'ko-cullable-each-deck-top' [15->16] + handler [each player reveals deck top, reshuffle-on-empty via shuffleContext; auto-KO only Wound/basic-S.H.I.E.L.D.-starter cullable cards to G.ko, KEEP real Heroes/Officer] + Melter Fight marker + drift/handler/parse tests; regen core.json/ledger:villains/effect-index/provenance; NO parser arm; hash impact via reshuffle + hashed Fight hook MUST be verified/re-recorded [masters-of-evil in scoring fixtures]; standard two-session lane; 2026-08-10, claude/amazing-lewin-ce0830)
- EC-555 — co2e-masters-of-evil-mark-covered-lines (WP-520; card-data-only; add a co2e -> masters-of-evil block to inputs/villain-effect-markers.json with THREE rows — melter.fight=[ko-cullable-each-deck-top], ultron.escape=[reveal-or-wound:hc:tech], whirlwind.fight=[ko-hero:current@rooftops+bridge]; regen co2e.json via apply-effect-markers.mjs + ledger:villains + effect-index; NO engine change, NO new primitive [so NO mechanic-provenance row — each line's provenance is the owning primitive's WP/D]; the three co2e lines flip unmarked->executable; deferred-epic lines enumerated in the WP + D-24333; hard-dep WP-519/D-24332 for the Melter marker; standard two-session lane; 2026-08-10, claude/co2e-moe-marking-draft)
- EC-556 — co2e-baron-zemo-ambush-capture-per-hq-avengers (WP-521; game-engine + card-data; new auto-resolve VillainEffectPrimitive [+1] + handler [attach 1 Bystander to the villain + 1 per G.hq avengers-team hero via a new HQ-by-trait count; attachBystanderToVillain at Ambush, award deferred to defeat, supply-bounded; self-narrate] + drift/handler/parse tests; co2e baron-zemo ambush marker + regen co2e.json/ledger:villains/effect-index/provenance; no ctx.random; standard two-session lane; 2026-08-10, claude/co2e-moe-new-primitives-draft)
- EC-557 — co2e-ultron-fight-hq-tech-hero-ko-or-gift (WP-522; game-engine [+ client if fork B] + card-data; new VillainEffectPrimitive [+1] + handler for the co2e Ultron Fight HQ tech-hero KO-or-gift; fidelity fork LOCKED in the WP [auto-resolve gift-highest-cost-hc:tech-to-current-player RECOMMENDED vs interactive pending-choice scry-ko shape]; gift->player discard [WP-514 path], KO->G.ko, HQ refill [captureHeroFromHq]; co2e ultron fight marker + regen + drift/handler/parse tests; standard two-session lane; 2026-08-10, claude/co2e-moe-new-primitives-draft)
- EC-558 — co2e-whirlwind-ambush-swap-two-city-villains (WP-523; game-engine + card-data; new VillainEffectPrimitive [+1] + handler swapping two VILLAIN-typed occupants' G.city spaces per a LOCKED deterministic rule [henchmen excluded via G.villainDeckCardTypes; no-op < 2 city villains]; co2e whirlwind ambush marker + regen + drift/handler/parse tests; determinism per the positional rule; standard two-session lane; 2026-08-10, claude/co2e-moe-new-primitives-draft)
- EC-560 — secret-invasion-six-hero-play-lobby-projection (WP-525; server + arena-client; scheme-aware GET /api/match/setup-requirements [optional schemeId -> resolveEffectiveHeroCount projection] + arena-client lobby threads selected schemeId into fetchSetupRequirements + computePlayerCountMismatches + scheme-aware autoplay default-loadout sizing; SHIP AS A SET with WP-524 so the play lobby never regresses; api-endpoints.md if the endpoint shape changes; standard two-session lane; 2026-08-11, claude/wp524-525-secret-invasion-heroes)
- EC-561 — url-preview-setup-requirement-display (WP-526; registry-viewer only; LIGHTWEIGHT LANE; add a scheme-aware setup-requirement line to LoadoutPreview.vue [mirrors the builder line, scheme-aware heroCount via getPlayerCountSetup + resolveEffectiveHeroCount off previewDocument.playerCount + composition.schemeId] + new LoadoutPreview.test.ts [SI preview shows 6 heroes, non-SI shows base]; additive, respects the read-only preview-first design; no engine/registry/server/contract/determinism surface, no D-entry; 2026-08-11, claude/wp526-preview-requirement)
- EC-559 — secret-invasion-six-hero-setup-requirement (WP-524; registry + game-engine + server + arena-client/registry-viewer lobby; scheme-aware resolveEffectiveHeroCount threaded through checkPlayerCountComposition + validatePlayerCountComposition + the server create-gate + lobby requirement display; FLAT 6 for Secret Invasion wherever base<6; scope hero-count only [Skrull-required OUT]; determinism confirmed suite-green [no committed fixture uses the scheme]; standard two-session lane; 2026-08-10, claude/wp524-secret-invasion-six-heroes)
- EC-562 — dashboard-money-widget-source-badge (WP-527; app-layer apps/dashboard; PaidActionErrorsWidget source badge from ServiceResponse.source [mirror RevenueChartWidget, v-if=sourceLabel] + useDataFreshness non-finite/absent updatedAt → 'Never' + new useDataFreshness.test.ts; NetRevenue/RevenueChart NOT touched; no server/route/D change; standard two-session lane; 2026-08-10, claude/wp527-dashboard-source-badge)
- EC-563 — bystander-lost-penalty-producer (WP-528; game-engine + wiki; replace the bystanderLost = 0 safe-skip in BOTH deriveScoringInputs [parScoring.logic.ts] and deriveScoringInputsFromFinalState [par.aggregator.ts, PAR-calibration mirror] with a pure end-of-match derivation counting bystander-typed G.escapedPile entries [mirrors bystandersRescued] + updated // why: + par.aggregator JSDoc fix + AC-1..AC-6 tests [count / empty / mixed no-double-count / buildScoreBreakdown flow-through / non-vacuous control-revert / aggregator symmetry incl. scheme-twist path] + flip wiki/scoring.md status; no new G field [derivation only -> finalStateHash + PRE_WP080 byte-identical]; no contract edit [PenaltyEventType union/array unchanged]; scaffold-first; standard two-session lane. Renumbered from EC-562 after #1339; 2026-08-11, claude/wp-penalty-producers)
- EC-564 — scheme-twist-negative-penalty-producer (WP-529; game-engine + wiki; replace the schemeTwistNegative = 0 safe-skip in BOTH deriveScoringInputs [parScoring.logic.ts] and deriveScoringInputsFromFinalState [par.aggregator.ts] with G.counters.schemeTwistCount ?? 0 + // why: + AC tests [count / zero-when-absent / buildScoreBreakdown flow-through / non-vacuous control-revert / aggregator symmetry] + flip wiki/scoring.md status; no new G field [existing counter -> finalStateHash + PRE_WP080 byte-identical]; no contract edit [PenaltyEventType union/array unchanged]; scaffold-first; standard two-session lane; 2026-08-11, claude/wp-scheme-twist-negative)
- EC-565 — heal-button-pending-choice-gate-parity (WP-530; arena-client only; LIGHTWEIGHT LANE; canHealWounds [useTurnActions.ts] gains the 5 missing engine-parity pending guards [discardToPlay/discardChoice/reorderChoice/defeatChoice/returnOnDiscard] + TurnActionBar.vue healGate() threads hasPendingReturnOnDiscard [pos 19, previously declared-but-unpassed]; useTurnActions.test.ts +5 parity cases + TurnActionBar.test.ts +1 wiring case; additive, no engine/contract/determinism surface, no new D-entry [applies D-24184/24284/24286/24291/24301]; renumbered from EC-563 after #1340; 2026-08-11, claude/heal-gate-parity)
- EC-566 — penalty-weight-4-3-1-rulebook-anchor (WP-531; docs + data + tests; set penaltyEventWeights to villainEscaped 100 / schemeTwistNegative 300 / bystanderLost 400 [4:3:1] in the 12-SCORING-REFERENCE default-weights table + the test scoring config [scoringConfigVersion 1->2] + reconcile any weight-asserting tests [scoringConfigLoader / parScoring.logic] scaffold-first + wiki/scoring.md if it states weights; structural invariants hold; no engine logic change; standard two-session lane; 2026-08-12, claude/wp-penalty-weight-anchor)
- EC-567 — paibok-fight-give-hq-hero-each-player (WP-532; [renumbered from EC-566/WP-531 per D-24245 first-to-main; penalty-weight #1352 landed 566 first] cross-layer game-engine + arena-client + server bot-loops + card-data; new INTERACTIVE VillainEffectPrimitive give-hq-hero-each-player [+1, the 20th] + handler [per player: remove one HQ Hero -> that player's discard + refill HQ slot; current/HUMAN player parks a new pending-choice type resolved via a new moves/*.resolve.ts + block-all guard + ai.legalMoves short-circuit + PENDING_CHOICE_MOVE_NAMES/PENDING_CHOICE_FLAGS bot wiring; non-current + BOT players auto-pick highest-cost synchronously] + UIState build/types/filter projection [HQ-sourced, per-chooser] + new arena-client prompt mounted in PlayDesktop + PlayMobile + drift/handler/parse/move-count [27->28] tests; core paibok fight marker + regen core.json/ledger:villains/effect-index; ships pending+projection+prompt together [no-UX-freeze]; co2e twin OUT; standard two-session lane; 2026-08-11, claude/wp-paibok-give-hq-hero)
- EC-568 — co2e-paibok-fight-give-hq-hero-each-player (WP-533; LIGHTWEIGHT LANE; card-data only; mark the co2e Skrulls [`skulls`] Paibok Fight with the EXISTING give-hq-hero-each-player primitive [WP-532/D-24343] + regen co2e.json/ledger:villains/effect-index; NO engine/client/server change, NO new primitive, NO new D [applies D-24343]; the +1-atk-per-HQ-hero-class passive OUT of scope [variable-attack, not a timing line]; scaffold-first [marker apply + :check]; two-commit topology; hard-dep WP-532/D-24343; 2026-08-12, claude/paibok-co2e)
- EC-569 — preferences-foundation-registry-viewer (WP-534; Client-UI apps/registry-viewer only; bring the never-merged WP-068 preferences foundation forward from retired commit 8ec6ceda under fresh numbers: Pinia init in main.ts + pinia dep; Zod prefs schema [3 shared-tier sections appearance/accessibility/advancedBase, PREFERENCES_SCHEMA_VERSION=2 versioned envelope] + section registry [registerSection, duplicate rejection] + createPreferencesStore factory [localStorage corruption-safe envelope, quota guard, resetAll/resetSection/importJson/exportJson, NO throw] + usePreferences composable + registerSections app stub [zero app sections yet] + tests [schema round-trip, corruption backup, quota degradation, duplicate rejection]; port docs/14-PREFERENCES-PANEL-IMPLEMENTATION-PLAN.md + land D-24344 Option-A path; NO user-visible UI change [bit-identical viewer output]; guardrails: *.schema.ts imports no vue/dom [purity test], Option-A no new package. Scaffold VERIFIED: src/prefs typecheck 0 errors + 17 tests pass on current main. Standard two-session lane; 2026-08-12, claude/wp534-preferences-foundation)
- EC-570 — rogue-copy-powers-hero-ability (WP-535; game-engine + arena-client + card-data; new `copy-powers` HeroKeyword [union+array + HANDLED_KEYWORDS + MVP_KEYWORDS + hero-ability-markers.json marker + regen core.json] + handler parking an interactive pending-choice [give-HQ-Hero WP-532 pattern, ~15 touch-points incl. UIState build+FILTER + block-all guards + move-count drift + ai.legalMoves] over Heroes played this turn [inPlay minus self]; resolve move re-fires the chosen hero's ability via reentrant executeHeroEffects [threads full MoveContext incl. random] + grants covert+copied-class for [hc:X] synergy [runtime dual-class, lazy G field]; FORKS locked-to-recommendation [interactive; re-fire ability not stats; full-dual-class vs covert-only; 0/1-eligible edge]; determinism: new pending/dual-class G fields lazy-materialized to avoid finalStateHash+PRE_WP080 re-pin, verified at execution; Steal Abilities OUT of scope; standard two-session lane; 2026-08-12, claude/wp535-copy-powers-draft)
- EC-571 — effects-viewer-set-filter (WP-536; apps/dashboard only; LIGHTWEIGHT LANE; add a SetFilter type + a `set` param to filterEntries [useEffectIndex.ts] + a Set dropdown in EffectsPage.vue [distinct sets read from the loaded index, default 'all', composes with scope/status/handler-only/search] + useEffectIndex.test.ts set-filter cases; additive, no schema / engine / determinism / generated-artifact surface, no new D [applies D-24292]; scaffold-first [dashboard test:coverage green]; two-commit topology; hard-dep WP-487/D-24292; 2026-08-12, claude/effects-set-filter)
- EC-572 — core-loki-master-strike (WP-537; game-engine only [+ test]; new resolveCoreLokiStrike + MASTERMIND_CORE_LOKI=‘core/loki’ + dispatch branch in mastermindStrikeHandler [mastermindHandlers.ts]; AUTO reveal-[hc:strength]-or-gainWound over all players [sorted], mirrors resolveRedSkullStrike + reveal-or-punish; gainWound threaded; NO new G field / pending-choice / UIState / client / card-marker / ledger regen; determinism reads-G-only, re-pin only if a committed fixture reveals a core/loki strike [none expected]; standard two-session lane; hard-dep WP-024 + WP-388/D-24192; paired WP-538; 2026-08-13, claude/core-master-strikes)
- EC-573 — core-dr-doom-master-strike (WP-538; game-engine + arena-client [cross-layer]; new resolveCoreDoomStrike + MASTERMIND_CORE_DR_DOOM=‘core/dr-doom’ + dispatch branch; per-player 6-card gate + reveal-[hc:tech]-or-put-2-on-top; INTERACTIVE current player [new PendingPutCardsOnDeckChoice + resolve move + block-all guards + move registration + move-count drift + ai.legalMoves + UIState build/types/filter + PlayDesktop/PlayMobile prompt], non-current+bots auto-pick cheapest-2; ships pending+projection+prompt together [no-UX-freeze]; lazy-materialize the hashed pending field to avoid finalStateHash+PRE_WP080 re-pin [verify at exec]; standard two-session lane; hard-dep WP-024 + WP-476/D-24284 + WP-242/D-24007 + WP-537; 2026-08-13, claude/core-master-strikes)
- EC-574 — portals-scheme-twist (WP-539; game-engine only [+ tests]; config entry [resolverId ‘portals’, lossThreshold 7, no resourceLossCondition] + DARK_PORTAL_COUNT counter + `portals` resolver + SchemeTwistResolverId union/registry drift + villain positional buff in resolveFightCost [+1 per Dark Portal in the villain’s city space, derived from the counter, leftmost=Bridge=idx4] + resolveMastermindFightCost(G) centralization [+1 when count>=1] consumed at fightMastermind/uiState.build/ai.legalMoves; no ctx.random, counter-only state, re-pin only if a committed fixture reaches a portals twist [none]; standard two-session lane; hard-dep WP-513/D-24325 + WP-489/D-24295 + D-24178; 2026-08-13, claude/wp-portals-scheme)
- EC-575 — partial-core-scheme-twist-fidelity (WP-540; game-engine only [+ tests]; (A) koFromHq gains a koAll?:boolean [KO ALL eligible HQ heroes when true] + Civil War config params {koAll:true}; (B) woundAll gains a data-driven escalation param [threshold->woundCount, current twist = schemeTwistCount+1 read pre-increment] + Cosmic Cube config params escalation [5->1, 7->3]; NO new resolverId/counter/persistent-shape [config data + existing woundAll/koFromHq + schemeTwistCount]; loss conditions unchanged [Civil War heroDeck-empty, Cosmic Cube twist-8]; no ctx.random; re-pin only if a committed fixture reaches these twists [none]; standard two-session lane; hard-dep D-24178 + WP-510 + WP-515; 2026-08-13, claude/partial-schemes)
- EC-576 — core-villain-fight-reward-effects (WP-541; game-engine + card-data; 2 new append-only VILLAIN_EFFECT_PRIMITIVES [gain-recruit-current, gain-officer-current] + handlers + registry + union/array drift; REUSE override-next-hand-size:7 for Savage Land Mutates [no new primitive]; 3 markers in scripts/convert-cards/inputs/villain-effect-markers.json [henchmen core/hand-ninjas.fight, villains core/hydra/hydra-kidnappers.fight, henchmen core/savage-land-mutates.fight] -> regen core.json via apply-effect-markers.mjs + ledger:villains + effect-index; keyword-less self-narrating auto-resolve, no pending-choice/client; may-gain-Officer auto-takes [D-24350]; no ctx.random; re-pin only if a committed fixture fights these [none]; standard two-session lane; hard-dep WP-485/D-24290 + WP-497/D-24300; 2026-08-13, claude/villain-fights)

## D

high-water: 24241

- D-24242 — seed-par-publication (WP-422; claimed in #993 before this ledger existed — reconciled 2026-07-25, infra/dedup-d24242)
- D-24243 — hugo-version-upgrade (2026-07-24, spec/wp-423-hugo-version-upgrade)
- D-24244 — bot-ally stop-drivers-on-sigterm (2026-07-25, fix/bot-ally-stop-drivers-on-sigterm)
- D-24245 — number-allocation-ledger mechanism (renumbered from D-24242 for the seed-PAR collision; 2026-07-25, infra/dedup-d24242)
- D-24246 — apex-legendary-combo-tier (4th shared comboTierForCount boundary; 2026-07-25, spec/wp-425-apex-combo-tier)
- D-24247 — bot-ally survive-db-blip (tolerate transient empty fetch; 2026-07-25, fix/bot-ally-survive-db-blip)
- D-24249 — diagnostic-transport-block (transport block in the play-surface diagnostic report; 2026-07-25, spec/wp-428-transport-diagnostics)
- D-24248 — bot-resolve-putbottomhq (getLegalMoves short-circuit for the 2 put-bottom-HQ choices; 2026-07-25, fix/bot-resolve-putbottomhq)
- D-24250 — transport-reconnect-resync-counters (reconnect/resync/watchdog counters in the transport diagnostics block; 2026-07-25, spec/wp-429-transport-counters)
- D-24251 — fluid-desktop responsive scaling (max-width play-area cap + fluid clamp card/gutter sizing, additive to D-12909; 2026-07-25, spec/wp-429-fluid-desktop-scaling)
- D-24252 — log-entry-bystander-and-hero-return (narrate the D-1701 city-entry bystander attach + the WP-214 captured-hero return-on-defeat into G.messages; log-only, hash-excluded per D-24081; 2026-07-25, fix/log-entry-bystander-and-hero-return)
- D-24253 — log-outcome-contract-design (WP-B.3 DESIGN: G.messages string[]→LogEntry[] records carrying a coarse LogOutcome for green/red/yellow colour-coding; retires the D-24100 effectProvenance heuristic; design ruling only, no code; 2026-07-25, spec/wpb3-log-outcome-design)
- D-24254 — remove-noncanonical-entry-bystander (supersedes D-1701; a villain/henchman no longer captures a bystander merely on City entry — non-canonical; corrects the D-18504→D-1701 mis-citation; 2026-07-25, fix/remove-noncanonical-entry-bystander)
- D-24255 — bot-ally-fault-observability (every bot-turn fault return logs its reason + turn/stage + the set block-all pending-choice flags; closes the silent-fault observability gap; server log-only; 2026-07-26, fix/bot-ally-fault-observability)
- D-24256 — bot-ally-ownership-guard (cross-instance driver-ownership lease: driver_owner + heartbeat_at on legendary.match_bot_ally, a per-tick atomic claim-or-renew gates which instance drives a bot seat, SIGTERM releases; picked Option B over a pg advisory lock — the shared max=10 pool cannot pin a client per driver; server only; 2026-07-26, worktree-wp-bot-ally-ownership-guard)
- D-24257 — logentry-card-field (LogEntry gains optional structured card ext-id; effectProvenance reads it for identification + association instead of prose; extends D-24253; 2026-07-27, spec/wp-logentry-card)
- D-24258 — dashboard-runtime-health-signal (on-request sampled server runtime health — process CPU% via cpuUsage delta, event-loop lag via perf_hooks.monitorEventLoopDelay p50/p99/max, RSS, uptime, cpuCount, WEB_CONCURRENCY — on admin-gated GET /api/dash/runtime; no DB/engine read; decision-support for the clustering question; 2026-07-27, worktree-wp-dashboard-runtime-health)
- D-24259 — faction-battle-cry-ip-reconciliation (narrative/wiki governance ruling: the IP boundary is split so fabricating/lifting lore into original copy stays forbidden, while surfacing a licensed character's/team's OWN signature catchphrase as a synergy call-out is permitted within confirmed Marvel/Upper Deck license scope; the faction-cry seed set is licensing-gated; render is an identity-string swap, no new engine field; renumbered from the colliding D-24238 draft in PR #979 — D-24238 landed as the deploy stale-bundle fix, #983; 2026-07-27, infra/synergy-callout-wiki)
- D-24260 — gauntlet-pack-identity-only (the downloadable Mastermind Gauntlet pack is an identity-only import token — pack_version + gauntlet{setAbbr,mastermindSlug,division,playerCount}; the server re-resolves legs + approved compositions from the live registry, the pack never carries them; strict per-pack_version validation rejects an unknown MAJOR version and unknown fields; DRAFTED here, landed at WP-440 execution; 2026-07-27, claude/wp440-gauntlet-pack)
- D-24261 — legends-inline-pack-build-and-showcase-pin (legends-board builds the gauntlet pack INLINE via a type-only contract import — NOT a runtime registry dependency — to preserve its vue-sole-runtime-dep / zero-API invariant, WP-343/345; plus the showcase-pin policy (core/magneto pinned first on the gauntlet index, display-only) and the gauntlet-<set>-<mm>-<div>-p<N>.gauntlet.json download-filename convention; DRAFTED here, landed at WP-441 execution; 2026-07-27, claude/wp441-legends-download)
- D-24262 — gauntlet-run-derived-progression-lock (THE load-bearing lock of the Mastermind Gauntlets epic: gauntlet progression is read-only DERIVED state — no future work may store leg-cleared / champion / pool-validity flags without a superseding architecture decision; plus the active-run uniqueness key (player_id, set_abbr, mastermind_slug, division, player_count) WHERE first_completed_at IS NULL, first_completed_at = write-once audit not championship truth, and run legs live in leg_picks jsonb NOT player_loadouts (the 50-cap untouched); reserved by WP-443, DRAFTED here, landed at WP-443 execution; 2026-07-27, claude/wp443-run-persistence)
- D-24263 — registry-viewer-gauntlet-pack-consumer (the Registry-Viewer resolves a WP-440 identity pack CLIENT-SIDE / zero-API from the bundled registry into a prefilled builder leg-loadout — a new "Load Gauntlet Pack" importer validates via validateGauntletPack, a leg/scheme picker + default-variant-0 approved composition from GAUNTLET_LOADOUT_MENUS + playerCount, heroes left empty; supply-pile counts stay at builder defaults 30/30/30/0, PLAYER_COUNT_SETUP carries only heroCount; unknown-gauntlet / unoffered-count → friendly message not a schema wall; the cards-side consumer of the identity-only token, the play-server import is WP-5, both resolve the same tiny token from the registry — the payoff of the identity-only design D-24260; reserved by WP-444, DRAFTED here, landed at WP-444 execution; 2026-07-28, claude/wp444-cards-consumer)
- D-24264 — gauntlet-run-import-idempotency-and-api-shape (the play-side import + run-CRUD API contract for /api/me/gauntlet-runs: IMPORT IS IDEMPOTENT — POST validates a WP-440 pack + confirms the gauntlet exists in the registry, then attaches to the existing ACTIVE run on the partial-unique (player_id,set_abbr,mastermind_slug,division,player_count) WHERE first_completed_at IS NULL conflict and returns it 200, never a 409/500; a fresh POST after first_completed_at is set opens a NEW active run; the run is INSERTed with EMPTY leg_picks — heroes start empty per the epic's pick-before-play rule; player_id-scoped like loadoutLibrary (cross-account → 404, no existence leak), Cache-Control: no-store, a closed GauntletRunErrorCode union; complements D-24262 which locks NO-stored-derived-state — this locks the API/derivation SHAPE, so the raw GET returns identity + leg_picks + timestamps only and the DERIVED read is the WP-446 follow-on consuming gauntletTruth.logic.ts; reserved by WP-445, DRAFTED here, landed at WP-445 execution; 2026-07-28, claude/wp445-run-api)
- D-24266 — villain-unmarked-timing-line-hollow (a printed villain/henchman Fight/Ambush/Escape line with NO `[effect:]` marker now records a `no-handler` hollow breadcrumb — mechanic `unmarked-ability` — instead of being silently dropped; supersedes WP-188's marker-free silent-skip; observability only, no gameplay change; the un-implemented mechanic itself is WP-447; 2026-07-28, infra/villain-unmarked-hollow-breadcrumb)
- D-24265 — gauntlet-run-derived-progression-semantics (the single-run derived-progression contract for GET /api/me/gauntlet-runs: the 5-state run status is DERIVED at read time from leg_picks + competitive_scores — needs-heroes (no leg has heroCount picks) | ready (>=1 leg fully picked, 0 cleared) | playing (>=1 cleared, not all) | all-legs-cleared (every leg cleared but NO budget-valid pool) | champion (every leg cleared AND a budget-valid fixed pool) — with all-legs-cleared and champion DISTINCT states; plus the derivation SHAPE: pool = union of leg_picks hero ids, budget headroom = heroCount+2 minus pool size, per-leg last-played = max(created_at) over the caller's qualifying scores, champion via findBestPoolAssignment over the caller's winning teams; the run read REUSES qualifiesAsLegClear + findBestPoolAssignment from WP-442 gauntletTruth.logic.ts with the SAME injected inputs the leaderboard uses so tracker + leaderboard cannot drift; complements D-24262 no-stored-derived-state and D-24264 import/API-shape by locking the run-status DERIVATION semantics + view shape; reserved by WP-446, DRAFTED here, landed at WP-446 execution; 2026-07-28, claude/wp446-progress)
- D-24267 — villain-scry-ko-own-deck-primitive (the sixth villain effect primitive `scry-ko-own-deck` — no-param, appended to the closed VILLAIN_EFFECT_PRIMITIVES union + array at position 6; look at min(2,deck.length) of the current/defeating player's deck, KO exactly one via the deterministic worst-first selectScryKoTarget — Wound → starting-SHIELD → lex-lowest — and leave the other on top; empty deck reachable no-op never hollow; AUTO-RESOLVED for this WP — the interactive player pick is a deferred follow-on per the koHeroCurrentPlayer WP-185→WP-242 precedent, because a block-all pending state without UX hard-freezes the client; Wounds are KO-preferred here unlike selectKoHeroTarget because this thins the deck; determinism-adjacent, no ctx.random/I/O/new-G-field; reserved by WP-447, DRAFTED here, landed at WP-447 execution; 2026-07-28, spec/wp-447-scry-ko-mechanic)
- D-24268 — composition-to-match-launch-primitive-and-wp303-supersession (the arena-client composition→match launch is now a SINGLE reusable primitive — launchMatchFromComposition in apps/arena-client/src/lobby/useCreateMatchFromComposition.ts — a never-throw async fn that runs createMatch(config, playerCount, token) → persistMatchSetup → joinMatch(seat 0) → navigate and returns a typed { ok:true, matchID } | { ok:false, message } result; the lobby's loadout-JSON path (submitFromJson) AND manual-create path (submitCreate) both consume it as the single source, replacing two byte-identical inline chains with ZERO behavior change; createWithBotAlly / joinExisting / startAutoplay stay separate primitives by API surface. SUPERSESSION: this narrows the long-deferred WP-303 placeholder — the "Load into lobby / play a saved loadout" integration cited in WP-302's WORK_INDEX row — whose launch MECHANISM is now this primitive; a future WP-303 becomes wiring a call site (a leg's approved composition + picked heroes → launchMatchFromComposition), not building the create-and-join chain. D-24087 (Profile Loadout Library, Active) is a separate profile surface and is UNAFFECTED. Reserved by WP-448, DRAFTED here, landed at WP-448 execution; 2026-07-28, claude/wp448-match-primitive)
- D-24269 — profile-gauntlet-tracker-ux-and-play-this-leg-composition (the `?route=me` gauntlet tracker UX contract + the resolution of the Play-this-leg composition-assembly question. Play this leg needs a nine-field MatchSetupConfig; the run stores only leg_picks (heroes). arena-client CANNOT import the registry, so villains/henchmen (approved variant 0, D-24199) + the four supply counts (no registry source) are supplied by the SERVER via an additive `launch` block on the WP-446 GauntletRunProgressView; the client derives only schemeId=`${setAbbr}/${legSchemeSlug}`, mastermindId=`${setAbbr}/${mastermindSlug}`, heroDeckIds=legPicks[scheme] and assembles the config. Locks: (a) tracker renders derived truth only, never recomputes status/champion/pool (D-24262); (b) all-legs-cleared ≠ champion visibly distinct; (c) chosen resolution = fold the minimal server launch-block into WP-449 over alternatives A2 (split to a server WP) / A3 (dedicated launch-config endpoint); (d) supply-count source sub-fork = fixed canonical server-wiring constants [recommended] vs a new registry per-count table — counts do NOT gate leg-clear (D-24187); (e) variant selector deferred (variant 0 only). Operator-reviewed forks: fold-vs-split, supply-count source, variant selector. Reserved by WP-449, DRAFTED here, lands at WP-449 execution; 2026-07-28, claude/wp449-tracker-ui)
- D-24270 — villain-gain-attached-hero-primitive (the seventh villain effect primitive `gain-attached-hero` — no-param, appended to the closed VILLAIN_EFFECT_PRIMITIVES union + array, append-only, position resolved against main per the WP-447 ordering coordination; dispatches to a DELIBERATE no-op handler returning targets:[] and mutating nothing — the real captured-hero return is the generic WP-431 awardAttachedHeroes at the fight site, run before the executor; the handler exists solely to make the printed `Fight: Gain that Hero` line a RECOGNIZED reachable effect so the D-24266 breadcrumb classifies it applied not no-handler/unmarked-ability; marks core skrull-queen-veranke + core skrull-shapeshifters + rvlt klaw; NO gameplay change, no new G mutation, but marking suppresses a HASHED G.diagnostics write (diagnostics is in the finalStateHash — hash-relevant where the 3 villains are fought; re-pin-free only because no pinned fixture defeats them, executor confirms); chosen over detector-suppression (no fight-site signal in the executor) and over moving the award into the handler (hash/ordering risk); reserved by WP-450, DRAFTED here, landed at WP-450 execution; 2026-07-28, spec/wp-450-gain-attached-hero)
- D-24271 — diagnostics-excluded-from-state-hash (INVARIANT: G.diagnostics — a runtime-only observation channel, WP-257 "never gameplay input" — is excluded from ALL state-hash surfaces; two LegendaryGameStates differing only in diagnostics hash identically under both hashGameState (the fixture-golden finalStateHash oracle, joining messages/logMeta/lastPlayEffectsFired) and computeStateHash (the production whole-G competitive/determinism oracle). Same rationale already accepted for messages/logMeta. Competitive-risk decision recorded: the computeStateHash change alters the competitive replayHash algorithm, but accepted scores are immutable + never re-verified (immune), the score math never reads diagnostics, and the only exposure is a one-time captured-pre/submitted-post transition window (rare, recoverable, no data loss) — operator sign-off at SPEC review, recommended default = hashGameState-only (zero competitive risk), both surfaces available for the unified end-state; reserved by WP-451, DRAFTED here, landed at WP-451 execution; 2026-07-29, spec/wp-451-diagnostics-hash-exclusion)
- D-24272 — bot-ally-coop-strength-metric (the Bot Ally Strengthening epic's success metric: co-op strength is measured by a deterministic win-rate + loss-cause-taxonomy harness over a fixed (config,seed) matrix — win = evaluateEndgame heroes-win; loss causes = scheme-completed / villains-escaped (escapedVillains ≥ ESCAPE_LIMIT) / tie / turn-cap-inconclusive; downstream bot WPs record their before/after number against it — NO committed win-rate artifact or freshness gate, the number is reported per-WP not regression-pinned; the separate dedicated-T3-ally-policy split, leaving the WP-049 CompetentHeuristic frozen as the balance baseline, is locked by a later WP's own D-entry; reserved by WP-452, DRAFTED here, landed at WP-452 execution; RENUMBERED from D-24263 after a landed collision; 2026-07-29, claude/wp452-bot-ally-winrate-harness)
- D-24273 — simulation-setup-shuffle-seeded (INVARIANT: every simulation setup path — simulateOneGame, simulateOneCoopGame, simulateOneGameAndCaptureMoves, par.aggregator's simulateOneGame — AND the fixture-replay harness runFixture shuffle the setup deck with the run-seeded mulberry32 Fisher–Yates (shuffleWithPrng, the same PRNG domain the per-turn reveal loop uses), NOT the makeMockCtx reverse mock; makeMockCtx stays reverse for unit tests, its reverse being a deliberate shuffle-ran proof pinned by buildInitialGameState.determinism.test.ts + replay.execute.test.ts PRE_WP080_HASH with ~190 importers; rationale: the reverse of the lexically-sorted setup deck clusters all virtual scheme-twist ids on top of the villain deck, so chained-reveal schemes (midtown-bank-robbery / negative-zone-prison-breakout) cascade into a turn-0 SCHEME_LOSS and every scheme's twists are front-loaded, making simulated win rates measure the shuffle artifact rather than play; consequence: sentinel-core-doom-2p fixture re-recorded, no committed PAR artifact so no PAR re-pin, production real-PRNG shuffle never affected; reserved by WP-453, DRAFTED here, landed at WP-453 execution; 2026-07-29, claude/kind-austin-dde1e0)
- D-24274 — gauntlet-loadout-qualification-guard-is-advisory (RENUMBERED from D-24273 after a landed collision; the cards.legendary-arena.com Loadout Builder surfaces gauntlet-leg qualification at BUILD time as a client-side ADVISORY never a gate: a pre-play badge from checkGauntletQualification comparing the draft villainGroupIds+henchmanGroupIds+playerCount against the mastermind approved GAUNTLET_LOADOUT_MENUS by EXACT set equality on full set-qualified ext_ids — stricter than the server bare-slug ScenarioKey villain-segment, correct because draft+menu both hold full ext_ids, and cannot false-negative per D-24131 same-set + exact henchman_key; the server matchesApprovedLoadout WP-395/D-24199 stays the SOLE adjudicator untouched so the badge is a prediction not enforcement and never blocks build/export/launch; a WP-444 pack-sourced draft auto-LOCKS villain/henchmen read-only heroes-only with an explicit Unlock adversaries opt-out, a hand-built draft never auto-locked but still shows the badge; D-24187 fixed-hero-pool constraint OUT of scope being cross-leg server-adjudicated; reserved by WP-454, DRAFTED here, landed at WP-454 execution; 2026-07-29, claude/wp454-gauntlet-loadout-qualification-guard)
- D-24275 — local-workindex-row-check-single-source (the WORK_INDEX row-format drift guard is exposed as a LOCAL pnpm workindex:rows:check that REUSES the single-source canonical WORK_INDEX_ROW_PATTERN by extracting it from apps/dashboard/scripts/build-governance-snapshot.mjs — the same text-extraction the dashboard workIndexRowPattern.test.ts uses, so there is exactly ONE canonical regex and the local check can never drift from the CI guard; CI enforcement remains the dashboard Dashboard Gates test, the local check is an authoring-time mirror not a replacement; no git-hook framework introduced, no dashboard change; reserved by WP-455, DRAFTED here, landed at WP-455 execution; 2026-07-29, claude/wp455-local-workindex-row-check)
- D-24276 — legends-mastermind-details-reveal-is-client-only (the legends board surfaces each gauntlet's approved composition — schemes + villain/henchmen groups per player count — as a client-only Show-details reveal rendered from the ALREADY-PUBLISHED gauntlet-index snapshot fields GauntletIndexEntry.legs + .approvedLoadouts WP-395/D-24199; NO publisher/server/snapshot-contract change and NO registry import — the board stays vue-only/zero-API and the data is already parsed and in hand, used today only for challenge links; this completes the D-24199 intent that the qualification requirement be VISIBLE to players D-24186/D-24190; the separate challenge-link/URL-param prefill fix in registry-viewer is out of scope; reserved by WP-456, DRAFTED here, landed at WP-456 execution; 2026-07-29, claude/wp456-legends-mastermind-details-reveal)
- D-24277 — challenge-link-count-consistency (the legends gauntlet INDEX Challenge CTA emits a player-count-consistent link: the SAME player count — the row's selectionFor(board).playerCount — feeds BOTH selectApprovedLoadout(entry, count) and buildChallengeUrl(..., count, loadout), so the pinned approved villains/henchmen and the URL playerCount agree and the cards builder opens on a QUALIFYING loadout; fixes the prior firstLegChallengeUrl which pinned the solo count-1 loadout but emitted no count so the builder defaulted to 2 and judged a solo composition at 2 players; the per-leg GauntletBoardPanel links already did this — this aligns the index CTA; the registry-viewer consumer is untouched because D-24190 applyPreviewToDraft already applies URL villains/henchmen onto the draft; client-only zero-API, no publisher/server/snapshot change; reserved by WP-457, DRAFTED here, landed at WP-457 execution; 2026-07-30, claude/wp457-challenge-link-count-consistency)
- D-24278 — collapse-approved-loadouts-to-one-variant (the approved gauntlet villain/henchmen menu is ONE canonical configuration per mastermind per player count — variant 0 — NOT the D-24199 menu of three; so ranked gauntlet qualification fixes the adversaries and the ONLY competitive variable is the heroes, a fair fixed-course time-trial that concentrates leaderboard scores 3× and cuts the PAR scenario count ~6,354→~2,118; casual play free selection and the core D-24199 rule (ranked legs require an approved loadout) are unchanged — only the menu size shrinks; done while competitive_scores is empty so zero scores are re-keyed or invalidated; single-lever VARIANTS_PER_MASTERMIND=1 + regenerate; supersedes D-24199 menu-size only; reserved by WP-458, DRAFTED here, landed at WP-458 execution; 2026-07-30, claude/wp458-459-collapse-and-reveal-layout)
- D-24284 — mastermind-reveal-or-discard-interactive (RESERVED: the mastermind Master-Strike "each player reveals an X-Men Hero or discards down to N cards" conditional + the INTERACTIVE discard-to-N choice. The reveal-check reads `G.cardTraits` team===x-men HAND-ONLY (strike is start-of-turn, inPlay empty — NOT the D-24281 hand+inPlay scope); the discard choice mirrors the WP-242/243 KO-a-Hero pending-choice pattern (queue + resolve move + block-all guard + UIState projection + client prompt + deterministic bot/sim default). Marks Magneto (the reported strike); other reveal-or-discard masterminds are a data-only follow-on. Reserved by WP-476, DRAFTED at draft time, landed at WP-476 execution; 2026-07-31, claude/spec-bug2-master-strike)
- D-24283 — per-scheme-approved-loadouts (the ranked gauntlet approved-loadout config is keyed per (SCHEME × mastermind × player-count) — a fully-authored YEAR-KEYED JSON, one ordered pool per scheme scaled by REQUIRED_GROUP_COUNTS — so a mastermind's schemes field different approved villains/henchmen. It is a ROOT data-model re-key: the approved-loadout truth had NO scheme dimension [keyed per set×mastermind×count], scheme was only used by callers to group wins into legs. Re-keyed ADDITIVELY [new per-scheme loader + optional scheme param, absent → per-mastermind fallback] so the 5-point lockstep — generated menu/loader [WP-471], shared truth matchesApprovedLoadout/qualifiesAsLegClear + leaderboard + publisher + D-24279 coverage [WP-472], run-tracker leg-clear + per-leg launch GauntletRunLaunch [WP-473], client consumers + LoadoutBuilder [WP-474], arena-client Play-this-leg [WP-475] — migrates one WP at a time without breaking the build. SUPERSEDES D-24278's per-mastermind axis, keeps 1 variant/leg + D-24199 core rule; PAR ~2,118 UNCHANGED [schemes already keyed distinct ScenarioKeys]; competitive_scores empty so zero migration. Reserved by WP-472, DRAFTED here, landed at WP-472 execution; RENUMBERED off #1118 which took D-24282; 2026-07-31, claude/spec-per-scheme-gauntlet-arc)
- D-24282 — doombot-scry-interactive-choice (scry-ko-own-deck (Doombot Legion Fight, WP-447 D-24267) upgrades from AUTO-resolve to an INTERACTIVE player choice for HUMAN players, mirroring the WP-242/243 KO-a-Hero pending-choice pattern: the villainEffectScryKoOwnDeck parker pushes a PendingScryKoChoice{choiceType:'scry-ko', playerID, revealedCardIds:[the top min(2,deck.length)]} onto a new runtime G.pendingScryKoChoices FIFO queue when deck≥2 and KOs nothing yet; a 1-card deck still auto-KOs (no choice) and 0 is a no-op (the WP-242 0/1/≥2 branch). resolveScryKoChoice({G,playerID},{cardId}) validates the FRONT entry (playerID + choiceType + cardId∈revealedCardIds), KOs the chosen card from the deck leaving the other on top, front-pops; all invalid states silent no-ops (moves never throw), queue byte-identical on no-op so the player resubmits. A hasPendingScryKoChoice predicate blocks every action move + the End-Turn/advanceStage gate while pending (the WP-242 block-all pattern) so the choice cannot be skipped — shipping the pending state WITHOUT this + the UIState projection + the client prompt would hard-freeze the client (project_pending_choice_no_ux_freeze), so all three ship together. UIState gains pendingScryKoChoice (front-of-queue, ≤2 revealed cards + a WP-249/D-24020 filtered private variant visible only to the choosing player). DETERMINISM PRESERVED for bots/sims: ai.legalMoves + simulation.runner + par.aggregator resolve the pending via selectScryKoTarget (the same worst-first card the old auto-resolve KO'd), so par/replay stay faithful and only live HUMAN play gets the prompt — but bot fixtures now park→resolve (2 moves vs 1) with a new hashed G.pendingScryKoChoices field, so a fixture re-pin is LIKELY and game.test.ts move-set +1 (resolveScryKoChoice). Cross-layer (game-engine parker/move/projection/guards/bot + arena-client PendingScryKoChoicePrompt.vue), single WP with per-layer allowlist + engine-then-client order; contract change (new pending-choice type + move + G field + UIState field) recorded by this D-entry; reserved by WP-470, DRAFTED here, landed at WP-470 execution; hard-deps WP-447 + WP-242/WP-243; 2026-07-30, SPEC-doombot-scry-interactive)
- D-24281 — villain-reveal-or-wound-primitive (the eighth villain effect primitive `reveal-or-wound` — a CONDITIONAL each-player effect parameterized by a hero-trait predicate {requireKind:'team'|'hero-class', requireValue}, appended to the closed VILLAIN_EFFECT_PRIMITIVES union + array at position 8 append-only with the drift test moved to 8; grammar reveal-or-wound:<team|hc>:<value> where hc/team mirror the card-text [hc:X]/[team:X] namespaces + the D-24076 [require-to-defeat:<kind>:<value>] grammar, hc→'hero-class' at parse; the descriptor gains requireKind + requireValue fields, requireValue normalized to the cardTraits slug space (normalizeTraitSlug). Semantics auto-resolved: for EACH player in Object.keys(playerZones).sort() the handler scans that player's HAND for a hero whose G.cardTraits {team,heroClass} matches and, only on NO match, applies gainWound (mirroring gain-wound:each's woundsDrawn bump when the wounded player is the current player) — a hand-only predicate DELIBERATELY narrower than D-24076 playerMeetsDefeatRequirement (hand+inPlay) because the printed 'reveal' is a hand action; a matching player reveals for free. No player choice (revealing to avoid the Wound is optimal for a Wound-averse player; the rare Wound-synergy decline is a documented deferral). Keyword-less descriptor so descriptorToLegacyKeyword→undefined and descriptorKey NOT extended, self-narrates via pushLog naming which players were wounded (scry-ko D-24267 precedent); frozen 10-keyword surface untouched. Marks CORE only 5 cards / 8 markers (sabretooth/frost-giant/zzzax each fight+escape, ymir ambush, ultron escape; every core Same-effect Escape marked) + removes stale _unassigned rows; ~35 cross-set instances data-only follow-on, conditional/compound variants deferred. Determinism-adjacent: no ctx.random/I/O/new-G-field but a REAL hashed Wound so re-pin LIKELY across these common core villains, executor confirms by running the suite; contract change (new primitive + descriptor fields) recorded by this D-entry; reserved by WP-469 (RENUMBERED from WP-468 after the concurrent WP-468/EC-503 collision), DRAFTED here, landed at WP-469 execution; 2026-07-30, SPEC-sabretooth-reveal-or-wound)
- D-24280 — villain-ko-hero-zone-param (source-zone-restricted each-player Hero KO is an optional zone?:'discard'|'hand' param on the existing ko-hero primitive NOT a new primitive — a targeting variant per D-24023 so VILLAIN_EFFECT_PRIMITIVES + drift test are untouched; grammar ko-hero:each:N:zone in the parser + apply-effect-markers.mjs local validator; new koHeroesFromZoneForPlayer KOs up to magnitude from ONLY the named zone reusing selectKoHeroTarget Wounds-excluded starter-first-then-lex-asc with NO cross-zone fallback, short/empty zone reachable no-op, wired into villainEffectKoHero each branch; descriptorKey DELIBERATELY omits zone so the zone-bearing descriptor reverse-maps to koHeroEachPlayerMag2 and narrates per-target KO'd-hero names for free WP-316 keeping the frozen 10-keyword surface + injective round-trip test untouched — a new ko-hero-from-zone primitive rejected for adding drift + needing its own self-narration; marks Juggernaut only core brotherhood/juggernaut ambush discard + escape hand, the other 11 corpus Each-player-KOs-from-their-zone lines carry a filter/choice clause deferred; inPlay not admissible, zone honored only on the each branch, interactive choice out of scope; determinism-adjacent no ctx.random/I/O/new-G-field but a REAL hashed KO so re-pin where a fixture reveals/escapes Juggernaut executor confirms; contract change a .types.ts field recorded by this D-entry; reserved by WP-463, DRAFTED here, landed at WP-463 execution; 2026-07-30, SPEC-onambush-juggernaut)
- D-24279 — legends-set-details-coverage-published (the legends gauntlet-index snapshot gains an additive optional sets: SetDetails[] field — one entry per set with ≥1 scheme, carrying the FULL per-set roster of masterminds/schemes/villain-groups/henchman-groups with the registry's authoritative names, and per villain/henchman group a usedByGauntlets boolean = its set-qualified setAbbr/slug id appears in ≥1 approved gauntlet config across the set's masterminds at any player count; masterminds/schemes carry no flag being covered by construction. Computed ONCE server-side in a pure buildSetDetailsCatalog from the wiring-injected set rosters + the approvedLoadoutsByGauntlet menu, so the zero-API legends board stays purely presentational and the snapshot is self-describing; the legends module keeps its no-registry-import lock, rosters ride the same server.mjs→scheduler→publishAllBoards injection path as gauntletCatalog. This REPORTS the villain/henchman coverage gap surfaced 2026-07-30 — across a set's masterminds the approved menu caps at ~4 villains/2 henchmen alphabetically-first so e.g. Core covers 4/7 villains + 2/4 henchmen — it does NOT close it; the operator chose ship-transparency-now, leaving whether a set challenge should fight everything as a separate decision. Additive+optional so a pre-WP-461 consumer ignores it; the client mirror WP-462 renders the flag verbatim without recompute. Reserved by WP-461, DRAFTED here, landed at WP-461 execution; 2026-07-30, claude/draft-set-details-reveal)
- D-24285 — reveal-look-empty-deck-reshuffle (RESERVED: reveal/look-at-top effects reshuffle the discard pile into the deck when the deck is exhausted mid-effect, matching the standard Legendary rule and the `drawCardsIntoHand` full-draw precedent, SUPERSEDING D-21502's deferred "reveal never reshuffles" no-op AND the WP-447 scry no-reshuffle stance — a SCOPED supersession limited to the heroEffectReveal peek-loop + villainEffectScryKoOwnDeck handler; the D-24200 reveal-eight mastermind strike KEEPS its deliberate no-top-up (a top-up there would turn the strike into a benefit) and is NOT superseded. Applies uniformly to the whole hero reveal family [heroEffectReveal peek loop — reveal-draw / reveal-ko / reveal-cost-attack / choose-discard-or-return / reveal-count>1] and to the villain scry handler [villainEffectScryKoOwnDeck — reshuffle to top up toward its look-2 before the 0/1/≥2 branch]. Reshuffle appends shuffleDeck(discard) after any cards already left on top, guarded by discard.length>0 so it terminates when nothing remains anywhere; determinism preserved via the injected ctx.random.Shuffle [no Math.random, no new G field, no snapshot change]. Villain side threads `random` through executeVillainAbilities [the villainDeck.reveal.ts RevealContext {random,ctx:{currentPlayer}} precedent]. Determinism-adjacent: any replay/record-game fixture that hits an empty-deck reveal/scry gets a new finalStateHash [intended — regenerate-with-note per the WP-447 pattern]. Reserved by WP-478, DRAFTED here, landed at WP-478 execution; 2026-07-31, claude/wp478-empty-deck-reshuffle-reveal-scry)
- D-24286 — reveal-remainder-interactive-reorder (RESERVED: The Amazing Spider-Man's "Put the rest back in any order" becomes an INTERACTIVE player reorder of the reveal remainder — the revealed-but-not-drawn cards, which the reveal loop leaves on top of the deck. A new HeroEffectDescriptor.reorderRemainder flag [set from a card marker on Amazing Spider-Man] makes heroEffectReveal, after its peek loop, park a PendingReorderChoice{choiceType:'reorder-deck-top', playerID, cardIds:[the remainder on top, in current order]} onto a new runtime G.pendingReorderChoices FIFO when ≥2 remained; resolveReorderChoice({G,playerID},{orderedCardIds}) validates the submission is a permutation of the parked ids and rewrites the top-N of the deck to that order, front-pops; <2 remainder auto-skips [nothing to order]; all invalid states silent no-ops [moves never throw]. A hasPendingReorderChoice predicate blocks every action move + the End-Turn/advanceStage gate while pending [the WP-470/476 block-all pattern] so the choice cannot be skipped; shipping the pending state WITHOUT the UIState projection + client prompt would hard-freeze the client [project_pending_choice_no_ux_freeze], so all three ship together. UIState gains pendingReorderChoice [front-of-queue, the remainder cards face-up, a D-24020 filtered private variant visible only to the choosing player]. Determinism preserved for bots/sims: ai.legalMoves + simulation.runner + par.aggregator resolve via an IDENTITY-order default [leave the remainder as the loop left it] so par/replay stay faithful and only live HUMAN play gets the prompt — but bot fixtures now park→resolve [+1 move] with a new hashed G.pendingReorderChoices field, so a fixture re-pin is LIKELY [finalStateHash + PRE_WP080 sentinel dual re-pin per reference_hashed_g_field_dual_repin] and game.test.ts move-set +1 [resolveReorderChoice]. NO ctx.random [reorder is a pure permutation]. Cross-layer [game-engine parker/move/projection/guards/bot + arena-client ReorderChoicePrompt.vue], single WP with per-layer allowlist + engine-then-client order; contract change [new pending-choice type + move + G field + UIState field + HeroEffectDescriptor.reorderRemainder] recorded by this D-entry; marks Amazing Spider-Man only, other reveal-and-reorder cards a data follow-on. Reserved by WP-479, DRAFTED here, landed at WP-479 execution; hard-deps WP-470 + WP-476 [pending-choice machinery precedents]; 2026-08-01, claude/wp479-reveal-remainder-reorder)
- D-24287 — villain-escape-becomes-scheme-twist (RESERVED: a villain's escape can trigger the active scheme's Scheme Twist — the first time the villain-escape path invokes the scheme-twist rule pipeline. New `become-scheme-twist` VILLAIN_EFFECT_PRIMITIVE [append-only union + array + drift; no-param token `[effect:become-scheme-twist]`; parseParameterizedEffect no-param branch; local array in apply-effect-markers.mjs] with a NO-OP executor handler `villainEffectBecomeSchemeTwist` returning {targets:[]} — its sole purpose is breadcrumb-suppression [a marked hook is neither unmarked (D-24266) nor hollow (WP-257)]; the executor cannot reach the rule pipeline [no implementationMap/hookRegistry-execution], so it does NOT itself trigger the twist. The MECHANIC is a fire-site bridge in villainDeck.reveal.ts's escape branch: after executeVillainAbilities('onEscape'), a helper scans the escaped card's onEscape hooks for the become-scheme-twist descriptor and, if present, runs executeRuleHooks(G, context, 'onSchemeTwistRevealed', {cardId: escapedCard}, G.hookRegistry, implementationMap) + applyRuleEffects(G, context, effects) — the SAME two-call pattern the reveal path uses for a revealed scheme-twist card (villainDeck.reveal.ts:398-411,429). The active scheme's twist resolver runs, schemeTwistCount increments, and the loss threshold is checked, via the generic buildGenericTwistEffects. Mystique still escapes normally: resolvers use twistCardId ONLY to stamp a schemeTwistResolved notableEvent [no card is routed to a twist pile], so passing the escaped ext_id is display-only. Marks Mystique [core brotherhood/mystique] Escape only [2 copies]; other 'becomes a scheme twist' villains are a data follow-on; the fire-site bridge is escape-only [Mystique's timing]. Determinism-adjacent: NO new G field, but a REAL hashed schemeTwistCount increment + a schemeTwistResolved notableEvent from a new trigger path → finalStateHash + PRE_WP080 sentinel re-pin LIKELY where a fixture escapes Mystique [regenerate-with-note]; the resolver may consume ctx.random, threaded via the RevealContext already passed to executeRuleHooks. Contract change [new closed-union primitive] recorded by this D-entry. Reserved by WP-481, DRAFTED here, landed at WP-481 execution; hard-deps D-24266 + WP-252 + WP-200 [schemeTwistHandler/onSchemeTwistRevealed pipeline]; 2026-08-01, claude/wp481-mystique-escape-scheme-twist)
- D-24288 — dococt-reveal-eight-topup (RESERVED: Doctor Octopus's Master Strike reveal-eight branch reshuffles the discard to reveal a FULL 8 when the deck is short, per the standard Legendary reveal-reshuffle rule — SUPERSEDING D-24200's "a short deck is revealed as-is, never topped up" clause AND the D-24285 reveal-eight carve-out ["the D-24200 reveal-eight strike KEEPS its no-top-up"]. Both deferrals are now moot: D-24200 cited "a reshuffle would need an interaction model this work does not introduce" but the top-up needs NO interaction model [the printed text is "put the rest back in RANDOM order" — a shuffle, not a player reorder], and WP-478 shipped the reusable reshuffleDiscardIntoDeck helper. In resolveDoctorOctopusReveal [mastermindHandlers.ts]: when deck.length < 8 and discard is non-empty, reshuffle the discard into the deck [reshuffleDiscardIntoDeck, wrapping the handler's shuffleFunction as a ShuffleProvider {random:{Shuffle:shuffleFunction}}; skip when shuffleFunction is null] before revealCount = min(8, deck.length); then reveal, discard non-grey Heroes [heroClass != null], and return the remainder in random order [D-24200's shuffle-back, UNCHANGED]. game-engine only [rules/mastermindHandlers.ts + test]; reuses the WP-478 helper [import from moves/drawCards.logic.js — both game-engine, pure]. Determinism-adjacent: the top-up consumes an extra ctx.random.Shuffle when the deck is short → a fixture that hits a short-deck reveal-eight gets a new finalStateHash; D-24200 states both hash oracles pin core/dr-doom and the reveal-eight branch is NOT reachable from the pinned fixture, so NO re-pin expected — confirm empirically. No new G field, no contract change [no new primitive/keyword — a mastermind-strike resolver internal]; a rationale-only D-entry. Reserved by WP-482, DRAFTED here, landed at WP-482 execution; hard-deps WP-397/D-24200 [the reveal-eight branch] + WP-478/D-24285 [reshuffleDiscardIntoDeck]; 2026-08-01, claude/wp482-dococt-reveal-eight-topup)
- D-24289 — effect-implementation-index (RESERVED: the generated Effect Implementation Index is a DERIVED join of the two committed mechanic ledgers into one dual-scope, schema-validated, CI-gated `data/metadata/effect-implementation-index.json` — never a hand-authored per-card lookup [the anti-pattern the marker/coverage system avoids]. Locks: top-level scope literal `"all"`; per-entry {extId,name,set,scope∈hero|villain,mechanic,status∈executable|deferred|condition|unsupported|unmarked,handler,wp,decision}; handler/wp/decision are VERBATIM ledger pass-through [populated for resolved rows in BOTH scopes; "" only on unsupported/unmarked; never fabricated]; mechanic token verbatim [(unmarked) sentinel stays literal, NOT normalized]; summary{totalEntries,byScope,byStatus} with fixed 5-key byStatus order; entries sorted (extId,mechanic); cards{} join with scope agreement; deterministic generatedAt sentinel [no Date.now()]; `EffectImplementationIndexSchema` data-only zod on the /schema subpath with .superRefine count+join+scope rejects; `effect-index:check` freshness gate in hero-effect-coverage. Co-locating handler metadata in the engine handler maps, per-ability-line/per-descriptor granularity, mechanic-token normalization, and the /debug/effects viewer + runtime tracing are explicitly DEFERRED. game-engine untouched; a rationale+contract D-entry for a derived data artifact. Reserved by WP-484, DRAFTED here, landed at WP-484 execution; hard-deps WP-269/D-24046 + WP-271; 2026-08-01, claude/wp484-effect-implementation-index)
- D-24290 — villain-effect-vocab-tier-a (RESERVED: adds the Tier-A auto-resolve VILLAIN_EFFECT_PRIMITIVES to the closed union + their parser grammar + handlers + apply-effect-markers.mjs vocabulary sync + card markers, implementing 3 unmarked Core villain abilities [Enchantress/Destroyer/Baron Zemo; Viper deferred to a later interactive tier]; append-only union+array+drift per D-24034; no new G field, no persistence/determinism surface [auto-resolve; no interactive pending-choice, no ctx.random]; contract change [new closed-union primitives] recorded here. Reserved by WP-485, DRAFTED at draft, landed at WP-485 execution; hard-deps D-24266 [unmarked breadcrumb] + D-24034 [unresolved-marker discipline]; 2026-08-01, claude/wp485-villain-effect-vocab-tier-a)
- D-24293 — ewiki-pagefind-search (amends the D-13808/D-13811 ewiki blanket JS-free CI enforcement to allow-list EXACTLY the one sanctioned chrome-level Pagefind search loader tagged `data-wiki-search`; adds the Pagefind build-time static index to wiki-viewer:build + the wiki-viewer CI gate + the render.yaml wiki buildCommand, mirroring the shipped www WP-005 pattern; no runtime backend and no content-side scripting so D-13808 constraints #1/#2 hold; determinism gate unaffected [Pagefind bundle excluded from the html+css hash, runs after the determinism step]; INFRA, no WP; 2026-08-01, infra/ewiki-pagefind-search)
- D-24295 — villain-effect-vocab-tier-b-city-spaces (Active (landed 2026-08-03 — game-engine 2281/0, no finalStateHash/PRE_WP080 re-pin, provenance map left unchanged): Tier B of the Core villain-effect-vocabulary arc — named city spaces + a location gate. Adds CITY_SPACE_NAMES as a pure engine constant [index 0-4; NO new G field], threads the fought cityIndex? to the villain-effect handlers, and a universal requireCitySpaces descriptor gate [@<space> marker suffix, fails closed]. NO new VillainEffectPrimitive [reuse gain-wound + capture-bystander; additive descriptor only: requireCitySpaces, each-other target, capture-bystander count]. Implements Abomination + the Lizard [Whirlwind deferred to Tier D, interactive]. Determinism: no new G field, but markers attach to the hashed villainAbilityHooks so re-pin only if a committed fixture deck includes these cards [none today]. Reserved by WP-489, DRAFTED at draft, landed at WP-489 execution; hard-deps WP-485/D-24290 + WP-252/D-24023 + WP-202 + WP-478/D-24285 + D-24266 + D-24034; renumbered from D-24291 [taken by #1160] on 2026-08-02; 2026-08-02, claude/wp486-villain-city-spaces)
- D-24296 — destroyer-shield-ko-basic-cards (Active: the Destroyer's ko-heroes-current-by-trait:team:shield KO is widened to also match the three teamless basic S.H.I.E.L.D. ext_ids [starting-shield-agent, starting-shield-trooper, pile-shield-officer] which carry no G.cardTraits entry — the KO handler ONLY, via a local koHeroMatchesTraitOrBasicShield predicate; the shared trait matchers are unchanged so the 65 corpus team:shield synergies + Baron Zemo rescue-count are unaffected. Narrow fix, not root-cause trait-tagging [operator ruling 2026-08-03]. No new G field/primitive; no re-pin. Reserved + landed by WP-490/EC-525; hard-deps WP-485/D-24290; 2026-08-03, claude/wp490-destroyer-shield-ko)
- D-24291 — instant-defeat-with-bystander (RESERVED: locks the instant-defeat hero-mechanic contract — new HERO keyword `defeat-with-bystander` [bare token, no magnitude] whose handler defeats one Villain in the city OR the Mastermind current tactic that HAS >=1 captured Bystander, WITHOUT spending attack, reusing the existing villain / mastermind-tactic defeat path [victory-pile move + onDefeat hooks + captured-Bystander rescue]. Target set = qualifying city villains + the mastermind-tactic when the Mastermind holds a captured Bystander; mandatory-if-able; 0 targets -> self-narrated no-op, exactly 1 -> auto-defeat [no prompt], >=2 -> an interactive PendingDefeatChoice [new hashed G.pendingDefeatChoices FIFO + resolveDefeatChoice move + block-all guard at every action site + UIState projection with the D-24011 private filter + deterministic bot/sim target default + arena-client prompt]. Card marker authored in hero-ability-markers.json for silent-sniper [core/msp1/3dtc] and applied by apply-hero-ability-markers.mjs [VALID_TOKEN_PATTERN gains the new token form]; new hashed G field -> dual re-pin only if a committed fixture plays it. Reserved by WP-486, DRAFTED at draft, landed at WP-486 execution; hard-deps D-24266 [unmarked breadcrumb] + the pending-choice precedents D-24284 / D-24286; 2026-08-01, claude/wp486-instant-defeat)
- D-24292 — debug-effects-viewer (RESERVED: the /debug/effects viewer is a READ-ONLY DASHBOARD consumer of the WP-484/D-24289 Effect Implementation Index — a build-time bundle copy of `data/metadata/effect-implementation-index.json` [the /coverage `build-coverage-ledger.mjs` precedent], schema-validated with `EffectImplementationIndexSchema`, rendered as a searchable per-card/per-mechanic view. It authors NO new effect data, adds NO second parser, and touches NO engine/registry/server/G surface [the anti-pattern D-24029/D-24046 avoid]. Runtime effect tracing + descriptor->handler granularity remain separate future WPs. Locks: host = apps/dashboard [ewiki debug-effects.md placement, beside /coverage + /debug]; the read-only-derived posture; the trace-deferred boundary; the build-time-bundle load path [mirrors /coverage, not R2]. Reserved by WP-487, DRAFTED here, landed at WP-487 execution; renumbered from D-24290 [taken by #1157]; hard-deps WP-484/D-24289; 2026-08-01, claude/wp-debug-effects-viewer)
- D-24299 — villain-effect-vocab-tier-d-viper (Active (landed 2026-08-03 — game-engine 2308 pass, no new G field, no re-pin): Tier D of the Core villain-effect-vocabulary arc — Viper's conditional each-player wound gated on a victory-pile villain-group predicate. Locks [1] a new keyword-less VillainEffectPrimitive gain-wound-unless-victory-villain-group [append-only union+array+drift, count 12→13]; [2] an additive descriptor field victoryVillainGroup [the target villain group slug, e.g. hydra]; [3] the marker grammar gain-wound-unless-victory-villain-group:<groupSlug>; [4] PATH B group identification — DERIVE the group ext_id prefix from the fought card's own ext_id [setAbbr = cardId before the unambiguous `-villain-` infix; prefix `${setAbbr}-villain-${groupSlug}-`] and match victory-pile villains by startsWith, excluding the fought cardId [`another`] — so NO new G field and NO villain-group map [a new setup-time G map would be hashed and force a BROAD finalStateHash+PRE_WP080 re-pin of ALL committed fixtures; Path B reads only already-hashed state]; [5] Viper marked on BOTH fight+escape. Handler mirrors reveal-or-wound [sorted per-player, supply-bound, woundsDrawn current-only, keyword-less self-narrate]. Determinism: no new G field, but the marker lands in the hashed villainAbilityHooks so re-pin only if a committed fixture deck includes core/hydra [none expected; verify at execution]. Reserved by WP-494, DRAFTED at draft, landed at WP-494 execution; hard-deps WP-469/D-24281 + WP-202/D-20203 + WP-252/D-24023 + WP-489/D-24295 + D-24266 + D-24034; 2026-08-03, claude/wp494-viper-tierd)
- D-24298 — villain-effect-vocab-tier-d-whirlwind (Active (landed 2026-08-03 — game-engine 2296 pass, arena-client unchanged, no re-pin; auto-vs-park rule landed in the physical-count P-vs-owed form per AC-3): Tier D of the Core villain-effect-vocabulary arc — the first INTERACTIVE villain effect: Whirlwind's location-gated magnitude-N current-player KO. Locks [1] the generalization of the ko-hero target:'current' handler to a magnitude M [park an interactive PendingKoHeroChoice only when eligible>M, else auto-KO min(eligible,M); M=1 is byte-identical to the koHeroCurrentPlayer path — additive per D-24034]; [2] the ko-hero:current:N [N>=2] marker grammar [magnitude 1 stays the bare ko-hero:current]; [3] REUSE of the WP-242 multi-KO pendingKoHeroChoices queue by pushing M entries [no new pending type, no resolve-move / UIState / arena-client change — the queue already projects remaining=queue-length and the prompt renders it] and REUSE of the WP-489 requireCitySpaces gate [Rooftops+Bridge; no gate-engine change]; [4] Whirlwind marker ko-hero:current:2@rooftops+bridge. NO new VillainEffectPrimitive [reuse ko-hero + the existing magnitude? field], NO new G field. magnitude>=2 is keyword-less [no legacy reverse-map] so it self-narrates. Determinism: no new G field, but the markers land in the hashed villainAbilityHooks and the KO pushes to the hashed pendingKoHeroChoices, so re-pin [dual] only if a committed fixture deck includes core/masters-of-evil [none expected; verify at execution]. Reserved by WP-492, DRAFTED at draft, landed at WP-492 execution; hard-deps WP-489/D-24295 + WP-242/D-24007 + WP-252/D-24023 + WP-202 + D-24266 + D-24034; 2026-08-03, claude/wp492-whirlwind-tierd)
- D-24297 — hero-ledger-per-design-granularity (RESERVED: locks the design-attribution hero-ledger contract — ADDITIVE, not a row re-partition. The hero mechanic ledger keeps ONE row per [extId x mechanic] and gains a `designs` array [slug-sorted {slug,name} of the carrying design(s), read verbatim from registry listCards; [] for the hero-level unmarked row] so /debug/effects + /coverage answer "which of the ~4 hero cards carries this effect" WITHOUT changing row identity — the ledger summary + /coverage metrics + card-mechanics.json all stay byte-identical [a per-design-row split was rejected: 246 hero×mechanic pairs carry a mechanic on 2+ designs → duplicate keys + inflated counts, and per-design unmarked would grow the glossary unmarked bucket 46→~231]. Unmarked stays hero-level [operator ruling]. Extends EffectImplementationEntrySchema with an additive optional `designs:{slug,name}[]` [.min(1) when present; marked-hero-only, villains + unmarked omit]. Determinism-neutral: ledger + index are BUILD-TIME generated artifacts, no G/ctx/hash surface, no re-pin. Reserved by WP-491, DRAFTED at draft, landed at WP-491 execution; hard-deps WP-484/D-24289 [effect index] + WP-487/D-24292 [/debug/effects viewer] + WP-259/D-24035 [/coverage viewer] + WP-271 [villain parity]; 2026-08-03, claude/wp491-hero-ledger-per-design)
- D-24294 — effect-runtime-tracing (RESERVED: locks the runtime effect-trace contract — a runtime-only structured trace of effect-descriptor dispatch [primitive + params + handler-map entry + zone-helper mutations + secondary fire site], NEVER in the hashed G [determinism-preserving; hash-excluded like the WP-451 diagnostics / hollow records]. Extends hollowEffect.record.ts to unify "handler never reached" + "handler reached but no mutation" + "secondary-site fire" in one trace surface. Key locks pending engine-surface research at draft: the exact hook point[s] in villainEffects.execute / heroEffects.execute, the trace data shape, and whether it is a hash-excluded G field or a module-level side channel [the determinism boundary]. Reserved by WP-488, DRAFTED at draft, landed at WP-488 execution; hard-deps WP-257/D-24034 + D-24287; 2026-08-02, claude/wp-effect-tracing)
- D-24300 — mastermind-tactic-onfight-framework (RESERVED: locks the Mastermind-Tactic-Fight execution contract. [1] defeatMastermindTacticCore [fightMastermind.ts] gains a tactic-onFight dispatch step — a per-tactic resolver keyed by the defeated tactic's ext_id, mirroring the WP-386/388 per-mastermind mastermindStrikeHandler dispatcher [hardcoded resolvers, NOT a data-driven marker vocabulary; operator ruling 2026-08-04 — extract a shared vocabulary later once ≥3 tactics reveal common primitives]. [2] Dispatch fires AFTER the tactic moves to the victory pile + bystander rescue, on the current player, with NO attack spend [the free defeat path stays as-is]. [3] The first resolver: co2e/doctor-octopus "Octet of Valence Electrons" sets a new OPTIONAL, lazily-created hashed field G.handSizeOverrides: Record<playerID,number> [per-player next-fill override; NOT seeded in buildInitialGameState] to 8, consumed + deleted at that player's next play-phase onBegin fill in place of HAND_SIZE [6]; this engine has NO end-of-turn cleanup draw, so "your new hand this turn" ≡ the defeating player's next onBegin fill. [4] No Magneto-cap interaction: MAGNETO_HAND_SIZE_LIMIT fires only when Magneto is the mastermind, Octet only when Doc Ock is — one mastermind per match, so the two never co-occur. Determinism: the field is gameplay-affecting [hashed], but lazy-create means no committed fixture [empty-registry PRE_WP080, core/dr-doom sentinel] creates the key → NO re-pin expected [verify both oracles at execution; drift = STOP, never blind-re-pin per reference_hashed_g_field_dual_repin]. onFight resolvers use ctx.random.* only where they reveal/shuffle [Octet does neither; follow-on High Octane will]. Out of scope: the other three Doc Ock tactics [separate WPs], other masterminds' tactics, tactic-effect narration richness beyond one log line, any card-data marker. Reserved by WP-497, DRAFTED at draft, landed at WP-497 execution; hard-deps WP-019 + WP-024 + WP-386/D-24188 + WP-388/D-24192 + WP-236; 2026-08-04, claude/wp497-tactic-onfight)
- D-24301 — hero-return-on-discard-ability (RESERVED: locks the return-on-discard reactive hero-ability contract [renumbered from D-24300 after a same-number race with #1215/#1217] — [1] new HeroKeyword `return-on-discard` [bare token, no magnitude] + the first reactive HeroAbilityTiming `onDiscard` [union+array lockstep, drift 33→34 keywords / 5→6 timings]; the keyword is a REACTIVE marker, NOT an onPlay-family effect. [2] A new forced-discard chokepoint discardFromHand[+ G-only checkReturnOnDiscard park helper] in the engine move layer, through which ALL card-effect hand→discard sources route [closed set of 5: resolveDiscardToPlay / discardChoice.resolve / mastermindHandlers.discardCardFromHand / schemeTwistResolvers.discardHand / dodgeCard], enforced by a cheat-proof drift-guard test [negative assertion: a synthetic hand→discard mutation outside the chokepoint FAILS the test]. [3] The reaction parks an OPTIONAL PendingReturnOnDiscard {playerID, cardId} on lazy-init G.pendingReturnOnDiscard [FIFO] — a "you may" decline-shaped choice {decline:true}|{cardId} [resolveOptionalPutBottomHQ template], accept moves discard→hand, decline pops [card stays in discard]. [4] Block-all guards at every action-move cluster + ai.legalMoves forced-resolve [bot default = accept, strictly beneficial + deterministic] + chooser-only UIState redaction. [5] The broad reading of "a card effect makes you discard" is LOCKED: the discard-to-play COST [operator's cited Determination/Optic Blast example] and the voluntary dodge discard both COUNT [in-scope]; normal end-of-turn cleanup does NOT [it never routes through the 5 sites]. Determinism: pendingReturnOnDiscard is lazy-init [never in Game.setup] so canonical-JSON omits it from the empty-replay final state → NO PRE_WP080_HASH / hashGameState re-pin; the gameplay-fixture finalStateHash re-pins ONLY if a committed fixture plays+returns Unending Energy [none expected; verify sentinel-core-doom-2p at execution]. Reserved by WP-498, DRAFTED at draft, landed at WP-498 execution; hard-deps WP-383/D-24184 + D-24139 + WP-248/D-24019 + WP-476/D-24284 + D-24266; 2026-08-04, claude/wp498-return-on-discard)
- D-24302 — join-by-match-id (Active (landed 2026-08-04): locks the join-by-reference contract — [1] the lobby accepts either a raw boardgame.io match ID or a full invite-link URL and extracts the match ID from the `match` query param [pure matchReference parser; trims whitespace; rejects empty/malformed with inline UX copy]; [2] resolution is a read-only single-match fetch [GET /games/legendary-arena/:id] so unlisted matches omitted from the public list are still joinable by ID; [3] the join reuses the existing joinMatch → POST /api/match/join at the first OPEN seat [no server change, no new endpoint, no new credential path]; [4] trust model is UNCHANGED from the existing WP-369 copy-link — a match is joinable by anyone holding its ID, exactly as it already is by anyone holding the shareable link; no new exposure. Scoped, rationale-only. Reserved by WP-499, DRAFTED at draft, landed at WP-499 execution; hard-deps WP-307/D-24092 + WP-369; 2026-08-04, claude/join-by-match-id)
- D-24304 — ewiki-canonical-source-frontmatter (Active (landed 2026-08-05): optional `canonical-source` front-matter for ewiki MIRROR pages — a singular repo-root-relative path naming the one upstream doc that owns a page's canonical prose [present only on mirror pages; must also be cited in `source`]. Consumed ONLY by editing-this-page.html [renders the mirror variant of #1225's "Editing this page" block, directing edits upstream then back]; NOT rendered in the metadata panel, so the six-field D-13810/EC-142 panel surface lock is untouched. AMENDS the D-13810 front-matter surface by ONE optional field. Schema: wiki/SCHEMA.md §Mirror pages; validation: check-links.mjs asserts the target exists + is cited in source. Only assignee today: development-workflow [its redundant hand-authored "Editing this page" block removed]. INFRA, no WP; 2026-08-05, spec/ewiki-canonical-source)
- D-24303 — auto-assign-handles (Active (landed 2026-08-05): locks the auto-assigned-changeable-handle contract — [1] `deriveHandle(displayName): string` slug rules [trim; lowercase; every run of non-`[a-z0-9]`→`_`; collapse `__`; strip leading/trailing `_`; ensure it matches `^[a-z][a-z0-9_]{2,23}$` (prefix a letter if it starts with a digit/underscore, pad/truncate to 3..24); empty-or-RESERVED_HANDLES result → `player` fallback]; [2] `assignAutoHandle(accountId, displayName, db)` idempotent + collision-safe [base, base2, base3…; a short ext_id-derived suffix as the final fallback; `UPDATE legendary.players SET handle_canonical=$, display_handle=$ WHERE ext_id=$ AND handle_canonical IS NULL`; leaves `handle_locked_at` NULL]; [3] wired into the account resolver AFTER `provisionPlayerAccount` for newly-provisioned accounts; [4] a one-time backfill via a NODE script reusing the SAME TS `deriveHandle`/`assignAutoHandle` [single source of truth — NOT a SQL migration, to avoid slug-logic divergence]; [5] AMENDS WP-101 mutual-presence invariant: `handle_canonical`+`display_handle` still move together, but `handle_locked_at` is now INDEPENDENT [NULL = auto-assigned/changeable; `now()` = explicit claim/immutable]. The change-handle/claim mechanism + `claimHandle`'s guard rewrite are OUT (paired follow-up). Scoped. Reserved by WP-500, DRAFTED at draft, landed at WP-500 execution; hard-deps WP-101 + WP-174 + D-24302; 2026-08-05, claude/wp500-auto-assign-handles)
- D-24306 — match-endgame-controls (Active (landed 2026-08-05): locks the End-Game / Play-Again model — [1] "End Game" is the MATCH_ENDED_EARLY endgame COUNTER, NOT boardgame.io events.endGame: the client-visible UIState.gameOver is derived by buildUIState from evaluateEndgame(G.counters), not ctx.gameover, so an events.endGame end would set ctx.gameover but NEVER surface the endgame panel [hard-freeze]. endMatchEarly [server-only move, client:false, NOT a CORE_MOVE_NAME; never throws; no-op when already over] sets the counter; evaluateEndgame checks it FIRST → { outcome:'tie', reason:'The players ended the match early.', endedEarly:true }; the top-level endIf ends the match for every seat. [2] endedEarly is an ADDITIVE optional field on EndgameResult + UIGameOverState [outcome STAYS 'tie'] — deliberately off the EndgameOutcome union to avoid its CompetitiveOutcome / migration-026 / dashboard / coop / campaign / sim blast radius. [3] An early-ended match is NEVER scored: server submitCompetitiveScoreImpl rejects endedEarly with a new ended_early SubmissionRejectionReason [union + SUBMISSION_REJECTION_REASONS array lockstep] BEFORE any INSERT [authoritative]; the client submit-on-gameover hook also skips the POST → 'ineligible' [advisory]. [4] "Play Again" relaunches SOLO with the same stashed loadout [readMatchSetup] + the same seat count [snapshot.players.length] via launchMatchFromComposition. No new top-level G field → no finalStateHash/PRE_WP080 re-pin [runtime-only counter]. Operator decisions: never-score-early-end + relaunch-solo. Scoped. Reserved by WP-502, landed at WP-502 execution; 2026-08-05, claude/wp-502-match-endgame-controls)
- D-24307 — doc-ock-villain-fight-hand-size (RESERVED: locks the core spider-foes Doctor Octopus VILLAIN Fight contract — a new keyword-less VillainEffectPrimitive override-next-hand-size [count 13→14, append-only union+array+drift] + the marker grammar override-next-hand-size:<N> that, at villain onFight, sets the current player's entry in the SHARED WP-497 hashed field G.handSizeOverrides [lazy-init] to <N> [Doc Ock N=8], consumed + cleared at WP-497's play-phase onBegin hand-fill. REUSE not re-build: no new G field, no new consumption point [WP-497 owns both]; this WP adds only the VILLAIN-side writer + the Doc Ock marker. Distinct execution path from the co2e mastermind Doc Ock Octet [WP-497 mastermind-tactic dispatcher] though the effect + field are shared. Live composition: the villain Doc Ock CAN co-occur with any mastermind [incl. Magneto's MAGNETO_HAND_SIZE_LIMIT trim], resolved at WP-497's single onBegin point — behavior verified at execution. Determinism: writes the WP-497-hashed handSizeOverrides so re-pin only if a committed fixture fights core/spider-foes/doctor-octopus [none expected; verify]. Only the core spider-foes Doc Ock marked [co2e/other-set villain twins deferred]. Reserved by WP-503, DRAFTED at draft, landed at WP-503 execution; HARD-DEP WP-497/D-24300 [the field + consumption; BLOCKED until it lands] + WP-252/D-24023 + WP-485/D-24290 + D-24266 + D-24034; 2026-08-05, claude/wp503-doc-ock-villain-fight)
- D-24305 — change-handle (Active (landed 2026-08-05): locks the change-handle contract — [1] new `changeHandle(accountId, requestedHandle, db)` in handle.logic.ts: `validateHandleFormat` [format+reserved, reused] → `UPDATE legendary.players SET handle_canonical=$2, display_handle=$3 WHERE ext_id=$1 AND handle_locked_at IS NULL RETURNING` [canonical from trim/lowercase, display preserves casing — the validateHandleFormat split]; 23505 → handle_taken; empty RETURNING → if the row exists with handle_locked_at set → handle_locked [NEW HandleErrorCode, union + HANDLE_ERROR_CODES array + drift-test lockstep], else unknown_account; a THIRD sanctioned writer of handle_canonical+display_handle [NEVER handle_locked_at; extends the D-24303 disjoint-writer amendment]. [2] Model = FREELY CHANGEABLE while unlocked [operator's auto-assign-but-changeable choice, D-24303]; the previous handle is RELEASED to the free pool [no redirect/reservation]. [3] `PATCH /api/me/handle` [authenticated-session; body {handle}] wraps changeHandle; api-endpoints.md new row + changeHandle Library-only row [D-11804]. [4] MyProfilePage.vue handle-edit affordance [shows current @handle + input + Change button + inline invalid/taken/reserved errors] + ownerProfileApi changeHandle wrapper. OUT: the explicit lock/claim action + rewiring claimHandle's `handle_canonical IS NULL` guard to `handle_locked_at IS NULL` [deferred — no handle is locked today, so change works for all]; per-account change rate-limit/cooldown [deferred anti-squat]. Scoped. Reserved by WP-501, DRAFTED at draft, landed at WP-501 execution; hard-deps WP-101 + WP-500/D-24303 + WP-104; 2026-08-05, claude/wp501-change-handle)
- D-24308 — friend-request-by-account-id (Active (landed 2026-08-06): locks the friend-request-by-identifier contract — POST /api/me/friends/requests accepts EITHER handle OR accountId [well-formed v4 UUID]; exactly one required. accountId resolves via findPlayerByAccountId → account_not_found [404] on miss; the new code is added to the FriendApiErrorCode union + array server-side AND the client mirror + both drift tests. FR-2 is preserved [output-redaction invariant — responses still never emit accountId; this change is input-only]. The accountId path funnels into the SAME abuse-control chain + sendFriendRequest. Clarifies FR-2 scope: accepting a self-held AccountId as request input emits no identifier and is a copy-paste capability [WP-499/D-24302 trust model]. Reserved by WP-504, DRAFTED at draft, landed at WP-504 execution; 2026-08-05, claude/wp504-friend-request-by-uuid)
- D-24310 — ewiki-canonical-source-cross-repo (Active (landed 2026-08-05): extends D-24304 with an optional `canonical-source-repo` front-matter field — a GitHub `owner/repo` slug — for MIRROR pages whose canonical doc lives in a DIFFERENT repo. When set, `canonical-source` is a path within that repo and the editing-this-page.html footer links `github.com/<owner/repo>/blob/main/<path>` [+ /edit/main]; when absent, same-repo behaviour is unchanged. check-links.mjs relaxes existence + source-membership for cross-repo [doc not checked out here], validating only slug format + non-empty path. NOT rendered in the metadata panel [six-field D-13810/EC-142 lock untouched]. Adopted by homepage-spec / homepage-appendix / homepage-review-template [legendary-arena/legendary-arena-website], which drop their interim #1250 provenance blockquote. Branch pinned to main. D-24309 intentionally skipped [reserved-in-flight for the WP-504 match-invite pair]. INFRA, no WP; 2026-08-05, infra/canonical-source-cross-repo)
- D-24311 — captured-cards-under-adversaries (RESERVED: reverses the D-12806 safe-skip / D-12805 Interpretation-B "do NOT flatten G.attachedBystanders" rule for CITY-VILLAIN captured bystanders. Adds UICityCard.attachedBystanderCount:number — a COUNT-ONLY, public board-state projection of G.attachedBystanders[villain].length — through the 5-step Board-Visible Field contract [declare on the UICityCard type, populate in buildUIState, pass through the filterUIStateForAudience city-space rebuild, audience-filter test, Play Diagnostics uiStateSnapshot]. Count-only [NOT UIDisplayEntry[]] is deliberate: captured bystanders are FACE DOWN, so the client shows "N captured" and never leaks which bystander it is [Jeff 2026-08-06]. Public audience disposition [visible to all — board state, not private]. Does NOT change the already-projected face-up UICityCard.attachedHeroes [WP-214] or mastermind.attachedBystanders [WP-154]; both are rendered client-side by WP-505 with no engine change. Reserved by WP-505, DRAFTED at draft, landed at WP-505 execution; 2026-08-06, claude/captured-cards-under-adversaries)
- D-24312 — magneto-tactic-crushing-shockwave (RESERVED: locks the resolution model for core Magneto's Crushing Shockwave tactic Fight — "Each other player reveals an [team:x-men] Hero or gains two Wounds" resolves as a NO-CHOICE deterministic per-player pass: a player holding an X-Men Hero in hand auto-reveals it at no cost; every other player gains exactly TWO Wounds from the supply [stopping early if the supply empties — logged no-op, moves never throw]. Scoped to every player EXCEPT the defeating ctx.currentPlayer ["each OTHER player"]. Chooses deterministic auto-resolve over a blocking multi-player pending-choice, per the D-24192 / D-24284 mastermind-strike precedent. Reserved by WP-506, DRAFTED at draft, landed at WP-506 execution; 2026-08-06, claude/wp506-507-magneto-tactic-fight)
- D-24313 — mastermind-tactic-effect-index-coverage (RESERVED: locks that mastermind TACTIC cards are enumerated in data/metadata/effect-implementation-index.json via a dedicated tactic feed in build-effect-implementation-index.mjs [NOT the hero/villain mechanic ledgers, which do not and will not carry tactics], keyed by the tactic ext_id ${setAbbr}-mastermind-${slug}-${tacticSlug}, with status/handler/WP derived from the tacticHandlers.ts dispatch table [executable when a resolver exists, unmarked/unsupported otherwise]. The generated artifact stays CI-gated by effect-index:check. Reserved by WP-507, DRAFTED at draft, landed at WP-507 execution; 2026-08-06, claude/wp506-507-magneto-tactic-fight)
- D-24314 — escaped-pile-bystander-carry-away (RESERVED: escaping villains carry their captured Bystanders into the Escaped Villains pile G.escapedPile, replacing resolveEscapedBystanders' return-to-supply. G.escapedPile is the canonical zone for every card carried off the city [escaped villains + their Bystanders]; the ESCAPED_VILLAINS counter still counts escaped villain cards only. Faithful to Universal Rules v23 escape handling; enables escaped-pile resource-loss counting. Reserved by WP-508, DRAFTED at draft, landed at WP-508 execution; 2026-08-07, claude/escaped-pile-resource-loss)
- D-24315 — scheme-resource-loss-condition (RESERVED: scheme resource-loss conditions are declared data-only on SchemeTwistConfig.resourceLossCondition. First kind 'escaped-pile-count' [cardType + threshold] sets SCHEME_LOSS when G.escapedPile holds >= threshold entries of cardType [classified via G.villainDeckCardTypes], checked in the escape path — evaluateEndgame stays counter-only. When a scheme declares a resourceLossCondition the twist-count doom-clock proxy [D-24178] is suppressed for that scheme. Wired for Midtown Bank Robbery [8 Bystanders]; twist-loss schemes [Portals, Cosmic Cube] declare none and keep the twist-loss. Reserved by WP-508, DRAFTED at draft, landed at WP-508 execution; 2026-08-07, claude/escaped-pile-resource-loss)
- D-24316 — negative-zone-villain-escape-loss (RESERVED: Negative Zone loses via SchemeTwistConfig.resourceLossCondition {kind:'escaped-pile-count',cardType:'villain',threshold:12} — reuse WP-508 kind, counting 'villain'-typed escapedPile entries [villains only per rulebook §Escaped Villains, henchmen excluded]; twist proxy suppressed. Reserved by WP-509, DRAFTED at draft, landed at WP-509 execution; 2026-08-07, claude/wp509-negative-zone-villain-loss)
- D-24324 — converted-card-villain-overlay (RESERVED: a card that "counts as" a villain of a named group is typed 'villain' in G.villainDeckCardTypes [native reveal/fight/escape routing; RevealedCardType closed union + drift guard NOT touched] plus a new G.convertedVillainOrigins: Record<CardExtId, ConvertedVillainOrigin> overlay ['killbot'; WP-514 adds 'skrull'] carrying the converted identity used for the distinct escaped count, dynamic attack, and display. Chosen over extending the closed RevealedCardType union to avoid routing/drift-guard/trigger ripple + stay faithful to tabletop "counts as". Reserved by WP-513, DRAFTED at draft, landed at WP-513 execution; 2026-08-08, claude/wp512-conversion-schemes-draft)
- D-24326 — skrull-conversion-overlay-and-cross-deck-shuffle (RESERVED: Secret Invasion extends the WP-513 converted-card overlay with origin 'skrull'; setup deterministically moves 12 random Heroes from the built Hero Deck reservoir into the Villain Deck's pre-shuffle array (typed 'villain' + convertedVillainOrigins 'skrull'), sequenced against the existing hero/villain shuffles so replay stays deterministic; loss via resourceLossCondition {kind:'escaped-converted-count',origin:'skrull',threshold:6} reusing WP-513's kind. Reserved by WP-514, DRAFTED at draft, landed at WP-514 execution; 2026-08-09, claude/wp514-secret-invasion)
- D-24328 — civil-war-2p-hero-deck-sizing (RESERVED: Super Hero Civil War builds a 4-hero-group Hero Deck at exactly 2 players — its printed setup "If only 2 players, use only 4 Heroes in the Hero Deck." A new resolveEffectiveHeroDeckIds (setup/schemeSetupSizing.ts) returns config.heroDeckIds.slice(0,4) for schemeId core/super-hero-civil-war when numPlayers === 2, else the requested ids unchanged; applied POST-VALIDATION at the buildHeroDeck call in buildInitialGameState (the loadout still validates its normal 5 ids; the scheme rule sizes the built deck — mirrors D-24321's wound sizing below the config floor). Makes WP-510/D-24318's hero-deck-depletion loss reachable at 2p (the 5-hero deck was too big to run out). Deterministic first-4 drop (the engine cannot ask which 4). Reserved by WP-515, DRAFTED at draft, landed at WP-515 execution; 2026-08-09, claude/wp515-civilwar-2p)
- D-24327 — skrull-combat-cost-proxy-attack-defeat-to-gain (RESERVED: a Skrull (converted Hero) resolves fight cost = (G.cardStats[id]?.cost ?? 0) + 2 via a new overlay-first resolveFightCost mode — a DOCUMENTED PROXY for the printed "Hero's VP + 2", chosen by operator 2026-08-09 because hero VP exists nowhere in the data (generated or upstream) and authoring it would fabricate values across every set; a // PROXY: swap seam becomes G.cardVictoryPoints[id] + 2 if hero VP is ever authored (cost typically exceeds VP, so the proxy skews Skrulls slightly harder than printed). When defeated a Skrull routes to the defeating player's discard ("gain that Hero") instead of the victory pile + clears its overlay, via a guarded converted-origin branch in the shared defeatCityVillainCore (non-skrull defeats unchanged; pushes the fought cardId directly to discard + logs the gain, NOT via awardAttachedHeroes which handles attached heroes); the twist moves the highest-cost HQ Hero (ties → lowest slot index) to the Sewers (pushVillainIntoCity space 0) as a Skrull + refillHqSlot. Reserved by WP-514, DRAFTED at draft, landed at WP-514 execution; 2026-08-09, claude/wp514-secret-invasion)
- D-24325 — killbots-faithful-model (RESERVED: the 18 villain-deck Bystanders convert to Killbot villains at setup; attack = a per-scheme "twists next to this Scheme" counter [G.counters, seeded 3, +1 per 'killbots' twist] via a new resolveFightCost converted-attack mode; loss via resourceLossCondition {kind:'escaped-converted-count',origin:'killbot',threshold:5} [reuses WP-508 escape-branch call site, counts by overlay origin]; twist-count proxy suppressed. Reserved by WP-513, DRAFTED at draft, landed at WP-513 execution; 2026-08-08, claude/wp512-conversion-schemes-draft)
- D-24322 — sim-pile-depletion-visibility-and-coverage-backdrop (RESERVED: the bgio-bypassing sim turn loops (simulation.runner.ts runPerTurnLoop + par.aggregator.ts) now call applyPileDepletionResourceLoss after each dispatched move, mirroring the bgio play-phase turn.onMove — so pile-depletion scheme losses (Civil War hero deck WP-510, Legacy Virus wounds WP-511) terminate in-sim instead of running to MAX_TURNS (escaped-pile losses already fired in the reveal move, so were sim-visible; pile-depletion losses fired only in turn.onMove, which the sim reimplements around). Surfaced by WP-511: the runtime-observed hollows harness + coop-winrate use Legacy Virus as their fixed backdrop, and its now-deck-dependent wound loss stopped terminating the solo sweep deterministically (10-min CI timeout). Both harness backdrops switched Legacy Virus -> Cosmic Cube (core/unleash-the-power-of-the-cosmic-cube), a true twist-loss scheme (deterministic ~twist 8, sim-visible via the twist-count path). Coverage cost: the runtime-observed artifact drops ~16->10 distinct mechanics (Cosmic Cube's wound-all twist plays fewer hero abilities); dashboard in-play coverage totalObs 178->163, percentResolved 33.1->36.2. Portals (12 mechanics) is better but loses via the unconfigured MVP fallback (fragile); a follow-up may optimize the backdrop. Reserved by WP-511, DRAFTED at draft, landed at WP-511 execution; 2026-08-08, claude/wp511-legacy-virus-wound-depletion)
- D-24320 — legacy-virus-wound-depletion-loss (RESERVED: Legacy Virus loses via resourceLossCondition {kind:'pile-depleted',pile:'wounds'} — reuse WP-510 kind, widen pile literal + remainingPileCount to 'wounds'->G.piles.wounds.length; evaluated on the existing turn.onMove chokepoint; twist proxy suppressed; no tie interaction. Reserved by WP-511, DRAFTED at draft, landed at WP-511 execution; 2026-08-07, claude/wp511-legacy-virus-wound-depletion)
- D-24321 — scheme-specific-setup-sizing (RESERVED: Legacy Virus wound stack built at 6xnumPlayers at setup [printed "6 Wounds per player"] via a pure resolveEffectiveWoundsCount helper in buildInitialGameState, applied as a POST-VALIDATION override to buildGlobalPiles. The 30 woundsCount config-floor D-24032 is NOT changed [floor governs requested config; scheme rule governs built pile, below floor by design]. First scheme-specific setup-sizing override [single explicit branch, not a framework]. Re-pins the sole Legacy Virus sentinel fixture finalStateHash [deliberate, verified]; PRE_WP080_HASH unaffected. Reserved by WP-511, DRAFTED at draft, landed at WP-511 execution; 2026-08-07, claude/wp511-legacy-virus-wound-depletion)
- D-24318 — pile-depleted-resource-loss-kind (RESERVED: widen SchemeResourceLossCondition to a discriminated union on kind, adding {kind:'pile-depleted',pile:'heroDeck'} [WP-511 extends pile to 'wounds']; new applyPileDepletionResourceLoss sets SCHEME_LOSS when the named pile is empty, wired to play-phase turn.onMove beside latchFinalTurnIfDeckExhausted — central per-move chokepoint distinct from WP-508's escape branch, catches any depletion path. Twist-proxy suppression inherited [kind-agnostic gate]. Reserved by WP-510, DRAFTED at draft, landed at WP-510 execution; 2026-08-07, claude/wp510-civil-war-hero-deck-loss)
- D-24319 — civil-war-hero-deck-loss-preempts-tie (RESERVED: Super Hero Civil War loses via pile-depleted/heroDeck; because evaluateEndgame checks SCHEME_LOSS before FINAL_TURN_TIE and resolveFinalTurnTieIfUnresolved sets the tie only when evaluateEndgame===null, a hero-deck-empty SCHEME_LOSS pre-empts the deck-exhaustion tie [D-24159] with NO change to finalTurn.logic.ts or evaluateEndgame — a consequence of counter precedence, not new tie logic. Reserved by WP-510, DRAFTED at draft, landed at WP-510 execution; 2026-08-07, claude/wp510-civil-war-hero-deck-loss)
- D-24317 — retire-generic-escape-limit-loss (RESERVED: remove the generic escapedVillains>=ESCAPE_LIMIT loss from evaluateEndgame; villain-escape losses per-scheme via 'escaped-pile-count'. ESCAPED_VILLAINS counter + ESCAPE_LIMIT constant retained [counter still increments; ESCAPE_LIMIT stays coopOutcome loss-cause heuristic + sweep.analyze diagnostic]. coopOutcome keeps 'loss-villains-escaped' + its escapedVillains>=ESCAPE_LIMIT branch as a documented heuristic; evaluateEndgame stays counter-only. Reserved by WP-509, DRAFTED at draft, landed at WP-509 execution; 2026-08-07, claude/wp509-negative-zone-villain-loss)
- D-24323 — coverage-backdrop-portals (RESERVED: the runtime-observed coverage sweep + co-op yardstick backdrop is switched Cosmic Cube -> Portals to the Dark Dimension [core/portals-to-the-dark-dimension], recovering the runtime-observed signal to 12 distinct mechanics [dashboard totalObs 163->188, resolvedObs 59, percentResolved 36.2->31.4]. Portals is a TRUE twist-loss scheme [printed "Twist 7: Evil Wins!"] that loses at twist 7 via the MVP_SCHEME_TWIST_THRESHOLD fallback — NO engine change — and its Dark-Portal twists buff the board rather than polluting hero decks [unlike Cosmic Cube's wound-all], so more hero abilities fire. The explicit-lossThreshold engine option [D-24322's follow-up suggestion] is REJECTED: a SCHEME_TWIST_CONFIGS entry needs a required resolverId [contract-touching] for zero behavioral gain. The old ~16-mechanic plateau is NOT recoverable [Legacy-Virus-specific, now deck-dependent; Portals plateaus at 12] so SEEDS_PER_BOARD stays 8. The coop yardstick is switched in lockstep [cosmetic — the 0% bot win-rate is backdrop-independent, a Bot-Ally-epic concern]. The coverage harness gains a deterministic-termination guard [throw ProbeFailure if any swept game did not reach endgame] so a future non-terminating backdrop fails loudly instead of timing out the sim:runtime-observed:check gate — the WP-511 lesson. No finalStateHash/PRE_WP080 interaction. Reserved by WP-512, DRAFTED at draft, landed at WP-512 execution; 2026-08-08, claude/priceless-morse-ed9637)
- D-24329 — ymir-fight-ko-wounds-current-hand-and-discard (RESERVED: the fifteenth VillainEffectPrimitive 'ko-wounds-current-hand-and-discard' [append-only D-24034, 14->15] — keyword-less no-param auto-resolve. Handler villainEffectKoWoundsCurrentHandAndDiscard KOs every Wound [WOUND_EXT_ID 'pile-wound'] from the current player's hand + discard to G.ko [koCard], matching by ext_id, self-narrating via pushLog. Two locked narrowings: "Choose a player" -> current player + "any number" -> KO-all in solo/co-op [rational chooser, pure upside, no selection UI/pending choice]; hand+discard ONLY [not in-play — Wounds are never played there, unlike ko-heroes-current-by-trait]. No new parser arm [terminal parts.length===1 branch]. Marker on Ymir Fight only. No ctx.random; no committed fixture references enemies-of-asgard/ymir so finalStateHash/PRE_WP080 byte-identical [verified]. Reserved by WP-516, DRAFTED at draft, landed at WP-516 execution; 2026-08-09, claude/clever-blackburn-5a6ccf)
- D-24330 — dr-readiness-feed-from-drill-issues (RESERVED: the dashboard DR-readiness signal derives from the "DR drill due" GitHub issues managed by the dr-drill-reminder workflow — the issue lifecycle [open past-its-month = overdue; newest closed = last drill; dated title] is already the operator's system of record for drills, plus a "- [ ] Drill passed" checkbox added to the issue body as the PASS/FAIL source, so a server /api/dash/dr-readiness feed reads it via the GitHub API rather than introducing a parallel committed status ledger or parsing DISASTER_RECOVERY.md §7. Reserved by WP-517, DRAFTED at draft, landed at WP-517 execution; renumbered from D-24329 after Ymir shipped under 516/551/329; 2026-08-09, claude/spec-dr-readiness-dashboard)
- D-24331 — skrull-gain-fight-overlay-narration (RESERVED: locks that Secret Invasion's "If you defeat that Hero, you gain it" gain MUST be announced in the fight overlay narrative, not only in the durable log. composeFightNarrative gains a fourth `skrullGained` parameter; when true it appends ` and gained the Hero into the active player's discard pile` after the bystander clause and before the `; Fight effect:` clause. The flag is captured in defeatCityVillainCore from `G.convertedVillainOrigins[cardId] === 'skrull'` BEFORE the origin-overlay delete. Rationale: the notableEvents fightResolved narrative is the only cross-audience surface that announces mid-fight outcomes; without the clause a Skrull defeat is indistinguishable on-screen from an ordinary villain kill and the gained Hero drops silently into the discard. Additive/hash-safe: the clause is empty for every non-Skrull defeat so non-Skrull narratives are byte-identical, and no committed fixture defeats a Skrull so the sentinel/PRE_WP080 hashes are unchanged. Reserved by WP-518, DRAFTED at draft, landed at WP-518 execution; hard-dep WP-514/D-24327 + WP-319/D-24105; 2026-08-10, claude/wp518-skrull-gain-narrative)
- D-24332 — melter-fight-ko-cullable-each-deck-top (RESERVED: the sixteenth VillainEffectPrimitive 'ko-cullable-each-deck-top' [append-only D-24034, 15->16] — keyword-less no-param auto-resolve. Handler villainEffectKoCullableEachDeckTop iterates every player [Object.keys(G.playerZones).sort()], reveals their deck top [reshuffleDiscardIntoDeck via shuffleContext when the deck is empty — the Legendary reveal-reshuffle rule, scry-ko precedent D-24285], and KOs it to G.ko ONLY when it is "cullable" — a Wound [WOUND_EXT_ID] or a basic starting S.H.I.E.L.D. card [SHIELD_AGENT_EXT_ID|SHIELD_TROOPER_EXT_ID, the selectScryKoTarget tiers-1-2 worst-worthy set from D-24267]; every real Hero and the S.H.I.E.L.D. Officer is KEPT [left on top]. Locked collapse of the printed "you choose to KO it or put it back": a rational cooperative fighting player thins weak starters/Wounds from any deck top [pure upside] and never KOs a real Hero — so the choice auto-resolves deterministically with NO pending choice, NO UIState field, NO client change [unlike WP-470's scry-ko, whose auto-resolve was upgraded to a pending choice ONLY because it force-KO'd one of two heroes; Melter's keep-option removes that failure mode]. Self-narrates via pushLog [keyword-less]. Marker on Melter Fight only. Uses ctx.random via the reshuffle + adds a hashed Fight hook to the masters-of-evil group; masters-of-evil is referenced by scoring fixtures, so finalStateHash/PRE_WP080/sentinel impact is verified at execution and any shifted hash re-recorded via record-game-fixture.mjs, never hand-edited. Reserved by WP-519, DRAFTED at draft, landed at WP-519 execution; 2026-08-10, claude/amazing-lewin-ce0830)
- D-24333 — co2e-masters-of-evil-curation-subset-and-deferred-epic (RESERVED: locks the co2e Masters-of-Evil faithfulness curation. THREE timing lines reduce to EXISTING villain primitives and are marked by WP-520 [card-data only, WP-185 conservatism-over-coverage]: Melter Fight -> ko-cullable-each-deck-top [WP-519/D-24332]; Ultron Escape -> reveal-or-wound:hc:tech [WP-469/D-24281]; Whirlwind Fight -> ko-hero:current@rooftops+bridge [magnitude-1 implicit + universal @location gate D-24295; ko-hero:current:1 is REJECTED by the parser per D-24298, so the bare current form carries the implicit magnitude-1]. THREE timing lines are DEFERRED because each needs a NEW VillainEffectPrimitive [future epic, one WP each]: Baron Zemo Ambush [base capture-bystander + one more per [team:avengers] Hero in the HQ — a dynamic HQ-team-count capture; no existing primitive counts HQ heroes by team]; Ultron Fight [choose a [hc:tech] Hero from the HQ, either KO it or choose a player to gain it — an interactive HQ-hero KO-or-gift; no existing primitive]; Whirlwind Ambush [two Villains in the city swap spaces — a city board-position manipulation; no existing primitive]. TWO passives are a separate non-hollow mechanic class [variable villain attack via resolveFightCost, NOT Fight/Ambush/Escape timing lines so they do NOT trip the D-24266 unmarked-ability detector]: Baron Zemo "+1[icon:attack] for each Bystander he has", Ultron "+1[icon:attack] for each [hc:tech] Hero in the HQ". WP-520 closes the covered subset only; the deferred set stays hollow until the epic lands. Reserved by WP-520, DRAFTED at draft, landed at WP-520 execution; 2026-08-10, claude/co2e-moe-marking-draft)
- D-24334 — co2e-baron-zemo-ambush-capture-bystanders-per-hq-avengers (RESERVED: the co2e Baron Zemo Ambush primitive [D-24333 epic 1/3] — attach ONE Bystander to Baron Zemo then ONE more per [team:avengers] Hero in G.hq. New auto-resolve VillainEffectPrimitive [append-only D-24034]; introduces the first HQ-by-trait COUNT scan [count G.hq slots whose G.cardTraits[cardId].team === 'avengers'], distinct from the existing player-zone trait scans. Bystanders attach via attachBystanderToVillain at the Ambush fire site; the award is deferred to defeat [the existing capture-bystander Ambush semantics], supply-bounded on G.piles.bystanders. Deterministic, no ctx.random, self-narrates [keyword-less]. Reserved by WP-521, DRAFTED at draft, landed at WP-521 execution; 2026-08-10, claude/co2e-moe-new-primitives-draft)
- D-24335 — co2e-ultron-fight-hq-tech-hero-ko-or-gift (RESERVED: the co2e Ultron Fight primitive [D-24333 epic 2/3] — "Choose a [hc:tech] Hero from the HQ. Either KO that Hero or choose a player to gain it." New VillainEffectPrimitive [append-only D-24034]. FIDELITY DECISION [locked at WP draft]: the printed double-choice collapses because in the shipped co-op modes gifting an HQ hero into a player's DISCARD strictly dominates KO'ing it [the player gains the card AND the HQ refills], so a rational cooperative chooser never KOs; the RECOMMENDED auto-resolve gifts the highest-cost [hc:tech] HQ hero to the CURRENT player's discard [the WP-514/D-24327 defeat-to-gain routing], deterministic, no pending choice, no client change; the alternative is a full interactive pending choice [scry-ko WP-470 shape] for real agency over which hero / KO-vs-gift / which player. Selection reuses captureHeroFromHq's HQ selection [highest-cost, hc:tech-filtered] + refillHqSlot. Reserved by WP-522, DRAFTED at draft, landed at WP-522 execution; 2026-08-10, claude/co2e-moe-new-primitives-draft)
- D-24336 — co2e-whirlwind-ambush-swap-two-city-villains (RESERVED: the co2e Whirlwind Ambush primitive [D-24333 epic 3/3] — "Two Villains in the city swap spaces." New VillainEffectPrimitive [append-only D-24034] and the FIRST City board-position manipulation. The card specifies neither a chooser nor which two villains, so the WP LOCKS a deterministic swap rule [decided at draft from the City structure — the most faithful "disrupt the board" reading]; swaps two VILLAIN-typed occupants of G.city [henchmen excluded via G.villainDeckCardTypes], no-op when fewer than two city villains are present. Fires at the Ambush site [cityIndex is undefined for Ambush; the swap is space-relative, not fought-space-relative]. Determinism: the positional rule uses no ctx.random; a tie among equal candidates resolves by a deterministic order. Reserved by WP-523, DRAFTED at draft, landed at WP-523 execution; 2026-08-10, claude/co2e-moe-new-primitives-draft)
- D-24338 — secret-invasion-six-hero-play-lobby-scheme-aware-projection (RESERVED: locks that the play-side requirement projection consumed by the arena-client lobby is SCHEME-AWARE, so the play lobby agrees with the WP-524/D-24337 engine + registry-viewer requirement of 6 hero groups for Secret Invasion. The server GET /api/match/setup-requirements [today PLAYER_COUNT_SETUP verbatim, no scheme input] gains an optional schemeId that projects resolveEffectiveHeroCount per player count; the arena-client lobby [layer-barred from importing registry] threads the selected schemeId into fetchSetupRequirements + computePlayerCountMismatches so a 6-hero Secret Invasion loadout passes the play-lobby Create gate and a 5-hero one is flagged BEFORE submit [not only rejected by the engine], and the server autoplay default-loadout sizing becomes scheme-aware [heroDeckIds.slice(0, effectiveHeroCount)] so a Secret Invasion autoplay/bot-ally build sizes to 6. WP-525 SHIPS AS A SET with WP-524 [D-24337]: WP-524 alone would make the play lobby DISABLE Create on a correct 6-hero Secret Invasion loadout [scheme-blind mismatch] and the engine reject the 5-hero fallback, so the scheme would be un-creatable via play.legendary-arena.com until this lands. api-endpoints.md replace-whole-row IFF the endpoint request/response shape changes. Reserved by WP-525, DRAFTED at draft, landed at WP-525 execution; hard-dep WP-524/D-24337; 2026-08-11, claude/wp524-525-secret-invasion-heroes)
- D-24337 — secret-invasion-six-hero-setup-requirement (RESERVED: locks that Secret Invasion of the Skrull Shapeshifters requires a SCHEME-AWARE effective hero-group count, not the standard PLAYER_COUNT_SETUP.heroCount. The printed "6 Heroes" setup clause [dropped by WP-514/D-24326, which scoped only the conversion + combat] is a requirement INCREASE — distinct from the two shipped schemeSetupSizing DOWNSIZE overrides [Legacy Virus wounds D-24321, Civil War 4-hero at 2p D-24328], which post-validation size a BUILT pile below the validated config. Because the operator must actually SUPPLY 6 hero-group ids, the override lives on the REQUIREMENT side: a new resolveEffectiveHeroCount[schemeId, numPlayers, baseHeroCount] applies FLAT 6 for this scheme wherever the base is below 6 [2/3/4p 5->6; 5p already 6; solo-1p 3->6 — the flat reading of the printed "6 Heroes", flagged for operator review], and is threaded through EVERY heroCount requirement site so lobby/gate/engine agree: checkPlayerCountComposition [registry] + validatePlayerCountComposition [game-engine] + the server create-gate + the registry-viewer/arena-client lobby requirement display. Scope is the hero-count fix ONLY; the co-located "Skrull Villain Group required" constraint [already satisfied in the reported match, so not the active defect] is Out-of-scope for a follow-up WP. Determinism: the effective count changes the hero-deck build for THIS scheme only; every other scheme/count is byte-identical, and no committed fixture materializes Secret Invasion [D-24327 grep], so finalStateHash/PRE_WP080/sentinel are unchanged — verified suite-green at execution. Reserved by WP-524, DRAFTED at draft, landed at WP-524 execution; hard-dep WP-514/D-24326 + WP-370/D-24165; 2026-08-10, claude/wp524-secret-invasion-six-heroes)
- D-24339 — bystander-lost-derived-from-escaped-pile (RESERVED: locks that the bystanderLost PenaltyEventType is derived at end-of-match from G.escapedPile [count entries whose villainDeckCardTypes is 'bystander'], NOT from a new G counter or event log. Chosen over the ENDGAME_CONDITIONS.BYSTANDERS_LOST counter the stale source comment suggested because G.escapedPile ALREADY durably records carried-away Bystanders [D-24314 carryEscapedBystandersToPile, fed from both the reveal and scheme-twist escape paths] so a pure derivation reads existing state and adds no G field -> finalStateHash / PRE_WP080 byte-identical [no dual re-pin]. Applied symmetrically in both deriveScoringInputs and the par.aggregator PAR-calibration mirror so a calibrated PAR is not systematically easier than the live score. Also records the scoring-version posture: wiring a penalty producer shifts the score distribution for a given config, but because this lands BEFORE any production ScenarioScoringConfig or legendary.competitive_scores row depends on bystanderLost, no retroactive scoringConfigVersion bump / re-score is owed; any producer wired AFTER production configs exist MUST bump scoringConfigVersion. Villains in escapedPile stay the villainEscaped/ESCAPED_VILLAINS count [no double-count]. Reserved by WP-528, DRAFTED at draft, landed at WP-528 execution; hard-dep WP-048 + D-24314; 2026-08-11, claude/wp-penalty-producers)
- D-24340 — scheme-twist-negative-counts-all-twists-from-schemeTwistCount (RESERVED: locks that the schemeTwistNegative PenaltyEventType is the count of ALL scheme twists flipped during the match [G.counters.schemeTwistCount], NOT a polarity-classified subset — every Legendary scheme twist advances the villain's scheme and is player-negative by definition [the community/rulebook -3 x twists formula counts every twist], so no per-twist outcome classification is needed or faithful. Derived at end-of-match from the existing durable counter [incremented by schemeHandlers.ts buildGenericTwistEffects for every revealed scheme twist, incl. the Mystique escape->twist and suppressed-doom-clock schemes], adding no new G field -> finalStateHash / PRE_WP080 byte-identical [no re-pin]. Applied symmetrically in both deriveScoringInputs and the par.aggregator PAR-calibration mirror. No scoringConfigVersion bump owed [lands before any production ScenarioScoringConfig / competitive_scores row depends on schemeTwistNegative]. Reserved by WP-529, DRAFTED at draft, landed at WP-529 execution; hard-dep WP-048 + D-24178; 2026-08-11, claude/wp-scheme-twist-negative)
- D-24341 — ai-second-brain-knowledge-platform-architecture (reserved 2026-08-11, spec/ai-second-brain-decision)
- D-24342 — penalty-weights-anchored-to-4-3-1-rulebook-ratio (RESERVED: locks the canonical penalty-weight relationship bystanderLost : schemeTwistNegative : villainEscaped = 4 : 3 : 1, the community/rulebook Total Score ratio [VP - 4*bystanders - 3*twists - 1*escapes] validated by the Legendary Leagues review, expressed centesimally as 400 : 300 : 100 [villainEscaped = the 1.00 unit; alternative scale 800/600/200 rejected as non-minimal]. Applies to the docs/12-SCORING-REFERENCE.md default-weights table [the frozen scoring surface, previously 500/400/200] and the test ScenarioScoringConfig [previously 300/50/100], both with a scoringConfigVersion bump. The two Legendary-Arena-specific penalties mastermindTacticUntaken + scenarioSpecificPenalty are NOT part of the rulebook triple and keep their values. Structural invariants [validateScoringConfig: bystanderLost > villainEscaped AND > bystanderReward] hold [400 > 100, 400 > 200 test / 400 > 300 reference]. No production ScenarioScoringConfig / competitive_scores row exists yet so no historical re-score is owed; the version bump is the forward comparability boundary [VISION section 22]. Reserved by WP-531, DRAFTED at draft, landed at WP-531 execution; hard-dep WP-048 + WP-528 + WP-529; 2026-08-12, claude/wp-penalty-weight-anchor)
- D-24343 — paibok-fight-give-hq-hero-each-player (RESERVED: [renumbered from D-24342 per D-24245 first-to-main; penalty-weight #1352 landed 24342 first] the twentieth VillainEffectPrimitive give-hq-hero-each-player [append-only D-24034, 19->20] and the second INTERACTIVE villain effect [after WP-492 Whirlwind magnitude-N KO]. For EACH player [Object.keys(G.playerZones).sort()], remove one HQ Hero, route it to that player's discard [WP-514/D-24327 defeat-to-gain path], and refill the vacated HQ slot from G.heroDeck [captureHeroFromHq/refillHqSlot, the WP-522/D-24335 give-hq-hero-by-trait-to-current mechanics minus the trait filter]. FIDELITY [locked at WP draft, Jeff 2026-08-11]: "Choose a Hero in the HQ" is a genuine per-player choice, so the HUMAN current player picks interactively [a NEW pending-choice type on the WP-476 PendingDiscardChoice shape: pending FIFO queue in G + resolve move + block-all cascade entry + ai.legalMoves short-circuit + PENDING_CHOICE_MOVE_NAMES/PENDING_CHOICE_FLAGS bot wiring + UIState projection sourced from the PUBLIC G.hq (so no D-24011 hand-leak redaction, but still per-chooser) + a new arena-client prompt]; every NON-current player and any BOT/auto-driven player auto-picks the HIGHEST-COST HQ Hero synchronously [reusing the selectHqHeroIndexByTraitHighestCost highest-cost logic without the trait filter], mirroring D-24284's current-parks/others-auto split. Ships the engine block + UIState projection + client prompt together [project_pending_choice_no_ux_freeze invariant — an engine block without a prompt is a hard-freeze]. Marks the CORE Skrulls Paibok Fight only; the co2e Paibok twin [also (unmarked)] is a fast follow-up. Reserved by WP-532, DRAFTED at draft, landed at WP-532 execution; hard-dep WP-242/D-24007 + WP-243/D-24010 + WP-476/D-24284 + WP-522/D-24335; 2026-08-11, claude/wp-paibok-give-hq-hero)
- D-24344 — preferences-foundation-option-a-path (RESERVED: re-locks the WP-068 "Option A" delivery path for the registry-viewer preferences foundation — everything under apps/registry-viewer/src/prefs/, NO new workspace package [packages/ui-preferences NOT created], registry-viewer stays an independent Vue 3 SPA. Supersedes the never-merged D-1414 [Option A, present only on retired commit 8ec6ceda, absent from main]. Reserved by WP-534, DRAFTED at draft, landed at WP-534 execution; hard-dep none; 2026-08-12, claude/wp534-preferences-foundation)
- D-24345 — rogue-copy-powers-semantics (RESERVED: locks the Rogue "Copy Powers" mechanic — "Play this card as a copy of another Hero you played this turn. This card is both [hc:covert] and the color you copy." Locks: (1) INTERACTIVE pending-choice [human picks one Hero played this turn — `playerZones.inPlay` minus self, real heroes only; bot auto-defaults], NOT an auto-collapse [unlike co2e Ultron/Melter there is no dominant "which hero to copy" choice]; (2) "copy" = RE-FIRE the chosen hero's on-play ability via the reentrant `executeHeroEffects(G, ctx, playerID, chosenHeroId)` [not its printed stats — Copy Powers has no printed attack]; the resolve move threads the FULL MoveContext incl. `ctx.random` [a first for a resolve move — the copied ability may draw/reshuffle]; (3) DUAL-CLASS: Copy Powers counts as `[hc:covert]` AND the copied hero's class for other cards' `[hc:X]` synergy scans — runtime-chosen, so a NEW mutable per-match `G` map [parallel to setup-static `cardSizeChangingClasses`/`getGrantedClasses`], lazy-materialized so non-Copy-Powers games keep byte-identical finalStateHash+PRE_WP080; (4) edge: 0 eligible heroes → clean no-op logged as such [not the generic "blocked-condition"], 1 eligible → [fork: auto vs still-park]. Chained pending choices supported [copying a hero whose own ability parks a choice — FIFO queue]. New keyword `copy-powers` [append-only to HeroKeyword union+array]. FORK 3 [full dual-class vs covert-only-simplified] + the 0/1-eligible edge are flagged operator-review. Reserved by WP-535, DRAFTED at draft, landed at WP-535 execution; hard-dep WP-532/D-24343 + WP-251/D-24022 + WP-368/D-24074 [size-changing dual-class precedent]; 2026-08-12, claude/wp535-copy-powers-draft)
- D-24346 — core-loki-master-strike-auto-reveal-or-wound (RESERVED: locks core Loki Master Strike as an AUTO reveal-[hc:strength]-Hero-or-gainWound over all players [no interactive choice — reveal is not a meaningful decision], keyed by mastermind selection core/loki [not a card marker], mirroring the resolveRedSkullStrike auto precedent; landed at WP-537 execution; 2026-08-13, WP-537)
- D-24347 — core-dr-doom-master-strike-interactive-put-2-on-top (RESERVED: locks core Dr. Doom Master Strike semantics — per-player exactly-6-card gate, reveal-[hc:tech]-Hero escape, and the put-2-cards-on-top penalty resolved INTERACTIVELY for the current player [new PendingPutCardsOnDeckChoice, selection order = top order] and auto cheapest-2 for non-current/bot players [WP-476 Magneto-strike + WP-532 Paibok precedent]; landed at WP-538 execution; 2026-08-13, WP-538)
- D-24348 — portals-scheme-dark-portal-attack-buffs (RESERVED: locks Core Portals as a TRUE twist-loss [lossThreshold 7, no resourceLossCondition] AND its Dark-Portal attack model — a new DARK_PORTAL_COUNT counter drives BOTH a positional villain buff [+1 to a villain whose city-space index has been portal’d, filled leftmost-first per the board-layout binding, derived from the counter in resolveFightCost] and a static Mastermind buff [+1 once >=1 portal, via a new centralized resolveMastermindFightCost]; counter-only, deterministic, no new persistent shape; landed at WP-539 execution; 2026-08-13, WP-539)
- D-24349 — partial-core-scheme-twist-fidelity-civilwar-koall-cosmiccube-escalation (RESERVED: locks the two partial Core scheme-twist fidelity fixes — (A) Super Hero Civil War twist KOs ALL HQ Heroes [koFromHq koAll param, config {koAll:true}], not two; (B) Unleash the Cosmic Cube twist applies the printed ESCALATION [0 wounds twists 1-4, 1 on 5-6, 3 on 7] via a data-driven woundAll escalation param keyed on the current twist number [schemeTwistCount+1, read at resolver time BEFORE the increment effect applies]. Both param-driven on the existing resolvers; no new resolverId/counter/persistent-shape; loss conditions unchanged. Landed at WP-540 execution; 2026-08-13, WP-540)
- D-24350 — core-villain-fight-reward-primitives-and-may-auto-take (RESERVED: adds 2 append-only auto-resolve VILLAIN_EFFECT_PRIMITIVES that REWARD the defeating player — gain-recruit-current [current player +N recruit] and gain-officer-current [current player gains a S.H.I.E.L.D. Officer from G.piles.officers; empty = no-op] — and RULES that HYDRA Kidnappers’ beneficial “you MAY gain a S.H.I.E.L.D. Officer” auto-takes [a pure benefit with no downside; no interactive choice], consistent with the auto-resolve keyword-less discipline. Savage Land Mutates REUSES override-next-hand-size:7 [no new primitive]. Landed at WP-541 execution; 2026-08-13, WP-541)
