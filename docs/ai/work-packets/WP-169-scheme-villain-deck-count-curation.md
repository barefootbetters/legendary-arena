# WP-169 — Scheme Villain-Deck Twist & Bystander Count Curation (Pipeline Data)

**Status:** Done — executed (see `WORK_INDEX.md` for the execution record; header flipped 2026-07-04 to match the status authority).
**Primary Layer:** Registry / Card Data Pipeline (`scripts/convert-cards/`)
**Dependencies:** WP-167 (complete) — provides the optional `SchemeSchema.villainDeckTwistCount` / `villainDeckBystanderCount` fields (D-16702), the `inputs/scheme-deck-counts.json` input file, and the `applySchemeDeckCounts` converter wiring + loud-fail. Does **not** depend on WP-168.

---

## Session Context

WP-167 added optional `villainDeckTwistCount` / `villainDeckBystanderCount` to
`SchemeSchema` and wired `convert-cards-v15.mjs` to apply them from
`inputs/scheme-deck-counts.json` (D-16702/D-16703). It populated exactly **one**
entry — `core.midtown-bank-robbery → { 8, 12 }` — as a proof of pipeline. Every
other scheme emits neither field, so WP-168's engine resolution falls back to
the locked defaults: **8** twists (D-1411) and **`numPlayers`** bystanders
(D-1412).

A data-curation investigation against the printed Setup text in all 40
`data/cards/*.json` found that the 8-twist default is correct for only about a
third of schemes; roughly two-thirds print a different fixed twist count
(7, 9, 10, 11, 6, 5, 4, …). The `numPlayers` bystander default is correct for
almost every scheme — only a small tail prints an explicit villain-deck
bystander count, including one scheme that prints **zero** (where the default
would wrongly add `numPlayers`). This packet curates the input file so the
registry data matches the printed setup, and fixes two latent gaps in the
WP-167 pipeline that block that curation.

---

## Goal

After this session, `inputs/scheme-deck-counts.json` carries a curated
villain-deck twist count for every scheme whose printed Setup specifies a single
fixed twist count other than 8, and a curated bystander count for every scheme
whose printed Setup specifies an explicit villain-deck bystander count
(including the explicit-zero case). All 40 `data/cards/*.json` are regenerated so
those schemes carry the printed counts; schemes that match the defaults, or
whose twist count is player-count-dependent, emit neither field and keep the
engine fallback. Two pipeline fixes make this possible and correct: the
`applySchemeDeckCounts` overlay assigns each count **independently** (an omitted
count keeps its engine default rather than being forced), and `apply-card-counts.mjs`
gains the same scheme-deck-count overlay so the 4 outlier sets
(`2099`, `amwp`, `wpnx`, `wtif`) can be curated at all. New fields remain
additive and optional, so every set file still validates.

---

## Assumes

- WP-167 complete and on `main`. Specifically:
  - `SchemeSchema` exposes optional `villainDeckTwistCount` and
    `villainDeckBystanderCount` (`z.number().int().min(0).optional()`).
  - `scripts/convert-cards/convert-cards-v15.mjs` defines `applySchemeDeckCounts(result, setAbbr)`
    (currently ~line 342) and calls it inside the per-set loop (~line 1558).
  - `scripts/convert-cards/inputs/scheme-deck-counts.json` exists with the single
    `core.midtown-bank-robbery → { villainDeckTwistCount: 8, villainDeckBystanderCount: 12 }`
    entry and a top-level `_note`.
  - `scripts/convert-cards/apply-card-counts.mjs` is the **only** producer for the
    4 outlier sets (`2099`, `amwp`, `wpnx`, `wtif`) and currently contains **no**
    scheme-deck-count logic.
- All 40 `data/cards/*.json` validate today against `SetDataSchema` and were last
  regenerated by WP-167 (idempotent).
- The locked engine fallbacks are **8** twists (D-1411) and `numPlayers`
  bystanders (D-1412); WP-168 (drafted) consumes each count as a single integer
  with those fallbacks.
- `pnpm --filter @legendary-arena/registry build` and `test` exit 0.
- `docs/ai/DECISIONS.md` exists; highest landed decision id is D-16802; this
  packet reserves D-16803 and D-16804.

If any of the above is false, this packet is **BLOCKED** and must not proceed.

---

## Context (Read First)

Before writing a single line:

- `docs/ai/ARCHITECTURE.md §Layer Boundary (Authoritative)` — Registry layer +
  the converter as build tooling that produces the data Registry validates.
  Neither contains gameplay logic.
- `scripts/convert-cards/convert-cards-v15.mjs` — read `applySchemeDeckCounts`
  (~line 342) and its call site (~line 1558). Note the unconditional
  `scheme.villainDeckTwistCount = counts.villainDeckTwistCount` /
  `scheme.villainDeckBystanderCount = counts.villainDeckBystanderCount`
  assignment and the existing loud-fail-on-unmatched-slug throw.
- `scripts/convert-cards/apply-card-counts.mjs` — the loud-fail counts-overlay
  pattern (villain copies, leads) added by WP-167; mirror it for scheme counts.
  Confirm it currently has no `SCHEME_DECK_COUNTS` load and no scheme overlay.
- `scripts/convert-cards/inputs/scheme-deck-counts.json` — current shape and
  `_note` (which currently over-claims that `apply-card-counts.mjs` applies these).
- `data/cards/*.json` — the **committed source of the printed counts**: each
  scheme's `cards[].abilities[]` carries a `"Setup: …"` line with the printed
  twist / bystander language. This is the extraction source for curation.
- `docs/ai/DECISIONS.md` — D-1411, D-1412 (the fallbacks), D-16702 (the field
  source), D-16703 (converter populates the values).
- `docs/ai/REFERENCE/00.2-data-requirements.md §1.5` — the scheme field table
  (the two optional fields, documented by WP-167; unaffected by this packet).
- `docs/ai/REFERENCE/00.6-code-style.md` — Rule 4 (no abbreviations), Rule 6
  (`// why:`), Rule 7 (no `.reduce()` with branching), Rule 11 (full-sentence
  errors), Rule 14 (field names match contract).

**Provenance decision (deliberate, recorded here):** a separate per-entry
provenance file (mapping each curated count back to its Setup line) was
considered and **rejected**. The printed Setup text is already committed in
`data/cards/*.json` `abilities[]`, so any curated count is verifiable by reading
the same scheme's Setup line; a manifest would duplicate committed data and
diverge from the existing convention (`hero-card-counts.json`,
`villain-card-counts.json` carry provenance only in their inline `_note`, not a
companion file). Curation traceability therefore lives in the committed Setup
text plus the `_note` methodology block (Scope C) plus D-16804.

---

## Vision Alignment

Per `docs/ai/REFERENCE/00.3-prompt-lint-checklist.md §17`. Clause numbers
reference `docs/01-VISION.md`. Triggered by §17.1 "Card data, card images, or
content semantics."

**Vision clauses touched:** §1 (Rules Authenticity — the villain deck must match
the exact printed Legendary composition; the flat-8 default mis-sizes the deck
for ~two-thirds of schemes), §2 (Content Authenticity — printed scheme setup
counts are authentic card content, not digital reinterpretation), §10 (Content
as Data — counts flow through the data pipeline from the printed Setup text, not
engine constants or hand-edited output), §14 (Explicit Decisions, No Silent
Drift — D-16803/D-16804 record the contract relaxation and the explicit
carve-out of player-count-dependent twist counts rather than silently
approximating them).

**Conflict assertion:** No conflict: this WP preserves all touched clauses. It
advances §1/§2 authenticity by sourcing real counts from the printed text and
§10 by keeping counts data-driven. It does not weaken §3/§8 determinism — it
adds no engine behavior or randomness.

**Non-Goal proximity (NG-1..7):** This packet curates optional, additive
registry data and regenerates card data through the converter. It introduces no
user-facing, paid, persuasive, or competitive surface; no card is gated, sold,
or varied by ownership or payer status. None of NG-1 through NG-7 are crossed.

**Determinism preservation (§3, §8):** Data-pipeline only; adds no engine
behavior or randomness. New fields are optional and additive, so every
regenerated `data/cards/*.json` validates and resolves unchanged except for the
curated twist/bystander counts. WP-168's deterministic resolution consumes the
data at setup time exactly as designed; identical setup config plus identical
seed still produce identical decks.

---

## Non-Negotiable Constraints

**Engine-wide (always apply — do not remove):**
- ESM only, Node v22+ — `import`/`export`, never `require()`
- `node:` prefix on all Node.js built-in imports
- Test files use `.test.ts` extension — never `.test.mjs`
- No database or network access in the registry package or the converter
- Full file contents for every new or modified file — no diffs, no snippets
- Human-style code per `docs/ai/REFERENCE/00.6-code-style.md`

**Packet-specific:**
- **No schema change.** `villainDeckTwistCount` / `villainDeckBystanderCount`
  already exist (WP-167 / D-16702). Do not touch `packages/registry/src/schema.ts`.
- **No engine change.** WP-168 consumes these counts; this packet only supplies
  data and pipeline plumbing.
- `data/cards/*.json` are **generated** — never hand-edit them. Change the input
  file and/or converter, then regenerate.
- `villainDeckTwistCount` / `villainDeckBystanderCount` are the locked field
  names; never abbreviate or rename (Rule 14).
- **Curation rule (twists):** encode `villainDeckTwistCount` only for a scheme
  whose printed Setup specifies a single **fixed** twist count **other than 8**.
  A scheme printing exactly 8 stays unencoded (default already correct). Never
  invent a count — extract it from the committed Setup line.
- **Carve-out rule (twists):** a scheme whose printed twist count is
  **player-count-dependent**, **additive-per-player**, or otherwise **not a
  single constant** is **NOT encoded** (see Out of Scope). A single integer field
  cannot represent it, and WP-168 reads a single integer.
- **Disambiguation (what counts as a single fixed villain-deck count):** encode
  only when the Setup names a number of twists **shuffled into the villain deck**
  that is constant regardless of player count and conditions. Twists placed
  **outside** the villain deck (e.g. "N additional Twists next to this Scheme")
  are **excluded from the count, not a carve-out trigger** — encode only the
  in-deck constant (e.g. Killbots prints "5 Twists … 3 additional Twists next to
  this Scheme" ⇒ encode `5`). Force a **carve-out** when the in-deck count itself
  is player-count-dependent, additive/subtractive per player, or conditionally
  extended ("add a 9th Twist if …"). When in doubt, carve out.
- **Curation rule (bystanders):** encode `villainDeckBystanderCount` only for a
  scheme whose printed Setup specifies an **explicit** villain-deck bystander
  count, including an explicit **zero** ("No Bystanders in the Villain Deck" ⇒
  `0`). A scheme that says nothing about villain-deck bystanders stays unencoded
  (the `numPlayers` default is correct).
- **At-least-one-count rule (relaxed contract, D-16803):** an entry in
  `scheme-deck-counts.json` must carry **at least one** of the two counts; it may
  carry only one. An omitted count keeps that scheme's engine default. The
  converter assigns each count independently and must never write an
  `undefined`-valued key.
- **Loud-fail contract (preserved + extended):** matching is exact `schemeSlug`
  equality, never fuzzy or name-normalized. Any entry whose slug matches no
  scheme in the set throws a full-sentence error naming the set and slug and
  exits non-zero, **before** that set's output is written. This already holds in
  `convert-cards-v15.mjs`; `apply-card-counts.mjs` must gain the identical guard
  for the 4 outlier sets.
- **Empty-entry loud-fail:** an entry present for a `schemeSlug` but carrying
  neither count is malformed input. Both converters throw a full-sentence error
  naming the set and slug and exit non-zero — never silently skip. `// why:`
  enforces the at-least-one-count contract (D-16803).
- **Field placement invariant:** the converter appends the count field(s) **after
  the scheme's existing keys** (matching WP-167's Midtown output — i.e. after
  `cards`), with `villainDeckTwistCount` before `villainDeckBystanderCount` when
  both are present. It must not reorder or move any existing scheme key. `// why:`
  stable key order keeps regeneration diff-free.
- **Mutation boundary:** `applySchemeDeckCounts` and the `apply-card-counts.mjs`
  overlay mutate **only** the two count fields on the matched scheme, in place;
  they must not clone, reconstruct, or re-serialize the `scheme` object or touch
  any other key. `// why:` prevents accidental key reordering or loss of unrelated
  fields.
- **Clean-diff / determinism:** new fields are appended in the existing fixed
  position; no key reordering or re-sorting. Regenerating twice yields zero diff
  (idempotent).
- **No unintended mutation:** the only deltas in regenerated `data/cards/*.json`
  are the curated scheme `villainDeckTwistCount` / `villainDeckBystanderCount`.
  WP-167's villain `copies`, `alwaysLeads[]` / `ledBy[]`, and every hero /
  keyword / image URL / ability / henchman / `physicalCards[]` field are
  unchanged.

**Session protocol:**
- If a scheme's Setup text is ambiguous about whether a printed count is a single
  constant or player-count-dependent, classify it as a **carve-out** (do not
  encode) and record it in the D-16804 carve-out list — never guess a flat value.

**Locked contract values (relevant subset):**
- **Engine fallbacks (do not encode when the printed value equals these):**
  twists `8` (D-1411); bystanders `numPlayers` (D-1412).
- **Field names:** `villainDeckTwistCount`, `villainDeckBystanderCount`.
- **Input shape:** `{ setAbbr: { schemeSlug: { villainDeckTwistCount?, villainDeckBystanderCount? } } }`
  (at least one count present per entry, per D-16803).
- **Midtown Bank Robbery (existing, keep):** `core.midtown-bank-robbery →
  { villainDeckTwistCount: 8, villainDeckBystanderCount: 12 }`. (Twist 8 is the
  default but is retained because the entry also carries the bystander override.)

---

## Scope (In)

### A) Converter — `scripts/convert-cards/convert-cards-v15.mjs` (modified)
- Rewrite the body of `applySchemeDeckCounts` so each count is assigned
  **independently and only when present** in the entry:
  - if the entry has `villainDeckTwistCount`, set it on the scheme; else leave the
    scheme's field absent.
  - same for `villainDeckBystanderCount`.
  - `// why:` an omitted count keeps the scheme's engine default (8 twists /
    `numPlayers` bystanders) per D-16702/D-16803; never write an `undefined` key.
- Preserve the existing loud-fail-on-unmatched-slug throw and the
  validate-before-write ordering. No other converter logic changes.

### B) Outlier applier — `scripts/convert-cards/apply-card-counts.mjs` (modified)
- Load `inputs/scheme-deck-counts.json` (mirroring the existing
  `villain-card-counts.json` / `leads.json` loads).
- For each of the 4 outlier sets it produces, apply the scheme counts with the
  **same independent-assignment + exact-slug loud-fail + validate-before-write**
  semantics as Scope A. `// why:` `apply-card-counts.mjs` is the only producer for
  these sets, so without this overlay any `2099` / `amwp` / `wpnx` / `wtif`
  scheme entry is silently ignored (D-16803).
- This corrects the `_note`'s currently-false claim that this script already
  applies scheme counts.
- The overlay must be **idempotent** — a second run over already-curated data
  produces no diff and preserves key order. (Covered by the global idempotency AC
  + Verification Step 5, which runs both converters; called out here because this
  script is the sole producer for the outlier sets.)

### C) Pipeline input — `scripts/convert-cards/inputs/scheme-deck-counts.json` (modified)
- Curate, by reading each scheme's committed Setup line in `data/cards/*.json`:
  - **Twist overrides:** every scheme whose Setup prints a single fixed twist
    count ≠ 8 → `{ villainDeckTwistCount: <n> }`. (Example: `core.portals-to-the-dark-dimension`
    prints "Setup: 7 Twists." → `{ villainDeckTwistCount: 7 }`.)
  - **Bystander overrides:** every scheme whose Setup prints an explicit
    villain-deck bystander count → `{ villainDeckBystanderCount: <n> }`, including
    the explicit-zero case (`chmp.hypnotize-every-human` prints "No Bystanders in
    the Villain Deck" → `0`).
  - **Both:** schemes printing both a non-8 twist count and an explicit bystander
    count carry both (example: `core.replace-earths-leaders-with-killbots` prints
    "5 Twists … 18 total Bystanders in the Villain Deck" → `{ 5, 18 }`; the "3
    additional Twists next to this Scheme" are set aside, not villain-deck cards).
  - Keep the existing Midtown entry.
- **Census completeness:** every scheme across all 40 sets is triaged during
  curation into exactly one of {default-8 (unencoded), flat-twist-encoded,
  bystander-encoded, carve-out}. Absence from the file means "intentionally
  default or carve-out", never oversight; the carve-out subset is finalized in
  D-16804.
- Update the top-level `_note` to state, at minimum: (i) the at-least-one-count
  contract (D-16803); (ii) omitted counts resolve to engine defaults (8 twists /
  `numPlayers` bystanders); (iii) which producer applies which sets —
  `convert-cards-v15.mjs` for the 36 npm sets, `apply-card-counts.mjs` for the 4
  outlier sets (correcting the prior over-claim); (iv) counts are extracted from
  the committed Setup text; (v) player-count-dependent twist counts are carved
  out (D-16804).

### D) Regenerate data — `data/cards/*.json` (regenerated)
- Run both converters to regenerate all 40 set files. The only gameplay-relevant
  deltas are the curated scheme `villainDeckTwistCount` / `villainDeckBystanderCount`.

### E) Tests — `packages/registry/src/schema.schemeDeckCounts.test.ts` (new)
- A **twist-only** curated scheme parses with `villainDeckTwistCount` present and
  `villainDeckBystanderCount` **absent** (proves independent assignment — the key
  is genuinely omitted, not `null`/`undefined`/defaulted).
- The **explicit-zero** bystander scheme parses with `villainDeckBystanderCount === 0`.
- An **outlier-set** scheme curated via `apply-card-counts.mjs` carries its
  curated count in the regenerated data (proves Scope B wiring).
- Every scheme that prints exactly 8 twists and no explicit bystander count
  carries **neither** field (proves the curation rule did not over-encode
  defaults).
- A known **carve-out** scheme (player-count-dependent twists, e.g. Super Hero
  Civil War) carries **no** `villainDeckTwistCount` after regeneration (proves
  carve-outs are not encoded).
- All 40 regenerated `data/cards/*.json` validate against `SetDataSchema`.
- Uses `node:test` / `node:assert`; no `boardgame.io` / `@legendary-arena/game-engine` import.

### F) Decisions — `docs/ai/DECISIONS.md` (modified)
- D-16803 and D-16804 (see Decisions to Record).

### G) Governance — `docs/ai/STATUS.md`, `WORK_INDEX.md`, `EC_INDEX.md` (modified)
- STATUS: record that scheme villain-deck twist/bystander counts are curated from
  printed setup across all 40 sets (player-count-dependent twists carved out).
- WORK_INDEX: check off WP-169. EC_INDEX: flip EC-187 to Done.

---

## Out of Scope

- **No schema changes** — the two fields already exist (WP-167 / D-16702).
- **No engine changes** — `buildVillainDeck` consuming these counts is WP-168.
- **Player-count-dependent / additive / non-constant twist counts are NOT
  encoded** (the carve-out). These schemes keep the 8-twist fallback pending a
  **future schema + engine WP** that can represent a per-player twist mapping
  (a single integer cannot; WP-168 reads a single integer). The carve-out is
  enumerated and finalized in D-16804. Representative carve-out schemes
  (executor verifies slugs during the census; the list below is illustrative,
  not exhaustive):
  - `core` / `msp1` "Super Hero Civil War" (2–3 players: 8; 4–5 players: 5)
  - `cvwr` (1–3 players: 9; 4–5 players: 6)
  - `dstr` (2 players: 9; 1 or 4 players: 10; 3 or 5 players: 11)
  - `rvlt` (1 player: 7; 2–3 players: 9; 4–5 players: 11)
  - `wpnx` (1 player: 8; 2–4 players: 10; 5 players: 11)
  - `bkwd` ("8 Twists, minus 1 Twist per player"; and the 11-base variant)
  - `mdns` ("6 Twists, plus 1 per player")
  - `dead` (1–3 players: 6; 4–5 players: 5)
  - `ssw2` ("8 Twists. (1 player: 4 Twists)")
  - borderline "base + conditional add" schemes (e.g. `fear`'s "8 Twists … a 9th
    Twist", `ssw1`'s extra-group split, `wtif`'s reality split) — classify as
    carve-outs unless the printed villain-deck twist count is unambiguously a
    single constant.
- **No bystander encoding for schemes that manipulate bystanders by mechanic
  rather than by villain-deck count** (e.g. `core.secret-invasion-…` shuffles
  Heroes, not bystanders) — only an explicit printed villain-deck bystander count
  is encoded.
- **No provenance companion file** (see Context — rejected deliberately).
- **No changes** to `villain-card-counts.json`, `leads.json`, villain `copies`,
  lead arrays, henchmen, schema, or `00.2` field tables (WP-167 territory).
- Refactors or "while I'm here" cleanups of the converters beyond Scope A/B.

---

## Files Expected to Change

- `scripts/convert-cards/convert-cards-v15.mjs` — **modified** — independent
  per-count assignment in `applySchemeDeckCounts`.
- `scripts/convert-cards/apply-card-counts.mjs` — **modified** — load + apply
  scheme-deck-counts for the 4 outlier sets (same loud-fail + validate-before-write).
- `scripts/convert-cards/inputs/scheme-deck-counts.json` — **modified** — curate
  flat twist overrides + explicit bystander overrides (incl. zero); update `_note`.
- `data/cards/*.json` — **regenerated** — curated scheme counts only (all 40 files).
- `packages/registry/src/schema.schemeDeckCounts.test.ts` — **new** — `node:test`
  coverage (twist-only omission, zero bystander, outlier-set application,
  no-over-encoding, carve-out-not-encoded, 40-file validation).
- `docs/ai/DECISIONS.md` — **modified** — D-16803, D-16804.
- `docs/ai/STATUS.md` — **modified** — record the curated capability.
- `docs/ai/work-packets/WORK_INDEX.md` — **modified** — check off WP-169.
- `docs/ai/execution-checklists/EC_INDEX.md` — **modified** — EC-187 → Done.

No other files may be modified.

---

## Acceptance Criteria

### Pipeline
- [ ] `applySchemeDeckCounts` assigns each count independently; a twist-only entry
      yields a scheme with `villainDeckTwistCount` set and **no**
      `villainDeckBystanderCount` key (and vice versa). No `undefined`-valued key
      is ever written.
- [ ] `apply-card-counts.mjs` applies scheme counts to the 4 outlier sets with the
      same exact-slug loud-fail (full-sentence error, non-zero exit, no partial
      write) on a slug matching no scheme.
- [ ] Both converters loud-fail on a bogus `schemeSlug` (manual probe in
      Verification Steps).
- [ ] Both converters loud-fail (full-sentence error, non-zero exit) on an entry
      that exists for a real `schemeSlug` but carries neither count (empty entry).

### Data
- [ ] Every scheme whose committed Setup prints a single fixed twist count ≠ 8
      carries that `villainDeckTwistCount`; every scheme printing exactly 8 (and
      no explicit bystander count) carries neither field.
- [ ] Every scheme printing an explicit villain-deck bystander count carries that
      `villainDeckBystanderCount`, including `chmp.hypnotize-every-human → 0`.
- [ ] No carve-out (player-count-dependent) scheme carries a
      `villainDeckTwistCount`.
- [ ] All 40 `data/cards/*.json` validate against `SetDataSchema`.
- [ ] No unintended mutation: the only deltas across all 40 files are scheme
      `villainDeckTwistCount` / `villainDeckBystanderCount`; WP-167's `copies`,
      `alwaysLeads[]` / `ledBy[]`, and all other fields are unchanged.
- [ ] Regenerating a second time produces zero diff (idempotent).

### Tests / Scope
- [ ] `pnpm --filter @legendary-arena/registry test` exits 0.
- [ ] New test uses `node:test` / `node:assert`; no `boardgame.io` /
      `@legendary-arena/game-engine` import.
- [ ] No files outside `## Files Expected to Change` modified
      (`git diff --name-only`).

---

## Verification Steps

```pwsh
# Step 1 — regenerate card data with both converters
node scripts/convert-cards/convert-cards-v15.mjs
node scripts/convert-cards/apply-card-counts.mjs
# Expected: exits 0; loud-fails only on genuine input mismatches

# Step 2 — run registry tests (includes 40-file validation + the new cases)
pnpm --filter @legendary-arena/registry test
# Expected: TAP output — all tests passing, 0 failing

# Step 3 — spot-check a twist-only scheme (no bystander key) and the zero case
Select-String -Path "data\cards\core.json" -Pattern "villainDeckTwistCount"
Select-String -Path "data\cards\chmp.json" -Pattern "villainDeckBystanderCount"
# Expected: portals carries twist 7 with no bystander key; hypnotize carries bystander 0

# Step 4 — spot-check an outlier-set scheme (proves apply-card-counts.mjs wiring)
Select-String -Path "data\cards\2099.json" -Pattern "villainDeckTwistCount"
# Expected: the curated non-8 twist count(s) for that set appear

# Step 5 — idempotency: a second regen must produce zero diff
node scripts/convert-cards/convert-cards-v15.mjs
node scripts/convert-cards/apply-card-counts.mjs
git diff --name-only -- data/cards/
# Expected: empty output

# Step 6 — confirm only expected files changed
git diff --name-only
# Expected: two converter scripts, scheme-deck-counts.json, data/cards/*.json, new test, governance

# Step 7 — loud-fail (manual): temporarily add a bogus schemeSlug under an outlier
#   set (e.g. "wtif": { "no-such-scheme": { "villainDeckTwistCount": 7 } }),
#   run apply-card-counts.mjs, confirm non-zero exit with a full-sentence error
#   naming the set + slug, then revert. Do NOT commit the bogus entry.
```

---

## Decisions to Record

- **D-16803 — Scheme-deck-count entries carry counts independently; the outlier
  applier applies them too.** An `inputs/scheme-deck-counts.json` entry must carry
  at least one of `villainDeckTwistCount` / `villainDeckBystanderCount` and may
  carry only one; an omitted count keeps that scheme's engine default (8 twists /
  `numPlayers` bystanders, D-1411/D-1412). `applySchemeDeckCounts` assigns each
  count independently and never writes an `undefined` key. `apply-card-counts.mjs`
  applies the same overlay (with the same exact-slug loud-fail) to the 4 outlier
  sets, correcting WP-167's `_note` over-claim. Relaxes the WP-167 "both counts
  required when a scheme is listed" note; the field semantics (D-16702) are
  unchanged.
- **D-16804 — Player-count-dependent scheme twist counts are carved out of
  curation pending a per-player representation.** Schemes whose printed villain-
  deck twist count varies with player count (or is additive-per-player, or is
  otherwise not a single constant) are **not** encoded in
  `scheme-deck-counts.json`; they keep the 8-twist fallback (D-1411). The current
  single-integer `villainDeckTwistCount` (D-16702) and WP-168's single-integer
  read cannot represent these. A future WP may introduce a per-player twist
  mapping (and the matching engine resolution) to close the gap. The finalized
  carve-out scheme list is recorded in this decision's body at execution time.

---

## Definition of Done

This packet is complete when ALL of the following are true:

- [ ] All acceptance criteria above pass
- [ ] `pnpm --filter @legendary-arena/registry build` exits 0
- [ ] `pnpm --filter @legendary-arena/registry test` exits 0
- [ ] All 40 `data/cards/*.json` validate against `SetDataSchema`
- [ ] Regenerating the data a second time produces no diff (converters idempotent)
- [ ] No unintended mutation: only scheme twist/bystander counts changed
- [ ] No files outside `## Files Expected to Change` were modified
      (confirmed with `git diff --name-only`)
- [ ] `docs/ai/STATUS.md` updated — scheme villain-deck counts curated; carve-out noted
- [ ] `docs/ai/DECISIONS.md` updated — D-16803, D-16804 recorded (D-16804 carries
      the finalized carve-out list)
- [ ] `docs/ai/work-packets/WORK_INDEX.md` has WP-169 checked off with today's date
- [ ] `docs/ai/execution-checklists/EC_INDEX.md` has EC-187 flipped to Done

---

## Lint Gate Self-Review

Per `docs/ai/REFERENCE/00.3-prompt-lint-checklist.md`. All 21 sections PASS or
are N/A with a stated reason.

| § | Section | Result |
|---|---|---|
| 1 | Work Packet Structure | PASS — all required sections present |
| 2 | Non-Negotiable Constraints block | PASS — engine-wide + packet-specific + curation/carve-out rules + session protocol + locked contract values |
| 3 | Prerequisites (`## Assumes`) | PASS — WP-167 landed, field/converter/input-file state, fallbacks, and the apply-card-counts gap all listed |
| 4 | Context References | PASS — converter line refs, the committed-Setup-text extraction source, DECISIONS D-1411/D-1412/D-16702/D-16703, 00.2 §1.5 |
| 5 | Files Expected to Change | PASS — each entry marked new/modified; `data/cards/*.json` regen is one glob entry gated by the §Scope-Enforcement `git diff --name-only` check |
| 6 | Naming Consistency | PASS — `villainDeckTwistCount` / `villainDeckBystanderCount` locked; no new field names |
| 7 | Dependency Discipline | PASS — no new npm dependencies; no schema/engine import |
| 8 | Architectural Boundaries | PASS — registry/pipeline only; converter is build tooling; no `game-engine` / `apps/server` / `pg` import; `G` untouched |
| 9 | Windows Compatibility | PASS — verification uses `pwsh` `Select-String` + `node` with `\` paths |
| 10 | Environment Variable Hygiene | N/A — introduces and consumes no environment variables |
| 11 | Authentication Clarity | N/A — no identity or endpoint surface |
| 12 | Test Quality | PASS — `node:test` / `node:assert`; no `boardgame.io/testing`; positive (curated) + negative (no-over-encoding, key-absent) + 40-file validation |
| 13 | Commands and Verification | PASS — exact converter `node` commands, `pnpm --filter` test, inline expected output, idempotency + manual loud-fail probe |
| 14 | Acceptance Criteria Quality | PASS — binary, observable items aligned to deliverables |
| 15 | Definition of Done | PASS — acceptance + STATUS + DECISIONS + WORK_INDEX + EC_INDEX + no-out-of-scope-files check |
| 16 | Code Style | PASS — mandates human-style code, `// why:` on the independent-assignment rationale, full-sentence loud-fail errors |
| 17 | Vision Alignment | PASS — `## Vision Alignment` block present (§1/§2/§10/§14, no-conflict assertion, NG-1..7, determinism line); triggered by §17.1 |
| 18 | Prose-vs-Grep Discipline | PASS — verification greps `data/cards/*.json` for the field names (not policed literals); count-bounded gates avoided |
| 19 | Bridge-vs-HEAD Staleness | N/A — not a repo-state-summarizing artifact |
| 20 | Funding Surface Gate | N/A — data-pipeline + docs only; no funding affordances or copy |
| 21 | API Catalog Update | N/A — touches no `apps/server` HTTP endpoint and no `apps/server`-reachable library function; registry data + converter only |
