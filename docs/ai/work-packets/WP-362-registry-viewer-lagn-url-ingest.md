# WP-362 — Loadout Tab: Open a LAGN From the URL (`?lagn=`) (Registry Viewer)

**Status:** Draft 2026-07-11 · **READY (not blocked — all hard-deps Done)** · **Standard two-session lane** (D-24028 — one pure decoder + one composable + an App.vue wiring/auto-switch; no server, no new contract). Pairs with **EC-392** (authored at execution-prep). Reserves **D-24154** (lands at execution).
**Primary Layer:** Registry Viewer (`apps/registry-viewer`)
**User-Visible Surface:** **cards.legendary-arena.com** (a `?lagn=…` link opens the Loadout tab pre-filled with that game's composition). **D-24026 live-verify APPLIES** (open a `?lagn=` URL → the Loadout tab shows the composition).
**Dependencies:** WP-291 (`parseLagnLoadout` — the LAGN-text → composition validator + the `applyLagnImport` draft-setter sequence) ✅ **Done**; WP-114 (`useSetupFromUrl` + the one-shot auto-switch-to-Loadout-tab pattern in `App.vue`) ✅; WP-245 (the `useLoadoutLagnExport` round-trip origin) ✅. **No unmerged dependency — executable now.**
**Baseline:** `origin/main` @ (capture `git rev-parse origin/main` at execution).

---

## Session Context

WP-291 gave the Loadout tab a LAGN importer (`parseLagnLoadout` + `applyLagnImport`) reachable only by file-upload/paste; WP-114 gave the viewer a URL-param setup deep-link (`useSetupFromUrl`) that auto-switches to the Loadout tab on mount; this packet joins the two — a `?lagn=<base64url>` param decodes to a LAGN, runs through the **existing** `parseLagnLoadout`, applies to the draft, and reuses WP-114's one-shot tab auto-switch.

---

## Goal

The Registry Viewer gains a `?lagn=<base64url>` URL parameter. On mount, when present, the viewer base64url-decodes it to LAGN JSON text, validates it through the **existing** `parseLagnLoadout` (`@legendary-arena/lagn`), applies the extracted composition to the Loadout draft via the existing draft setters, and one-shot auto-switches to the Loadout tab (reusing WP-114's `hasAppliedUrlAutoSwitch` machinery). A malformed/invalid payload switches to the tab and surfaces the validator's full-sentence errors instead of failing silently. This is what WP-363's play-surface "View loadout" link targets: a self-contained deep-link the viewer renders with **no** call back to the game server (no viewer auth, no CORS).

---

## User-Visible Impact

A visitor who opens a `https://cards.legendary-arena.com/?lagn=…` link lands directly on the Loadout tab with the Mastermind, Scheme, villain/henchman groups, hero decks, supply counts, and player count already filled in — the exact loadout the link encoded. A bad link still opens the Loadout tab and shows a clear "this LAGN could not be read" message rather than a blank builder. (The link itself is generated by WP-363's in-match "View loadout in Registry Viewer" affordance.)

---

## Assumes

- **`parseLagnLoadout(jsonText)` (WP-291) validates LAGN text and returns `{ ok, composition | errors }`.** It calls the published `validate()` and maps `setup` → the MATCH-SETUP composition (incl. `shield_officers_count → officersCount`). (Verified: `apps/registry-viewer/src/lib/loadoutLagnImport.ts`.)
- **`LoadoutBuilder.applyLagnImport` (WP-291) is the draft-apply sequence** — `resetDraft()` then `setScheme`/`setMastermind`/`addVillainGroup`/`addHenchmanGroup`/`addHeroGroup`/`setCount`/`setPlayerCount`. The URL path applies the same sequence to the same `UseLoadoutDraftApi`. (Verified: `apps/registry-viewer/src/components/LoadoutBuilder.vue`.)
- **`App.vue` owns `loadoutDraftApi` (`shallowRef<UseLoadoutDraftApi | null>`, null until the registry loads), `activeView` (`"cards" | "themes" | "loadout"`), and the one-shot `hasAppliedUrlAutoSwitch` guard.** `useSetupFromUrl` is instantiated once in `onMounted` after the registry resolves. (Verified: `App.vue:90-135, 337-353`.)
- **The viewer test runner is `node --import tsx --test src/**/*.test.ts`; typecheck is `vue-tsc --noEmit`.** (Verified: `apps/registry-viewer/package.json` scripts.)
- **Browser `atob` is available** for base64 decode; base64url (`-_`, no padding) is normalized to base64 before `atob`, and UTF-8 bytes are decoded to text. No new dependency.

If any of the above is false, this packet is **BLOCKED** and must not proceed.

---

## Context (Read First)

- `apps/registry-viewer/src/lib/loadoutLagnImport.ts` — `parseLagnLoadout` (reused verbatim; the URL decoder feeds it text).
- `apps/registry-viewer/src/lib/setupUrlParams.ts` + `apps/registry-viewer/src/composables/useSetupFromUrl.ts` — the WP-114 pure-URL-parse + read-once-on-mount pattern to mirror for the new decoder/composable.
- `apps/registry-viewer/src/components/LoadoutBuilder.vue` — `applyLagnImport` (the exact draft-setter sequence to reuse) + the `lagnImportErrors` surface (the error-display idiom to mirror for a URL-origin error).
- `apps/registry-viewer/src/App.vue` — `onMounted` registry-load block, `loadoutDraftApi`, `activeView`, `hasAppliedUrlAutoSwitch`, the existing setup-param auto-switch (extend this block).
- `docs/ai/REFERENCE/00.2-data-requirements.md §8.1` — the 9 canonical composition field names (unchanged; `parseLagnLoadout` already maps them).
- `docs/ai/REFERENCE/00.6-code-style.md` — Rule 4 (no abbreviations), Rule 6 (`// why:`), Rule 11 (full-sentence errors), Rule 13 (ESM).

---

## Non-Negotiable Constraints

**Engine-wide (always apply — do not remove):**
- ESM only, Node v22+; `node:` prefix where applicable; `.test.ts`; human-style code per `00.6`; full-sentence errors; `// why:` on non-obvious choices; JSDoc; no branching `.reduce()`.
- Full file contents for every new/modified file — no diffs, no snippets.

**Packet-specific:**
- **No new dependency.** base64url decode uses the browser `atob` + a small normalize/UTF-8 step; no `Buffer`, no npm package.
- **Reuse `parseLagnLoadout` verbatim — do not fork the validator.** The decoder only turns the URL param into the JSON **text** `parseLagnLoadout` already accepts; all validation stays in the one WP-291 module.
- **Pure decoder.** `lagnUrlParam.ts` has no Vue, no DOM beyond `atob`, no network, no clock/RNG; deterministic (same param string → same text or same error). It never throws — a malformed param returns a typed error result.
- **One-shot auto-switch.** Reuse `hasAppliedUrlAutoSwitch`; the `?lagn=` read + apply + tab-switch happens once on mount, never re-fires on later reactive changes (mirrors WP-114).
- **`?lagn=` wins over the 5-field setup params.** When both are present, the full LAGN composition is authoritative and the `?schemeId=…` setup-preview params are ignored — document with a `// why:`.
- **Fail-visible, never silent.** A malformed base64 or an invalid LAGN still switches to the Loadout tab and shows the full-sentence error(s); it never leaves the builder blank with no explanation and never applies a partial composition.
- **No server call, no auth.** The viewer decodes a self-contained payload; it never fetches from the game server, adds no CORS surface, and requires no session.

**Session protocol:**
- If the draft-setter names or the `parseLagnLoadout` result shape are unclear, stop and read `LoadoutBuilder.vue` / `loadoutLagnImport.ts` — do not invent setters.

**Locked contract values:**
- **URL param key:** `lagn` (value = base64url of the UTF-8 LAGN JSON text).
- **Composition field names** (produced by `parseLagnLoadout`, unchanged): `schemeId`, `mastermindId`, `villainGroupIds`, `henchmanGroupIds`, `heroDeckIds`, `bystandersCount`, `woundsCount`, `officersCount`, `sidekicksCount` + `playerCount`.
- **Precedence:** `?lagn=` present ⇒ ignore `?schemeId=`/`?mastermindId=`/`?villainGroupIds=`/`?henchmanGroupIds=`/`?heroDeckIds=`.

---

## Debuggability & Diagnostics

The decode + apply is deterministic: the same `?lagn=` string produces the same draft state or the same error, inspectable in the Loadout tab. Three observable outcomes: valid → tab pre-filled; malformed base64 → tab + "could not be decoded" error; valid base64 but invalid LAGN → tab + `parseLagnLoadout`'s field-level errors. No hidden side effects; the decoder is unit-tested with pure string inputs and the composable with an injected draft api.

---

## Scope (In)

### A) `lib/lagnUrlParam.ts` (new) — pure decoder
- `parseLagnUrlParam(search): ParseLagnUrlParamResult` where the result is `{ present: false }` | `{ present: true; ok: true; text: string }` | `{ present: true; ok: false; error: string }`:
  - `URLSearchParams(search).get('lagn')` — absent ⇒ `{ present: false }`.
  - base64url → base64 (`-`→`+`, `_`→`/`, right-pad `=` to a multiple of 4), `atob`, then UTF-8-decode the resulting byte string to text. Add `// why:` — `atob` yields a Latin-1 byte string; the UTF-8 step recovers multi-byte card names.
  - Any decode failure ⇒ `{ present: true, ok: false, error: 'The loadout link could not be decoded. It may be truncated or corrupted — ask for a fresh link from the game.' }` (full sentence). Never throws.

### B) `composables/useLagnFromUrl.ts` (new) — read-once + apply
- `useLagnFromUrl(draftApi: UseLoadoutDraftApi): { hasLagnParam: boolean; lagnUrlErrors: string[] }`:
  - Reads `window.location.search` **once** at instantiation (mirrors `useSetupFromUrl`).
  - `parseLagnUrlParam(...)` absent ⇒ `{ hasLagnParam: false, lagnUrlErrors: [] }`.
  - Decode error ⇒ `{ hasLagnParam: true, lagnUrlErrors: [<decode error>] }` (tab still switches; nothing applied).
  - Text ⇒ `parseLagnLoadout(text)`; `ok: false` ⇒ `{ hasLagnParam: true, lagnUrlErrors: result.errors }`; `ok: true` ⇒ apply the composition to `draftApi` via the WP-291 setter sequence (`resetDraft` → `setScheme`/`setMastermind`/`addVillainGroup`/`addHenchmanGroup`/`addHeroGroup`/`setCount`×4/`setPlayerCount`) and return `{ hasLagnParam: true, lagnUrlErrors: [] }`.
  - Add `// why:` — the setter sequence duplicates `LoadoutBuilder.applyLagnImport`'s apply (second call site; still under the rule-of-three, so duplicated rather than abstracted).

### C) `App.vue` (modified) — wire + auto-switch + error surface
- In the `onMounted` registry-load block, after `useSetupFromUrl`, call `useLagnFromUrl(loadoutDraftApi.value)` (guard for the null-until-loaded api).
- Extend the one-shot auto-switch condition so `hasLagnParam` (like `setupHasUrlParams`) sets `activeView = 'loadout'` once (`hasAppliedUrlAutoSwitch`). `?lagn=` precedence: when `hasLagnParam` is true, skip applying the setup-preview params.
- Store `lagnUrlErrors` in a top-level ref and render it in the Loadout tab as a dismissible error banner (mirror the `lagnImportErrors` display idiom) so a bad link is explained on-screen.

### D) Tests
- `lib/lagnUrlParam.test.ts` — absent param → `{ present: false }`; a valid base64url of a Tier-1 LAGN → `{ present:true, ok:true, text }` that round-trips to the same JSON; a UTF-8 card name survives the round-trip; a truncated/`!!!` param → `{ present:true, ok:false, error }` (full sentence); never throws.
- `composables/useLagnFromUrl.test.ts` — with an injected fake `UseLoadoutDraftApi`: a valid `?lagn=` calls the setters with the decoded composition and returns no errors; an invalid LAGN returns `parseLagnLoadout`'s errors and calls no setters; an absent param returns `hasLagnParam:false`.

---

## Out of Scope

- **No server call / no auth / no CORS** — the viewer only decodes a self-contained URL payload; fetching a LAGN by matchId from the game server is **WP-361** (server) consumed by **WP-363** (client).
- **No outbound link/QR generation in the viewer** — the viewer is the consumer; the link is generated on the play surface (WP-363).
- **No change to `parseLagnLoadout` / `loadoutLagnImport.ts`** — it is reused verbatim (a locked WP-291 module for this packet's purpose).
- **No change to the WP-114 5-field setup deep-link** (`setupUrlParams.ts` / `useSetupFromUrl.ts`) — the new `?lagn=` param is additive and takes precedence when both are present.
- **No Tier-2/Tier-3 handling** — `parseLagnLoadout` already reads only `setup` + `player_count`; the extra tiers are validated-but-ignored upstream.
- Refactors or "while I'm here" cleanups to LoadoutBuilder/App beyond the wiring above.

---

## Files Expected to Change

- `apps/registry-viewer/src/lib/lagnUrlParam.ts` — **new** — pure base64url `?lagn=` decoder → LAGN text or typed error
- `apps/registry-viewer/src/composables/useLagnFromUrl.ts` — **new** — read-once, decode → `parseLagnLoadout` → apply to draft
- `apps/registry-viewer/src/lib/lagnUrlParam.test.ts` — **new** — `node:test` coverage (decode)
- `apps/registry-viewer/src/composables/useLagnFromUrl.test.ts` — **new** — `node:test` coverage (apply, injected draft api)
- `apps/registry-viewer/src/App.vue` — **modified** — instantiate `useLagnFromUrl`, extend the one-shot auto-switch, render the `lagnUrlErrors` banner in the Loadout tab
- Governance: `WORK_INDEX.md` + `DECISIONS.md` (**D-24154**) + `STATUS.md` + `wiki/lagn-v1.md`. `EC_INDEX.md` + EC-392 at execution-prep.

**~2 new code + 2 tests + 1 App wiring. Standard two-session lane.**

No other files may be modified.

---

## Contract

### URL param
`?lagn=<base64url(UTF-8 LAGN JSON)>` on the Registry Viewer origin (`cards.legendary-arena.com`). Present ⇒ auto-switch to the Loadout tab; decode + `parseLagnLoadout` apply on success, full-sentence error banner on failure; takes precedence over the WP-114 `?schemeId=…` setup params.

### Locked Values
| Key | Value |
|---|---|
| Param key | `lagn` (base64url of the UTF-8 LAGN JSON) |
| Validator | reuse `parseLagnLoadout` (WP-291) verbatim — no fork |
| Apply | WP-291 setter sequence on `UseLoadoutDraftApi` |
| Auto-switch | one-shot via `hasAppliedUrlAutoSwitch` (WP-114 pattern) |
| Precedence | `?lagn=` present ⇒ ignore `?schemeId=`/etc. |
| Failure | switch to tab + show full-sentence error(s); never blank, never partial |
| Network | none — self-contained payload, no server call, no auth |

---

## Acceptance Criteria

1. `parseLagnUrlParam` returns `{ present:false }` with no `lagn` key; `{ present:true, ok:true, text }` for a valid base64url payload that round-trips to the original JSON (incl. a UTF-8 card name); `{ present:true, ok:false, error }` (full sentence) for a corrupt payload; never throws (**AC-1**).
2. `useLagnFromUrl` applies the decoded composition to the injected draft api via the WP-291 setters on a valid `?lagn=`, returns `parseLagnLoadout`'s errors (and applies nothing) on an invalid one, and returns `hasLagnParam:false` when the param is absent (**AC-2**).
3. On mount with a valid `?lagn=`, `App.vue` switches `activeView` to `'loadout'` once and the builder shows the encoded composition; with a bad `?lagn=`, it switches and shows the error banner (**AC-3**).
4. When both `?lagn=` and `?schemeId=` are present, the LAGN composition wins and the setup-preview params are not applied (**AC-4**).
5. No new npm dependency; `lagnUrlParam.ts` imports no Vue/network and never throws (confirmed with `Select-String` + the never-throws test) (**AC-5**).
6. `parseLagnLoadout` / `loadoutLagnImport.ts` and the WP-114 `setupUrlParams.ts` / `useSetupFromUrl.ts` are unmodified (confirmed with `git diff`) (**AC-6**).
7. `pnpm --filter registry-viewer build` 0; `pnpm --filter registry-viewer typecheck` 0; `pnpm --filter registry-viewer test` green (**AC-7**).

---

## Verification Steps

```pwsh
pnpm --filter registry-viewer build       # 0
pnpm --filter registry-viewer typecheck    # 0 (vue-tsc)
pnpm --filter registry-viewer test         # lagnUrlParam + useLagnFromUrl suites green
Select-String -Path "apps\registry-viewer\src\lib\lagnUrlParam.ts" -Pattern "from ['""]vue['""]|fetch(|Buffer"   # no output (pure, no Vue/network/Buffer)
Select-String -Path "apps\registry-viewer\src\composables\useLagnFromUrl.ts" -Pattern "parseLagnLoadout"   # reuses the WP-291 validator
git diff --name-only   # only the ## Files Expected to Change set; loadoutLagnImport.ts / setupUrlParams.ts / useSetupFromUrl.ts absent
```

---

## Definition of Done

- [ ] All acceptance criteria pass
- [ ] `lagnUrlParam.ts` — pure base64url decoder → LAGN text or full-sentence error; never throws
- [ ] `useLagnFromUrl.ts` — read-once, decode → `parseLagnLoadout` → apply via WP-291 setters; returns `hasLagnParam` + `lagnUrlErrors`
- [ ] `App.vue` — one-shot auto-switch on `hasLagnParam`, `?lagn=` precedence over setup params, error banner in the Loadout tab
- [ ] `parseLagnLoadout`/`loadoutLagnImport.ts` + WP-114 setup-param files unmodified; no new dependency
- [ ] `pnpm --filter registry-viewer build`/`typecheck`/`test` green
- [ ] `DECISIONS.md` **D-24154** landed; `WORK_INDEX` (WP-362) + `STATUS.md` + `wiki/lagn-v1.md` updated
- [ ] **User-visible verification (D-24026):** APPLIES. On the deployed viewer, open `https://cards.legendary-arena.com/?lagn=<a real Tier-1 export>` → the Loadout tab opens pre-filled with that composition; open a truncated `?lagn=` → the tab opens with the decode-error banner. Screenshot both. Operator-pending on deploy.

---

## Vision Alignment

**Vision clauses touched:** §10a (Registry Viewer public surface — a new deep-link entry into the Loadout tab), §18 (LAGN interoperability — consuming the open notation). **Conflict assertion:** No conflict — an additive URL entry point that reuses the published validator and the existing builder; no data model or scoring change. **Non-Goal check (user-facing surface):** none of NG-1..7 crossed — the Loadout tab is a free public inspection surface; a pre-filled loadout confers no gameplay advantage and gates nothing. **Determinism:** N/A — a client-side URL decode + form pre-fill; no engine, `G`, RNG, scoring, or replay.

## Funding Surface Gate

**N/A** — this WP adds a URL-driven pre-fill of the Loadout tab; it introduces no global-nav / registry-viewer / profile funding affordance, no donate/support copy, and no funding-channel integration (§20.1 surfaces absent).

## Lint Gate Self-Review (00.3)

- §1–§21: PASS or N/A-with-reason. Highlights — §5 standard lane (decoder + composable + App wiring); §7 no new dependency (browser `atob`, explicit); §8 frontend boundary (viewer imports the `registry` package + `@legendary-arena/lagn` via WP-291; no engine/server import; no independent R2 fetch — the payload is self-contained); §11 N/A (no auth — self-contained payload, no server call); §12 tests present (`node:test`, no network/DB, injected draft api); §15.1 surface = `cards.legendary-arena.com` → DoD has the live-on-surface open-the-link check; §17 §10a/§18 addressed, determinism N/A; §20 N/A-with-reason; §21 N/A (no `apps/server` endpoint or library fn touched — viewer-only). §18 greps target identifiers + a purity absence check, not a count-echo.

## Pre-Flight / Copilot (drafter self-review, standard lane)

**Pre-flight (01.4): READY.** Hard-deps Done on `main` (WP-291 importer, WP-114 auto-switch). No blocker. Scope locked to ~2 code + 2 tests + one App.vue wiring, single app.

**Copilot (01.7): PASS.** Failure modes pinned: (a) forking the validator → **reuse `parseLagnLoadout` verbatim, decoder only produces text**; (b) a silent blank builder on a bad link → **fail-visible: switch to tab + full-sentence error banner**; (c) auto-switch re-firing → **one-shot `hasAppliedUrlAutoSwitch`**; (d) `?lagn=` vs `?schemeId=` ambiguity → **`?lagn=` precedence, documented**; (e) UTF-8 card names garbled by `atob` → **explicit UTF-8 decode step + round-trip test**; (f) pulling in a base64 npm dep → **browser `atob`, no dependency**. No BLOCK.

## Decision (reserved, lands at execution)

Reserves **D-24154**: the Registry Viewer `?lagn=` deep-link. Locks: (1) a `?lagn=<base64url(UTF-8 LAGN JSON)>` URL param; (2) on mount, decode → the **existing** `parseLagnLoadout` (no validator fork) → apply to the Loadout draft via the WP-291 setter sequence → one-shot auto-switch to the Loadout tab (WP-114 machinery); (3) **`?lagn=` takes precedence** over the WP-114 five-field setup params when both are present; (4) **fail-visible** — a malformed base64 or invalid LAGN still opens the tab and shows full-sentence errors, never a blank/partial builder; (5) **no server call, no auth, no CORS** — the viewer renders a self-contained payload (the arena client, not the viewer, holds the session and fetches the LAGN in WP-363). base64url decode uses the browser `atob` + a UTF-8 step; no new dependency. Drafted 2026-07-11; not yet landed.
